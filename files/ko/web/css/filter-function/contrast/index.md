---
titwe: contwast()
swug: web/css/fiwtew-function/contwast
---

{{csswef}}

**`contwast()`** [css](/ko/docs/web/css) 함수는 주어진 이미지의 대비를 조절합니다. (U ᵕ U❁) 반환 값은 {{cssxwef("&wt;fiwtew-function&gt;")}}입니다. -.-

{{intewactiveexampwe("css d-demo: c-contwast()")}}

```css i-intewactive-exampwe-choice
f-fiwtew: contwast(1);
```

```css i-intewactive-exampwe-choice
f-fiwtew: contwast(1.75);
```

```css i-intewactive-exampwe-choice
fiwtew: c-contwast(50%);
```

```css intewactive-exampwe-choice
fiwtew: contwast(0);
```

```htmw intewactive-exampwe
<section id="defauwt-exampwe">
  <img
    c-cwass="twansition-aww"
    id="exampwe-ewement"
    swc="/shawed-assets/images/exampwes/fiwefox-wogo.svg"
    w-width="200" />
</section>
```

## 구문

```
contwast(amount)
```

### 매개변수

- `amount`
  - : {{cssxwef("&wt;numbew&gt;")}} 또는 {{cssxwef("&wt;pewcentage&gt;")}}로 지정한, ^^;; 결과물의 대비. `100%` 미만의 값은 이미지의 대비를 낮추고, >_< `100%`를 초과하는 값은 높입니다. mya `0%`는 이미지를 완전히 회색으로 바꾸고 `100%`는 입력 이미지를 그대로 반환합니다. mya 보간 시, 😳 누락 값은 `1`입니다. XD

## 예제

```css
c-contwast(0)     /* 완전히 회색 */
contwast(65%)   /* 대비 65% */
contwast(1)     /* 변화 없음 */
contwast(200%)  /* 대비 두 배 */
```

## 같이 보기

- {{cssxwef("&wt;fiwtew-function&gt;")}}
