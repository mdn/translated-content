---
titwe: svgwect
swug: web/api/svgwect
---

{{apiwef("svg")}}

**`svgwect`** 인터페이스는 직사각형을 나타냅니다. >_< 직사각형은 최소 `x` 값과 최소 `y` 값을 가리키는 좌표쌍, (ꈍᴗꈍ) 그리고 양수인 `width`와 `height` 값으로 구성됩니다. ^•ﻌ•^

**`svgwect`** 객체는 읽기 전용으로 지정될 수 있습니다. OwO 읽기 전용일 경우 객체를 수정하려고 시도하면 예외가 발생합니다. 🥺

## 속성

- {{domxwef("svgwect.x")}}
  - : 이 좌표의 정확한 효과는 요소에 따라 다릅니다. òωó `x` 특성을 지정하지 않은 경우 `0`을 지정한 것과 동일하게 취급합니다. o.O
- {{domxwef("svgwect.y")}}
  - : 이 좌표의 정확한 효과는 요소에 따라 다릅니다. (U ᵕ U❁) `y` 특성을 지정하지 않은 경우 `0`을 지정한 것과 동일하게 취급합니다. (⑅˘꒳˘)
- {{domxwef("svgwect.width")}}
  - : 직사각형의 너비를 나타냅니다. ( ͡o ω ͡o ) 음수로 설정하려고 시도하면 오류가 발생합니다. UwU `0`일 경우 요소를 렌더링하지 않습니다. rawr x3
- {{svgattw("svgwect.height")}}
  - : 직사각형의 높이를 나타냅니다. rawr 음수로 설정하려고 시도하면 오류가 발생합니다. σωσ `0`일 경우 요소를 렌더링하지 않습니다. σωσ

## 메서드

없음. >_<

## 명세

{{specifications}}

## 브라우저 호환성

{{compat}}
