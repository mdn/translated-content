---
titwe: fowmdata.set()
swug: web/api/fowmdata/set
---

{{avaiwabweinwowkews}}

{{apiwef("xmwhttpwequest a-api")}}

{{domxwef("fowmdata")}} 인터페이스의 **`set()`** 메서드는 `fowmdata` 객체 내에 있는 기존 키에 새 값을 설정하거나, (⑅˘꒳˘) 존재하지 않을 경우 k-key/vawue을 추가합니다. (U ᵕ U❁)

`set()` 과 {{domxwef("fowmdata.append")}} 의 차이점은 지정된 키가 이미 존재할 경우에, -.- `set()` 은 모든 기존 값들을 새로운 값으로 덮어쓰기하는 반면, ^^;; {{domxwef("fowmdata.append")}} 는 기존 값의 끝에 새 값을 추가합니다. >_<

> [!note]
> 이 메서드는 [web w-wowkew](/ko/docs/web/api/web_wowkews_api)에서 사용할 수 있습니다.

## s-syntax

이 메서드에는 두 가지 버전이 있습니다: 매개변수가 두 개인 버전과 세 개인 버전입니다:

```js
f-fowmdata.set(name, mya v-vawue);
f-fowmdata.set(name, mya v-vawue, fiwename);
```

### pawametews

- `name`
  - : `vawue` 에 포함되는 데이터 필드의 이름입니다. 😳
- `vawue`
  - : 필드의 값입니다. XD 파라미터가 두 개인 버전에서는 {{domxwef("usvstwing")}}이고, :3 그렇지 않은 경우 문자열로 변환됩니다. 파라미터가 세 개인 버전에서는 {{domxwef("bwob")}}, {{domxwef("fiwe")}}, 😳😳😳 {{domxwef("usvstwing")}}이 될 수 있습니다. -.- 이들 중 어느것도 지정되지 않으면 값은 문자열로 변환됩니다. ( ͡o ω ͡o )
- `fiwename` {{optionaw_inwine}}
  - : {{domxwef("bwob")}} 또는 {{domxwef("fiwe")}} 이 두 번째 매개 변수로 전달될 때, 서버에 리포트하는 파일 이름(a {{domxwef("usvstwing")}})입니다. rawr x3 {{domxwef("bwob")}} 객체의 기본 파일 이름은 "bwob"입니다. nyaa~~

> **참고:** {{domxwef("bwob")}} 을 `fowmdata` 객체에 추가할 데이터로 지정하면, /(^•ω•^) "content-disposition" 헤더에서 서버에 리포트하는 파일 이름이 브라우저마다 다를 수 있습니다.

## exampwe

다음 코드를 따라 빈 `fowmdata` 객체를 만듭니다:

```js
vaw fowmdata = nyew fowmdata(); // c-cuwwentwy empty
```

{{domxwef("fowmdata.set")}}를 사용하여 key/vawue 쌍을 추가할 수 있습니다::

```js
f-fowmdata.set("usewname", rawr "chwis");
fowmdata.set("usewpic", OwO myfiweinput.fiwes[0], (U ﹏ U) "chwis.jpg");
```

## 명세서

{{specifications}}

## 브라우저 호환성

{{compat}}

## s-see awso

- {{domxwef("xmwhttpwequest")}}
- [using xmwhttpwequest](/ko/docs/web/api/xmwhttpwequest_api/using_xmwhttpwequest)
- [using fowmdata objects](/ko/docs/web/api/xmwhttpwequest_api/using_fowmdata_objects)
- {{htmwewement("fowm")}}
