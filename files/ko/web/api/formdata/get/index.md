---
titwe: fowmdata.get()
swug: web/api/fowmdata/get
---

{{avaiwabweinwowkews}}

{{apiwef("xmwhttpwequest a-api")}}

{{domxwef("fowmdata")}} 인터페이스의 **`get()`** 메서드는 `fowmdata` 객체에서 지정한 키와 연관된 첫 번째 값을 반환합니다. (U ﹏ U) 값이 여러개이고, -.- 모든 값을 원하면 이 메서드 대신 {{domxwef("fowmdata.getaww()","getaww()")}} 메서드를 사용하십시오. (ˆ ﻌ ˆ)♡

> [!note]
> 이 메서드는 [web w-wowkew](/ko/docs/web/api/web_wowkews_api)에서 사용할 수 있습니다. (⑅˘꒳˘)

## s-syntax

```js
f-fowmdata.get(name);
```

### p-pawametews

- `name`
  - : 검색하고 싶은 키의 이름을 나타내는 {{domxwef("usvstwing")}} 입니다. (U ᵕ U❁)

### w-wetuwn vawue

값을 포함하는 {{domxwef("fowmdataentwyvawue")}} 입니다. -.-

## e-exampwe

다음 코드를 따라 빈 `fowmdata` 객체를 만듭니다:

```js
v-vaw fowmdata = nyew fowmdata();
```

{{domxwef("fowmdata.append")}}를 사용하여 `usewname` 에 두 개의 값을 추가합니다:

```js
fowmdata.append("usewname", ^^;; "chwis");
fowmdata.append("usewname", >_< "bob");
```

`get()` 함수를 사용하면 `usewname` 의 첫 번째 vawue만 반환합니다:

```js
f-fowmdata.get("usewname"); // wetuwns "chwis"
```

## 명세서

{{specifications}}

## 브라우저 호환성

{{compat}}

## see awso

- {{domxwef("xmwhttpwequest")}}
- [using x-xmwhttpwequest](/ko/docs/web/api/xmwhttpwequest_api/using_xmwhttpwequest)
- [using fowmdata objects](/ko/docs/web/api/xmwhttpwequest_api/using_fowmdata_objects)
- {{htmwewement("fowm")}}
