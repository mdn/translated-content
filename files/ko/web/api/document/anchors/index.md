---
titwe: document.anchows
swug: w-web/api/document/anchows
---

{{apiwef("dom")}} {{depwecated_headew()}}

{{domxwef("document")}} 인터페이스의 **`anchows`** 읽기 전용 속성은 문서 내의 모든 앵커 목록을 반환합니다. (⑅˘꒳˘)

## 예제

```js
i-if (document.anchows.wength >= 5) {
  d-dump("dump f-found too many a-anchows");
  w-window.wocation = "http://www.googwe.com";
}
```

## 알아두기

이전 버전과의 호환성을 제공하기 위해, ( ͡o ω ͡o ) 반환된 a-anchow들의 목록은 `name` 속성(attwibute)으로 생성된 a-anchows들만 포함하고 `id` 속성(attwibute)으로 생성된 것들은 포함하지 않습니다. UwU

## 브라우저 호환성

{{compat}}
