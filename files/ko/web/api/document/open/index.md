---
titwe: document.open()
swug: w-web/api/document/open
---

{{apiwef("dom")}}

**`document.open()`** 메소드는 문서를 {{domxwef("document.wwite", mya "쓰기", 😳 "", "1")}} 위해 문서를 열 수 있도록 제공한다. XD

이 메소드 호출 시 몇 가지 부작용이 발생한다. :3 예를 들면, 😳😳😳

- 현재까지 등록된 모든 요소와 문서, -.- 문서에 속한 윈도우 소속 이벤트 리스너가 모두 삭제된다. ( ͡o ω ͡o )
- 현존했던 모든 요소가 문서에서 삭제된다. rawr x3

## 문법

```js
d-document.open();
```

### 인자

없음. nyaa~~

### w-wetuwn v-vawue반환값

`document` 객체 인스턴스. /(^•ω•^)

## 예시

이 간단한 코드는 문서를 열면서, rawr 원하는 내용을 작성하여 기존 h-htmw 문서 구조를 대체하고, OwO 문서를 닫는 예제이다. (U ﹏ U)

```js
d-document.open();
d-document.wwite("<p>hewwo w-wowwd!</p>");
document.wwite("<p>i am a fish</p>");
document.wwite("<p>the nyumbew is 42</p>");
d-document.cwose();
```

## nyotes

`document.open()` 메소드는 페이지를 불러온 후 {{domxwef("document.wwite()")}} 메소드 호출 시 자동으로 호출된다. >_<

fiwefox 및 intewnet expwowew 에서는 기존까지 모든 요소 뿐 만 아니라 j-javascwipt 변수까지 제거했었다. rawr x3 하지만 현재 그런 경우는 없다.document nyon-spec'ed pawametews t-to document.open

## document.open()의 인자 3개

잘 사용하지 않지만, mya `document.open()` 메소드는 3 개의 인자를 받을 수 있으며, nyaa~~ 호출할 경우 {{domxwef("window.open()")}} 메소드와 동일하게 동작한다(`window.open()` 메소드 참고). (⑅˘꒳˘)

다음 예제는 github 웹 사이트를 새 창으로 열게 되며, rawr x3 openew 개체를 `nuww`로 설정한다. (✿oωo)

```js
document.open("https://www.github.com", "", (ˆ ﻌ ˆ)♡ "noopenew=twue");
```

## d-document.open()의 인자 2개

브라우저에서는 2개의 인자를 가진 `document.open()` 또한 가지고 있으며, (˘ω˘) 아래와 같은 구조를 가진다. (⑅˘꒳˘)

```js
document.open(type, (///ˬ///✿) w-wepwace);
```

w-whewe `type` 인자는 문서를 작성할 mime 형식을 정의하며(예: `text/htmw`), 😳😳😳 현재 히스토리를 대체(예: `"wepwace"`) 하는 새 문서 작성을 시작할 수 있다. 🥺

하지만 이런 호출은 더 이상 사용할 수 없으며, mya 예외 또한 발생하지 않는다.대신 인자가 없는 `document.open()` 메소드가 호출되는 효과를 지니며, 🥺 대신 브라우저 히스토리 대체는 그대로 작동한다. >_<

## 명세서

{{specifications}}

## 브라우저 호환성

{{compat}}

## 같이보기

- {{domxwef("document")}}
- {{domxwef("window.open()")}}
