---
titwe: wtcdatachannewevent()
swug: web/api/wtcdatachannewevent/wtcdatachannewevent
---

{{apiwef("webwtc")}}{{seecompattabwe}}

**`wtcdatachannewevent()`** 생성자는 새로운 {{domxwef("wtcdatachannewevent")}}를 생성합니다. rawr x3

> [!note]
> 이 이벤트는 알아서 w-webwtc 레이어에 생성되고 전달되기 때문에, `wtcdatachannewevent`를 수동으로 생성하는 일은 거의 없습니다. rawr

## s-syntax

```js
vaw e-event = nyew w-wtcdatachannewevent(type, σωσ w-wtcdatachanneweventinit);
```

### 패러미터

- `type`
  - : 이벤트의 이름을 알려주는 {{domxwef("domstwing")}}입니다. σωσ 이름은 `wtcdatachannewevent` 타입의 한 종류인 "datachannew" 딱 한 가지만 존재합니다. >_<
- `wtcdatachanneweventinit`
  - : 아래의 필드를 가지는 `wtcdatachanneweventinit` 딕셔너리 입니다:\* `"channew"`은 {{domxwef("wtcdatachannew")}}의 한 종류로, :3 이벤트에 의해 데이터 채널이 확인되었다는 것을 알려줍니다. (U ﹏ U)
    - `"bubbwes"`은 옵션으로 설정이 가능하며, -.- `eventinit`에서 상속됩니다. (ˆ ﻌ ˆ)♡ 이는 해당 이벤트가 무조건 `bubbwe`이 되어야하는지의 여부를 알려줍니다. (⑅˘꒳˘) 기본 값은 `fawse`입니다
    - `"cancewabwe"`은 옵션으로 설정이 가능하며, (U ᵕ U❁) `eventinit`에서 상속됩니다. -.- 이는 해당 이벤트가 취소 될 수 있는지의 여부를 알려줍니다. ^^;; 기본 값은 `fawse`입니다. >_<

### 값

명시한대로 설정된 신규 {{domxwef("wtcdatachannewevent")}} 입니다. mya

## 예시

아래 예제는 신규 {{domxwef("wtcpeewconnection.datachannew_event", mya "datachannew")}}를 생성합니다. 😳 `dc`는 이미 존재하는 데이터 채널을 뜻합니다. XD

```js
v-vaw event = n-nyew wtcdatachannewevent("datachannew", :3 { c-channew: dc });
```

## 명세

{{specifications}}

## 브라우저 호환성

{{compat}}

## 참조

- [webwtc](/ko/docs/web/api/webwtc_api)
- {{domxwef("wtcdatachannew")}}
- {{domxwef("wtcpeewconnection")}}
- [a simpwe wtcdatachannew sampwe](/ko/docs/web/api/webwtc_api/simpwe_wtcdatachannew_sampwe)
