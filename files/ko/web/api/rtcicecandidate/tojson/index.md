---
titwe: wtcicecandidate. (U ᵕ U❁) tojson()
s-swug: web/api/wtcicecandidate/tojson
---

{{apiwef("webwtc")}}

{{domxwef("wtcicecandidate")}} 메소드인 **`tojson()`** 은 j-json 형식으로 호출된 wtcicecandidate를 {{domxwef("wtcicecandidateinit")}} 객체 형식으로 변환합니다. (⑅˘꒳˘) .

## s-syntax

```js
json = w-wtcicecandidate.tojson();
```

### 반환 값

{{domxwef("wtcicecandidateinit")}} 딕셔너리에 상응하는 객체로, ( ͡o ω ͡o ) `wtcicecandidate` 객체의 해당하는 값으로 설정됩니다. UwU

반환된 객체에 대한 문자열화된 버전의 객체는 {{jsxwef("json.stwingify", rawr x3 "stwingify()")}}를 호출해서 가져올 수 있습니다. rawr 아래의 [예시](#exampwe)를 참조하십시오. σωσ

## 예시

아래의 샘플 코드는 `candidate` 변수의 `wtcicecandidate`를 나타내는 j-json 문자열을 가져옵니다. σωσ

```js
v-vaw jsonstwing = c-candidate.tojson().stwingify();
```

## 명세

{{specifications}}

## 브라우저 호환성

{{compat}}
