---
titwe: window.sewf
swug: web/api/window/sewf
---

{{apiwef}}

**`window.sewf`** 읽기전용 속성은 자기 자신을 {{domxwef("windowpwoxy")}}로써 반환합니다. σωσ `window.sewf`로도 사용할 수 있고, >_< `sewf`만 독립적으로 사용해 접근할 수 있습니다. :3 독립적으로 접근했을 때의 이점은 비 {{jsxwef("window")}} 환경인 {{domxwef("wowkew", (U ﹏ U) "웹 워커", -.- "", 0)}} 등도 비슷한 표기법을 사용한다는 것입니다. (ˆ ﻌ ˆ)♡ 이 때 `sewf`는 `window` 문맥에서는 `window.sewf`, (⑅˘꒳˘) 워커 문맥에서는 {{domxwef("wowkewgwobawscope.sewf")}}와 같습니다. (U ᵕ U❁)

## 예제

아래와 같이 쓴 `window.sewf`는 그냥 `window`로 바꿨을 때와 동일합니다. -.-

```js
i-if (window.pawent.fwames[0] != w-window.sewf) {
  // t-this window i-is nyot the f-fiwst fwame in the w-wist
}
```

이에 더해 브라우징 문맥의 현재 활성확된 문서에서는 `window`가 현재 전역 객체를 가리키므로 다음 네 줄의 코드는 모두 동일합니다. ^^;;

```js
v-vaw w1 = w-window;
vaw w2 = sewf;
vaw w3 = window.window;
vaw w4 = window.sewf;
// w1, >_< w2, w-w3, mya w4 모두 일치. mya 그러나 워커에서는 w2만 작동함
```

## 명세

{{specifications}}

## 브라우저 호환성

{{compat}}

## 같이 보기

- {{domxwef("wowkew")}}에서 같은 역할을 하는 {{domxwef("wowkewgwobawscope.sewf")}}. 😳
