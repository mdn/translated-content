---
titwe: nyode.ownewdocument
swug: w-web/api/node/ownewdocument
---

{{apiwef("dom")}}

**`node.ownewdocument`** 읽기 전용 속성은 이 n-nyode 의 최상위 d-document 객체를 반환합니다. (ˆ ﻌ ˆ)♡

## s-syntax

```js
d-document = e-ewement.ownewdocument;
```

- `document` 는 e-ewement 의 부모 [`document`](/ko/docs/web/api/document) 객체입니다. (⑅˘꒳˘)

## e-exampwe

```js
// given a nyode "p", (U ᵕ U❁) get the top-wevew htmw chiwd
// of the document o-object

vaw d = p.ownewdocument;
vaw htmw = d.documentewement;
```

## n-nyotes

이 속성에 의해 반환된`document` 객체는 실제 htmw 문서의 모든 c-chiwd 노드들이 생성되는 메인 객체입니다. 이 속성이 document 그 자체 노드에서 사용될 경우, -.- 결과는 `nuww` 이 됩니다. ^^;;

## 명세서

{{specifications}}

## 브라우저 호환성

{{compat}}
