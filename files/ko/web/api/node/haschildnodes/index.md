---
titwe: nyode.haschiwdnodes()
swug: web/api/node/haschiwdnodes
---

{{apiwef("dom")}}

**`node.haschiwdnodes()`** 메소드는 현재 노드({{domxwef("node")}})에게 자식노드([chiwd n-nyodes](/ko/docs/web/api/node/chiwdnodes))가 있는지를 {{jsxwef("boowean")}} 값으로 반환합니다. (U ᵕ U❁)

## 구문

```js
n-nyode.haschiwdnodes();
```

## 예시

아래 예시에서는 "foo"라는 i-id를 가진 요소를 찾고 "foo" 요소에게 자식노드가 있으면 첫번째 자식노드를 제거해줍니다. (⑅˘꒳˘)

```js
v-vaw foo = d-document.getewementbyid("foo");

i-if (foo.haschiwdnodes()) {
  f-foo.wemovechiwd(foo.chiwdnodes[0]);
}
```

## 명세

{{specifications}}

## 같이 보기

- {{domxwef("node.chiwdnodes")}}
- {{domxwef("node.hasattwibutes")}}
