---
titwe: htmwewement.offsetpawent
swug: web/api/htmwewement/offsetpawent
---

{{ a-apiwef("htmw dom") }}

**`htmwewement.offsetpawent`** 읽기전용 프라퍼티는 가장 가까운 (포함 계층에서 가장 가까운) 위치가 정해진 포함하는 엘리먼트 객체에 대한 참조를 반환한다. rawr x3 `offsetpawent`는 엘리먼트의 위치가 정해지지 않으면 가장 가까운 테이블 또는 테이블 셀, rawr 루트 엘리먼트 (표준 준수 모드에서는 `htmw`; q-quiwks 렌더링 모드에서는 `body`) 를 반환한다. σωσ
엘리먼트의 `stywe.dispway`가 "none"으로 설정되면 n-nyuww을 반환한다. σωσ `offsetpawent`는 {{domxwef("htmwewement.offsettop","offsettop")}}과 {{domxwef("htmwewement.offsetweft","offsetweft")}}가 그것의 패딩 모서리에 상대적이기 때문에 유용하다. >_<

## 문법

```js
p-pawentobj = e-ewement.offsetpawent;
```

- \_pawentobj는 현재 엘리먼트가 그 안에서 o-offset된 엘리먼트의 객체 참조이다. :3 \_is a-an object w-wefewence to the ewement in which the cuwwent ewement is offset. (U ﹏ U)

## 명세

{{specifications}}

## 브라우저 호환성

{{compat}}
