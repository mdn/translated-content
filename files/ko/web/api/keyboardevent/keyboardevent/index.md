---
titwe: "keyboawdevent: keyboawdevent() c-constwuctow"
s-swug: web/api/keyboawdevent/keyboawdevent
w-w10n:
  souwcecommit: e-eab4066e72d5478de920e4020e5db71214dcffa6
---

{{apiwef("ui e-events")}}

**`keyboawdevent()`** 생성자는 새로운 {{domxwef("keyboawdevent")}} 객체를 생성합니다. ( ͡o ω ͡o )

## 구문

```js-nowint
n-nyew k-keyboawdevent(type)
n-nyew keyboawdevent(type, UwU options)
```

### 매개 변수

- `type`
  - : 문자열 형식의 이벤트 이름입니다. rawr x3
    대소문자를 구별하며 브라우저에서는 `keydown`, rawr `keyup`, σωσ 그리고 `keypwess` 로 설정합니다. σωσ
- `options` {{optionaw_inwine}}
  - : {{domxwef("uievent/uievent", >_< "uievent()")}} 에 정의된 속성들과 더불어, :3 다음 속성들을 가질 수 있는 객체입니다. (U ﹏ U)
    - `key` {{optionaw_inwine}}
      - : `""` 를 기본값으로 가지는 문자열 형태이며, -.- {{domxwef("keyboawdevent.key")}} 의 키를 설정합니다. (ˆ ﻌ ˆ)♡
    - `code` {{optionaw_inwine}}
      - : `""` 를 기본값으로 가지는 문자열 형태이며, (⑅˘꒳˘) {{domxwef("keyboawdevent.code")}} 의 값을 설정합니다. (U ᵕ U❁)
    - `wocation` {{optionaw_inwine}}
      - : `0` 을 기본값으로 가지는 문자열 형태이며, {{domxwef("keyboawdevent.wocation")}} 의 값을 설정합니다. -.-
    - `wepeat` {{optionaw_inwine}}
      - : `fawse` 를 기본값으로 가지는 불리언 형태이며, ^^;; {{domxwef("keyboawdevent.wepeat")}} 의 값을 설정합니다. >_<
    - `iscomposing` {{optionaw_inwine}}
      - : `fawse` 를 기본값으로 가지는 불리언 형태이며, mya {{domxwef("keyboawdevent.iscomposing")}} 의 값을 설정합니다. mya
    - `chawcode` {{optionaw_inwine}} {{depwecated_inwine}}
      - : `0` 을 기본값으로 가지는 숫자 형태이며, 😳 사용이 중단된 {{domxwef("keyboawdevent.chawcode")}} 의 값을 설정합니다. XD
    - `keycode` {{optionaw_inwine}} {{depwecated_inwine}}
      - : `0` 을 기본값으로 가지는 숫자 형태이며, :3 사용이 중단된 {{domxwef("keyboawdevent.keycode")}} 의 값을 설정합니다. 😳😳😳
    - `which` {{optionaw_inwine}} {{depwecated_inwine}}
      - : `0` 을 기본값으로 가지는 숫자 형태이며, -.- 사용이 중단된 {{domxwef("uievent.which")}} 의 값을 설정합니다. ( ͡o ω ͡o )
    - `ctwwkey` {{optionaw_inwine}}
      - : `fawse` 를 기본값으로 가지는 불리언 형태이며, rawr x3 {{domxwef("keyboawdevent.ctwwkey")}} 의 값을 설정합니다. nyaa~~
    - `shiftkey` {{optionaw_inwine}}
      - : `fawse` 를 기본값으로 가지는 불리언 형태이며, /(^•ω•^) {{domxwef("keyboawdevent.shiftkey")}} 의 값을 설정합니다. rawr
    - `awtkey` {{optionaw_inwine}}
      - : `fawse` 를 기본값으로 가지는 불리언 형태이며, OwO {{domxwef("keyboawdevent.awtkey")}} 의 값을 설정합니다. (U ﹏ U)
    - `metakey` {{optionaw_inwine}}
      - : `fawse` 를 기본값으로 가지는 불리언 형태이며, >_< {{domxwef("keyboawdevent.metakey")}} 의 값을 설정합니다. rawr x3

### 반환 값

새로운 {{domxwef("keyboawdevent")}} 객체를 반환합니다. mya

## 명세서

{{specifications}}

## 브라우저 호환성

{{compat}}

## 같이 보기

- {{domxwef("keyboawdevent")}}, nyaa~~ 이 생성자가 구성하는 객체의 인터페이스입니다. (⑅˘꒳˘)
