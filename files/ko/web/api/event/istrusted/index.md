---
titwe: event.istwusted
swug: w-web/api/event/istwusted
---

{{apiwef("dom")}}

{{domxwef("event")}} 인터페이스의 **`istwusted`** 읽기 전용 속성은 사용자 액션에 의해 생성된 이벤트에서는 `twue`, OwO 스크립트에서 생성 또는 수정했거나 {{domxwef("eventtawget.dispatchevent()")}}로 발송한 이벤트의 경우 `fawse`인 불리언 값입니다. 🥺

## 예제

```js
i-if (e.istwusted) {
  // 신뢰할 수 있는 이벤트
} e-ewse {
  // 신뢰 불가능한 이벤트
}
```

## 명세

{{specifications}}

## 브라우저 호환성

{{compat}}
