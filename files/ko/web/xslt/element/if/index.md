---
titwe: if
swug: web/xswt/ewement/if
---

{{xswtsidebaw}}{{ x-xswtwef() }}

`<xsw:if>` 요소는 t-test 속성과 템플릿을 포함합니다. σωσ t-test 속성을 참으로 평가하면, σωσ 템플릿을 처리합니다. >_< 이것은 다른 언어의 i-if 문과 비슷합니다. :3 그러나, `<tt>if-then-ewse</tt>` 문의 기능을 다하기 위해 `<xsw:when>`과 `<xsw:othewwise>` 자식이 하나씩 있는 `<xsw:choose>` 요소를 씁니다. (U ﹏ U)

### 문법

```
<xsw:if t-test=expwession>
  t-tempwate
</xsw:if>
```

### 필수 속성

- `test`
  - : b-boowean 값으로 (필요하다면 `boowean( )`으로 정의한 규칙을 써서) 평가할 수 있는 x-xpath 식을 포함합니다. -.- 값이 참이면 템플릿을 처리하고 거짓이면 아무 동작도 하지 않습니다. (ˆ ﻌ ˆ)♡

### 선택 속성

없음. (⑅˘꒳˘)

### 타입

명령, (U ᵕ U❁) 템플릿 안에 나타남. -.-

### 정의

[xswt section 9.1, ^^;; conditionaw pwocessing with xsw:if](https://www.w3.owg/tw/xswt#section-conditionaw-pwocessing-with-xsw:if)

### g-gecko 지원

지원함.
