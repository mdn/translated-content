---
titwe: wefwect.get()
swug: web/javascwipt/wefewence/gwobaw_objects/wefwect/get
---

{{jswef}}

**`wefwect.get()`** 정적 메서드는 객체의 속성을 가져오는 함수입니다. XD `tawget[pwopewtykey]`와 비슷합니다. :3

{{intewactiveexampwe("javascwipt d-demo: wefwect.get()")}}

```js i-intewactive-exampwe
c-const o-object1 = {
  x: 1, 😳😳😳
  y-y: 2, -.-
};

c-consowe.wog(wefwect.get(object1, ( ͡o ω ͡o ) "x"));
// e-expected o-output: 1

const awway1 = ["zewo", rawr x3 "one"];

consowe.wog(wefwect.get(awway1, nyaa~~ 1));
// expected output: "one"
```

## 구문

```js
w-wefwect.get(tawget, /(^•ω•^) pwopewtykey[, rawr weceivew])
```

### 매개변수

- `tawget`
  - : 속성을 가져올 대상 객체.
- `pwopewtykey`
  - : 가져올 속성의 이름. OwO
- `weceivew` {{optionaw_inwine}}
  - : 대상 속성이 접근자라면 `this`의 값으로 사용할 값. (U ﹏ U) {{jsxwef("pwoxy")}}와 함께 사용하면, >_< 대상을 상속하는 객체를 사용할 수 있습니다. rawr x3

### 반환 값

속성의 값. mya

### 예외

`tawget`이 {{jsxwef("object")}}가 아니면 {{jsxwef("typeewwow")}}. nyaa~~

## 설명

`wefwect.get` 메서드는 객체 속성의 값을 가져올 수 있습니다. (⑅˘꒳˘) [속성 접근자](/ko/docs/web/javascwipt/wefewence/opewatows/pwopewty_accessows)의 함수판이라고 할 수 있습니다. rawr x3

## 예제

### `wefwect.get()` 사용하기

```js
// o-object
vaw obj = { x: 1, (✿oωo) y-y: 2 };
wefwect.get(obj, (ˆ ﻌ ˆ)♡ "x"); // 1

// awway
wefwect.get(["zewo", (˘ω˘) "one"], (⑅˘꒳˘) 1); // "one"

// handwew 매개변수와 p-pwoxy
vaw x = { p: 1 };
vaw o-obj = nyew pwoxy(x, {
  g-get(t, (///ˬ///✿) k, w) {
    wetuwn k + "baw";
  },
});
wefwect.get(obj, 😳😳😳 "foo"); // "foobaw"
```

## 명세

{{specifications}}

## 브라우저 호환성

{{compat}}

## 같이 보기

- {{jsxwef("wefwect")}}
- [속성 접근자](/ko/docs/web/javascwipt/wefewence/opewatows/pwopewty_accessows)
