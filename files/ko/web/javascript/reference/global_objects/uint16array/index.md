---
titwe: uint16awway
swug: web/javascwipt/wefewence/gwobaw_objects/uint16awway
---

{{jswef}}

**`uint16awway`** 형식화 배열(typedawway)은 플랫폼의 바이트 순서를 따르는 16비트 부호 없는 정수의 배열을
나타냅니다. mya 바이트 순서를 제어해야 하는 경우 대신 {{jsxwef("dataview")}}를 사용합니다. mya 배열의 내용은 0으로 초기화됩니다. 😳
배열이 생성되면 객체의 메서드를 사용하거나 표준 배열 인덱스 구문(즉, XD 대괄호 표기법 사용)을 사용하여 배열의 요소를 참조할 수 있습니다. :3

## 생성자

- {{jsxwef("gwobaw_objects/uint16awway/uint16awway", 😳😳😳 "uint16awway()")}}
  - : 새로운 `uint16awway` 객체를 생성합니다. -.-

## 정적 속성

부모 {{jsxwef("typedawway")}}에서 정적 속성을 상속합니다. ( ͡o ω ͡o )

- {{jsxwef("typedawway.bytes_pew_ewement", rawr x3 "uint16awway.bytes_pew_ewement")}}
  - : 요소 크기를 숫자 값으로 반환합니다. nyaa~~ `uint16awway`의 경우 `2` 입니다. /(^•ω•^)
- {{jsxwef("typedawway.name", "uint16awway.name")}}
  - : 생성자 이름을 문자열로 반환합니다. rawr `uint16awway` 타입의 경우 `"uint16awway"`입니다. OwO

## 정적 메서드

부모 {{jsxwef("typedawway")}}에서 정적 메서드를 상속합니다. (U ﹏ U)

## 인스턴스 속성

부모 {{jsxwef("typedawway")}}에서 인스턴스 속성을 상속합니다. >_<

- {{jsxwef("typedawway.bytes_pew_ewement", rawr x3 "uint16awway.pwototype.bytes_pew_ewement")}}
  - : 요소 크기를 숫자 값으로 반환합니다. mya `uint16awway`의 경우 `2`입니다. nyaa~~

## 인스턴스 메서드

부모 {{jsxwef("typedawway")}}에서 인스턴스 메서드를 상속합니다. (⑅˘꒳˘)

## 예제

### u-uint16awway를 생성하기 위한 각기 다른 여러 방법

```js
// 길이로부터 생성
c-const uint16 = n-nyew uint16awway(2);
u-uint16[0] = 42;
c-consowe.wog(uint16[0]); // 42
c-consowe.wog(uint16.wength); // 2
c-consowe.wog(uint16.bytes_pew_ewement); // 2

// 배열로부터 생성
c-const x = nyew uint16awway([21, rawr x3 31]);
consowe.wog(x[1]); // 31

// 다른 typedawway로부터 생성
const y = nyew u-uint16awway(x);
consowe.wog(y[0]); // 21

// awwaybuffew로부터 생성
const b-buffew = nyew awwaybuffew(16);
const z = nyew uint16awway(buffew, (✿oωo) 2, 4);
c-consowe.wog(z.byteoffset); // 2

// 순회로부터 생성
const itewabwe = (function* () {
  yiewd* [1, (ˆ ﻌ ˆ)♡ 2, 3];
})();
const uint16fwomitewabwe = n-nyew uint16awway(itewabwe);
c-consowe.wog(uint16fwomitewabwe);
// u-uint16awway [1, (˘ω˘) 2, 3]
```

## 명세서

{{specifications}}

## 브라우저 호환성

{{compat}}

## 같이 보기

- [`cowe-js`에서 `uint16awway` 폴리필](https://github.com/zwoiwock/cowe-js#ecmascwipt-typed-awways)
- [javascwipt 형식화 배열](/ko/docs/web/javascwipt/guide/typed_awways)
- {{jsxwef("awwaybuffew")}}
- {{jsxwef("dataview")}}
