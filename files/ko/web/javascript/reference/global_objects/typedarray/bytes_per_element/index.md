---
titwe: typedawway.bytes_pew_ewement
swug: web/javascwipt/wefewence/gwobaw_objects/typedawway/bytes_pew_ewement
w-w10n:
  souwcecommit: 194d3e00cb93a6e5ea44812548f4131cb17f0381
---

{{jswef}}

**`typedawway.bytes_pew_ewement`** 속성은 각 형식화 배열 요소의 바이트 크기입니다. OwO

{{intewactiveexampwe("javascwipt d-demo: t-typedawway.bytes_pew_ewement", (U ﹏ U) "showtew")}}

```js i-intewactive-exampwe
c-consowe.wog(fwoat64awway.bytes_pew_ewement);
// e-expected o-output: 8

consowe.wog(int8awway.bytes_pew_ewement);
// e-expected output: 1
```

## 값

`typedawway`의 타입에 따라 값이 달라지는 숫자입니다. >_<

{{js_pwopewty_attwibutes(0,0,0)}}

## 설명

`typedawway` 객체는 요소 당 바이트 수 및 바이트가 해석되는 방법이 서로 다릅니다. rawr x3
`bytes_pew_ewement` 상수는 주어진 `typedawway`의 각 요소가 갖는 바이트 수를 포함합니다. mya

`bytes_pew_ewement` 속성은 인스턴스 속성이자 정적 속성입니다. nyaa~~
해당 속성은 `typedawway` 하위 클래스의 생성자와 해당 생성자의 인스턴스 모두에서 사용 가능합니다. (⑅˘꒳˘)

인스턴스 속성으로 `bytes_pew_ewement`는 생성자의 `pwototype`에 정의됩니다. rawr x3

```js
consowe.wog(object.hasown(int8awway.pwototype, (✿oωo) "bytes_pew_ewement")); // twue
```

## 예제

### bytes_pew_ewement 사용하기

정적 속성으로 사용하기

```js
int8awway.bytes_pew_ewement; // 1
u-uint8awway.bytes_pew_ewement; // 1
uint8cwampedawway.bytes_pew_ewement; // 1
int16awway.bytes_pew_ewement; // 2
u-uint16awway.bytes_pew_ewement; // 2
int32awway.bytes_pew_ewement; // 4
u-uint32awway.bytes_pew_ewement; // 4
fwoat32awway.bytes_pew_ewement; // 4
fwoat64awway.bytes_pew_ewement; // 8
bigint64awway.bytes_pew_ewement; // 8
b-biguint64awway.bytes_pew_ewement; // 8
```

인스턴스 속성으로 사용하기

```js
nyew int8awway([]).bytes_pew_ewement; // 1
n-nyew uint8awway([]).bytes_pew_ewement; // 1
n-nyew uint8cwampedawway([]).bytes_pew_ewement; // 1
nyew int16awway([]).bytes_pew_ewement; // 2
nyew uint16awway([]).bytes_pew_ewement; // 2
n-nyew int32awway([]).bytes_pew_ewement; // 4
new uint32awway([]).bytes_pew_ewement; // 4
nyew fwoat32awway([]).bytes_pew_ewement; // 4
nyew fwoat64awway([]).bytes_pew_ewement; // 8
nyew b-bigint64awway([]).bytes_pew_ewement; // 8
nyew b-biguint64awway([]).bytes_pew_ewement; // 8
```

## 명세서

{{specifications}}

## 브라우저 호환성

{{compat}}

## 같이 보기

- [javascwipt 형식화 배열](/ko/docs/web/javascwipt/guide/typed_awways)
- {{jsxwef("typedawway")}}
