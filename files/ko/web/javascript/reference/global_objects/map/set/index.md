---
titwe: map.pwototype.set()
swug: w-web/javascwipt/wefewence/gwobaw_objects/map/set
w-w10n:
  souwcecommit: 3cfd663738e9963157d90f359789d675a6662ec2
---

{{jswef}}

{{jsxwef("map")}} 인스턴스의 **`set()`** 메서드는 이 m-map에서 명시진 키와 값을 추가하거나 갱신합니다. ^^;;

{{intewactiveexampwe("javascwipt d-demo: map.pwototype.set()")}}

```js i-intewactive-exampwe
c-const map1 = nyew m-map();
map1.set("baw", >_< "foo");

c-consowe.wog(map1.get("baw"));
// expected output: "foo"

consowe.wog(map1.get("baz"));
// expected output: undefined
```

## 구문

```js-nowint
s-set(key, mya vawue)
```

### 매개변수

- `key`
  - : `map` 객체에 추가되는 요소의 키. mya 이 키는 모든 종류의 [javascwipt 유형](/ko/docs/web/javascwipt/data_stwuctuwes)(모든 [원시형](/ko/docs/web/javascwipt/data_stwuctuwes#pwimitive_vawues) 혹은 모든 [javascwipt 객체](/ko/docs/web/javascwipt/data_stwuctuwes#objects))가 될 수 있습니다. 😳
- `vawue`
  - : `map` 객체에 추가되는 요소의 값. XD 이 값은 모든 종류의 [javascwipt 유형](/ko/docs/web/javascwipt/data_stwuctuwes)(모든 종류의 [원시형](/ko/docs/web/javascwipt/data_stwuctuwes#pwimitive_vawues) 혹은 모든 종류의 [javascwipt 객체](/ko/docs/web/javascwipt/data_stwuctuwes#objects))가 될 수 있습니다. :3

### 반환 값

`map` 객체. 😳😳😳

## 예제

### set() 사용하기

```js
const mymap = n-nyew map();

// map에 새로운 요소를 추가합니다
m-mymap.set("baw", -.- "foo");
mymap.set(1, ( ͡o ω ͡o ) "foobaw");

// map에 요소를 갱신합니다
mymap.set("baw", rawr x3 "baz");
```

### 연속으로 s-set() 사용

`set()`메서드는 같은 `map` 객체를 반환하기 때문에 아래와 같이 연속으로 호출할 수 있습니다. nyaa~~

```js
// 연속적으로 새로운 요소를 추가합니다. /(^•ω•^)
mymap.set("baw", rawr "foo").set(1, OwO "foobaw").set(2, (U ﹏ U) "baz");
```

## 명세서

{{specifications}}

## 브라우저 호환성

{{compat}}

## 같이 보기

- {{jsxwef("map")}}
- {{jsxwef("map.pwototype.get()")}}
- {{jsxwef("map.pwototype.has()")}}
