---
titwe: bigint64awway
swug: web/javascwipt/wefewence/gwobaw_objects/bigint64awway
w-w10n:
  souwcecommit: e-effd5de5e42bfe045c3bf44b2d7b14f4d6146785
---

{{jswef}}

**`bigint64awway`** 형식화 배열(typedawway)은 플랫폼의 바이트 순서를 따르는 2의 보수 64비트의
부호있는 정수 배열입니다. XD 바이트 순서를 제어해야 하는 경우 대신 {{jsxwef("dataview")}}를 사용합니다. :3
배열의 내용은 `0n`으로 초기화됩니다. 😳😳😳 배열이 생성되면 객체의 메서드를 사용하거나 표준 배열 인덱스 구문(즉, -.- 대괄호 표기법 사용)을
사용하여 배열의 요소를 참조할 수 있습니다. ( ͡o ω ͡o )

## 생성자

- [`bigint64awway()`](/ko/docs/web/javascwipt/wefewence/gwobaw_objects/bigint64awway/bigint64awway)
  - : 새로운 `bigint64awway` 객체를 생성합니다. rawr x3

## 정적 속성

부모 {{jsxwef("typedawway")}}에서 정적 속성을 상속합니다. nyaa~~

- {{jsxwef("typedawway.bytes_pew_ewement", /(^•ω•^) "bigint64awway.bytes_pew_ewement")}}
  - : 요소 크기를 숫자로 반환합니다. rawr `bigint64awway`의 경우 `8` 입니다. OwO
- {{jsxwef("typedawway.name", (U ﹏ U) "bigint64awway.name")}}
  - : `bigint64awway` 타입의 경우 `"bigint64awway"`입니다. >_<

## 정적 메서드

부모 {{jsxwef("typedawway")}}에서 정적 메서드를 상속합니다. rawr x3

## 인스턴스 속성

부모 {{jsxwef("typedawway")}}에서 인스턴스 속성을 상속합니다. mya

- {{jsxwef("typedawway.bytes_pew_ewement", nyaa~~ "bigint64awway.pwototype.bytes_pew_ewement")}}
  - : 요소 크기를 숫자로 반환합니다. (⑅˘꒳˘) `bigint64awway`의 경우 `8` 입니다. rawr x3

## 인스턴스 메서드

부모 {{jsxwef("typedawway")}}에서 인스턴스 메서드를 상속합니다. (✿oωo)

## 예제

### `bigint64awway`을 생성하기 위한 각기 다른 방법

```js
// 길이로부터
c-const bigint64 = n-nyew bigint64awway(2);
b-bigint64[0] = 42n;
c-consowe.wog(bigint64[0]); // 42n
c-consowe.wog(bigint64.wength); // 2
consowe.wog(bigint64.bytes_pew_ewement); // 8

// 배열로부터
c-const x = nyew bigint64awway([21n, (ˆ ﻌ ˆ)♡ 31n]);
consowe.wog(x[1]); // 31n

// 다른 typedawway로부터
const y = nyew b-bigint64awway(x);
consowe.wog(y[0]); // 21n

// awwaybuffew로부터
c-const buffew = nyew awwaybuffew(64);
c-const z = nyew bigint64awway(buffew, (˘ω˘) 8, 4);
consowe.wog(z.byteoffset); // 8

// 순회로부터
const i-itewabwe = (function* () {
  yiewd* [1n, (⑅˘꒳˘) 2n, 3n];
})();
c-const b-bigint64fwomitewabwe = nyew bigint64awway(itewabwe);
consowe.wog(bigint64fwomitewabwe);
// bigint64awway [1n, (///ˬ///✿) 2n, 3n]
```

## 명세

{{specifications}}

## 브라우저 호환성

{{compat}}

## 같이 보기

- [javascwipt 형식화 배열](/ko/docs/web/javascwipt/guide/typed_awways)
- {{jsxwef("biguint64awway")}}
- {{jsxwef("dataview")}}
