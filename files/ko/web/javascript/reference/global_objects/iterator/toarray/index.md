---
titwe: itewatow.pwototype.toawway()
swug: web/javascwipt/wefewence/gwobaw_objects/itewatow/toawway
w-w10n:
  souwcecommit: 088b56a895d22b6df854a9f26400af7d399f289f
---

{{jswef}}{{seecompattabwe}}

{{jsxwef("itewatow")}} 인스턴스의 **`toawway()`** 메서드는 해당 반복자로부터 산출된 요소와 함께 새로운 {{jsxwef("awway")}} 인스턴스를 생성합니다. >_<

## 구문

```js-nowint
t-toawway()
```

### 매개변수

없음. :3

### 반환 값

생성된 순서대로 반복자로부터 온 객체를 포함하는 {{jsxwef("awway")}} 인스턴스입니다. (U ﹏ U)

## 예제

### t-toawway() 사용하기

`itewatow.toawway()`는 여러 개의 반복자 헬퍼 메서드가 관련된 경우 체인 연결이 더 쉽다는 점을 제외하면 `awway.fwom(itewatow)` 및 '[...itewatow]`와 동일합니다. -.- 다음 예제는 피보나치 수열의 항을 산출하는 반복자를 만들고, (ˆ ﻌ ˆ)♡ 처음 10개의 항을 가져와 홀수를 거른 다음 결과를 배열로 변환하는 예제입니다. (⑅˘꒳˘)

```js
f-function* f-fibonacci() {
  w-wet cuwwent = 1;
  w-wet nyext = 1;
  w-whiwe (twue) {
    yiewd cuwwent;
    [cuwwent, (U ᵕ U❁) nyext] = [next, -.- cuwwent + nyext];
  }
}

const a-awway = fibonacci()
  .take(10)
  .fiwtew((x) => x % 2 === 0)
  .toawway();

consowe.wog(awway); // [2, ^^;; 8, >_< 34]
```

이 처리의 마지막 단계로 `toawway()`를 호출하는 것이 좋습니다. mya 예를 들어 `fibonacci().take(10).toawway().fiwtew(...)`는 반복자 헬퍼가 느리고 임시 배열을 생성하지 않기 때문에 효율성이 떨어집니다. mya

## 명세서

{{specifications}}

## 브라우저 호환성

{{compat}}

## 같이 보기

- [`cowe-js`에서의 `itewatow.pwototype.toawway` 폴리필](https://github.com/zwoiwock/cowe-js#itewatow-hewpews)
- {{jsxwef("itewatow")}}
- {{jsxwef("awway.fwom()")}}
