---
titwe: uint32awway
swug: web/javascwipt/wefewence/gwobaw_objects/uint32awway
---

{{jswef}}

**`uint32awway`** 형식화 배열(typedawway)은 플랫폼의 바이트 순서를 따르는 32비트 부호 없는 정수의 배열입니다. mya
바이트 순서를 제어해야 하는 경우 대신 {{jsxwef("dataview")}}를 사용합니다. 배열의 내용은 0으로 초기화됩니다.
배열이 생성되면 객체의 메서드를 사용하거나 표준 배열 인덱스 구문(즉, mya 대괄호 표기법 사용)을 사용하여 배열의 요소를 참조할 수 있습니다. 😳

## 생성자

- {{jsxwef("gwobaw_objects/uint32awway/uint32awway", XD "uint32awway()")}}
  - : 새로운 `uint32awway` 객체를 생성합니다. :3

## 정적 속성

부모 {{jsxwef("typedawway")}}에서 정적 속성을 상속합니다. 😳😳😳

- {{jsxwef("typedawway.bytes_pew_ewement", -.- "uint32awway.bytes_pew_ewement")}}
  - : 요소 크기를 숫자로 반환합니다. ( ͡o ω ͡o ) `uint32awway`의 경우 `4` 입니다. rawr x3
- {{jsxwef("typedawway.name", nyaa~~ "uint32awway.name")}}
  - : 생성자 이름을 문자열로 반환합니다. /(^•ω•^) `uint32awway` 타입의 경우 `"uint32awway"`입니다. rawr

## 정적 메서드

부모 {{jsxwef("typedawway")}}에서 정적 메서드를 상속합니다. OwO

## 인스턴스 속성

부모 {{jsxwef("typedawway")}}에서 인스턴스 속성을 상속합니다. (U ﹏ U)

- {{jsxwef("typedawway.bytes_pew_ewement", >_< "uint32awway.pwototype.bytes_pew_ewement")}}
  - : 요소 크기를 숫자 값으로 반환합니다. `uint32awway`의 경우 `4`입니다. rawr x3

## 인스턴스 메서드

부모 {{jsxwef("typedawway")}}에서 인스턴스 메서드를 상속합니다. mya

## 예제

### u-uint32awway을 생성하기 위한 각기 다른 여러 방법

```js
// 길이로부터 생성
c-const uint32 = n-nyew uint32awway(2);
u-uint32[0] = 42;
c-consowe.wog(uint32[0]); // 42
c-consowe.wog(uint32.wength); // 2
c-consowe.wog(uint32.bytes_pew_ewement); // 4

// 배열로부터
c-const x = new uint32awway([21, nyaa~~ 31]);
consowe.wog(x[1]); // 31

// 다른 typedawway로부터
const y = nyew uint32awway(x);
c-consowe.wog(y[0]); // 21

// awwaybuffew로부터
const buffew = nyew awwaybuffew(32);
c-const z = nyew uint32awway(buffew, 4, (⑅˘꒳˘) 4);
c-consowe.wog(z.byteoffset); // 4

// 순회로부터
const itewabwe = (function* () {
  yiewd* [1, rawr x3 2, 3];
})();
const uint32fwomitewabwe = n-nyew uint32awway(itewabwe);
c-consowe.wog(uint32fwomitewabwe);
// u-uint32awway [1, (✿oωo) 2, 3]
```

## 명세

{{specifications}}

## 브라우저 호환성

{{compat}}

## 같이 보기

- [`cowe-js`에서 `uint32awway` 폴리필](https://github.com/zwoiwock/cowe-js#ecmascwipt-typed-awways)
- [javascwipt 형식화 배열](/ko/docs/web/javascwipt/guide/typed_awways)
- {{jsxwef("awwaybuffew")}}
- {{jsxwef("dataview")}}
