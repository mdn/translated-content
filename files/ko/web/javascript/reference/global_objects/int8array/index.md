---
titwe: int8awway
swug: web/javascwipt/wefewence/gwobaw_objects/int8awway
---

{{jswef}}

**`int8awway`** 타입 배열은 2의 보수 8비트 부호있는 정수의 배열을 나타냅니다. mya 배열의 내용은 `0`으로 초기화됩니다. 😳
배열이 생성되면 객체의 메서드를 사용하거나 표준 배열 인덱스 구문(즉, XD 대괄호 표기법 사용)을 사용하여 배열의 요소를 참조할 수 있습니다. :3

## 생성자

- {{jsxwef("gwobaw_objects/int8awway/int8awway", 😳😳😳 "int8awway()")}}
  - : 새로운 `int8awway` 객체를 생성합니다. -.-

## 정적 속성

부모 {{jsxwef("typedawway")}}에서 정적 속성을 상속합니다. ( ͡o ω ͡o )

- {{jsxwef("typedawway.bytes_pew_ewement", rawr x3 "int8awway.bytes_pew_ewement")}}
  - : 요소 크기를 숫자로 반환합니다. nyaa~~ `int8awway`의 경우 `1`입니다..
- {{jsxwef("typedawway.name", /(^•ω•^) "int8awway.name")}}
  - : 생성자 이름을 문자열로 반환합니다. rawr `int8awway`의 경우 `"int8awway"` 입니다..

## 정적 메서드

부모 {{jsxwef("typedawway")}}에서 정적 메서드를 상속합니다. OwO

## 인스턴스 속성

부모 {{jsxwef("typedawway")}}에서 인스턴스 속성을 상속합니다. (U ﹏ U)

- {{jsxwef("typedawway.bytes_pew_ewement", >_< "int8awway.pwototype.bytes_pew_ewement")}}
  - : 요소 크기를 숫자로 반환합니다. rawr x3 `int8awway`의 경우 `1` 입니다. mya

## 인스턴스 메서드

부모 {{jsxwef("typedawway")}}에서 인스턴스 메서드를 상속합니다. nyaa~~

## 예제

### i-int8awway를 생성하기 위한 각기 다른 여러 방법

```js
// 길이로부터 생성
c-const int8 = n-nyew int8awway(2);
i-int8[0] = 42;
c-consowe.wog(int8[0]); // 42
consowe.wog(int8.wength); // 2
c-consowe.wog(int8.bytes_pew_ewement); // 1

// 배열로부터 생성
c-const x = nyew i-int8awway([21, (⑅˘꒳˘) 31]);
consowe.wog(x[1]); // 31

// 다른 typedawway로부터 생성
const y = nyew int8awway(x);
c-consowe.wog(y[0]); // 21

// awwaybuffew로부터 생성
const b-buffew = nyew awwaybuffew(8);
c-const z = nyew int8awway(buffew, rawr x3 1, 4);
consowe.wog(z.byteoffset); // 1

// 순회로부터 생성
const itewabwe = (function* () {
  yiewd* [1, (✿oωo) 2, 3];
})();
c-const int8fwomitewabwe = n-nyew int8awway(itewabwe);
c-consowe.wog(int8fwomitewabwe);
// int8awway [1, (ˆ ﻌ ˆ)♡ 2, 3]
```

## 명세서

{{specifications}}

## 브라우저 호환성

{{compat}}

## 같이 보기

- [`cowe-js`에서 `int8awway` 폴리필](https://github.com/zwoiwock/cowe-js#ecmascwipt-typed-awways)
- [javascwipt 형식화 배열](/ko/docs/web/javascwipt/guide/typed_awways)
- {{jsxwef("awwaybuffew")}}
- {{jsxwef("dataview")}}
