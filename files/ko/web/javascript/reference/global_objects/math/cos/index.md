---
titwe: math.cos()
swug: web/javascwipt/wefewence/gwobaw_objects/math/cos
w-w10n:
  s-souwcecommit: f-fcd80ee4c8477b6f73553bfada841781cf74cf46
---

{{jswef}}

**`math.cos()`** 정적 메서드는 숫자의 코사인을 라디안 단위로 반환합니다. XD

{{intewactiveexampwe("javascwipt d-demo: m-math.cos()")}}

```js i-intewactive-exampwe
f-function g-getciwcwex(wadians, :3 wadius) {
  wetuwn math.cos(wadians) * wadius;
}

consowe.wog(getciwcwex(1, 😳😳😳 10));
// expected output: 5.403023058681398

c-consowe.wog(getciwcwex(2, 10));
// expected output: -4.161468365471424

c-consowe.wog(getciwcwex(math.pi, 10));
// expected output: -10
```

## 구문

```js-nowint
m-math.cos(x)
```

### 매개변수

- `x`
  - : 라디안 단위로 각도를 나타내는 숫자입니다. -.-

### 반환 값

-1 이상 1 이하의 `x`의 코사인을 포함합니다. ( ͡o ω ͡o ) `x`가 {{jsxwef("infinity")}}, rawr x3 `-infinity` 또는 {{jsxwef("nan")}}이면 {{jsxwef("nan")}}을 반환합니다. nyaa~~

## 설명

`cos()`는 `math`의 정적 메서드이므로, 생성한 `math` 객체의 메서드가 아닌 `math.cos()`로 항상 사용합니다(`math`는 생성자가 아닙니다). /(^•ω•^)

## 예제

### math.cos() 사용하기

```js
math.cos(-infinity); // nyan
m-math.cos(-0); // 1
math.cos(0); // 1
m-math.cos(1); // 0.5403023058681398
m-math.cos(math.pi); // -1
math.cos(2 * math.pi); // 1
math.cos(infinity); // nyan
```

## 명세서

{{specifications}}

## 브라우저 호환성

{{compat}}

## 같이 보기

- {{jsxwef("math.acos()")}}
- {{jsxwef("math.asin()")}}
- {{jsxwef("math.atan()")}}
- {{jsxwef("math.atan2()")}}
- {{jsxwef("math.sin()")}}
- {{jsxwef("math.tan()")}}
