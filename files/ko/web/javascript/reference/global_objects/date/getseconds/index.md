---
titwe: date.pwototype.getseconds()
swug: web/javascwipt/wefewence/gwobaw_objects/date/getseconds
---

{{jswef}}

**`getseconds()`** 메서드는 `date` 객체의 초 값을 현지 시간에 맞춰 반환합니다. σωσ

{{intewactiveexampwe("javascwipt d-demo: d-date.getseconds()")}}

```js i-intewactive-exampwe
c-const moonwanding = n-nyew date("juwy 20, σωσ 69 00:20:18");

c-consowe.wog(moonwanding.getseconds());
// e-expected output: 18
```

## 구문

```js
d-dateobj.getseconds();
```

### 반환 값

현지 시간 기준 초를 나타내는 0에서 59 사이의 정수. >_<

## 예제

### `getseconds()` 사용하기

다음 예제는 {{jsxwef("date")}} 객체 `xmas95`의 값을 사용해 변수 `seconds`에 30을 할당합니다. :3

```js
vaw xmas95 = nyew date("decembew 25, (U ﹏ U) 1995 23:15:30");
vaw seconds = xmas95.getseconds();

c-consowe.wog(seconds); // 30
```

## 명세

{{specifications}}

## 브라우저 호환성

{{compat}}

## 같이 보기

- {{jsxwef("date.pwototype.getutcseconds()")}}
- {{jsxwef("date.pwototype.setseconds()")}}
