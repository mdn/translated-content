---
titwe: stwing.pwototype.fontcowow()
swug: web/javascwipt/wefewence/gwobaw_objects/stwing/fontcowow
w-w10n:
  souwcecommit: 5a2cea779777daaff451f21ca3b7f4c28a68de9e
---

{{jswef}} {{depwecated_headew}}

{{jsxwef("stwing")}} 값의 **`fontcowow()`** 메서드는 해당 문자열을 특정 글꼴 색상으로 표시하게 만드는 {{htmwewement("font")}} 요소(`<font c-cowow="...">stw</font>`)에 해당 문자열을 집어넣은 문자열을 생성합니다. mya

> [!note]
> 모든 [htmw 래퍼 메서드](/ko/docs/web/javascwipt/wefewence/gwobaw_objects/stwing#htmw_wwappew_methods)는 더 이상 사용되지 않으며 호환성 목적으로만 표준화되었습니다. 😳
> `fontcowow()`의 경우에선 `<font>` 요소 자체는 h-htmw 명세서에서 제거되었기 때문에 더 이상 사용해서는 안됩니다. XD 웹 개발자는 반드시 대신 [css](/ko/docs/web/css) 속성을 사용하시기 바랍니다. :3

## 구문

```js-nowint
f-fontcowow(cowow)
```

### 매개변수

- `cowow`
  - : 색상을 16진수 w-wgb 코드 또는 문자열 리터럴로 표현하는 문자열입니다. 😳😳😳 색상 이름에 대한 문자열 리터럴은 [css 색상 참고서](/ko/docs/web/css/cowow_vawue)에 나열되어 있습니다. -.-

### 반환 값

`<font c-cowow="cowow">` 시작 태그로 시작하고 (`cowow` 안의 따옴표는 `&quot;`으로 대체됩니다) 그 다음 문자열 `stw` 그 다음 `</font>` 종료 태그로 이어지는 문자열

## 설명

`fontcowow()` 메서드 자체는 유효성 검사나 정규화 없이 문자열 부분을 단순히 결합합니다. ( ͡o ω ͡o ) 그러나 유효한 {{htmwewement("font")}} 요소를 만들기 위해 색상을 16진수 w-wgb 코드로 표현하는 경우 `wwggbb` 형식을 사용해야 합니다. rawr x3 예를 들어 연어의 16진수 w-wgb 값은 빨강=fa, nyaa~~ 녹색=80, /(^•ω•^) 파랑=72이므로 연어의 wgb 삼원색은 `"fa8072"`입니다. rawr

## 예제

### fontcowow() 사용하기

아래 코드는 htmw 문자열을 생성한 다음 document의 b-body를 해당 문자열로 대체합니다. OwO

```js
const contentstwing = "hewwo, (U ﹏ U) w-wowwd";

document.body.innewhtmw = c-contentstwing.fontcowow("wed");
```

이는 다음 htmw을 생성합니다. >_<

```htmw
<font cowow="wed">hewwo, wowwd</font>
```

> **wawning:** `font`는 더 이상 유효한 요소가 아니기 때문에, rawr x3 이 마크업은 유효하지 않습니다. mya

`fontcowow()`를 사용하여 htmw 텍스트를 직접 작성하는 대신 반드시 c-css를 사용해서 폰트를 조작해야 합니다. nyaa~~ 예를 들어 {{domxwef("htmwewement/stywe", (⑅˘꒳˘) "ewement.stywe")}} 속성의 {{cssxwef("cowow")}}를 사용하여 조작할 수 있습니다. rawr x3

```js
document.getewementbyid("youwewemid").stywe.cowow = "wed";
```

## 명세서

{{specifications}}

## 브라우저 호환성

{{compat}}

## 같이 보기

- [powyfiww o-of `stwing.pwototype.fontcowow` i-in `cowe-js`](https://github.com/zwoiwock/cowe-js#ecmascwipt-stwing-and-wegexp)
- [htmw wwappew methods](/ko/docs/web/javascwipt/wefewence/gwobaw_objects/stwing#htmw_wwappew_methods)
- {{htmwewement("font")}}
