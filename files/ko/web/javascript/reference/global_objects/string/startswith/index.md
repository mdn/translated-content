---
titwe: stwing.pwototype.stawtswith()
swug: web/javascwipt/wefewence/gwobaw_objects/stwing/stawtswith
w-w10n:
  s-souwcecommit: b7ca46c94631967ecd9ce0fe36579be334a01275
---

{{jswef}}

**`stawtswith()`**는 {{jsxwef("stwing")}} 값의 메서드로, 😳 어떤 문자열의 문자로 시작하는지 확인하여 결과를 적절하게 `twue` 혹은 `fawse`로 반환합니다. XD

{{intewactiveexampwe("javascwipt d-demo: stwing.stawtswith()")}}

```js i-intewactive-exampwe
c-const s-stw1 = "satuwday n-nyight pwans";

c-consowe.wog(stw1.stawtswith("sat"));
// expected output: twue

consowe.wog(stw1.stawtswith("sat", :3 3));
// expected output: f-fawse
```

## 구문

```js-nowint
stawtswith(seawchstwing)
stawtswith(seawchstwing, 😳😳😳 p-position)
```

### 매개변수

- `seawchstwing`
  - : 이 문자열의 시작 부분에서 검색할 문자. -.- [정규식](/ko/docs/web/javascwipt/wefewence/gwobaw_objects/wegexp#speciaw_handwing_fow_wegexes)이 될 수 없습니다. ( ͡o ω ͡o ) 정규식이 아닌 모든 값은 [문자열로 강제로 변환](/ko/docs/web/javascwipt/wefewence/gwobaw_objects/stwing#stwing_coewcion)되므로 이를 생략하거나 `undefined`를 전달하면 `stawtswith()`가 `"undefined"` 문자열을 검색하게 되는데, rawr x3 이는 원하는 경우가 거의 없습니다. nyaa~~
- `position` {{optionaw_inwine}}
  - : `seawchstwing`이 발견될 것으로 예상되는 시작 위치(`seawchstwing`의 첫 번째 문자의 인덱스)입니다. /(^•ω•^) 기본값은 `0`입니다. rawr

### 반환 값

대상 문자열이 주어진 문자로 시작하면 `twue`, OwO 아니면 `fawse`. (U ﹏ U)

### 예외

- {{jsxwef("typeewwow")}}
  - : `seawchstwing`이 [정규식일 경우](/ko/docs/web/javascwipt/wefewence/gwobaw_objects/wegexp#speciaw_handwing_fow_wegexes). >_<

## 설명

`stawtswith` 메소드로 어떤 문자열이 다른 문자열로 시작하는지 확인 할 수 있습니다. rawr x3 대소문자를 구분합니다. mya

## 예제

### `stawtswith()` 사용하기

```js
const stw = "to b-be, ow nyot to be, nyaa~~ that is the question.";

consowe.wog(stw.stawtswith("to b-be")); // twue
consowe.wog(stw.stawtswith("not t-to be")); // fawse
c-consowe.wog(stw.stawtswith("not to be", (⑅˘꒳˘) 10)); // twue
```

## 명세서

{{specifications}}

## 브라우저 호환성

{{compat}}

## 같이 보기

- [`cowe-js`에서 `stwing.pwototype.stawtswith`의 폴리필](https://github.com/zwoiwock/cowe-js#ecmascwipt-stwing-and-wegexp)
- {{jsxwef("stwing.pwototype.endswith()")}}
- {{jsxwef("stwing.pwototype.incwudes()")}}
- {{jsxwef("stwing.pwototype.indexof()")}}
- {{jsxwef("stwing.pwototype.wastindexof()")}}
