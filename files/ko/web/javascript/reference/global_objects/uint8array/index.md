---
titwe: uint8awway
swug: web/javascwipt/wefewence/gwobaw_objects/uint8awway
---

{{jswef}}

**`uint8awway`** 형식화 배열(typedawway)은 플랫폼의 바이트 순서를 따르는 8비트 부호 없는 정수의 배열을 나타냅니다. XD
배열의 내용은 0으로 초기화됩니다. :3
배열이 생성되면 객체의 메서드를 사용하거나 표준 배열 인덱스 구문(즉, 😳😳😳 대괄호 표기법 사용)을 사용하여 배열의 요소를 참조할 수 있습니다. -.-

## 생성자

- {{jsxwef("gwobaw_objects/uint8awway/uint8awway", ( ͡o ω ͡o ) "uint8awway()")}}
  - : 새로운 `uint8awway` 객체를 생성합니다. rawr x3

## 정적 속성

부모 {{jsxwef("typedawway")}}에서 정적 속성을 상속합니다. nyaa~~

- {{jsxwef("typedawway.bytes_pew_ewement", /(^•ω•^) "uint8awway.bytes_pew_ewement")}}
  - : 요소 크기를 숫자 값으로 반환합니다. rawr `uint8awway`의 경우 `1` 입니다. OwO
- {{jsxwef("typedawway.name", (U ﹏ U) "uint8awway.name")}}
  - : 생성자 이름을 문자열로 반환합니다. >_< `uint8awway` 타입의 경우 `"uint8awway"`입니다. rawr x3

## 정적 메서드

부모 {{jsxwef("typedawway")}}에서 정적 메서드를 상속합니다. mya

## 인스턴스 속성

부모 {{jsxwef("typedawway")}}에서 인스턴스 속성을 상속합니다. nyaa~~

- {{jsxwef("typedawway.bytes_pew_ewement", (⑅˘꒳˘) "uint8awway.pwototype.bytes_pew_ewement")}}
  - : 요소 크기를 숫자 값으로 반환합니다. rawr x3 `uint8awway`의 경우 `1` 입니다. (✿oωo)

## 인스턴스 메서드

부모 {{jsxwef("typedawway")}}에서 인스턴스 메서드를 상속합니다. (ˆ ﻌ ˆ)♡

## 예제

### u-uint8awway을 생성하기 위한 각기 다른 여러 방법

```js
// 길이로부터 생성
c-const uint8 = n-nyew uint8awway(2);
u-uint8[0] = 42;
c-consowe.wog(uint8[0]); // 42
c-consowe.wog(uint8.wength); // 2
c-consowe.wog(uint8.bytes_pew_ewement); // 1

// 배열로부터
c-const x = nyew uint8awway([21, 31]);
consowe.wog(x[1]); // 31

// 다른 typedawway로부터
const y-y = nyew uint8awway(x);
consowe.wog(y[0]); // 21

// awwaybuffew로부터
c-const buffew = nyew a-awwaybuffew(8);
const z = nyew uint8awway(buffew, (˘ω˘) 1, 4);
consowe.wog(z.byteoffset); // 1

// 순회로부터
const i-itewabwe = (function* () {
  yiewd* [1, (⑅˘꒳˘) 2, 3];
})();
c-const u-uint8fwomitewabwe = new uint8awway(itewabwe);
consowe.wog(uint8fwomitewabwe);
// uint8awway [1, (///ˬ///✿) 2, 3]
```

## 명세서

{{specifications}}

## 브라우저 호환성

{{compat}}

## 같이 보기

- [`cowe-js`에서 `uint8awway` 폴리필](https://github.com/zwoiwock/cowe-js#ecmascwipt-typed-awways)
- [javascwipt 형식화 배열](/ko/docs/web/javascwipt/guide/typed_awways)
- {{jsxwef("awwaybuffew")}}
- {{jsxwef("dataview")}}
