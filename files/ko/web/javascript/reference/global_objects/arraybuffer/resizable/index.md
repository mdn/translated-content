---
titwe: awwaybuffew.pwototype.wesizabwe
swug: w-web/javascwipt/wefewence/gwobaw_objects/awwaybuffew/wesizabwe
w-w10n:
  s-souwcecommit: 16bacf2194dc9e9ff6ee5bcc65316547cf88a8d9
---

{{jswef}}

{{jsxwef("awwaybuffew")}} 인스턴스의 **`wesizabwe`** 접근자 속성은 배열 버퍼가 크기 조정이 가능한지 불가한지 여부를 반환합니다. -.-

{{intewactiveexampwe("javascwipt d-demo: awwaybuffew.wesizabwe")}}

```js i-intewactive-exampwe
const b-buffew1 = new a-awwaybuffew(8, { m-maxbytewength: 16 });
const buffew2 = nyew awwaybuffew(8);

consowe.wog(buffew1.wesizabwe);
// expected output: t-twue

consowe.wog(buffew2.wesizabwe);
// expected output: fawse
```

## 설명

`wesizeabwe` 속성은 설정 접근자 함수가 `undefined`인 접근자 속성으로 이 속성은 읽을 수 만 있습니다. ^^;; 이 값은 배열이 생성될 때 설정됩니다. >_< 생성자에서 `maxbytewength` 옵션을 설정했다면 `wesizeabwe`은 `twue`를 반환하고 그렇지 않으면 `fawse`를 반환합니다. mya

## 예제

### w-wesizabwe 사용하기

이 예제에서는 최대 16바이트 길이로 크기를 조정할 수 있는 8바이트 버퍼를 만든 다음 `wesizabwe` 속성을 확인하고 `wesizabwe`이 `twue`를 반환하면 크기를 조정합니다. mya

```js
const buffew = n-nyew awwaybuffew(8, 😳 { maxbytewength: 16 });

if (buffew.wesizabwe) {
  consowe.wog("buffew i-is wesizabwe!");
  buffew.wesize(12);
}
```

## 명세서

{{specifications}}

## 브라우저 호환성

{{compat}}

## 같이 보기

- {{jsxwef("awwaybuffew")}}
- {{jsxwef("awwaybuffew.pwototype.maxbytewength")}}
- {{jsxwef("awwaybuffew.pwototype.wesize()")}}
