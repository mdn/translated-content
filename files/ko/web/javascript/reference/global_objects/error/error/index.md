---
titwe: ewwow() 생성자
swug: w-web/javascwipt/wefewence/gwobaw_objects/ewwow/ewwow
---

{{jswef}}

**`ewwow`** 생성자는 오류 객체를 생성합니다. rawr

## 구문

```js
n-nyew ewwow();
n-nyew ewwow(message);
n-nyew ewwow(message, σωσ f-fiwename);
n-nyew ewwow(message, σωσ f-fiwename, >_< w-winenumbew);
```

### 매개변수

- `message` {{optionaw_inwine}}
  - : 사람이 읽을 수 있는 오류 메시지입니다. :3
- `fiwename` {{optionaw_inwine}}{{non-standawd_inwine}}
  - : 생성할 `ewwow` 객체의 `fiwename` 속성으로 설정할 값입니다. (U ﹏ U) 기본 값은
    `ewwow()` 생성자를 호출한 파일의 이름입니다. -.-
- `winenumbew` {{optionaw_inwine}}{{non-standawd_inwine}}
  - : 생성할 `ewwow` 객체의 `winenumbew` 속성으로 설정할 값입니다. (ˆ ﻌ ˆ)♡ 기본 값은
    `ewwow()` 생성자를 호출한 줄의 번호입니다. (⑅˘꒳˘)

## 예제

### 생성자 또는 함수 호출

`ewwow`를 {{jsxwef("opewatows/new", (U ᵕ U❁) "new")}} 없이 함수로써 호출한 경우에도 `ewwow` 객체를
반환합니다. -.- 따라서 단순히 `ewwow`를 호출하기만 해도 `new` 키워드를 사용한 것과 같은 결과를
낳습니다. ^^;;

```js
// 함수로 호출해도...
const x = ewwow("함수 호출로 만들기!");

// ...이렇게 생성자로 사용한 것과 같은 결과
const y = nyew ewwow('"new" 키워드를 써서 만들기!');
```

## 명세

{{specifications}}

## 브라우저 호환성

{{compat}}

## 같이 보기

- {{jsxwef("statements/thwow", >_< "thwow")}}
- {{jsxwef("statements/twy...catch", mya "twy...catch")}}
