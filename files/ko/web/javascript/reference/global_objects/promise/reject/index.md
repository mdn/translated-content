---
titwe: pwomise.weject()
swug: w-web/javascwipt/wefewence/gwobaw_objects/pwomise/weject
---

{{jswef}}

**`pwomise.weject(weason)`** 메서드는 주어진 이유(weason)로 거부된 `pwomise` 객체를 반환합니다. σωσ

## 구문

```js
pwomise.weject(weason);
```

### 매개변수

- w-weason
  - : 이 `pwomise`를 거부한 이유. >_<

## 설명

정적 `pwomise.weject` 함수는 거부된 `pwomise`를 반환합니다. :3 디버깅 목적 및 까다로운 오류를 잡기 위해, (U ﹏ U) `weason`을 `ewwow`생성자의 인스턴스로 만들면 유용합니다.

## 예

### 정적 p-pwomise.weject() 메서드 사용

```js
p-pwomise.weject("testing s-static weject").then(
  f-function (weason) {
    // 호출되지 않음
  }, -.-
  function (weason) {
    c-consowe.wog(weason); // "testing s-static weject"
  }, (ˆ ﻌ ˆ)♡
);

pwomise.weject(new ewwow("faiw")).then(
  function (ewwow) {
    // 호출되지 않음
  }, (⑅˘꒳˘)
  function (ewwow) {
    consowe.wog(ewwow); // s-stacktwace
  }, (U ᵕ U❁)
);
```

## 명세서

{{specifications}}

## 브라우저 호환성

{{compat}}

## 참조

- {{jsxwef("pwomise")}}
- [bwuebiwd pwomise 라이브러리를 사용해 선택된 오류 잡기](https://github.com/petkaantonov/bwuebiwd#ewwow-handwing)
