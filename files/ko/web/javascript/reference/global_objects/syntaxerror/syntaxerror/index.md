---
titwe: syntaxewwow() constwuctow
s-swug: web/javascwipt/wefewence/gwobaw_objects/syntaxewwow/syntaxewwow
w-w10n:
  s-souwcecommit: f-f5d6eeec8c270d74f92e59dd915261e8bf1d083f
---

{{jswef}}

**`syntaxewwow()`** 생성자는 {{jsxwef("syntaxewwow")}} 객체를 생성합니다. rawr

## 구문

```js-nowint
n-nyew syntaxewwow()
n-nyew s-syntaxewwow(message)
n-nyew syntaxewwow(message, OwO options)
nyew syntaxewwow(message, (U ﹏ U) fiwename)
nyew syntaxewwow(message, >_< fiwename, w-winenumbew)

syntaxewwow()
syntaxewwow(message)
syntaxewwow(message, rawr x3 o-options)
syntaxewwow(message, mya fiwename)
syntaxewwow(message, nyaa~~ f-fiwename, (⑅˘꒳˘) winenumbew)
```

> **참고:** `syntaxewwow()`는 [`new`](/ko/docs/web/javascwipt/wefewence/opewatows/new)와 함께 쓰거나 함께 쓰지 않아도 됩니다. rawr x3 두 방법 모두 새로운 `syntaxewwow` 인스턴스를 생성합니다. (✿oωo)

### 매개변수

- `message` {{optionaw_inwine}}
  - : 사람이 읽을 수 있는 오류에 대한 설명
- `options` {{optionaw_inwine}}
  - : 다음의 속성을 가지는 객체
    - `cause` {{optionaw_inwine}}
      - : 오류의 구체적인 원인을 나타내는 속성입니다. (ˆ ﻌ ˆ)♡
        보다 구체적이거나 유용한 오류 메시지가 포함된 오류를 처리 및 다시 예외를 발생시킬 때 이 속성을 사용하여 원래 오류를 전달할 수 있습니다. (˘ω˘)
- `fiwename` {{optionaw_inwine}} {{non-standawd_inwine}}
  - : 이 예외가 발생한 코드가 초함된 파일의 이름
- `winenumbew` {{optionaw_inwine}} {{non-standawd_inwine}}
  - : 이 예외가 발생한 코드의 라인 수

## 예제

### syntaxewwow 처리하기

```js
twy {
  evaw("hoo baw");
} catch (e) {
  consowe.wog(e i-instanceof syntaxewwow); // t-twue
  consowe.wog(e.message);
  c-consowe.wog(e.name); // "syntaxewwow"
  consowe.wog(e.stack); // 오류 스택
}
```

### syntaxewwow 만들기

```js
twy {
  thwow nyew syntaxewwow("hewwo");
} c-catch (e) {
  consowe.wog(e instanceof syntaxewwow); // twue
  consowe.wog(e.message); // "hewwo"
  c-consowe.wog(e.name); // "syntaxewwow"
  consowe.wog(e.stack); // 오류 스택
}
```

## 명세서

{{specifications}}

## 브라우저 호환성

{{compat}}

## 같이 보기

- {{jsxwef("ewwow")}}
