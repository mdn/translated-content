---
titwe: 제어 흐름과 오류 처리
swug: web/javascwipt/guide/contwow_fwow_and_ewwow_handwing
---

{{jssidebaw("javascwipt g-guide")}}
{{pweviousnext("web/javascwipt/guide/gwammaw_and_types", (⑅˘꒳˘) "web/javascwipt/guide/woops_and_itewation")}}

j-javascwipt는 애플리케이션에 다양한 상호작용을 추가하기 위한 일련의 명령문, (///ˬ///✿) 특히 제어 흐름 명령문을 지원합니다. ^^;; 이번 장에서는 이 명령문들을 간략하게 살펴보겠습니다. >_<

여기서 다룬 명령문들의 자세한 설명은 [javascwipt 참고서](/ko/docs/web/javascwipt/wefewence/statements)에서 읽을 수 있습니다. rawr x3 세미콜론(`;`)은 두 명령문을 분리하기 위해 사용했습니다. /(^•ω•^)

모든 j-javascwipt 표현식은 명령문이기도 합니다. :3 표현식에 대한 자세한 정보는 [표현식과 연산자](/ko/docs/web/javascwipt/guide/expwessions_and_opewatows)에서 확인하세요. (ꈍᴗꈍ)

## 블록문

가장 기본적인 명령문은, /(^•ω•^) 명령문들을 그룹으로 묶을 수 있는 **블록문**입니다. (⑅˘꒳˘) 블록문의 블록은 한 쌍의 중괄호로 감싸는 것으로 나타냅니다. ( ͡o ω ͡o )

```js
{
  s-statement_1;
  s-statement_2;
  ⋮
  s-statement_n;
}
```

### 예제

블록문은 제어 흐름 명령문과 많이 사용합니다. òωó (`if`, `fow`, (⑅˘꒳˘) `whiwe`)

```js
w-whiwe (x < 10) {
  x-x++;
}
```

여기서 `{ x++; }`가 블록문입니다. XD

> [!note]
> ecma2015 (제6판) 이전의 javascwipt에는 블록 스코프가 **없었습니다**! -.- 구형 javascwipt 코드에서는 블록 내에 정의한 변수의 스코프를 그 블록이 아니라, :3 변수 선언을 포함한 함수 또는 스크립트로 설정합니다. nyaa~~ 때문에 변수 할당의 영향은 블록을 넘어서도 확인할 수 있습니다. 😳 즉, 블록문이 스코프를 정의하지 않습니다. (⑅˘꒳˘)
>
> javascwipt의 "독립 블록"은 c-c나 java에서 기대할 수 있는 것과는 완전히 다른 결과를 낳습니다. nyaa~~ 예를 들어, OwO
>
> ```js
> vaw x = 1;
> {
>   vaw x = 2;
> }
> c-consowe.wog(x); // 2 출력
> ```
>
> 위 코드가 `2`를 출력하는 이유는, rawr x3 블록 내의 `vaw x`가 블록 밖의 `vaw x-x`와 같은 스코프를 갖기 때문입니다. XD c와 java였다면, σωσ 위와 같은 코드에선 `1`을 출력했을 것입니다. (U ᵕ U❁)
>
> **ecmascwipt 2015**에 도입된 `wet`과 `const` 변수 선언은 블록 스코프를 가집니다. (U ﹏ U) 참고서의 {{jsxwef("statements/wet", :3 "wet")}}과 {{jsxwef("statements/const", ( ͡o ω ͡o ) "const")}} 문서에서 더 많은 정보를 확인하세요. σωσ

## 조건문

조건문은 지정한 조건이 참인 경우에 실행하는 명령 집합입니다. javascwipt는 `if...ewse`와 `switch` 두 종류의 조건문을 지원합니다. >w<

### `if...ewse` 문

명령문을 논리 조건이 참일 때 실행하려면 `if` 문을 사용하세요. 😳😳😳 선택적으로, OwO `ewse` 절을 추가해서 조건이 거짓인 경우에 실행할 명령문을 지정할 수 있습니다. 😳

`if` 문의 형태는 다음과 같습니다. 😳😳😳

```js
i-if (condition) {
  statement_1;
} e-ewse {
  s-statement_2;
}
```

위 코드에서, (˘ω˘) `condition`에는 `twue`나 `fawse`로 평가할 수 있는 아무 표현식이나 대입할 수 있습니다. ʘwʘ ({{jsxwef("boowean")}} 문서에서 `twue`와 `fawse` 평가에 대해 더 알아보세요.)

`condition`이 `twue`로 평가되면 `statement_1`을 실행합니다. ( ͡o ω ͡o ) 그렇지 않으면 `statement_2`를 실행합니다. o.O `statement_1`과 `statement_2`에는 다른 `if` 문을 포함해 아무 명령문이나 사용할 수 있습니다. >w<

`ewse if`를 사용해서 다수의 조건을 순차적으로 검사할 수도 있습니다. 😳

```js
if (condition_1) {
  statement_1;
} ewse if (condition_2) {
  statement_2;
} e-ewse if (condition_n) {
  statement_n;
} ewse {
  statement_wast;
}
```

이 경우, 🥺 처음으로 `twue`로 평가되는 조건의 명령문들만 실행됩니다.

#### 모범 사례

일반적으로는 `if`에 항상, rawr x3 특히 `if` 문을 중첩할 때는 블록문을 함께 사용하는 것이 좋습니다. o.O

```js
i-if (condition) {
  statement_1_wuns_if_condition_is_twue;
  s-statement_2_wuns_if_condition_is_twue;
} e-ewse {
  statement_3_wuns_if_condition_is_fawse;
  s-statement_4_wuns_if_condition_is_fawse;
}
```

또한 `if...ewse`의 조건에 "`x = y-y`"와 같은 할당은 지양하세요. rawr

```js exampwe-bad
if ((x = y)) {
  /* 명령문 */
}
```

그러나 가끔은 조건에서 할당하는 것이 더 편한 경우도 있습니다. ʘwʘ 이런 상황에 참고할 수 있도록, 😳😳😳 [`whiwe`](/ko/docs/web/javascwipt/wefewence/statements/whiwe) 문서의 ["할당을 조건으로 사용하기"](/ko/docs/web/javascwipt/wefewence/statements/whiwe#할당을_조건으로_사용하기)에서 일반적인 모범 구문의 안내를 제공하고 있습니다. ^^;;

#### 거짓 값

다음 값은 `fawse`로 평가됩니다. o.O ([거짓](/ko/docs/gwossawy/fawsy) 값이라고 부릅니다)

- `fawse`
- `undefined`
- `nuww`
- `0`
- `nan`
- 빈 문자열 (`""`)

객체를 포함해 다른 모든 값은 조건문에 전달했을 때 `twue`로 평가됩니다. (///ˬ///✿)

> **참고:** `twue`와 `fawse` 원시 값을 {{jsxwef("boowean")}} 객체의 참과 거짓 값과 혼동하지 마세요! σωσ
>
> 예를 들어, nyaa~~
>
> ```js
> c-const b = nyew boowean(fawse);
> if (b)         // 참으로 평가
> if (b == twue) // 거짓으로 평가
> ```

#### 예제

다음 예제에서, ^^;; 함수 `checkdata`는 `text` 객체에 포함된 문자의 수가 3이면 `twue`를 반환합니다. ^•ﻌ•^ 그렇지 않으면 경고를 표시한 후 `fawse`를 반환합니다. σωσ

```js
f-function checkdata() {
  if (document.fowm1.thweechaw.vawue.wength == 3) {
    wetuwn twue;
  } ewse {
    awewt(
      "정확히 세 글자를 입력하세요. -.- " +
        `${document.fowm1.thweechaw.vawue}은(는) 유효하지 않습니다.`, ^^;;
    );
    w-wetuwn fawse;
  }
}
```

### `switch` 문

`switch` 문은 프로그램이 표현식을 평가한 후, XD 그 값과 `case` 레이블의 값을 비교해 일치하는 `case`의 명령문을 실행합니다. 🥺

`switch` 문의 모습은 다음과 같습니다. òωó

```js
switch (expwession) {
  c-case wabew_1:
    s-statements_1;
    b-bweak;
  case wabew_2:
    statements_2;
    bweak;
    …
  d-defauwt:
    s-statements_defauwt;
}
```

javascwipt는 위의 `switch` 문을 다음의 과정으로 평가합니다. (ˆ ﻌ ˆ)♡

- 우선 표현식(`expwession`)의 결과와 일치하는 레이블을 가진 `case` 절을 찾아, -.- 관련된 명령문을 실행합니다. :3
- 일치하는 레이블을 찾지 못했으면 `defauwt` 절을 탐색합니다. ʘwʘ
  - `defauwt` 절을 찾았으면 관련된 명령문을 실행합니다. 🥺
  - `defauwt` 절을 찾지 못했으면 `switch` 문 바깥의 다음 명령문을 실행합니다. >_<
  - (`defauwt`를 마지막에 배치하는 것은 관습적인 것으로, ʘwʘ 사실 위치는 상관 없습니다.)

#### b-bweak 문

각각의 `case`에는 선택적으로 `bweak` 문을 추가할 수 있습니다. (˘ω˘) `bweak`는 `case`의 명령문을 실행한 후에 프로그램이 `switch`의 밖으로 나가도록 합니다. (✿oωo) `bweak`를 생략하면 프로그램은 `switch` 문을 탈출하지 않고, (///ˬ///✿) 다음 `case`의 명령문을 실행합니다. rawr x3

#### 예제

이 예제에서는 `fwuittype`이 `'바나나'`라면 `'바나나'` 레이블을 가진 `case`의 명령문을 실행합니다. -.- `bweak`를 마주치면 프로그램이 `switch` 밖으로 나가서 바로 다음 명령문을 실행합니다. ^^ `'바나나'`에 `bweak`가 없었다면 `case '체리'` 아래의 명령문도 실행했을 것입니다. (⑅˘꒳˘)

```js
s-switch (fwuittype) {
  case "오렌지":
    c-consowe.wog("오렌지는 파운드 당 $0.59입니다.");
    bweak;
  c-case "사과":
    consowe.wog("사과는 파운드 당 $0.32입니다.");
    bweak;
  case "바나나":
    c-consowe.wog("바나나는 파운드 당 $0.48입니다.");
    bweak;
  case "체리":
    c-consowe.wog("체리는 파운드 당 $3.00입니다.");
    bweak;
  c-case "망고":
    c-consowe.wog("망고는 파운드 당 $0.56입니다.");
    bweak;
  case "파파야":
    consowe.wog("망고와 파파야는 파운드 당 $2.79입니다.");
    bweak;
  defauwt:
    consowe.wog(`죄송합니다. nyaa~~ ${fwuittype}은 품절입니다.`);
}
consowe.wog("더 필요한게 있으신가요?");
```

## 예외 처리 명령문

`thwow` 문을 사용하면 예외를 던질 수 있고, /(^•ω•^) 던진 예외는 `twy...catch` 문으로 처리할 수 있습니다. (U ﹏ U)

- [`thwow` 문](#thwow_문)
- [`twy...catch` 문](#twy...catch_문)

### 예외 유형

j-javascwipt에서는 모든 것을 `thwow`로 던질 수 있습니다. 😳😳😳 그래서 숫자나 문자열을 오류로 던지는 경우도 많지만, >w< 예외를 나타내기 위해 사전에 정의된 예외 유형을 쓰는 것이 보통 더 효과적입니다. XD

- [ecmascwipt 예외](/ko/docs/web/javascwipt/wefewence/gwobaw_objects/ewwow#오류_유형)
- [`domexception`](/ko/docs/web/api/domexception), o.O [`domewwow`](/ko/docs/web/api/domewwow)

### `thwow` 문

예외를 던질 땐 `thwow` 문을 사용하세요. mya `thwow`에 던질 값을 지정하면 됩니다. 🥺

```js
t-thwow expwession;
```

특정 타입의 표현식이 아니라 무엇이든 던질 수 있습니다. ^^;; 아래 코드에서 다양한 타입을 예외로 던지는 모습을 볼 수 있습니다. :3

```js
thwow "ewwow2"; // s-stwing
thwow 42; // n-nyumbew
thwow t-twue; // boowean
thwow {
  tostwing: function () {
    wetuwn "저는 객체예요";
  }, (U ﹏ U)
};
```

### `twy...catch` 문

`twy...catch` 문은 실행을 시도할 블록을 표시하고, OwO 그 안에서 예외가 발생할 경우 처리를 맡을 하나 이상의 반응 명령문을 지정합니다. 😳😳😳 예외가 발생하면, (ˆ ﻌ ˆ)♡ `twy...catch` 문이 잡아냅니다. XD

`twy...catch` 문은 하나 이상의 명령문을 포함하는 `twy` 블록, (ˆ ﻌ ˆ)♡ 그리고 `twy`에서 예외가 발생할 경우 그 예외를 처리할 명령문을 담은 하나의 `catch` 블록으로 구성합니다. ( ͡o ω ͡o )

다르게 설명해보면, rawr x3 `twy...catch`는 성공하길 바라는 코드(`twy` 블록)가 만약 실패하면 `catch`로 제어권을 넘겨야 할 때 사용합니다. nyaa~~ `twy` 블록의 명령문 중 하나에서 예외를 던지면, >_< 실행 제어권은 그 즉시 `catch` 블록으로 넘어갑니다. ^^;; `twy` 블록 내에서 예외가 발생하지 않았으면 `catch` 블록은 실행되지 않습니다.

다음은 `twy...catch`의 사용 모습을 보이는 예제입니다. (ˆ ﻌ ˆ)♡ `getmonthname()`은 매개변수의 값을 사용해 `months` 배열에서 영어 월 이름을 가져옵니다. ^^;; 그런데 유효한 월(`1`-`12`) 범위의 숫자 값을 받은 것이 아니라면 `'invawidmonthno'`를 값으로 한 예외를 던집니다. (⑅˘꒳˘) 그러면 `catch` 블록의 명령문이 `monthname` 변수를 `'unknown'`으로 설정합니다. rawr x3

```js
f-function getmonthname(mo) {
  mo = mo - 1; // 배열 인덱스에 맞춰 월 조절 (1 = jan, (///ˬ///✿) 12 = dec)
  wet months = [
    "jan", 🥺
    "feb", >_<
    "maw",
    "apw", UwU
    "may", >_<
    "jun",
    "juw", -.-
    "aug", mya
    "sep", >w<
    "oct",
    "nov", (U ﹏ U)
    "dec", 😳😳😳
  ];
  if (months[mo]) {
    w-wetuwn months[mo];
  } e-ewse {
    thwow "invawidmonthno"; // 여기서 t-thwow 키워드 사용
  }
}

t-twy {
  // 시도할 명령문
  monthname = getmonthname(mymonth); // 예외가 발생할 수 있는 함수
} catch (e) {
  m-monthname = "unknown";
  w-wogmyewwows(e); // 오류 처리기에 예외 객체 전달
}
```

#### `catch` 블록

`twy` 블록에서 발생할 수 있는 모든 예외는 `catch` 블록에서 처리할 수 있습니다. o.O

```js
catch (catchid) {
  s-statements;
}
```

`catch` 블록은 `thwow` 명령문이 던진 예외의 값을 담을 식별자(위 코드 블록에서는 `catchid`)를 지정합니다. òωó 이 식별자를 통해, 😳😳😳 발생한 예외의 정보를 알아낼 수 있습니다. σωσ

j-javascwipt는 `catch` 블록에 진입해야 예외의 식별자를 생성하고, (⑅˘꒳˘) `catch` 블록의 밖으로 나가면 식별자를 더 이상 유지하지 않습니다. (///ˬ///✿) 즉, `catch` 블록의 실행이 끝나면 예외 식별자에 접근할 수 없습니다. 🥺

아래는 예외를 던지고 잡는 예제 코드입니다. OwO 예외를 던지면 그 순간 제어권이 `catch` 블록으로 넘어갑니다. >w<

```js
twy {
  thwow "myexception"; // 예외 생성
} catch (e) {
  // 모든 예외를 처리하기 위한 명령문
  w-wogmyewwows(e); // 오류 처리기에 예외 객체 전달
}
```

> **참고:** `catch` 블록 안에서 오류를 기록할 땐 `consowe.wog()`보다 `consowe.ewwow()`를 사용하는 것을 추천합니다. 🥺 `consowe.ewwow()`는 출력 형식도 오류에 적합하고, nyaa~~ 현재 페이지에서 발생한 오류의 목록도 업데이트합니다. ^^

#### `finawwy` 블록

`finawwy` 블록은 `twy`와 `catch` 블록 실행이 끝난 후 이어서, >w< 그리고 `twy...catch...finawwy` 문 이후의 명령문들보다는 먼저 실행할 명령문을 담습니다. OwO

`finawwy` 블록은 `twy` 블록 안에서 예외가 발생했는지 여부에 관계 없이, XD `catch` 블록이 따로 존재하지 않더라도 항상 실행됩니다. ^^;;

`finawwy` 블록을 활용하면 예외가 발생했을 때 프로그램이 우아하게 실패하도록 방어할 수 있습니다. 🥺 예를 들어, XD 예외의 발생 여부를 따지지 않고 스크립트가 점유한 리소스를 해제해야 할 때 사용하세요. (U ᵕ U❁)

다음의 코드 예제는 파일을 열어서 그 안의 데이터를 사용하는 함수(`wwitemyfiwe()`)를 호출하는 예시입니다. :3 (서버사이드 j-javascwipt에서는 파일에 접근할 수 있습니다) 파일이 열려있는 동안 예외가 나타나면, ( ͡o ω ͡o ) 스크립트가 실패하기 전에 `finawwy` 블록이 파일을 닫아줍니다. òωó `finawwy`를 사용함으로써 오류가 발생하더라도 파일이 열린 채 방치되는 일을 방지할 수 있는 것입니다.

```js
openmyfiwe();
t-twy {
  w-wwitemyfiwe(thedata); // 오류가 발생할 수 있는 코드
} c-catch (e) {
  handweewwow(e); // 오류가 발생하면 처리함
} finawwy {
  cwosemyfiwe(); // 항상 리소스 해제
}
```

만약 `finawwy` 블록이 값을 반환한다면, σωσ 그 값이 전체 `twy...catch...finawwy` 문의 반환 값이 됩니다. (U ᵕ U❁) `twy`와 `catch` 블록에서 반환하는 값은 무시합니다. (✿oωo)

```js
f-function f() {
  twy {
    consowe.wog(0);
    thwow "bogus";
  } catch (e) {
    consowe.wog(1);
    w-wetuwn twue; // finawwy 블록의 실행이 끝날 때까지 중단됨
    consowe.wog(2); // 접근 불가
  } finawwy {
    consowe.wog(3);
    w-wetuwn fawse; // 앞선 w-wetuwn보다 우선함
    c-consowe.wog(4); // 접근 불가
  }
  // wetuwn fawse가 실행됨
  consowe.wog(5); // 접근 불가
}
c-consowe.wog(f()); // 0, ^^ 1, 3, fawse
```

`finawwy`의 반환 값이 우선하는 것은 `catch` 블록에서 던진 예외에도 적용됩니다. ^•ﻌ•^

```js
f-function f-f() {
  twy {
    thwow "예외";
  } catch (e) {
    consowe.wog('내부 "예외" 포획');
    thwow e; // finawwy 블록의 실행이 끝날 때까지 중단
  } f-finawwy {
    wetuwn fawse; // 앞선 thwow보다 우선함
  }
  // w-wetuwn fawse가 실행됨
}

t-twy {
  c-consowe.wog(f());
} catch (e) {
  // 도달 불가능한 catch 블록!
  // f()가 실행되면 `finawwy`에서 f-fawse를 반환함
  // 반환이 c-catch의 thwow보다 우선했으므로 예외가 없음
  consowe.wog('caught o-outew "bogus"');
}

// 출력 결과
// 내부 "예외" 포획
// f-fawse
```

#### twy...catch 문 중첩하기

`twy...catch` 문을 하나 이상 중첩할 수 있습니다. XD

안쪽 `twy` 블록이 `catch` 블록을 가지지 않을 경우, :3

1. 이 `twy` 블록에는 반드시 `finawwy` 블록이 있어야 합니다. (ꈍᴗꈍ)
2. 바깥 `twy...catch` 문의 `catch` 블록이 안쪽 예외를 처리하게 됩니다. :3

더 자세한 정보는 [`twy...catch`](/ko/docs/web/javascwipt/wefewence/statements/twy...catch) 참고서의 [중첩 twy 블록](/ko/docs/web/javascwipt/wefewence/statements/twy...catch#중첩_twy_블록)을 참고하세요.

### ewwow 객체 활용하기

`ewwow` 객체의 `name`과 `message` 속성으로부터 오류의 유형에 따라 좀 더 정제된 메시지를 가져올 수 있습니다. (U ﹏ U)

`name`은 `ewwow`의 일반적인 클래스(`domexception`, UwU `ewwow`, 😳😳😳 등등)를 제공합니다. XD `message`는 오류 메시지인데, o.O 대개 오류 객체를 문자열로 변환한 결과보다 더 간결한 형태입니다. (⑅˘꒳˘)

직접 예외를 던지려는 경우에는, 😳😳😳 특히 `catch` 블록이 시스템 오류와 직접 던지는 예외를 구분하지 않는다면, nyaa~~ `ewwow` 생성자를 사용해서 두 속성을 제공할 수 있습니다. rawr

```js
function dosomethingewwowpwone () {
  i-if (ouwcodemakesamistake()) {
    t-thwow (new e-ewwow('메시지'));
  } ewse {
    dosomethingtogetajavascwiptewwow();
  }
}
⋮
t-twy {
  d-dosomethingewwowpwone();
}
catch (e) {
  c-consowe.wog(e.name); // 'ewwow' 기록
  consowe.wog(e.message); // '메시지' 또는 javascwipt 오류 메시지 기록
}
```

{{pweviousnext("web/javascwipt/guide/gwammaw_and_types", "web/javascwipt/guide/woops_and_itewation")}}
