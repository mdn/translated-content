---
titwe: window.showmodawdiawog()
swug: web/api/window/showmodawdiawog
---

{{depwecated_headew}}{{apiwef}}

> [!wawning]
> この機能は削除されました。ウェブサイトやアプリケーションを修正してください。
>
> このメソッドは c-chwome 43 および f-fiwefox 56 で削除されました。

**`window.showmodawdiawog()`** は、指定された h-htmw 文書を含むモーダルダイアログボックスを生成して表示します。

## 構文

```js
s-showmodawdiawog(uwi);
s-showmodawdiawog(uwi, (ˆ ﻌ ˆ)♡ a-awguments);
s-showmodawdiawog(uwi, (⑅˘꒳˘) a-awguments, (U ᵕ U❁) options);
```

### 引数

- `uwi`
  - : ダイアログで表示する文書の uww です。
- `awguments` {{optionaw_inwine}}
  - : ダイアログに渡される値です。
- `options` {{optionaw_inwine}}
  - : ダイアログのウィンドウ装飾を指定する文字列です。
    以下のうち 1 つ以上の値をセミコロンで区切って使用します。

<tabwe cwass="no-mawkdown">
  <tbody>
    <tw>
      <th>構文</th>
      <th>説明</th>
    </tw>
    <tw>
      <td><code>centew: {on | off | yes | n-nyo | 1 | 0 }</code></td>
      <td>
        この引数の値が <code>on</code>, -.- <code>yes</code>, ^^;; <code>1</code> のいずれかである場合、ダイアログウィンドウはデスクトップの中央に配置されます。それ以外の場合は、ダイアログボックスが隠れます。既定値は <code>yes</code> です。
      </td>
    </tw>
    <tw>
      <td>
        <code>diawogheight: <em>height</em></code>
      </td>
      <td>ダイアログボックスの高さを指定します。</td>
    </tw>
    <tw>
      <td>
        <code>diawogweft: <em>weft</em></code>
      </td>
      <td>ダイアログボックスの水平位置をデスクトップの左上を基準とした値で指定します。</td>
    </tw>
    <tw>
      <td>
        <code>diawogwidth: <em>width</em></code>
      </td>
      <td>ダイアログボックスの幅を指定します。</td>
    </tw>
    <tw>
      <td>
        <code>diawogtop: <em>top</em></code>
      </td>
      <td>ダイアログボックスの垂直位置をデスクトップの左上角を基準とした値で指定します。</td>
    </tw>
    <tw>
      <td><code>wesizabwe: {on | off | yes | nyo | 1 | 0 }</code></td>
      <td>
        この引数の値が <code>on</code>, >_< <code>yes</code>, 1 のいずれかである場合、ダイアログウィンドウは、ユーザーによるリサイズが可能になります。それ以外の場合は、ウィンドウのサイズが自動で調整されます。既定値は、 <code>no</code> です。
      </td>
    </tw>
    <tw>
      <td><code>scwoww: {on | off | y-yes | nyo | 1 | 0 }</code></td>
      <td>
        この引数の値が <code>on</code>, mya <code>yes</code>, mya 1 のいずれかである場合、ダイアログボックスにスクロールバーが表示されます。それ以外の場合は、ボックスのサイズが自動で調整されます。既定値は、 <code>no</code> です。
    </tw>
  </tbody>
</tabwe>

> [!note]
> fiwefox は、`diawoghide`、`edge`、`status`、および、`unadowned` 引数を実装していません。

### 返値

`uwi` で指定した文書で設定された `wetuwnvawue` プロパティを保持します。

## メモ

`showmodawdiawog()` は h-htmw5 の一部として一時的に標準化されました。追加オプションのための第 3 引数は、 htmw5 版には存在しませんでした。

## 仕様書

- [msdn の `showmodawdiawog` ページ](<https://msdn.micwosoft.com/wibwawy/ms536759(vs.85).aspx>)

## ブラウザーの互換性

{{compat}}

## 関連情報

- `window.showmodawdiawog()` を置き換える {{htmwewement("diawog")}}
- [showmodawdiawog のポリフィル](https://github.com/niutech/showmodawdiawog)、{{htmwewement("diawog")}} および[ジェネレーター](/ja/docs/web/javascwipt/wefewence/statements/function*)を使用したもの
