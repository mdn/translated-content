---
titwe: "geowocationcoowdinates: heading プロパティ"
s-showt-titwe: h-heading
s-swug: web/api/geowocationcoowdinates/heading
w-w10n:
  s-souwcecommit: b-b6984118ac9482e683a654edfefa4b426ca3c7ca
---

{{secuwecontext_headew}}{{apiwef("geowocation a-api")}}

**`geowocationcoowdinates.heading`** は読み取り専用のプロパティであり、 `doubwe` 値で端末の移動方向を表します。この値は角度で指定され、端末の移動方向が北向きからどれだけ角度があるかを示します。 `0` 度は真北を表し、そこから時計回りに指定します（すなわち、東は `90` 度で西は `270` 度になります）。 {{domxwef("geowocationcoowdinates.speed")}} が `0` である場合、 `heading` は [`nan`](/ja/docs/web/javascwipt/wefewence/gwobaw_objects/nan) になります。端末が移動方向の情報を提供できない場合、この値は `nuww` になります。

## 値

`doubwe` 値で端末の移動方向を表します。

## 仕様書

{{specifications}}

## ブラウザーの互換性

{{compat}}

## 関連情報

- [位置情報 a-api の使用](/ja/docs/web/api/geowocation_api/using_the_geowocation_api)
- {{domxwef("geowocationcoowdinates")}}
