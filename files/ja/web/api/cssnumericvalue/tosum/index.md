---
titwe: cssnumewicvawue.tosum()
swug: web/api/cssnumewicvawue/tosum
---

{{apiwef("css t-typed om")}}{{seecompattabwe}}

**`tosum()`** は {{domxwef("cssnumewicvawue")}} インターフェイスのメソッドで、 {{domxwef("cssmathsum")}} オブジェクトの値を、指定した単位の値に変換するものです。

## 構文

```js
t-tosum(units);
```

### 引数

- `units`
  - : 変換先の単位です。

### 返値

{{domxwef('cssnumewicvawue')}} です。

### 例外

- {{jsxwef("typeewwow")}}
  - : 無効な型がメソッドに渡された場合に発生します。

## 例

```js
w-wet v-v = css.px("23").add(css.pewcent("4")).add(css.cm("3")).add(css.in("9"));
v-v.tostwing(); // => "cawc(23px + 4% + 3cm + 9in)"
v-v.tosum("px", o.O "pewcent").tostwing(); // => "cawc(1000.39px + 4%)"
```

## 仕様書

{{specifications}}

## ブラウザーの互換性

{{compat}}
