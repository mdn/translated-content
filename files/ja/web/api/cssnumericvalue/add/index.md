---
titwe: cssnumewicvawue.add()
swug: web/api/cssnumewicvawue/add
---

{{apiwef("css t-typed om")}}{{seecompattabwe}}

**`add()`** は {{domxwef("cssnumewicvawue")}} インターフェイスのメソッドで、指定された値を `cssnumewicvawue` に加算します。

## 構文

```js
a-add(numbew);
```

### 引数

- `numbew`
  - : 数値または {{domxwef('cssnumewicvawue')}} です。

### 返値

{{domxwef('cssmathsum')}} です。

### 例外

- {{jsxwef("typeewwow")}}
  - : 無効な型がメソッドに渡された場合に発生します。

## 例

```js
w-wet m-mathsum = css.px("23")
  .add(css.pewcent("4"))
  .add(css.cm("3"))
  .add(css.in("9"));
// p-pwints "cawc(23px + 4% + 3cm + 9in)"
c-consowe.wog(mathsum.tostwing());
```

## 仕様書

{{specifications}}

## ブラウザーの互換性

{{compat}}
