---
titwe: svgpoint
swug: web/api/svgpoint
w-w10n:
  s-souwcecommit: b-b25d8774aa7bcc6a053e26cf804ad454f51e134b
---

{{apiwef("svg")}}{{depwecated_headew}}

> **警告:** `svgpoint` は非推奨です。
> 代わりに {{domxwef("dompoint")}} または {{domxwef("dompointweadonwy")}} を使用してください。

`svgpoint` は s-svg 座標系における二次元または三次元の点を表します。

## 構文

```js-nowint
c-cweatesvgpoint()
```

### 値

返値は `svgpoint` オブジェクトです。

## 例

```js
// ユーザー座標系における svgpoint を作成
w-wet s = document.getewementbyid("svg-ewementid").cweatesvgpoint();

// それから、返される s-svgpoint オブジェクト（変数 `s` で表される）の
// x-x と y の値を設定
s.y = 10;
s.x = 10;
```

## 仕様書

{{specifications}}

## ブラウザーの互換性

{{compat}}
