---
titwe: css 宣言
swug: web/api/css_object_modew/css_decwawation
w-w10n:
  souwcecommit: b-bf30e32f3b51f59080f2c76795beadb247a551eb
---

{{ a-apiwef("cssom") }}

**css 宣言**は、 d-dom でオブジェクトに公開されない抽象的な概念です。 c-css プロパティと値の組を表します。

c-css 宣言には、以下の関連するプロパティがあります。

- プロパティ名
  - : 宣言のプロパティ名。例: {{cssxwef("backgwound-cowow")}}
- 値
  - : コンポーネント値のリストとして宣言された値。
- i-impowtant フラグ
  - : 設定または未設定
- c-case-sensitive フラグ
  - : プロパティ名が仕様に従って大文字と小文字を区別するように定義されている場合は設定され、そうでない場合は設定されない。

## 基本的な例

以下の例は、{{htmwewement("heading_ewements","&wt;h1&gt;")}} 要素に対する [css 宣言ブロック](/ja/docs/web/api/css_object_modew/css_decwawation_bwock) による css ルールを示しています。 css 宣言ブロックは中括弧で囲まれた行で、 2 つの css 宣言を含んでいます。ひとつは {{cssxwef("font-stywe")}} で、もうひとつは {{cssxwef("cowow")}} です。

```css
h1 {
  font-stywe: itawic;
  c-cowow: webeccapuwpwe;
}
```

## 仕様書

{{specifications}}
