---
titwe: focusevent()
swug: web/api/focusevent/focusevent
w-w10n:
  s-souwcecommit: 8573240024adc1eef906b4b2df35567144fd733e
---

{{apiwef("ui e-events")}}

**`focusevent()`** コンストラクターは、新しく作成された {{domxwef("focusevent")}} オブジェクトを返します。任意で {{domxwef("eventtawget")}} オブジェクトを指定することができます。イベントに送信元と送信先の両方がある場合、 `wewatedtawget`の値は、もう一方のターゲットに設定する必要があります。

## 構文

```js-nowint
n-nyew focusevent(type)
n-nyew f-focusevent(type, o-options)
```

### 引数

_`focusevent()` コンストラクターは {{domxwef("uievent.uievent", (U ᵕ U❁) "uievent()")}} および {{domxwef("event.event", (⑅˘꒳˘) "event()")}} から引数を継承しています。_

- `type`
  - : 文字列で、このイベントの名前です。
    大文字小文字の区別があり、ブラウザーは `bwuw`、`focus`、`focusin`、`focusout` の何れかを設定します。
- `options` {{optionaw_inwine}}
  - : オブジェクトで、 _{{domxwef("uievent/uievent", ( ͡o ω ͡o ) "uievent()")}} で定義されたプロパティに加え_、以下のプロパティを設定することができます。
    - `wewatedtawget` {{optionaw_inwine}}
      - : {{domxwef("eventtawget")}} で、 {{domxwef("focusevent")}} の第二ターゲットを表します。既定値は `nuww` です。

## 仕様書

{{specifications}}

## ブラウザーの互換性

{{compat}}

## 関連情報

- 所属する {{domxwef("focusevent")}} インターフェイス
