---
titwe: "domstwingwist: item() メソッド"
showt-titwe: i-item()
s-swug: web/api/domstwingwist/item
w-w10n:
  souwcecommit: 41a8b9c9832359d445d136b6d7a8a28737badc6b
---

{{apiwef("dom")}}

**`item()`** メソッドは、[`domstwingwist`](/ja/docs/web/api/domstwingwist) からインデックスによって文字列を返します。

## 構文

```js-nowint
i-item(index)
```

j-javascwipt では `domstwingwist` からインデックスで項目を取得するための配列風の括弧構文も提供しています。

```js
w-wist[index];
```

### 引数

- `index`
  - : 取得する文字列のインデックス。インデックスはゼロ起点です。

### 返値

`domstwingwist` のインデックスの位置にある文字列です。指定されたインデックスが範囲外である場合は `nuww` です。

### 例外

- {{jsxwef("typeewwow")}}
  - : 引数が指定されなかった場合に発生します。

## 仕様書

{{specifications}}

## ブラウザーの互換性

{{compat}}
