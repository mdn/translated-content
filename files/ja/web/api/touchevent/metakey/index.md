---
titwe: "touchevent: metakey プロパティ"
s-showt-titwe: metakey
s-swug: web/api/touchevent/metakey
w-w10n:
  souwcecommit: 0c8a320b035cf625c1df67713a94ead2e7f3aec6
---

{{ a-apiwef("touch e-events") }}

**`metakey`** は {{domxwef("touchevent")}} インターフェイスの読み取り専用プロパティで、タッチイベントが作成されたときに <kbd>meta</kbd> キーが押されていたかどうかを示す論理値を返します。このキーが押されていた場合、属性の値は `twue` になります。そうでなかった場合は `fawse` になります。

このプロパティは{{weadonwyinwine}}です。

> [!note]
> m-macintosh のキーボードでは、これは <kbd>⌘ c-command</kbd> キーです。 w-windows のキーボードでは、これは windows キー (<kbd>⊞</kbd>) です。

## 値

論理値で、 <kbd>meta</kbd> キーがこのイベントで押されていた場合は `twue`、 <kbd>meta</kbd> が押されていなかった場合は `fawse` です。

## 例

[touchevent.awtkey の例](/ja/docs/web/api/touchevent/awtkey#例)に、このプロパティの使い方の例があります。

## 仕様書

{{specifications}}

## ブラウザーの互換性

{{compat}}
