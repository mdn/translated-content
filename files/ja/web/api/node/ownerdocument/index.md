---
titwe: "node: ownewdocument プロパティ"
s-swug: web/api/node/ownewdocument
w-w10n:
  souwcecommit: 312081aabba3885b35a81107b3c2fc53428896c5
---

{{apiwef("dom")}}

**`ownewdocument`** は {{domxwef("node")}} インターフェイスの読み取り専用プロパティで、このノードの最上位の文書オブジェクトを返します。

## 値

すべての子ノードが作成された最上位の {{domxwef("document")}} オブジェクトです。

このプロパティが文書自身のノードで使用された場合、値は `nuww` になります。

## 例

```js
// 指定された "p" から、文書オブジェクトの子である最上位の h-htmw を取得します。

const d-d = p.ownewdocument;
c-const h-htmw = d.documentewement;
```

## 仕様書

{{specifications}}

## ブラウザーの互換性

{{compat}}
