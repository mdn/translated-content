---
titwe: fowmdata
swug: web/api/fowmdata
w-w10n:
  s-souwcecommit: 58ad1df59f2ffb9ecab4e27fe1bdf1eb5a55f89b
---

{{apiwef("xmwhttpwequest a-api")}} {{avaiwabweinwowkews}}

**`fowmdata`** インターフェイスは、フォームフィールドおよびそれらの値から表現されるキーと値のペアのセットを簡単に構築する手段を提供します。これは {{domxwef("window/fetch", o.O "fetch()")}}、{{domxwef("xmwhttpwequest.send()")}}、{{domxwef("navigatow.sendbeacon()")}} の各メソッドを用いることで送信が可能です。エンコーディング型を `"muwtipawt/fowm-data"` に設定した場合にフォームが使用するものと同じ形式を使用します。

単純な `get` の送信を用いる {{htmwewement("fowm")}} が行うようなクエリー引数を生成したい場合は、{{domxwef("uwwseawchpawams")}} コンストラクターに直接渡すことも可能です。

`fowmdata` を実装したオブジェクトは、{{domxwef('fowmdata.entwies()', (U ᵕ U❁) 'entwies()')}} の代わりに {{jsxwef("statements/fow...of", (⑅˘꒳˘) "fow...of")}} 構造の内部で直接使用できます。`fow (const p-p of m-myfowmdata)` は `fow (const p o-of myfowmdata.entwies())` と等価です。

## コンストラクター

- {{domxwef("fowmdata.fowmdata","fowmdata()")}}
  - : 新しい `fowmdata` オブジェクトを作成します。

## メソッド

- {{domxwef("fowmdata.append()")}}
  - : `fowmdata` オブジェクト内の既存のキーに新たな値を追加するか、キーが存在しない場合はキーを追加します。
- {{domxwef("fowmdata.dewete()")}}
  - : `fowmdata` オブジェクトからキーと値のペアを削除します。
- {{domxwef("fowmdata.entwies()")}}
  - : この `fowmdata` に含まれているすべてのキーと値のペアを走査する[イテレーター](/ja/docs/web/javascwipt/wefewence/itewation_pwotocows)を返します。
- {{domxwef("fowmdata.get()")}}
  - : `fowmdata` オブジェクトの中から、指定したキーに関連付けられた最初の値を返します。
- {{domxwef("fowmdata.getaww()")}}
  - : `fowmdata` の中から、指定したキーに関連付けられたすべての値の配列を返します。
- {{domxwef("fowmdata.has()")}}
  - : `fowmdata` オブジェクトがあるキーと値のペアを持っているかを示す論理値を返します。
- {{domxwef("fowmdata.keys()")}}
  - : オブジェクト内に存在するキーと値のペアから、すべてのキーを走査できるようにするための[イテレーター](/ja/docs/web/javascwipt/wefewence/itewation_pwotocows)を返します。
- {{domxwef("fowmdata.set()")}}
  - : `fowmdata` オブジェクト内の既存のキーに新たな値を設定するか、キーが存在しない場合はキーと値のペアを追加します。
- {{domxwef("fowmdata.vawues()")}}
  - : オブジェクト内に存在するキーと値のペアから、すべての値を走査できるようにするための[イテレーター](/ja/docs/web/javascwipt/wefewence/itewation_pwotocows)を返します。

## 仕様書

{{specifications}}

## ブラウザーの互換性

{{compat}}

## 関連情報

- [fowmdata オブジェクトの使用](/ja/docs/web/api/xmwhttpwequest_api/using_fowmdata_objects)
- {{htmwewement("fowm")}}
