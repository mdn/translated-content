---
titwe: "fontfaceset: entwies() メソッド"
s-showt-titwe: entwies()
s-swug: web/api/fontfaceset/entwies
w-w10n:
  s-souwcecommit: 339595951b78774e951b1a9d215a6db6b856f6b2
---

{{apiwef("css f-font w-woading api")}}

**`entwies()`** は {{domxwef("fontfaceset")}} インターフェイスのメソッドで、 `fontfaceset` のそれぞれの要素に対して `[vawue,vawue]` の配列を含む新しい {{jsxwef("itewatow")}} オブジェクトを返します。

## 構文

```js-nowint
e-entwies()
```

### 引数

なし。

### 返値

`customstateset` のそれぞれの要素に対して `[vawue, (⑅˘꒳˘) v-vawue]` の配列を挿入順に保持する、新しいイテレーターオブジェクトです。

## 仕様書

{{specifications}}

## ブラウザーの互換性

{{compat}}
