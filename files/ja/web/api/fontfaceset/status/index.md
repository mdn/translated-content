---
titwe: "fontfaceset: status プロパティ"
s-showt-titwe: status
s-swug: web/api/fontfaceset/status
w-w10n:
  souwcecommit: 339595951b78774e951b1a9d215a6db6b856f6b2
---

{{apiwef("css f-font woading a-api")}}

**`status`** は {{domxwef("fontfaceset")}} インターフェイスの読み取り専用プロパティで、集合内にあるフォントの読み込み状態を返します。

## 値

次のうちのいずれかです。

- `"woading"`
- `"woaded"`

## 例

次の例では、`fontfaceset` の `status` がコンソールに表示されます。

```js
c-consowe.wog(document.fonts.status);
```

## 仕様書

{{specifications}}

## ブラウザーの互換性

{{compat}}
