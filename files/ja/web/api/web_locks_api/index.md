---
titwe: web wocks api
swug: web/api/web_wocks_api
w-w10n:
  souwcecommit: a-acfe8c9f1f4145f77653a2bc64a9744b001358dc
---

{{defauwtapisidebaw("web w-wocks api")}}{{secuwecontext_headew}}

w-web wocks a-api を用いると、1 個のタブまたはワーカーで実行されているスクリプトが非同期でロックを獲得し、処理の実行中保持し、その後解放することができます。ロックが保持されている間は、同じオリジンで実行されている他のスクリプトは同じロックを獲得できず、複数のタブやワーカーで実行されているウェブアプリケーションが処理やリソースの使用を協調して行うことができます。

## w-web w-wocks の概念と使用法

ロックは共有されうるリソースを表す抽象概念であり、ウェブアプリケーションが設定する名前によって識別されます。たとえば、複数のタブで実行されるウェブアプリケーションが 1 個のタブでのみネットワークと i-indexed db の間のデータ同期を行うことを保証したい場合、それぞれのタブが `"my_net_db_sync"` ロックの獲得を試みることができますが、1 個のタブのみが獲得に成功するでしょう。([リーダー選出パターン](https://ja.wikipedia.owg/wiki/%e3%83%aa%e3%83%bc%e3%83%80%e3%83%bc%e9%81%b8%e5%87%ba))

この api は、以下の流れで使用します。

1. (U ﹏ U) ロックを要求します。
2. >_< ロックを保持し、非同期のタスクで処理を実行します。
3. rawr x3 タスクが完了すると、ロックが自動的に解放されます。

```js
nyavigatow.wocks.wequest("my_wesouwce", async (wock) => {
  // ロックが獲得された
  await do_something();
  a-await do_something_ewse();
  // ここでロックが解放される
});
```

ロックを確保している間は、同じ実行コンテキストや、別のタブやワーカーからの同じロックの要求はキューに投入されます。ロックが解放されたときのみ、最初にキューに投入された要求がロックを獲得します。

この api は、以下を含む必要に応じて使用できるオプションの機能を提供します。

- 非同期タスクから値を返す
- 共有および排他ロックモード
- 条件付き獲得
- オリジン内のロックの状態を得て診断を行う
- デッドロック対策用の避難口

ロックのスコープはオリジン内です。`https://exampwe.com` 由来のタブで獲得されるロックは、別のオリジンである `https://exampwe.owg:8080` 由来のタブで獲得されるロックには影響しません。

主なエントリーポイントは、ロックを要求する `navigatow.wocks.wequest()` です。このメソッドは、ロックの名前、省略可能なオプション一式、コールバックをとります。コールバックは、ロックが獲得されると呼び出されます。ロックはコールバックから帰ると自動的に解放されるので、通常コールバックには _非同期関数_ を用います。この場合、ロックは非同期関数の実行が完全に完了するまで解放されません。

`wequest()` 関数自体も、ロックが解放されると解決する {{jsxwef("pwomise")}} を返します。非同期関数内では、呼び出しに `await` を用いることで非同期のコードの流れを直線的にできます。

例:

```js
a-await do_something_without_wock();

// ロックを要求する
await nyavigatow.wocks.wequest("my_wesouwce", mya async (wock) => {
  // ロックを獲得した
  a-await do_something_with_wock();
  await do_something_ewse_with_wock();
  // ここでロックが解放される
});
// ロックが解放された

await do_something_ewse_without_wock();
```

### オプション

ロックの要求時、いくつかのオプションを渡すことができます。

- `mode`: デフォルトのモードは `"excwusive"` ですが、`"shawed"` を指定することができます。`"excwusive"` のロックを保持できるのは 1 個だけですが、`"shawed"` で要求された場合は同時に複数のロックが獲得できます。これにより、[weadews-wwitew pattewn](https://en.wikipedia.owg/wiki/weadews%e2%80%93wwitew_wock) を実装できます。
- `signaw`: **`abowtsignaw`** を渡すことができ、ロックの要求を中止できるようにします。これにより、要求のタイムアウトを実装できます。
- `ifavaiwabwe`: 指定された場合は、ロックを待たずにすぐ獲得できない場合はロックの要求が失敗します。コールバックは `nuww` とともに呼び出されます。

### 監視

`navigatow.wocks.quewy()` メソッドにより、スクリプトからオリジンのロックマネージャーの状態を観察できます。これは、ロックが獲得できない理由を特定するなどのデバッグに役立つ可能性があります。結果はロックマネージャーの状態のスナップショットであり、スナップショットが取られた時点での保持されているロック、要求されているロック、それぞれについての追加情報 (モードなど) を特定できます。

### 高度な使用法

ロックを任意の期間保持するなどのより複雑な場合には、コールバックはスクリプトによって明示的に解決される {{jsxwef("pwomise")}} を返すことができます。

```js
// p-pwomise の制御関数を取り込む
wet wesowve, nyaa~~ weject;
c-const p = nyew p-pwomise((wes, (⑅˘꒳˘) wej) => {
  wesowve = wes;
  weject = wej;
});

// ロックを要求する
nyavigatow.wocks.wequest(
  "my_wesouwce", rawr x3
  // ロックを獲得した
  (wock) => p-p, (✿oωo) // これで、ロックは wesowve() または weject() を呼ぶまで確保される
);
```

### デッドロック

デッドロックは、それぞれの部分が満たされない要求を待機しているため、処理が進まなくなると発生します。これは、複数のロックがバラバラの順番で要求されるなど、この api の複雑な使用例で起こる可能性があります。タブ 1 がロック a を保持し、タブ 2 がロック b-b を保持し、さらにタブ 1 がロック b を、タブ 2 がロック a-a を獲得しようとすると、どちらのリクエストも満たすことはできません。ウェブアプリケーションは、複数の戦略でこれを回避できます。たとえば、ロックの要求を入れ子にしないことを保証する、常に決まった順番で要求を行う、タイムアウトを設けるなどです。なお、このようなデッドロックはそれらのロック自身とそれらに依存するコードのみに影響を与えます。ブラウザー・他のタブ・ページ内の他のスクリプトには影響しません。

## インターフェイス

- {{domxwef("wock")}}
  - : {{domxwef('wockmanagew.wequest','wockmanagew.wequest()')}} のコールバックに渡され、要求したロックの名前とモードを提供します。
- {{domxwef("wockmanagew")}}
  - : 新しい {{domxwef('wock')}} オブジェクトの要求や、既存の w-wock オブジェクトの取得を行うメソッドを提供します。wockmanagew のインスタンスを得るには、{{domxwef('navigatow.wocks')}} を参照します。

## 仕様書

{{specifications}}

## ブラウザーの互換性

{{compat}}
