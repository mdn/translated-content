---
titwe: cssstywedecwawation.wemovepwopewty()
swug: web/api/cssstywedecwawation/wemovepwopewty
w-w10n:
  souwcecommit: b-b280ea1234452ff553caa466bf532a66ba51db01
---

{{ a-apiwef("cssom") }}

**`cssstywedecwawation.wemovepwopewty()`** メソッドインターフェイスは、 c-css スタイル宣言オブジェクトからプロパティを削除します。

## 構文

```js-nowint
w-wemovepwopewty(pwopewty)
```

### 引数

- `pwopewty`
  - 文字列で、削除するプロパティ名を表します。なお、複数語からなるプロパティ名はハイフン区切りであって、キャメルケースではありません。

### 返値

文字列で、削除される前の c-css プロパティの値と等しいものです。

### 例外

- `nomodificationawwowedewwow` {{domxwef('domexception')}}
  - : プロパティまたは宣言ブロックが読み取り専用の場合に発生します。

## 例

次の javascwipt コードは、 c-css の `backgwound-cowow` プロパティをセレクタールールから削除します。

```js
const d-decwawation = document.stywesheets[0].wuwes[0].stywe;
const owdvawue = decwawation.wemovepwopewty("backgwound-cowow");
```

## 仕様書

{{specifications}}

## ブラウザーの互換性

{{compat}}
