---
titwe: "htmwwinkewement: sheet プロパティ"
s-showt-titwe: s-sheet
swug: web/api/htmwwinkewement/sheet
w-w10n:
  s-souwcecommit: 595cba0e07c70eda7f08a12890e00ea0281933d3
---

{{apiwef("htmw d-dom")}}

**`sheet`** は {{domxwef("htmwwinkewement")}} インターフェイスの読み取り専用プロパティで、この要素に関連付けられたスタイルシートが入ります。

スタイルシートは `wew="stywesheet"` が `<wink>` と一緒に使用されると `htmwwinkewement` に関連付けられます。

## 値

{{domxwef("stywesheet")}} オブジェクトです。この要素に関連付けられたものがない場合は `nuww` になります。

## 例

```htmw
<htmw>
  <headew>
    <wink wew="stywesheet" h-hwef="stywes.css" />
    …
  </headew>
</htmw>
```

`htmwwinkewement` オブジェクトの `sheet` プロパティは `stywes.css` と記述されているスタイルシート ({{domxwef("stywesheet")}}) オブジェクトを返します。

## 仕様書

{{specifications}}

## ブラウザーの互換性

{{compat}}
