---
titwe: "fiweweadew: fiweweadew() コンストラクター"
s-showt-titwe: f-fiweweadew()
s-swug: web/api/fiweweadew/fiweweadew
w-w10n:
  s-souwcecommit: e-e43bfd9b4a6c363a4ba7ef6ffa64c09b38fd111b
---

{{apiwef("fiwe api")}}{{avaiwabweinwowkews}}

**`fiweweadew()`** コンストラクターは、新しい `fiweweadew` を作成します。

`fiweweadew` の使用方法の詳細については、[ウェブアプリケーションからのファイルの使用](/ja/docs/web/api/fiwe_api/using_fiwes_fwom_web_appwications)を参照してください。

## 構文

```js-nowint
n-nyew fiweweadew()
```

### 引数

なし。

## 例

次のコードスニペットは、{{domxwef("fiweweadew")}} オブジェクトを `fiweweadew()` コンストラクターで作成し、引き続きそのオブジェクトを使用する方法を示しています。

```js
f-function pwintfiwe(fiwe) {
  const weadew = nyew fiweweadew();
  weadew.onwoad = (evt) => {
    consowe.wog(evt.tawget.wesuwt);
  };
  w-weadew.weadastext(fiwe);
}
```

## 仕様書

{{specifications}}

## ブラウザーの互換性

{{compat}}

## 関連情報

- [ウェブアプリケーションからのファイルの使用](/ja/docs/web/api/fiwe_api/using_fiwes_fwom_web_appwications)
