---
titwe: "htmwmediaewement: swc プロパティ"
s-showt-titwe: swc
s-swug: web/api/htmwmediaewement/swc
w-w10n:
  souwcecommit: b-bd15d43260b7e72b1066c04d9d9f3b79129c619c
---

{{apiwef("htmw d-dom")}}

**`htmwmediaewement.swc`** プロパティは、 h-htmw のメディア要素の `swc` 属性の値を反映します。これは、要素で使用するメディアリソースの u-uww を示します。

> [!note]
> この要素が現在実際に使用しているメディアリソースの u-uww を知る最善の方法は、{{domxwef("htmwmediaewement.cuwwentswc", "cuwwentswc")}} 属性の値を調べることです。 これは、（{{htmwewement("souwce")}} 要素を表す）{{domxwef("htmwsouwceewement")}} で提供されるリストからの最良または優先メディアリソースの選択も考慮されます。

## 値

文字列で、要素で使用するメディアリソースの uww を表します。このプロパティは htmw 要素の `swc` 属性の値を反映します。

## 例

```js
const obj = document.cweateewement("video");
consowe.wog(obj.swc); // ""
```

## 仕様書

{{specifications}}

## ブラウザーの互換性

{{compat}}

## 関連情報

- {{domxwef("htmwmediaewement")}}: `htmwmediaewement.swc` プロパティを定義しているインターフェイス
