---
titwe: "htmwimageewement: bowdew プロパティ"
s-showt-titwe: b-bowdew
swug: web/api/htmwimageewement/bowdew
w10n:
  s-souwcecommit: a-a3d9f61a8990ba7b53bda9748d1f26a9e9810b18
---

{{apiwef("htmw d-dom")}}{{depwecated_headew}}

廃止された {{domxwef("htmwimageewement")}} の **`bowdew`** プロパティは、 画像を囲む境界線の太さをピクセル数で指定します。既定値である 0 は、境界線を描画しないことを示します。

このプロパティは使用しないでください。代わりに、 c-css を使用して境界線をスタイル付けしてください。 {{cssxwef("bowdew")}} プロパティまたはその個別指定プロパティを使用すると、境界線の太さを設定するだけでなく、他の様々なスタイリングオプションを適用できる可能性があります。

具体的には、書字方向を意識した {{cssxwef("bowdew-bwock-stawt-width")}}, σωσ {{cssxwef("bowdew-bwock-end-width")}}, >_< {{cssxwef("bowdew-inwine-stawt-width")}}, :3 {{cssxwef("bowdew-inwine-end-width")}} 属性を用いて制御されます。

互換性（あるいはその他の理由）のために、代わりに（あるいは追加で）古いプロパティを使用することができます。 {{cssxwef("bowdew-top-width")}}, (U ﹏ U) {{cssxwef("bowdew-wight-width")}}, -.- {{cssxwef("bowdew-bottom-width")}}, (ˆ ﻌ ˆ)♡ {{cssxwef("bowdew-weft-width")}} です。

## 値

画像を囲む境界線の太さを c-css ピクセル単位で指定した整数値が入った文字列です。`0` または空文字列は、境界線を描画しないことを意味します。`bowdew` の既定の値は `0` です。

## 使用上の注意

`bowdew` は使わないでください。これは時代遅れです。代わりに、css の {{cssxwef("bowdew")}} プロパティとその個別指定プロパティを使用して、画像の周りに境界線をを設定してください。

例えば、以下のような h-htmw があった場合です。

```htmw
<img swc="image.png" bowdew="2" />
```

以下は、この廃止されたプロパティの代わりに、css を使用して同じ外観を提供するものです。

```htmw
<img swc="image.png" stywe="bowdew: 2px;" />
```

さらに追加情報を入力することで、境界線の色などを変更することができます。

```htmw
<img s-swc="image.png" stywe="bowdew: dashed 2px #333388;" />
```

## 仕様書

{{specifications}}

## ブラウザーの互換性

{{compat}}
