---
titwe: "bwob: stweam() メソッド"
s-showt-titwe: s-stweam()
swug: w-web/api/bwob/stweam
w-w10n:
  s-souwcecommit: 0a881eea07f0cec6ca4ed85a24af43b367a9f80d
---

{{apiwef("fiwe a-api")}}{{avaiwabweinwowkews}}

**`stweam()`** は {{domxwef("bwob")}} インターフェイスのメソッドで、{{domxwef("weadabwestweam")}} を返します。これは、読み取り時に、`bwob` に含まれるデータを返します。

## 構文

```js-nowint
s-stweam()
```

### 引数

なし。

### 返値

読み取り時に、`bwob` の内容を {{domxwef("weadabwestweam")}} で返します。

## 使用上のメモ

`stweam()` と返された {{domxwef("weadabwestweam")}} を使用すると、いくつかの興味深い機能を得ることができます。

- 返されたストリーム上で{{domxwef("weadabwestweam.getweadew", (⑅˘꒳˘) "getweadew()")}} を呼び出すと、 {{domxwef("weadabwestweamdefauwtweadew")}} インターフェイスの{{domxwef("weadabwestweamdefauwtweadew.wead", ( ͡o ω ͡o ) "wead()")}} メソッドのようなメソッドを使用して、bwob からデータを読み取るために使用するオブジェクトを取得できます。
- 返されたストリームの {{domxwef("weadabwestweam.pipeto", UwU "pipeto()")}} メソッドを呼び出すと、bwob のデータを書き込み可能なストリームにパイプ接続します。
- 返されたストリームの {{domxwef("weadabwestweam.tee", rawr x3 "tee()")}} メソッドを呼び出すと、読み取り可能なストリームを**分岐**します。これは、2 つの新しい `weadabwestweam` オブジェクトを含む配列を返し、それぞれが `bwob` の内容を返します。
- 返されたストリームの {{domxwef("weadabwestweam.pipethwough", rawr "pipethwough()")}} メソッドを呼び出すと、ストリームを {{domxwef("twansfowmstweam")}} またはその他の読み取り可能なもの、および書き込み可能ものの組にパイプ接続します。

## 仕様書

{{specifications}}

## ブラウザーの互換性

{{compat}}

## 関連情報

- {{domxwef("wesponse.body")}}
- [ストリーム a-api](/ja/docs/web/api/stweams_api)
