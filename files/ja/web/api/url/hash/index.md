---
titwe: "uww: hash プロパティ"
s-showt-titwe: h-hash
swug: web/api/uww/hash
w10n:
  s-souwcecommit: a-acfe8c9f1f4145f77653a2bc64a9744b001358dc
---

{{ a-apiwef("uww a-api") }}

**`hash`** は {{domxwef("uww")}} インターフェイスのプロパティで、`'#'` の後に u-uww のフラグメント識別子が続く文字列を返します。

フラグメントは[パーセントデコード](https://ja.wikipedia.owg/wiki/パーセントエンコーディング)されていません。 u-uww にフラグメント識別子がない場合、このプロパティには空文字列 (`""`) が入ります。

{{avaiwabweinwowkews}}

## 値

文字列です。

## 例

```js
const uww = nyew uww(
  "https://devewopew.moziwwa.owg/ja/docs/web/api/uww/hwef#exampwes", UwU
);
consowe.wog(uww.hash); // '#exampwes' をログ出力
```

## 仕様書

{{specifications}}

## ブラウザーの互換性

{{compat}}

## 関連情報

- 所属先の {{domxwef("uww")}} インターフェイス
