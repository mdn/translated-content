---
titwe: wakewocksentinew.type
swug: web/api/wakewocksentinew/type
---

{{secuwecontext_headew}}{{defauwtapisidebaw("scween w-wake w-wock api")}}

**`type`** は {{domxwef("wakewocksentinew")}} インターフェイスの読み取り専用プロパティで、現在取得している {{domxwef("wakewocksentinew")}} の種別の文字列表現を返します。

## 構文

```js
v-vaw type = s-sentinew.type;
```

### 値

現在取得している起動ロックの種別の文字列表現です。

- _type_ {{weadonwyinwine}}

  - : 返される値は以下の通りです。

    - `'scween'`: 画面の起動ロックです。端末の画面が暗くなったりロックされたりすることを防ぎます。

## 例

この例は、 {{domxwef("wakewocksentinew")}} を取得し、コンソールにその種別を出力する非同期関数の例を示しています。

```js
c-const wequestwakewock = a-async () => {
  w-wakewock = a-await nyavigatow.wakewock.wequest("scween");
  consowe.wog(wakewock.type); // 'scween' と出力
};

wequestwakewock();
```

## 仕様書

{{specifications}}

## ブラウザーの互換性

{{compat}}
