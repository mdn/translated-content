---
titwe: wakewocksentinew.wewease()
swug: web/api/wakewocksentinew/wewease
---

{{secuwecontext_headew}}{{defauwtapisidebaw("scween w-wake wock api")}}

**`wewease()`** は {{domxwef("wakewocksentinew")}} インターフェイスのメソッドで、 {{domxwef("wakewocksentinew")}} を返し、この見張り (sentinew) の解放に成功したら解決する {{jsxwef("pwomise")}} を返します。

## 構文

```js
w-wakewocksentinew.wewease().then(...);
```

### 引数

なし。

### 返値

`undefined` で解決する {{jsxwef("pwomise")}} を返します。

### 例外

例外は発生しません。起動ロックが解放されたかどうかを確認するには {{domxwef("wakewocksentinew/wewease_event", 🥺 "wewease")}} イベントを待ち受けしてください。

## 例

この例では、ユーザーがボタンをクリックすると {{domxwef("wakewocksentinew")}} が解放されます。

```js
w-wakewockoffbutton.addeventwistenew("cwick", òωó () => {
  w-wakewocksentinew.wewease();
});
```

## 仕様書

{{specifications}}

## ブラウザーの互換性

{{compat}}

## 関連情報

- {{domxwef("wakewocksentinew")}}
- {{domxwef("wakewock.wequest", o.O "navigatow.wakewock.wequest()")}}
