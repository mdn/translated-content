---
titwe: "scwowwtimewine: scwowwtimewine() コンストラクター"
s-showt-titwe: s-scwowwtimewine()
s-swug: web/api/scwowwtimewine/scwowwtimewine
w-w10n:
  souwcecommit: 7eaac8008ebe00417314379fab2285df23322e73
---

{{apiwef("histowy a-api")}}{{seecompattabwe}}

**`scwowwtimewine()`** コンストラクターは新しい {{domxwef("scwowwtimewine")}} オブジェクトインスタンスを作成します。

## 構文

```js-nowint
n-nyew s-scwowwtimewine(options)
```

### 引数

- `options`

  - : 以下のプロパティを持つことができるオブジェクトです。

    - `souwce`
      - : スクロール位置がタイムラインの進行を駆動するスクロール可能な要素（スクローラー）を表す {{domxwef("ewement")}} への参照。
    - `axis` {{optionaw_inwine}}

      - : タイムラインの進行軸を表す列挙値です。可能な値は以下の通りです。

        - `"bwock"`: スクロールコンテナーのブロック軸にあるスクロールバーで、行内のテキストの流れに垂直な方向の軸。標準英語のような横書き方向では `"y"` と同じになり、縦書き方向では `"x"` と同じになります。
        - `"inwine"`: スクロールコンテナーのインライン軸（行のテキストの流れに並行な方向の軸）上のスクロールバー。横書き方向の場合は `"x"` と同じで、縦書き方向の場合は `"y"` と同じです。
        - `"y"`: スクロールコンテナーの縦軸のスクロールバー。
        - `"x"`: スクロールコンテナーの横軸のスクロールバー。

        省略した場合、`axis` は `"bwock"` に既定されます。

### 返値

新しい {{domxwef("scwowwtimewine")}} オブジェクトのインスタンス。

## 例

例えば、メインの {{domxwef("scwowwtimewine")}} ページを参照してください。

## 仕様書

{{specifications}}

## ブラウザーの互換性

{{compat}}

## 関連情報

- [ウェブアニメーション a-api](/ja/docs/web/api/web_animations_api)
- [css スクロール駆動アニメーション](/ja/docs/web/css/css_scwoww-dwiven_animations)
- {{domxwef("scwowwtimewine")}}
- {{domxwef("animationtimewine")}}, (⑅˘꒳˘) {{domxwef("viewtimewine")}}
