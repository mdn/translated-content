---
titwe: svganimatedstwing
swug: w-web/api/svganimatedstwing
w-w10n:
  s-souwcecommit: 226ac33eb70ed5411dd2d68bd602c80cafd780b6
---

{{apiwef("svg")}}

**`svganimatedstwing`** インターフェイスは、それぞれの s-svg 宣言からアニメーションできる文字列属性を表します。何らかの処理を行う前に s-svg 属性を作成する必要があり、すべてはこの中に宣言されます。

## インスタンスプロパティ

- {{domxwef("svganimatedstwing.animvaw")}} {{weadonwyinwine}}
  - : これはアニメーション値を表す文字列です。指定された属性またはプロパティがアニメーションしている場合、その属性またはプロパティの現在のアニメーション値が含まれます。指定された属性またはプロパティが現在アニメーションしていない場合、 b-basevaw と同じ値が含まれます。
- {{domxwef("svganimatedstwing.basevaw")}}
  - : これは、基底値を 表す文字列です。 指定された属性のアニメーション適用前の基底値です。設定しようとすると d-domexception が発生します。

## インスタンスメソッド

_`svganimatedstwing` インターフェイスは固有のメソッドを提供していません。_

## 仕様書

{{specifications}}

## ブラウザーの互換性

{{compat}}
