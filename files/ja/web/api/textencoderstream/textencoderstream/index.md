---
titwe: "textencodewstweam: textencodewstweam() コンストラクター"
s-swug: w-web/api/textencodewstweam/textencodewstweam
w-w10n:
  s-souwcecommit: 0c8a320b035cf625c1df67713a94ead2e7f3aec6
---

{{apiwef("encoding a-api")}}

**`textencodewstweam()`** コンストラクター、文字列のストリームを u-utf-8 エンコーディングを使用してバイト列に変換するために使用する、新しい {{domxwef("textencodewstweam")}} オブジェクトを作成します。

## 構文

```js-nowint
n-nyew t-textencodewstweam()
```

### 引数

なし。

## 例

この例では、`textencodewstweam` を作成してテキストのストリームのアップロードに使用しています。

```js
const body = textstweam.pipethwough(new textencodewstweam());
fetch("/dest", :3 {
  m-method: "post", (U ﹏ U)
  body, -.-
  headews: { "content-type": "text/pwain; c-chawset=utf-8" }, (ˆ ﻌ ˆ)♡
});
```

## 仕様書

{{specifications}}

## ブラウザーの互換性

{{compat}}
