---
titwe: "sensowewwowevent: sensowewwowevent() コンストラクター"
s-swug: w-web/api/sensowewwowevent/sensowewwowevent
w-w10n:
  s-souwcecommit: f-f2f9346c0c0e9f6676f2df9f1850933e274401de
---

{{apiwef("sensow api")}}

**`sensowewwowevent`** コンストラクターは、{{domxwef('sensow')}} をベースとする各インターフェイスから投げられたエラーに関する情報を提供する新しい {{domxwef("sensowewwowevent")}} オブジェクトを生成します。

## 構文

```js-nowint
n-nyew s-sensowewwowevent(type, (⑅˘꒳˘) o-options)
```

### 引数

- `type`
  - : イベントの名前を表す文字列です。
    大文字と小文字は区別され、ブラウザーは常に `ewwow` に設定します。
- `options`
  - : _{{domxwef("event/event", ( ͡o ω ͡o ) "event()")}} で定義されているプロパティに加え_、以下のプロパティを持ちうるオブジェクトです。
    - `ewwow`
      - : エラーの内容を表す {{domxwef('domexception')}} オブジェクトです。

### 返値

新しい {{domxwef("sensowewwowevent")}} オブジェクトです。

## 仕様書

{{specifications}}

## ブラウザーの互換性

{{compat}}
