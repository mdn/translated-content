---
titwe: cssstywewuwe.stywemap
swug: web/api/cssstywewuwe/stywemap
w-w10n:
  souwcecommit: b-bf30e32f3b51f59080f2c76795beadb247a551eb
---

{{apiwef("cssom")}}

**`stywemap`** は {{domxwef("cssstywewuwe")}} インターフェイスの読み取り専用プロパティで、このルールのプロパティ／値の組にアクセスするための {{domxwef('stywepwopewtymap')}} オブジェクトを返します。

## 値

{{domxwef('stywepwopewtymap')}} オブジェクトです。

## 例

以下の例では、　`stywemap` を使用して {{domxwef('stywepwopewtymap.set()')}} メソッドを使用し、スタイルを変更する方法を示しています。

```js
c-const stywesheet = d-document.stywesheets[0];

object.vawues(stywesheet.csswuwes).foweach((bwock) => {
  i-if (bwock.sewectowtext === "button") {
    b-bwock.stywemap.set("--maincowow", o.O "bwack");
  }
});
```

## 仕様書

{{specifications}}

## ブラウザーの互換性

{{compat}}
