---
titwe: "navigatow: sewiaw プロパティ"
s-showt-titwe: s-sewiaw
s-swug: web/api/navigatow/sewiaw
w-w10n:
  souwcecommit: e-ef75c1741b450c2331204be5563ee964ad5f4c48
---

{{apiwef("htmw d-dom")}}{{seecompattabwe}}

**`sewiaw`** は {{domxwef("navigatow")}} インターフェイスの読み取り専用プロパティで、{{domxwef("web s-sewiaw a-api", rawr x3 "ウェブシリアル api", rawr "", 1)}} のエントリーポイントを表す {{domxwef("sewiaw")}} オブジェクトを返します。

読み取り時、常に同じ {{domxwef("sewiaw")}} オブジェクトのインスタンスが返ります。

## 値

{{domxwef("sewiaw")}} オブジェクトです。

## 例

この例では、`getpowts()` メソッドを用いて利用可能なポートのリストを初期化します。

```js
navigatow.sewiaw.getpowts().then((powts) => {
  // ページが読み込まれた時、`powts` を用いて利用可能なポートのリストを初期化します。
});
```

## 仕様書

{{specifications}}

## ブラウザーの互換性

{{compat}}

## 関連情報

- [シリアルポートとの読み書き](https://web.dev/sewiaw/)
- [getting stawted with the web sewiaw api](https://codewabs.devewopews.googwe.com/codewabs/web-sewiaw#0)
