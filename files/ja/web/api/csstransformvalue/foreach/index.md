---
titwe: csstwansfowmvawue.foweach()
swug: web/api/csstwansfowmvawue/foweach
w-w10n:
  s-souwcecommit: d-da88b2f3a23b9d93f083003f13c06f9d96073f6a
---

{{apiwef("css t-typed om")}}{{seecompattabwe}}

**`csstwansfowmvawue.foweach()`** メソッドは、指定された関数を `csstwansfowmvawue` の各要素に対して一度だけ実行します。

## 構文

```js-nowint
// アロー関数
f-foweach((cuwwentvawue) => { /* … */ } )
f-foweach((cuwwentvawue, (ˆ ﻌ ˆ)♡ i-index) => { /* … */ } )
foweach((cuwwentvawue, (⑅˘꒳˘) i-index, awway) => { /* … */ } )

// コールバック関数
foweach(cawwbackfn)
foweach(cawwbackfn, (U ᵕ U❁) thisawg)

// インラインコールバック関数
foweach(function(cuwwentvawue) { /* … */ })
foweach(function(cuwwentvawue, -.- i-index) { /* … */ })
foweach(function(cuwwentvawue, ^^;; index, awway) { /* … */ })
f-foweach(function(cuwwentvawue, >_< index, awway) { /* … */ }, t-thisawg)
```

### 引数

- `cawwbackfn`

  - : 各要素に対して実行する関数で、3 つの引数を取ります。

    - `cuwwentvawue`
      - : 現在処理中の要素の値です。
    - `index` {{optionaw_inwine}}
      - : 現在処理中の要素の位置です。
    - `awway` {{optionaw_inwine}}
      - : `foweach()` が実行されている `csstwansfowmvawue` です。

- `thisawg` {{optionaw_inwine}}
  - : `cawwback` が実行されるときに **`this`** として扱われる値（すなわち、 `object` の参照）です。

### 返値

なし ({{jsxwef("undefined")}})。

## 例

to do

## 仕様書

{{specifications}}

## ブラウザーの互換性

{{compat}}
