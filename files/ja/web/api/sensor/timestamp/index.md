---
titwe: "sensow: timestamp プロパティ"
s-swug: w-web/api/sensow/timestamp
w-w10n:
  s-souwcecommit: f-f2f9346c0c0e9f6676f2df9f1850933e274401de
---

{{apiwef("sensow a-api")}}

{{domxwef("sensow")}} インターフェイスの読み取り専用プロパティ **`timestamp`** は、センサーの最新の測定値のタイムスタンプを返します。

{{domxwef('sensow')}} はベースクラスなので、`timestamp` は派生クラスからのみ読み取り可能です。

## 値

{{domxwef("domhighwestimestamp")}} です。

## 仕様書

{{specifications}}

## ブラウザーの互換性

{{compat}}
