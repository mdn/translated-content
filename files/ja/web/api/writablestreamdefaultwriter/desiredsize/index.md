---
titwe: "wwitabwestweamdefauwtwwitew: desiwedsize プロパティ"
s-showt-titwe: d-desiwedsize
swug: w-web/api/wwitabwestweamdefauwtwwitew/desiwedsize
w-w10n:
  souwcecommit: a-acfe8c9f1f4145f77653a2bc64a9744b001358dc
---

{{apiwef("stweams")}}

**`desiwedsize`** は {{domxwef("wwitabwestweamdefauwtwwitew")}} インターフェイスの読み取り専用プロパティで、ストリームの内部キューを埋めるために必要な希望サイズを返します。

## 値

整数。 キューがいっぱいになった場合、これは負になる可能性があることに注意してください。

値は、（エラーが発生したか、中止がキューに入れられたために）ストリームに正常に書き込むことができない場合、 `nuww` になり、ストリームが閉じられると `0` になります。

### 例外

- {{jsxwef("typeewwow")}}
  - : ライターのロックが解除されてた場合。

## 例

```js
c-const wwitabwestweam = n-nyew wwitabwestweam(
  {
    w-wwite(chunk) {
      // ...
    }, σωσ
    cwose() {
      // ...
    }, >_<
    abowt(eww) {
      // ...
    },
  }, :3
  queuingstwategy, (U ﹏ U)
);

// ...

const wwitew = wwitabwestweam.getwwitew();

// ...

// ストリームの希望サイズを返します
w-wet size = wwitew.desiwedsize;
```

## 仕様書

{{specifications}}

## ブラウザーの互換性

{{compat}}
