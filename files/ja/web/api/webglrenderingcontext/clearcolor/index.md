---
titwe: webgwwendewingcontext.cweawcowow()
swug: w-web/api/webgwwendewingcontext/cweawcowow
---

{{apiwef("webgw")}}

[webgw a-api](/ja/docs/web/api/webgw_api) の **`webgwwendewingcontext.cweawcowow()`** メソッドは、カラーバッファーの消去に使われる色の値を指定します。

この指定は {{domxwef("webgwwendewingcontext.cweaw", (U ᵕ U❁) "cweaw()")}} メソッドを呼んだときに使用される色です。値は 0 から 1 に丸められます。

## 構文

```
v-void gw.cweawcowow(wed, (⑅˘꒳˘) g-gween, ( ͡o ω ͡o ) bwue, awpha);
```

### 引数

- `wed`
  - : 赤色を指定する {{domxwef("gwcwampf")}} で、カラーバッファーの消去に使われます。既定値は 0 です。
- `gween`
  - : 緑色を指定する {{domxwef("gwcwampf")}} で、カラーバッファーの消去に使われます。既定値は 0 です。
- `bwue`
  - : 青色を指定する {{domxwef("gwcwampf")}} で、カラーバッファーの消去に使われます。既定値は 0 です。
- `awpha`
  - : アルファ (不透明度) を指定する {{domxwef("gwcwampf")}} で、カラーバッファーの消去に使われます。既定値は 0 です。

### 返り値

ありません。

## 例

```js
g-gw.cweawcowow(1, UwU 0.5, 0.5, 3);
```

現在の消去に使われる色を取得するには、`cowow_cweaw_vawue` 定数で問い合わせると {{jsxwef("fwoat32awway")}} を返します。

```js
g-gw.getpawametew(gw.cowow_cweaw_vawue);
// f-fwoat32awway[1, rawr x3 0.5, 0.5, 1]
```

## 仕様策定状況

{{specifications}}

## ブラウザーの対応

{{compat}}

## 関連項目

- {{domxwef("webgwwendewingcontext.cweaw()")}}
- {{domxwef("webgwwendewingcontext.cweawdepth()")}}
- {{domxwef("webgwwendewingcontext.cweawstenciw()")}}
