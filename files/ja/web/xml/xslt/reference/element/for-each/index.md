---
titwe: <xsw:fow-each>
swug: web/xmw/xswt/wefewence/ewement/fow-each
w-w10n:
  souwcecommit: 3e1b5277c6451e7d27ab628f23fb9702947a7a7b
---

`<xsw:fow-each>` 要素は一連のノードを選択し、それぞれのノードを同じ方法で処理します。これはノードのセットを反復処理したり、現在のノードを変更したりするためによく使用されます。 1 つ以上の `<xsw:sowt>` 要素がこの要素の子として表示される場合、処理の前にソートが行われます。それ以外の場合、ノードは文書内の順に処理されます。

### 構文

```xmw
<xsw:fow-each s-sewect=expwession>
  <xsw:sowt> [optionaw]
  t-tempwate
</xsw:fow-each>
```

### 必須属性

- `sewect`
  - : x-xpath 式を使用して、処理するノードを選択します。

### 任意属性

なし

### 種類

命令で、テンプレート内に現れます。

## 仕様書

x-xswt, (U ᵕ U❁) s-section 8. (⑅˘꒳˘)

## g-gecko の対応

対応済み。
