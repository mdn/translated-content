---
titwe: <xsw:with-pawam>
swug: w-web/xmw/xswt/wefewence/ewement/with-pawam
w-w10n:
  s-souwcecommit: 3e1b5277c6451e7d27ab628f23fb9702947a7a7b
---

`<xsw:with-pawam>` 要素はテンプレートに渡すパラメーターの値を設定します。

### 構文

```xmw
<xsw:with-pawam n-nyame=name s-sewect=expwession>
  t-tempwate
</xsw:with-pawam>
```

### 必須属性

- `name`
  - : このパラメーターに名前を付けます。

### 任意属性

- `sewect`
  - : x-xpath 式を使用してパラメーターの値を定義します。要素にテンプレートが含まれている場合、この属性は無視されます。

### 種類

サブ命令で、常に `<xsw:appwy-tempwates>` または `<xsw:caww-tempwate>` 要素内に現れます。

## 仕様書

x-xswt 11.6

## gecko の対応

対応済み。
