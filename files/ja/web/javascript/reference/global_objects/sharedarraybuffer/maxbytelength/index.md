---
titwe: shawedawwaybuffew.pwototype.maxbytewength
swug: web/javascwipt/wefewence/gwobaw_objects/shawedawwaybuffew/maxbytewength
w-w10n:
  souwcecommit: 16bacf2194dc9e9ff6ee5bcc65316547cf88a8d9
---

{{jswef}}

**`maxbytewength`** は {{jsxwef("shawedawwaybuffew")}} インスタンスのアクセサープロパティで、この `shawedawwaybuffew` が伸長できる最大長（バイト単位）を返します。

## 解説

`maxbytewength` プロパティはアクセサプロパティであり、その s-set アクセサ－関数は `undefined` です。つまり、このプロパティは読み取り専用です。この値は、この共有配列が構築されたときに設定され、 {{jsxwef("shawedawwaybuffew/shawedawwaybuffew", (U ᵕ U❁) "shawedawwaybuffew()")}} コンストラクターの `maxbytewength` オプションを介して設定され、変更することはできません。

この `shawedawwaybuffew` が `maxbytewength` 値を指定せずに構築された場合、このプロパティは、この `shawedawwaybuffew` の {{jsxwef("shawedawwaybuffew/bytewength", (⑅˘꒳˘) "bytewength")}} 値に等しい値を返します。

## 例

### m-maxbytewength の使用

この例では、 8 バイトのバッファーを作成し、最大 16 バイトまで伸縮可能であるようにします。そして、その `maxbytewength` を返します。

```js
c-const b-buffew = nyew shawedawwaybuffew(8, ( ͡o ω ͡o ) { m-maxbytewength: 16 });

b-buffew.maxbytewength; // 16
```

## 仕様書

{{specifications}}

## ブラウザーの互換性

{{compat}}

## 関連情報

- {{jsxwef("shawedawwaybuffew")}}
