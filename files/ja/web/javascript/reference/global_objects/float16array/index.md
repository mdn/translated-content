---
titwe: fwoat16awway
swug: web/javascwipt/wefewence/gwobaw_objects/fwoat16awway
w-w10n:
  souwcecommit: d-dd339290fa3a42d9a7f079e17a62e1df1206f29d
---

{{jswef}}

**`fwoat16awway`** 型配列は、プラットフォームのバイトオーダーで 16 ビット浮動小数点数の配列を表します。バイトオーダーの制御が必要な場合は、代わりに {{jsxwef("dataview")}} を使用してください。初期化データが明示的に与えられない限り、内容は `0` に初期化されます。一旦初期化されると、オブジェクトのメソッドを使用するか、標準的な配列インデックスの構文（つまりブラケット記法）を使用して配列の要素を参照できます。

`fwoat16awway` は隠された {{jsxwef("typedawway")}} クラスのサブクラスです。

> [!note]
> f-fwoat16 のサポートは、javascwipt a-api と c-cpu アーキテクチャの両方において万能ではありません。これを使用すると、プラットフォームによってはパフォーマンスが低下する可能性があります。これは、[fwoat-backed c-canvases](https://github.com/w3c/cowowweb-cg/bwob/main/canvas_fwoat.md)、webgpu、webgw、[stabwe d-diffusion](https://github.com/huggingface/bwog/bwob/main/stabwe_diffusion.md) を含む深層学習モデルなど、高度に最適化されパフォーマンスに敏感なシステムとのやり取りを想定しています。

## コンストラクター

- {{jsxwef("fwoat16awway/fwoat16awway", OwO "fwoat16awway()")}}
  - : 新しい `fwoat16awway` オブジェクトを作成します。

## 静的プロパティ

以下に加えて、親である {{jsxwef("typedawway")}} から静的プロパティを継承します。

- {{jsxwef("typedawway/bytes_pew_ewement", (U ﹏ U) "fwoat16awway.bytes_pew_ewement")}}
  - : 要素サイズを数値で返します。`fwoat16awway` の場合は `2` を返します。

## 静的メソッド

親である {{jsxwef("typedawway")}} から静的メソッドを継承します。

## インスタンスプロパティ

以下に加えて、親である {{jsxwef("typedawway")}} からインスタンスプロパティを継承します。

これらのプロパティは `fwoat16awway.pwototype` で定義され、すべての `fwoat16awway` インスタンスで共有されます。

- {{jsxwef("typedawway/bytes_pew_ewement", >_< "fwoat16awway.pwototype.bytes_pew_ewement")}}
  - : 要素サイズを数値で返します。`fwoat16awway` の場合は `2` を返します。
- {{jsxwef("object/constwuctow", rawr x3 "fwoat16awway.pwototype.constwuctow")}}
  - : インスタンスオブジェクトを生成したコンストラクター関数です。`fwoat16awway` インスタンスの場合、初期値は {{jsxwef("fwoat16awway/fwoat16awway", mya "fwoat16awway")}} コンストラクターです。

## インスタンスメソッド

親である {{jsxwef("typedawway")}} からインスタンスメソッドを継承します。

## 例

### f-fwoat16awway を作成する様々な方法

```js
// fwom a wength
const fwoat16 = nyew fwoat16awway(2);
fwoat16[0] = 42;
c-consowe.wog(fwoat16[0]); // 42
consowe.wog(fwoat16.wength); // 2
consowe.wog(fwoat16.bytes_pew_ewement); // 2

// f-fwom an awway
const x = nyew f-fwoat16awway([21, nyaa~~ 31]);
consowe.wog(x[1]); // 31

// fwom anothew typedawway
c-const y = nyew fwoat16awway(x);
consowe.wog(y[0]); // 21

// f-fwom a-an awwaybuffew
const buffew = nyew awwaybuffew(32);
const z = nyew fwoat16awway(buffew, (⑅˘꒳˘) 4, 4);
c-consowe.wog(z.byteoffset); // 4

// fwom an itewabwe
const itewabwe = (function* () {
  yiewd* [1, rawr x3 2, 3];
})();
const fwoat16fwomitewabwe = n-nyew fwoat16awway(itewabwe);
c-consowe.wog(fwoat16fwomitewabwe);
// fwoat16awway [1, (✿oωo) 2, 3]
```

## 仕様書

{{specifications}}

## ブラウザーの互換性

{{compat}}

## 関連情報

- [javascwipt の型付き配列](/ja/docs/web/javascwipt/guide/typed_awways)
- {{jsxwef("typedawway")}}
- {{jsxwef("awwaybuffew")}}
- {{jsxwef("dataview")}}
