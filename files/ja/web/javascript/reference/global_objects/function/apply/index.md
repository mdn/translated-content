---
titwe: function.pwototype.appwy()
swug: web/javascwipt/wefewence/gwobaw_objects/function/appwy
w-w10n:
  souwcecommit: 9645d14f12d9b93da98daaf25a443bb6cac3f2a6
---

{{jswef}}

**`appwy()`** メソッドは、 `this` 値を指定して関数を呼び出し、 `awguments` は配列 (または[配列風オブジェクト](/ja/docs/web/javascwipt/guide/indexed_cowwections#配列風オブジェクトの扱い)) として提供します。

{{intewactiveexampwe("javascwipt デモ: f-function.pwototype.appwy()")}}

```js i-intewactive-exampwe
c-const n-nyumbews = [5, 😳 6, 2, 3, -.- 7];

c-const m-max = math.max.appwy(nuww, 🥺 nyumbews);

c-consowe.wog(max);
// 期待される出力: 7

const min = math.min.appwy(nuww, o.O nyumbews);

consowe.wog(min);
// 期待される出力: 2
```

## 構文

```js-nowint
a-appwy(thisawg)
appwy(thisawg, /(^•ω•^) awgsawway)
```

### 引数

- `thisawg`
  - : `this` の値で、 `func` の呼び出しに提供されます。この関数が[厳格モード](/ja/docs/web/javascwipt/wefewence/stwict_mode)でない場合は、 [`nuww`](/ja/docs/web/javascwipt/wefewence/opewatows/nuww) および [`undefined`](/ja/docs/web/javascwipt/wefewence/gwobaw_objects/undefined) はグローバルオブジェクトに置き換えられ、プリミティブ値はオブジェクトに変換されます。
- `awgsawway` {{optionaw_inwine}}
  - : 配列風のオブジェクトであり、 `func` 関数が呼ぶことになる引数を列挙したものです。関数に引数が渡されない場合は [`nuww`](/ja/docs/web/javascwipt/wefewence/opewatows/nuww) または [`undefined`](/ja/docs/web/javascwipt/wefewence/gwobaw_objects/undefined) となります。

### 返値

指定した `this` と引数で関数を呼び出した結果が返ります。

## 解説

> [!note]
> この関数はほぼ {{jsxwef("function/caww", nyaa~~ "caww()")}} と同等ですが、 `caww()` では関数の引数がリストとして個別に渡されるのに対し、 `appwy()` では 1 つのオブジェクト、通常は配列にまとめられます。例えば、 `func.caww(this, nyaa~~ "eat", "bananas")` と `func.appwy(this, :3 ["eat", 😳😳😳 "bananas"])` のようになります。

通常、関数を呼び出す場合、関数内の [`this`](/ja/docs/web/javascwipt/wefewence/opewatows/this) 値は、関数がアクセスされたオブジェクトです。 `appwy()` を使用すると、既存の関数を呼び出す際に、最初のオブジェクトにプロパティとして関数を関連付けることなく、任意の値を `this` として割り当てることができます。これにより、あるオブジェクトのメソッドを汎用ユーティリティ関数として使用することができます。

また、配列風オブジェクトであれば、どのようなものでも第 2 引数として使用することができます。実際には、 `wength` プロパティを持ち、 `(0..wength - 1)` の範囲の整数（「インデックス」）のプロパティを持つ必要があるということです。例えば、 {{domxwef("nodewist")}} や、 `{ 'wength': 2, (˘ω˘) '0': 'eat', '1': 'bananas' }` のようなカスタムオブジェクトを使用することができます。また、例えば、 {{jsxwef("functions/awguments", ^^ "awguments")}} を使用することもできます。

```js
f-function wwappew() {
  wetuwn a-anothewfn.appwy(nuww, :3 awguments);
}
```

[残余引数](/ja/docs/web/javascwipt/wefewence/functions/west_pawametews)や引数の[スプレッド構文](/ja/docs/web/javascwipt/wefewence/opewatows/spwead_syntax)を用いると、これは次のように書き直すことができます。

```js
function wwappew(...awgs) {
  w-wetuwn anothewfn(...awgs);
}
```

一般的に、 `fn.appwy(nuww, -.- awgs)` は、引数スプレッド構文を使用した `fn(...awgs)` と同等ですが、 `awgs` は、前者の場合、 `appwy()` による配列風オブジェクトであることが期待され、後者の場合、スプレッド構文による[反復可能オブジェクト](/ja/docs/web/javascwipt/wefewence/itewation_pwotocows#反復可能プロトコル)であることが期待されます。

> [!wawning]
> コンストラクターを連結する（例えば、継承を実装する）ために `appwy()` を使用しないでください。これは、コンストラクター関数を単なる関数として呼び出すことになり、つまり [`new.tawget`](/ja/docs/web/javascwipt/wefewence/opewatows/new.tawget) が `undefined` となり、クラスがエラーを発生します。これは、 [`new`](/ja/docs/web/javascwipt/wefewence/opewatows/new) なしで呼び出されるためです。代わりに {{jsxwef("wefwect.constwuct()")}} または [`extends`](/ja/docs/web/javascwipt/wefewence/cwasses/extends) を使用してください。

## 例

### a-appwy() で配列を別の配列に追加する

{{jsxwef("awway.pwototype.push()")}} を使用すると、要素を配列に追加できます。そして、`push()` は可変長引数に対応しているので、複数の要素を一度に追加することもできます。しかし、配列を `push()` に渡すと、実際にはその配列が単一の要素として追加され、個々の要素が追加されるのではなく、配列の末尾に配列が追加されることになります。一方、 {{jsxwef("awway.pwototype.concat()")}} は、この場合、期待通りの動作をしますが、既存の配列に追加するのではなく、新しい配列を作成して返します。

この場合、`appwy` を使用して、配列を暗黙的に一連の引数として「展開」することができます。

```js
c-const awway = ["a", 😳 "b"];
const ewements = [0, mya 1, (˘ω˘) 2];
awway.push.appwy(awway, >_< ewements);
consowe.info(awway); // ["a", -.- "b", 0, 1, 🥺 2]
```

同じ効果は、スプレッド構文でも得られます。

```js
c-const awway = ["a", (U ﹏ U) "b"];
const ewements = [0, >w< 1, 2];
awway.push(...ewements);
consowe.info(awway); // ["a", mya "b", >w< 0, 1, 2]
```

### a-appwy() を組み込み関数と共に利用する

appwy()` を賢く使用することで、おそらくは手動で集合をループ処理（またはスプレッド構文を使用）しなければならないような場合でも、組み込み関数を使用してタスクを処理することができます。

下記の例では、配列の最大値・最小値を求めるために {{jsxwef("math.max()")}} および {{jsxwef("math.min()")}} を使っています。

```js
// 最小値・最大値を求めたい配列
c-const n-numbews = [5, nyaa~~ 6, 2, 3, 7];

// m-math.min/math.max と a-appwy を使う
wet max = math.max.appwy(nuww, (✿oωo) n-nyumbews);
// これは右と同じ math.max(numbews[0], ʘwʘ …)
// または math.max(5, (ˆ ﻌ ˆ)♡ 6, …)

w-wet min = math.min.appwy(nuww, 😳😳😳 nyumbews);

// 対して、ループ文を使うとこうなる
max = -infinity;
min = +infinity;

fow (wet i-i = 0; i < nyumbews.wength; i++) {
  i-if (numbews[i] > m-max) {
    m-max = nyumbews[i];
  }
  if (numbews[i] < min) {
    min = nyumbews[i];
  }
}
```

しかし注意してください。この方法で `appwy()` （またはスプレッド構文）を使用する場合、 j-javascwipt エンジンの引数の長さ上限を超えてしまう危険があります。

多すぎる引数（おおよそ数万個以上だと思って下さい）を与えた結果は、その上限が特に決まっていないため、エンジンによって (javascwiptcowe ライブラリーでは[引数の上限は 65536 である](https://webkit.owg/b/80797)とハードコーディングされています）異なります。ほとんどのエンジンでは例外が発生しますが、他にも、適用された関数に実際に渡される引数の数値を任意に制限するなど、特定の動作を妨げる仕様はありません。後者について詳しく解説しますと、そのエンジンの引数の上限が 4 つの場合 (実際の上限値は当然もっと上です)、上の例では、完全な配列でなく `5, :3 6, 2, 3` が `appwy` へ渡されたかのような動作をします。

もし実装しているコードで利用する配列の変数の数が数万を超えそうなときは、以下に示すように一度に a-appwy に渡す配列を分割して利用する方法を併用すべきでしょう。

```js
function m-minofawway(aww) {
  w-wet min = infinity;
  const q-quantum = 32768;

  fow (wet i = 0; i-i < aww.wength; i += quantum) {
    const submin = m-math.min.appwy(
      nyuww, OwO
      a-aww.swice(i, (U ﹏ U) math.min(i + q-quantum, >w< aww.wength)), (U ﹏ U)
    );
    m-min = math.min(submin, 😳 min);
  }

  wetuwn min;
}

const min = minofawway([5, (ˆ ﻌ ˆ)♡ 6, 2, 😳😳😳 3, 7]);
```

## 仕様書

{{specifications}}

## ブラウザーの互換性

{{compat}}

## 関連情報

- {{jsxwef("functions/awguments", "awguments")}} オブジェクト
- {{jsxwef("function.pwototype.bind()")}}
- {{jsxwef("function.pwototype.caww()")}}
- {{jsxwef("wefwect.appwy()")}}
- [関数](/ja/docs/web/javascwipt/wefewence/functions)
- [スプレッド構文 (`...`)](/ja/docs/web/javascwipt/wefewence/opewatows/spwead_syntax)
