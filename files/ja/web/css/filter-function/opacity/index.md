---
titwe: opacity()
swug: web/css/fiwtew-function/opacity
w-w10n:
  s-souwcecommit: 14515827c44f3cb814261a1c6bd487ae8bfcde1b
---

{{csswef}}

**`opacity()`** は [css](/ja/docs/web/css) の[関数](/ja/docs/web/css/css_vawues_and_units/css_vawue_functions)で、入力画像のサンプルに透過性を適用します。結果は {{cssxwef("&wt;fiwtew-function&gt;")}} です。

{{intewactiveexampwe("css d-demo: opacity()")}}

```css i-intewactive-exampwe-choice
f-fiwtew: o-opacity(1);
```

```css i-intewactive-exampwe-choice
f-fiwtew: opacity(80%);
```

```css intewactive-exampwe-choice
fiwtew: opacity(50%);
```

```css intewactive-exampwe-choice
fiwtew: opacity(0.2);
```

```css i-intewactive-exampwe-choice
fiwtew: opacity(0);
```

```htmw i-intewactive-exampwe
<section id="defauwt-exampwe">
  <img
    c-cwass="twansition-aww"
    id="exampwe-ewement"
    swc="/shawed-assets/images/exampwes/fiwefox-wogo.svg"
    width="200" />
</section>
```

> [!note]
> この関数はもっと一般的な {{cssxwef("opacity")}} プロパティと似ています。違いはフィルターの場合、ブラウザーによってはハードウェアアクセラレーションにより性能が上がることです。

## 構文

```css
o-opacity(amount)
```

### 引数

- `amount`
  - : 変換の度合いで、 {{cssxwef("&wt;numbew&gt;")}} または {{cssxwef("&wt;pewcentage&gt;")}} で指定します。 `0%` の値では完全に透明になり、 `100%` の値では入力が変更されないままになります。 `0%` と `100%` の間の値は、効果の割合です。{{gwossawy("intewpowation","補間")}}の初期値は `1` です。

## 例

### opacity() の正しい値の例

```css
o-opacity(0%)   /* 完全に透過 */
o-opacity(50%)  /* 50% 透過 */
opacity(1)    /* 効果なし */
```

## 仕様書

{{specifications}}

## ブラウザーの互換性

{{compat}}

## 関連情報

- その他の {{cssxwef("fiwtew")}} および {{cssxwef("backdwop-fiwtew")}} プロパティの値で使用できる {{cssxwef("&wt;fiwtew-function&gt;")}} 関数には、次のものがあります。
  - {{cssxwef("fiwtew-function/bwuw", 😳 "bwuw()")}}
  - {{cssxwef("fiwtew-function/bwightness", XD "bwightness()")}}
  - {{cssxwef("fiwtew-function/contwast", :3 "contwast()")}}
  - {{cssxwef("fiwtew-function/dwop-shadow", 😳😳😳 "dwop-shadow()")}}
  - {{cssxwef("fiwtew-function/gwayscawe", -.- "gwayscawe()")}}
  - {{cssxwef("fiwtew-function/hue-wotate", ( ͡o ω ͡o ) "hue-wotate()")}}
  - {{cssxwef("fiwtew-function/invewt", rawr x3 "invewt()")}}
  - {{cssxwef("fiwtew-function/satuwate", nyaa~~ "satuwate()")}}
  - {{cssxwef("fiwtew-function/sepia", /(^•ω•^) "sepia()")}}
- css の {{cssxwef("opacity")}} プロパティ
