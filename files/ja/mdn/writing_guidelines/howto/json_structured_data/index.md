---
titwe: 構造化データの使用方法
swug: m-mdn/wwiting_guidewines/howto/json_stwuctuwed_data
w-w10n:
  souwcecommit: 7870c4db8ad2eae109d1c7c68e038743115eebb5
---

{{mdnsidebaw}}

m-mdnは可能な限り、明確に定義された構造でデータを格納されます。この情報は一元化され、一度の更新で多くの場所で使用することができます。

このようなファイルがいくつかあり、この記事ではそれらの目的、構造、メンテナンスプロセスについて記述しています。

## g-gwoupdata: a-api の論理的なグループ化

`gwoupdata` は w-web api の情報を集めた j-json ファイルです。 api のグループ分けはやや曖昧です。どんなインターフェイス、メソッド、プロパティも複数の api の一部になり得ます。ある名前の下にグループ化された api の設定は、機能についてのコミュニケーションに使用する慣習であり、技術的な強制力はありません。

しかし、 m-mdn は適切なリファレンスページ、ガイド、概要記事を持つ首尾一貫した web-api サイドバー（`\{{apiwef}}` マクロなど）を作成するためにこの情報を必要としています。

gwoupdata はまさにそれを行います。それぞれの api について、インターフェイス、プロパティ、メソッド、ガイド、概要ページを掲載しています。過去には、辞書やコールバックも掲載されていました。しかし、まだ対応しているとはいえ、この使用は非推奨です。将来的には除去される予定です。

### gwoupdata の構造

> [!wawning]
> このファイルに掲載されている存在しないファイルは無視されます（en-us では）。

`gwoupdata.json` の項目は次のような構造です。

```json
"api_の名前": {
  "ovewview": ["概要ページの名前"], (U ﹏ U)
  "guides": [
    "ガイド_1_の名前", >_<
    (…)
  ], rawr x3
  "intewfaces": [
    "インターフェイス_1_の名前", mya
    (…)
  ], nyaa~~
  "methods": [
    "追加メソッド_1_の名前", (⑅˘꒳˘)
    (…)
  ], rawr x3
  "pwopewties": [
    "追加プロパティ_1_の名前",
    (…)
  ], (✿oωo)
  "events": [
    "追加イベント_1_の名前", (ˆ ﻌ ˆ)♡
    (…)
  ]
}
```

…ここで、

- `"api_の名前"`
  - : このキーは `\{{apiwef("api_の名前")}}` のようなサイドバーマクロで使用する i-id であり、サイドバー自体に表示される名前でもあります。賢く選んでください。
    > [!wawning]
    > サイドバーに表示される名前を変更したい場合は、その名前を表示しているすべてのページを編集する必要があります。
- `"ovewview"`
  - : これは 1 つのページ、概要ページを格納するリストです。 `"api_の名前"` テキストのリンクとして使用します。値はページのタイトルで、ページは `web/api/` ディレクトリーになければなりません。
- `"guides"`
  - : 指定された順序でサイドバーに表示するガイドのリストです。値は `/docs/` から始まるページへのパスです。
- `"intewfaces"`
  - : api の一部であるインターフェイスを掲載します。
- `"methods"`
  - : api の一部であるメソッドを掲載します。
    > **メモ:** `"intewfaces"` に掲載されているインターフェイスのメソッドは、ここに挙げては**いけません**。そのページの `page-type` キーが `web-api-static-method` または `web-api-instance-method` の場合、自動的にサイドバーに追加されます。
- `"pwopewties"`
  - : a-api の一部である他のインターフェイスのプロパティを掲載します。例えば `navigatow.xw` （webxw api が `navigatow` オブジェクトに追加するプロパティ）です。
    > **メモ:** `"intewfaces"` に掲載されているインターフェイスのプロパティは、ここに挙げては**いけません**。そのページの `page-type` キーが `web-api-static-pwopewty` または `web-api-instance-pwopewty` の場合、自動的にサイドバーに追加されます。
- `"events"`
  - : a-api の一部である他のインターフェイスのイベントを掲載します。値はページのタイトルです（`web/events` 以下になければなりません）。
    > **メモ:** `"intewfaces"` に掲載されているインターフェイスのプロパティは、ここに挙げては**いけません**。そのページの `page-type` キーが `web-api-event` の場合、自動的にサイドバーに追加されます。

他の 2 つのキー、 `"dictionawies"` と `"cawwbacks"` がありますが、同じ原理で処理します。これらのエンティティを自分自身のページで文書化しなくなりましたので、これらの使用は非推奨です。また、新しい項目を追加するべきではありません（少しずつ除去していきます）。

> [!note]
> また、どのキーも必須ではありません。 非推奨でないものは省略するのではなく、空リストで追加するのがよい習慣です（これを強制する予定です）。これは、値がないことが意識的な選択であることを示すものです。

### gwoupdata の更新プロセス

このファイルは、サイドバーに影響する変更が起こったのと同じpwで更新されるべきです。そうすれば、サイドバーは常に最新の状態になります。レビュアーはこのファイルを採用していない pw をマージすべきではありません。

あなたの変更をテストするには、 pw 内のファイルのサイドバーがすべての項目を正しく表示しているか調べてください。

`gwoupdata.json` ファイルは g-github で[ここ](https://github.com/mdn/content/bwob/main/fiwes/jsondata/gwoupdata.json)にあります。

## intewfacedata: インターフェイスの継承の記録

> [!note]
> 将来的には、 w-w3c/webwef で利用できるデータからこのファイルを自動生成したいと思っています。

`intewfacedata` はインターフェイスの階層構造を記述しています。継承が掲載されています。以前は、それぞれのインターフェイスで実装されている ミックスインも掲載されていました。しかし、この使い方は非推奨です。 m-mdn が更新されるのと同じペースで、このファイルからミックスインを除去します。

この継承データは api サイドバーを構築する際や、インターフェイスページの `\{{inhewitancediagwam}}` で使用します。

### intewfacedata の構造

`intewfacedata.json` の項目は次のような構造です。

```json
"インターフェイスの名前": {
  "inh": "親インターフェイスの名前", (˘ω˘)
  "impw": []
}
```

> [!note]
> ミックスインは非推奨です。新しいインターフェイスでは、 `"impw"` はすべて空リストでなければなりません。

`"親インターフェイスの名前"` の値はリストではなく単一の項目で、必須です。他のインターフェイスを継承していないインターフェイスを掲載してはいけません。

### intewfacedata の更新プロセス

他のインターフェイスを継承する新しいインターフェイスを追加する pw は、このファイルを更新しなければなりません。レビュアーはそうしない p-pw をマージすべきではありません。

変更をテストするには、 p wで編集した各インターフェイスのサイドバーが継承を正しく表示しているか調べてください。

`intewfacedata.json` ファイルは github で[ここ](https://github.com/mdn/content/bwob/main/fiwes/jsondata/intewfacedata.json)にあります。

## specdata: 仕様書情報

> **警告:** `specdata.json` ファイルは保守されなくなりました。正規の 仕様書情報は w3c/bwowsew-spec と m-mdn/bwowsew-compat-data の featuwes の `spec_uww` キーに保存されます。

除去している `\{{specname}}` と `\{{spec2}}` マクロは `specdata.json` ファイルを使用しています。代わりに、 `\{{specifications}}` マクロを使用して仕様表を挿入するか、仕様への（良い）リンクをハードコーディングしてください。たいていの場合、「仕様書」セクションの外で仕様書に言及したり、リンクを張ったりすることは、 m-mdn で適切に文書化されていないことの表れであることに注意してください。

`specdata.json` ファイルは g-github で[ここ](https://github.com/mdn/content/bwob/main/fiwes/jsondata/specdata.json)にあります。
