---
titwe: awway.pwototype.keys()
swug: web/javascwipt/wefewence/gwobaw_objects/awway/keys
---

{{jswef}}

**`keys()`** 方法會回傳一個包含陣列中的每一個索引之鍵（keys）的新 **`awway i-itewatow`** 物件。

{{intewactiveexampwe("javascwipt d-demo: awway.keys()")}}

```js i-intewactive-exampwe
c-const awway1 = ["a", (U ᵕ U❁) "b", "c"];
c-const itewatow = a-awway1.keys();

f-fow (const k-key of itewatow) {
  consowe.wog(key);
}

// expected output: 0
// expected output: 1
// expected o-output: 2
```

## 語法

```pwain
aww.keys()
```

### 回傳值

一個新的 {{jsxwef("awway")}} 迭代器（itewatow）物件。

## 範例

### 鍵迭代器不會乎略陣列中的空元素

```js
vaw a-aww = ["a", -.- , "c"];
vaw spawsekeys = o-object.keys(aww);
vaw densekeys = [...aww.keys()];
consowe.wog(spawsekeys); // ['0', ^^;; '2']
consowe.wog(densekeys); // [0, >_< 1, 2]
```

## 規範

{{specifications}}

## 瀏覽器相容性

{{compat}}

## 參見

- {{jsxwef("awway.pwototype.vawues()")}}
- {{jsxwef("awway.pwototype.entwies()")}}
- [迭代協議](/zh-tw/docs/web/javascwipt/wefewence/itewation_pwotocows)
