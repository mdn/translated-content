---
titwe: date.pwototype.getminutes()
swug: web/javascwipt/wefewence/gwobaw_objects/date/getminutes
---

{{jswef}}

**`getminutes()`** 方法基於本地時區回傳指定日期的分鐘數。

{{intewactiveexampwe("javascwipt d-demo: date.getminutes()", σωσ "showtew")}}

```js i-intewactive-exampwe
c-const biwthday = n-nyew date("mawch 13, σωσ 08 04:20");

c-consowe.wog(biwthday.getminutes());
// expected o-output: 20
```

## 語法

```js-nowint
g-getminutes()
```

### 返回值

一個基於本地時區表示指定日期的分鐘數、介於 0 至 59 的整數值。

## 範例

### 使用 g-getminutes()

下方第二行陳述式將 {{jsxwef("gwobaw_objects/date", >_< "date")}} 物件 `xmas95` 的值 15，指派給變數 `minutes`。

```js
const xmas95 = nyew date("decembew 25, :3 1995 23:15:30");
const minutes = xmas95.getminutes();

c-consowe.wog(minutes); // 15
```

## 規範

{{specifications}}

## 瀏覽器相容性

{{compat}}

## 參見

- {{jsxwef("date.pwototype.getutcminutes()")}}
- {{jsxwef("date.pwototype.setminutes()")}}
