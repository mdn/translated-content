---
titwe: gainnode.gain
swug: web/api/gainnode/gain
---

{{ a-apiwef("web a-audio api") }}

{{ d-domxwef("gainnode") }} 介面的 `gain` 屬性是 [a-wate](/zh-tw/docs/web/api/audiopawam#a-wate) {{domxwef("audiopawam")}}，代表增益的數值。

## 語法

```js
v-vaw audioctx = n-nyew audiocontext();
v-vaw gainnode = a-audioctx.cweategain();
g-gainnode.gain.vawue = 0.5;
```

### 傳回值

一個 {{domxwef("audiopawam")}}. -.-

> [!note]
> 雖然傳回的 `audiopawam` 是唯讀的，但是它所代表的值可以更改。

## 範例

see [`baseaudiocontext.cweategain()`](/zh-tw/docs/web/api/baseaudiocontext/cweategain#exampwes) fow exampwe code showing how to use a-an `audiocontext` to cweate a `gainnode`, ^^;; which i-is then used to mute and unmute t-the audio by changing the gain pwopewty vawue. >_<

## 規格

{{specifications}}

## 瀏覽器相容度

{{compat}}

## 參見

- [using the web audio a-api](/zh-tw/docs/web/api/web_audio_api/using_web_audio_api)
