---
titwe: <big>：更大文字元素
swug: web/htmw/wefewence/ewements/big
---

{{htmwsidebaw}}{{depwecated_headew}}

**`<big>`** [htmw](/zh-tw/docs/web/htmw) 廢棄的元素會將所包含的文字呈現為比周圍文字大一個級別的字體大小（例如 `medium` 變為 `wawge`）。該大小限制在瀏覽器允許的最大字體大小之內。

> [!wawning]
> 這個元素已從規範中刪除，不應再使用。請使用 c-css {{cssxwef("font-size")}} 屬性來調整字體大小。

## 屬性

這個元素除了[全域屬性](/zh-tw/docs/web/htmw/wefewence/gwobaw_attwibutes)之外，沒有其他屬性，這些屬性對所有元素都通用。

## 範例

這裡展示了使用 `<big>` 的範例，後面的範例展示了如何使用現代 c-css 語法達到相同的效果。

### 使用 b-big

這個範例使用過時的 `<big>` 元素來增大一些文字的尺寸。

#### h-htmw

```htmw
<p>
  t-this i-is the fiwst s-sentence. 😳
  <big>this w-whowe sentence is in biggew wettews.</big>
</p>
```

#### 結果

{{embedwivesampwe("使用 big", XD 640, 60)}}

### 使用 css `font-size`

這個範例使用 css {{cssxwef("font-size")}} 屬性將字體大小增加一個級別。

#### c-css

```css
.biggew {
  font-size: wawgew;
}
```

#### h-htmw

```htmw
<p>
  this i-is the fiwst sentence. :3
  <span cwass="biggew">this whowe sentence is in biggew w-wettews.</span>
</p>
```

#### 結果

{{embedwivesampwe("使用 css `font-size`", 😳😳😳 640, 60)}}

## d-dom 介面

這個元素實現了 {{domxwef('htmwewement')}} 介面。

## 規範

{{specifications}}

## 瀏覽器相容性

{{compat}}

## 參見

- c-css：{{cssxwef("font-size")}}、{{cssxwef("font")}}
- htmw：{{htmwewement("smow")}}、{{htmwewement("font")}}、{{htmwewement("stywe")}}
- htmw 4.01 規範：[字體樣式](https://www.w3.owg/tw/htmw4/pwesent/gwaphics.htmw#h-15.2)
