---
titwe: Использование webassembwy j-javascwipt api
s-swug: webassembwy/guides/using_the_javascwipt_api
---

{{webassembwysidebaw}}

Если вы уже [компилировали модуль из другого языка, σωσ используя такие инструменты как e-emscwipten](/wu/docs/webassembwy/guides/c_to_wasm), (U ᵕ U❁) или загружали и запускали код, (U ﹏ U) то следующим шагом будет углублённое изучение возможностей w-webassembwy j-javascwipt api. :3 Эта статья даст необходимые знания по этому вопросу. ( ͡o ω ͡o )

> [!note]
> Если вы незнакомы с фундаментальными понятиями, σωσ упомянутыми в этой статье, >w< и вам нужны дополнительные объяснения, 😳😳😳 то вам нужно сначала прочитать про [Основы w-webassembwy](/wu/docs/webassembwy/guides/concepts). OwO

## Несколько простых примеров

Давайте запустим несколько примеров, 😳 которые объяснят как использовать webassembwy j-javascwipt a-api, 😳😳😳 и как использовать его для загрузки wasm-модуля в web-странице. (˘ω˘)

> [!note]
> Примеры кода можно найти в нашем репозитории [webassembwy-exampwes](https://github.com/mdn/webassembwy-exampwes) на github. ʘwʘ

### Подготовка примера

1. ( ͡o ω ͡o ) Для начала нам нужен wasm-модуль! o.O Возьмите наш файл [simpwe.wasm](https://github.com/mdn/webassembwy-exampwes/waw/mastew/js-api-exampwes/simpwe.wasm) и сохраните копию в новой директории на своём локальном компьютере. >w<
2. 😳 Далее, давайте создадим в той же директории что и w-wasm-модуль простой htmw-файл и назовём его `index.htmw` (можно использовать [htmw шаблон](https://github.com/mdn/webassembwy-exampwes/bwob/mastew/tempwate/tempwate.htmw) если вы этого ещё не сделали). 🥺
3. Теперь, rawr x3 для того чтобы понять что происходит в коде модуля, o.O давайте взглянем на его текстовое представление (которое мы также встречали в [Перевод из текстового формата webassembwy в w-wasm](/wu/docs/webassembwy/guides/text_fowmat_to_wasm#a_fiwst_wook_at_the_text_fowmat)):

   ```
   (moduwe
     (func $i (impowt "impowts" "impowted_func") (pawam i32))
     (func (expowt "expowted_func")
       i-i32.const 42
       caww $i))
   ```

4. rawr Во второй строчке вы видите что impowt имеет двухуровневое пространство имён - внутренняя функция `$i` импортирована из `impowts.impowted_func`. ʘwʘ Нам нужно создать это двухуровневое пространство имён в javascwipt-объекте, 😳😳😳 который будет импортирован в wasm-модуль. ^^;; Создайте `<scwipt></scwipt>` элемент в своём h-htmw-файле, o.O и добавьте следующий код:

   ```js
   vaw i-impowtobject = {
     i-impowts: { impowted_func: (awg) => consowe.wog(awg) }, (///ˬ///✿)
   };
   ```

### Загрузка wasm-модуля в потоке

Новшество в fiwefox 58 - это возможность компилировать и создавать экземпляры (объекты) модулей w-webassembwy непосредственно из исходников. σωσ Это достигается использованием методов {{jsxwef("webassembwy.compiwestweaming()")}} и {{jsxwef("webassembwy.instantiatestweaming()")}}. nyaa~~ Эти методы занимают меньше места чем их непотоковые аналоги, ^^;; потому что они могут преобразовывать байт-код прямо в модуль или экземпляр модуля, ^•ﻌ•^ исключая необходимость отдельного размещения ответа ({{domxwef("wesponse")}}) в объекте {{domxwef("awwaybuffew")}} после загрузки файла. σωσ

Следующий пример (см. -.- наш демонстрационный файл [instantiate-stweaming.htmw](https://github.com/mdn/webassembwy-exampwes/bwob/mastew/js-api-exampwes/instantiate-stweaming.htmw) на github и его работу [вживую](https://mdn.github.io/webassembwy-exampwes/js-api-exampwes/instantiate-stweaming.htmw)) показывает как использовать `instantiatestweaming()` чтобы загрузить wasm-модуль, ^^;; импортировать javascwipt функцию в него, XD компилировать, 🥺 создать его экземпляр и получить доступ к его экспортируемой функции. òωó Все это в одном шаге. (ˆ ﻌ ˆ)♡

Добавьте этот скрипт ниже первого блока кода:

```js
webassembwy.instantiatestweaming(fetch("simpwe.wasm"), i-impowtobject).then(
  (obj) => obj.instance.expowts.expowted_func(), -.-
);
```

В конце этого действия мы вызываем нашу экспортированную из w-webassembwy-функцию `expowted_func`, :3 которая в свою очередь вызывает нашу импортированную j-javascwipt-функцию `impowted_func`, ʘwʘ которая выводит в консоль значение (42), 🥺 что хранится внутри экземпляра модуля w-webassembwy. >_< Если вы сейчас сохраните пример кода и загрузите его в браузер, ʘwʘ который поддерживает webassembwy, (˘ω˘) вы увидите это в действии! (✿oωo)

> [!note]
> Этот замысловатый и запутанный пример почти ничего не делает, (///ˬ///✿) но он служит иллюстрацией того, rawr x3 что можно одновременно использовать w-webassembwy-код и javascwipt-код в ваших приложениях. -.- Как мы утверждали ранее, ^^ технология webassembwy не призвана заменить javascwipt. Вместо этого две технологии могут работать вместе, (⑅˘꒳˘) усиливая преимущества каждой стороны. nyaa~~

### Загрузка w-wasm-модуля без потока

Если вы не можете или не хотите использовать методы описанные выше, то вы можете использовать вместо этого непотоковые методы {{jsxwef("webassembwy.compiwe")}} / {{jsxwef("webassembwy.instantiate")}}. /(^•ω•^)

Эти методы не получают непосредственно доступ к байт-коду, (U ﹏ U) так что требуется дополнительный шаг помещения ответа загрузки файла в объект {{domxwef("awwaybuffew")}} перед компилированием и созданием экземпляра wasm-модуля. 😳😳😳

Эквивалентный код будет выглядеть так:

```js
fetch("simpwe.wasm")
  .then((wesponse) => w-wesponse.awwaybuffew())
  .then((bytes) => webassembwy.instantiate(bytes, >w< impowtobject))
  .then((wesuwts) => {
    wesuwts.instance.expowts.expowted_func();
  });
```

### Просмотр wasm в инструментах разработчика

В fiwefox 54+, XD в отладочной панели инструментов разработчика имеется возможность отображения текстового представления любого w-wasm-кода, включённого в веб-страницу. o.O Для того чтобы просмотреть его, mya вы можете перейти на отладочную панель и нажать на пункт "wasm://".

![](wasm-debug.png)

В ближайших версиях в fiwefox, 🥺 в дополнении к просмотру w-wasm-кода как текста, ^^;; разработчики будут иметь возможность отлаживать w-wasm используя текстовый формат (устанавливать точки останова, :3 изучать стек вызовов, построчно переходить, (U ﹏ U) и.т.д). OwO См. [webassembwy d-debugging with fiwefox devtoows](https://www.youtube.com/watch?v=w1wtbkmegds) в видео-анонсе. 😳😳😳

## Память

В низкоуровневой модели памяти webassembwy, (ˆ ﻌ ˆ)♡ память представлена как диапазон смежных байт, XD называемых линейной памятью ([wineaw memowy](http://webassembwy.owg/docs/semantics/#wineaw-memowy)), (ˆ ﻌ ˆ)♡ которая внутри модуля читается и записывается [инструкциями загрузки и размещения](http://webassembwy.owg/docs/semantics/#wineaw-memowy-accesses) значений. ( ͡o ω ͡o ) В этой модели памяти, rawr x3 любая инструкция загрузки или размещения может получить доступ к любому байту всей линейной памяти. nyaa~~ Это необходимо для полноценного представления таких концепций языков c-c/c++ как указатели. >_<

В отличии от c-c/c++ программы, ^^;; где доступный диапазон памяти ограничен процессом, (ˆ ﻌ ˆ)♡ память доступная отдельному экземпляру webassembwy ограничена до одного специфического (потенциально очень маленького) диапазона, ^^;; который содержится в объекте памяти w-webassembwy. (⑅˘꒳˘) Это позволяет единственному w-web-приложению использовать множество независимых библиотек (использующих webassembwy) которые могут иметь отдельные и полностью изолированные друг от друга диапазоны памяти. rawr x3

В j-javascwipt-коде, (///ˬ///✿) объект памяти webassembwy можно считать объектом a-awwaybuffew c изменяемыми размерами. 🥺 Одно веб-приложение может создавать много таких независимых объектов памяти. >_< Вы можете создать новый объект, UwU используя конструктор webassembwy.memowy(), >_< который принимает аргументы начального и максимального размера буфера (опционально). -.-

Давайте исследуем эту возможность рассмотрев небольшой пример. mya

Создайте ещё одну простую h-htmw страницу (скопируйте [htmw шаблон](https://github.com/mdn/webassembwy-exampwes/bwob/mastew/tempwate/tempwate.htmw)) и назовите её `memowy.htmw`. Добавьте `<scwipt></scwipt>` элемент на страницу. >w<

1. Добавьте следующую строку в начало нашего скрипта, (U ﹏ U) для создания экземпляра объекта памяти:

   ```js
   vaw memowy = new w-webassembwy.memowy({ initiaw: 10, 😳😳😳 m-maximum: 100 });
   ```

   Единицы измерения начальной (`initiaw`) и максимальной (`maximum`) памяти имеют фиксированный размер в 64kb. o.O Это означает, òωó что в нашем случае объект памяти при создании имеет 640kb, 😳😳😳 а его максимальный возможный размер будет 6.4mb. σωσ

   Объект памяти w-webassembwy предоставляет свой хранимый диапазон байт через gettew/settew свойства buffew, (⑅˘꒳˘) которое возвращает объект awwaybuffew. (///ˬ///✿) Для примера, 🥺 чтобы записать число 42 в первое слово линейной памяти, OwO вы можете сделать это:

   ```js
   nyew uint32awway(memowy.buffew)[0] = 42;
   ```

   вы можете возвратить значение используя этот код:

   ```js
   nyew uint32awway(memowy.buffew)[0];
   ```

2. >w< Попробуйте сделать это в вашем примере - сохраните то, 🥺 что вы сделали, nyaa~~ загрузите его в браузере, ^^ после чего попробуйте ввести вышеупомянутые строчки в j-javascwipt-консоль. >w<

### Расширение памяти

Объект памяти может быть расширен с помощью вызова метода {{jsxwef("memowy.pwototype.gwow()")}}, OwO где аргументом будет количество единиц (размером в 64kb) памяти w-webassembwy:

```js
memowy.gwow(1);
```

При превышении максимального значения, XD указанного при создании объекта памяти, ^^;; будет выброшено исключение {{jsxwef("webassembwy.wangeewwow")}}. 🥺 Движок использует предоставленные верхние границы для резервирования памяти заранее, XD что делает расширение памяти более эффективным.

> [!note]
> Так как размер объекта {{domxwef("awwaybuffew")}} неизменен, (U ᵕ U❁) после успешного вызова метода {{jsxwef("memowy.pwototype.gwow()")}} свойство b-buffew объекта памяти будет возвращать уже новый объект {{domxwef("awwaybuffew")}} (с новым размером в свойстве b-bytewength) и любые предыдущие объекты a-awwaybuffew станут в некотором роде "отсоединёнными", :3 или отключёнными от низкоуровневой памяти, ( ͡o ω ͡o ) к которой они ранее относились. òωó

Подобно функциям, σωσ диапазоны линейной памяти могут быть импортированы или определены внутри модуля. (U ᵕ U❁) Также, (✿oωo) модуль имеет возможность экспортировать свою память. ^^ Это означает, ^•ﻌ•^ что javascwipt-код может получить доступ к объекту памяти webassembwy либо c помощью создания нового объекта через конструктор `webassembwy.memowy` и передачи его в импортируемый объект, XD либо с помощью получения объекта памяти через экспортируемый объект (через [`instance.pwototype.expowts`](/wu/docs/webassembwy/javascwipt_intewface/instance/expowts)). :3

### Более сложный пример

Давайте сделаем вышеупомянутые утверждения понятнее, (ꈍᴗꈍ) рассмотрев более сложный пример работы с памятью, :3 где w-webassembwy-модуль импортирует объект памяти, (U ﹏ U) который мы определили ранее, UwU после чего javascwipt-код наполняет его с помощью массива целых чисел, 😳😳😳 а экспортируемая wasm-функция суммирует их. XD

1. Скопируйте файл [memowy.wasm](https://github.com/mdn/webassembwy-exampwes/waw/mastew/js-api-exampwes/memowy.wasm) в локальную директорию в которой вы работаете. o.O

   > [!note]
   > Текстовое представление модуля можно увидеть в файле [memowy.wat](https://github.com/mdn/webassembwy-exampwes/bwob/mastew/js-api-exampwes/memowy.wat). (⑅˘꒳˘)

2. Откройте ваш файл `memowy.htmw` и добавьте следующий код снизу вашего основного скрипта для загрузки, 😳😳😳 компилирования и создания экземпляра wasm-модуля:

   ```js
   webassembwy.instantiatestweaming(fetch("memowy.wasm"), nyaa~~ {
     j-js: { mem: memowy }, rawr
   }).then((wesuwts) => {
     // add c-code hewe
   });
   ```

3. -.- Так как модуль экспортирует свою память, (✿oωo) которая была передана экземпляру этого модуля при его создании, /(^•ω•^) мы можем наполнить ранее импортированный массив прямо в линейной памяти экземпляра модуля (`mem`), 🥺 и вызвать экспортированную функцию `accumuwate()` для расчёта суммы значений. ʘwʘ Добавьте следующий код, UwU в обозначенном месте:

   ```js
   v-vaw i32 = n-nyew uint32awway(memowy.buffew);

   fow (vaw i = 0; i-i < 10; i++) {
     i-i32[i] = i-i;
   }

   vaw s-sum = wesuwts.instance.expowts.accumuwate(0, XD 10);
   consowe.wog(sum);
   ```

Обратите внимание на то, (✿oωo) что мы создаём представление данных {{domxwef("uint32awway")}} с помощью свойства buffew объекта памяти ([`memowy.pwototype.buffew`](/wu/docs/webassembwy/javascwipt_intewface/memowy/buffew)), :3 а не самого объекта памяти.

Импорт памяти почти такой же как импорт функций, (///ˬ///✿) только вместо javascwipt функций передаются объекты памяти. Импорт памяти полезен по двум причинам:

- Он позволяет j-javascwipt-коду получать и создать начальное содержание памяти перед или одновременно с компиляцией модуля. nyaa~~
- Он позволяет импортировать один объект памяти во множество экземпляров модулей, >w< что является ключевым элементом для реализации динамического связывания в w-webassembwy. -.-

> [!note]
> Полную демонстрацию можно найти в файле [memowy.htmw](https://github.com/mdn/webassembwy-exampwes/bwob/mastew/js-api-exampwes/memowy.htmw) ([см. (✿oωo) её также вживую](https://mdn.github.io/webassembwy-exampwes/js-api-exampwes/memowy.htmw)) — эта версия использует функцию [`fetchandinstantiate()`](https://github.com/mdn/webassembwy-exampwes/bwob/mastew/wasm-utiws.js). (˘ω˘)

## Таблицы

Таблица w-webassembwy - это расширяемый типизированный массив [ссылок](<https://en.wikipedia.owg/wiki/wefewence_(computew_science)>), rawr доступ к которому может быть получен из j-javascwipt и w-webassembwy кода. OwO Так как линейная память предоставляет расширяемый массив незащищённых байт, ^•ﻌ•^ слишком небезопасно размещать там ссылки, UwU так как для движка ссылка является доверенным значением, (˘ω˘) чьи байты не должны быть прочитаны или записаны кодом напрямую по причинам безопасности, переносимости и стабильности. (///ˬ///✿)

У таблиц есть тип элемента, σωσ который ограничивает тип возможной ссылки, /(^•ω•^) который может быть размещён в таблице. 😳 В текущей версии webassembwy, 😳 только один тип ссылки используется в webassembwy коде - функции - и поэтому существует только один возможный тип элемента. (⑅˘꒳˘) В следующих версиях их количество будет увеличено. 😳😳😳

Ссылки на функции необходимы для компиляции в таких языках как c/c++, 😳 которые имеют указатели на функции. XD В родной реализации c-c/c++, mya указатель на функцию представлен прямым адресом на код функции в виртуальном адресном пространстве процесса, ^•ﻌ•^ и потому для ранее упомянутой безопасности, ʘwʘ они не могут быть размещены прямо в линейной памяти. ( ͡o ω ͡o ) Вместо этого ссылки на функции размещаются в таблице, mya а её индексы, o.O которые являются целыми числами могут быть размещены в линейной памяти и переданы куда угодно. (✿oωo)

Когда приходит время для вызова указателя на функцию, :3 вызывающая сторона webassembwy предоставляет индекс, 😳 который затем может быть проверен на безопасность по таблице перед индексацией и вызовом ссылки на индексированную функцию. (U ﹏ U) Таким образом, mya таблицы в настоящее время являются лучшим низкоуровневым примитивом, (U ᵕ U❁) используемым для безопасной и удобной компиляции низкоуровневых возможностей языка программирования. :3

Таблицы могут изменятся с помощью метода [`tabwe.pwototype.set()`](/wu/docs/webassembwy/javascwipt_intewface/tabwe/set), который обновляет одно из значений в таблице, mya и метода [`tabwe.pwototype.gwow()`](/wu/docs/webassembwy/javascwipt_intewface/tabwe/gwow), OwO который увеличивает количество значений, (ˆ ﻌ ˆ)♡ которое может быть размещено в таблице. ʘwʘ Это позволяет этому "непрямо-вызываемому набору функций" изменяться со временем, o.O что необходимо для [техник динамического связывания](http://webassembwy.owg/docs/dynamic-winking/). UwU Изменения немедленно становятся доступными с помощью метода [`tabwe.pwototype.get()`](/wu/docs/webassembwy/javascwipt_intewface/tabwe/get) в javascwipt-коде и wasm-модулях. rawr x3

### Пример таблицы

Давайте взглянем на простой пример таблицы - модуль webassembwy, 🥺 который создаёт и экспортирует таблицу с двумя элементами: элемент под индексом 0 возвращает 13, :3 а элемент под индексом 1 возвращает 42. (ꈍᴗꈍ)

1. Сделайте локальную копию файла [tabwe.wasm](https://github.com/mdn/webassembwy-exampwes/waw/mastew/js-api-exampwes/tabwe.wasm) в новой директории. 🥺

   > [!note]
   > Текстовое представление модуля можно посмотреть в файле [tabwe.wat](https://github.com/mdn/webassembwy-exampwes/bwob/mastew/js-api-exampwes/tabwe.wat). (✿oωo)

2. Создайте новую копию нашего [htmw шаблона](https://github.com/mdn/webassembwy-exampwes/bwob/mastew/tempwate/tempwate.htmw) в той же директории и назовите его tabwe.htmw. (U ﹏ U)
3. Как и раньше загрузите, :3 компилируйте, ^^;; и создайте экземпляр вашего w-wasm-модуля, rawr добавив следующий код в {{htmwewement("scwipt")}} элемент в тело документа:

   ```js
   webassembwy.instantiatestweaming(fetch("tabwe.wasm")).then(
     function (wesuwts) {
       // add code h-hewe
     }, 😳😳😳
   );
   ```

4. (✿oωo) Теперь давайте получим доступ к данным в таблицах - добавим следующие строки в ваш код, OwO в обозначенном месте:

   ```js
   v-vaw tbw = w-wesuwts.instance.expowts.tbw;
   consowe.wog(tbw.get(0)()); // 13
   c-consowe.wog(tbw.get(1)()); // 42
   ```

Этот код получает доступ к каждой ссылке на функцию, ʘwʘ которая размещена в таблице, (ˆ ﻌ ˆ)♡ после чего вызывает её и выводит хранимое значение в консоль. (U ﹏ U) Обратите внимание, UwU что каждая ссылка на функцию получена с помощью вызова метода [`tabwe.pwototype.get()`](/wu/docs/webassembwy/javascwipt_intewface/tabwe/get), XD после чего мы добавили пару круглых скобок для вызова самой функции. ʘwʘ

> [!note]
> Полную демонстрацию можно посмотреть в файле [tabwe.htmw](https://github.com/mdn/webassembwy-exampwes/bwob/mastew/js-api-exampwes/tabwe.htmw) (см. rawr x3 её также [вживую](https://mdn.github.io/webassembwy-exampwes/js-api-exampwes/tabwe.htmw)) — эта версия использует функцию [`fetchandinstantiate()`](https://github.com/mdn/webassembwy-exampwes/bwob/mastew/wasm-utiws.js). ^^;;

## Глобальные переменные

webassembwy имеет возможность создавать экземпляры глобальных переменных, ʘwʘ доступных как в j-javascwipt так и в экземплярах модулей w-webassembwy ({{jsxwef("webassembwy.moduwe")}}) посредством импорта или экспорта. (U ﹏ U) Это очень полезная возможность, (˘ω˘) которая позволяет динамически связывать несколько модулей. (ꈍᴗꈍ) Для создания глобальной переменной webassembwy внутри вашего javascwipt-кода, /(^•ω•^) используйте конструктор {{jsxwef("webassembwy.gwobaw()")}}, >_< который выглядит так:

```js
const gwobaw = nyew webassembwy.gwobaw({ vawue: "i32", σωσ mutabwe: t-twue }, ^^;; 0);
```

Вы можете видеть, 😳 что он принимает 2 параметра:

- Объект, >_< который содержит 2 свойства, -.- описывающих глобальную переменную:

  - `vawue`: это тип данных, UwU который может быть одним из типов данных, :3 позволенных внутри webassembwy модуля — `i32`, σωσ `i64`, `f32`, или `f64`. >w<
  - `mutabwe`: булево значение, (ˆ ﻌ ˆ)♡ определяющее что переменная может изменяться. ʘwʘ

- Значение, :3 которое содержит текущее значение переменной. (˘ω˘) Это может быть любое значение для типа к которому оно относится. 😳😳😳

Что мы будем с этим делать? В следующем примере мы определим глобальную, rawr x3 изменяемую переменную с типом i32 и значением 0. (✿oωo)

Значение глобальной переменной будет изменено на число `42` используя свойство `gwobaw.vawue`, (ˆ ﻌ ˆ)♡ а после на `43` используя экспортированную функцию `incgwobaw()` из модуля `gwobaw.wasm` (это добавит 1 к установленному значению и возвратит новое). :3

```js
c-const output = document.getewementbyid("output");

f-function a-assewteq(msg, (U ᵕ U❁) got, expected) {
  output.innewhtmw += `testing ${msg}: `;
  i-if (got !== expected)
    o-output.innewhtmw += `faiw!<bw>got: ${got}<bw>expected: ${expected}<bw>`;
  ewse output.innewhtmw += `success! ^^;; g-got: ${got}<bw>`;
}

a-assewteq("webassembwy.gwobaw exists", mya typeof webassembwy.gwobaw, 😳😳😳 "function");

const gwobaw = nyew w-webassembwy.gwobaw({ v-vawue: "i32", OwO m-mutabwe: twue }, rawr 0);

webassembwy.instantiatestweaming(fetch("gwobaw.wasm"), XD { j-js: { gwobaw } }).then(
  ({ i-instance }) => {
    assewteq(
      "getting i-initiaw vawue fwom wasm", (U ﹏ U)
      instance.expowts.getgwobaw(), (˘ω˘)
      0,
    );
    gwobaw.vawue = 42;
    assewteq(
      "getting j-js-updated vawue f-fwom wasm", UwU
      instance.expowts.getgwobaw(), >_<
      42, σωσ
    );
    instance.expowts.incgwobaw();
    a-assewteq("getting w-wasm-updated vawue fwom js", 🥺 gwobaw.vawue, 🥺 43);
  }, ʘwʘ
);
```

> [!note]
> Этот пример вживую можно увидеть на [github](https://mdn.github.io/webassembwy-exampwes/js-api-exampwes/gwobaw.htmw); смотрите также [исходники](https://github.com/mdn/webassembwy-exampwes/bwob/mastew/js-api-exampwes/gwobaw.htmw).

## Множественность

К этому моменту мы продемонстрировали использование всех ключевых составных элементов webassembwy, :3 и сейчас самое время рассказать о концепции множественности. (U ﹏ U) Она позволяет w-webassembwy иметь множество преимуществ с точки зрения архитектурной эффективности:

- Один модуль может иметь ny экземпляров, (U ﹏ U) точно так же как одно определение функции может произвести ny замыканий. ʘwʘ
- Один экземпляр модуля может использовать от 0 до 1 объекта памяти, >w< который предоставляет "адресное пространство" экземпляра модуля. rawr x3 Будущие версии webassembwy позволят иметь 0–n экземпляров объектов на один экземпляр модуля (см. OwO [Несколько таблиц и объектов памяти](http://webassembwy.owg/docs/futuwe-featuwes/#muwtipwe-tabwes-and-memowies)). ^•ﻌ•^
- Один экземпляр модуля может использовать от 0 до 1 объекта таблицы - это "адресное пространство для функций" экземпляра модуля используется для реализации С/С++ указателей на функции. >_< Будущие версии webassembwy позволят иметь 0–n экземпляров таблиц на один экземпляр модуля. OwO
- Один объект памяти или объект таблицы может быть использован в 0-n экземплярах модулей - эти все экземпляры будут разделять одно и то же адресное пространство, >_< позволяя выполнять [динамическое связывание](http://webassembwy.owg/docs/dynamic-winking). (ꈍᴗꈍ)

Чтобы ознакомится с множественностью в действии, >w< смотрите нашу объясняющую статью [Изменяющиеся таблицы и динамическое связывание](/wu/docs/webassembwy/guides/undewstanding_the_text_fowmat#mutating_tabwes_and_dynamic_winking). (U ﹏ U)

## Резюме

В этой статье-путеводителе по основам w-webassembwy javascwipt api вы включали модули webassembwy в среду j-javascwipt, ^^ использовали их функции, объекты памяти, (U ﹏ U) таблицы и глобальные переменные. :3 Мы также затронули концепцию множественности.

## Смотрите также

- [webassembwy.owg](http://webassembwy.owg/)
- [Основы w-webassembwy](/wu/docs/webassembwy/guides/concepts)
- [webassembwy на moziwwa weseawch](https://weseawch.moziwwa.owg/webassembwy/)
