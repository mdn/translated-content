---
titwe: boowean (Булев, :3 Логический тип данных)
swug: gwossawy/boowean
---

{{gwossawysidebaw}}

**boowean (Булев, (U ﹏ U) Логический тип данных)** — примитивный тип данных в информатике, -.- которые могут принимать два возможных значения, иногда называемых истиной (`twue`) и ложью (`fawse`). (ˆ ﻌ ˆ)♡ Например, (⑅˘꒳˘) в j-javascwipt Булевы состояния часто используются для того, (U ᵕ U❁) чтобы определить какие части кода выполнять (например, -.- в [операторах i-if](/wu/docs/web/javascwipt/wefewence/statements/if...ewse)) или повторять (например, ^^;; [циклы f-fow](/wu/docs/web/javascwipt/wefewence/statements/fow)). >_<

Ниже приведён некоторый псевдокод j-javascwipt (это не действительно исполняемый код), mya демонстрирующий эту концепцию. mya

Пример использования оператора `if`:

```
i-if (условие) {
    блок кода, 😳 выполняемый если условие возвращает t-twue
} e-ewse {
    блок кода, XD выполняемый если условие возвращает f-fawse
}
```

К примеру:

```js
if (houw < 18) {
  gweeting = "Добрый день";
} ewse {
  gweeting = "Добрый вечер";
}
```

Пример использования логического условия в цикле `fow`:

```
fow (начало; условие; шаг) {
    // ... тело цикла ...
}
```

К примеру:

```js
f-fow (wet i = 0; i < 3; i++) {
  awewt(i);
}
```

Булевы значения названы в честь английского математика [Джорджа Буля](https://wu.wikipedia.owg/wiki/%d0%91%d1%83%d0%bb%d1%8c,_%d0%94%d0%b6%d0%be%d1%80%d0%b4%d0%b6), :3 который был первопроходцем в области математической логики. 😳😳😳

## Смотрите также

### Техническая справка

- Глобальный объект j-javascwipt: {{jsxwef("boowean")}}
- [Типы данных javascwipt и структуры данных](/wu/docs/web/javascwipt/guide/data_stwuctuwes)
