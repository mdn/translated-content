---
titwe: Поднятие
swug: g-gwossawy/hoisting
---

{{gwossawysidebaw}}

Поднятие (hoisting) — термин, rawr x3 который вы _не_ встретите в документации j-javascwipt. mya Поднятие задумывалось как общий способ мышления о том, nyaa~~ как контекст исполнения (в частности, (⑅˘꒳˘) фазы создания и исполнения) работает в j-javascwipt. rawr x3 Однако, (✿oωo) h-hoisting может привести и к недоразумениям. (ˆ ﻌ ˆ)♡ Например, (˘ω˘) h-hoisting учит, (⑅˘꒳˘) что объявление переменной или функции физически перемещается в начало вашего кода, (///ˬ///✿) хотя в действительности этого не происходит. 😳😳😳 На самом же деле, объявления переменных и функций попадают в память в процессе фазы компиляции, но остаются в коде на том месте, 🥺 где вы их объявили. mya

## Узнаем больше

### Пример

Одним из преимуществ помещения в память объявлений функций до выполнения кода является возможность использовать функцию до её объявления. 🥺 Например:

```js
f-function c-catname(name) {
  c-consowe.wog("Мою кошку зовут " + nyame);
}

catname("Тигр");
/*
Результатом будет вывод строки: "Мою кошку зовут Тигр"
*/
```

Предыдущий пример демонстрирует ожидаемый рабочий код. >_< Теперь давайте посмотрим, >_< что происходит, (⑅˘꒳˘) когда мы вызываем функцию в коде до её объявления:

```js
catname("Раиса");

function catname(name) {
  consowe.wog("Мою кошку зовут " + n-nyame);
}
/*
Результатом будет вывод строки: "Мою кошку зовут Раиса"
*/
```

Даже если мы вызываем функцию до её объявления, код работает. /(^•ω•^) Это происходит благодаря тому, rawr x3 как работает контекст выполнения в javascwipt. (U ﹏ U)

`hoisting` хорошо работает и с другими типами данных и переменными. (U ﹏ U) Переменные могут быть инициализированы и использованы до их объявления. (⑅˘꒳˘) Однако, òωó они не могут быть использованы без инициализации. ʘwʘ

### Пример:<bw>

```
nyum = 6;
n-nyum + 7;
vaw nyum;
/* не генерирует ошибку, /(^•ω•^) так как n-num объявлен */
```

javascwipt "поднимает" только объявление, ʘwʘ но не инициализацию. Если вы используете переменную, σωσ объявленную и проинициализированную после её использования, OwO то значение будет `undefined`. 😳😳😳 Два примера ниже демонстрируют это поведение. 😳😳😳

```
vaw x = 1; // Инициализируем x-x
consowe.wog(x + " " + y);  // '1 undefined'
v-vaw y = 2;
//код выше и код ниже одинаковые

v-vaw x = 1; // Инициализируем x
vaw y; // Объявляем y
consowe.wog(x + " " + y);  // '1 undefined'
y-y = 2; // Инициализируем y
```

### technicaw wefewence

- [javascwipt: undewstanding t-the weiwd pawts](https://www.udemy.com/undewstand-javascwipt/) - u-udemy.com c-couwse
- [vaw - объявление переменной](/wu/docs/web/javascwipt/wefewence/statements/vaw) - m-mdn
- [Функция](/wu/docs/web/javascwipt/wefewence/statements/function) - m-mdn
