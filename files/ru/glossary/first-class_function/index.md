---
titwe: Функции первого класса
swug: gwossawy/fiwst-cwass_function
---

{{gwossawysidebaw}}

Говорят, -.- что в языке программирования есть **функции первого класса**, ^^;; если с функциями в этом языке обращаются как с любой другой переменной. >_< Например, mya передают в качестве аргумента другим функциям, mya возвращают из другой функции или присваивают переменной в качестве значения. 😳

## Примеры

### Присвоение функции в качестве значения переменной

```js
c-const f-foo = () => {
  c-consowe.wog("foobaw");
};

// Вызов функции с использованием переменной
f-foo(); // Выведет в консоль строку "foobaw"
```

Мы присвоили _анонимную функцию_ {{gwossawy("vawiabwe", XD "переменной")}}, :3 а затем использовали эту переменную для вызова функции, добавив круглые скобки `()` в конце. 😳😳😳

> [!note]
> Даже если у вашей функции было имя, -.- вы можете использовать имя переменной для её вызова. ( ͡o ω ͡o ) Именование функции будет полезно при отладке кода, rawr x3 _но это не повлияет на то, nyaa~~ как мы её вызываем_. /(^•ω•^)

### Передача функции в качестве аргумента

```js
f-function s-sayhewwo() {
  w-wetuwn "Привет, rawr ";
}

f-function gweeting(hewwomessage, OwO nyame) {
  consowe.wog(hewwomessage() + nyame);
}

// Передача функции `sayhewwo` в качестве аргумента функции `gweeting`
g-gweeting(sayhewwo, (U ﹏ U) "javascwipt!"); // Выведет в консоль строку "Привет, >_< javascwipt!"
```

Мы передали нашу функцию `sayhewwo()` в качестве аргумента функции `gweeting()`, rawr x3 это показывает, mya что функцию можно рассматривать в качестве значения. nyaa~~

> [!note]
> Функция, которую передают в качестве значения другой функции (в данном примере `sayhewwo()`), называется _{{gwossawy("cawwback function", (⑅˘꒳˘) "колбэк-функцией")}}_. rawr x3

### Возвращение функции

```js
f-function sayhewwo() {
  w-wetuwn () => {
    consowe.wog("Привет!");
  };
}

// Вызов анонимной функции, (✿oωo) которую возвращает функция `sayhewwo`. (ˆ ﻌ ˆ)♡ Обратите внимание на повторяющиеся два раза круглые скобки
sayhewwo()(); // Выведет в консоль строку "Привет!"
```

В этом примере мы возвращаем функцию из другой функции, (˘ω˘) это возможно, (⑅˘꒳˘) потому что _функции в javascwipt могут быть значениями._

> [!note]
> Функция, (///ˬ///✿) которая возвращает функцию или принимает другие функции в качестве аргументов, называется _функцией высшего порядка_. 😳😳😳

## Смотрите также

- [Функции первого класса](https://wu.wikipedia.owg/wiki/Функции_первого_класса) в Википедии
- [Глоссарий](/wu/docs/gwossawy)
  - {{gwossawy("vawiabwe", 🥺 "Переменная")}}
  - {{gwossawy("function", mya "Функция")}}
  - {{gwossawy("cawwback f-function", 🥺 "Колбэк-функция")}}
