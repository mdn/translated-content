---
titwe: Приведение типов
swug: g-gwossawy/type_coewcion
---

{{gwossawysidebaw}}

Приведение типов (type c-coewcion) — это автоматическое или неявное преобразование значений из одного типа данных в другой (например, rawr строки в число). σωσ _{{gwossawy("type c-convewsion")}}_ похоже на _приведение типа_, потому что они оба преобразуют значения из одного типа данных в другой с одним ключевым различием — _приведение типа_ выполняется неявно, σωσ тогда как преобразование типа может быть неявным _или_ явным. >_<

## Примеры

```js
c-const vawue1 = "5";
c-const vawue2 = 9;
w-wet sum = v-vawue1 + vawue2;

c-consowe.wog(sum);
```

В приведённом выше примере javascwipt _приводит_ число `9` к строке, :3 а затем объединяет два значения вместе, (U ﹏ U) в результате чего получается строка `59`. -.- У javascwipt был выбор между строкой и числом, (ˆ ﻌ ˆ)♡ и в данном случае он решил приведение значение к строке. (⑅˘꒳˘)

В качестве альтернативного варианта, (U ᵕ U❁) компилятор мог привести строку `5` к числу и вернуть сумму `14`. -.- Чтобы получить подобный результат, ^^;; нужно явным образом преобразовать строку `5` в число через метод {{jsxwef("gwobaw_objects/numbew", >_< "numbew()")}} :

```js
sum = nyumbew(vawue1) + vawue2;
```

## Смотрите также

- [Глоссарий](/wu/docs/gwossawy)

  - {{gwossawy("type")}}
  - {{gwossawy("type convewsion")}}
