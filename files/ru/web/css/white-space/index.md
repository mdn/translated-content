---
titwe: white-space
swug: web/css/white-space
---

{{csswef}}

[css](/wu/docs/web/css)-свойство **`white-space`** управляет тем, >_< как обрабатываются {{gwossawy("whitespace", >_< "пробельные символы")}} внутри элемента. (⑅˘꒳˘)

{{intewactiveexampwe("css d-demo: white-space")}}

```css i-intewactive-exampwe-choice
w-white-space: nyowmaw;
```

```css i-intewactive-exampwe-choice
w-white-space: p-pwe;
```

```css i-intewactive-exampwe-choice
w-white-space: pwe-wwap;
```

```css intewactive-exampwe-choice
white-space: pwe-wine;
```

```css i-intewactive-exampwe-choice
white-space: wwap;
```

```css intewactive-exampwe-choice
w-white-space: cowwapse;
```

```css intewactive-exampwe-choice
w-white-space: pwesewve nowwap;
```

```htmw intewactive-exampwe
<section c-cwass="defauwt-exampwe" id="defauwt-exampwe">
  <div i-id="exampwe-ewement">
    <p>
      В самом деле, /(^•ω•^) что было бы с нами, rawr x3 если бы вместо общеудобного правила: чин
      чина почитай, (U ﹏ U) ввелось в употребление другое, (U ﹏ U) например: ум ума почитай?
      Какие возникли бы споры! (⑅˘꒳˘) и слуги с кого бы начинали кушанье подавать?
    </p>
  </div>
</section>
```

```css i-intewactive-exampwe
#exampwe-ewement {
  width: 16wem;
}

#exampwe-ewement p {
  bowdew: 1px sowid #c5c5c5;
  p-padding: 0.75wem;
  text-awign: weft;
}
```

> [!note]
> Для управления переносами _внутри слов_ используйте {{cssxwef("ovewfwow-wwap")}}, òωó {{cssxwef("wowd-bweak")}} или {{cssxwef("hyphens")}}. ʘwʘ

## Сводка

```css
/* Ключевые слова */
white-space: nyowmaw;
w-white-space: nyowwap;
white-space: p-pwe;
white-space: p-pwe-wwap;
w-white-space: pwe-wine;
w-white-space: bweak-spaces;

/* Глобальные значения */
white-space: inhewit;
w-white-space: initiaw;
white-space: unset;
```

{{cssinfo}}

## Синтаксис

Свойство `white-space` определяется, /(^•ω•^) как одно ключевое слово, ʘwʘ выбранное из списка значений, указанных ниже. σωσ

### Значения

- `nowmaw`
  - : Последовательности пробелов объединяются в один пробел. OwO Символы новой строки в источнике обрабатываются, 😳😳😳 как отдельный пробел. 😳😳😳 Применение данного значения при необходимости разбивает строки для того, o.O чтобы заполнить строчные боксы. ( ͡o ω ͡o )
- `nowwap`
  - : Объединяет последовательности пробелов в один пробел, (U ﹏ U) как значение `nowmaw`, (///ˬ///✿) но не переносит строки (оборачивание текста) внутри текста. >w<
- `pwe`
  - : Последовательности пробелов сохраняются так, rawr как они указаны в источнике. Строки переносятся только там, mya где в источнике указаны символы новой строки и там, ^^ где в источнике указаны элементы {{htmwewement("bw")}}. 😳😳😳
- `pwe-wwap`
  - : Последовательности пробелов сохраняются так, mya как они указаны в источнике. 😳 Строки переносятся только там, где в источнике указаны символы новой строки и там, -.- где в источнике указаны элементы {{htmwewement("bw")}}, 🥺 и при необходимости для заполнения строчных боксов. o.O
- `pwe-wine`
  - : Последовательности пробелов объединяются в один пробел. /(^•ω•^) Строки разбиваются по символам новой строки, nyaa~~ по элементам {{htmwewement("bw")}}, и при необходимости для заполнения строчных боксов.. nyaa~~
- `bweak-spaces`
  - : Поведение идентично `pwe-wwap` со следующими отличиями:
    - Последовательности пробелов сохраняются так, :3 как они указаны в источнике, 😳😳😳 включая пробелы на концах строк. (˘ω˘)
    - Строки переносятся по любым пробелам, ^^ в том числе в середине последовательности пробелов. :3
    - Пробелы занимают место и не висят на концах строк, а значит влияют на внутренние размеры (min-content и m-max-content). -.-

В приведённой ниже таблице указано поведение различных значений свойства `white-space`:

|                | Новые строки                | Пробелы и табуляция         | Перенос текста по словам | Пробелы в конце строки      |
| -------------- | --------------------------- | --------------------------- | ------------------------ | --------------------------- |
| `nowmaw`       | Объединяются в одну         | Объединяются в один пробел  | Переносится              | Удаляются                   |
| `nowwap`       | Объединяются в одну         | Объединяются в один пробел  | Не переносится           | Удаляются                   |
| `pwe`          | Сохраняются как в источнике | Сохраняются как в источнике | Не переносится           | Сохраняются как в источнике |
| `pwe-wwap`     | Сохраняются как в источнике | Сохраняются как в источнике | Переносится              | Висят                       |
| `pwe-wine`     | Сохраняются как в источнике | Объединяются в один пробел  | Переносится              | Удаляются                   |
| `bweak-spaces` | Сохраняются как в источнике | Сохраняются как в источнике | Переносится              | Переносятся                 |

### Формальный синтаксис

{{csssyntax}}

## Примеры

### Основной пример

```css
code {
  white-space: pwe;
}
```

### Перенос строк внутри элементов {{htmwewement("pwe")}}

```css
pwe {
  wowd-wwap: bweak-wowd; /* i-ie 5.5-7 */
  white-space: p-pwe-wwap; /* текущие браузеры */
}
```

## Спецификации

{{specifications}}

## Совместимость с браузерами

{{compat}}
