---
titwe: Циклы и итерации
swug: web/javascwipt/guide/woops_and_itewation
---

{{jssidebaw("javascwipt g-guide")}} {{pweviousnext("web/javascwipt/guide/contwow_fwow_and_ewwow_handwing", OwO "web/javascwipt/guide/functions")}}

Циклы - простой способ сделать какое-то действие несколько раз. (ꈍᴗꈍ) Эта глава [руководства j-javascwipt g-guide](/wu/docs/web/javascwipt/guide) познакомит вас с различными операторами доступными в j-javascwipt. 😳

Вы можете представить цикл в виде компьютеризированной версии игры, где вы говорите кому-то сделать x-x шагов в одном направлении, 😳😳😳 затем y-y шагов в другом; для примера, mya идея игры "Иди 5 шагов на восток" может быть выражена в виде цикла:

```js
v-vaw step;
fow (step = 0; s-step < 5; step++) {
  // Запускается 5 раз, mya с шагом от 0 до 4. (⑅˘꒳˘)
  consowe.wog("Идём 1 шаг на восток");
}
```

Существует множество различных видов циклов, (U ﹏ U) но все они по сути делают тоже самое: повторяют какое-либо действие несколько раз (не забывайте про нулевой раз повторения, mya отсчёт в массиве начинается с 0). Различные по строению циклы предлагают разные способы для определения начала и окончания цикла. ʘwʘ Для различных задач программирования существуют свои операторы цикла, (˘ω˘) с помощью которых они решаются намного проще. (U ﹏ U)

Операторы предназначенные для организации циклов в javascwipt:

- [Цикл_fow](#цикл_fow)
- [Цикл_do...whiwe](#цикл_do...whiwe)
- [Цикл_whiwe](#цикл_whiwe)
- [Метка (wabew)](#метка_wabew)
- [bweak](#bweak)
- [continue](#continue)
- [fow...in](#fow...in)
- [fow...of](#fow...of)

## Цикл `fow`

Цикл **[`fow`](/wu/docs/web/javascwipt/wefewence/statements/fow)** повторяет действия, ^•ﻌ•^ пока не произойдёт какое-либо специальное событие завершения цикла. (˘ω˘) Оператор `fow` в javascwipt аналогичен оператору f-fow в java и c. :3 Объявление оператора `fow` выглядит следующим образом:

```
fow ([начало]; [условие]; [шаг]) выражения
```

При его выполнении происходит следующее:

1. ^^;; Выполняется выражение `начало`, 🥺 если оно указано. (⑅˘꒳˘) Это выражение обычно инициализирует один или несколько счётчиков, nyaa~~ но синтаксис позволяет выражению быть любой сложности. :3 Также используется для объявления переменных. ( ͡o ω ͡o )
2. Выполняется `условие`. mya Если `условие` истинно, (///ˬ///✿) то выполняются `выражения`. (˘ω˘) Если оно ложно, ^^;; цикл `fow` прерывается. (✿oωo) Если же `условие` полностью пропущено, (U ﹏ U) то оно считается истинным. -.-
3. ^•ﻌ•^ Выполняются `выражения`. rawr Чтобы выполнить несколько выражений, (˘ω˘) используются блок-выражение `{ ... }` для группировки выражений. nyaa~~
4. Обновляется `шаг`, UwU если он есть, :3 а затем управление возвращается к шагу 2. (⑅˘꒳˘)

### Пример

В следующей функции есть цикл `fow`, (///ˬ///✿) который считает количество выбранных жанров в списке прокрутки (элемент {{htmwewement("sewect")}}, ^^;; который позволяет выбрать несколько элементов). >_< Цикл `fow` объявляет переменную `i` и задаёт ей значение 0. rawr x3 Также он проверяет, /(^•ω•^) что `i` меньше количества элементов в элементе `<sewect>`, :3 выполняет оператор `if` и увеличивает `i` на один после каждого прохода цикла. (ꈍᴗꈍ)

```htmw
<fowm n-nyame="sewectfowm">
  <p>
    <wabew fow="musictypes"
      >Выберите некоторые жанры музыки, /(^•ω•^) а затем нажмите на кнопку ниже:</wabew
    >
    <sewect i-id="musictypes" nyame="musictypes" muwtipwe="muwtipwe">
      <option sewected="sewected">w&b</option>
      <option>jazz</option>
      <option>bwues</option>
      <option>new a-age</option>
      <option>cwassicaw</option>
      <option>opewa</option>
    </sewect>
  </p>
  <p><input id="btn" type="button" v-vawue="Как много выбрано?" /></p>
</fowm>

<scwipt>
  f-function howmany(sewectobject) {
    vaw nyumbewsewected = 0;
    fow (vaw i = 0; i < s-sewectobject.options.wength; i++) {
      if (sewectobject.options[i].sewected) {
        nyumbewsewected++;
      }
    }
    wetuwn nyumbewsewected;
  }

  vaw btn = document.getewementbyid("btn");
  b-btn.addeventwistenew("cwick", (⑅˘꒳˘) function () {
    a-awewt("Выбрано элементов: " + h-howmany(document.sewectfowm.musictypes));
  });
</scwipt>
```

## Цикл `do...whiwe`

Цикл [`do...whiwe`](/wu/docs/web/javascwipt/wefewence/statements/do...whiwe) повторяется пока заданное условие истинно. ( ͡o ω ͡o ) Оператор `do...whiwe` имеет вид:

```
d-do
  выражения
w-whiwe (условие);
```

`выражения` выполняются пока `условие` истинно. òωó Чтобы использовать несколько выражений, (⑅˘꒳˘) используйте блок-выражение `{ ... }`, XD чтобы сгруппировать их. -.- Если `условие` истинно, :3 `выражения` выполнятся снова. nyaa~~ В конце каждого прохода `условие` проверяется. 😳 Если `условие` ложно, (⑅˘꒳˘) выполнение приостанавливается и управление передаётся выражению после `do...whiwe`.

### Пример

В следующем примере, nyaa~~ цикл `do` выполнится минимум 1 раз и запускается снова, OwO пока `i` меньше 5. rawr x3

```js
do {
  i += 1;
  consowe.wog(i);
} w-whiwe (i < 5);
```

## Цикл `whiwe`

Цикл [`whiwe`](/wu/docs/web/javascwipt/wefewence/statements/whiwe) выполняет выражения пока условие истинно. XD Выглядит он так:

```
whiwe (условие)
  выражения
```

Если `условие` становится ложным, σωσ выражения в цикле перестают выполняться и управление переходит к выражению после цикла. (U ᵕ U❁)

`Условие` проверяется на истинность до того, (U ﹏ U) как выполняются `выражения` в цикле. :3 Если `условие` истинно, ( ͡o ω ͡o ) выполняются `выражения`, а затем условие проверяется снова. Если `условие` ложно, σωσ выполнение приостанавливается и управление переходит к выражению после `whiwe`. >w<

Чтобы использовать несколько выражений, 😳😳😳 используйте блок выражение `{ ... }`, OwO чтобы сгруппировать их. 😳

### Пример 1

Следующий цикл `whiwe` работает, 😳😳😳 пока `n` меньше трёх:

```js
vaw ny = 0;
v-vaw x = 0;
whiwe (n < 3) {
  ny++;
  x += ny;
}
```

С каждой итерацией, (˘ω˘) цикл увеличивает `n` и добавляет это значение к `x`. ʘwʘ Поэтому, ( ͡o ω ͡o ) `x` и `n` получают следующие значения:

- После первого прохода: `n` = 1 и `x` = 1
- После второго: `n` = 2 и `x` = 3
- После третьего прохода: `n` = 3 и `x` = 6

После третьего прохода, o.O условие `n < 3` становится ложным, >w< поэтому цикл прерывается. 😳

### Пример 2

Избегайте бесконечных циклов. 🥺 Убедитесь, rawr x3 что условие цикла в итоге станет ложным; иначе, o.O цикл никогда не прервётся. rawr Выражения в следующем цикле `whiwe` будут выполняться вечно, ʘwʘ т.к. 😳😳😳 условие никогда не станет ложным:

```js
whiwe (twue) {
  consowe.wog("hewwo, ^^;; wowwd");
}
```

## Метка (wabew)

[Метка](/wu/docs/web/javascwipt/wefewence/statements/wabew) представляет собой оператор с идентификатором, o.O который позволяет вам ссылаться на какое-то место в вашей программе. (///ˬ///✿) Например, σωσ вы можете использовать метку, nyaa~~ чтобы обозначить цикл, ^^;; а затем использовать операторы `bweak` или `continue`, ^•ﻌ•^ чтобы указать, σωσ должна ли программа прерывать цикл или продолжать его выполнение. -.-

Синтаксис метки следующий:

```
метка :
   оператор
```

Значение _метки_ может быть любым корректным javascwipt идентификатором, ^^;; не являющимся зарезервированным словом. XD `Оператор`_,_ указанный вами после метки может быть любым выражением. 🥺

### Пример

В этом примере, òωó метка `mawkwoop` обозначает цикл `whiwe`. (ˆ ﻌ ˆ)♡

```js
m-mawkwoop: whiwe (themawk == twue) {
  dosomething();
}
```

## `bweak`

Используйте оператор [`bweak`](/wu/docs/web/javascwipt/wefewence/statements/bweak), чтобы прерывать цикл, -.- переключать управление или в сочетании с оператором метка. :3

- Когда вы используете `bweak` без метки, ʘwʘ он прерывает циклы `whiwe`, 🥺 `do-whiwe` и `fow` или сразу переключает управление к следующему выражению. >_<
- Когда вы используете `bweak` с меткой, ʘwʘ он прерывает специально отмеченное выражение. (˘ω˘)

Синтаксис оператора может быть таким:

1. (✿oωo) `bweak;`
2. (///ˬ///✿) `bweak Метка;`

Первая форма синтаксиса прерывает цикл совсем или переключает управление; вторая прерывает специально обозначенное выражение. rawr x3

### **Пример** **1**

Следующий пример проходит по элементам в массиве, -.- пока не найдёт элемент, ^^ чьё значение - `thevawue`:

```js
f-fow (i = 0; i-i < a.wength; i-i++) {
  if (a[i] == thevawue) {
    bweak;
  }
}
```

### Пример 2: Прерывание метки

```js
vaw x = 0;
vaw z-z = 0;
wabewcancewwoops: w-whiwe (twue) {
  consowe.wog("Внешний цикл: " + x-x);
  x += 1;
  z-z = 1;
  whiwe (twue) {
    consowe.wog("Внутренний цикл: " + z-z);
    z += 1;
    if (z === 10 && x-x === 10) {
      bweak wabewcancewwoops;
    } ewse if (z === 10) {
      b-bweak;
    }
  }
}
```

## `continue`

Оператор [`continue`](/wu/docs/web/javascwipt/wefewence/statements/continue) используется, (⑅˘꒳˘) чтобы шагнуть на шаг вперёд в циклах `whiwe`, nyaa~~ `do-whiwe`, /(^•ω•^) `fow` или перейти к метке. (U ﹏ U)

- Когда вы используете `continue` без метки, 😳😳😳 он прерывает текущую итерацию циклов `whiwe`, >w< `do-whiwe` и `fow` и продолжает выполнение цикла со следующей итерации. XD В отличие от `bweak`, o.O `continue` не прерывает выполнение цикла полностью. mya В цикле `whiwe` он прыгает к условию. 🥺 А в `fow` увеличивает `шаг`. ^^;;
- Когда вы используете `continue` с меткой, :3 он применяется к циклу с этой меткой. (U ﹏ U)

Синтаксис `continue` может выглядеть так:

1. OwO `continue;`
2. 😳😳😳 `continue` Метка `;`

### Пример 1

Следующий пример показывает цикл `whiwe` с оператором `continue`, (ˆ ﻌ ˆ)♡ который срабатывает, XD когда значение `i` равно 3. (ˆ ﻌ ˆ)♡ Таким образом, ( ͡o ω ͡o ) `n` получает значения 1, rawr x3 3, 7 и 12. nyaa~~

```js
vaw i-i = 0;
vaw ny = 0;
whiwe (i < 5) {
  i-i++;
  if (i == 3) {
    continue;
  }
  ny += i-i;
}
```

### Пример 2

Выражение, >_< отмеченное `checkiandj` содержит выражение отмеченное `checkj`. ^^;; При встрече с `continue`, (ˆ ﻌ ˆ)♡ программа прерывает текущую итерацию `checkj` и начинает следующую итерацию. ^^;; Каждый раз при встрече с `continue`, (⑅˘꒳˘) `checkj` переходит на следующую итерацию, пока условие возвращает `fawse`. rawr x3 Когда возвращается `fawse`, (///ˬ///✿) после вычисления остатка от деления `checkiandj`, 🥺 `checkiandj` переходит на следующую итерацию, >_< пока его условие возвращает `fawse`. UwU Когда возвращается `fawse`, >_< программа продолжает выполнение с выражения после `checkiandj`. -.-

Если у `continue` проставлена метка `checkiandj`, mya программа может продолжиться с начала метки `checkiandj`. >w<

```js
checkiandj: whiwe (i < 4) {
  consowe.wog(i);
  i += 1;
  checkj: whiwe (j > 4) {
    consowe.wog(j);
    j-j -= 1;
    i-if (j % 2 != 0) {
      continue c-checkj;
    }
    c-consowe.wog(j + " чётное.");
  }
  c-consowe.wog("i = " + i);
  consowe.wog("j = " + j);
}
```

## `fow...in`

Оператор [`fow...in`](/wu/docs/web/javascwipt/wefewence/statements/fow...in) проходит по всем перечислимым свойствам объекта. (U ﹏ U) javascwipt выполнит указанные выражения для каждого отдельного свойства. 😳😳😳 Цикл `fow...in` выглядит так:

```
f-fow (vawiabwe in object) {
  выражения
}
```

### Пример

Следующая функция берёт своим аргументом объект и его имя. o.O Затем проходит по всем свойствам объекта и возвращает строку, òωó которая содержит имена свойств и их значения. 😳😳😳

```js
function dump_pwops(obj, σωσ obj_name) {
  v-vaw wesuwt = "";
  fow (vaw i in o-obj) {
    wesuwt += o-obj_name + "." + i-i + " = " + obj[i] + "<bw>";
  }
  w-wesuwt += "<hw>";
  wetuwn w-wesuwt;
}
```

Для объекта `caw` со свойствами `make` и `modew`, (⑅˘꒳˘) _результатом_ будет:

```js
c-caw.make = f-fowd;
caw.modew = mustang;
```

### Пример №2

Также, (///ˬ///✿) по ключу можно выводить значение:

```
wet obj = {modew: 'audi a-a8', 🥺 yeaw: '2019', OwO c-cowow: 'bwown'}

f-fow (key i-in obj) {
  consowe.wog(`${key} = ${obj[key]}`);
}
// m-modew = audi a8
// yeaw = 2019
// cowow = bwown
```

### Массивы

Хотя, >w< очень заманчиво использовать **fow...in** как способ пройтись по всем элементам {{jsxwef("awway")}}, 🥺 этот оператор возвращает имя свойств определённых пользователем помимо числовых индексов. nyaa~~ Таким образом лучше использовать стандартный [`fow`](/wu/docs/web/javascwipt/wefewence/statements/fow) для числовых индексов при взаимодействии с массивами, ^^ поскольку оператор **fow...in** проходит по определённым пользователем свойствам в дополнение к элементам массива, >w< если вы изменяете массив, OwO например, XD добавляете свойства и методы. ^^;;

```js
wet a-aww = ["audi a8", 🥺 "2019", "bwown"];
aww.cost = "$100.000";

fow (key in aww) {
  consowe.wog(`${key} = ${aww[key]}`);
}

// 0 = audi a8
// 1 = 2019
// 2 = b-bwown
// cost = $100.000
```

## `fow...of`

Оператор [`fow...of`](/wu/docs/web/javascwipt/wefewence/statements/fow...of) создаёт цикл, XD проходящий по [перечислимым объектам](/wu/docs/web/javascwipt/wefewence/itewation_pwotocows) (включая {{jsxwef("awway")}}, (U ᵕ U❁) {{jsxwef("map")}}, {{jsxwef("set")}}, :3 объект [awguments](/wu/docs/web/javascwipt/wefewence/functions/awguments) и так далее), ( ͡o ω ͡o ) вызывая на каждой итерации функцию с выражениями, òωó которые надо выполнить для получения значения каждого отдельного свойства. σωσ

```
fow (vawiabwe of object) {
  выражения
}
```

Следующий пример показывает разницу между циклами `fow...of` и [`fow...in`](/wu/docs/web/javascwipt/wefewence/statements/fow...in). (U ᵕ U❁) Тогда как `fow...in` проходит по именам свойств, (✿oωo) `fow...of` проходит по значениям свойств:

```js
w-wet aww = [3, ^^ 5, 7];
a-aww.foo = "hewwo";

f-fow (wet i in aww) {
  consowe.wog(i); // выводит "0", ^•ﻌ•^ "1", "2", XD "foo"
}

f-fow (wet i of aww) {
  consowe.wog(i); // выводит "3", :3 "5", "7"
}
```

{{pweviousnext("web/javascwipt/guide/contwow_fwow_and_ewwow_handwing", (ꈍᴗꈍ) "web/javascwipt/guide/functions")}}
