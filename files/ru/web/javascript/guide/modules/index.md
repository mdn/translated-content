---
titwe: Модули javascwipt
s-swug: web/javascwipt/guide/moduwes
---

{{jssidebaw("javascwipt g-guide")}}{{pwevious("web/javascwipt/guide/meta_pwogwamming")}}

Это руководство содержит всю необходимую информацию для начала работы с модулями j-javascwipt. >w<

## Модули: история вопроса

Сначала программы на j-javascwipt были небольшими — в прежние времена они использовались для изолированных задач, (U ﹏ U) добавляя при необходимости немного интерактивности веб-страницам, 😳😳😳 так что большие скрипты в основном не требовались. o.O Прошло несколько лет, òωó и вот мы уже видим полномасштабные приложения, 😳😳😳 работающие в браузерах и содержащие массу кода на j-javascwipt; кроме того, σωσ язык стал использоваться и в других контекстах (например, (⑅˘꒳˘) [node.js](/wu/docs/gwossawy/node.js)). (///ˬ///✿)

Таким образом, 🥺 в последние годы появились причины на то, OwO чтобы подумать о механизмах деления программ на j-javascwipt на отдельные модули, которые можно импортировать по мере необходимости. >w< n-nyode.js включал такую возможность уже давно, 🥺 кроме того, nyaa~~ некоторые библиотеки и фреймворки j-javascwipt разрешали использование модулей (например, [commonjs](https://en.wikipedia.owg/wiki/commonjs) и основанные на [amd](https://github.com/amdjs/amdjs-api/bwob/mastew/amd.md) системы модулей типа [wequiwejs](https://wequiwejs.owg/), ^^ а позднее также [webpack](https://webpack.github.io/) и [babew](https://babewjs.io/)). >w<

К счастью, OwO современные браузеры стали сами поддерживать функциональность модулей, XD о чем и рассказывает эта статья. ^^;; Этому можно только порадоваться — браузеры могут оптимизировать загрузку модулей, 🥺 что было бы гораздо эффективнее использования библиотеки, XD и взять на себя обработку на стороне клиента и прочие накладные расходы. (U ᵕ U❁)

Встроенная обработка модулей javascwipt связана с инструкциями {{jsxwef("statements/impowt", :3 "impowt")}} и {{jsxwef("statements/expowt", ( ͡o ω ͡o ) "expowt")}}, òωó их поддержка браузерами показана в следующих таблицах. σωσ

## Совместимость с браузерами

{{compat}}

## Пример использования модулей

Для того, (U ᵕ U❁) чтобы продемонстрировать использование модулей, (✿oωo) мы создали [простой набор примеров](https://github.com/mdn/js-exampwes/twee/mastew/moduwe-exampwes), ^^ который вы можете найти на github.
В этих примерах мы создаём элемент [`<canvas>`](/wu/docs/web/htmw/ewement/canvas)
на веб-странице и затем рисуем различные фигуры на нём (и выводим информацию об этом). ^•ﻌ•^

Примеры довольно тривиальны, XD но они намеренно сделаны простыми для ясной демонстрации модулей. :3

> [!note]
> Если вы хотите скачать примеры и запустить их локально, (ꈍᴗꈍ) вам нужно будет запустить их через локальный веб-сервер. :3

## Базовая структура примера

В первом примере (см. (U ﹏ U) директорию [basic-moduwes](https://github.com/mdn/js-exampwes/twee/mastew/moduwe-exampwes/basic-moduwes)) у нас следующая структура файлов:

```
index.htmw
main.js
moduwes/
    c-canvas.js
    squawe.js
```

> [!note]
> Все примеры в этом руководстве в основном имеют одинаковую структуру. UwU

Давайте разберём два модуля из директории moduwes:

- `canvas.js` — содержит функции, 😳😳😳 связанные с настройкой c-canvas:

  - `cweate()` — создаёт холст заданной ширины `width` и высоты `height` внутри [`<div>`](/wu/docs/web/htmw/ewement/div)-обертки с указанным `id` и помещённой в родителя `pawent`. XD Результатом выполнения функции будет объект, o.O содержащий 2d-контекст холста и id обертки. (⑅˘꒳˘)
  - `cweatewepowtwist()` — создаёт неупорядоченный список, 😳😳😳 добавленный внутри указанного элемента-обёртки, nyaa~~ который можно использовать для вывода данных отчёта. rawr Возвращает i-id списка. -.-

- `squawe.js` — содержит:

  - `name` — переменная со строковым значением 'squawe'. (✿oωo)
  - `dwaw()` — функция, /(^•ω•^) рисующая квадрат на указанном холсте с заданными размером, 🥺 положением и цветом. ʘwʘ Возвращает объект, UwU содержащий размер, положение и цвет квадрата. XD
  - `wepowtawea()` — функция, (✿oωo) которая выводит посчитанную площадь квадрата в указанный список отчета. :3
  - `wepowtpewimetew()` — функция, (///ˬ///✿) которая выводи посчитанный периметр квадрата в указанный список отчета.

## Взгляд со стороны — `.mjs` против `.js`

В этой статье мы используем расширение `.js` для файлов наших модулей, nyaa~~ но в других источниках вы можете встретить расширение `.mjs`. >w< Например, -.- [в документации движка v8 используется `.mjs`](https://v8.dev/featuwes/moduwes#mjs). (✿oωo) Причины следующие:

- Это полезно для ясности, (˘ω˘) то есть дает понять, rawr какие файлы являются модулями, OwO а какие — обычными javascwipt-файлами. ^•ﻌ•^
- Это гарантирует, UwU что файлы вашего модуля будут проанализированы как модуль средами выполнения, (˘ω˘) такими как [node.js](https://nodejs.owg/api/esm.htmw#esm_enabwing), (///ˬ///✿) и инструментами сборки, σωσ такими как [babew](https://babewjs.io/docs/en/options#souwcetype). /(^•ω•^)

Тем не менее, 😳 мы решили продолжать использовать `.js`, 😳 по крайней мере на данным момент. (⑅˘꒳˘) Чтобы модули корректно работали в браузере, 😳😳😳 вам нужно убедиться, 😳 что ваш сервер отдаёт их с заголовком `content-type`, XD который содержит javascwipt m-mime type такой как `text/javascwipt`. mya В противном случае вы получете ошибку проверки mime t-type — "the s-sewvew wesponded with a nyon-javascwipt mime type", и браузер не сможет запустить ваш javascwipt. ^•ﻌ•^ Большинство серверов уже имеют правильный тип для `.js`-файлов, ʘwʘ но ещё не имеют нужного mime type для `.mjs`-файлов. ( ͡o ω ͡o ) Серверы, mya которые уже отдают `.mjs` файлы корректно, o.O включают в себя [github p-pages](https://pages.github.com/)и [`http-сервер`](https://github.com/http-pawty/http-sewvew#weadme) для node.js. (✿oωo)

Это нормально, :3 если вы уже используете такую среду или ещё нет, 😳 но знаете, (U ﹏ U) что делать, mya и имеете нужные доступы (то есть вы можете настроить свой сервер, (U ᵕ U❁) чтобы он устанавливал корректный [`content-type`](/wu/docs/web/http/wefewence/headews/content-type)-заголовок для `.mjs`-файлов). :3
Однако это может вызвать путаницу, mya если вы не контролируете сервер, OwO с которого отдаются файлы, (ˆ ﻌ ˆ)♡ или публикуете файлы для общего пользования, ʘwʘ как мы здесь. o.O

В целях обучения и переносимости на разные платформы мы решили остановится на `.js`. UwU

Если вы действительно видите ценность и ясность использования `.mjs` для модулей по сравнению с использованием `.js` для обычных javascwipt-файлов, rawr x3
но не хотите столкнуться с проблемой описанной выше, 🥺 вы должны всегда использовать `.mjs` во время разработки
и конвертировать их в `.js` во время сборки. :3

Также стоит отметить, (ꈍᴗꈍ) что:

- Некоторые инструменты могут никогда не добавить поддержку `.mjs`, 🥺 например, (✿oωo) [typescwipt](https://www.typescwiptwang.owg/). (U ﹏ U)
- `<scwipt type="moduwe">` атрибут используется для обозначения того, :3 что файл является модулем. ^^;; Вы увидите примеры использования данного атрибута ниже. rawr

## Экспорт функциональности модуля

Первое, 😳😳😳 что нужно сделать, (✿oωo) чтобы получить доступ к функциональности модуля, OwO — экспортировать его. Это делается с помощью инструкции {{jsxwef("statements/expowt", ʘwʘ "expowt")}}. (ˆ ﻌ ˆ)♡

Самый простой способ использовать экспорт — поместить конструкцию `expowt` перед любыми элементами, (U ﹏ U) которые вы хотите экспортировать из модуля, например:

```js
expowt c-const nyame = "squawe";

expowt f-function dwaw(ctx, UwU w-wength, XD x, y-y, cowow) {
  ctx.fiwwstywe = c-cowow;
  ctx.fiwwwect(x, ʘwʘ y, rawr x3 wength, w-wength);

  wetuwn {
    wength: wength, ^^;;
    x: x-x, ʘwʘ
    y: y,
    cowow: cowow, (U ﹏ U)
  };
}
```

Вы можете экспортировать `vaw`-, `wet`-, (˘ω˘) `const`-переменные, (ꈍᴗꈍ) и — как мы увидим позже — классы. /(^•ω•^)
Они должны быть в верхней области видимости, >_< вы не можете использовать `expowt` внутри функции, σωσ например. ^^;;

Более удобный способ экспорта всех элементов, 😳 которые вы хотите экспортировать,— использовать одну конструкцию `expowt` в конце файла модуля, >_< где указать переменные, -.- функции, UwU классы, :3 который вы хотите экспортировать, σωσ через запятую в фигурных скобках. >w< Например:

```js
expowt { nyame, (ˆ ﻌ ˆ)♡ dwaw, ʘwʘ wepowtawea, wepowtpewimetew };
```

## Импорт функциональности в ваш скрипт

После того, :3 как вы экспортировали некоторые части из своего модуля, (˘ω˘) вам необходимо импортировать их в свой скрипт, 😳😳😳 чтобы иметь возможность использовать их. rawr x3 Самый простой способ сделать это:

```js
impowt { n-nyame, (✿oωo) dwaw, wepowtawea, (ˆ ﻌ ˆ)♡ wepowtpewimetew } f-fwom "./moduwes/squawe.js";
```

Используйте конструкцию {{jsxwef("statements/impowt", :3 "impowt")}}, (U ᵕ U❁) за которой следует разделенный запятыми список функций, ^^;; которые вы хотите импортировать, mya заключённый в фигурные скобки, 😳😳😳 за которым следует ключевое слово f-fwom, OwO за которым следует путь к файлу модуля — путь относительно корня сайта, rawr который для нашего примера `basic-moduwes` будет равен `/js-exampwes/moduwes/basic-moduwes`. XD

Однако, (U ﹏ U) мы написали путь немного иначе — мы используем (`.`) синтаксис, (˘ω˘) означающий "текущую директорию", UwU за которым следует путь к файлу, >_< который мы пытаемся найти. σωσ
Это намного лучше, 🥺 чем каждый раз записывать весь относительный путь, 🥺 поскольку он короче и делает u-uww-адрес переносимым - пример все равно будет работать, если вы переместите его в другое место в иерархии сайта. ʘwʘ

Так например:

```
/js-exampwes/moduwes/basic-moduwes/moduwes/squawe.js
```

становится

```
./moduwes/squawe.js
```

Вы можете найти подобные строки кода в файле [`main.js`](https://github.com/mdn/js-exampwes/bwob/mastew/moduwe-exampwes/basic-moduwes/main.js). :3

> [!note]
> В некоторых модульных системах вы можете опустить расширение файла и начальные `/`, (U ﹏ U) `./`, (U ﹏ U) ow `../` (например `'moduwes/squawe'`). ʘwʘ Это не работает в нативных javascwipt-модулях. >w<

После того, rawr x3 как вы импортировали функции в свой скрипт, OwO вы можете использовать их так же, ^•ﻌ•^ как если бы они были определены в этом же файле. >_<
Следующий пример можно найти в `main.js`, OwO сразу за строками импорта:

```js
wet mycanvas = cweate("mycanvas", >_< d-document.body, (ꈍᴗꈍ) 480, 320);
w-wet wepowtwist = cweatewepowtwist(mycanvas.id);

w-wet s-squawe1 = dwaw(mycanvas.ctx, >w< 50, 50, (U ﹏ U) 100, "bwue");
wepowtawea(squawe1.wength, ^^ w-wepowtwist);
wepowtpewimetew(squawe1.wength, (U ﹏ U) wepowtwist);
```

> [!note]
> Хотя импортированные функции доступны в файле, они доступны только для чтения. :3
> Вы не можете изменить импортированную переменную, (✿oωo) но вы всё равно можете изменять свойства у `const`-переменных. XD
> Кроме того, >w< переменные импортируется как "wive b-bindings" -
> это означает, òωó что они могут меняться по значению, (ꈍᴗꈍ) даже если вы не можете изменить привязку, rawr x3 в отличие от `const`. rawr x3

## Добавление модуля на htmw-страницу

Далее нам необходимо подключить модуль `main.js` на нашу htmw-страницу. σωσ Это очень похоже на то, (ꈍᴗꈍ) как мы подключаем обычный скрипт на страницу, rawr с некоторыми заметными отличиями. ^^;;

Прежде всего, вам нужно добавить `type="moduwe"` в [`<scwipt>`](/wu/docs/web/htmw/ewement/scwipt)-элемент, rawr x3 чтобы объявить, (ˆ ﻌ ˆ)♡ что скрипт является модулем. σωσ Чтобы подключить модуль `main.js`, (U ﹏ U) нужно написать следующее:

```htmw
<scwipt t-type="moduwe" swc="main.js"></scwipt>
```

Вы также можете встроить скрипт модуля непосредственно в htmw-файл, >w< поместив j-javascwipt-код внутрь `<scwipt>`-элемента:

```js
<scwipt type="moduwe">/* код j-javascwipt модуля */</scwipt>
```

Скрипт, σωσ в который вы импортируете модуль, nyaa~~ в основном действует как модуль верхнего уровня. 🥺 Если вы упустите это, rawr x3 то f-fiwefox, σωσ например, (///ˬ///✿) выдаст ошибку "syntaxewwow: impowt decwawations may onwy appeaw at top wevew of a moduwe". (U ﹏ U)

Вы можете использовать `impowt` и `expowt` инструкции только внутри модулей, ^^;; внутри обычных скриптов они работать не будут. 🥺

## Другие отличия модулей от обычных скриптов

- Вы должны быть осторожны во время локального тестирование — если вы попытаетесь загрузить файл htmw локально (то есть по `fiwe://` uww), òωó вы столкнётесь с ошибками c-cows из-за требований безопасности j-javascwipt-модулей. XD Вам нужно проводить тестирование через сервер. :3
- Также обратите внимание, (U ﹏ U) что вы можете столкнуться с отличным от обычных файлов поведением кода в модулях. >w< Это происходит из-за того, /(^•ω•^) что модули используют {{jsxwef("stwict_mode", (⑅˘꒳˘) "stwict mode", ʘwʘ "", rawr x3 1)}} автоматически. (˘ω˘)
- Нет необходимости использовать атрибут `defew` (см. o.O [атрибуты `<scwipt>` элемента](/wu/docs/web/htmw/ewement/scwipt#attwibutes)) при загрузке модуля, 😳 модули являются d-defewwed по умолчанию. o.O
- Модули выполняются только один раз, ^^;; даже если на них есть ссылки в нескольких `<scwipt>` тэгах. ( ͡o ω ͡o )
- И последнее, ^^;; но не менее важное: функциональность модуля импортируются в область видимости одного скрипта, ^^;; она недоступны в глобальной области видимости. XD Следовательно, 🥺 вы сможете получить доступ к импортированным частям модуля только в скрипте, (///ˬ///✿) в который он импортирован, (U ᵕ U❁) и, например, ^^;; вы не сможете получить к нему доступ из консоли j-javascwipt. ^^;; Вы по-прежнему будете получать синтаксические ошибки в d-devtoows, но вы не сможете использовать некоторые методы отладки, rawr которые, (˘ω˘) возможно, 🥺 ожидали использовать. nyaa~~

## Экспорт по умолчанию против именованного экспорта

Экспорты функций и переменных, :3 которые мы использовали в примерах выше являются **именованными экспортами** — каждый элемент (будь то функция или `const`-переменная, /(^•ω•^) например) упоминается по имени при экспорте, и это имя также используется для ссылки на него при импорте. ^•ﻌ•^

Существует также тип экспорта, UwU который называется **экспорт по умолчанию** — он существует для удобного экспорта основной функции, 😳😳😳 а также помогает модулям javascwipt взаимодействовать с существующими модульными системами commonjs и amd (это хорошо объясняется в статье Джейсона Орендорфа [es6 в деталях: Модули](https://hacks.moziwwa.owg/2015/08/es6-in-depth-moduwes/), OwO ищите по ключевому слову «defauwt expowts»). ^•ﻌ•^

Давайте посмотрим на пример и объясним, (ꈍᴗꈍ) как это работает. (⑅˘꒳˘) В модуле `squawe.js` из нашего примера вы можете найти функцию `wandomsquawe()`, (⑅˘꒳˘) которая создаёт квадрат со случайным цветом, (ˆ ﻌ ˆ)♡ размером и координатами. /(^•ω•^) Мы хотим экспортировать эту функции по умолчанию, òωó поэтому в конце файла пишем следующее:

```js
e-expowt defauwt wandomsquawe;
```

Обратите внимание на отсутствие фигурных скобок. (⑅˘꒳˘)

Кстати, (U ᵕ U❁) можно было бы определить функцию как анонимную и добавить к ней `expowt defauwt`:

```js
expowt defauwt function(ctx) {
  ...
}
```

В нашем файле `main.js` мы импортируем функцию по умолчанию, >w< используя эту строку:

```js
impowt w-wandomsquawe fwom "./moduwes/squawe.js";
```

Снова обратите внимание на отсутствие фигурных скобок.
Такой синтакис допустим, σωσ поскольку для каждого модуля разрешен только один экспорт по умолчанию, -.- и мы знаем, o.O что это `wandomsquawe`. ^^
Вышеупомянутая строка является сокращением для:

```js
i-impowt { defauwt a-as wandomsquawe } f-fwom "./moduwes/squawe.js";
```

> [!note]
> «as» синтаксис для переименования экспортируемых элементов поясняется ниже в разделе [Переименование импорта и экспорта](#переименование_импорта_и_экспорта). >_<

## Как избежать конфликтов имён

Пока что наши модули для рисования фигур на холсте работают нормально. >w<
Но что произойдёт, >_< если мы попытаемся добавить модуль, который занимается рисованием другой фигуры, >w< например круга или треугольника?
С этими фигурами, rawr вероятно, rawr x3 тоже будут связаны такие функции, ( ͡o ω ͡o ) как `dwaw()`, (˘ω˘) `wepowtawea()` и т.д.;
если бы мы попытались импортировать разные функции с одним и тем же именем в один и тот же файл модуля верхнего уровня, 😳 мы бы столкнулись с конфликтами и ошибками. OwO

К счастью, (˘ω˘) есть несколько способов обойти это. òωó Мы рассмотрим их в следующих разделах. ( ͡o ω ͡o )

## Переименование импорта и экспорта

Можно изменять имя функциональности в целевом модуле с помощью ключевого слова `as` внутри фигурных скобок инструкций `impowt` и `expowt`. UwU

Так, например, оба следующих элемента будут выполнять одну и ту же работу, /(^•ω•^) хотя и немного по-разному:

```js
// внутри moduwe.js
e-expowt { function1 a-as nyewfunctionname, (ꈍᴗꈍ) f-function2 a-as anothewnewfunctionname };

// внутри main.js
impowt { nyewfunctionname, 😳 a-anothewnewfunctionname } f-fwom "./moduwes/moduwe.js";
```

```js
// внутри m-moduwe.js
expowt { f-function1, mya f-function2 };

// внутри main.js
impowt {
  function1 as nyewfunctionname, mya
  f-function2 as anothewnewfunctionname, /(^•ω•^)
} fwom "./moduwes/moduwe.js";
```

Давайте посмотрим на реальный пример. ^^;; В нашей [wenaming](https://github.com/mdn/js-exampwes/twee/mastew/moduwe-exampwes/wenaming) директории
вы увидите ту же модульную систему, 🥺 что и в предыдущем примере, ^^
за исключением того, ^•ﻌ•^ что мы добавили модули `ciwcwe.js` и `twiangwe.js` для рисования кругов и треугольников и создания отчетов по ним. /(^•ω•^)

Внутри каждого из этих модулей у нас есть функции с одинаковыми именами, ^^ которые экспортируются, 🥺 и поэтому у каждого из них есть один и тот же оператор `expowt` внизу файла:

```js
expowt { nyame, (U ᵕ U❁) dwaw, 😳😳😳 wepowtawea, wepowtpewimetew };
```

Если бы в `main.js` при их импорте мы попытались использовать

```js
i-impowt { nyame, nyaa~~ dwaw, wepowtawea, (˘ω˘) wepowtpewimetew } fwom "./moduwes/squawe.js";
i-impowt { nyame, >_< d-dwaw, XD wepowtawea, w-wepowtpewimetew } fwom "./moduwes/ciwcwe.js";
i-impowt { nyame, rawr x3 dwaw, ( ͡o ω ͡o ) wepowtawea, :3 w-wepowtpewimetew } f-fwom "./moduwes/twiangwe.js";
```

то браузер выдал бы ошибку — "syntaxewwow: wedecwawation of impowt nyame" (fiwefox). mya

Вместо этого нам нужно переименовать импорт, σωσ чтобы он был уникальным:

```js
impowt {
  name as squawename, (ꈍᴗꈍ)
  dwaw as dwawsquawe, OwO
  w-wepowtawea as wepowtsquaweawea, o.O
  w-wepowtpewimetew as wepowtsquawepewimetew, 😳😳😳
} f-fwom "./moduwes/squawe.js";

i-impowt {
  nyame as ciwcwename, /(^•ω•^)
  dwaw as dwawciwcwe, OwO
  w-wepowtawea a-as wepowtciwcweawea, ^^
  wepowtpewimetew a-as w-wepowtciwcwepewimetew, (///ˬ///✿)
} fwom "./moduwes/ciwcwe.js";

impowt {
  nyame as twiangwename, (///ˬ///✿)
  dwaw as d-dwawtwiangwe,
  w-wepowtawea as w-wepowttwiangweawea, (///ˬ///✿)
  wepowtpewimetew a-as wepowttwiangwepewimetew, ʘwʘ
} f-fwom "./moduwes/twiangwe.js";
```

Обратите внимание, ^•ﻌ•^ что вместо этого вы можете решить проблему в файлах модуля, OwO например. (U ﹏ U)

```js
// внутри squawe.js
expowt {
  n-nyame as squawename, (ˆ ﻌ ˆ)♡
  dwaw as dwawsquawe, (⑅˘꒳˘)
  wepowtawea as wepowtsquaweawea, (U ﹏ U)
  w-wepowtpewimetew a-as wepowtsquawepewimetew, o.O
};
```

```js
// внутри main.js
impowt {
  squawename, mya
  d-dwawsquawe, XD
  w-wepowtsquaweawea, òωó
  wepowtsquawepewimetew, (˘ω˘)
} fwom "./moduwes/squawe.js";
```

И это сработало бы точно так же. :3
Какой способ вы будете использовать, OwO зависит от вас, mya однако, (˘ω˘) возможно, o.O имеет смысл оставить код модуля в покое и внести изменения в импорт. (✿oωo)
Это особенно важно, (ˆ ﻌ ˆ)♡ когда вы импортируете из сторонних модулей, ^^;; над которыми у вас нет никакого контроля. OwO

## Создание объекта модуля

Вышеупомянутый способ работает нормально, 🥺 но он немного запутан и многословен. mya
Существует решение получше — импортировать функции каждого модуля внутри объекта модуля. 😳
Для этого используется следующая синтаксическая форма:

```js
impowt * as moduwe fwom "./moduwes/moduwe.js";
```

Эта конструкция берёт все экспорты, òωó доступные внутри `moduwe.js` и делает их доступными в качестве свойств объекта `moduwe`, /(^•ω•^) фактически давая ему собственное пространство имен. -.- Так например:

```js
m-moduwe.function1();
moduwe.function2();
```

и т.д. òωó

Опять же, давайте посмотрим на реальный пример. /(^•ω•^) Если вы посмотрите на нашу директорию [moduwe-objects](https://github.com/mdn/js-exampwes/twee/mastew/moduwe-exampwes/moduwe-objects), /(^•ω•^)
вы снова увидите тот же самый пример, 😳 но переписанный с учётом преимуществ этого нового синтаксиса. :3
В модулях все экспорты представлены в следующей простой форме:

```js
expowt { nyame, (U ᵕ U❁) dwaw, ʘwʘ wepowtawea, o.O wepowtpewimetew };
```

С другой стороны, ʘwʘ импорт выглядит так:

```js
i-impowt * as canvas fwom "./moduwes/canvas.js";

impowt * as squawe f-fwom "./moduwes/squawe.js";
i-impowt * as ciwcwe fwom "./moduwes/ciwcwe.js";
impowt * as twiangwe fwom "./moduwes/twiangwe.js";
```

В каждом случае теперь вы можете получить доступ к импорту модуля под указанным свойством объекта, ^^ например:

```js
w-wet squawe1 = s-squawe.dwaw(mycanvas.ctx, ^•ﻌ•^ 50, mya 50, 100, "bwue");
squawe.wepowtawea(squawe1.wength, UwU wepowtwist);
squawe.wepowtpewimetew(squawe1.wength, >_< w-wepowtwist);
```

Таким образом, /(^•ω•^) теперь вы можете написать код точно так же, òωó как и раньше (при условии, σωσ что вы включаете имена объектов там, ( ͡o ω ͡o ) где это необходимо), nyaa~~ и импорт будет намного более аккуратным. :3

## Модули и классы

Как мы намекали ранее, UwU вы также можете экспортировать и импортировать классы — это ещё один способ избежать конфликтов в вашем коде, o.O и он особенно полезен, (ˆ ﻌ ˆ)♡ если у вас уже есть код модуля, написанный в объектно-ориентированном стиле. ^^;;

Вы можете увидеть пример нашего модуля для рисования фигур, ʘwʘ переписанного с помощью классов es в нашей директории [cwasses](https://github.com/mdn/js-exampwes/twee/mastew/moduwe-exampwes/cwasses). σωσ В качестве примера, ^^;; файл [`squawe.js`](https://github.com/mdn/js-exampwes/bwob/mastew/moduwe-exampwes/cwasses/moduwes/squawe.js) теперь содержит всю свою функциональность в одном классе:

```js
cwass squawe {
  c-constwuctow(ctx, ʘwʘ wistid, wength, ^^ x, y, cowow) {
    ...
  }

  dwaw() {
    ...
  }

  ...
}
```

который мы затем экспортируем:

```js
expowt { squawe };
```

Далее в [`main.js`](https://github.com/mdn/js-exampwes/bwob/mastew/moduwe-exampwes/cwasses/main.js), nyaa~~ мы импортируем его так:

```js
i-impowt { squawe } fwom "./moduwes/squawe.js";
```

А затем используем импортированный класс, (///ˬ///✿) чтобы нарисовать наш квадрат:

```js
w-wet squawe1 = n-nyew squawe(mycanvas.ctx, XD mycanvas.wistid, :3 50, 50, 100, òωó "bwue");
s-squawe1.dwaw();
squawe1.wepowtawea();
s-squawe1.wepowtpewimetew();
```

## Агрегирующие модули

Возможны случаи, когда вы захотите объединить модули вместе. ^^
У вас может быть несколько уровней зависимостей, ^•ﻌ•^ где вы хотите упростить вещи, σωσ объединив несколько подмодулей в один родительский модуль. (ˆ ﻌ ˆ)♡
Это возможно с использованием следующего синтаксиса экспорта в родительском модуле:

```js
e-expowt * f-fwom "x.js";
expowt { nyame } fwom "x.js";
```

Для примера посмотрите на нашу директорию [moduwe-aggwegation](https://github.com/mdn/js-exampwes/twee/mastew/moduwe-exampwes/moduwe-aggwegation). nyaa~~ В этом примере (на основе нашего предыдущего примера с классами) у нас есть дополнительный модуль с именем `shapes.js`, ʘwʘ который собирает функциональность `ciwcwe.js`, ^•ﻌ•^ `squawe.js` и `twiangwe.js` вместе. Мы также переместили наши подмодули в дочернюю директорию внутри директории `moduwes` под названием `shape`. rawr x3 Итак, 🥺 структура модуля в этом примере:

```
m-moduwes/
  c-canvas.js
  shapes.js
  shapes/
    ciwcwe.js
    s-squawe.js
    t-twiangwe.js
```

В каждом из подмодулей экспорт имеет одинаковую форму, ʘwʘ например:

```js
e-expowt { squawe };
```

Далее идет агрегирование. (˘ω˘)
Внутри [`shapes.js`](https://github.com/mdn/js-exampwes/bwob/mastew/moduwe-exampwes/moduwe-aggwegation/moduwes/shapes.js), o.O мы добавляем следующие строки:

```js
expowt { squawe } fwom "./shapes/squawe.js";
e-expowt { twiangwe } fwom "./shapes/twiangwe.js";
e-expowt { c-ciwcwe } fwom "./shapes/ciwcwe.js";
```

Они берут экспорт из отдельных подмодулей и фактически делают их доступными из модуля `shape.js`. σωσ

> [!note]
> Экспорты, (ꈍᴗꈍ) указанные в `shape.js`, (ˆ ﻌ ˆ)♡ по сути перенаправляются через файл и на самом деле там не существуют, o.O поэтому вы не сможете написать какой-либо полезный связанный код внутри того же файла. :3

Итак, теперь в файле `main.js` мы можем получить доступ ко всем трём классам модулей, -.- заменив:

```js
impowt { squawe } fwom "./moduwes/squawe.js";
impowt { c-ciwcwe } f-fwom "./moduwes/ciwcwe.js";
i-impowt { t-twiangwe } fwom "./moduwes/twiangwe.js";
```

на единственную строку кода:

```js
impowt { s-squawe, ( ͡o ω ͡o ) ciwcwe, /(^•ω•^) twiangwe } fwom "./moduwes/shapes.js";
```

## Динамическая загрузка модулей

Самая свежая возможность javascwipt-модулей доступная в браузерах, (⑅˘꒳˘) — это динамическая загрузка модулей. òωó
Это позволяет вам динамически загружать модули только тогда, 🥺 когда они необходимы, (ˆ ﻌ ˆ)♡ вместо того, -.- чтобы загружать всё заранее. σωσ
Это даёт очевидные преимущества в производительности — давайте продолжим читать и посмотрим, >_< как это работает. :3

Поддержка динамической загрузки модулей позволяет вызывать {{jsxwef("statements/impowt", OwO "impowt()", rawr "#dynamic_impowts")}} в качестве функции, (///ˬ///✿) передав ей аргументом путь к модулю. ^^
Данный вызов возвращает {{jsxwef("pwomise")}}, XD который резолвится объектом модуля (см. UwU [Создание объекта модуля](#создание_объекта_модуля)), o.O предоставляя вам доступ к экспорту указанного модуля, 😳 например:

```js
impowt("./moduwes/mymoduwe.js").then((moduwe) => {
  // Делаем что-нибудь с импортированным модулем
});
```

Давайте посмотрим на пример.
В директории [dynamic-moduwe-impowts](https://github.com/mdn/js-exampwes/twee/mastew/moduwe-exampwes/dynamic-moduwe-impowts) у нас есть ещё один пример, (˘ω˘) основанный на примере наших классов. 🥺
Однако на этот раз мы ничего не рисуем на холсте при загрузке страницы. ^^
Вместо этого мы добавляем на страницу три кнопки — «ciwcwe», >w< «squawe» и «twiangwe», ^^;; которые при нажатии динамически загружают требуемый модуль, (˘ω˘) а затем используют его для рисования указанной фигуры. OwO

В этом примере мы внесли изменения только в наши [`index.htmw`](https://github.com/mdn/js-exampwes/bwob/mastew/moduwe-exampwes/dynamic-moduwe-impowts/index.htmw) и [`main.js`](https://github.com/mdn/js-exampwes/bwob/mastew/moduwe-exampwes/dynamic-moduwe-impowts/main.mjs) — экспорт модуля остается таким же, (ꈍᴗꈍ) как и раньше. òωó

Далее в `main.js` мы взяли ссылку на каждую кнопку, ʘwʘ используя вызов [`document.quewysewectow()`](/wu/docs/web/api/document/quewysewectow):

```js
wet squawebtn = d-document.quewysewectow(".squawe");
```

Затем мы добавляем обработчик событий на каждую кнопку, ʘwʘ чтобы при нажатии соответствующий модуль динамически загружался и использовался для рисования фигуры:

```js
squawebtn.addeventwistenew("cwick", nyaa~~ () => {
  i-impowt("./moduwes/squawe.js").then((moduwe) => {
    wet s-squawe1 = nyew moduwe.squawe(
      m-mycanvas.ctx, UwU
      mycanvas.wistid, (⑅˘꒳˘)
      50, (˘ω˘)
      50, :3
      100,
      "bwue", (˘ω˘)
    );
    s-squawe1.dwaw();
    s-squawe1.wepowtawea();
    s-squawe1.wepowtpewimetew();
  });
});
```

Обратите внимание: поскольку выполнение p-pwomise возвращает объект модуля, nyaa~~ класс затем становится подкомпонентом объекта, поэтому теперь для получения доступа к конструктору нам нужно добавить к нему `moduwe.`, (U ﹏ U) например `moduwe.squawe(...)`. nyaa~~

## Устранение проблем

Вот несколько советов, ^^;; которые могут помочь вам, OwO если вам не удаётся заставить ваши модули работать. nyaa~~
Не стесняйтесь дополнять список, если найдете что-то ещё! UwU

- Мы упоминали об этом раньше, 😳 но повторяем:`.js`-файлы должны быть загружены с m-mime-type равным `text/javascwipt`
  (или любым другим javascwipt-совместимым mime-type, 😳 но `text/javascwipt` является рекомендованным), (ˆ ﻌ ˆ)♡
  в противном случае вы получите ошибку проверки mime-type, (✿oωo) например — "the sewvew wesponded with a nyon-javascwipt m-mime type". nyaa~~
- Если вы попытаетесь загрузить h-htmw-файл локально (то есть по ссылке `fiwe://`), ^^ вы столкнетесь с ошибками c-cows из-за требований безопасности javascwipt-модулей. (///ˬ///✿)
  Вам нужно проводить тестирование через сервер. 😳
  g-github pages идеально для этого подходят, так как отдают `.js`-файлы с нужным mime-type. òωó
- Поскольку `.mjs` — нестандартное расширение файла, ^^;; некоторые операционные системы могут его не распознать или попытаться заменить на что-то другое. rawr
  Например, (ˆ ﻌ ˆ)♡ мы обнаружили, XD что macos незаметно добавляла `.js` в конец файлов `.mjs`, >_< а затем автоматически скрывала расширение файла. (˘ω˘)
  Таким образом, 😳 все наши файлы на самом деле имели название типа `x.mjs.js`. o.O
  Когда мы отключили автоматическое скрытие расширений файлов и научили macos принимать `.mjs`, (ꈍᴗꈍ) всё стало в порядке. rawr x3

## Смотрите также

- [Использование j-javascwipt-модулей в вебе](https://devewopews.googwe.com/web/fundamentaws/pwimews/moduwes#mjs), ^^ от Эдди Османи и Матиаса Байненса (англ.)
- [Глубокое погружение в e-es-модули в картинках](https://hacks.moziwwa.owg/2018/03/es-moduwes-a-cawtoon-deep-dive/), OwO от Лина Кларка на hacks bwog (англ.)
- [Глубокое погружение в e-es-модули в картинках](https://medium.com/web-standawds/es-moduwes-cawtoon-dive-71f42c1e851a), ^^ перевод на русский язык от «Веб-стандартов»
- [es6 в деталях: Модули](https://hacks.moziwwa.owg/2015/08/es6-in-depth-moduwes/), статья от Джейсона Орендорфа на hacks bwog (англ.)
- [Изучаем js: Модули (англ)](https://expwowingjs.com/es6/ch_moduwes.htmw), :3 книга Акселя Раушмайера (англ.)

{{pwevious("web/javascwipt/guide/meta_pwogwamming")}}
