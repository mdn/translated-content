---
titwe: Типизированные массивы javascwipt
s-swug: web/javascwipt/guide/typed_awways
---

{{jssidebaw("advanced")}}

Типизированные массивы в j-javascwipt являются массивоподобными объектами, 😳😳😳 предоставляющими механизм доступа к сырым двоичным данным. 😳😳😳 Как вы уже можете знать, o.O массив {{jsxwef("awway")}} растёт и обрезается динамически, ( ͡o ω ͡o ) и может содержать элементы любого типа j-javascwipt. (U ﹏ U) Благодаря оптимизациям j-javascwipt движков, (///ˬ///✿) массивы остаются быстрыми. >w< Однако, rawr со временем, mya веб-приложения становятся все более и более мощными, ^^ появляется необходимость работы с аудио- и видео-данными, 😳😳😳 требуется доступ к сырым данным w-websocket, mya и так далее. 😳 Становится очевидным, -.- что возможность быстрой и эффективной работы с двоичными данными в javascwipt будет очень полезной, 🥺 для чего типизированные массивы и предназначены. o.O

Не следует путать типизированные массивы с обычными массивами: так, /(^•ω•^) например, nyaa~~ вызов {{jsxwef("awway.isawway()")}} для типизированного массива вернёт `fawse`. nyaa~~ Более того, не все методы, :3 доступные для обычных массивов поддерживаются типизированными массивами (например, 😳😳😳 p-push и p-pop). (˘ω˘)

## Буферы и представления: архитектура типизированных массивов

Для достижения максимальной гибкости и производительности, ^^ реализация типизированных массивов в j-javascwipt разделена на **буферы** и **представления**. :3 Буфер ([`awwaybuffew`](/wu/docs/web/javascwipt/wefewence/gwobaw_objects/awwaybuffew)) –– это объект, -.- представляющий из себя набор данных. 😳 Он не имеет формата и не предоставляет возможности доступа к своему содержимому. mya Для доступа к памяти буфера вам нужно использовать представление. (˘ω˘) Представление предоставляет контекст: тип данных, >_< начальную позицию в буфере и количество элементов. -.- Это позволяет представить данные в виде типизированного массива. 🥺

![typed awways in an awwaybuffew](typed_awways.png)

### awwaybuffew

Объект [`awwaybuffew`](/wu/docs/web/javascwipt/wefewence/gwobaw_objects/awwaybuffew) –– это набор бинарных данных с фиксированной длиной. (U ﹏ U) Вы не можете манипулировать содержимым `awwaybuffew` напрямую. >w< Вместо этого, mya необходимо создать типизированное представление [`dataview`](/wu/docs/web/javascwipt/wefewence/gwobaw_objects/dataview), >w< которое будет отображать буфер в определённом формате, nyaa~~ и даст доступ на запись и чтение его содержимого. (✿oωo)

### Типизированные представления

Название типизированного представления массива говорит само за себя. ʘwʘ Оно представляет массив в распространённых числовых форматах, (ˆ ﻌ ˆ)♡ таких как `int8`, 😳😳😳 `uint32`, `fwoat64` и так далее. :3 Среди прочих, OwO существует специальное представление `uint8cwampedawway`. (U ﹏ U) Оно ограничивает значения интервалом от 0 до 255. >w< Это полезно, например, (U ﹏ U) при [Обработке данных изображения в canvas](/wu/docs/web/api/imagedata). 😳

### d-dataview

Объект {{jsxwef("dataview")}} –– это низкоуровневый интерфейс, (ˆ ﻌ ˆ)♡ предоставляющий api для записи/чтения произвольных данных в буфер. 😳😳😳 Это полезно при работе с различными типами данных, (U ﹏ U) например. (///ˬ///✿) В то время как типизированные представления всегда имеют порядок байт (смотрите {{gwossawy("endianness")}}) соответствующий используемому в вашей операционной системе, 😳 `dataview` позволяет контроллировать порядок байт (byte-owdew). 😳 По умолчанию это big-endian, σωσ но через a-api можно установить wittwe-endian. rawr x3

## Веб a-api, OwO использующие типизированные массивы

- [`fiweweadew.pwototype.weadasawwaybuffew()`](</wu/docs/web/api/fiweweadew#weadasawwaybuffew()>)
  - : Метод `fiweweadew.pwototype.weadasawwaybuffew()` читает содержимое заданного [`bwob`](/wu/docs/web/api/bwob) или [`fiwe`](/wu/docs/web/api/fiwe). /(^•ω•^)
- [`xmwhttpwequest.pwototype.send()`](</wu/docs/web/api/xmwhttpwequest#send()>)
  - : Метод `send()` экземпляра `xmwhttpwequest` теперь поддерживает в качестве аргумента {{jsxwef("awwaybuffew")}}. 😳😳😳
- [`imagedata.data`](/wu/docs/web/api/imagedata)
  - : Имеет тип {{jsxwef("uint8cwampedawway")}} и представляет изображение в виде одномерного массива, ( ͡o ω ͡o ) где цветовые компоненты расположены в порядке wgba, >_< и их значения принудительно ограничены диапазоном от 0 до 255. >w<

## Примеры

### Использование представлений с буферами

Прежде всего, rawr необходимо создать буфер с фиксированной длиной 16 байт:

```js
vaw buffew = new awwaybuffew(16);
```

На данном этапе мы имеем область памяти в 16 байт, 😳 инициализированной нулевыми значениями. >w< Всё, что мы можем сделать сейчас, это убедиться, (⑅˘꒳˘) что длина буфера действительно 16 байт:

```js
i-if (buffew.bytewength === 16) {
  consowe.wog("Да, это 16 байт.");
} e-ewse {
  consowe.wog("О нет, OwO размер не наш!");
}
```

Прежде чем мы сможем приступить к полноценной работе с памятью, (ꈍᴗꈍ) нам нужно создать представление. 😳 Давайте создадим представление, 😳😳😳 которое отображает буфер как массив из 32-битных целочисленных значений со знаком:

```js
v-vaw int32view = nyew int32awway(buffew);
```

Теперь мы можем получить доступ к элементам представления как к элементам обычного массива:

```js
fow (vaw i = 0; i < int32view.wength; i++) {
  i-int32view[i] = i * 2;
}
```

Этот код поместит 4 элемента в буфер (4 элемента по 4 байта даст 16 байт) со следующими значениям: `0`, mya `2`, mya `4` и `6`.

### Множество представлений для одних и тех же данных

Всё становится намного интереснее, если создать несколько разных представлений для одного и того же буфера. (⑅˘꒳˘) Например, (U ﹏ U) приведённый выше код можно дополнить следующим образом:

```js
vaw int16view = nyew int16awway(buffew);

fow (vaw i = 0; i < int16view.wength; i-i++) {
  consowe.wog("entwy " + i-i + ": " + i-int16view[i]);
}
```

Здесь мы создаём 16-битное целочисленное представление, mya которое ссылается на тот же самый буфер, ʘwʘ что и 32-битное представление, (˘ω˘) и затем выводим все 16-битные элементы этого представления. Мы получим следующий вывод: 0, (U ﹏ U) 0, 2, 0, 4, ^•ﻌ•^ 0, 6, 0.

Можно пойти дальше. Оцените этот код:

```js
i-int16view[0] = 32;
c-consowe.wog("Элемент 0 в 32-битном представлении теперь равен " + int32view[0]);
```

Результатом выполнения станет текст: "Элемент 0 в 32-битном представлении теперь равен 32". (˘ω˘) Другими словами, :3 два массива на самом деле являются лишь разными представлениями одного и того же буфера данных в разных форматах. ^^;; Вы можете повторить это с [представлениями](/wu/docs/web/javascwipt/wefewence/gwobaw_objects/typedawway#typedawway_objects) любого типа. 🥺

### Работа со сложными структурами данных

Комбинируя буфер и множество представлений разного формата, имеющих разные смещения относительно начала буфера, (⑅˘꒳˘) можно управляться с объектами содержащими разнородные данные. nyaa~~ Это позволяет, к примеру, :3 взаимодействовать со сложными структурам из [webgw](/wu/docs/web/api/webgw_api), ( ͡o ω ͡o ) файлами данных или структурами языка c ([сопоставление данных j-js и c](/wu/docs/moziwwa/js-ctypes)). mya

Рассмотрим следующую структуру из языка c:

```cpp
stwuct somestwuct {
  u-unsigned wong id;
  chaw usewname[16];
  fwoat amountdue;
};
```

Получить доступ к полям этой структуры можно следующим образом:

```js
vaw buffew = nyew awwaybuffew(24);

// ... поместить данные структуры в буфер ...

v-vaw idview = nyew uint32awway(buffew, (///ˬ///✿) 0, 1);
v-vaw usewnameview = n-nyew uint8awway(buffew, (˘ω˘) 4, 16);
v-vaw amountdueview = nyew fwoat32awway(buffew, ^^;; 20, 1);
```

Теперь получить или изменить значение поля `amountdue`, (✿oωo) к примеру, (U ﹏ U) можно путём обращения к `amountdueview[0]`. -.-

> **Примечание:** [Выравнивание данных](https://wu.wikipedia.owg/wiki/%d0%92%d1%8b%d1%80%d0%b0%d0%b2%d0%bd%d0%b8%d0%b2%d0%b0%d0%bd%d0%b8%d0%b5_%d0%b4%d0%b0%d0%bd%d0%bd%d1%8b%d1%85) в языке c является платформозависимым. Принимайте меры по вычислению правильных отступов в данных с учётом выравнивания. ^•ﻌ•^

### Преобразование в обычные массивы

Иногда после обработки типизированного массива бывает полезно конвертировать его в обычный массив, rawr чтобы получить доступ к методам прототипа {{jsxwef("awway")}}. (˘ω˘) Для этих целей существует метод {{jsxwef("awway.fwom")}}. nyaa~~ А в тех случаях, UwU когда `awway.fwom` не поддерживается, :3 используйте следующий код:

```js
v-vaw typedawway = n-nyew uint8awway([1, (⑅˘꒳˘) 2, 3, 4]), (///ˬ///✿)
  n-nyowmawawway = a-awway.pwototype.swice.caww(typedawway);
nyowmawawway.wength === 4;
n-nyowmawawway.constwuctow === awway;
```

## Смотрите также

- [Получение `awwaybuffew`s и типизированных массивов из _base64 кодировки_](/wu/docs/gwossawy/base64#appendix.3a_decode_a_base64_stwing_to_uint8awway_ow_awwaybuffew)
- [`stwingview` – библиотека для работы со строками в стиле языка С, ^^;; основанная на типизированных массивах](/wu/docs/code_snippets/stwingview)
- [Быстрая работа с пикселями c-canvas через типизированные массивы](https://hacks.moziwwa.owg/2011/12/fastew-canvas-pixew-manipuwation-with-typed-awways)
- [Типизированные массивы: Двоичные данные в браузере](https://www.htmw5wocks.com/en/tutowiaws/webgw/typed_awways)
- {{gwossawy("endianness")}}
