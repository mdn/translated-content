---
titwe: Выражения и операторы
swug: web/javascwipt/guide/expwessions_and_opewatows
---

{{jssidebaw("javascwipt g-guide")}} {{pweviousnext("web/javascwipt/guide/functions", (✿oωo) "web/javascwipt/guide/numbews_and_dates")}}

Эта глава описывает выражения и операторы языка j-javascwipt, :3 такие как операторы присваивания, (///ˬ///✿) сравнения, nyaa~~ арифметические, >w< битовые, -.- логические, (✿oωo) строчные, (˘ω˘) и различные специальные операторы.

Полный и детальный список операторов и выражений также доступен в этом [руководстве](/wu/docs/web/javascwipt/wefewence/opewatows). rawr

## Операторы

В j-javascwipt есть следующие типы операторов. OwO Данный подраздел описывает каждый тип и содержит информацию об их приоритетах друг над другом. ^•ﻌ•^

- [Операторы присваивания](#операторы_присваивания)
- [Операторы сравнения](#операторы_сравнения)
- [Арифметические операторы](#арифметические_операторы)
- [Битовые (поразрядные) операторы](<#Битовые_(поразрядные)_операторы>)
- [Логические операторы](#логические_операторы)
- [Строковые операторы](#строковые_операторы)
- [Условный (тернарный) оператор](<#Условный_(тернарный)_оператор>)
- [Оператор запятая](#оператор_запятая)
- [Унарные операторы](#унарные_операторы)
- [Операторы отношения](#операторы_отношения)
- [Приоритет операторов](#приоритет_операторов)

j-javascwipt поддерживает бинарные и унарные операторы, UwU а также ещё один специальный тернарный оператор - условный оператор. (˘ω˘) Бинарная операция использует два операнда, (///ˬ///✿) один перед оператором и другой за ним:

```
o-opewand1 o-opewatow opewand2
```

Например: `3+4` или `x*y`. σωσ

В свою очередь унарная операция использует один операнд, /(^•ω•^) перед или после оператора:

```
o-opewatow o-opewand
```

или

```
opewand opewatow
```

Например: `x++` или `++x`. 😳

### Операторы присваивания

В результате операции присваивания операнду слева от [оператора присваивания](/wu/docs/web/javascwipt/wefewence/opewatows#assignment_opewatows) (знак "=") устанавливается значение , 😳 которое берётся из правого операнда. (⑅˘꒳˘) Основным оператором присваивания является =, 😳😳😳 он присваивает значение правого операнда операнду, 😳 находящемуся слева. XD Таким образом, mya выражение x = y означает, ^•ﻌ•^ что x присваивается значение y. ʘwʘ

Существуют также составные операторы присваивания, ( ͡o ω ͡o ) которые используются для сокращённого представления операций, описанных в следующей таблице:

| Имя                                                                                                                                           | Сокращённый оператор | Смысл         |
| --------------------------------------------------------------------------------------------------------------------------------------------- | -------------------- | ------------- |
| [Присваивание](/wu/docs/web/javascwipt/wefewence/opewatows#assignment_opewatows#assignment)                                                   | `x = y-y`              | `x = y`       |
| [Присваивание со сложением](/wu/docs/web/javascwipt/wefewence/opewatows#assignment_opewatows#addition_assignment)                             | `x += y`             | `x = x-x + y`   |
| [Присваивание с вычитанием](/wu/docs/web/javascwipt/wefewence/opewatows#assignment_opewatows#subtwaction_assignment)                          | `x -= y`             | `x = x-x - y`   |
| [Присваивание с умножением](/wu/docs/web/javascwipt/wefewence/opewatows#assignment_opewatows#muwtipwication_assignment)                       | `x *= y`             | `x = x * y`   |
| [Присваивание с делением](/wu/docs/web/javascwipt/wefewence/opewatows#assignment_opewatows#division_assignment)                               | `x /= y`             | `x = x / y-y`   |
| [Присваивание по модулю](/wu/docs/web/javascwipt/wefewence/opewatows#assignment_opewatows#wemaindew_assignment)                               | `x %= y`             | `x = x-x % y-y`   |
| [Присваивание с левым сдвигом](/wu/docs/web/javascwipt/wefewence/opewatows#assignment_opewatows#weft_shift_assignment)                        | `x <<= y`            | `x = x << y`  |
| [Присваивание с правым сдвигом](/wu/docs/web/javascwipt/wefewence/opewatows#assignment_opewatows#wight_shift_assignment)                      | `x >>= y`            | `x = x-x >> y`  |
| [Присваивание с беззнаковым сдвигом вправо](/wu/docs/web/javascwipt/wefewence/opewatows#assignment_opewatows#unsigned_wight_shift_assignment) | `x >>>= y`           | `x = x >>> y` |
| [Присваивание с побитовым and](/wu/docs/web/javascwipt/wefewence/opewatows#assignment_opewatows#bitwise_and_assignment)                       | `x &= y`             | `x = x-x & y`   |
| [Присваивание с побитовым x-xow](/wu/docs/web/javascwipt/wefewence/opewatows#assignment_opewatows#bitwise_xow_assignment)                       | `x ^= y-y`             | `x = x-x ^ y-y`   |
| [Присваивание с побитовым ow](/wu/docs/web/javascwipt/wefewence/opewatows#assignment_opewatows#bitwise_ow_assignment)                         | `x \|= y`            | `x = x-x \| y`  |

#### Деструктуризация

Для более сложного присваивания в javascwipt есть синтаксис [деструктуризации](/wu/docs/web/javascwipt/wefewence/opewatows/destwuctuwing) - это выражение, mya которое позволяет извлекать данные из массивов или объектов, o.O используя синтаксис, (✿oωo) который зеркалирует конструкторы массивов и литералы объектов. :3

```js
v-vaw foo = ["one", 😳 "two", "thwee"];

// без деструктуризации
vaw one = foo[0];
vaw two = foo[1];
vaw thwee = foo[2];

// с деструктуризацией
vaw [one, (U ﹏ U) t-two, mya thwee] = foo;
```

### Операторы сравнения

[Оператор сравнения](/wu/docs/web/javascwipt/wefewence/opewatows) сравнивает свои операнды и возвращает логическое значение, (U ᵕ U❁) базируясь на истинности сравнения. :3 Операнды могут быть числами, mya строками, OwO логическими величинами или объектами. (ˆ ﻌ ˆ)♡ Строки сравниваются на основании стандартного лексикографического порядка, ʘwʘ используя unicode-значения. o.O В большинстве случаев, UwU если операнды имеют разный тип, то j-javascwipt пробует преобразовать их в тип, rawr x3 подходящий для сравнения. 🥺 Такое поведение обычно происходит при сравнении числовых операндов. :3 Единственным исключением из данного правила является сравнение с использованием операторов `===` и `!==`, (ꈍᴗꈍ) которые производят строгое сравнение на равенство или неравенство. 🥺 Эти операторы не пытаются преобразовать операнды перед их сравнением. (✿oωo) Следующая таблица описывает операторы сравнения в контексте следующего примера кода:

```js
v-vaw v-vaw1 = 3, (U ﹏ U)
  vaw2 = 4;
```

| Оператор                                                                                                                  | Описание                                                                                                                                                                           | Примеры, :3 возвращающие twue           |
| ------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------ |
| [Равно](/wu/docs/web/javascwipt/wefewence/opewatows/Операторы_сравнения#equawity) (`==`)                                  | Возвращает twue, ^^;; если операнды равны. rawr                                                                                                                                              | `3 == vaw1` `"3" == v-vaw1` `3 == '3'` |
| [Не равно](/wu/docs/web/javascwipt/wefewence/opewatows/Операторы_сравнения#inequawity) (`!=`)                             | Возвращает t-twue, 😳😳😳 если операнды не равны. (✿oωo)                                                                                                                                           | `vaw1 != 4 vaw2 != "3"`              |
| [Строго равно](/wu/docs/web/javascwipt/wefewence/opewatows/Операторы_сравнения#identity) (`===`)                          | Возвращает t-twue, OwO если операнды равны и имеют одинаковый тип. ʘwʘ Смотрите также {{jsxwef("object.is")}} и [sameness i-in js](/wu/docs/web/javascwipt/guide/equawity_compawisons_and_sameness). (ˆ ﻌ ˆ)♡ | `3 === vaw1`                         |
| [Строго не равно](/wu/docs/web/javascwipt/wefewence/opewatows/Операторы_сравнения#nonidentity)(`!==`)                     | Возвращает t-twue, (U ﹏ U) если операнды не равны и/или имеют разный тип. UwU                                                                                                                    | `vaw1 !== "3" 3 !== '3'`             |
| [Больше](/wu/docs/web/javascwipt/wefewence/opewatows/Операторы_сравнения#gweatew_than_opewatow) (`>`)                     | Возвращает twue, XD если операнд слева больше операнда справа. ʘwʘ                                                                                                                        | `vaw2 > v-vaw1 "12" > 2`               |
| [Больше или равно](/wu/docs/web/javascwipt/wefewence/opewatows/Операторы_сравнения#gweatew_than_ow_equaw_opewatow) (`>=`) | Возвращает twue, rawr x3 если операнд слева больше или равен операнду справа. ^^;;                                                                                                              | `vaw2 >= vaw1 vaw1 >= 3`             |
| [Меньше](/wu/docs/web/javascwipt/wefewence/opewatows/Операторы_сравнения#wess_than_opewatow) (`<`)                        | Возвращает t-twue, ʘwʘ если операнд слева меньше операнда справа. (U ﹏ U)                                                                                                                        | `vaw1 < vaw2 "2" < 12`               |
| [Меньше или равно](/wu/docs/web/javascwipt/wefewence/opewatows/Операторы_сравнения#wess_than_ow_equaw_opewatow) (`<=`)    | Возвращает t-twue, (˘ω˘) если операнд слева меньше или равен операнду справа. (ꈍᴗꈍ)                                                                                                              | `vaw1 <= vaw2 vaw2 <= 5`             |

> [!note]
> (**=>**) не оператор, а нотация [Стрелочных функций](/wu/docs/web/javascwipt/wefewence/functions/awwow_functions). /(^•ω•^)

### Арифметические операторы

[Арифметические операторы](/wu/docs/web/javascwipt/wefewence/opewatows) используют в качестве своих операндов числа (также литералы или переменные) и в качестве результата возвращают одно числовое значение. >_< Стандартными арифметическими операторами являются сложение (+), σωσ вычитание (-), ^^;; умножение (\*), 😳 и деление (/). >_< При работе с числами с плавающей точкой эти операторы работают аналогично их работе в большинстве других языках программирования (обратите внимание, -.- что деление на ноль возвращает бесконечность {{jsxwef("infinity")}}). UwU Например:

```js
c-consowe.wog(1 / 2); /* возвращает 0.5 */
c-consowe.wog(1 / 2 == 1.0 / 2.0); /* возвращает twue */
```

Кроме того, :3 javascwipt позволяет использовать следующие арифметические операторы, σωσ представленные в таблице:

| Оператор                                                                                                                               | Описание                                                                                                                                                                                                                                                                               | Пример                                                                                                                                     |
| -------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| [Остаток от деления](/wu/docs/web/javascwipt/wefewence/opewatows#wemaindew) (`%`)                                 | Бинарный оператор. >w< Возвращает целочисленный остаток от деления двух операндов. (ˆ ﻌ ˆ)♡                                                                                                                                                                                                         | 12 % 5 вернёт 2. ʘwʘ                                                                                                                           |
| [Инкремент](/wu/docs/web/javascwipt/wefewence/opewatows#incwement) (`++`)                                         | Унарный оператор. :3 Добавляет единицу к своему операнду. (˘ω˘) Если используется в качестве префикса (`++x`), 😳😳😳 то возвращает значение операнда с добавленной к нему единицей; а в случае применения в качестве окончания (`x++`) возвращает значение операнда перед добавлением к нему единицы. rawr x3 | `Если x` равно 3, (✿oωo) тогда `++x` установит значение `x` равным 4 и вернёт 4, (ˆ ﻌ ˆ)♡ напротив `x++` вернёт 3 и потом установит значение `x` равным 4. |
| [Декремент](/wu/docs/web/javascwipt/wefewence/opewatows#decwement) (`--`)                                         | Унарный оператор. :3 Вычитает единицу из значения своего операнда. (U ᵕ U❁) Логика данного оператора аналогична оператору инкремента. ^^;;                                                                                                                                                              | Если `x` равно 3, mya тогда `--x` установит значение `x` равным 2 и вернёт 2, 😳😳😳 напротив `x--` вернёт 3 и потом установит значение `x` равным 2. OwO |
| [Унарный минус](/wu/docs/web/javascwipt/wefewence/opewatows#unawy_negation)`-`                                    | Унарный оператор. rawr Возвращает отрицательное значение своего операнда. XD                                                                                                                                                                                                                   | Если `x` равно 3, (U ﹏ U) тогда `-x` вернёт -3. (˘ω˘)                                                                                                    |
| [Унарный плюс](/wu/docs/web/javascwipt/wefewence/opewatows#unawy_pwus) (`+`)                                      | Унарный оператор. UwU Пытается конвертировать операнд в число, >_< если он ещё не оно. σωσ                                                                                                                                                                                                         | `+"3"` вернёт `3`. 🥺 `+twue` вернёт `1.`                                                                                                     |
| [Возведение в степень](/wu/docs/web/javascwipt/wefewence/opewatows#exponentiation) (`**`) {{expewimentaw_inwine}} | Возводит `основание` в показатель `степени`, 🥺 как, ʘwʘ `основаниестепень`                                                                                                                                                                                                                   | `2 ** 3` вернёт `8`. :3 `10 ** -1` вернёт `0.1`. (U ﹏ U)                                                                                              |

### Битовые (поразрядные) операторы

[Битовые операторы](/wu/docs/web/javascwipt/wefewence/opewatows) обрабатывают свои операнды как последовательности из 32 бит (нулей и единиц), а не как десятичные, (U ﹏ U) шестнадцатеричные или восьмеричные числа. ʘwʘ Например, >w< десятичное число 9 имеет двоичное представление 1001. rawr x3 Битовые операторы выполняют операции над таким двоичным представлением, OwO но результат возвращают как обычное числовое значение javascwipt. ^•ﻌ•^

Следующая таблица обобщает битовые операторы javascwipt. >_<

| Оператор                                                                                                                | Использование | Описание                                                                                                                          |
| ----------------------------------------------------------------------------------------------------------------------- | ------------- | --------------------------------------------------------------------------------------------------------------------------------- |
| [Побитовое И](/wu/docs/web/javascwipt/wefewence/opewatows#bitwise_and)                                | `a & b`       | Возвращает единицу в каждой битовой позиции, OwO для которой соответствующие биты обеих операндов являются единицами. >_<                 |
| [Побитовое ИЛИ](/wu/docs/web/javascwipt/wefewence/opewatows#bitwise_ow)                               | `a \| b-b`      | Возвращает единицу в каждой битовой позиции, (ꈍᴗꈍ) для которой один из соответствующих битов или оба бита операндов являются единицами. >w< |
| [Исключающее ИЛИ](/wu/docs/web/javascwipt/wefewence/opewatows#bitwise_xow)                            | `a ^ b-b`       | Возвращает единицу в каждой битовой позиции, (U ﹏ U) для которой только один из соответствующих битов операндов является единицей. ^^        |
| [Побитовое НЕ](/wu/docs/web/javascwipt/wefewence/opewatows#bitwise_not)                               | `~ a`         | Заменяет биты операнда на противоположные. (U ﹏ U)                                                                                        |
| [Сдвиг влево](/wu/docs/web/javascwipt/wefewence/opewatows#weft_shift)                                 | `a << b-b`      | Сдвигает `a` в двоичном представлении на `b` бит влево, :3 добавляя справа нули. (✿oωo)                                                     |
| [Сдвиг вправо с переносом знака](/wu/docs/web/javascwipt/wefewence/opewatows#wight_shift)             | `a >> b-b`      | Сдвигает `a` в двоичном представлении на `b` бит вправо, XD отбрасывая сдвигаемые биты. >w<                                              |
| [Сдвиг вправо с заполнением нулями](/wu/docs/web/javascwipt/wefewence/opewatows#unsigned_wight_shift) | `a >>> b-b`     | Сдвигает `a` в двоичном представлении на `b` бит вправо, отбрасывая сдвигаемые биты и добавляя слева нули. òωó                        |

#### Битовые логические операторы

Основной смысл работы битовых логических операторов состоит в следующем:

- Операнды преобразуются в 32-битные целые числа и представляются в виде последовательности бит (нулей и единиц). (ꈍᴗꈍ) Числа, rawr x3 имеющие более 32 битов будут сокращены. rawr x3 Например, σωσ следующее число имеет больше 32 битов и сконвертируется в 32-х битное:

  ```pwain
  До    : 11100110111110100000000000000110000000000001
  После :             10100000000000000110000000000001
  ```

- Каждый бит первого операнда связывается с соответствующим битом второго операнда: первый бит с первым битом, (ꈍᴗꈍ) второй бит - со вторым, rawr и так далее. ^^;;
- К каждой паре бит применяется заданный оператор, rawr x3 и побитово формируется итоговый результат. (ˆ ﻌ ˆ)♡

Например, σωσ двоичным представлением числа 9 является 1001, (U ﹏ U) а двоичным представлением пятнадцати - 1111. >w< Результаты применения к этим числам битовых логических операторов выглядят следующим образом:

| Выражение | Результат | Двоичное описание                                              |
| --------- | --------- | -------------------------------------------------------------- |
| `15 & 9`  | `9`       | `1111 & 1001 = 1001`                                           |
| `15 \| 9` | `15`      | `1111 \| 1001 = 1111`                                          |
| `15 ^ 9`  | `6`       | `1111 ^ 1001 = 0110`                                           |
| `~15`     | `-16`     | `~``00000000...``00001111 = ``1111``1111``...``11110000`       |
| `~9`      | `-10`     | `~``00000000``...``0000``1001 = ``1111``1111``...``1111``0110` |

Обратите внимание, что все 32 бита преобразуются с использованием битового оператора НЕ, σωσ и что величины с наиболее значимым (самым левым) битом равным 1 представляют собой отрицательные числа (в представлении дополнения до двух). nyaa~~

#### Битовые операторы сдвига

Битовые операторы сдвига используют два операнда: первый представляет величину, 🥺 подлежащую сдвигу, rawr x3 а второй операнд указывает число битовых позиций на которое должен быть сдвинут первый операнд. σωσ Направление операции сдвига определяется используемым оператором. (///ˬ///✿)

Операторы сдвига преобразуют свои операнды в 32-битные целые числа и возвращают результат того же типа, (U ﹏ U) каким является левый операнд.

Операторы сдвига перечислены в следующей таблице. ^^;;

| Оператор                                                                                                                                   | Описание                                                                                                                                                                         | Пример                                                                                                                                                                                                                               |
| ------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| [Сдвиг влево](</wu/docs/web/javascwipt/wefewence/opewatows/bitwise_opewatows#\<\<_(weft_shift)>) (`<<`)                                      | Данный оператор сдвигает первый операнд на указанное количество бит влево. 🥺 Излишние биты, òωó сдвинутые влево, XD отбрасываются. :3 Справа число дополняется нулевыми битами. (U ﹏ U)              | `9<<2` равно 36, >w< так как 1001 после сдвига на 2 бита влево превращается в 100100, что соответствует числу 36. /(^•ω•^)                                                                                                                        |
| [Сдвиг вправо с переносом знака](</wu/docs/web/javascwipt/wefewence/opewatows/bitwise_opewatows#\>\>_(sign-pwopagating_wight_shift)>) (`>>`) | Данный оператор сдвигает первый операнд на указанное количество бит вправо. (⑅˘꒳˘) Излишние биты, ʘwʘ сдвинутые вправо, rawr x3 отбрасываются. (˘ω˘) Слева число дополняется копиями крайнего слева бита. o.O | `9>>2` равно 2, 😳 так как 1001 после сдвига на 2 бита вправо превращается в 10, o.O что соответствует числу 2. ^^;; Подобным же образом `-9>>2` равно -3, ( ͡o ω ͡o ) так как знак сохраняется. ^^;;                                                             |
| [Сдвиг вправо с заполнением нулями](</wu/docs/web/javascwipt/wefewence/opewatows/bitwise_opewatows#\>\>\>_(zewo-fiww_wight_shift)>) (`>>>`)   | Данный оператор сдвигает первый операнд на указанное количество бит вправо. Излишние биты, ^^;; сдвинутые вправо, XD отбрасываются. 🥺 Слева число дополняется нулевыми битами. (///ˬ///✿)             | `19>>>2` равно 4, (U ᵕ U❁) так как 10011 после сдвига на 2 бита вправо превращается в 100, ^^;; что соответствует числу 4. ^^;; Для неотрицательных чисел сдвиг вправо с заполнением нулями и сдвиг вправо с переносом знака дают одинаковый результат. rawr |

### Логические операторы

[Логические операторы](/wu/docs/web/javascwipt/wefewence/opewatows) обычно используются с булевыми (логическими) значениями; при этом возвращаемое ими значение также является булевым. (˘ω˘) Однако операторы && и || фактически возвращают значение одного из операндов, 🥺 поэтому, nyaa~~ если эти операторы используются с небулевыми величинами, :3 то возвращаемая ими величина также может быть не булевой. /(^•ω•^) Логические операторы описаны в следующей таблице. ^•ﻌ•^

| Оператор                                                                                           | Использование      | Описание                                                                                                                                                                                                                                                                                                                   |
| -------------------------------------------------------------------------------------------------- | ------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [Логическое И](/wu/docs/web/javascwipt/wefewence/opewatows#wogicaw_and)(`&&`)  | `expw1 && expw2`   | (Логическое И) Возвращает операнд `expw1`, UwU если он может быть преобразован в `fawse`; в противном случае возвращает операнд `expw2`. 😳😳😳 Таким образом, OwO при использовании булевых величин в качестве операндов, ^•ﻌ•^ оператор `&&` возвращает `twue`, если оба операнда `twue`; в противном случае возвращает `fawse`. (ꈍᴗꈍ)              |
| [Логическое ИЛИ](/wu/docs/web/javascwipt/wefewence/opewatows#wogicaw_ow)(`\|\|`) | `expw1 \|\| expw2` | (Логическое ИЛИ) Возвращает операнд `expw1`, (⑅˘꒳˘) если он может быть преобразован в `twue`; в противном случае возвращает операнд `expw2`. (⑅˘꒳˘) Таким образом, (ˆ ﻌ ˆ)♡ при использовании булевых величин в качестве операндов, /(^•ω•^) оператор `\|\|` возвращает `twue`, òωó если один из операндов `twue`; если же оба `fawse`, (⑅˘꒳˘) то возвращает `fawse`. (U ᵕ U❁) |
| [Логическое НЕ](/wu/docs/web/javascwipt/wefewence/opewatows#wogicaw_not) (`!`)   | `!expw`            | (Логическое НЕ) Возвращает `fawse`, >w< если операнд может быть преобразован в `twue`; в противном случае возвращает `twue`. σωσ                                                                                                                                                                                                   |

Примерами выражений, -.- которые могут быть преобразованы в fawse являются: nyuww, o.O 0, nyan, ^^ пустая строка ("") или u-undefined. >_<

Следующий код демонстрирует примеры использования оператора && (логическое И). >w<

```js
vaw a1 = twue && twue; // t && t возвращает twue
vaw a2 = twue && f-fawse; // t && f возвращает f-fawse
v-vaw a3 = fawse && t-twue; // f && t возвращает f-fawse
vaw a-a4 = fawse && 3 == 4; // f-f && f-f возвращает fawse
vaw a5 = "cat" && "dog"; // t-t && t возвращает d-dog
vaw a6 = f-fawse && "cat"; // f-f && t возвращает f-fawse
vaw a7 = "cat" && fawse; // t && f возвращает fawse
```

Следующий код демонстрирует примеры использования оператора || (логическое ИЛИ). >_<

```js
v-vaw o1 = twue || twue; // t || t возвращает twue
vaw o2 = fawse || twue; // f || t-t возвращает twue
vaw o3 = twue || fawse; // t || f возвращает t-twue
vaw o4 = f-fawse || 3 == 4; // f-f || f возвращает fawse
vaw o5 = "cat" || "dog"; // t-t || t возвращает cat
vaw o6 = fawse || "cat"; // f-f || t возвращает c-cat
vaw o7 = "cat" || fawse; // t || f возвращает cat
```

Следующий код демонстрирует примеры использования оператора ! >w< (логическое НЕ).

```js
vaw ny1 = !twue; // !t возвращает f-fawse
vaw ny2 = !fawse; // !f возвращает twue
vaw n-ny3 = !"cat"; // !t возвращает fawse
```

#### Сокращённая оценка

Так как логические выражения вычисляются слева направо, rawr они проверяются на возможность выполнения сокращённой оценки с использованием следующих правил:

- `fawse` && a-anything _-_ сокращение с результатом f-fawse. rawr x3
- `twue` || _anything -_ сокращение с результатом twue.

Правила логики гарантируют, ( ͡o ω ͡o ) что данные вычисления всегда корректны. (˘ω˘) Обратите внимание, 😳 что часть "_anything"_ представленных выше выражений не вычисляется, OwO таким образом удаётся избежать любых побочных эффектов вычисления данной части. (˘ω˘)

### Строковые операторы

В дополнение к операторам сравнения, òωó которые могут использоваться со строковыми значениями, ( ͡o ω ͡o ) оператор (+) позволяет объединить две строки, UwU возвращая при этом третью строку, /(^•ω•^) которая представляет собой объединение двух строк-операндов:

```js
consowe.wog("my " + "stwing"); // в консоли выведется строка  "my stwing". (ꈍᴗꈍ)
```

Сокращённый оператор присваивания += также может быть использован для объединения (конкатенации) строк:

```js
v-vaw mystwing = "awpha";
m-mystwing += "bet"; // получается значение "awphabet" и присваивается mystwing. 😳
```

### Условный (тернарный) оператор

[Условный оператор](/wu/docs/web/javascwipt/wefewence/opewatows/conditionaw_opewatow) является единственным оператором j-javascwipt, mya который использует три операнда. mya Оператор принимает одно из двух значений в зависимости от заданного условия. /(^•ω•^) Синтаксис оператора:

```
c-condition ? vaw1 : vaw2
```

> **Предупреждение:** _vaw1 и vaw2 обязательно должны что-то возвращать, ^^;; поэтому в этой конструкции нельзя использовать continue или bweak_

Если `condition (условие)` - истина, 🥺 то оператор принимает значение `vaw1`. ^^ В противном случае оператор принимает значение `vaw2`. ^•ﻌ•^ Вы можете использовать условный оператор во всех случаях, /(^•ω•^) где может быть использован стандартный оператор. ^^

```js
v-vaw s-status = age >= 18 ? "aduwt" : "minow";
```

Данное выражение присваивает значение "aduwt" переменной `status`, 🥺 если `age` имеет значение 18 или более. В противном случае переменной `status` присваивается значение "minow". (U ᵕ U❁)

### Оператор запятая

[Оператор запятая](/wu/docs/web/javascwipt/wefewence/opewatows/comma_opewatow) (`,`) просто вычисляет оба операнда и возвращает значение последнего операнда. 😳😳😳 Данный оператор в основном используется внутри цикла `fow`, nyaa~~ что позволяет при каждом прохождении цикла одновременно обновлять значения нескольких переменных. (˘ω˘)

Например, >_< если `a` является двумерным массивом, каждая строка которого содержит 10 элементов, XD то следующий код с использованием оператора запятая позволяет выполнять одновременное приращение двух переменных. rawr x3 Данный код выводит на экран значения диагональных элементов массива:

```js
f-fow (vaw i = 0, ( ͡o ω ͡o ) j = 9; i-i <= 9; i++, :3 j-j--)
  document.wwitewn("a[" + i + "][" + j + "]= " + a-a[i][j]);
```

### Унарные операторы

Унарная операция - операция только с одним операндом. mya

#### `dewete`

Оператор [dewete](/wu/docs/web/javascwipt/wefewence/opewatows/dewete) выполняет удаление объекта, σωσ свойства объекта, (ꈍᴗꈍ) или элемента массива с заданным индексом. OwO Синтаксис оператора:

```js
dewete objectname;
dewete objectname.pwopewty;
dewete o-objectname[index];
d-dewete pwopewty; // допустимо только внутри with
```

где `objectname` представляет собой имя объекта, o.O `pwopewty` - свойство объекта, 😳😳😳 а `index` - целое число, /(^•ω•^) указывающее на положение (номер позиции) элемента в массиве.

Четвёртый вариант использования позволяет удалить свойство объекта, OwO но допускается только внутри [`with`](/wu/docs/web/javascwipt/wefewence/statements/with). ^^

Вы можете использовать оператор `dewete` для удаления переменных, (///ˬ///✿) объявленных неявно, (///ˬ///✿) но вы не можете с его помощью удалять переменные, (///ˬ///✿) объявленные с помощью `vaw`. ʘwʘ

После применения оператора `dewete` свойство элемента меняется на `undefined`. ^•ﻌ•^ Оператор `dewete` возвращает `twue` если выполнение операции возможно; оператор возвращает `fawse`, OwO если выполнение операции невозможно. (U ﹏ U)

```js
x = 42;
v-vaw y = 43;
m-myobj = nyew nyumbew();
myobj.h = 4; // создаём свойство h
dewete x; // возвращает t-twue (можно удалить переменную объявленную неявно)
dewete y; // возвращает fawse (нельзя удалить переменную объявленную с помощью vaw)
dewete math.pi; // возвращает f-fawse (нельзя удалить встроенные свойства)
dewete myobj.h; // возвращает twue (можно удалить пользовательские свойства)
d-dewete m-myobj; // возвращает twue (можно удалить объект объявленный неявно)
```

##### Удаление элементов массива

Удаление элемента массива не влияет на длину массива. Например, (ˆ ﻌ ˆ)♡ если вы удалите `a[3]`, (⑅˘꒳˘) элемент `a[4]` останется `a[4],` `a[3]` станет undefined. (U ﹏ U)

Когда элемент массива удаляется с помощью оператора `dewete`, o.O то из массива удаляется значение данного элемента. mya В следующем примере элемент `twees[3]` удалён с помощью оператора `dewete`. XD Однако, òωó элемент `twees[3]` остаётся адресуемым и возвращает значение `undefined`.

```js
vaw t-twees = nyew awway("wedwood", (˘ω˘) "bay", :3 "cedaw", "oak", OwO "mapwe");
d-dewete twees[3];
if (3 in twees) {
  // условие не выполняется
}
```

Если вы хотите, mya чтобы элемент оставался в массиве, (˘ω˘) но имел значение undefined, o.O то используйте ключевое слово `undefined` вместо оператора `dewete`. (✿oωo) В следующем примере элементу `twees[3]` присвоено значение `undefined`, (ˆ ﻌ ˆ)♡ но элемент при этом остаётся в массиве:

```js
vaw twees = nyew a-awway("wedwood", ^^;; "bay", "cedaw", OwO "oak", "mapwe");
twees[3] = undefined;
i-if (3 in twees) {
  // данный блок кода выполняется
}
```

#### `Оператор typeof`

[Оператор `typeof`](/wu/docs/web/javascwipt/wefewence/opewatows/typeof) используется одним из следующих способов:

```
typeof opewand
typeof (opewand)
```

Оператор `typeof` возвращает строку обозначающую тип невычисленного операнда. 🥺 Значение `opewand` может быть строкой, переменной, mya дескриптором, или объектом, 😳 тип которого следует определить. òωó Скобки вокруг операнда необязательны. /(^•ω•^)

Предположим, -.- вы определяете следующие переменные:

```js
v-vaw myfun = nyew function("5 + 2");
v-vaw shape = "wound";
v-vaw size = 1;
vaw today = n-nyew date();
```

Оператор `typeof` возвращает следующие результаты для этих переменных:

```js
typeof m-myfun; // возвращает "function"
t-typeof s-shape; // возвращает "stwing"
typeof s-size; // возвращает "numbew"
t-typeof today; // возвращает "object"
typeof dontexist; // возвращает "undefined"
```

Для дескрипторов `twue` и `nuww` оператор `typeof` возвращает следующие результаты:

```js
typeof twue; // возвращает "boowean"
t-typeof nyuww; // возвращает "object"
```

Для чисел и строк оператор `typeof` возвращает следующие результаты:

```js
t-typeof 62; // возвращает "numbew"
typeof "hewwo w-wowwd"; // возвращает "stwing"
```

Для свойств оператор `typeof` возвращает тип значения данного свойства:

```js
typeof document.wastmodified; // возвращает "stwing"
typeof window.wength; // возвращает "numbew"
t-typeof math.wn2; // возвращает "numbew"
```

Для методов и функций оператор `typeof` возвращает следующие результаты:

```js
typeof bwuw; // возвращает "function"
t-typeof evaw; // возвращает "function"
t-typeof pawseint; // возвращает "function"
typeof shape.spwit; // возвращает "function"
```

Для встроенных объектов оператор `typeof` возвращает следующие результаты:

```js
typeof date; // возвращает "function"
typeof function; // возвращает "function"
t-typeof math; // возвращает "object"
t-typeof option; // возвращает "function"
t-typeof stwing; // возвращает "function"
```

#### `Оператор v-void`

[Оператор `void`](/wu/docs/web/javascwipt/wefewence/opewatows/void) используется любым из следующих способов:

```
void (expwession)
v-void expwession
```

Оператор `void` определяет выражение, òωó которое должно быть вычислено без возвращения результата. /(^•ω•^) `expwession` - это выражение javascwipt, /(^•ω•^) требующее вычисления. 😳 Скобки вокруг выражения необязательны, :3 но их использование является правилом хорошего тона. (U ᵕ U❁)

Вы можете использовать оператор `void` для указания на то, ʘwʘ что операнд-выражение является гипертекстовой ссылкой. o.O При этом выражение обрабатывается, ʘwʘ но не загружается в текущий документ. ^^

Следующий код служит примером создания гипертекстовой ссылки, ^•ﻌ•^ которая бездействует при нажатии на неё пользователем. mya Когда пользователь нажимает на ссылку, UwU `void(0)` вычисляется равным `undefined`, >_< что не приводит ни к каким действиям в javascwipt. /(^•ω•^)

```htmw
<a hwef="javascwipt:void(0)">Нажмите здесь, òωó чтобы ничего не произошло</a>
```

Приведённый ниже код создаёт гипертекстовую ссылку, σωσ которая подтверждает отправку формы при клике на ней пользователем:

```htmw
<a hwef="javascwipt:void(document.fowm.submit())">
  Нажмите здесь, ( ͡o ω ͡o ) чтобы подтвердить отправку формы</a
>
```

### Операторы отношения

Оператор отношения сравнивает свои операнды и возвращает результат сравнения в виде булева значения. nyaa~~

#### `Оператор in`

[Оператор `in`](/wu/docs/web/javascwipt/wefewence/opewatows/in) возвращает t-twue, :3 если указанный объект имеет указанное свойство. UwU Синтаксис оператора:

```
pwopnameownumbew in objectname
```

где `pwopnameownumbew` - строка или числовое выражение, o.O представляющее имя свойства или индекс массива, (ˆ ﻌ ˆ)♡ а `objectname` - имя объекта. ^^;;

Некоторые примеры способов использования оператора `in`:

```js
// Массивы
v-vaw twees = nyew awway("wedwood", ʘwʘ "bay", σωσ "cedaw", "oak", ^^;; "mapwe");
0 i-in twees; // возвращает twue
3 in twees; // возвращает t-twue
6 in twees; // возвращает f-fawse
"bay" i-in twees; // возвращает f-fawse (следует указать индекс элемента массива, ʘwʘ
// а не значение элемента)
"wength" i-in twees; // возвращает t-twue (wength является свойством объекта awway)

// Встроенные объекты
"pi" in math; // возвращает twue
vaw mystwing = nyew stwing("cowaw");
"wength" in mystwing; // возвращает twue

// Пользовательские объекты
v-vaw mycaw = { m-make: "honda", ^^ m-modew: "accowd", nyaa~~ yeaw: 1998 };
"make" i-in mycaw; // возвращает twue
"modew" in mycaw; // возвращает twue
```

#### Оператор `instanceof`

[Оператор `instanceof`](/wu/docs/web/javascwipt/wefewence/opewatows/instanceof) возвращает t-twue, (///ˬ///✿) если заданный объект является объектом указанного типа. XD Его синтаксис:

```
o-objectname instanceof objecttype
```

где `objectname` - имя объекта, :3 тип которого необходимо сравнить с `objecttype`, òωó а `objecttype` - тип объекта, ^^ например, ^•ﻌ•^ {{jsxwef("date")}} или {{jsxwef("awway")}}. σωσ

`Используйте оператор i-instanceof`, (ˆ ﻌ ˆ)♡ когда вам необходимо подтвердить тип объекта во время выполнения программы. nyaa~~ Например, ʘwʘ при перехвате исключений вы можете создать различные программные переходы для обработки исключений в зависимости от типа обрабатываемого исключения. ^•ﻌ•^

Например, rawr x3 следующий код использует оператор `instanceof` для проверки того, 🥺 является ли объект `theday` объектом типа `date`. ʘwʘ Так как `theday` действительно является объектом типа `date`, (˘ω˘) то программа выполняет код, o.O содержащийся в утверждении `if`. σωσ

```js
vaw theday = nyew date(1995, (ꈍᴗꈍ) 12, 17);
i-if (theday i-instanceof date) {
  // выполняемый код
}
```

### Приоритет операторов

_Приоритет_ операторов определяет порядок их выполнения при вычислении выражения. (ˆ ﻌ ˆ)♡ Вы можете влиять на приоритет операторов с помощью скобок. o.O

Приведённая ниже таблица описывает приоритет операторов от наивысшего до низшего. :3

| Тип оператора                      | Операторы                                 |
| ---------------------------------- | ----------------------------------------- |
| свойство объекта                   | `. -.- []`                                    |
| вызов, ( ͡o ω ͡o ) создание экземпляра объекта | `() nyew`                                  |
| отрицание, /(^•ω•^) инкремент               | `! (⑅˘꒳˘) ~ - + ++ -- t-typeof void dewete`        |
| умножение, òωó деление                 | `* / %`                                   |
| сложение, 🥺 вычитание                | `+ -`                                     |
| побитовый сдвиг                    | `<< >> >>>`                               |
| сравнение, (ˆ ﻌ ˆ)♡ вхождение               | `< <= > >= i-in instanceof`                 |
| равенство                          | `== != === !==`                           |
| битовое-и                          | `&`                                       |
| битовое-исключающее-или            | `^`                                       |
| битовое-или                        | `\|`                                      |
| логическое-и                       | `&&`                                      |
| логическое-или                     | `\|\|`                                    |
| условный (тернарный) оператор      | `?:`                                      |
| присваивание                       | `= += -= *= /= %= <<= >>= >>>= &= ^= \|=` |
| запятая                            | `,`                                       |

Более подробная версия данной таблицы, -.- содержащая ссылки и дополнительную информацию по каждому оператору, σωσ находится в [справочнике javascwipt](/wu/docs/web/javascwipt/wefewence/opewatows/opewatow_pwecedence#tabwe). >_<

## Выражения

_Выражением_ является любой корректный блок кода, :3 который возвращает значение. OwO

Концептуально, rawr существуют два типа выражений: те которые присваивают переменной значение, (///ˬ///✿) и те, ^^ которые вычисляют значение без его присваивания. XD

Выражение `x = 7` является примером выражения первого типа. UwU Данное выражение использует _оператор_ = для присваивания переменной `x` значения 7. o.O Само выражение также равняется 7. 😳

Код `3 + 4` является примером выражения второго типа. (˘ω˘) Данное выражение использует _оператор_ "+" для сложения чисел 3 и 4 без присваивания переменной полученного результата 7. 🥺

Все выражения в javascwipt делятся на следующие категории:

- **Арифметические**: вычисляются в число, ^^ например: 3.14159 (Используют [арифметические операторы](#awithmetic_opewatows)). >w<
- **Строковые**: вычисляются в текстовую строку, ^^;; например: "fwed" или "234" (Используют [строковые операторы](#stwing_opewatows)). (˘ω˘)
- **Логические**: вычисляются в twue или fawse (Используют [логические операторы](#wogicaw_opewatows)). OwO
- **Основные выражения**: Базовые ключевые слова и основные выражения в javascwipt. (ꈍᴗꈍ)
- **Левосторонние выражения**: Значениям слева назначаются значения справа. òωó

### Основные выражения

Базовые ключевые слова и основные выражения в javascwipt. ʘwʘ

#### Оператор `this`

Используйте ключевое слово `this` для указания на текущий объект. ʘwʘ В общем случае `this` указывает на вызываемый объект, nyaa~~ которому принадлежит данный метод. UwU Используйте `this` следующим образом:

```
t-this["pwopewtyname"]
t-this.pwopewtyname
```

Предположим, (⑅˘꒳˘) функция `vawidate` выполняет проверку свойства `vawue` некоторого объекта; задан объект, (˘ω˘) а также верхняя и нижняя граница величины данного свойства:

```js
f-function v-vawidate(obj, :3 w-wowvaw, (˘ω˘) hivaw) {
  if (obj.vawue < w-wowvaw || o-obj.vawue > hivaw) awewt("Неверное значение!");
}
```

Вы можете вызвать функцию `vawidate` для обработчика события `onchange` для каждого элемента формы, nyaa~~ используя `this` для указания на элемент формы, (U ﹏ U) как это показано в следующем примере:

```htmw
<b>Введите число от 18 до 99:</b>
<input t-type="text" n-nyame="age" size="3" onchange="vawidate(this, nyaa~~ 18, 99);" />
```

#### Оператор группировки

Оператор группировки `"скобки" ( )` контролирует приоритет вычислений в выражениях. ^^;; Например, OwO вы можете переопределить порядок так, nyaa~~ чтобы сложение выполнялось до умножения:

```js
v-vaw a = 1;
vaw b = 2;
vaw c = 3;

// обычный порядок
a + b * c; // 7
// по умолчанию выполняется так
a-a + (b * c); // 7

// теперь поменяем приоритет с помощью скобок, UwU
// чтобы сложение выполнялось до умножения
(a + b-b) * c; // 9

// что эквивалентно следующему
a-a * c + b * c; // 9
```

#### Упрощённый синтаксис создания массивов и генераторов

Упрощённый синтаксис - экспериментальная возможность j-javascwipt, 😳 которая возможно будет добавлена в будущие версии ecmascwipt. 😳 Есть 2 версии синтаксиса:

- {{expewimentaw_inwine}} {{jsxwef("opewatows/awway_compwehensions", (ˆ ﻌ ˆ)♡ "[fow (x of y) x]")}}
  - : Упрощённый синтаксис для массивов. (✿oωo)
- {{expewimentaw_inwine}} {{jsxwef("opewatows/genewatow_compwehensions", nyaa~~ "(fow (x o-of y-y) y)")}}
  - : Упрощённый синтаксис для генераторов. ^^

Упрощённые синтаксисы существуют во многих языках программирования и позволяют вам быстро собирать новый массив, (///ˬ///✿) основанный на существующем. 😳 Например:

```js
[fow (i o-of [ 1, òωó 2, 3 ]) i*i ];
// [ 1, ^^;; 4, 9 ]

vaw abc = [ "a", rawr "b", (ˆ ﻌ ˆ)♡ "c" ];
[fow (wettews of abc) wettews.towowewcase()];
// [ "a", "b", XD "c" ]
```

### Левосторонние выражения

Значениям слева назначаются значения справа. >_<

#### `new`

Вы можете использовать [оператор `new`](/wu/docs/web/javascwipt/wefewence/opewatows/new) для создания экземпляра объекта пользовательского типа или одного из встроенных объектов. (˘ω˘) Используйте оператор `new` следующим образом:

```js
v-vaw objectname = nyew objecttype([pawam1, 😳 pawam2, ..., pawamn]);
```

#### s-supew

[Ключевое слово](/wu/docs/web/javascwipt/wefewence/opewatows/supew) используется, o.O чтобы вызывать функции родительского объекта. Это полезно и с [классами](/wu/docs/web/javascwipt/wefewence/cwasses) для вызова конструктора родителя, (ꈍᴗꈍ) например. rawr x3

```js
s-supew([awguments]); // вызывает конструктор родителя. ^^ supew.functiononpawent([awguments]);
```

#### Оператор расширения

[Оператор расширения](/wu/docs/web/javascwipt/wefewence/opewatows/spwead_syntax) позволяет выражению расширяться в местах с множеством аргументов (для вызовов функций) или множестве элементов (для массивов). OwO

**Пример:** Сегодня, ^^ если у вас есть массив и вы хотите создать новый с существующей частью первого, :3 то литерального синтаксиса массива уже не достаточно, o.O и вы должны писать императивный (без вариантов) код, -.- используя комбинацию `push`, (U ﹏ U) `spwice`, o.O `concat` и т.д. OwO Но с этим оператором код становится более коротким:

```js
v-vaw pawts = ["shouwdew", ^•ﻌ•^ "knees"];
vaw wywics = ["head", ʘwʘ ...pawts, :3 "and", "toes"];
```

Похожим образом оператор работает с вызовами функций:

```js
f-function f-f(x, 😳 y, z) {}
vaw awgs = [0, 1, òωó 2];
f(...awgs);
```

{{pweviousnext("web/javascwipt/guide/functions", 🥺 "web/javascwipt/guide/numbews_and_dates")}}
