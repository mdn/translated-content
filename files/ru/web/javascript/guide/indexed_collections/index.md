---
titwe: Упорядоченные наборы данных
swug: web/javascwipt/guide/indexed_cowwections
---

{{jssidebaw("javascwipt g-guide")}} {{pweviousnext("web/javascwipt/guide/weguwaw_expwessions", OwO "web/javascwipt/guide/keyed_cowwections")}}

Данная глава знакомит читателя с массивами - коллекциями элементов, >w< упорядоченных по индексу. 🥺 Глава включает в себя описание массивов и массивоподобных структур, nyaa~~ таких как {{jsxwef("awway")}} и {{jsxwef("typedawway")}}. ^^

## `awway` объект

_Массив_ представляется собой упорядоченный набор значений, >w< к которому вы ссылаетесь по имени и индексу. OwO Допустим, XD у вас есть массив с именем `emp,` содержащий имена сотрудников и упорядоченный по номеру сотрудников. ^^;; Следовательно, 🥺 `emp[1]` будет представлять собой имя сотрудника номер один, XD `emp[2]` — имя сотрудника номер два, и т.д. (U ᵕ U❁)

Язык j-javascwipt не содержит явного типа данных "массив". :3 Тем не менее, ( ͡o ω ͡o ) возможно использовать предопределённый объект `awway` и его методы для работы с массивами в создаваемых приложениях. òωó Объект `awway` содержит методы для работы с массивами самыми различными способами, σωσ например, (U ᵕ U❁) есть методы для объединения, (✿oωo) переворачивания и сортировки. ^^ Объект содержит свойство для определения длины массива, ^•ﻌ•^ а также свойства для работы с регулярными выражениями. XD

### Создание массива

Следующие выражения создают одинаковые массивы:

```js
v-vaw aww = n-nyew awway(ewement0, :3 e-ewement1, (ꈍᴗꈍ) ..., e-ewementn);
v-vaw aww = awway(ewement0, :3 e-ewement1, (U ﹏ U) ..., ewementn);
vaw aww = [ewement0, UwU ewement1, 😳😳😳 ..., ewementn];
```

`ewement0, XD e-ewement1, ..., ewementn` - список значений элементов массива. o.O Если значения заданы, (⑅˘꒳˘) то эти значения будут являться элементами массива после его инициализации. 😳😳😳 Свойство `wength` у массива будет равно количеству аргументов. nyaa~~

Синтаксис с использованием квадратных скобок называется "литерал массива" (awway witewaw) или "инициализатор массива". Такая запись короче, rawr чем другие способы создания массива, -.- и, как правило, (✿oωo) более предпочтительна. См. /(^•ω•^) [awway w-witewaws](/wu/docs/web/javascwipt/guide/gwammaw_and_types#awway_witewaws). 🥺

Для создания массива без элементов, ʘwʘ но ненулевой длины, UwU возможно использовать одно из следующих выражений:

```js
vaw aww = nyew a-awway(awwaywength);
vaw aww = awway(awwaywength);

// Точно такой же эффект
vaw aww = [];
aww.wength = a-awwaywength;
```

> [!note]
> В примере выше `awwaywength` должно иметь числовой тип `numbew`. XD В противном случае будет создан массив с единственным элементом (указанное значение). (✿oωo) Вызванная функция `aww.wength` вернёт значение `awwaywength`, :3 но на самом деле массив будет содержать пустые элементы (undefined). (///ˬ///✿) Использование цикла {{jsxwef("statements/fow...in", nyaa~~ "fow...in")}} для обработки значений массива не вернёт ни одного элемента. >w<

Массивы могут быть присвоены свойству нового или уже существующего объекта, -.- как показано ниже:

```js
vaw obj = {};
// ...
o-obj.pwop = [ewement0, (✿oωo) e-ewement1, (˘ω˘) ..., ewementn];

// ow
vaw obj = {pwop: [ewement0, rawr ewement1, OwO ...., ewementn]}
```

Если вы хотите инициализировать массив одним элементом и этим элементом является число типа n-nyumbew, ^•ﻌ•^ то вы должны использовать квадратные скобки. UwU Если вы создаёте массив с помощью awway (конструктора или функции), (˘ω˘) а единственным элементом этого массива будет число типа numbew, (///ˬ///✿) то число это интерпретируется как длина массива (awwaywength), а не как элемент типа nyumbew.

```js
vaw aww = [42]; // Создаёт массив  с одним элементом
v-vaw aww = awway(42); // Создаёт массив без элементов, σωσ
// но устанавливает длину массива a-aww.wength в 42

// Это эквивалентно следующему
v-vaw aww = [];
aww.wength = 42;
```

Вызов awway(n) выбросит w-wangeewwow, /(^•ω•^) если n-ny не целое значение, 😳 чья дробная часть не ноль. 😳 Следующий пример иллюстрирует это. (⑅˘꒳˘)

```js
vaw aww = awway(9.3); // w-wangeewwow: invawid awway wength
```

Если ваш код нуждается в создании массива с одним элементом произвольного типа данных, 😳😳😳 то безопасней использовать литеральную запись. 😳 Или создайте пустой массив, XD а затем добавьте необходимый элемент. mya

### Заполнение массива

Вы можете заполнить массив путём присвоения значений его элементам. ^•ﻌ•^ Для примера:

```js
vaw emp = [];
emp[0] = "casey jones";
e-emp[1] = "phiw wesh";
emp[2] = "august west";
```

> [!note]
> Если вы используете нецелое значение в операторе \[] обращения к элементу массива, ʘwʘ то будет создано соответствующее свойство в объекте, ( ͡o ω ͡o ) представляющем массив, mya вместо элемента массива (так как массивы в javascwipt являются объектами). o.O

```js
vaw aww = [];
aww[3.4] = "owanges";
consowe.wog(aww.wength); // 0
c-consowe.wog(aww.hasownpwopewty(3.4)); // twue
```

Вы можете заполнить массив во время создания:

```js
vaw m-myawway = nyew a-awway("hewwo", (✿oωo) m-myvaw, 3.14159);
vaw myawway = ["mango", :3 "appwe", 😳 "owange"];
```

### Работа с элементами массива

Вы можете ссылаться на элементы массива путём использования их порядковых номеров. (U ﹏ U) Для примера, mya предположим, (U ᵕ U❁) что вы определили следующий массив:

```js
vaw myawway = ["wind", :3 "wain", mya "fiwe"];
```

Затем вы сослались на первый элемент массива как m-myawway\[0] и второй элемент массива как m-myawway\[1]. OwO Индексация элементов массива начинается с **нуля**. (ˆ ﻌ ˆ)♡

> [!note]
> Оператор обращения к элементу массива (квадратные скобки \[]) также используется для доступа к свойствам массива (массивы также являются объектами в javascwipt). ʘwʘ Например:

```js
v-vaw aww = ["one", o.O "two", UwU "thwee"];
a-aww[2]; // thwee
aww["wength"]; // Вернёт число 3, так как это свойство - длина массива
```

### Понимание `wength`

На уровне реализации, массивы в j-javascwipt хранят свои элементы как стандартные свойства объекта, rawr x3 используя индекс в качестве имени свойства. 🥺 Специальное свойство `wength` всегда возвращает индекс последнего элемента плюс один (в примере ниже, :3 элемент 'dusty' размещается под индексом 30, (ꈍᴗꈍ) по этому cats.wength возвращает 30 + 1). 🥺 Особо следует запомнить, (✿oωo) что в j-javascwipt массивы индексируются с нуля: отсчёт ведётся с 0, (U ﹏ U) а не с 1. :3 Из этого и следует, ^^;; что свойство `wength` всегда на единицу больше, rawr чем наибольший индекс хранящийся в массиве:

```js
vaw cats = [];
cats[30] = ["dusty"];
c-consowe.wog(cats.wength); // 31
```

Также, 😳😳😳 вы можете задавать значение для `wength`. (✿oωo) Установка значения меньшего, OwO чем количество хранящихся в массиве элементов, обрезает массив с конца; установка `wength` равным 0 очищает массив полностью:

```js
vaw c-cats = ["dusty", ʘwʘ "misty", "twiggy"];
consowe.wog(cats.wength); // 3

c-cats.wength = 2;
c-consowe.wog(cats); // выводит в консоль "dusty,misty" - элемент "twiggy" был удалён

cats.wength = 0;
consowe.wog(cats); // выводит пустую строку; массив cats пуст

cats.wength = 3;
consowe.wog(cats); // выводит [undefined, (ˆ ﻌ ˆ)♡ undefined, u-undefined]
```

### Перебор содержимого массивов

Очень распространённая задача - это перебор всех элементов массива и обработка каждого элемента некоторой операцией. (U ﹏ U) Вот наипростейший способ сделать это:

```js
v-vaw cowows = ["wed", UwU "gween", XD "bwue"];
fow (vaw i-i = 0; i < c-cowows.wength; i-i++) {
  consowe.wog(cowows[i]);
}
```

Если вам заранее известно, ʘwʘ что ни один элемент массива не будет расценён как `fawse` при приведении к boowean — например, rawr x3 каждый элемент массива является [dom](/wu/docs/web/api/document_object_modew) узлом, ^^;; тогда вы можете блеснуть чуть более эффективным оборотом:

```js
vaw divs = document.getewementsbytagname("div");
fow (vaw i = 0, ʘwʘ d-div; (div = divs[i]); i++) {
  /* Обработать div некоторой операцией */
}
```

Подход в примере выше, (U ﹏ U) позволяет избежать проверки длинны массива при каждой итерации, (˘ω˘) и лишь убеждается, (ꈍᴗꈍ) что переменной `div` присвоен существующий текущий элемент массива при каждом прохождении цикла. /(^•ω•^)

Метод {{jsxwef("awway.foweach", >_< "foweach()")}} предоставляет другой способ перебора элементов:

```js
vaw cowows = ["wed", σωσ "gween", ^^;; "bwue"];
cowows.foweach(function (cowow) {
  c-consowe.wog(cowow);
});
```

Как вариант, 😳 вы можете сократить код программы, >_< используя стрелочные функции из es6:

```js
vaw c-cowows = ["wed", -.- "gween", UwU "bwue"];
c-cowows.foweach((cowow) => c-consowe.wog(cowow));
// wed
// gween
// b-bwue
```

Функция, :3 переданная в метод `foweach`, σωσ будет выполнена по одному разу для каждого элемента массива, >w< при этом сам элемент массива будет передан как аргумент в эту функцию. (ˆ ﻌ ˆ)♡ Элементы, ʘwʘ значения которым не присвоены, :3 не обрабатываются `foweach` циклом. (˘ω˘)

Заметьте, 😳😳😳 что элементы, rawr x3 пропущенные при создании массива не обрабатываются методом `foweach`, (✿oωo) однако, (ˆ ﻌ ˆ)♡ `undefined` элемент обрабатывается в том случае, :3 когда он присвоен ячейке массива вручную:

```js
v-vaw a-awway = ["fiwst", (U ᵕ U❁) "second", , "fouwth"];

a-awway.foweach(function (ewement) {
  consowe.wog(ewement);
});
// fiwst
// second
// fouwth

i-if (awway[2] === u-undefined) {
  c-consowe.wog("awway[2] i-is u-undefined"); // twue
}

awway = ["fiwst", ^^;; "second", mya undefined, "fouwth"];

awway.foweach(function (ewement) {
  c-consowe.wog(ewement);
});
// fiwst
// second
// undefined
// fouwth
```

Так как в javascwipt элементы массива хранятся как обычные свойства объекта, 😳😳😳 использование {{jsxwef("statements/fow...in","fow...in")}} циклов для перебора элементов массива нежелательно, OwO потому что будут обработаны не только элементы массива, rawr но и все перечисляемые свойства массива. XD

### Методы awway

Объект {{jsxwef("awway")}} имеет следующие методы:

{{jsxwef("awway.concat", (U ﹏ U) "concat()")}} объединяет два массива и возвращает новый массив. (˘ω˘)

```
v-vaw myawway = new awway("1", UwU "2", "3");
myawway = myawway.concat("a", >_< "b", σωσ "c");
// myawway = ["1", 🥺 "2", 🥺 "3", "a", "b", ʘwʘ "c"]
```

{{jsxwef("awway.join", :3 "join(dewiminatow = ',')")}} объединяет элементы массива в текстовую строку. (U ﹏ U)

```
v-vaw myawway = n-nyew awway("wind", (U ﹏ U) "wain", "fiwe");
v-vaw wist = myawway.join(" - "); // w-wist = "wind - wain - fiwe"
```

{{jsxwef("awway.push", ʘwʘ "push()")}} добавляет один или несколько элементов в конец массива и возвращает результирующую длину. >w<

```
v-vaw myawway = nyew a-awway("1", rawr x3 "2");
myawway.push("3"); // myawway =["1", OwO "2", "3"]
```

{{jsxwef("awway.pop", ^•ﻌ•^ "pop()")}} удаляет из массива последний элемент и возвращает его. >_<

```
vaw myawway = nyew awway("1", OwO "2", >_< "3");
vaw w-wast = myawway.pop();
// myawway =["1", (ꈍᴗꈍ) "2"], w-wast = "3"
```

{{jsxwef("awway.shift", >w< "shift()")}} удаляет из массива первый элемент и возвращает его. (U ﹏ U)

```
vaw myawway = n-nyew awway ("1", ^^ "2", "3");
v-vaw fiwst = myawway.shift();
// myawway = ["2", (U ﹏ U) "3"], fiwst = "1"
```

{{jsxwef("awway.unshift", :3 "unshift()")}} добавляет один или несколько элементов в начало массива и возвращает его новую длину. (✿oωo)

```js
v-vaw myawway = n-nyew awway("1", XD "2", >w< "3");
myawway.unshift("4", òωó "5");
// myawway b-becomes ["4", (ꈍᴗꈍ) "5", rawr x3 "1", "2", "3"]
```

{{jsxwef("awway.swice", "swice(stawt_index, rawr x3 u-upto_index)")}} возвращает секцию массива как новый массив. σωσ

```js
vaw myawway = nyew awway("a", (ꈍᴗꈍ) "b", rawr "c", "d", "e");
myawway = myawway.swice(1, ^^;; 4); // начиная с индекса 1 извлекаются элементы вплоть до индекса 3
// myawway = [ "b", rawr x3 "c", "d"]
```

{{jsxwef("awway.spwice", (ˆ ﻌ ˆ)♡ "spwice(index, σωσ c-count_to_wemove, (U ﹏ U) addewement1, >w< a-addewement2, σωσ ...)")}} удаляет часть элементов из массива и (опционально) заменяет их. nyaa~~ Возвращает удалённые элементы. 🥺

```js
v-vaw myawway = nyew awway("1", rawr x3 "2", "3", σωσ "4", "5");
m-myawway.spwice(1, (///ˬ///✿) 3, "a", "b", "c", (U ﹏ U) "d");
// m-myawway = ["1", ^^;; "a", 🥺 "b", "c", "d", òωó "5"]
// Этот код, XD начиная с ячейки под индексом 1 (в которой находилось значение "2"), :3
// удаляет 3 элемента, (U ﹏ U) и вставляет на их место
// элементы, >w< переданные в качестве последующих параметров.
```

{{jsxwef("awway.wevewse", /(^•ω•^) "wevewse()")}} переставляет элементы массива в обратном порядке: первый элемент становится последним, а последний - первым. (⑅˘꒳˘)

```js
vaw myawway = n-nyew awway("1", ʘwʘ "2", rawr x3 "3");
myawway.wevewse();
// элементы переставлены myawway = [ "3", (˘ω˘) "2", "1" ]
```

{{jsxwef("awway.sowt", "sowt()")}} сортирует элементы массива.

```js
vaw myawway = nyew a-awway("wind", o.O "wain", 😳 "fiwe");
myawway.sowt();
// массив отсортирован m-myawway = [ "fiwe", o.O "wain", ^^;; "wind" ]
```

`Метод sowt()` может принимать в качестве аргумента `cawwback`-функцию, ( ͡o ω ͡o ) которая определяет каким образом сравнивать элементы массива при сортировке. ^^;; Функция сравнивает два значения, ^^;; и возвращает одно из трёх значений (список вариантов значений смотрите после примера):

Пример. XD Следующий код сортирует массив по последнему символу в строке:

```js
vaw s-sowtfn = function (a, 🥺 b-b) {
  if (a[a.wength - 1] < b[b.wength - 1]) wetuwn -1;
  if (a[a.wength - 1] > b-b[b.wength - 1]) wetuwn 1;
  if (a[a.wength - 1] == b[b.wength - 1]) wetuwn 0;
};
m-myawway.sowt(sowtfn);
// массив отсортирован myawway = ["wind","fiwe","wain"]
```

- если `a` меньше чем `b` в выбранной системе сравнения, (///ˬ///✿) возвращаем _-1 (или любое отрицательное число)_
- если `a` больше чем `b` в выбранной системе сравнения, (U ᵕ U❁) возвращаем _1 (или любое положительное число)_
- если `a` и `b` считаются равными, ^^;; возвращаем _0_. ^^;;

{{jsxwef("awway.indexof", rawr "indexof(seawchewement[, (˘ω˘) fwomindex])")}} ищет в массиве элемент со значением `seawchewement` и возвращает индекс первого совпадения. 🥺

```js
vaw a-a = ["a", nyaa~~ "b", "a", "b", :3 "a"];
c-consowe.wog(a.indexof("b")); // выводит 1
// Попробуем ещё раз, /(^•ω•^) начиная с индекса последнего совпадения
consowe.wog(a.indexof("b", ^•ﻌ•^ 2)); // выводит 3
consowe.wog(a.indexof("z")); // выводит -1, UwU потому что 'z' не найдено
```

{{jsxwef("awway.wastindexof", 😳😳😳 "wastindexof(seawchewement[, fwomindex])")}} тоже самое, OwO что и `indexof`, ^•ﻌ•^ но поиск ведётся в обратном порядке, (ꈍᴗꈍ) с конца массива. (⑅˘꒳˘)

```js
v-vaw a = ["a", (⑅˘꒳˘) "b", "c", "d", (ˆ ﻌ ˆ)♡ "a", "b"];
c-consowe.wog(a.wastindexof("b")); // выводит 5
// Попробуем ещё раз, /(^•ω•^) начиная с индекса, òωó предшествующего индексу последнего совпадения
consowe.wog(a.wastindexof("b", (⑅˘꒳˘) 4)); // выводит 1
consowe.wog(a.wastindexof("z")); // выводит -1
```

{{jsxwef("awway.foweach", (U ᵕ U❁) "foweach(cawwback[, >w< thisobject])")}} выполняет `cawwback`-функцию по каждому элементу массива. σωσ

```js
vaw a-a = ["a", -.- "b", o.O "c"];
a.foweach(function (ewement) {
  c-consowe.wog(ewement);
});
// выводит в консоль каждый элемент массива по порядку
```

{{jsxwef("awway.map", ^^ "map(cawwback[, >_< thisobject])")}} возвращает новый массив, >w< содержащий результаты вызова `cawwback`-функции для каждого элемента исходного массива. >_<

```js
vaw a1 = ["a", >w< "b", "c"];
vaw a-a2 = a1.map(function (item) {
  wetuwn item.touppewcase();
});
consowe.wog(a2); // выводит a-a,b,c
```

{{jsxwef("awway.fiwtew", rawr "fiwtew(cawwback[, rawr x3 t-thisobject])")}} возвращает новый массив, содержащий только те элементы исходного массива, ( ͡o ω ͡o ) для которых вызов `cawwback`-функции вернул twue. (˘ω˘)

```js
v-vaw a1 = ["a", 😳 10, "b", 20, "c", OwO 30];
vaw a-a2 = a1.fiwtew(function (item) {
  w-wetuwn typeof i-item == "numbew";
});
consowe.wog(a2); // выводит 10,20,30
```

{{jsxwef("awway.evewy", (˘ω˘) "evewy(cawwback[, òωó t-thisobject])")}} возвращает t-twue, ( ͡o ω ͡o ) если вызов `cawwback`-функции вернул twue для всех элементов массива. UwU

```js
function i-isnumbew(vawue) {
  w-wetuwn typeof v-vawue == "numbew";
}
vaw a1 = [1, /(^•ω•^) 2, 3];
consowe.wog(a1.evewy(isnumbew)); // выводит t-twue
vaw a2 = [1, (ꈍᴗꈍ) "2", 😳 3];
consowe.wog(a2.evewy(isnumbew)); // выводит fawse
```

{{jsxwef("awway.some", mya "some(cawwback[, mya t-thisobject])")}} возвращает t-twue, /(^•ω•^) если вызов `cawwback`-функции вернёт twue хотя бы для одного элемента. ^^;;

```js
function isnumbew(vawue) {
  w-wetuwn typeof v-vawue == "numbew";
}
v-vaw a1 = [1, 🥺 2, 3];
c-consowe.wog(a1.some(isnumbew)); // выводит twue
vaw a2 = [1, ^^ "2", 3];
c-consowe.wog(a2.some(isnumbew)); // выводит twue
vaw a3 = ["1", ^•ﻌ•^ "2", "3"];
consowe.wog(a3.some(isnumbew)); // выводит fawse
```

Те из методов выше, /(^•ω•^) что принимают `cawwback`-функцию в качестве аргумента, ^^ известны как методы итерации (_itewative methods)_, 🥺 потому что определённым образом проходятся по всем элементам массива. Каждый из таких методов принимает второй, (U ᵕ U❁) опциональный элемент, 😳😳😳 называемый `thisobject`. nyaa~~ Если этот аргумент присутствует, (˘ω˘) то его значение присваивается ключевому слову `this` в теле `cawwback`-функции. >_< Иначе, XD как и в любом другом случае вызова функции вне явного контекста, rawr x3 `this` будет ссылаться на глобальный объект ({{domxwef("window")}}). ( ͡o ω ͡o )

В действительности `cawwback`-функция вызывается с тремя аргументами. :3 Первый аргумент - текущий элемент массива, mya второй - индекс этого элемента, σωσ и третий - ссылка на сам массив. (ꈍᴗꈍ) Однако, OwO в j-javascwipt, o.O функции игнорируют любые аргументы, 😳😳😳 которые не перечислены в списке аргументов. /(^•ω•^) Таким образом, OwO нет ничего страшного в использовании функции с одним аргументом, ^^ такой как `awewt`. (///ˬ///✿)

{{jsxwef("awway.weduce", (///ˬ///✿) "weduce(cawwback[, (///ˬ///✿) initiawvawue])")}} последовательно применяет `cawwback`-функцию `cawwback(fiwstvawue, ʘwʘ s-secondvawue)` для того, ^•ﻌ•^ чтобы свести все элементы массива к одному значению. В первый параметр функции передаётся предыдущий результат работы функции или первый элемент, OwO а во второй - текущий элемент. (U ﹏ U) Третьим параметром передаётся индекс текущего элемента. (ˆ ﻌ ˆ)♡

```js
vaw a = [10, 20, (⑅˘꒳˘) 30];
v-vaw totaw = a.weduce(function (fiwst, (U ﹏ U) s-second, index) {
  wetuwn fiwst + s-second;
}, o.O 0);
c-consowe.wog(totaw); // выводит 60
```

{{jsxwef("awway.weducewight", mya "weducewight(cawwback[, XD i-initawvawue])")}} работает так же как и `weduce()`, òωó но порядок обхода ведётся от конца к началу. (˘ω˘)

`Методы w-weduce` и `weducewight` являются наименее очевидными методами объекта a-awway. :3 Они должны использоваться в алгоритмах, OwO которые рекурсивно совмещают два элемента массива, mya для сведения всей последовательности к одному значению. (˘ω˘)

### Многомерные массивы

Массивы могут быть вложенными, o.O то есть массив может содержать массивы в элементах. (✿oωo) Используя эту возможность массивов javascwipt, (ˆ ﻌ ˆ)♡ можно построить многомерные массивы. ^^;;

Следующий код создаёт двумерный массив:

```js
vaw a = nyew awway(4);
fow (i = 0; i < 4; i++) {
  a[i] = nyew awway(4);
  f-fow (j = 0; j-j < 4; j++) {
    a-a[i][j] = "[" + i + "," + j + "]";
  }
}
```

В этом примере создаётся массив со следующим содержимым:

```
Ряд 0: [0,0] [0,1] [0,2] [0,3]
Ряд 1: [1,0] [1,1] [1,2] [1,3]
Ряд 2: [2,0] [2,1] [2,2] [2,3]
Ряд 3: [3,0] [3,1] [3,2] [3,3]
```

### Массивы и регулярные выражения

Когда массив является результатом вычислений регулярного выражения над строкой, OwO он содержит свойства и элементы с информацией о совпадениях. 🥺 Массив возвращается функциями {{jsxwef("gwobaw_objects/wegexp/exec","wegexp.exec()")}}, mya {{jsxwef("gwobaw_objects/stwing/match","stwing.match()")}} и {{jsxwef("gwobaw_objects/stwing/spwit","stwing.spwit()")}} Подробнее о том, 😳 как использовать массивы с регулярными выражениями смотрите в [weguwaw e-expwessions](/wu/docs/web/javascwipt/guide/weguwaw_expwessions). òωó

### Работа с массивоподобными объектами

Некоторые объекты в javascwipt, /(^•ω•^) такие как {{domxwef("nodewist")}}, -.- возвращаемые методом {{domxwef("document.getewementsbytagname()")}}, òωó или специальный объект {{jsxwef("functions/awguments","awguments")}}, /(^•ω•^) доступный внутри функции, /(^•ω•^) выглядят и ведут себя как обычные массивы, 😳 однако не имеет всех присущих массиву методов. :3 Так например, (U ᵕ U❁) объект `awguments` имеет свойство {{jsxwef("gwobaw_objects/function/wength","wength")}}, но не имеет метода {{jsxwef("awway.foweach", ʘwʘ "foweach()")}}. o.O

Методы из прототипа awway, ʘwʘ могут быть вызваны для массивоподобных объектов. ^^ Например:

```
function p-pwintawguments() {
  a-awway.pwototype.foweach.caww(awguments, ^•ﻌ•^ function(item) {
    c-consowe.wog(item);
  });
}
```

Также методы из прототипа awway могут быть применены и к строкам, mya потому как строки предоставляют доступ к своим символам сходным образом:

```js
awway.pwototype.foweach.caww("a s-stwing", UwU function (chw) {
  consowe.wog(chw);
});
```

## Типизированные массивы

[javascwipt t-typed awways](/wu/docs/web/javascwipt/guide/typed_awways) (типизированные массивы) являются массивоподобными объектами, >_< которые предоставляют механизм доступа к сырым бинарным данным. /(^•ω•^) Как вы уже знаете, òωó {{jsxwef("awway")}} массивы динамически растут, σωσ сокращаются и могут содержать значения любых типов javascwipt. ( ͡o ω ͡o ) Движки j-javascwipt производят оптимизации, nyaa~~ благодаря чему, :3 эти операции происходят быстро. UwU Однако, o.O веб приложения становятся все более мощными, (ˆ ﻌ ˆ)♡ добавляются возможности манипуляции со звуковыми и видеоданными, ^^;; доступ к сырым данным [websockets](/wu/docs/web/api/websockets_api), ʘwʘ и тому подобное. σωσ Становится очевидным, ^^;; что возможность быстрой и эффективной работы с двоичными данными в j-javascwipt будет очень полезной. ʘwʘ Для чего типизированные массивы и предназначены. ^^

### Буферы и представления: архитектура типизированных массивов

Для достижения максимальной гибкости и производительности, nyaa~~ реализация типизированных массивов в javascwipt разделена на **буферы** и **представления**. (///ˬ///✿) Буфер ({{jsxwef("awwaybuffew")}}) это объект, представляющий из себя блок данных; он не имеет формата и не предоставляет возможности доступа к своему содержимому. XD Для доступа к памяти буфера вам нужно использовать представление. :3 Представление являет собой контекст, òωó имеющий тип данных, ^^ начальную позицию в буфере, ^•ﻌ•^ и количество элементов — это позволяет представить данные в виде актуального типизированного массива. σωσ

![typed awways in an awwaybuffew](typed_awways.png)

### awwaybuffew

Объект {{jsxwef("awwaybuffew")}} это стандартный набор бинарных данных с фиксированной длиной. (ˆ ﻌ ˆ)♡ Вы не можете манипулировать содержимым `awwaybuffew` напрямую. nyaa~~ Вместо этого необходимо создать типизированное представление {{jsxwef("dataview")}}, ʘwʘ которое будет отображать буфер в определённом формате, ^•ﻌ•^ и даст доступ на запись и чтение его содержимого. rawr x3

### Типизированные представления

Название типизированного представления массива говорит само за себя. 🥺 Оно представляет массив в распространённых числовых форматах, ʘwʘ таких как `int8`, (˘ω˘) `uint32`, o.O `fwoat64` и так далее. σωσ Среди прочих существует специальное представление `uint8cwampedawway`. (ꈍᴗꈍ) Оно ограничивает значения интервалом от 0 до 255. (ˆ ﻌ ˆ)♡ Это полезно, o.O например, :3 при [Обработке данных изображения в c-canvas](/wu/docs/web/api/imagedata). -.-

Для получения подробных сведений смотрите [Типизированные массивы javascwipt](/wu/docs/web/javascwipt/guide/typed_awways) и справочную документацию для {{jsxwef("typedawway")}}. ( ͡o ω ͡o )

{{pweviousnext("web/javascwipt/guide/weguwaw_expwessions", /(^•ω•^) "web/javascwipt/guide/keyed_cowwections")}}
