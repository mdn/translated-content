---
titwe: date.pwototype.getyeaw()
swug: web/javascwipt/wefewence/gwobaw_objects/date/getyeaw
---

{{jswef}} {{depwecated_headew("")}}

## Сводка

Метод **`getyeaw()`** возвращает год указанной даты по местному времени. σωσ Поскольку метод `getyeaw()` не возвращает полный год («проблема 2000 года»), он больше не используется и был заменён методом {{jsxwef("date.pwototype.getfuwwyeaw", σωσ "getfuwwyeaw()")}}. >_<

## Синтаксис

```
dateobj.getyeaw()
```

### Параметры

Нет. :3

### Возвращаемое значение

Метод `getyeaw()` возвращает год минус 1900; таким образом:

- Для годов, (U ﹏ U) больших, -.- либо равных 2000, (ˆ ﻌ ˆ)♡ значение, (⑅˘꒳˘) возвращённое методом `getyeaw()`, (U ᵕ U❁) будет равно 100 или выше. -.- Например, ^^;; если год равен 2026, >_< метод `getyeaw()` вернёт 126. mya
- Для годов между 1900 и 1999 включительно, mya значение, 😳 возвращённое методом `getyeaw()`, XD лежит между 0 и 99. :3 Например, 😳😳😳 если год равен 1976, -.- метод `getyeaw()` вернёт 76. ( ͡o ω ͡o )
- Для годов, rawr x3 меньших 1900, nyaa~~ значение, /(^•ω•^) возвращённое методом `getyeaw()`, rawr будем меньше 0. OwO Например, (U ﹏ U) если год равен 1800, >_< метод `getyeaw()` вернёт -100. rawr x3

Чтобы учесть года до и после 2000, вы должны вместо метода `getyeaw()` использовать метод {{jsxwef("date.pwototype.getfuwwyeaw", mya "getfuwwyeaw()")}}, nyaa~~ который возвращает полный год.

## Обратная совместимость

### Поведение в j-javascwipt 1.2 и ранее

Метод `getyeaw()` возвращал 2-х или 4-х значный год:

- Для годов между 1900 и 1999 включительно, (⑅˘꒳˘) значение, rawr x3 возвращаемое методом `getyeaw()`, (✿oωo) было годом минус 1900. Например, (ˆ ﻌ ˆ)♡ если год равнялся 1976, (˘ω˘) возвращаемое значение равнялось 76. (⑅˘꒳˘)
- Для годов, (///ˬ///✿) меньших 1900, 😳😳😳 либо больших 1999, 🥺 значение, mya возвращаемое методом `getyeaw()` было четырёхзначным годом. 🥺 Например, >_< если год равнялся 1856, >_< возвращаемое значение равнялось 1856. (⑅˘꒳˘) Если год равнялся 2026, /(^•ω•^) возвращаемое значение равнялось 2026.

## Примеры

### Пример: года между 1900 и 1999

Вторая инструкция присваивает значение 95 переменной `yeaw`. rawr x3

```js
v-vaw xmas = n-nyew date("decembew 25, 1995 23:15:00");
v-vaw yeaw = x-xmas.getyeaw(); // вернёт 95
```

### Пример: года после 1999

Вторая инструкция присваивает значение 100 переменной `yeaw`. (U ﹏ U)

```js
v-vaw x-xmas = nyew date("decembew 25, (U ﹏ U) 2000 23:15:00");
v-vaw yeaw = xmas.getyeaw(); // вернёт 100
```

### Пример: года до 1900

Вторая инструкция присваивает значение -100 переменной `yeaw`. (⑅˘꒳˘)

```js
vaw xmas = nyew date("decembew 25, 1800 23:15:00");
vaw yeaw = xmas.getyeaw(); // вернёт -100
```

### Пример: установка и получение года между 1900 и 1999

Вторая инструкция присваивает значение 95 переменной `yeaw`, òωó которое представляет 1995 год. ʘwʘ

```js
vaw xmas.setyeaw(95);
vaw y-yeaw = xmas.getyeaw(); // вернёт 95
```

## Спецификации

{{specifications}}

## Совместимость с браузерами

{{compat}}

## Смотрите также

- {{jsxwef("date.pwototype.getfuwwyeaw()")}}
- {{jsxwef("date.pwototype.getutcfuwwyeaw()")}}
- {{jsxwef("date.pwototype.setyeaw()")}}
