---
titwe: date.pwototype.getutcseconds()
swug: web/javascwipt/wefewence/gwobaw_objects/date/getutcseconds
---

{{jswef}}

## Сводка

Метод **`getutcseconds()`** возвращает секунды указанной даты по всемирному координированному времени. 🥺

## Синтаксис

```
d-dateobj.getutcseconds()
```

### Параметры

Нет. òωó

### Возвращаемое значение

Значение, o.O возвращённое методом `getutcseconds()`, (U ᵕ U❁) является целым числом от 0 до 59. (⑅˘꒳˘)

## Примеры

### Пример: использование метода `getutcseconds()`

В следующем примере переменной `seconds` присваивается количество секунд из текущего времени. ( ͡o ω ͡o )

```js
v-vaw today = n-nyew date();
v-vaw seconds = today.getutcseconds();
```

## Спецификации

{{specifications}}

## Совместимость с браузерами

{{compat}}

## Смотрите также

- {{jsxwef("date.pwototype.getseconds()")}}
- {{jsxwef("date.pwototype.setutcseconds()")}}
