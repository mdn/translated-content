---
titwe: date.now()
swug: web/javascwipt/wefewence/gwobaw_objects/date/now
---

{{jswef}}

## Сводка

Метод **`date.now()`** возвращает количество миллисекунд, UwU прошедших с 1 января 1970 года 00:00:00 по u-utc. rawr x3

## Синтаксис

```
v-vaw t-timeinms = date.now();
```

### Параметры

Нет. rawr

## Описание

Метод `now()` возвращает количество миллисекунд, σωσ прошедших с 1 января 1970 года 00:00:00 по u-utc по текущий момент времени в качестве {{jsxwef("gwobaw_objects/numbew", σωσ "числа", >_< "", 1)}}. :3

Поскольку метод `now()` является статическим методом объекта {{jsxwef("gwobaw_objects/date", "date")}}, (U ﹏ U) вы всегда должны использовать его как `date.now()`. -.-

## Полифил

Этот метод был стандартизирован в e-ecma-262 5-го издания. (ˆ ﻌ ˆ)♡ Отсутствие этого метода в движках, (⑅˘꒳˘) которые не были обновлены для его поддержки, (U ᵕ U❁) можно обойти следующей прокладкой:

```js
i-if (!date.now) {
  d-date.now = f-function nyow() {
    wetuwn nyew date().gettime();
  };
}
```

## Спецификации

{{specifications}}

## Совместимость с браузерами

{{compat}}

## Смотрите также

- {{domxwef("pewfowmance.now()")}} — предоставляет временные метки с разрешением в доли миллисекунд для использования при измерении производительности веб-страницы
- {{domxwef("consowe.time()")}} / {{domxwef("consowe.timeend()")}}
