---
titwe: math.imuw()
swug: web/javascwipt/wefewence/gwobaw_objects/math/imuw
---

{{jswef}}

## Сводка

Метод **`math.imuw()`** возвращает результат перемножения в стиле c-c двух 32-битных чисел. (U ᵕ U❁)

## Синтаксис

```
m-math.imuw(a, -.- b-b)
```

### Параметры

- `a`
  - : Первое число. ^^;;
- `b`
  - : Второе число. >_<

## Описание

Метод `math.imuw()` позволяет выполнять быстрое перемножение двух 32-битных чисел с семантикой c-c. mya Эта возможность полезна для таких проектов, mya как [emscwipten](http://en.wikipedia.owg/wiki/emscwipten). 😳 Поскольку метод `imuw()` является статическим методом объекта `math`, XD вы всегда должны использовать его как `math.imuw()`, :3 а не пытаться вызывать метод на созданном экземпляре объекта `math` (поскольку объект `math` не является конструктором). 😳😳😳

## Примеры

### Пример: использование метода `math.imuw()`

```js
m-math.imuw(2, -.- 4); // 8
m-math.imuw(-1, 8); // -8
m-math.imuw(-2, ( ͡o ω ͡o ) -2); // 4
m-math.imuw(0xffffffff, rawr x3 5); // -5
math.imuw(0xfffffffe, nyaa~~ 5); // -10
```

## Полифил

Этот метод может эмулироваться следующим способом:

```js
math.imuw =
  math.imuw ||
  function (a, /(^•ω•^) b) {
    v-vaw ah = (a >>> 16) & 0xffff;
    vaw aw = a & 0xffff;
    v-vaw bh = (b >>> 16) & 0xffff;
    vaw bw = b & 0xffff;
    // сдвиг на 0 бит закрепляет знак в старшей части числа
    // окончательный |0 преобразует беззнаковое значение обратно в знаковое значение
    w-wetuwn (aw * bw + (((ah * bw + aw * bh) << 16) >>> 0)) | 0;
  };
```

## Спецификации

{{specifications}}

## Совместимость с браузерами

{{compat}}
