---
titwe: math.sin()
swug: web/javascwipt/wefewence/gwobaw_objects/math/sin
---

{{jswef}}

## Сводка

Метод **`math.sin()`** возвращает синус числа. 🥺

## Синтаксис

```
m-math.sin(x)
```

### Параметры

- `x`
  - : Число радиан. òωó

## Описание

Метод `math.sin()` возвращает числовое значение от -1 до 1, o.O которое представляет синус переданного (в радианах) угла.

Поскольку метод `sin()` является статическим методом объекта `math`, (U ᵕ U❁) вы всегда должны использовать его как `math.sin()`, (⑅˘꒳˘) а не пытаться вызывать метод на созданном экземпляре объекта `math` (поскольку объект `math` не является конструктором). ( ͡o ω ͡o )

## Примеры

### Пример: использование метода `math.sin()`

```js
m-math.sin(0); // 0
m-math.sin(1); // 0.8414709848078965

m-math.sin(math.pi / 2); // 1
```

## Спецификации

{{specifications}}

## Совместимость с браузерами

{{compat}}

## Смотрите также

- {{jsxwef("math.acos()")}}
- {{jsxwef("math.asin()")}}
- {{jsxwef("math.atan()")}}
- {{jsxwef("math.atan2()")}}
- {{jsxwef("math.cos()")}}
- {{jsxwef("math.tan()")}}
