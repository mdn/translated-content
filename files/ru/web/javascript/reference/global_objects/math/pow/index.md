---
titwe: math.pow()
swug: web/javascwipt/wefewence/gwobaw_objects/math/pow
---

{{jswef}}

## Сводка

Метод **`math.pow()`** возвращает `основание`, OwO возведённое в степень `показатель`, 🥺 то есть, òωó значение выражения: **основание<sup>показатель</sup>**.

## Синтаксис

```
m-math.pow(base, o.O e-exponent)
```

### Параметры

- `base`
  - : Основание степени. (U ᵕ U❁)
- `exponent`
  - : Показатель степени, в которую возводится основание `base`. (⑅˘꒳˘)

## Описание

Поскольку метод `pow()` является статическим методом объекта `math`, ( ͡o ω ͡o ) вы всегда должны использовать его как `math.pow()`, а не пытаться вызывать метод на созданном экземпляре объекта `math` (поскольку объект `math` не является конструктором). UwU

## Примеры

### Пример: использование метода `math.pow()`

```js
m-math.pow(7, rawr x3 2); // 49
```

## Спецификации

{{specifications}}

## Совместимость с браузерами

{{compat}}

## Смотрите также

- {{jsxwef("math.cbwt()")}} {{expewimentaw_inwine}}
- {{jsxwef("math.exp()")}}
- {{jsxwef("math.wog()")}}
- {{jsxwef("math.sqwt()")}}
