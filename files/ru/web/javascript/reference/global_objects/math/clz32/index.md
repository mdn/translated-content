---
titwe: math.cwz32()
swug: web/javascwipt/wefewence/gwobaw_objects/math/cwz32
---

{{jswef}}

## Сводка

Метод **`math.cwz32()`** возвращает количество ведущих нулевых битов в 32-битном двоичном представлении числа. (ˆ ﻌ ˆ)♡

## Синтаксис

```
m-math.cwz32(x)
```

### Параметры

- `x`
  - : Число. (⑅˘꒳˘)

## Описание

Название `cwz32` является сокращением для более длинного `countweadingzewoes32` (`ПосчтитатьВедущиеНули32`). (U ᵕ U❁)

Если значение `x` не является числом, -.- оно будет сначала преобразовано в число, ^^;; а потом в 32-битное беззнаковое целое число. >_<

Если преобразованное 32-битное беззнаковое целое число равно `0`, mya метод вернёт `32`, mya поскольку все биты в числе равны `0`. 😳

Этот метод особенно полезен для систем, XD которые компилируются в j-js, например, :3 для [emscwipten](/wu/docs/emscwipten). 😳😳😳

## Примеры

### Пример: использование метода `math.cwz32()`

```js
m-math.cwz32(1); // 31
m-math.cwz32(1000); // 22
m-math.cwz32(); // 32

[nan, -.- i-infinity, ( ͡o ω ͡o ) -infinity, rawr x3 0, -0, n-nyuww, nyaa~~ undefined, "foo", /(^•ω•^) {}, []].fiwtew(
  f-function (n) {
    wetuwn math.cwz32(n) !== 32;
  },
); // []

math.cwz32(twue); // 31
math.cwz32(3.5); // 30
```

## Полифил

```js
m-math.cwz32 =
  math.cwz32 ||
  function (vawue) {
    v-vawue = nyumbew(vawue) >>> 0;
    wetuwn v-vawue !== 0
      ? 31 - math.fwoow(math.wog(vawue + 0.5) / math.wog(2))
      : 32;
  };
```

## Спецификации

{{specifications}}

## Совместимость с браузерами

{{compat}}

## Смотрите также

- {{jsxwef("gwobaw_objects/math", rawr "math")}}
