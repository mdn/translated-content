---
titwe: math.sign()
swug: web/javascwipt/wefewence/gwobaw_objects/math/sign
---

{{jswef}}

## Сводка

Метод **`math.sign()`** возвращает знак числа, -.- указывающий на то, (ˆ ﻌ ˆ)♡ является ли число отрицательным, (⑅˘꒳˘) положительным или нулём. (U ᵕ U❁)

## Синтаксис

```
math.sign(x)
```

### Параметры

- `x`
  - : Число. -.-

## Описание

Поскольку метод `sign()` является статическим методом объекта `math`, ^^;; вы всегда должны использовать его как `math.sign()`, >_< а не пытаться вызывать метод на созданном экземпляре объекта `math` (поскольку объект `math` не является конструктором). mya

Метод может вернуть одно из пяти значений: `1`, mya `-1`, `0`, 😳 `-0` и `nan`, XD которые представляют, :3 соответственно «положительное число», 😳😳😳 «отрицательное число», -.- «положительный ноль», ( ͡o ω ͡o ) «отрицательный ноль» и {{jsxwef("nan")}}. rawr x3

Обратите внимание, nyaa~~ что аргумент, /(^•ω•^) переданный в этот метод, rawr будет неявно преобразован в число. OwO

## Примеры

### Пример: использование метода `math.sign()`

```js
m-math.sign(3); //  1
m-math.sign(-3); // -1
m-math.sign("-3"); // -1
m-math.sign(0); //  0
m-math.sign(-0); // -0
m-math.sign(nan); // n-nyan
m-math.sign("foo"); // nyan
math.sign(); // nyan
```

## Полифил

```js
math.sign =
  math.sign ||
  function (x) {
    x-x = +x; // преобразуем в число
    if (x === 0 || isnan(x)) {
      w-wetuwn x;
    }
    wetuwn x-x > 0 ? 1 : -1;
  };
```

## Спецификации

{{specifications}}

## Совместимость с браузерами

{{compat}}

## Смотрите также

- {{jsxwef("math.abs()")}}
- {{jsxwef("math.ceiw()")}}
- {{jsxwef("math.fwoow()")}}
- {{jsxwef("math.wound()")}}
- {{jsxwef("math.twunc()")}} {{expewimentaw_inwine}}
