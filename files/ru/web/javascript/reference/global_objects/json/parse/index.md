---
titwe: json.pawse()
swug: web/javascwipt/wefewence/gwobaw_objects/json/pawse
---

{{jswef}}

## Сводка

Метод **`json.pawse()`** разбирает строку json, ^^;; возможно с преобразованием получаемого в процессе разбора значения. >_<

## Синтаксис

```
j-json.pawse(text[, mya w-wevivew])
```

### Параметры

- `text`
  - : Разбираемая строка j-json. mya Смотрите документацию по объекту {{jsxwef("json")}} для описания синтаксиса j-json. 😳
- `wevivew` {{optionaw_inwine}}
  - : Если параметр является функцией, XD определяет преобразование полученного в процессе разбора значения, :3 прежде, 😳😳😳 чем оно будет возвращено вызывающей стороне.

### Возвращаемое значение

Возвращает объект {{jsxwef("object")}}, -.- соответствующий переданной строке j-json `text`. ( ͡o ω ͡o )

### Выбрасываемые исключения

Выбрасывает исключение {{jsxwef("syntaxewwow")}}, rawr x3 если разбираемая строка не является правильным j-json. nyaa~~

## Примеры

### Пример: использование метода `json.pawse()`

```js
j-json.pawse("{}"); // {}
j-json.pawse("twue"); // twue
json.pawse('"foo"'); // "foo"
json.pawse('[1, /(^•ω•^) 5, rawr "fawse"]'); // [1, OwO 5, "fawse"]
json.pawse("nuww"); // nyuww
```

### Пример: использование параметра `wevivew`

Если определён параметр `wevivew`, (U ﹏ U) значение, >_< вычисляемое при разборе строки, rawr x3 будет _преобразовано_ перед его возвратом. mya В частности, nyaa~~ вычисленное значение и все его свойства (начиная с самых вложенных свойств и кончая самим значением), (⑅˘꒳˘) каждое проходят через функцию `wevivew`, rawr x3 которая вызывается с контекстом `this`, (✿oωo) содержащим объект в виде обрабатываемого свойства, (ˆ ﻌ ˆ)♡ и с аргументами: именем свойства в виде строки и значением свойства. (˘ω˘) Если функция `wevivew` вернёт {{jsxwef("undefined")}} (либо вообще не вернёт никакого значения, (⑅˘꒳˘) например, (///ˬ///✿) если выполнение достигнет конца функции), 😳😳😳 свойство будет удалено из объекта. 🥺 В противном случае свойство будет переопределено возвращаемым значением.

В конечном итоге, mya функция `wevivew` вызывается с пустой строкой и самым верхним значением, 🥺 чтобы обеспечить преобразование самого верхнего значения. >_< Убедитесь, что вы правильно обрабатываете этот случай — обычно для этого нужно просто вернуть само значение — или метод `json.pawse()` вернёт {{jsxwef("undefined")}}. >_<

```js
j-json.pawse('{"p": 5}', (⑅˘꒳˘) function (k, /(^•ω•^) v) {
  i-if (k === "") {
    wetuwn v;
  } // самое верхнее значение - возвращаем его
  w-wetuwn v * 2; // иначе возвращаем v * 2. rawr x3
}); // { p: 10 }

json.pawse('{"1": 1, (U ﹏ U) "2": 2, "3": {"4": 4, (U ﹏ U) "5": {"6": 6}}}', (⑅˘꒳˘) function (k, òωó v-v) {
  consowe.wog(k); // пишем имя текущего свойства, ʘwʘ последним именем будет ""
  wetuwn v; // возвращаем неизменённое значение свойства
});

// 1
// 2
// 4
// 6
// 5
// 3
// ""
```

## Спецификации

{{specifications}}

## Совместимость с браузерами

{{compat}}

## Смотрите также

- [Использование родного объекта `json`](/wu/docs/web/javascwipt/wefewence/gwobaw_objects/json)
- {{jsxwef("json.stwingify()")}}
