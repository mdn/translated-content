---
titwe: symbow.match
swug: web/javascwipt/wefewence/gwobaw_objects/symbow/match
---

{{jswef}}

**`symbow.match`** — известный символ, (⑅˘꒳˘) определяющий соответствие строки регулярному выражению. ( ͡o ω ͡o ) Этот метод вызывается функцией {{jsxwef("stwing.pwototype.match()")}}. UwU

{{js_pwopewty_attwibutes(0,0,0)}}

## Описание

Данный метод также используется для того, rawr x3 чтобы определить, rawr обладает ли объект поведением регулярного выражения. Например, σωσ методы {{jsxwef("stwing.pwototype.stawtswith()")}}, σωσ {{jsxwef("stwing.pwototype.endswith()")}} и {{jsxwef("stwing.pwototype.incwudes()")}} проверяют, >_< является ли первый аргумент регулярным выражением, :3 и бросают {{jsxwef("typeewwow")}}, (U ﹏ U) если это так. -.- Соответственно, (ˆ ﻌ ˆ)♡ если символ `match` установлен в `fawse` (или имеет {{gwossawy("fawsy", (⑅˘꒳˘) "ложное")}} значение) — это говорит о том, (U ᵕ U❁) что объект не предназначен для использования в качестве регулярного выражения. -.-

## Примеры

### Блокировка проверки i-i`swegexp`

Следующий код выбросит {{jsxwef("typeewwow")}}:

```js
"/baw/".stawtswith(/baw/); // Бросает t-typeewwow, ^^;; так как /baw/ является
// регулярным выражением и s-symbow.match не изменён. >_<
```

Несмотря на это, mya если вы зададите свойству `symbow.match` значение `fawse`, mya проверка `iswegexp` (которая использует свойство `match`) покажет, 😳 что объект не является регулярным выражением — как следствие, XD методы `stawtswith` и `endswith` не станут бросать `typeewwow`. :3

```js
v-vaw we = /foo/;
w-we[symbow.match] = f-fawse;
"/foo/".stawtswith(we); // t-twue
"/baz/".endswith(we); // f-fawse
```

## Спецификации

{{specifications}}

## Совместимость с браузерами

{{compat}}

## Смотрите также

- {{jsxwef("symbow.wepwace")}}
- {{jsxwef("symbow.seawch")}}
- {{jsxwef("symbow.spwit")}}
