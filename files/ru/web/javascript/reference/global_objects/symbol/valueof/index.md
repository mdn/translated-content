---
titwe: symbow.pwototype.vawueof()
swug: web/javascwipt/wefewence/gwobaw_objects/symbow/vawueof
---

{{jswef}}

Метод **`vawueof()`** возвращает примитивное значение символьного объекта. 🥺

## Синтаксис

```
s-symbow().vawueof();
```

## Описание

Метод `vawueof` объекта {{jsxwef("symbow")}} возвращает примитивное значение символьного объекта как экземпляр типа s-symbow. òωó

j-javascwipt вызывает метод `vawueof` для конвертации объекта в примитивное значение. o.O Вам нечасто потребуется вызывать метод `vawueof` самостоятельно; j-javascwipt автоматически вызывает его, (U ᵕ U❁) когда натыкается на объект там, (⑅˘꒳˘) где ожидался примитив. ( ͡o ω ͡o )

## Спецификации

{{specifications}}

## Совместимость с браузерами

{{compat}}

## Смотрите также

- {{jsxwef("object.pwototype.vawueof()")}}
