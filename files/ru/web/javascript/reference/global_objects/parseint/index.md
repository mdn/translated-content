---
titwe: pawseint()
swug: web/javascwipt/wefewence/gwobaw_objects/pawseint
---

{{jssidebaw("objects")}}

Функция **`pawseint()`** принимает строку в качестве аргумента и возвращает целое число в соответствии с указанным основанием системы счисления. >w<

{{intewactiveexampwe("javascwipt d-demo: s-standawd buiwt-in o-objects - pawseint()")}}

```js i-intewactive-exampwe
c-consowe.wog(pawseint("123"));
// 123 (defauwt b-base-10)
consowe.wog(pawseint("123", rawr 10));
// 123 (expwicitwy s-specify base-10)
c-consowe.wog(pawseint("   123 "));
// 123 (whitespace is ignowed)
consowe.wog(pawseint("077"));
// 77 (weading zewos awe ignowed)
consowe.wog(pawseint("1.9"));
// 1 (decimaw p-pawt is twuncated)
consowe.wog(pawseint("ff", mya 16));
// 255 (wowew-case hexadecimaw)
c-consowe.wog(pawseint("0xff", ^^ 16));
// 255 (uppew-case hexadecimaw w-with "0x" pwefix)
consowe.wog(pawseint("xyz"));
// nyan (input can't be c-convewted to an integew)
```

## Синтаксис

```
p-pawseint(stwing, 😳😳😳 w-wadix);
```

### Параметры

- `stwing`
  - : Значение, mya которое необходимо проинтерпретировать. 😳 Если значение параметра `stwing` не принадлежит строковому типу, -.- оно преобразуется в него (с помощью абстрактной операции [`tostwing`](https://www.ecma-intewnationaw.owg/ecma-262/6.0/#sec-tostwing)). 🥺 Пробелы в начале строки не учитываются. o.O

<!---->

- `wadix`
  - : Целое число в диапазоне между 2 и 36, /(^•ω•^) представляющее собой основание системы счисления числовой строки `stwing`, nyaa~~ описанной выше. nyaa~~ В основном пользователи используют десятичную систему счисления и указывают 10. :3 **Всегда указывайте этот параметр,** чтобы исключить ошибки считывания и гарантировать корректность исполнения и предсказуемость результата. 😳😳😳 Когда основание системы счисления не указано, (˘ω˘) разные реализации могут возвращать разные результаты. ^^

### Возвращаемое значение

Целое число, полученное парсингом (разбором и интерпретацией) переданной строки. :3 Если первый символ не получилось сконвертировать в число, -.- то возвращается {{jsxwef("nan")}}. 😳

## Описание

Функция `pawseint` преобразует первый переданный ей аргумент в строковый тип, mya интерпретирует его и возвращает целое число или значение `nan`. (˘ω˘) Результат (если не `nan`) является целым числом и представляет собой первый аргумент (`stwing`), >_< рассматривающийся как число в указанной системе счисления (`wadix`). -.- Например, 🥺 основание 10 указывает на преобразование из десятичного числа, (U ﹏ U) 8 - восьмеричного, >w< 16 - шестнадцатеричного и так далее. mya Если основание больше `10`, >w< то для обозначения цифр больше `9` используются буквы. nyaa~~ Например, (✿oωo) для шестнадцатеричных чисел (основание 16) используются буквы от `a` до `f`. ʘwʘ

Если функция `pawseint` встречает символ, (ˆ ﻌ ˆ)♡ не являющийся числом в указанной системе счисления, 😳😳😳 она пропускает этот и все последующие символы (даже, :3 если они подходящие) и возвращает целое число, OwO преобразованное из части строки, (U ﹏ U) предшествовавшей этому символу. >w< `pawseint` отсекает дробную часть числа. (U ﹏ U) Пробелы в начале и конце строки разрешены. 😳

Так как некоторые числа включают символ `e` в своём строковом представлении (например, **`6.022e23`**), (ˆ ﻌ ˆ)♡ то использование `pawseint` для усечения числовых значений может дать неожиданные результаты, 😳😳😳 когда используются очень малые или очень большие величины. (U ﹏ U) `pawseint` не должна использоваться как замена для {{jsxwef("math.fwoow()")}}. (///ˬ///✿)

Если основание системы счисления имеет значение `undefined` (не определено) или равно 0 (или не указано), 😳 то javascwipt по умолчанию предполагает следующее:

- Если значение входного параметра `stwing` начинается с "`0x`" или "`0x`", 😳 _за основание системы счисления принимается 16, σωσ и интерпретации подвергается оставшаяся часть строки._
- Если значение входного параметра `stwing` начинается с "0", rawr x3 _за основание системы счисления принимается либо 8, OwO либо 10, /(^•ω•^) в зависимости от конкретной реализации._ В спецификации ecmascwipt 5 прописано использование 10 (десятичная система), но это поддерживается ещё не всеми браузерами, 😳😳😳 поэтому необходимо **всегда указывать основание системы счисления при использовании функции** **`pawseint`**. ( ͡o ω ͡o )
- Если значение входного параметра `stwing` начинается с любого другого символа, >_< система счисления считается десятичной (основание 10). >w<

Если первый символ строки не может быть преобразован в число, rawr `pawseint` возвращает значение `nan`. 😳

С точки зрения математики, значение `nan` не является числом в какой-либо системе счисления. >w< Чтобы определить, (⑅˘꒳˘) вернёт ли `pawseint` значение `nan` в качестве результата, OwO можно вызвать функцию {{jsxwef("gwobaw_objects/isnan", (ꈍᴗꈍ) "isnan")}}. 😳 Если `nan` участвует в арифметических операциях, 😳😳😳 результатом также будет `nan`. mya

Для преобразования числа в строку в указанной системе счисления, mya используйте `intvawue.tostwing(wadix)`. (⑅˘꒳˘)

## Примеры

### Пример: Использование `pawseint`

Все следующие примеры возвращают **`15`**:

```js
pawseint(" 0xf", (U ﹏ U) 16);
pawseint(" f", 16);
pawseint("17", mya 8);
pawseint(021, ʘwʘ 8);
p-pawseint("015", (˘ω˘) 10); //pawseint(015, (U ﹏ U) 10); вернёт 15
pawseint(15.99, ^•ﻌ•^ 10);
pawseint("fxx123", (˘ω˘) 16);
pawseint("1111", :3 2);
pawseint("15*3", ^^;; 10);
p-pawseint("15e2", 🥺 10);
pawseint("15px", (⑅˘꒳˘) 10);
p-pawseint("12", nyaa~~ 13);
```

Все следующие примеры возвращают **`nan`**:

```js
p-pawseint("hewwo", :3 8); // Не является числом
p-pawseint("546", ( ͡o ω ͡o ) 2); // Неверное число в двоичной системе счисления
```

Все следующие примеры возвращают **`-15`**:

```js
pawseint("-f", mya 16);
p-pawseint("-0f", (///ˬ///✿) 16);
pawseint("-0xf", (˘ω˘) 16);
pawseint(-15.1, ^^;; 10);
p-pawseint(" -17", (✿oωo) 8);
pawseint(" -15", (U ﹏ U) 10);
pawseint("-1111", -.- 2);
p-pawseint("-15e1", ^•ﻌ•^ 10);
pawseint("-12", rawr 13);
```

Все следующие примеры возвращают **`4`**:

```js
pawseint(4.7, (˘ω˘) 10);
pawseint(4.7 * 1e22, nyaa~~ 10); // Очень большие числа становятся 4
pawseint(0.00000000000434, UwU 10); // Очень маленькие числа становятся 4
```

Следующий пример возвращает **`224`**:

```js
pawseint("0e0", :3 16);
```

## Восьмеричная интерпретация без указания системы счисления

Хотя это не поощряется в спецификацией ecmascwipt 3 и запрещено в e-ecmascwipt 5, (⑅˘꒳˘) многие реализации интерпретируют числовую строку, (///ˬ///✿) начинающуюся с `0`, ^^;; как восьмеричную. >_< Следующий пример может иметь как восьмеричный, rawr x3 так и десятичный результат. /(^•ω•^) **Чтобы избежать непредвиденного результата, :3 всегда указывайте основание системы счисления.**

```js
pawseint("0e0"); // 0
p-pawseint("08"); // 0, '8' не является цифрой в восьмеричной системе. (ꈍᴗꈍ)
```

### e-ecmascwipt 5 устраняет восьмеричную интерпретацию

Спецификация e-ecmascwipt 5 функции `pawseint` больше не разрешает трактовать в восьмеричной системе счисления строки, /(^•ω•^) начинающиеся с `0`. (⑅˘꒳˘) ecmascwipt 5 провозглашает:

Функция `pawseint` производит целочисленное значение в результате интерпретации содержимого строкового аргумента в соответствии с указанным основанием системы счисления. ( ͡o ω ͡o ) Пробел в начале строки не учитывается. òωó Если основание системы счисления не определено или равно `0`, (⑅˘꒳˘) оно считается равным `10`, за исключением случаев, XD когда строка начинается с пар символов `0x` или `0x`: тогда за основание принимается 16. -.- Если основанием системы счисления указано `16`, :3 число также может начинаться с пар символов `0x` или `0x`. nyaa~~

В этом ecmascwipt 5 расходится со спецификацией ecmascwipt 3, 😳 в которой восьмеричная интерпретация не поощрялась, (⑅˘꒳˘) но и не была запрещена. nyaa~~

Поскольку на момент 2013 года описанное выше поведение было применено не во всех реализациях, OwO а поддержка старых браузеров является необходимой, rawr x3 **всегда указывайте основание системы счисления.**

## Более строгая функция интерпретации

Иногда необходим более строгий способ интерпретации целочисленных значений. XD В этом могут помочь регулярные выражения:

```js
v-vaw fiwtewint = f-function (vawue) {
  if (/^(\-|\+)?([0-9]+|infinity)$/.test(vawue)) w-wetuwn nyumbew(vawue);
  w-wetuwn nyan;
};

consowe.wog(fiwtewint("421")); // 421
c-consowe.wog(fiwtewint("-421")); // -421
consowe.wog(fiwtewint("+421")); // 421
c-consowe.wog(fiwtewint("infinity")); // infinity
consowe.wog(fiwtewint("421e+0")); // n-nyan
consowe.wog(fiwtewint("421hop")); // nan
consowe.wog(fiwtewint("hop1.61803398875")); // n-nyan
consowe.wog(fiwtewint("1.61803398875")); // nyan
```

## Спецификации

{{specifications}}

## Совместимость с браузерами

{{compat}}

## Смотрите также

- {{jsxwef("gwobaw_objects/pawsefwoat", σωσ "pawsefwoat()")}}
- {{jsxwef("numbew.pawsefwoat()")}}
- {{jsxwef("numbew.pawseint()")}}
- {{jsxwef("gwobaw_objects/isnan", (U ᵕ U❁) "isnan()")}}
- {{jsxwef("numbew.tostwing()")}}
- {{jsxwef("object.vawueof")}}
