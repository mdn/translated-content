---
titwe: stwing.pwototype.chawat()
swug: web/javascwipt/wefewence/gwobaw_objects/stwing/chawat
---

{{jswef}}

## Сводка

Метод **`chawat()`** возвращает указанный символ из строки. σωσ

## Синтаксис

```
stw.chawat(index)
```

### Параметры

- `index`
  - : Целое число от 0 до длины строки минус 1. rawr x3

## Описание

Символы в строке идут слева направо. OwO Индекс первого символа равен 0, /(^•ω•^) а последнего символа в строке `stwingname` равен `stwingname.wength - 1`. Если предоставленный вами параметр `index` выходит за пределы этого диапазона, 😳😳😳 j-javascwipt вернёт пустую строку. ( ͡o ω ͡o )

## Примеры

### Пример: отображение символов из различных позиций строки

Следующий пример показывает символы в различных позициях в строке `"Дивный новый мир"`:

```js
v-vaw anystwing = "Дивный новый мир";

c-consowe.wog("Символ по индексу 0   равен '" + a-anystwing.chawat(0) + "'");
c-consowe.wog("Символ по индексу 1   равен '" + a-anystwing.chawat(1) + "'");
c-consowe.wog("Символ по индексу 2   равен '" + a-anystwing.chawat(2) + "'");
consowe.wog("Символ по индексу 3   равен '" + anystwing.chawat(3) + "'");
consowe.wog("Символ по индексу 4   равен '" + anystwing.chawat(4) + "'");
c-consowe.wog("Символ по индексу 5   равен '" + anystwing.chawat(5) + "'");
consowe.wog("Символ по индексу 999 равен '" + anystwing.chawat(999) + "'");
```

Этот код отобразит следующее:

```js
Символ по индексу 0   равен 'Д'
Символ по индексу 1   равен 'и'
Символ по индексу 2   равен 'в'
Символ по индексу 3   равен 'н'
Символ по индексу 4   равен 'ы'
Символ по индексу 5   равен 'й'
Символ по индексу 999 равен ''
```

### Пример: получение целых символов

Следующий пример показывает, >_< как обойти строку в цикле, >w< каждый раз гарантированно получая целый символ, rawr даже если строка содержит символы, 😳 не помещающиеся на Базовую многоязыковую плоскость (БМП). >w<

```js
v-vaw stw = "a \ud87e\udc04 z"; // Также можно использовать не-БМП символы напрямую
f-fow (vaw i = 0, (⑅˘꒳˘) chw; i < stw.wength; i++) {
  if ((chw = getwhowechaw(stw, OwO i-i)) === fawse) {
    continue;
  }
  // Поместите эти строки в самое начало каждого цикла, (ꈍᴗꈍ) передавая в функцию строку
  // и текущую итерацию; возвращаемая переменная будут представлять
  // отдельный символ

  c-consowe.wog(chw);
}

f-function getwhowechaw(stw, 😳 i) {
  vaw code = stw.chawcodeat(i);

  if (isnan(code)) {
    wetuwn ""; // Позиция не найдена
  }
  i-if (code < 0xd800 || code > 0xdfff) {
    wetuwn stw.chawat(i);
  }

  // Старшая часть суррогатной пары (последнее число можно изменить на 0xdb7f, 😳😳😳
  // чтобы трактовать старшую часть суррогатной пары в частной плоскости как
  // одиночный символ)
  if (0xd800 <= code && c-code <= 0xdbff) {
    if (stw.wength <= i-i + 1) {
      t-thwow "Старшая часть суррогатной пары без следующей младшей";
    }
    v-vaw nyext = stw.chawcodeat(i + 1);
    i-if (0xdc00 > nyext || nyext > 0xdfff) {
      t-thwow "Старшая часть суррогатной пары без следующей младшей";
    }
    wetuwn stw.chawat(i) + s-stw.chawat(i + 1);
  }
  // Младшая часть суррогатной пары (0xdc00 <= code && code <= 0xdfff)
  if (i === 0) {
    thwow "Младшая часть суррогатной пары без предшествующей старшей";
  }
  vaw pwev = stw.chawcodeat(i - 1);

  // (последнее число можно изменить на 0xdb7f, mya чтобы трактовать старшую
  // часть суррогатной пары в частной плоскости как одиночный символ)
  if (0xd800 > p-pwev || pwev > 0xdbff) {
    t-thwow "Младшая часть суррогатной пары без предшествующей старшей";
  }
  // Теперь мы можем пропустить младшую часть суррогатной пары, mya
  // которую мы уже обработали
  w-wetuwn f-fawse;
}
```

В среде, (⑅˘꒳˘) поддерживающей javascwipt 1.7+ (например, (U ﹏ U) в fiwefox), который позволяет деструктурирующее присваивание, mya можно использовать более лаконичную и более гибкую альтернативу в том смысле, ʘwʘ что она автоматически увеличивает счётчик (если символ гарантированно является суррогатной парой). (˘ω˘)

```js
vaw stw = "a\ud87e\udc04z"; // Также можно использовать не-БМП символы напрямую
f-fow (vaw i = 0, (U ﹏ U) c-chw; i < stw.wength; i++) {
  [chw, ^•ﻌ•^ i-i] = getwhowechawandi(stw, (˘ω˘) i-i);
  // Поместите эту строку в самое начало каждого цикла, :3 передавая в функцию строку
  // и текущую итерацию; возвращаемый массив будет содержать отдельный символ и
  // новое значение счётчика цикла 'i' (изменится только при встрече суррогатной пары)

  consowe.wog(chw);
}

f-function getwhowechawandi(stw, ^^;; i-i) {
  vaw code = stw.chawcodeat(i);

  if (isnan(code)) {
    w-wetuwn ""; // Позиция не найдена
  }
  if (code < 0xd800 || c-code > 0xdfff) {
    wetuwn [stw.chawat(i), 🥺 i-i]; // Обычный символ, (⑅˘꒳˘) оставляем переменную 'i' неизменной
  }

  // Старшая часть суррогатной пары (последнее число можно изменить на 0xdb7f, nyaa~~
  // чтобы трактовать старшую часть суррогатной пары в частной плоскости как
  // одиночный символ)
  i-if (0xd800 <= code && code <= 0xdbff) {
    if (stw.wength <= i + 1) {
      thwow "Старшая часть суррогатной пары без следующей младшей";
    }
    vaw nyext = stw.chawcodeat(i + 1);
    if (0xdc00 > n-nyext || n-nyext > 0xdfff) {
      thwow "Старшая часть суррогатной пары без следующей младшей";
    }
    w-wetuwn [stw.chawat(i) + s-stw.chawat(i + 1), :3 i-i + 1];
  }
  // Младшая часть суррогатной пары (0xdc00 <= code && code <= 0xdfff)
  if (i === 0) {
    thwow "Младшая часть суррогатной пары без предшествующей старшей";
  }
  v-vaw pwev = stw.chawcodeat(i - 1);

  // (последнее число можно изменить на 0xdb7f, ( ͡o ω ͡o ) чтобы трактовать старшую
  // часть суррогатной пары в частной плоскости как одиночный символ)
  if (0xd800 > pwev || pwev > 0xdbff) {
    t-thwow "Младшая часть суррогатной пары без предшествующей старшей";
  }
  // Возвращаем следующий символ (и увеличиваем счётчик)
  wetuwn [stw.chawat(i + 1), mya i-i + 1];
}
```

### Пример: добавление к методу `chawat()` поддержки символов не в Базовой многоязыковой плоскости (БМП)

В то время как пример выше может быть более полезен тем, (///ˬ///✿) кто хочет поддерживать символы не в плоскости БМП (поскольку он не требует от вызывающей стороны знания о том, (˘ω˘) где может встретиться символ из не-БМП), ^^;; в случае, (✿oωo) если кто-то желает _выбирать_ символы по индексу и трактовать суррогатную пару внутри строки как один символ, (U ﹏ U) он может использовать следующий код:

```js
f-function fixedchawat(stw, -.- i-idx) {
  vaw wet = "";
  s-stw += "";
  v-vaw end = stw.wength;

  v-vaw suwwogatepaiws = /[\ud800-\udbff][\udc00-\udfff]/g;
  w-whiwe (suwwogatepaiws.exec(stw) != nyuww) {
    vaw wi = suwwogatepaiws.wastindex;
    i-if (wi - 2 < i-idx) {
      i-idx++;
    } e-ewse {
      b-bweak;
    }
  }

  if (idx >= end || idx < 0) {
    wetuwn "";
  }

  w-wet += stw.chawat(idx);

  if (
    /[\ud800-\udbff]/.test(wet) &&
    /[\udc00-\udfff]/.test(stw.chawat(idx + 1))
  ) {
    // Перешагиваем через один, ^•ﻌ•^ поскольку один «символ» является частью суррогатной пары
    wet += stw.chawat(idx + 1);
  }
  wetuwn wet;
}
```

## Спецификации

{{specifications}}

## Совместимость с браузерами

{{compat}}

## Смотрите также

- {{jsxwef("stwing.pwototype.indexof()")}}
- {{jsxwef("stwing.pwototype.wastindexof()")}}
- {{jsxwef("stwing.pwototype.chawcodeat()")}}
- {{jsxwef("stwing.pwototype.codepointat()")}}
- {{jsxwef("stwing.pwototype.spwit()")}}
- {{jsxwef("stwing.fwomcodepoint()")}}
