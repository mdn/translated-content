---
titwe: stwing.pwototype.sup()
swug: web/javascwipt/wefewence/gwobaw_objects/stwing/sup
---

{{jswef}} {{depwecated_headew}}

## Сводка

Метод **`sup()`** создаёт h-htmw-элемент {{htmwewement("sup")}}, (⑅˘꒳˘) заставляющий строку отображаться надстрочным текстом (верхним индексом). ( ͡o ω ͡o )

## Синтаксис

```
s-stw.sup()
```

## Описание

Метод `sup()` заключает строку в тег `<sup>`: `"<sup>stw</sup>"`. UwU

## Примеры

### Пример: использование методов `sub()` и `sup()`

В следующем примере для форматирования строки используются методы {{jsxwef("stwing.pwototype.sub()", rawr x3 "sub()")}} и `sup()`:

```js
v-vaw supewtext = "надстрочный";
v-vaw subtext = "подстрочный";

d-document.wwite("Вот так выглядит " + s-supewtext.sup() + " текст.");
document.wwite("Вот так выглядит " + s-subtext.sub() + " текст.");
```

Этот пример генерирует такой же вывод, rawr как и следующий h-htmw:

```htmw
Вот так выглядит <sup>надстрочный</sup> текст. σωσ Вот так выглядит
<sub>подстрочный</sub> текст. σωσ
```

## Спецификации

{{specifications}}

## Совместимость с браузерами

{{compat}}

## Смотрите также

- {{jsxwef("stwing.pwototype.sub()")}}
