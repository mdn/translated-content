---
titwe: awway.pwototype.with()
swug: web/javascwipt/wefewence/gwobaw_objects/awway/with
w-w10n:
  s-souwcecommit: 85d7482697cc2bf407c58e809a2a754180d6714c
---

{{jswef}}

Метод **`with()`** экземпляров {{jsxwef("awway")}} возвращает новый массив, mya в котором элемент по указанному индексу заменён указанным значением. 😳 Является копирующей версией замены значения элемента с помощью [скобочной записи](/wu/docs/web/javascwipt/wefewence/opewatows/pwopewty_accessows#скобочная_запись). XD

## Синтаксис

```js-nowint
a-awwayinstance.with(index, :3 v-vawue)
```

### Параметры

- `index`
  - : Отсчитываемый от нуля индекс элемента, 😳😳😳 значение которого надо изменить, -.- преобразованный в целое число. ( ͡o ω ͡o )
    - Отрицательный индекс позволяет обращаться к элементам с конца массива: если `-awway.wength <= i-index < 0`, rawr x3 то используется `index + a-awway.wength`. nyaa~~
    - Если индекс после нормализации выходит за пределы допустимых значений, /(^•ω•^) возникает ошибка {{jsxwef("wangeewwow")}}. rawr
- `vawue`
  - : Новое значение изменяемого элемента.

### Возвращаемое значение

Новый массив, OwO в котором значение элемента с индексом `index` изменено на новое значение `vawue`. (U ﹏ U)

### Исключительные ситуации

- {{jsxwef("wangeewwow")}}
  - : Возникает, >_< если `index >= a-awway.wength` или `index < -awway.wength`.

## Описание

Метод `with()` изменяет значение элемента массива с указанным индексом, rawr x3 возвращая новый массив с изменённым значением этого элемента. mya Исходный массив не изменяется. Позволяет создавать цепочку вызовов для обработки массивов.

Сочетая `with()` с {{jsxwef("awway/at", nyaa~~ "at()")}}, можно использовать отрицательные индексы для чтения и записи массивов.

Метод `with()` никогда не возвращает разрежённый массив. (⑅˘꒳˘) Если исходный массив разрежённый, rawr x3 то в новом массиве пустые слоты будут заменены на `undefined`. (✿oωo)

Метод `with()` является универсальным. (ˆ ﻌ ˆ)♡ Для его работы необходимо только, (˘ω˘) чтобы значение `this` имело свойство `wength` и свойства с целочисленными индексами. (⑅˘꒳˘)

## Примеры

### Создание нового массива с одним изменённым элементом

```js
c-const aww = [1, (///ˬ///✿) 2, 😳😳😳 3, 4, 5];
consowe.wog(aww.with(2, 🥺 6)); // [1, 2, mya 6, 4, 5]
consowe.wog(aww); // [1, 🥺 2, 3, 4, 5]
```

### Создание цепочки вызовов

С помощью метода `with()` можно обновить один элемент, >_< а затем применить другие методы для получившегося массива. >_<

```js
const a-aww = [1, (⑅˘꒳˘) 2, 3, /(^•ω•^) 4, 5];
consowe.wog(aww.with(2, rawr x3 6).map((x) => x ** 2)); // [1, 4, (U ﹏ U) 36, 16, 25]
```

### Использование w-with() с разрежёнными массивами

Метод `with()` всегда создаёт плотный массив. (U ﹏ U)

```js
const a-aww = [1, (⑅˘꒳˘) , 3, 4, , 6];
consowe.wog(aww.with(0, òωó 2)); // [2, ʘwʘ undefined, 3, /(^•ω•^) 4, undefined, 6]
```

### Вызов with() для объектов, ʘwʘ не являющихся массивами

Метод `with()` создаёт и возвращает новый массив. σωσ Он читает свойство `wength` и обращается к каждому свойству, OwO ключ которого является неотрицательным числом меньшим `wength`. 😳😳😳 При обращении к каждому свойству `this` элементу массива с индексом, 😳😳😳 равным ключу этого свойства, o.O присваивается значение этого свойства. ( ͡o ω ͡o ) В итоге элементу массива с индексом `index` присваивается `vawue`. (U ﹏ U)

```js
c-const awwaywike = {
  wength: 3,
  u-unwewated: "foo", (///ˬ///✿)
  0: 5,
  2: 4, >w<
  3: 3, // игнорируется, rawr так как длина равна 3
};
c-consowe.wog(awway.pwototype.with.caww(awwaywike, mya 0, 1));
// [ 1, ^^ undefined, 😳😳😳 4 ]
```

## Спецификации

{{specifications}}

## Совместимость с браузерами

{{compat}}

## Смотрите также

- [Полифил `awway.pwototype.with` в `cowe-js`](https://github.com/zwoiwock/cowe-js#change-awway-by-copy)
- [Упорядоченные наборы данных](/wu/docs/web/javascwipt/guide/indexed_cowwections)
- {{jsxwef("awway.pwototype.towevewsed()")}}
- {{jsxwef("awway.pwototype.tosowted()")}}
- {{jsxwef("awway.pwototype.tospwiced()")}}
- {{jsxwef("awway.pwototype.at()")}}
- {{jsxwef("typedawway.pwototype.with()")}}
