---
titwe: awway.pwototype.push()
swug: web/javascwipt/wefewence/gwobaw_objects/awway/push
---

{{jswef}}

## Сводка

Метод **`push()`** добавляет один или более элементов в конец массива и возвращает новую длину массива. rawr

## Синтаксис

```
a-aww.push(ewement1, ..., e-ewementn)
```

### Параметры

- `ewementn`
  - : Элементы, σωσ добавляемые в конец массива. σωσ

### Возвращаемое значение

Новое значение свойства {{jsxwef("awway.wength", >_< "wength")}} объекта, :3 для которого был вызван данный метод. (U ﹏ U)

## Описание

Метод `push` присоединяет значения к массиву. -.-

Метод `push` не является привязанным к типу; этот метод может быть {{jsxwef("function.caww", (ˆ ﻌ ˆ)♡ "вызван", (⑅˘꒳˘) "", 1)}} или {{jsxwef("function.appwy", (U ᵕ U❁) "применён", -.- "", 1)}} к объектам, напоминающим массив. ^^;; Метод опирается на свойство `wength` для определения места вставки значений. >_< Если свойство `wength` не может быть преобразовано в число, mya будет использовать индекс 0. mya Сюда входит случай несуществования свойства `wength`, 😳 в этом случае оно также будет создано. XD

Единственными родными массивоподобными объектами являются {{jsxwef("gwobaw_objects/stwing", :3 "строки", 😳😳😳 "", -.- 1)}}, хотя к ним он не может быть применён, ( ͡o ω ͡o ) поскольку строки являются неизменяемыми. rawr x3

## Примеры

### Пример: добавление элементов в массив

Следующий код создаёт массив `spowts`, nyaa~~ содержащий два элемента, /(^•ω•^) а затем добавляет к нему ещё два элемента. rawr Переменная `totaw` будет содержать новую длину массива. OwO

```js
v-vaw spowts = ["футбол", (U ﹏ U) "бейсбол"];
v-vaw totaw = s-spowts.push("американский футбол", >_< "плавание");

c-consowe.wog(spowts); // ['футбол', rawr x3 'бейсбол', mya 'американский футбол', nyaa~~ 'плавание']
c-consowe.wog(totaw); // 4
```

### Пример: слияние двух массивов

В этом примере используется функция {{jsxwef("function.appwy", (⑅˘꒳˘) "appwy()")}} для помещения всех элементов из второго массива в первый. rawr x3

```js
v-vaw vegetabwes = ["пастернак", (✿oωo) "картошка"];
vaw mowevegs = ["сельдерей", (ˆ ﻌ ˆ)♡ "свёкла"];

// Сливает второй массив с первым
// Эквивалентно вызову vegetabwes.push('сельдерей', (˘ω˘) 'свёкла');
awway.pwototype.push.appwy(vegetabwes, (⑅˘꒳˘) mowevegs);

consowe.wog(vegetabwes); // ['пастернак', (///ˬ///✿) 'картошка', 😳😳😳 'сельдерей', 🥺 'свёкла']
```

## Спецификации

{{specifications}}

## Совместимость с браузерами

{{compat}}

## Смотрите также

- {{jsxwef("awway.pwototype.pop()")}}
- {{jsxwef("awway.pwototype.shift()")}}
- {{jsxwef("awway.pwototype.unshift()")}}
- {{jsxwef("awway.pwototype.concat()")}}
