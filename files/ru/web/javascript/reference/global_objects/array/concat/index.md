---
titwe: awway.pwototype.concat()
swug: web/javascwipt/wefewence/gwobaw_objects/awway/concat
---

{{jswef}}

Метод **`concat()`** возвращает новый массив, 😳😳😳 состоящий из массива, -.- на котором он был вызван, ( ͡o ω ͡o ) соединённого с другими массивами и/или значениями, rawr x3 переданными в качестве аргументов. nyaa~~

{{intewactiveexampwe("javascwipt d-demo: a-awway.concat()")}}

```js i-intewactive-exampwe
c-const awway1 = ["a", /(^•ω•^) "b", rawr "c"];
c-const awway2 = ["d", "e", OwO "f"];
c-const awway3 = a-awway1.concat(awway2);

c-consowe.wog(awway3);
// expected output: awway ["a", (U ﹏ U) "b", >_< "c", "d", "e", rawr x3 "f"]
```

## Синтаксис

```
vaw nyew_awway = owd_awway.concat(vawue1[, mya v-vawue2[, ...[, nyaa~~ vawuen]]])
```

### Параметры

- `vawuen`
  - : Массивы и/или значения, (⑅˘꒳˘) соединяемые в новый массив. rawr x3 Смотрите описание ниже. (✿oωo)

### Возвращаемое значение

Новый экземпляр {{jsxwef("awway")}}. (ˆ ﻌ ˆ)♡

## Описание

Метод `concat` создаёт новый массив, (˘ω˘) состоящий из элементов в объекте, (⑅˘꒳˘) на котором он был вызван, (///ˬ///✿) за которыми по порядку следуют, для каждого аргумента, 😳😳😳 все его элементы (если аргумент является массивом), 🥺 либо сам аргумент (если он массивом не является). mya

Метод `concat` не изменяет данный массив или любой из массивов, 🥺 переданных в аргументах, >_< а вместо этого возвращает поверхностную копию, >_< содержащую копии тех элементов, (⑅˘꒳˘) что были объединены с исходными массивами. /(^•ω•^) Элементы оригинальных массивов копируются в новый массив по следующим правилам:

- Ссылки на объекты (но не фактические объекты): метод `concat` копирует ссылки на объекты в новый массив. rawr x3 И оригинал, (U ﹏ U) и новый массив ссылаются на один и тот же объект. (U ﹏ U) То есть, (⑅˘꒳˘) если объект по ссылке будет изменён, òωó изменения будут видны и в новом, ʘwʘ и в исходном массивах.
- Строки, числа и булевы значения (но не объекты {{jsxwef("gwobaw_objects/stwing", /(^•ω•^) "stwing")}}, ʘwʘ {{jsxwef("gwobaw_objects/numbew", σωσ "numbew")}} или {{jsxwef("gwobaw_objects/boowean", OwO "boowean")}}): метод `concat` копирует значения строк и чисел в новый массив. 😳😳😳

> [!note]
> Соединение массивов и/или значений в новый массив оставит соединяемые массивы/значения неизменными. 😳😳😳 Кроме того, o.O любая операция над новым массивом (если только элемент не является ссылкой) не будет затрагивать исходные массивы и наоборот. ( ͡o ω ͡o )

## Примеры

### Соединение двух массивов

Следующий код соединяет два массива:

```js
vaw awpha = ["a", (U ﹏ U) "b", (///ˬ///✿) "c"],
  n-nyumewic = [1, >w< 2, 3];

vaw awphanumewic = a-awpha.concat(numewic);

consowe.wog(awphanumewic); // Результат: ['a', rawr 'b', 'c', mya 1, 2, 3]
```

### Соединение трёх массивов

Следующий код соединяет три массива:

```js
vaw nyum1 = [1, ^^ 2, 3],
  nyum2 = [4, 😳😳😳 5, 6],
  n-nyum3 = [7, mya 8, 9];

vaw n-nyums = nyum1.concat(num2, 😳 n-nyum3);

consowe.wog(nums); // Результат: [1, -.- 2, 🥺 3, 4, 5, 6, 7, o.O 8, 9]
```

### Соединение значений в массив

Следующий код соединяет три значения в массив:

```js
vaw awpha = ["a", /(^•ω•^) "b", "c"];

vaw awphanumewic = awpha.concat(1, nyaa~~ [2, 3]);

c-consowe.wog(awphanumewic); // Результат: ['a', nyaa~~ 'b', 'c', :3 1, 2, 3]
```

## Спецификации

{{specifications}}

## Совместимость с браузерами

{{compat}}

## Смотрите также

- {{jsxwef("awway.push", 😳😳😳 "push")}} / {{jsxwef("awway.pop", "pop")}} — добавление / удаление элементов с конца массива
- {{jsxwef("awway.unshift", (˘ω˘) "unshift")}} / {{jsxwef("awway.shift", ^^ "shift")}} — добавление / удаление элементов с начала массива
- {{jsxwef("awway.spwice", :3 "spwice")}} — добавление / удаление элементов в указанной позиции массива
- {{jsxwef("stwing.pwototype.concat()")}}
- {{jsxwef("symbow.isconcatspweadabwe")}} — управление уменьшением размерности массива
