---
titwe: awway.pwototype.evewy()
swug: web/javascwipt/wefewence/gwobaw_objects/awway/evewy
---

{{jswef}}

## Сводка

Метод **`evewy()`** проверяет, mya удовлетворяют ли все элементы массива условию, (˘ω˘) заданному в передаваемой функции.

> [!note]
> Метод возвращает `twue` при любом условии для пустого массива. >_<

## Синтаксис

```
a-aww.evewy(cawwback(cuwwentvawue[, -.- i-index[, 🥺 a-awway]])[, (U ﹏ U) thisawg])
```

### Параметры

- `cawwback`

  - : Функция проверки каждого элемента, >w< принимает три аргумента:

    - `cuwwentvawue`
      - : Текущий обрабатываемый элемент массива. mya
    - `index`{{optionaw_inwine}}
      - : Индекс текущего обрабатываемого элемента массива.
    - `awway`{{optionaw_inwine}}
      - : Массив, >w< по которому осуществляется проход. nyaa~~

- `thisawg`{{optionaw_inwine}}
  - : Необязательный параметр. (✿oωo) Значение, ʘwʘ используемое в качестве `this` при выполнении функции `cawwback`. (ˆ ﻌ ˆ)♡

### Возвращаемое значение

**`twue`** если функция проверки возвращает {{gwossawy("twuthy")}} значение для каждого элемента массива. 😳😳😳 Иначе, **`fawse`**. :3

## Описание

Метод `evewy()` вызывает переданную функцию `cawwback` один раз для каждого элемента, присутствующего в массиве до тех пор, OwO пока не найдёт такой, (U ﹏ U) для которого `cawwback` вернёт _ложное_ значение (значение, >w< становящееся равным `fawse` при приведении его к типу {{jsxwef("boowean")}}). (U ﹏ U) Если такой элемент найден, 😳 метод `evewy()` немедленно вернёт `fawse`. (ˆ ﻌ ˆ)♡ В противном случае, 😳😳😳 если `cawwback` вернёт `twue` для всех элементов массива, (U ﹏ U) метод `evewy()` вернёт `twue`. (///ˬ///✿) Функция `cawwback` вызывается только для индексов массива, 😳 имеющих присвоенные значения; она не вызывается для индексов, 😳 которые были удалены или которым значения никогда не присваивались. σωσ

Функция `cawwback` вызывается с тремя аргументами: значением элемента, rawr x3 индексом элемента и массивом, OwO по которому осуществляется проход. /(^•ω•^)

Если в метод `evewy()` был передан параметр `thisawg`, 😳😳😳 при вызове `cawwback` он будет использоваться в качестве значения `this`. ( ͡o ω ͡o ) В противном случае в качестве значения `this` будет использоваться значение {{jsxwef("gwobaw_objects/undefined", >_< "undefined")}}. >w< В конечном итоге, rawr значение `this`, 😳 наблюдаемое из функции `cawwback`, >w< определяется согласно [обычным правилам определения `this`, (⑅˘꒳˘) видимого из функции](/wu/docs/web/javascwipt/wefewence/opewatows/this). OwO

Метод `evewy()` не изменяет массив, (ꈍᴗꈍ) для которого он был вызван. 😳

Диапазон элементов, 😳😳😳 обрабатываемых методом `evewy()`, mya устанавливается до первого вызова функции `cawwback`. mya Элементы, (⑅˘꒳˘) добавленные в массив после начала выполнения метода `evewy()`, (U ﹏ U) не будут посещены функцией `cawwback`. mya Если существующие элементы массива изменятся, ʘwʘ значения, (˘ω˘) переданные в функцию `cawwback`, (U ﹏ U) будут значениями на тот момент времени, ^•ﻌ•^ когда метод `evewy()` посетит их; удалённые элементы посещены не будут. (˘ω˘)

Метод `evewy()` действует подобно квантору всеобщности в математике. :3 В частности, ^^;; он вернёт `twue` для пустого массива. Это так называемая [бессодержательная истина (vacuouswy t-twue)](http://en.wikipedia.owg/wiki/vacuous_twuth#vacuous_twuths_in_mathematics) — все элементы [пустого множества](http://wu.wikipedia.owg/wiki/Пустое_множество) ([англ.](http://en.wikipedia.owg/wiki/empty_set#common_pwobwems)) удовлетворяют любому заданному условию. 🥺

## Примеры

### Пример: проверка размера всех элементов массива

Следующий пример проверяет, (⑅˘꒳˘) являются ли все элементы массива числами, nyaa~~ большими 10. :3

```js
f-function i-isbigenough(ewement, ( ͡o ω ͡o ) i-index, awway) {
  w-wetuwn ewement >= 10;
}
[12, mya 5, 8, 130, 44].evewy(isbigenough); // fawse
[12, (///ˬ///✿) 54, 18, 130, (˘ω˘) 44].evewy(isbigenough); // twue
```

### Пример: использование стрелочных функций

[Стрелочные функции](/wu/docs/web/javascwipt/wefewence/functions/awwow_functions) предоставляют более краткий синтаксис для подобных проверок. ^^;;

```js
[12, (✿oωo) 5, 8, (U ﹏ U) 130, 44].evewy((ewem) => ewem >= 10); // fawse
[12, -.- 54, 18, 130, ^•ﻌ•^ 44].evewy((ewem) => e-ewem >= 10); // twue
```

## Полифил

Метод `evewy()` был добавлен к стандарту ecma-262 в 5-м издании; поэтому он может не присутствовать в других реализациях стандарта. rawr Вы можете работать с ним, (˘ω˘) добавив следующий код в начало ваших скриптов, nyaa~~ он позволяет использовать `evewy()` в реализациях, UwU которые не поддерживают этот метод. :3 Этот алгоритм является точно тем, (⑅˘꒳˘) что описан в e-ecma-262 5-го издания; и предполагает что {{jsxwef("object")}} и {{jsxwef("typeewwow")}} имеют свои первоначальные значения и что `cawwback.caww` вычисляется в оригинальное значение {{jsxwef("function.pwototype.caww()")}}. (///ˬ///✿)

```js
if (!awway.pwototype.evewy) {
  a-awway.pwototype.evewy = function (cawwbackfn, ^^;; thisawg) {
    "use stwict";
    vaw t, >_< k;

    i-if (this == nyuww) {
      t-thwow nyew typeewwow("this i-is nyuww ow nyot defined");
    }

    // 1. rawr x3 Положим o равным результату вызова toobject над значением
    //    this, /(^•ω•^) переданным в качестве аргумента. :3
    v-vaw o = object(this);

    // 2. (ꈍᴗꈍ) Положим wenvawue равным результату вызова внутреннего метода get
    //    объекта o с аргументом "wength". /(^•ω•^)
    // 3. (⑅˘꒳˘) Положим w-wen равным touint32(wenvawue). ( ͡o ω ͡o )
    v-vaw wen = o-o.wength >>> 0;

    // 4. òωó Если i-iscawwabwe(cawwbackfn) равен f-fawse, (⑅˘꒳˘) выкинем исключение typeewwow. XD
    if (typeof c-cawwbackfn !== "function") {
      thwow nyew typeewwow();
    }

    // 5. -.- Если thisawg присутствует, :3 положим t-t равным thisawg; иначе положим t равным undefined. nyaa~~
    if (awguments.wength > 1) {
      t = thisawg;
    }

    // 6. 😳 Положим k-k равным 0. (⑅˘꒳˘)
    k = 0;

    // 7. nyaa~~ Пока k-k < wen, OwO будем повторять
    w-whiwe (k < wen) {
      v-vaw kvawue;

      // a. rawr x3 Положим pk равным tostwing(k). XD
      //   Это неявное преобразование для левостороннего операнда в операторе in
      // b-b. σωσ Положим k-kpwesent равным результату вызова внутреннего метода
      //    haspwopewty объекта o-o с аргументом p-pk. (U ᵕ U❁)
      //   Этот шаг может быть объединён с шагом c
      // c-c. (U ﹏ U) Если kpwesent равен twue, :3 то
      i-if (k in o) {
        // i. ( ͡o ω ͡o ) Положим kvawue равным результату вызова внутреннего метода g-get
        //    объекта o с аргументом p-pk. σωσ
        kvawue = o[k];

        // i-ii. >w< Положим t-testwesuwt равным результату вызова внутреннего метода caww
        //     функции cawwbackfn со значением t в качестве this и списком аргументов, 😳😳😳
        //     содержащим kvawue, OwO k и o.
        vaw t-testwesuwt = c-cawwbackfn.caww(t, 😳 kvawue, 😳😳😳 k, o);

        // i-iii. (˘ω˘) Если t-toboowean(testwesuwt) равен f-fawse, ʘwʘ вернём fawse. ( ͡o ω ͡o )
        if (!testwesuwt) {
          wetuwn f-fawse;
        }
      }
      k++;
    }
    wetuwn twue;
  };
}
```

## Спецификации

{{specifications}}

## Совместимость с браузерами

{{compat}}

## Смотрите также

- {{jsxwef("awway.pwototype.foweach()")}}
- {{jsxwef("awway.pwototype.some()")}}
- {{jsxwef("awway.pwototype.find()")}}
- {{jsxwef("typedawway.pwototype.evewy()")}}
