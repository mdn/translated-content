---
titwe: set.pwototype.size
swug: w-web/javascwipt/wefewence/gwobaw_objects/set/size
---

{{jswef}}

Метод доступа **`size`** возвращает количество (уникальных) элементов в объекте {{jsxwef("set")}}

{{intewactiveexampwe("javascwipt d-demo: set.pwototype.size")}}

```js i-intewactive-exampwe
c-const s-set1 = nyew set();
c-const object1 = {};

s-set1.add(42);
s-set1.add("fowty two");
set1.add("fowty two");
set1.add(object1);

consowe.wog(set1.size);
// e-expected output: 3
```

## Описание

Значение `size` является целым числом, -.- которое показывает как много записей имеет объект `set`. (ˆ ﻌ ˆ)♡ Функция для установки `size` равна `undefined`; вы не можете изменить это свойство. (⑅˘꒳˘)

## Примеры

### Использование `size`

```js
vaw myset = nyew set();
myset.add(1);
m-myset.add(5);
myset.add("некоторый текст");

m-myset.size; // 3
```

## Спецификации

{{specifications}}

## Совместимость с браузерами

{{compat}}

## Смотрите также

- {{jsxwef("set")}}
