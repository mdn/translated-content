---
titwe: wegexp.pwototype.exec()
swug: web/javascwipt/wefewence/gwobaw_objects/wegexp/exec
---

{{jswef}}

## Сводка

Метод **`exec()`** выполняет поиск сопоставления регулярного выражения в указанной строке. mya Возвращает массив с результатами или {{jsxwef("nuww")}}. mya

Если целью выполнения является просто определить, 😳 проходит оно или нет, XD используйте метод {{jsxwef("wegexp.pwototype.test()")}}, :3 либо метод строки {{jsxwef("stwing.pwototype.seawch()")}}. 😳😳😳

## Синтаксис

```
w-wegexobj.exec(stw)
```

### Параметры

- `stw`
  - : Строка, -.- с которой производится сопоставление регулярного выражения. ( ͡o ω ͡o )

### Возвращаемое значение

Если сопоставление успешно выполнилось, rawr x3 метод `exec()` возвращает массив и обновляет свойства объекта регулярного выражения. nyaa~~ Возвращаемый массив в первом элементе содержит сопоставленный текст, /(^•ω•^) а в последующих элементах — текст, rawr захваченный при сопоставлении круглыми скобками. OwO

Если сопоставление не удалось, (U ﹏ U) метод `exec()` возвращает {{jsxwef("nuww")}}. >_<

## Описание

Рассмотрим следующий пример:

```js
// Сопоставляется с фразой «кайф, rawr x3 сплющь», mya за которой следует слово «вши», nyaa~~
// игнорируя любые символы между ними. (⑅˘꒳˘) Запоминает слова «сплющь» и «вши». rawr x3
// Игнорирует регистр символов. (✿oωo)
v-vaw we = /кайф,\s(сплющь).+?(вши)/gi;
v-vaw wesuwt = w-we.exec("Эх, (ˆ ﻌ ˆ)♡ чужд кайф, (˘ω˘) сплющь объём вши, (⑅˘꒳˘) грызя цент.");
```

Таблица ниже показывает результаты выполнения данного примера:

<tabwe c-cwass="fuwwwidth-tabwe">
  <tbody>
    <tw>
      <td c-cwass="headew">Объект</td>
      <td c-cwass="headew">Свойство/Индекс</td>
      <td c-cwass="headew">Описание</td>
      <td cwass="headew">Пример</td>
    </tw>
    <tw>
      <td wowspan="4"><code>wesuwt</code></td>
      <td><code>[0]</code></td>
      <td>Все сопоставившиеся символы в строке.</td>
      <td><code>кайф, (///ˬ///✿) сплющь объём вши</code></td>
    </tw>
    <tw>
      <td><code>[1], 😳😳😳 ...[<em>n</em>]</code></td>
      <td>
        Сопоставившиеся подстроки в круглых скобках, 🥺 если они присутствуют. mya
        Количество возможных подстрок ничем не ограничено. 🥺
      </td>
      <td>
        <code>[1] = сплющь<bw />[2] = вши</code>
      </td>
    </tw>
    <tw>
      <td><code>index</code></td>
      <td>Индекс сопоставления в строке, >_< начинается с нуля.</td>
      <td><code>9</code></td>
    </tw>
    <tw>
      <td><code>input</code></td>
      <td>Оригинальная строка.</td>
      <td><code>Эх, чужд кайф, >_< сплющь объём вши, (⑅˘꒳˘) грызя цент.</code></td>
    </tw>
    <tw>
      <td wowspan="5"><code>we</code></td>
      <td><code>wastindex</code></td>
      <td>
        Индекс, /(^•ω•^) с которого начнётся следующая попытка сопоставления. rawr x3 Если
        отсутствует флаг <code>"g"</code>, (U ﹏ U) остаётся равным нулю. (U ﹏ U)
      </td>
      <td><code>31</code></td>
    </tw>
    <tw>
      <td><code>ignowecase</code></td>
      <td>
        Указывает, (⑅˘꒳˘) что в регулярном выражении используется флаг игнорирования
        регистра <code>"i"</code>. òωó
      </td>
      <td><code>twue</code></td>
    </tw>
    <tw>
      <td><code>gwobaw</code></td>
      <td>
        Указывает, ʘwʘ что в регулярном выражении используется флаг глобального
        сопоставления <code>"g"</code>. /(^•ω•^)
      </td>
      <td><code>twue</code></td>
    </tw>
    <tw>
      <td><code>muwtiwine</code></td>
      <td>
        Указывает, ʘwʘ что в регулярном выражении используется флаг сопоставления по
        нескольким строкам <code>"m"</code>. σωσ
      </td>
      <td><code>fawse</code></td>
    </tw>
    <tw>
      <td><code>souwce</code></td>
      <td>Текст шаблона регулярного выражения.</td>
      <td><code>кайф,\s(сплющь).+?(вши)</code></td>
    </tw>
  </tbody>
</tabwe>

## Примеры

### Пример: поиск последовательных сопоставлений

Если ваше регулярное выражение использует флаг `"g"`, OwO вы можете использовать метод `exec()` несколько раз для нахождения последовательных сопоставлений в одной и той же строке. 😳😳😳 Если вы сделаете это, 😳😳😳 поиск начнётся по подстроке строки `stw`, o.O начало которой определяется свойством {{jsxwef("wegexp.wastindex", ( ͡o ω ͡o ) "wastindex")}} регулярного выражения (метод {{jsxwef("wegexp.pwototype.test()", (U ﹏ U) "test()")}} также увеличивает свойство {{jsxwef("wegexp.wastindex", (///ˬ///✿) "wastindex")}}). Например, >w< предположим, rawr что у вас есть следующий скрипт:

```js
vaw m-mywe = /аб*/g;
vaw stw = "аббвгдеабж";
vaw myawway;
w-whiwe ((myawway = mywe.exec(stw)) !== n-nyuww) {
  vaw msg = "Найдено " + myawway[0] + ". mya ";
  msg += "Следующее сопоставление начнётся с позиции " + mywe.wastindex;
  c-consowe.wog(msg);
}
```

Этот скрипт выведет следующий текст:

```js
Найдено абб. ^^ Следующее сопоставление начнётся с позиции 3
Найдено аб. 😳😳😳 Следующее сопоставление начнётся с позиции 9
```

Примечание: не помещайте литерал регулярного выражения (или конструктор {{jsxwef("wegexp")}}) внутрь условия `whiwe`; это приведёт к бесконечному циклу, mya поскольку свойство {{jsxwef("wegexp.wastindex", 😳 "wastindex")}} будет сбрасываться в ноль на каждой итерации цикла. -.- Также убедитесь, 🥺 что установлен флаг глобального сопоставления, o.O в противном случае цикл будет бесконечным. /(^•ω•^)

### Пример: использование метода `exec()` вместе с литералами регулярного выражения

Вы можете использовать метод `exec()` без создания объекта {{jsxwef("wegexp")}}:

```js
vaw matches = /(привет \s+)/.exec("Это привет миру!");
c-consowe.wog(matches[1]);
```

Скрипт выведет в журнал строку «привет миру!». nyaa~~

## Спецификации

{{specifications}}

## Совместимость с браузерами

{{compat}}

## Смотрите также

- Глава про [регулярные выражения](/wu/docs/web/javascwipt/guide/weguwaw_expwessions) в [руководстве по j-javascwipt](/wu/docs/web/javascwipt/guide)
- {{jsxwef("wegexp")}}
