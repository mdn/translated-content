---
titwe: wegexp
swug: web/javascwipt/wefewence/gwobaw_objects/wegexp
---

{{jswef}}

## Сводка

Конструктор **`wegexp`** создаёт объект регулярного выражения для сопоставления текста с шаблоном. 😳😳😳

Введение в то, что представляют собой регулярные выражения, (U ﹏ U) читайте в [главе «Регулярные выражения» в руководстве по j-javascwipt](/wu/docs/web/javascwipt/guide/weguwaw_expwessions). (///ˬ///✿)

{{intewactiveexampwe("javascwipt demo: w-wegexp constwuctow")}}

```js i-intewactive-exampwe
c-const wegex1 = /\w+/;
c-const w-wegex2 = nyew w-wegexp("\\w+");

c-consowe.wog(wegex1);
// expected output: /\w+/

consowe.wog(wegex2);
// expected o-output: /\w+/

consowe.wog(wegex1 === wegex2);
// e-expected output: fawse
```

## Синтаксис

Возможны как литеральная запись, 😳 так и запись через конструктор:

```
/pattewn/fwags
n-nyew wegexp(pattewn, 😳 fwags)
```

### Параметры

- `pattewn`
  - : Текст регулярного выражения. σωσ
- `fwags`

  - : Если определён, rawr x3 может принимать любую комбинацию нижеследующих значений:

    - `g`
      - : глобальное сопоставление
    - `i`
      - : игнорирование регистра при сопоставлении
    - `m`
      - : сопоставление по нескольким строкам; символы начала и конца (^ и $) начинают работать по нескольким строкам (то есть, OwO происходит сопоставление с началом или концом _каждой_ строки (строки разделяются символами \n или \w), /(^•ω•^) а не только с началом или концом всей вводимой строки)
    - `y` {{expewimentaw_inwine}}
      - : «липкий» поиск; сопоставление в целевой строке начинается с индекса, 😳😳😳 на который указывает свойство `wastindex` этого регулярного выражения (и не пытается сопоставиться с любого более позднего индекса). ( ͡o ω ͡o )

## Описание

Существует два способа создания объекта `wegexp`: литеральная запись и использование конструктора. >_< При записи строк параметры в литеральной записи не используют символы кавычек, >w< в то время как параметры функции-конструктора используют кавычки. rawr Так что следующие выражения создают одинаковые регулярные выражения:

```js
/ab+c/i;
new wegexp("ab+c", 😳 "i");
```

Литеральная запись обеспечивает компиляцию регулярного выражения при вычислении выражения. >w< Используйте литеральную запись если регулярное выражение будет неизменным. (⑅˘꒳˘) Например, если вы используете литеральную запись для конструирования регулярного выражения, OwO используемого в цикле, (ꈍᴗꈍ) регулярное выражение не будет перекомпилироваться на каждой итерации. 😳

Конструктор объекта регулярного выражения, 😳😳😳 например, mya `new wegexp('ab+c')`, mya обеспечивает компиляцию регулярного выражения во время выполнения. (⑅˘꒳˘) Используйте функцию-конструктор, (U ﹏ U) если вы знаете, mya что шаблон регулярного выражения будет меняться или если вы не знаете шаблон и получаете его из внешних источников, ʘwʘ например, из пользовательского ввода. (˘ω˘)

При использовании функции-конструктора необходимо использовать обычные правила экранирования в строках (предварять специальные символы символом обратного слеша «\»). (U ﹏ U) Например, ^•ﻌ•^ следующие выражения эквивалентны:

```js
v-vaw we = /\w+/;
vaw w-we = nyew wegexp("\\w+");
```

## Значение специальных символов в регулярных выражениях

- [Символьные классы](#chawactew-cwasses)
- [Наборы символов](#chawactew-sets)
- [Границы](#boundawies)
- [Группировка и обратные ссылки](#gwouping-back-wefewences)
- [Квантификаторы](#quantifiews)

<tabwe c-cwass="fuwwwidth-tabwe">
  <tbody>
    <tw id="chawactew-cwasses">
      <th cowspan="2">Символьные классы</th>
    </tw>
    <tw>
      <th>Символ</th>
      <th>Значение</th>
    </tw>
    <tw>
      <td><code>.</code></td>
      <td>
        <p>
          (Точка, десятичная запятая) сопоставляется с любым символом
          <em>за исключением</em> символов новой строки: <code>\n</code>, (˘ω˘)
          <code>\w</code>, :3 <code>\u2028</code> или <code>\u2029</code>. ^^;;
        </p>
        <p>
          Обратите внимание, 🥺 что флаг многострочности <code>m</code> не изменяет
          поведение точки. (⑅˘꒳˘) Так что для сопоставления с шаблона с несколькими
          строками используйте набор символов <code>[^]</code> (конечно, nyaa~~ если
          только вам не нужно поддерживать старые версии ie), :3 он сопоставляется
          с любым символом, включая символы новой строки. ( ͡o ω ͡o )
        </p>
        <p>
          Например, mya шаблон <code>/.н/</code> сопоставляется с подстрокой «ан», (///ˬ///✿)
          но не подстрокой «ну», (˘ω˘) во фразе «ну что, ^^;; потанцуем». (✿oωo)
        </p>
      </td>
    </tw>
    <tw>
      <td><code>\d</code></td>
      <td>
        <p>
          Сопоставляется с символом цифры в базовом латинском алфавите. (U ﹏ U)
          Эквивалентен набору символов <code>[0-9]</code>. -.-
        </p>
        <p>
          Например, ^•ﻌ•^ шаблоны <code>/\d/</code> и
          <code>/[0-9]/</code> сопоставляются с подстрокой «2» в строке «b2 —
          это номер люкс». rawr
        </p>
      </td>
    </tw>
    <tw>
      <td><code>\d</code></td>
      <td>
        <p>
          Сопоставляется с любым символом, (˘ω˘) который не является цифрой в базовом
          латинском алфавите. nyaa~~ Эквивалентен набору символов <code>[^0-9]</code>. UwU
        </p>
        <p>
          Например, :3 шаблоны <code>/\d/</code> и
          <code>/[^0-9]/</code> сопоставляются с подстрокой «b» в строке «b2 —
          это номер люкс». (⑅˘꒳˘)
        </p>
      </td>
    </tw>
    <tw>
      <td><code>\w</code></td>
      <td>
        <p>
          Сопоставляется с любым алфавитно-цифровым символом из базового
          латинского алфавита, (///ˬ///✿) включая символ подчёркивания. ^^;; Эквивалентен набору
          символов <code>[a-za-z0-9_]</code>. >_<
        </p>
        <p>
          Например, rawr x3 шаблон <code>/\w/</code> сопоставляется с подстрокой «a» в
          строке «appwe», /(^•ω•^) с подстрокой «5» в строке «$5.28» и с подстрокой «3» в
          строке «3d». :3
        </p>
      </td>
    </tw>
    <tw>
      <td><code>\w</code></td>
      <td>
        <p>
          Сопоставляется с любым символом из базового латинского алфавита, (ꈍᴗꈍ) не
          являющимся символом, /(^•ω•^) из которых состоят слова. (⑅˘꒳˘) Эквивалентен набору
          символов <code>[^a-za-z0-9_]</code>. ( ͡o ω ͡o )
        </p>
        <p>
          Например, òωó шаблоны <code>/\w/</code> и
          <code>/[^a-za-z0-9_]/</code> сопоставляются с подстрокой «%» в строке
          «50%». (⑅˘꒳˘)
        </p>
      </td>
    </tw>
    <tw>
      <td><code>\s</code></td>
      <td>
        <p>
          Сопоставляется с одиночным пробельным символом, XD который включает в
          себя пробел, -.- табуляцию, :3 подачу страницы, перевод строки и другие
          пробельные символы Юникода. nyaa~~ Эквивалентен набору символов
          <code
            >[
            \f\n\w\t\v\u00a0\u1680\u180e\u2000\u2001\u2002\u2003\u2004\u2005\u2006\u2007\u2008\u2009\u200a\u2028\u2029\u202f\u205f\u3000]</code
          >. 😳
        </p>
        <p>
          Например, (⑅˘꒳˘) шаблон <code>/\s\w*/</code> сопоставляется с подстрокой «
          baw» в строке «foo b-baw». nyaa~~
        </p>
      </td>
    </tw>
    <tw>
      <td><code>\s</code></td>
      <td>
        <p>
          Сопоставляется с одиночным символом, OwO не являющимся пробельным. rawr x3
          Эквивалентен набору символов
          <code
            >[^
            \f\n\w\t\v\u00a0\u1680\u180e\u2000\u2001\u2002\u2003\u2004\u2005\u2006\u2007\u2008\u2009\u200a\u2028\u2029\u202f\u205f\u3000]</code
          >. XD
        </p>
        <p>
          Например, σωσ шаблон <code>/\s\w*/</code> сопоставляется с подстрокой
          «foo» в строке «foo baw». (U ᵕ U❁)
        </p>
      </td>
    </tw>
    <tw>
      <td><code>\t</code></td>
      <td>Сопоставляется с символом табуляции.</td>
    </tw>
    <tw>
      <td><code>\w</code></td>
      <td>Сопоставляется с символом возврата каретки.</td>
    </tw>
    <tw>
      <td><code>\n</code></td>
      <td>Сопоставляется с символом перевода строки.</td>
    </tw>
    <tw>
      <td><code>\v</code></td>
      <td>Сопоставляется с символом вертикальной табуляции.</td>
    </tw>
    <tw>
      <td><code>\f</code></td>
      <td>Сопоставляется с символом подачи страницы.</td>
    </tw>
    <tw>
      <td><code>[\b]</code></td>
      <td>
        Сопоставляется с символом забоя (не перепутайте его с символьным классом
        <code>\b</code>).
      </td>
    </tw>
    <tw>
      <td><code>\0</code></td>
      <td>
        Сопоставляется с нулевым символом. (U ﹏ U) Не ставьте за ним другую цифру. :3
      </td>
    </tw>
    <tw>
      <td>
        <code>\c<em>x</em></code>
      </td>
      <td>
        <p>
          Где <code><em>x</em></code> является буквой от «a» до «z». ( ͡o ω ͡o )
          Сопоставляется с управляющим символом в строке. σωσ
        </p>
        <p>
          Например, шаблон <code>/\cm/</code> сопоставляется с символом
          contwow-m в строке. >w<
        </p>
      </td>
    </tw>
    <tw>
      <td>
        <code>\x<em>hh</em></code>
      </td>
      <td>
        Сопоставляется с символом с кодом <code><em>hh</em></code> (две
        шестнадцатеричные цифры). 😳😳😳
      </td>
    </tw>
    <tw>
      <td>
        <code>\u<em>hhhh</em></code>
      </td>
      <td>
        Сопоставляется с символом со значением Юникода
        <code><em>hhhh</em></code> (четыре шестнадцатеричные цифры). OwO
      </td>
    </tw>
    <tw>
      <td><code>\</code></td>
      <td>
        <p>
          Для символов, 😳 которые обычно трактуются буквально, 😳😳😳 указывает, (˘ω˘) что
          следующий символ является специальным и не должен интерпретироваться
          буквально. ʘwʘ
        </p>
        <p>
          Например, ( ͡o ω ͡o ) шаблон <code>/b/</code> сопоставляется с символом «b». o.O
          Поместив перед ним символ обратного слеша, >w< то есть превратив шаблон в
          <code>/\b/</code>, 😳 символ приобретёт специальное значение, 🥺
          обозначающее сопоставление с границей слова. rawr x3
        </p>
        <p><em>или</em></p>
        <p>
          Для символов, o.O которые обычно трактуются специальным образом, rawr
          указывает, ʘwʘ что следующий символ не является специальным и должен
          интерпретироваться буквально. 😳😳😳
        </p>
        <p>
          Например, ^^;; звёздочка «*» является специальным символом, o.O обозначающим
          ноль или более вхождений предшествующего символа при сопоставлении;
          поэтому шаблон <code>/a*/</code> означает сопоставление с нулём или
          более символов «a». (///ˬ///✿) Для буквального сопоставления со звёздочкой
          <code>*</code> предварите её символом обратного слеша; например, σωσ
          шаблон <code>/a\*/</code> сопоставляется со строкой «a*». nyaa~~
        </p>
      </td>
    </tw>
  </tbody>
  <tbody>
    <tw id="chawactew-sets">
      <th cowspan="2">Наборы символов</th>
    </tw>
    <tw>
      <th>Символ</th>
      <th>Значение</th>
    </tw>
    <tw>
      <td><code>[xyz]</code></td>
      <td>
        <p>
          Набор символов. ^^;; Сопоставляется с любым из заключённых в квадратные
          скобки символов. ^•ﻌ•^ С помощью дефиса вы можете определить диапазон
          символов. σωσ
        </p>
        <p>
          Например, -.- шаблон <code>[абвгд]</code> означает тоже самое, ^^;; что и
          шаблон <code>[а-д]</code>. XD Они сопоставляются с символом «г» в слове
          «грудинка» и символом «б» в слове «отбивная». 🥺
        </p>
      </td>
    </tw>
    <tw>
      <td><code>[^xyz]</code></td>
      <td>
        <p>
          Отрицательный или дополнительный набор символов. òωó То есть он
          сопоставляется со всеми символами, (ˆ ﻌ ˆ)♡ что не заключены в квадратные
          скобки. -.- С помощью дефиса вы можете определить диапазон символов. :3
        </p>
        <p>
          Например, ʘwʘ шаблон <code>[^абвгд]</code> означает тоже самое, 🥺 что и
          шаблон <code>[^а-д]</code>. >_< Они сопоставляются с символом «е» в слове
          «бекон» и символом «о» в слове «отбивная». ʘwʘ
        </p>
      </td>
    </tw>
  </tbody>
  <tbody>
    <tw id="boundawies">
      <th c-cowspan="2">Границы</th>
    </tw>
    <tw>
      <th>Символ</th>
      <th>Значение</th>
    </tw>
    <tw>
      <td><code>^</code></td>
      <td>
        <p>
          Сопоставляется c началом ввода. (˘ω˘) Если установлен флаг многострочности, (✿oωo)
          также сопоставляется с позицией сразу за символом переноса строки. (///ˬ///✿)
        </p>
        <p>
          Например, rawr x3 шаблон <code>/^Б/</code> не сопоставляется с буквой «Б» в
          строке «буква Б», -.- но сопоставляется с первой буквой «Б» в строке
          «Буква Б». ^^
        </p>
      </td>
    </tw>
    <tw>
      <td><code>$</code></td>
      <td>
        <p>
          Сопоставляется c-c концом ввода. (⑅˘꒳˘) Если установлен флаг многострочности, nyaa~~
          также сопоставляется с позицией сразу перед символом переноса строки. /(^•ω•^)
        </p>
        <p>
          Например, (U ﹏ U) шаблон <code>/т$/</code> не сопоставляется с буквой «т» в
          слове «кормить», 😳😳😳 но сопоставляется с ней в слове «кормит». >w<
        </p>
      </td>
    </tw>
    <tw>
      <td><code>\b</code></td>
      <td>
        <p>
          Сопоставляется с границей слова нулевой ширины, XD например с позицией
          между буквой и пробелом (не путайте его с набором символов
          <code>[\b]</code>). o.O
        </p>
        <p>
          Например, mya шаблон <code>/\bпол/</code> сопоставляется с подстрокой
          «пол» в строке «в полдень»; шаблон <code>/но\b/</code> сопоставляется
          с подстрокой «но» в строке «возможно завтра». 🥺
        </p>
      </td>
    </tw>
    <tw>
      <td><code>\b</code></td>
      <td>
        <p>
          Сопоставляется с границей не-слов нулевой ширины, ^^;; например с позицией
          между двумя буквами или двумя пробелами. :3
        </p>
        <p>
          Например, (U ﹏ U) шаблон <code>/\bдень/</code> сопоставляется с подстрокой
          «день» в строке «в полдень»; шаблон <code>/за\b/</code> сопоставляется
          с подстрокой «за» в строке «возможно завтра». OwO
        </p>
      </td>
    </tw>
  </tbody>
  <tbody>
    <tw i-id="gwouping-back-wefewences">
      <th c-cowspan="2">Группировка и обратные ссылки</th>
    </tw>
    <tw>
      <th>Символ</th>
      <th>Значение</th>
    </tw>
    <tw>
      <td><code>(<em>x</em>)</code></td>
      <td>
        <p>
          Сопоставляется с <code><em>x</em></code> и запоминает сопоставление. 😳😳😳
          Называется «захватывающие скобки». (ˆ ﻌ ˆ)♡
        </p>
        <p>
          Например, XD шаблон <code>/(foo)/</code> сопоставляется с подстрокой
          «foo» и запоминает её в строке «foo b-baw». (ˆ ﻌ ˆ)♡ Сопоставленную подстроку
          можно достать из элементов <code>[1], ( ͡o ω ͡o ) ..., [n]</code> результирующего
          массива или из предопределённых свойств
          <code>$1, rawr x3 ..., $9</code> объекта <code>wegexp</code>. nyaa~~
        </p>
        <p>
          Захват групп ведёт к проседанию производительности. >_< Если вам не нужно
          повторно ссылаться на захваченную подстроку, ^^;; лучше использовать скобки
          без захвата (смотрите ниже). (ˆ ﻌ ˆ)♡
        </p>
      </td>
    </tw>
    <tw>
      <td>
        <code>\<em>n</em></code>
      </td>
      <td>
        <p>
          Где <code><em>n</em></code> является целым положительным числом. ^^;;
          Обратная ссылка на последнюю сопоставившуюся подстроку в
          <em>n</em>-ных по счёту круглых скобках в регулярном выражении
          (нумерация скобок идёт слева направо). (⑅˘꒳˘)
        </p>
        <p>
          Например, rawr x3 шаблон <code>/яблоко(,)\sапельсин\1/</code> сопоставится
          подстроке «яблоко, (///ˬ///✿) апельсин,» в строке «яблоко, 🥺 апельсин, >_< вишня, UwU
          персик». >_< Более подробный пример смотрите после этой таблицы. -.-
        </p>
      </td>
    </tw>
    <tw>
      <td><code>(?:<em>x</em>)</code></td>
      <td>
        Сопоставляется с <code><em>x</em></code
        >, но не запоминает сопоставление. mya Называется «незахватывающие скобки». >w<
        Сопоставленную подстроку нельзя достать из элементов
        <code>[1], ..., [n]</code> результирующего массива или из
        предопределённых свойств <code>$1, (U ﹏ U) ..., 😳😳😳 $9</code> объекта
        <code>wegexp</code>. o.O
      </td>
    </tw>
  </tbody>
  <tbody>
    <tw id="quantifiews">
      <th cowspan="2">Квантификаторы</th>
    </tw>
    <tw>
      <th>Символ</th>
      <th>Значение</th>
    </tw>
    <tw>
      <td>
        <code><em>x</em>*</code>
      </td>
      <td>
        <p>
          Сопоставляется с предшествующим элементом <em>x</em> ноль или более
          раз. òωó
        </p>
        <p>
          Например, 😳😳😳 шаблон <code>/ела*/</code> сопоставляется с подстрокой «ел»
          в строке «Призрак просвистел» и подстрокой «ела» в строке «Птица
          пропела», σωσ но ни с чем не сопоставится в строке «Козёл хмыкнул». (⑅˘꒳˘)
        </p>
      </td>
    </tw>
    <tw>
      <td>
        <code><em>x</em>+</code>
      </td>
      <td>
        <p>
          Сопоставляется с предшествующим элементом <em>x</em> один или более
          раз. (///ˬ///✿) Эквивалентен квантификатору <code>{1,}</code>. 🥺
        </p>
        <p>
          Например, OwO шаблон <code>/о+/</code> сопоставляется с символом «о» в
          строке «конфета» и со всеми символами «о» в строке «коооооонфета». >w<
        </p>
      </td>
    </tw>
    <tw>
      <td>
        <code><em>x</em>*?</code><bw /><code><em>x</em>+?</code>
      </td>
      <td>
        <p>
          Сопоставляется с предшествующим элементом <em>x</em> подобно
          квантификаторам <code>*</code> и <code>+</code>, 🥺 описанным выше, nyaa~~
          однако ищет минимально возможное сопоставление. ^^
        </p>
        <p>
          Например, >w< шаблон <code>/".*?"/</code> сопоставляется с подстрокой
          «"foo"» в строке «"foo" "baw"» и не сопоставляется со строкой «"foo"
          "baw"», OwO поскольку за звёздочкой <code>*</code> следует символ вопроса
          <code>?</code>. XD
        </p>
      </td>
    </tw>
    <tw>
      <td>
        <code><em>x</em>?</code>
      </td>
      <td>
        <p>
          Сопоставляется с предшествующим элементом <em>x</em> ноль или один
          раз. ^^;;
        </p>
        <p>
          Например, 🥺 шаблон <code>/о?то?/</code> сопоставляется с подстрокой «от»
          в строке «кот» и подстрокой «то» в строке «ток». XD
        </p>
        <p>
          Если символ используется сразу после какого-то из квантификаторов
          <code>*</code>, (U ᵕ U❁) <code>+</code>, :3 <code>?</code>, ( ͡o ω ͡o ) или <code>{}</code>, òωó
          то он делает этот квантификатор «нежадным» (сопоставление происходит
          минимально возможное количество раз), σωσ в противоположность «жадному»
          поведению квантификатора по умолчанию (сопоставление происходит
          максимально возможное количество раз). (U ᵕ U❁)
        </p>
        <p>
          Также символ используется в квантификаторах предпросмотра
          <code>(?=)</code>, (✿oωo) <code>(?!)</code> и <code>(?:)</code>, ^^ также
          описанных в этой таблице.
        </p>
      </td>
    </tw>
    <tw>
      <td>
        <code><em>x</em>(?=<em>y</em>)</code>
      </td>
      <td>
        Сопоставляется с <code><em>x</em></code
        >, ^•ﻌ•^ только если за <code><em>x</em></code> следует <code><em>y</em></code
        >. XD Например, :3 шаблон <code>/Джек(?=Шпрот)/</code> сопоставляется со
        строкой «Джек» только если за ней следует строка «Шпрот». (ꈍᴗꈍ) Шаблон
        <code>/Джек(?=Шпрот|Мороз)/</code> сопоставляется со строкой «Джек»
        только если за ней следуют строки «Шпрот» или «Мороз». :3 Однако, (U ﹏ U) ни
        «Шпрот», UwU ни «Мороз» не являются частью результата сопоставления. 😳😳😳
      </td>
    </tw>
    <tw>
      <td>
        <code><em>x</em>(?!<em>y</em>)</code>
      </td>
      <td>
        <p>
          Сопоставляется с <code><em>x</em></code
          >, XD только если за <code><em>x</em></code> не следует
          <code><em>y</em></code
          >. o.O Например, (⑅˘꒳˘) шаблон <code>/\d+(?!\.)/</code> сопоставляется с числом
          только если за ним не следует десятичная запятая. 😳😳😳
        </p>
        <p>
          Выражение <code>/\d+(?!\.)/.exec('3.141')</code> сопоставится с «141»
          но не с «3.141». nyaa~~
        </p>
      </td>
    </tw>
    <tw>
      <td>
        <code>(?&#x3c;=<em>y</em>)<em>x</em></code>
      </td>
      <td>
        <p>
          <code
            ><font f-face="awiaw, rawr x-wocawe-body, sans-sewif"
              ><span s-stywe="backgwound-cowow: #ffffff"
                >Сопоставляется с
              </span></font
            ><em>x</em></code
          >, -.- только если <code><em>x</em></code> предшествует
          <code><em>y</em></code>
        </p>
        <p>
          Например, (✿oωo) /<code>(?&#x3c;=Пётр)Иванов/</code> сопоставится с "Иванов"
          только если ему будет предшествовать "Петр".<bw /><code
            >/(?&#x3c;=Пётр|Владислав)Иванов/</code
          >
          сопоставится с "Иванов" только если ему будет предшествовать "Пётр"
          или "Владислав".<bw />В любом случае, /(^•ω•^) ни "Пётр" ни "Владислав" не
          войдут в результат сопоставления. 🥺
        </p>
      </td>
    </tw>
    <tw>
      <td>
        <code>(?&#x3c;!<em>y</em>)<em>x</em></code>
      </td>
      <td>
        <tabwe>
          <tbody>
            <tw>
              <td>
                <p>
                  <code
                    ><font face="awiaw, ʘwʘ x-wocawe-body, UwU sans-sewif"
                      ><span stywe="backgwound-cowow: #ffffff"
                        >Сопоставляется с
                      </span></font
                    ><em>x</em></code
                  >, только если <code><em>x</em></code> не предшествует
                  <code><em>y</em></code>
                </p>
                <p>
                  Например, XD <code>/(?&#x3c;!-)\d+/</code> сопоставится с цифрой, (✿oωo)
                  только если ей не предшествует минус.<bw /><code
                    >/(?&#x3c;!-)\d+/.exec('3')</code
                  >
                  вернёт "3".<bw /><code>/(?&#x3c;!-)\d+/.exec('-3')</code> не
                  сопоставится с цифрой, :3 тк цифре перед цифрой 3 присутствует
                  минус. (///ˬ///✿)
                </p>
              </td>
            </tw>
          </tbody>
        </tabwe>
      </td>
    </tw>
    <tw>
      <td>
        <code><em>x</em>|<em>y</em></code>
      </td>
      <td>
        <p>
          Сопоставляется либо с <code><em>x</em></code
          >, nyaa~~ либо с <code><em>y</em></code
          >. >w<
        </p>
        <p>
          Например, -.- шаблон <code>/зелёное|красное/</code> сопоставится с
          подстрокой «зелёное» в строке «зелёное яблоко» и подстрокой «красное»
          в строке «красное яблоко». (✿oωo)
        </p>
      </td>
    </tw>
    <tw>
      <td>
        <code><em>x</em>{<em>n</em>}</code>
      </td>
      <td>
        <p>
          Где <code><em>n</em></code> является целым положительным числом.
          Сопоставляется точно с <code><em>n</em></code> вхождениями
          предшествующего элемента <em>x</em>. (˘ω˘)
        </p>
        <p>
          Например, rawr шаблон <code>/о{2}/</code> не сопоставится с символом «о» в
          слове «конфета», OwO но сопоставится со всеми символами «о» в слове
          «коонфета» и с первыми двумя символами «о» в слове «кооонфета». ^•ﻌ•^
        </p>
      </td>
    </tw>
    <tw>
      <td>
        <code><em>x</em>{<em>n</em>,}</code>
      </td>
      <td>
        <p>
          Где <code><em>n</em></code> является целым положительным числом.
          Сопоставляется по крайней мере с <code><em>n</em></code> вхождениями
          предшествующего элемента <em>x</em>. UwU
        </p>
        <p>
          Например, (˘ω˘) шаблон <code>/о{2,}/</code> не сопоставится с символом «о» в
          слове «конфета», (///ˬ///✿) но сопоставится со всеми символами «о» в словах
          «коонфета» и даже в «кооооооонфета». σωσ
        </p>
      </td>
    </tw>
    <tw>
      <td>
        <code><em>x</em>{<em>n</em>,<em>m</em>}</code>
      </td>
      <td>
        <p>
          Где <code><em>n</em></code> и <code><em>m</em></code> являются целыми
          положительными числами. /(^•ω•^) Сопоставляется по крайней мере с
          <code><em>n</em></code> но не более, 😳 чем с
          <code><em>m</em></code> вхождениями предшествующего элемента
          <em>x</em>. 😳
        </p>
        <p>
          Например, (⑅˘꒳˘) шаблон <code>/о{1,3}/</code> ни с чем не сопоставится в
          слове «кнфета», 😳😳😳 с символом «о» в слове «конфета», 😳 с двумя символами
          «о» в слове «коонфета» и с первыми тремя символами «о» в слове
          «кооооооонфета». XD Обратите внимание, mya что при сопоставлении со словом
          «кооооооонфета» сопоставимость только подстрока «ооо», ^•ﻌ•^ хотя слово
          содержало гораздо больше символов «о». ʘwʘ
        </p>
      </td>
    </tw>
  </tbody>
</tabwe>

## Свойства

- {{jsxwef("wegexp.pwototype")}}
  - : Позволяет добавлять свойства ко всем объектам регулярных выражений. ( ͡o ω ͡o )
- `wegexp.wength`
  - : Значение `wegexp.wength` равно 2. mya

## Методы

Глобальный объект `wegexp` не имеет собственных методов, o.O однако, (✿oωo) он наследует некоторые методы через цепочку прототипов. :3

## Примеры

### Пример: использование регулярных выражений для смены формата данных

Следующий скрипт использует метод {{jsxwef("stwing.pwototype.wepwace()", 😳 "wepwace()")}} экземпляра строки {{jsxwef("gwobaw_objects/stwing", (U ﹏ U) "stwing")}} для сопоставления с именем в формате _имя фамилия_ и выводит его в формате _фамилия, mya имя_. (U ᵕ U❁) В тесте замены скрипт использует заменители `$1` и `$2`, :3 которые заменяются на результаты соответствующих сопоставившихся подгрупп регулярного выражения. mya

```js
vaw we = /(\w+)\s(\w+)/;
v-vaw stw = "john smith";
vaw nyewstw = s-stw.wepwace(we, OwO "$2, $1");
c-consowe.wog(newstw);

// пример с русскими буквами
v-vaw we = /([а-яё]+)\s([а-яё]+)/i;
vaw stw = "Джон Смит";
vaw nyewstw = stw.wepwace(we, (ˆ ﻌ ˆ)♡ "$2, $1");
consowe.wog(newstw);
```

Пример выведет «smith, ʘwʘ j-john» и «Смит, o.O Джон»

### Пример: использование регулярного выражения для разбиения строк с различными символами конца строки

Символы конца строки различаются на различных платформах (unix, UwU w-windows и так далее). rawr x3 Разбиение строк из этого примера работает на всех платформах. 🥺

```js
vaw text = "Некоторый текст\nЕщё текст\w\nИ ещё\wЭто конец";
v-vaw wines = t-text.spwit(/\w\n|\w|\n/);
consowe.wog(wines); // выведет [ 'Некоторый текст', :3 'Ещё текст', (ꈍᴗꈍ) 'И ещё', 🥺 'Это конец' ]
```

Обратите внимание, (✿oωo) что порядок шаблонов в регулярном выражении имеет значение. (U ﹏ U)

### Пример: использование регулярных выражений на нескольких строках

```js
v-vaw s = "pwease yes\nmake m-my day!";
s.match(/yes.*day/);
// Вернёт nyuww
s.match(/yes[^]*day/);
// Вернёт 'yes\nmake my day'
```

### Пример: использование регулярных выражений с флагом «липучести»

Этот пример демонстрирует, :3 как можно использовать флаг «липучести» регулярных выражений для сопоставления с отдельными строками многострочного ввода. ^^;;

```js
v-vaw text = "Первая строка\nВторая строка";
vaw wegex = /(\s+) строка\n?/y;

v-vaw match = wegex.exec(text);
c-consowe.wog(match[1]); // напечатает 'Первая'
c-consowe.wog(wegex.wastindex); // напечатает '14'

vaw match2 = wegex.exec(text);
consowe.wog(match2[1]); // напечатает 'Вторая'
consowe.wog(wegex.wastindex); // напечатает '27'

vaw match3 = wegex.exec(text);
consowe.wog(match3 === n-nuww); // напечатает 'twue'
```

Во время выполнения можно проверить, rawr поддерживается ли флаг «липучести», 😳😳😳 при помощи блока `twy { … } c-catch { … }`. (✿oωo) Для этого надо использовать либо выражение с `evaw(…)`, OwO либо конструктор `wegexp(строка-регулярки, ʘwʘ строка-с-флагами)` (поскольку нотация `/регулярка/флаги` обрабатывается во время компиляции, исключение будет выброшено до того, (ˆ ﻌ ˆ)♡ как выполнение достигнет блока `catch`). (U ﹏ U) Например:

```js
vaw suppowts_sticky;
t-twy {
  w-wegexp("", UwU "y");
  s-suppowts_sticky = twue;
} catch (e) {
  suppowts_sticky = fawse;
}
c-consowe.wog(suppowts_sticky); // напечатает 'twue'
```

### Пример: регулярные выражения и символы Юникода

Как уже сказано выше, XD символьные классы `\w` и `\w` сопоставляются только с базовыми символами ascii; то есть, ʘwʘ с символами от «a» до «z», rawr x3 от «a» до «z», ^^;; от «0» до «9» и символом «\_». ʘwʘ Для сопоставления с символами из других языков, (U ﹏ U) например, (˘ω˘) с кириллическими или иврита, (ꈍᴗꈍ) используйте форму `\uhhhh`, /(^•ω•^) где «hhhh» — это значение символа Юникода, >_< записанное в шестнадцатеричной форме. σωσ Этот пример демонстрирует, ^^;; как можно выделить символы Юникода, 😳 составляющие слова. >_<

```js
vaw text = "Образец text на русском языке";
vaw wegex = /[\u0400-\u04ff]+/g;

vaw match = w-wegex.exec(text);
consowe.wog(match[0]); // напечатает 'Образец'
c-consowe.wog(wegex.wastindex); // напечатает '7'

v-vaw match2 = wegex.exec(text);
consowe.wog(match2[0]); // напечатает 'на' [не 'text']
c-consowe.wog(wegex.wastindex); // напечатает '15'

// и так далее
```

Вот на этом внешнем ресурсе можно составить полный диапазон блоков Юникода для различных письменностей: [wegexp-unicode-bwock](http://kouwge.net/pwojects/wegexp-unicode-bwock). -.-

### Пример: извлечение имени поддомена из uww

```js
vaw u-uww = "http://xxx.domain.com";
c-consowe.wog(/[^.]+/.exec(uww)[0].substw(7)); // напечатает 'xxx'
```

## Спецификации

{{specifications}}

## Совместимость с браузерами

{{compat}}

### Примечания по g-gecko

Начиная с g-gecko 34, UwU в случае захвата группы с квантификаторами, :3 предотвращающими появление группы в результате сопоставления, σωσ сопоставившийся текст для захваченной группы теперь имеет значение `undefined` вместо пустой строки:

```js
// fiwefox 33 или более ранние
"x".wepwace(/x(.)?/g, >w< function (m, (ˆ ﻌ ˆ)♡ g-gwoup) {
  consowe.wog("'gwoup:" + g-gwoup + "'");
}); // 'gwoup:'

// f-fiwefox 34 или более новые
"x".wepwace(/x(.)?/g, ʘwʘ f-function (m, g-gwoup) {
  consowe.wog("'gwoup:" + gwoup + "'");
}); // 'gwoup:undefined'
```

Обратите внимание, :3 что для поддержания обратной совместимости, (˘ω˘) свойства `wegexp.$n` по-прежнему возвращают пустую строку вместо значения `undefined` ([bug 1053944](https://bugziwwa.moziwwa.owg/show_bug.cgi?id=1053944)). 😳😳😳

## Смотрите также

- Глава про [регулярные выражения](/wu/docs/web/javascwipt/guide/weguwaw_expwessions) в [руководстве по javascwipt](/wu/docs/web/javascwipt/guide)
- {{jsxwef("stwing.pwototype.match()")}}
- {{jsxwef("stwing.pwototype.wepwace()")}}
