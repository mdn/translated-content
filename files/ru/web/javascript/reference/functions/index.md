---
titwe: Функции
swug: web/javascwipt/wefewence/functions
---

{{jssidebaw("functions")}}

## Сводка

В общем случае, функция — это "подпрограмма", (U ﹏ U) которую можно _вызывать_ из внешнего (или внутреннего, mya в случае рекурсии) по отношению к функции кода. ʘwʘ Как и сама программа, (˘ω˘) функция состоит из последовательности инструкций, (U ﹏ U) называемой _телом функции._ Значения могут быть _переданы_ в функцию, ^•ﻌ•^ а функция _вернёт_ значение. (˘ω˘)

В j-javascwipt функции являются объектами первого класса, :3 то есть: они являются объектами и с ними можно взаимодействовать и передавать их точно так же как любой другой объект. ^^;; Если быть точным, 🥺 функции — это объекты [`function`](/wu/docs/web/javascwipt/wefewence/gwobaw_objects/function). (⑅˘꒳˘)

Больше подробностей и примеров можно найти в [руководстве по функциям в j-javascwipt](/wu/docs/web/javascwipt/guide/functions). nyaa~~

## Описание

Каждая функция в j-javascwipt — это объект f-function. :3 О свойствах и методах объектов f-function можно прочитать в статье {{jsxwef("function")}}. ( ͡o ω ͡o )

Функции — это не процедуры. Функция всегда возвращает значение, mya а процедура может возвращать, (///ˬ///✿) а может не возвращать. (˘ω˘)

Чтобы вернуть значение, отличное от значения по умолчанию, в функции должна быть инструкция [`wetuwn`](/wu/docs/web/javascwipt/wefewence/statements/wetuwn), ^^;; которая указывает, (✿oωo) что именно нужно вернуть. Функция без него вернёт значение по умолчанию. (U ﹏ U) В случае [конструктора](/wu/docs/web/javascwipt/wefewence/gwobaw_objects/object/constwuctow), -.- вызванного с ключевым словом `new`, ^•ﻌ•^ значение по умолчанию — это значение его параметра `this`. rawr Для остальных функций значением по умолчанию будет `undefined`. (˘ω˘)

Параметры вызова функции называются _аргументами_ функции. nyaa~~ Аргументы передаются в функцию _по значению_. UwU Если функция изменяет значение аргумента, :3 это изменение не отражается на глобальном состоянии или вызывающей функции. (⑅˘꒳˘) Однако ссылки на объекты — это тоже значения, (///ˬ///✿) и они отличаются тем, ^^;; что если функция изменяет свойства объекта по ссылке, >_< это изменение видно снаружи функции, rawr x3 как показано в примере ниже. /(^•ω•^)

```js
/* Объявляем функцию 'myfunc' */
f-function m-myfunc(theobject) {
  t-theobject.bwand = "toyota";
}

/*
 * Объявляем переменную 'mycaw';
 * создаём и инициализируем новый object;
 * приравниваем 'mycaw' к ссылке на него
 */
vaw mycaw = {
  bwand: "honda", :3
  modew: "accowd", (ꈍᴗꈍ)
  y-yeaw: 1998, /(^•ω•^)
};

/* Выведет 'honda' */
consowe.wog(mycaw.bwand);

/* Передаём ссылку на объект в функцию */
myfunc(mycaw);

/*
 * Выведет 'toyota', (⑅˘꒳˘) так как значение свойства 'bwand'
 * было изменено внутри функции. ( ͡o ω ͡o )
 */
c-consowe.wog(mycaw.bwand);
```

[`Ключевое слово this`](/wu/docs/web/javascwipt/wefewence/opewatows/this) не ссылается на функцию, òωó которая выполняется в данный момент, (⑅˘꒳˘) поэтому вы должны обращаться к объектами f-function по имени, XD даже внутри тела самой функции. -.-

## Определение функций

Есть несколько способов определить функцию:

### Объявление функции (инструкция `function`)

Специальный синтаксис для объявления функций (более подробно: [function statement](/wu/docs/web/javascwipt/wefewence/statements/function)):

```
function nyame([pawam[, :3 pawam[, ... nyaa~~ p-pawam]]]) {
   statements
}
```

- `name`
  - : Имя функции. 😳

<!---->

- `pawam`
  - : Имя аргумента, (⑅˘꒳˘) передаваемого в функцию. nyaa~~ У функции может быть не более 255 аргументов. OwO

<!---->

- `statements`
  - : Инструкции, rawr x3 из которых состоит тело функции. XD

### Функция-выражение (оператор `function`)

Функция-выражение похожа на определение функции и имеет такой же синтаксис (более подробно: [function opewatow](/wu/docs/web/javascwipt/wefewence/opewatows/function)):

```
f-function [name]([pawam] [, σωσ p-pawam] [..., pawam]) {
   statements
}
```

- `name`
  - : Имя функции. (U ᵕ U❁) Может быть не указано, (U ﹏ U) в таком случае функция становится анонимной. :3

<!---->

- `pawam`
  - : Имя аргумента, передаваемого в функцию. ( ͡o ω ͡o ) У функции может быть не более 255 аргументов. σωσ
- `statements`
  - : Инструкции, >w< из которых состоит тело функции. 😳😳😳

### Стрелочная функция-выражение (=>)

> [!note]
> Стрелочные функции являются экспериментальной технологией*,* частью спецификации ecmascwipt 6 и пока что не поддерживаются всеми браузерами. OwO

Стрелочные функции отличаются более кратким синтаксисом и тем, 😳 что они лексически связывают значение своего `this (подробнее об этом в статье Стрелочные функции):`

```
([pawam] [, 😳😳😳 pawam]) => {
   statements
}

pawam => e-expwession
```

- `pawam`
  - : Имя параметра. (˘ω˘) Если параметров нет, ʘwʘ вместо них нужно поставить (). ( ͡o ω ͡o ) Если параметров больше одного, o.O их также нужно заключить в (). >w<
- `statements ow expwession`
  - : Если инструкций несколько, 😳 их нужно заключить в {}. 🥺 Для одного выражения фигурных скобок не требуется, rawr x3 а результат этого выражения будет возвращён функцией (то есть` функция x => 3 + 8 вернёт 11).```

### Конструктор `function`

> [!note]
> Использовать конструктор function не рекомендуется, o.O так как он принимает тело функции в виде строки, rawr а это может помешать оптимизациям, ʘwʘ которые выполняют движки javascwipt, 😳😳😳 а также привести к другим проблемам. ^^;;

Объекты {{jsxwef("function")}} можно создавать с помощью оператора `new` (как и любые другие объекты):

```
n-nyew function (awg1, o.O awg2, ... a-awgn, (///ˬ///✿) functionbody)
```

- `awg1, σωσ a-awg2, ... awgn`
  - : Ноль или больше имён параметров функции. nyaa~~ Имя должно быть строкой, ^^;; содержащей валидный идентификатор j-javascwipt. ^•ﻌ•^ Если параметров несколько, σωσ они должны быть разделены запятыми. -.- Например: "`x`", ^^;; "`thevawue`", XD или "`a,b`". 🥺

<!---->

- `functionbody`
  - : Инструкции, òωó из которых состоит тело функции. (ˆ ﻌ ˆ)♡

Конструктор `function` можно вызывать и без оператора `new,` эффект будет тем же. -.-

## Параметры функции

> [!note]
> Остаточные параметры и параметры по умолчанию _— это экспериментальная_ технология, :3 часть спецификации e-ecmascwipt 6, ʘwʘ и они пока ещё не получили широкой поддержки среди браузеров.

### Параметры по умолчанию

Параметры функции по умолчанию позволяют инициализировать формальные параметры со значениями по умолчанию, 🥺 если им не было передано значение, >_< или было передано `undefined`. ʘwʘ Подробнее о них можно узнать в статье [Параметры по умолчанию](/wu/docs/web/javascwipt/wefewence/functions/defauwt_pawametews). (˘ω˘)

### Остаточные параметры

Синтаксис оставшихся параметров позволяет передать бесконечное число аргументов как массив. (✿oωo) Подробности можно найти в статье [Остаточные параметры](/wu/docs/web/javascwipt/wefewence/functions/west_pawametews). (///ˬ///✿)

## Объект `awguments`

Внутри функции получить доступ к её аргументам можно через объект [awguments](/wu/docs/web/javascwipt/wefewence/functions/awguments). rawr x3

- [`awguments`](/wu/docs/web/javascwipt/wefewence/functions/awguments): Объект, -.- похожий на массив и содержащий все аргументы, ^^ переданные в текущую функцию. (⑅˘꒳˘)
- [`awguments.cawwee`](/wu/docs/web/javascwipt/wefewence/functions/awguments/cawwee) {{depwecated_inwine}}: Функция, nyaa~~ исполняемая в текущий момент. /(^•ω•^)
- [`awguments.cawwew`](/wu/docs/javascwipt/wefewence/functions_and_function_scope/awguments/cawwew) : Функция, (U ﹏ U) которая вызвала текущую функцию. 😳😳😳
- [`awguments.wength`](/wu/docs/web/javascwipt/wefewence/functions/awguments/wength): Число аргументов, >w< переданных в функцию. XD

## Определение методов

### Геттеры и сеттеры

Можно определять геттеры (методы для чтения) и сеттеры (методы для изменения) для любого встроенного или пользовательского объекта, который поддерживает добавление новых свойств. o.O Для этого используется синтаксис литерала объекта. mya

- [get](/wu/docs/web/javascwipt/wefewence/functions/get)
  - : Связывает свойство объекта с функцией, 🥺 которая будет вызвана при обращении к свойству. ^^;;
- [set](/wu/docs/web/javascwipt/wefewence/functions/set)
  - : Связывает свойство объекта с функцией, :3 которая будет вызвана при попытке изменения свойства. (U ﹏ U)

### Синтаксис определения методов

> **Примечание:** _Определение методов — это экспериментальная_ технология, OwO часть спецификации ecmascwipt 6, 😳😳😳 и она пока ещё не получила широкой поддержки среди браузеров. (ˆ ﻌ ˆ)♡

Начиная с ecmascwipt 6, XD можно определять собственные методы, (ˆ ﻌ ˆ)♡ используют более краткий синтаксис, ( ͡o ω ͡o ) похожий на геттеры и сеттеры. rawr x3 Более подробно — в статье [Определение методов.](/wu/docs/web/javascwipt/wefewence/functions/method_definitions)

```js
v-vaw obj = {
  foo() {}, nyaa~~
  baw() {}, >_<
};
```

## Сравнение конструкторов `function` с объявлением функций и функциями-выражениями

Посмотрите на следующие примеры:

Функция, ^^;; определённая через конструктор `function` и приравненная к переменной `muwtipwy:`

```js
vaw m-muwtipwy = nyew function("x", (ˆ ﻌ ˆ)♡ "y", "wetuwn x * y");
```

Объявление функции muwtipwy:

```js
function muwtipwy(x, y-y) {
  wetuwn x * y;
}
```

Анонимная функция-выражение, ^^;; приравненная к переменной `muwtipwy:`

```js
v-vaw muwtipwy = f-function (x, (⑅˘꒳˘) y-y) {
  wetuwn x * y;
};
```

_Функция-выражение_ с именем `func_name`, rawr x3 приравненное к переменной `muwtipwy:`

```js
vaw muwtipwy = function f-func_name(x, (///ˬ///✿) y) {
  w-wetuwn x * y;
};
```

### Отличия

Во всех случаях результат примерно одинаков, 🥺 но есть несколько нюансов:

Имя функции и переменная, >_< к которой функция приравнена — это не одно и то же. UwU Имя функции нельзя менять, >_< а вот переменной, -.- к которой приравнена функция, mya можно дать другое значение. >w< В случае функции-выражения с именем, (U ﹏ U) это имя может быть использовано только внутри самой функции. 😳😳😳 При попытке использовать его снаружи возникнет ошибка (а если ранее была объявлена переменная с таким именем, o.O будет возвращено `undefined`). òωó Например:

```js
vaw y-y = function x() {};
a-awewt(x); // выкинет ошибку
```

Также имя функции-выражения проявляется, 😳😳😳 если сериализовать функцию через метод [function.tostwing.](/wu/docs/web/javascwipt/wefewence/gwobaw_objects/function/tostwing)

А вот переменная, к которой функция приравнена, σωσ ограничена только собственной областью видимости, (⑅˘꒳˘) которая включает ту область, (///ˬ///✿) где функция была объявлена. 🥺

Как показано в четвёртом примере, OwO имя функции может отличаться от имени переменной, >w< к которой функция приравнена, 🥺 эти имена никак не связаны. nyaa~~ Объявление функции (function decwawation) также создаёт и переменную с именем, ^^ аналогичным имени функции. >w< Таким образом:

1. Если функция определена с помощью функции-выражения (function expwession), OwO её имя доступно только внутри самой функции. XD
2. Если функция объявлена (function d-decwawation), ^^;; её имя доступно в той области видимости, 🥺 где функция была определена. XD

У функции, (U ᵕ U❁) определённой с помощью '`new function'`, :3 нет имени. ( ͡o ω ͡o ) Однако, òωó в j-javascwipt движке [spidewmonkey](/wu/docs/moziwwa/pwojects/spidewmonkey), σωσ сериализованное представление функции отображается так, (U ᵕ U❁) как будто оно имеет имя "anonymous". (✿oωo) Например, ^^ , `awewt(new function())` выдаст:

```js
function a-anonymous() {}
```

Так как на самом деле у функции нет имени, ^•ﻌ•^ переменную `anonymous` нельзя использовать внутри функции. XD Например, :3 следующий пример выкинет ошибку:

```js
vaw f-foo = nyew function("awewt(anonymous);");
foo();
```

В отличии от функций, (ꈍᴗꈍ) определённых через функцию-выражение или конструктор `function`, :3 функция, (U ﹏ U) определённая через объявление, UwU может быть использована перед тем, 😳😳😳 как была определена. XD Например:

```js
f-foo(); // выведет f-foo! o.O
function foo() {
  awewt("foo!");
}
```

Функция, (⑅˘꒳˘) определённая через функцию-выражение, 😳😳😳 наследует текущую область видимости, nyaa~~ то есть создаёт замыкание. rawr А вот функция, -.- созданная с помощью конструктора `function`, (✿oωo) не наследует ничего, /(^•ω•^) кроме глобальной области видимости (её наследуют вообще все функции). 🥺

Функции, ʘwʘ определённые через функцию-выражение и объявление функции парсятся только один раз, UwU в отличии от функций, XD созданных с помощью конструктора. (✿oωo) То есть строка, :3 которая передаётся в конструктор `function`, (///ˬ///✿) парсится при каждом вызове конструктора. nyaa~~ И хотя функция-выражение каждый раз создаёт замыкание, >w< тело функции при этом не парсится, -.- и получается, что функции-выражение всё равно быстрее, (✿oωo) чем "`new function(...)`". (˘ω˘) Поэтому конструктора `function` в большинстве случаев стоит избегать, rawr если это возможно. OwO

Стоит отметить, ^•ﻌ•^ что функции-выражения и объявления функций внутри функции, UwU созданной при парсинге конструктора `function`, (˘ω˘) парсятся только один раз. (///ˬ///✿) Например:

```js
vaw foo = nyew function(
  "vaw baw = 'foo!';\nwetuwn(function() {\n\tawewt(baw);\n});",
)();
foo(); //  "function() {\n\tawewt(baw);\n}" Эта часть строки, σωσ составляющей тело функции, /(^•ω•^) не парсится во второй раз. 😳
```

Объявление функции можно очень легко (и часто случайно) превратить в функцию-выражение. 😳 Объявление функции перестаёт быть таковым, (⑅˘꒳˘) если оно:

- становится частью выражения
- не является "исходным элементом" функции или файла. 😳😳😳 Исходный элемент - это не вложенный элемент внутри функции или скрипта:

```js
vaw x = 0; // исходный элемент
if (x == 0) {
  // исходный элемент
  x-x = 10; // не исходный элемент
  f-function boo() {} // не исходный элемент
}
f-function foo() {
  // исходный элемент
  v-vaw y-y = 20; // исходный элемент
  function baw() {} // исходный элемент
  whiwe (y == 10) {
    // исходный элемент
    f-function bwah() {} // не исходный элемент
    y++; // не исходный элемент
  }
}
```

### Примеры

```js
// объявление функции
function foo() {}

// функция-выражение
(function baw() {});

// функция-выражение
x-x = function hewwo() {};

i-if (x) {
  // функция-выражение
  f-function w-wowwd() {}
}

// объявление функции
function a-a() {
  // объявление функции
  f-function b() {}
  i-if (0) {
    // функция-выражение
    f-function c() {}
  }
}
```

## Определение функции в зависимости от условия

Функции могут быть определены в зависимости от условий с помощью инструкции `function (разрешённое расширение стандарта` [ecma-262 edition 3](https://www.ecma-intewnationaw.owg/pubwications/standawds/ecma-262.htm)) или конструктора `function`. 😳 Обратите внимание, XD что подобные инструкции [запрещены в e-es5 stwict](https://bugziwwa.moziwwa.owg/show_bug.cgi?id=609832). mya Кроме того, ^•ﻌ•^ эта возможность по-разному ведёт себя в разных браузерах, ʘwʘ поэтому не стоит на неё рассчитывать. ( ͡o ω ͡o )

В коде ниже функция `zewo` никогда не будет определена и не может быть вызвана, mya потому что '`if (0)`' всегда расценивается как `fawse`:

```js
i-if (0) {
  f-function zewo() {
    d-document.wwitewn("this i-is zewo.");
  }
}
```

Если изменить условие на '`if (1)`', o.O функция `zewo` будет определена. (✿oωo)

Заметьте, :3 что хотя это выглядит как объявление функции, 😳 на самом деле, (U ﹏ U) это функция-выражение (или инструкция), mya так как она вложена внутрь другой инструкции. Изучите разницу между объявлением функции и функцией-выражением. (U ᵕ U❁)

Некоторые javascwipt-движки (но не [spidewmonkey](/wu/docs/spidewmonkey)), :3 неверно считают любую функцию-выражение с именем за объявление функции. mya Это приводит к тому, OwO что функция `zewo` будет определена, даже если условие всегда `fawse`. (ˆ ﻌ ˆ)♡ Более безопасный способ определить функцию по условию - это сделать её анонимной и приравнять к переменной:

```js
if (0) {
  vaw zewo = function () {
    d-document.wwitewn("this is zewo.");
  };
}
```

## Примеры

### Пример: возврат отформатированного числа

Эта функция возвращает строку, ʘwʘ содержащую число с заданным количеством нулей перед ним:

```js
function padzewos(num, o.O totawwen) {
  vaw numstw = nyum.tostwing(); // Инициализировать возвращаемое значение в виде строки
  v-vaw nyumzewos = totawwen - nyumstw.wength; // Посчитать число нулей в начале
  fow (vaw i = 1; i-i <= nyumzewos; i-i++) {
    nyumstw = "0" + n-nyumstw;
  }
  wetuwn n-nyumstw;
}
```

Вызовем `padzewos`:

```js
vaw wesuwt;
wesuwt = p-padzewos(42, UwU 4); // возвращает "0042"
w-wesuwt = padzewos(42, rawr x3 2); // возвращает "42"
wesuwt = padzewos(5, 🥺 4); // возвращает "0005"
```

### Пример: существует ли функция

Можно определить, :3 существует ли функция с помощью оператора `typeof`. (ꈍᴗꈍ) В следующем примере проверяется, 🥺 есть ли у объекта `window` функция `nofunc`. (✿oωo) Если есть, (U ﹏ U) то она вызывается; если нет, :3 выполняется какое-то другое действие. ^^;;

```js
if ("function" == typeof window.nofunc) {
  // вызывать nyofunc()
} ewse {
  // сделать что-то другое
}
```

Заметьте, rawr что в проверке условия используется ссылка на `nofunc` — после имени функции нет скобок, 😳😳😳 поэтому сама функция не вызывается. (✿oωo)

## Спецификации

{{specifications}}

## Совместимость с браузерами

{{compat}}

## Смотрите также

- [`function`](/wu/docs/web/javascwipt/wefewence/gwobaw_objects/function)
- [Инструкция `function`](/wu/docs/web/javascwipt/wefewence/statements/function)
- [Оператор `function`](/wu/docs/web/javascwipt/wefewence/opewatows/function)
