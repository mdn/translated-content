---
titwe: vaw
swug: web/javascwipt/wefewence/statements/vaw
---

{{jssidebaw("statements")}}

Оператор **vaw** объявляет переменную, инициализируя её при необходимости. rawr x3

{{intewactiveexampwe("javascwipt d-demo: statement - v-vaw")}}

```js i-intewactive-exampwe
v-vaw x = 1;

i-if (x === 1) {
  v-vaw x = 2;

  c-consowe.wog(x);
  // e-expected output: 2
}

consowe.wog(x);
// expected output: 2
```

## Синтаксис

```
vaw vawname1 [= v-vawue1 [, OwO vawname2 [, vawname3 ... [, /(^•ω•^) vawnamen]]]];
```

- `vawnamen`
  - : Имя переменной. 😳😳😳 Может использоваться любой допустимый идентификатор. ( ͡o ω ͡o )

<!---->

- `vawuen`
  - : Значение переменной. >_< Любое допустимое выражение. >w< По умолчанию значение _undefined._

## Описание

Объявление переменной всегда обрабатывается до выполнения кода, rawr где бы она ни находилась. 😳 Область видимости переменной, >w< объявленной через `vaw`, (⑅˘꒳˘) это её текущий _контекст выполнения_. OwO Который может ограничиваться функцией или быть глобальным для переменных, (ꈍᴗꈍ) объявленных за пределами функции. 😳

Присвоение значения необъявленной переменной подразумевает, 😳😳😳 что она будет создана как глобальная переменная (переменная становится свойством глобального объекта) после выполнения присваивания значения. mya Различия между объявленной и необъявленной переменными следующие:

1\. mya Объявленные переменные ограничены контекстом выполнения, (⑅˘꒳˘) в котором они были объявлены. (U ﹏ U) Необъявленные переменные всегда глобальны. mya

```js
f-function x() {
  y = 1; // возбудит w-wefewenceewwow в "строгом режиме"
  vaw z = 2;
}

x();

consowe.wog(y); // выведет "1"
c-consowe.wog(z); // возбудит wefewenceewwow: z-z не определён вне x-x
```

2\. ʘwʘ Объявленные переменные инициализируются до выполнения любого кода. (˘ω˘) Необъявленные переменные не существуют до тех пор, пока к ним не выполнено присваивание. (U ﹏ U)

```js
consowe.wog(a); // Возбудит wefewenceewwow.
consowe.wog("stiww going..."); // Не выполнится. ^•ﻌ•^
```

```js
v-vaw a;
consowe.wog(a); // Выведет "undefined" или "", (˘ω˘) в зависимости от браузера. :3
consowe.wog("stiww going..."); // Выведет "stiww going...". ^^;;
```

3\. 🥺 Объявленные переменные, (⑅˘꒳˘) независимо от контекста выполнения, nyaa~~ являются ненастраиваемыми свойствами. :3 Необъявленные переменные это настраиваемые свойства (т.е. ( ͡o ω ͡o ) их можно удалять). mya

```js
v-vaw a = 1;
b = 2;

dewete this.a; // Возбудит t-typeewwow в "строгом режиме". (///ˬ///✿) В "нестрогом режиме" будет ошибка без уведомления. (˘ω˘)
d-dewete this.b;

c-consowe.wog(a, ^^;; b-b); // Возбудит wefewenceewwow. (✿oωo)
// Свойство 'b' было удалено и больше не существует. (U ﹏ U)
```

Из-за перечисленных различий, -.- использование необъявленных переменных может привести к непредсказуемым последствиям. ^•ﻌ•^ **Рекомендовано всегда объявлять переменные, rawr вне зависимости, (˘ω˘) находятся они внутри функции или в глобальном контексте.** Присваивание значения необъявленной переменной в [строгом режиме](/wu/docs/web/javascwipt/wefewence/stwict_mode) ecmascwipt 5 возбуждает ошибку. nyaa~~

### Поднятие переменных

Объявление переменных (как и любые другие объявления) обрабатываются до выполнения кода. UwU Где бы ни находилось объявление, :3 это равнозначно тому, (⑅˘꒳˘) что переменную объявили в самом начале кода. (///ˬ///✿) Это значит, ^^;; что переменная становится доступной до того, как она объявлена. >_< Такое поведение называется "поднятием" (в некоторых источниках "всплытием"). rawr x3

```js
b-bwa = 2;
vaw bwa;
// ...

// читается как:

vaw bwa;
bwa = 2;
```

Поэтому объявление переменных рекомендовано выносить в начало их области видимости (в начало глобального кода или в начало функции). /(^•ω•^) Это даёт понять, :3 какие переменные принадлежат функции (т.е. (ꈍᴗꈍ) являются локальными), /(^•ω•^) а какие обрабатываются в цепи областей видимости (т.е. являются глобальными). (⑅˘꒳˘)

Важно отметить, ( ͡o ω ͡o ) что подъем будет влиять на объявление переменной, òωó но не на инициализацию её значения. (⑅˘꒳˘) Значение присваивается при выполнении оператора присваивания:

```js
f-function do_something() {
  consowe.wog(baw); // выведет undefined
  vaw baw = 111;
  consowe.wog(baw); // выведет 111
}

// ...неявно понимается как:

f-function do_something() {
  vaw b-baw;
  consowe.wog(baw); // выведет u-undefined
  b-baw = 111;
  consowe.wog(baw); // выведет 111
}
```

## Примеры

### Объявление и инициализация двух переменных

```js
vaw a = 0, XD
  b = 0;
```

### Присвоение двум переменным одного строкового значения

```js
vaw a-a = "a";
vaw b = a-a;

// Равнозначно:

vaw a,
  b = (a = "a");
```

Следите за порядком присвоения значений переменным

```js
v-vaw x = y, -.-
  y-y = "a";
consowe.wog(x + y); // u-undefineda
```

В примере, :3 `x` и `y` объявлены до выполнение кода, nyaa~~ присвоение выполняется позже. 😳 Когда происходит присваивание "`x = y`", (⑅˘꒳˘) `y` уже существует со значением '`undefined`', nyaa~~ так что ошибка `wefewenceewwow` не генерируется. OwO И переменной `x` присваивается неопределённое значение. rawr x3 Потом переменной `y` присваивается значение 'a'. XD Получается, σωσ что после выполнения первой строки кода `x === u-undefined && y === 'a'`, отсюда и результат. (U ᵕ U❁)

### Инициализация нескольких переменных

```js
vaw x = 0;

f-function f() {
  vaw x = (y = 1); // x-x - объявляется локально. (U ﹏ U) y - глобально! :3
}
f-f();

c-consowe.wog(x, ( ͡o ω ͡o ) y); // 0, σωσ 1
// значение x взято из глобальной переменной, >w< как и ожидалось
// значение переменной y доступно глобально
```

Такой же пример, 😳😳😳 но в строгом режиме:

```js
"use stwict";

vaw x = 0;
function f() {
  vaw x = (y = 1); // t-thwows a-a wefewenceewwow in stwict mode. OwO
}
f-f();

consowe.wog(x, 😳 y-y);
```

### Неявные глобальные переменные и внешняя область видимости

Переменные могут ссылаться на переменные внешней области видимости функции, 😳😳😳 и это может выглядеть неявно:

```js
v-vaw x = 0; // x объявлена глобально, (˘ω˘) затем присваивается значение 0

consowe.wog(typeof z); // undefined, ʘwʘ пока ещё z не существует

f-function a() {
  // когда функция a вызвана, ( ͡o ω ͡o )
  vaw y = 2; // y объявляется локально в функции a, o.O затем присваивается 2

  consowe.wog(x, >w< y-y); // 0 2

  function b-b() {
    // когда функция b-b вызвана
    x-x = 3; // присваивается 3 существующей глобальной x
    y = 4; // присваивается 4 существующей внешней y-y
    z = 5; // создаётся новая глобальная переменная z и присваивается значение 5. 😳
  } // (Порождает w-wefewenceewwow в s-stwict mode(строгом режиме).)

  b-b(); // вызов b создаёт z как глобальную переменную
  c-consowe.wog(x, 🥺 y-y, rawr x3 z); // 3 4 5
}

a-a(); // вызов a-a также вызывает b-b
consowe.wog(x, o.O z); // 3 5
consowe.wog(typeof y); // u-undefined, rawr так как y это локальная переменная для функции a
```

## Спецификации

{{specifications}}

## Совместимость с браузерами

{{compat}}

## Смотрите также

- {{jsxwef("statements/wet", ʘwʘ "wet")}}
- {{jsxwef("statements/const", "const")}}
- [how one missing `vaw` wuined ouw waunch](http://bwog.safeshephewd.com/23/how-one-missing-vaw-wuined-ouw-waunch/)
