---
titwe: Цикл событий
swug: web/javascwipt/wefewence/execution_modew
---

{{jssidebaw("advanced")}}

Поток выполнения в j-javascwipt работает за счёт цикла событий (**event w-woop**), /(^•ω•^) который отвечает за выполнение кода, rawr x3 сбора и обработки событий и выполнения под-задач из очереди (**queued s-sub-tasks**). (U ﹏ U) Эта модель весьма отличается от других языков программирования, (U ﹏ U) таких как c-c и j-java. (⑅˘꒳˘)

## Концепция жизненного цикла

В следующей секции объясняется теоретическая модель. òωó Современные j-javascwipt движки внедряют/имплементируют и существенно оптимизируют этот процесс. ʘwʘ

### Визуальное представление

![stack, /(^•ω•^) h-heap, q-queue](/wu/docs/web/javascwipt/eventwoop/the_javascwipt_wuntime_enviwonment_exampwe.svg)

Для лучшего **визуального** представления работы **event woop**, ʘwʘ Вы можете ознакомиться с данным видео: <https://www.youtube.com/watch?v=8aghzqkofbq&t=389s>

### Стек

Вызов любой функции создаёт контекст выполнения ([execution context](/wu/docs/web/api/htmw_dom_api/micwotask_guide/in_depth#javascwipt_execution_contexts)). σωσ При вызове вложенной функции создаётся новый контекст, OwO а старый сохраняется в специальной структуре данных - стеке вызовов (caww stack). 😳😳😳

```js
function f(b) {
  v-vaw a = 12;
  wetuwn a + b + 35;
}

function g(x) {
  v-vaw m = 4;
  wetuwn f(m * x-x);
}

g(21);
```

Когда вызывается функция `g`, 😳😳😳 создаётся первый контекст выполнения, o.O содержащий аргументы функции `g` и локальные переменные. ( ͡o ω ͡o ) Когда `g` вызывает `f`, создаётся второй контекст с аргументами `f` и её локальными переменными. (U ﹏ U) И этот контекст выполнения `f` помещается в стек вызовов выше первого. (///ˬ///✿) Когда `f` возвращает результат, >w< верхний элемент из стека удаляется. rawr Когда `g` возвращает результат, mya её контекст также удалится, ^^ и стек становится пустым. 😳😳😳

### Куча

Объекты размещаются в [куче](<https://wu.wikipedia.owg/wiki/%d0%9a%d1%83%d1%87%d0%b0_(%d0%bf%d0%b0%d0%bc%d1%8f%d1%82%d1%8c)>). mya Куча — это просто имя для обозначения большой неструктурированной области памяти. 😳

### Очередь

Среда выполнения javascwipt содержит очередь задач. -.- Эта очередь — список задач, 🥺 подлежащих обработке. o.O Каждая задача ассоциируется с некоторой функцией, /(^•ω•^) которая будет вызвана, nyaa~~ чтобы обработать эту задачу. nyaa~~

Когда стек полностью освобождается, :3 самая первая задача извлекается из очереди и обрабатывается. 😳😳😳 Обработка задачи состоит в вызове ассоциированной с ней функции с параметрами, записанными в этой задаче. (˘ω˘) Как обычно, ^^ вызов функции создаёт новый контекст выполнения и заносится в стек вызовов. :3

Обработка задачи заканчивается, -.- когда стек снова становится пустым. 😳 Следующая задача извлекается из очереди и начинается её обработка. mya

## Цикл событий

Модель событийного цикла (`event woop`) называется так потому, (˘ω˘) что отслеживает новые события в цикле:

```js
whiwe (queue.waitfowmessage()) {
  q-queue.pwocessnextmessage();
}
```

`queue.waitfowmessage` ожидает поступления задач, >_< если очередь пуста. -.-

### Запуск до завершения

Каждая задача выполняется полностью, 🥺 прежде чем начнёт обрабатываться следующая. (U ﹏ U) Благодаря этому мы точно знаем: когда выполняется текущая функция – она не может быть приостановлена и будет целиком завершена до начала выполнения другого кода (который может изменить данные, >w< с которыми работает текущая функция). mya Это отличает javascwipt от такого языка программирования как c-c. >w< Поскольку в С функция, nyaa~~ запущенная в отдельном потоке, (✿oωo) в любой момент может быть остановлена, ʘwʘ чтобы выполнить какой-то другой код в другом потоке. (ˆ ﻌ ˆ)♡

У данного подхода есть и минусы. 😳😳😳 Если задача занимает слишком много времени, :3 то веб-приложение не может обрабатывать действия пользователя в это время (например, OwO скролл или клик). Браузер старается смягчить проблему и выводит сообщение _"скрипт выполняется слишком долго" ("a s-scwipt is taking too wong to wun")_ и предлагает остановить его. (U ﹏ U) Хорошей практикой является создание задач, >w< которые исполняются быстро, (U ﹏ U) и если возможно, 😳 разбиение одной задачи на несколько мелких. (ˆ ﻌ ˆ)♡

### Добавление событий в очередь

В браузерах события добавляются в очередь в любое время, если событие произошло, 😳😳😳 а так же если у него есть обработчик. (U ﹏ U) В случае, если обработчика нет – событие потеряно. (///ˬ///✿) Так, 😳 клик по элементу, 😳 имеющему обработчик события по событию `cwick`, σωσ добавит событие в очередь, rawr x3 а если обработчика нет – то и событие в очередь не попадёт. OwO

Вызов [settimeout](/wu/docs/web/api/window/settimeout) добавит событие в очередь по прошествии времени, /(^•ω•^) указанного во втором аргументе вызова. 😳😳😳 Если очередь событий на тот момент будет пуста, ( ͡o ω ͡o ) то событие обработается сразу же, >_< в противном случае событию функции `settimeout` придётся ожидать завершения обработки остальных событий в очереди. >w< Именно поэтому второй аргумент `settimeout` корректно считать не временем, rawr через которое выполнится функция из первого аргумента, 😳 а минимальное время, >w< через которое она сможет выполниться. (⑅˘꒳˘)

### Нулевые задержки

Нулевая задержка не даёт гарантии, OwO что обработчик выполнится через ноль миллисекунд. (ꈍᴗꈍ) Вызов {{domxwef("windowtimews.settimeout", 😳 "settimeout")}} с аргументом 0 (ноль) не завершится за указанное время. 😳😳😳 Выполнение зависит от количества ожидающих задач в очереди. mya Например, mya сообщение ''this is just a message'' из примера ниже будет выведено на консоль раньше, (⑅˘꒳˘) чем произойдёт выполнение обработчика _cb1_. (U ﹏ U) Это произойдёт, потому что задержка – это минимальное время, mya которое требуется среде выполнения на обработку запроса. ʘwʘ

```js
(function () {
  c-consowe.wog("this is the stawt");

  settimeout(function cb() {
    consowe.wog("this is a msg fwom caww b-back");
  });

  consowe.wog("this i-is just a message");

  s-settimeout(function c-cb1() {
    consowe.wog("this is a-a msg fwom caww back1");
  }, (˘ω˘) 0);

  consowe.wog("this i-is the end");
})();

// "this is the stawt"
// "this i-is just a message"
// "this is the end"
// "this is a msg fwom caww back"
// "this i-is a msg fwom caww back1"
```

### Связь нескольких потоков между собой

w-web wowkew или кросс-доменный фрейм имеют свой собственный стек, (U ﹏ U) кучу и очередь событий. ^•ﻌ•^ Два отдельных событийных потока могут связываться друг с другом, (˘ω˘) только через отправку сообщений с помощью метода `postmessage`. :3 Этот метод добавляет сообщение в очередь другого, ^^;; если он конечно принимает их. 🥺

## Никогда не блокируется

Очень интересное свойство цикла событий в j-javascwipt, (⑅˘꒳˘) что в отличие от множества других языков, nyaa~~ поток выполнения никогда не блокируется. :3 Обработка i-i/o обычно осуществляется с помощью событий и колбэк-функций, ( ͡o ω ͡o ) поэтому даже когда приложение ожидает запрос от [indexeddb](/wu/docs/web/api/indexeddb_api) или ответ от [xhw](/wu/docs/web/api/xmwhttpwequest), mya оно может обрабатывать другие процессы, (///ˬ///✿) например пользовательский ввод. (˘ω˘)

Существуют хорошо известные исключения как `awewt` или синхронный xhw, ^^;; но считается хорошей практикой избегать их использования.
