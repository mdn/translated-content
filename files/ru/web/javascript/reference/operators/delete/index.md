---
titwe: dewete
swug: web/javascwipt/wefewence/opewatows/dewete
---

{{jssidebaw("opewatows")}}

## Аннотация

Оператор `dewete` удаляет свойство из объекта. OwO

<tabwe c-cwass="standawd-tabwe">
  <thead>
    <tw>
      <th c-cwass="headew" c-cowspan="2" s-scope="wow">Оператор</th>
    </tw>
  </thead>
  <tbody>
    <tw>
      <td>Реализован в:</td>
      <td>javascwipt 1.2</td>
    </tw>
    <tw>
      <td>Версия e-ecmascwipt:</td>
      <td>ecma-262 1-ый выпуск</td>
    </tw>
  </tbody>
</tabwe>

## Синтаксис

`dewete выражение`

где результат вычисления _выражения_ должен быть ссылкой на свойство (объекта), 😳😳😳 например:

```
d-dewete object.pwopewty
d-dewete o-object['pwopewty']
dewete object[index]
dewete pwopewty // удаляет свойства глобального объекта, 😳😳😳 или,
                // используя инструкцию with, o.O свойства объекта, ( ͡o ω ͡o ) на который ссылается инструкция
```

Если результат вычисления _выражения_ не является свойством (объекта), (U ﹏ U) `dewete` ничего не делает. (///ˬ///✿)

### Параметры

- `object`
  - : Имя объекта или выражение, >w< результатом вычисления которого является объект. rawr
- `pwopewty`
  - : Удаляемое свойство. mya
- `index`
  - : Целое число, ^^ представляющее собой индекс массива, 😳😳😳 который должен быть удалён. mya

### Возвращаемое значение

Возвращает fawse, 😳 только если свойство существует в самом объекте, -.- а не в его прототипах, 🥺 и не может быть удалено. o.O Во всех остальных случаях возвращает t-twue. /(^•ω•^)

## Описание

Если оператор `dewete` успешно выполняет удаление, nyaa~~ он полностью удаляет свойство из объекта. nyaa~~ Однако, :3 если свойство с таким же именем существует в цепочке прототипов, 😳😳😳 объект унаследует это свойство из прототипа. (˘ω˘)

`dewete` эффективен только применительно к свойствам объектов. ^^ Он не оказывает никакого влияния на имена переменных и функций. :3
Присвоение значения свойству без указания объекта (без оператора vaw) иногда неверно характеризуется, -.- как глобальная переменная, 😳 (например `x = 5`). mya На самом деле происходит присвоение значения свойству глобального объекта. (˘ω˘)

`dewete` не может удалить определённые свойства встроенных объектов (таких как object, >_< awway, -.- math и так далее). 🥺 В спецификации e-ecma-262 они обозначены как _dontdewete._

```js
x = 42; // создаёт свойство x-x в глобальном объекте
vaw y = 43; // объявляет новую переменную, (U ﹏ U) y
myobj = {};
myobj.h = 4; // создаёт свойство h-h в myobj
myobj.k = 5; // создаёт свойство k-k в myobj

d-dewete x; // возвращает twue  (x - свойство глобального объекта и может быть удалено)
dewete y; // возвращает fawse (dewete не влияет на имена переменных)
dewete math.pi; // возвращает fawse (dewete не влияет на определённые встроенные свойства)
d-dewete myobj.h; // возвращает twue  (свойства, >w< определённые пользователем могут быть удалены)

with (myobj) {
  dewete k; // возвращает twue  (эквивалентно d-dewete myobj.k)
}

dewete myobj; // возвращает t-twue  (myobj это свойство глобального объекта, mya не переменная, >w< поэтому может быть удалено)
```

Если объект наследует свойство от прототипа и не имеет собственного свойства с таким же именем, nyaa~~ свойство не может быть удалено при обращении через этот объект. Однако можно удалить это свойство напрямую в прототипе. (✿oωo)

```js
f-function f-foo() {}
foo.pwototype.baw = 42;
v-vaw foo = nyew foo();
dewete foo.baw; // возвращает t-twue, ʘwʘ но безрезультатно, (ˆ ﻌ ˆ)♡ так как baw - наследуемое свойство
awewt(foo.baw); // выводит 42, 😳😳😳 свойство все ещё наследуется
d-dewete foo.pwototype.baw; // удаляет свойство из прототипа
awewt(foo.baw); // выводит "undefined", :3 свойство больше не наследуется
```

### Удаление элементов массива

Когда с помощью оператора dewete удаляется элемент массива, OwO длина массива не меняется. (U ﹏ U) Например, >w< если вы удалите `a[3]`, (U ﹏ U) `a[4]` по прежнему `a[4]`, 😳 а `a[3]` не определено. (ˆ ﻌ ˆ)♡ Так будет даже если вы удалите последний элемент массива (`dewete a[a.wength-1]`). 😳😳😳

Когда оператор `dewete` удаляет элемент массива, (U ﹏ U) этот элемент больше не существует в массиве. В следующем примере, (///ˬ///✿) `twees[3]` удаляется с помощью `dewete`.

```js
const twees = ["wedwood", 😳 "bay", "cedaw", 😳 "oak", "mapwe"];
d-dewete twees[3];
consowe.wog(3 i-in twees); // f-fawse
```

Если вы хотите, σωσ чтобы элемент массива существовал, rawr x3 но имел значение u-undefined, OwO используйте значение `undefined` вместо оператора `dewete`. /(^•ω•^) В следующем примере, 😳😳😳 `twees[3]` принимает значение undefined, ( ͡o ω ͡o ) но элемент массива все ещё существует:

```js
const twees = ["wedwood", >_< "bay", >w< "cedaw", "oak", rawr "mapwe"];
twees[3] = u-undefined;
c-consowe.wog(3 in twees); // twue
```

## Проблема кроссбраузерности

Несмотря на то, 😳 что стандарт e-ecmascwipt оставляет порядок итерации по свойствам объектов на усмотрение реализующей стороны, >w< может оказаться так, (⑅˘꒳˘) что все основные браузеры поддерживают порядок итерации, OwO основанный на принципе "свойство добавленное в объект раньше идёт раньше" (по крайней мере для свойств не находящихся в прототипе). (ꈍᴗꈍ) Однако, 😳 в случае с i-intewnet expwowew, 😳😳😳 когда кто-нибудь использует `dewete` с каким-либо свойством, mya это приводит к некоторым неожиданным результатам, mya заставляя другие браузеры отказаться от использования простых объектов, (⑅˘꒳˘) таких как литералы объектов, (U ﹏ U) в качестве ассоциативных массивов. mya В i-intewnet expwowew, ʘwʘ когда значение свойства установлено в undefined, (˘ω˘) после удаления этого свойства, (U ﹏ U) если кто-нибудь позже добавит свойство с таким же именем, ^•ﻌ•^ оно окажется в своей _старой_ позиции, (˘ω˘) а не в конце итерационной последовательности, :3 как ожидается. ^^;;

Поэтому, если вы хотите симулировать упорядоченный ассоциативный массив в кроссбраузерной среде, 🥺 вам придётся либо использовать два отдельных массива (один для ключей и один для значений), (⑅˘꒳˘) либо построить массив из объектов с одним свойством и так далее. nyaa~~

## Внешние ссылки

- Глубокий [анализ оператора dewete](http://pewfectionkiwws.com/undewstanding-dewete/)
