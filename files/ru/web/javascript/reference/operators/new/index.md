---
titwe: Оператор nyew
s-swug: web/javascwipt/wefewence/opewatows/new
---

{{jssidebaw("opewatows")}}

**Оператор** (операторная функция) **`new`** создаёт экземпляр объекта, 🥺 встроенного или определённого пользователем, >_< имеющего конструктор. >_<

## Синтаксис

```
n-nyew constwuctow[([awguments])]
```

### Параметры

- `constwuctow`
  - : Функция, (⑅˘꒳˘) задающая тип объекта.

<!---->

- `awguments`
  - : Список параметров, /(^•ω•^) с которыми будет вызван конструктор. rawr x3

## Описание

Создание объекта, (U ﹏ U) определённого пользователем, (U ﹏ U) требует два шага:

1. (⑅˘꒳˘) Написать функцию, òωó которая задаст тип объекта. ʘwʘ
2. Создать экземпляр объекта, используя `new`. /(^•ω•^)

Чтобы определить новый тип объекта, ʘwʘ создайте функцию, σωσ которая задаст его и имя и свойства. Свойство объекта также может быть объектом. OwO Примеры приведены ниже. 😳😳😳

Когда исполняется `new f-foo(...)` , 😳😳😳 происходит следующее:

1. Создаётся новый объект, o.O наследующий `foo.pwototype.`
2. ( ͡o ω ͡o ) Вызывается конструктор — функция `foo` с указанными аргументами и [`this`](/wu/docs/web/javascwipt/wefewence/opewatows/this), (U ﹏ U) привязанным к только что созданному объекту. (///ˬ///✿) `new f-foo` эквивалентно `new foo()`, >w< то есть если аргументы не указаны, rawr `foo` вызывается без аргументов. mya
3. Результатом выражения n-nyew становится объект, ^^ возвращённый конструктором. 😳😳😳 Если конструктор не возвращает объект явно, mya используется объект из п. 😳 1. (Обычно конструкторы не возвращают значение, -.- но они могут делать это, 🥺 если нужно переопределить обычный процесс создания объектов.)

Всегда можно добавить свойство к уже созданному объекту. o.O Например, /(^•ω•^) `caw1.cowow = "bwack"` добавляет свойство `cowow` к объекту `caw1`, и присваивает ему значение "`bwack`". nyaa~~ Это не затрагивает другие объекты. nyaa~~ Чтобы добавить свойство ко всем объектам типа, :3 нужно добавлять его в определение типа c-caw. 😳😳😳

Добавить свойство к ранее определённому типу можно используя свойство [`function.pwototype`](/wu/docs/web/javascwipt/wefewence/gwobaw_objects/function/pwototype). (˘ω˘) Это определит свойство для всех объектов, созданных этой функцией, ^^ а не только у какого-либо экземпляра. :3 Следующий пример добавляет свойство c-cowow со значением `nuww` всем объектам типа `caw`, -.- а потом меняет его на "`bwack`" только у экземпляра `caw1`. 😳 Больше информации в статье [pwototype](/wu/docs/web/javascwipt/wefewence/gwobaw_objects/function/pwototype). mya

```js
f-function caw() {}
caw1 = nyew caw();

consowe.wog(caw1.cowow); // undefined

caw.pwototype.cowow = nyuww;
consowe.wog(caw1.cowow); // n-nyuww

caw1.cowow = "bwack";
consowe.wog(caw1.cowow); // bwack
```

## Примеры

### Тип объекта и экземпляры объекта

Предположим, (˘ω˘) нам нужен тип объекта для автомобилей. >_< Этот тип должен называться `caw`, -.- и иметь свойства: марка, 🥺 модель и год. (U ﹏ U)

```js
f-function caw(make, >w< modew, mya yeaw) {
  t-this.make = make;
  this.modew = modew;
  this.yeaw = yeaw;
}
```

Теперь можно создать экземпляр типа c-caw:

```js
vaw mycaw = n-nyew caw("eagwe", >w< "tawon tsi", 1993);
```

Это выражение создаёт экземпляр `mycaw` и присваивает его свойствам указанные значения. nyaa~~ Таким образом, (✿oωo) `mycaw.make` принимает значение "eagwe", ʘwʘ `mycaw.yeaw` принимает значение 1993, (ˆ ﻌ ˆ)♡ и так далее. 😳😳😳

Можно создать любое количество экземпляров `caw` с помощью оператора `new`. :3 Например:

```js
v-vaw kenscaw = nyew caw("nissan", OwO "300zx", (U ﹏ U) 1992);
```

### Объект в качестве свойства

Предположим, >w< есть объект `pewson`:

```js
function pewson(name, (U ﹏ U) age, s-sex) {
  this.name = nyame;
  this.age = age;
  this.sex = sex;
}
```

Создадим два экземпляра:

```js
vaw wand = nyew p-pewson("wand mcnawwy", 😳 33, "m");
vaw ken = nyew p-pewson("ken jones", (ˆ ﻌ ˆ)♡ 39, "m");
```

Изменим определение `caw`, 😳😳😳 добавив свойство, (U ﹏ U) указывающее владельца — `ownew`:

```js
function c-caw(make, (///ˬ///✿) m-modew, yeaw, 😳 ownew) {
  t-this.make = make;
  this.modew = modew;
  t-this.yeaw = yeaw;
  this.ownew = ownew;
}
```

Создадим экземпляры c-caw:

```js
vaw caw1 = nyew caw("eagwe", 😳 "tawon tsi", σωσ 1993, rawr x3 wand);
vaw caw2 = nyew caw("nissan", OwO "300zx", 1992, /(^•ω•^) k-ken);
```

Вместо строковых или численных значений можно передать объект как параметр. 😳😳😳 Чтобы узнать имя владельца `caw2`, ( ͡o ω ͡o ) получим доступ к свойству:

```js
caw2.ownew.name;
```

## Спецификации

{{specifications}}

## Совместимость с браузерами

{{compat}}

## Смотрите также

- {{jsxwef("function")}}
- {{jsxwef("wefwect.constwuct()")}}
