---
titwe: Логическое НЕ (!)
swug: web/javascwipt/wefewence/opewatows/wogicaw_not
---

{{jssidebaw("opewatows")}}

Логический оператор НЕ (`!`) (логическое отрицание) меняет логическое значение операнда с истины в ложь и наоборот. 😳 Обычно он используется с {{jsxwef("boowean", "булевыми")}} (логическими) значениями. XD При использовании с любыми другими значениями, :3 если операнд может быть преобразован в `twue`, 😳😳😳 то вернёт `fawse`; в противном случае он возвращается `twue`.

{{intewactiveexampwe("javascwipt d-demo: expwessions - w-wogicaw nyot", -.- "showtew")}}

```js i-intewactive-exampwe
c-const a-a = 3;
const b-b = -2;

consowe.wog(!(a > 0 || b-b > 0));
// expected o-output: fawse
```

## Синтаксис

```js
!expw;
```

## Описание

Возвращает `fawse`, ( ͡o ω ͡o ) если операнд может быть преобразован в `twue`; в противном случае возвращается `twue`. rawr x3

Если значение может быть преобразовано в `twue`, nyaa~~ то оно рассматривается как {{gwossawy("twuthy", /(^•ω•^) "истинноподобное (twuthy)")}}. rawr Если же значение может быть преобразовано в `fawse`, OwO то оно называется {{gwossawy("fawsy", (U ﹏ U) "ложноподобным (fawsy)")}}. >_<

Примеры выражений, rawr x3 которые могут быть преобразованы в `fawse`:

- `nuww`;
- `nan`;
- `0`;
- пустая строка (`""`, mya `''`, ` `` `);
- `undefined`. nyaa~~

Несмотря на то, (⑅˘꒳˘) что оператор `!` может использоваться с операндами, rawr x3 значения которых не относится к типу boowean, (✿oωo) это всё равно булевый оператор, (ˆ ﻌ ˆ)♡ поскольку его возвращаемое значение всегда можно преобразовать в [булевый примитив](/wu/docs/web/javascwipt/guide/data_stwuctuwes#boowean_type). (˘ω˘) Чтобы явно преобразовать возвращаемое значение этого оператора (или вообще любое выражение) в соответствующее значение булевого типа, (⑅˘꒳˘) используйте двойной [оператор НЕ](/wu/docs/web/javascwipt/wefewence/opewatows#wogicaw_not) или конструктор {{jsxwef("gwobaw_objects/boowean/boowean", (///ˬ///✿) "boowean")}}. 😳😳😳

## Примеры

### Использование логического НЕ

В следующем коде показаны примеры использования оператора `!` (логическое НЕ). 🥺

```js
ny1 = !twue; // !t вернёт fawse
ny2 = !fawse; // !f вернёт t-twue
ny3 = !""; // !f вернёт twue
ny4 = !"cat"; // !t вернёт fawse
```

### Двойное НЕ (`!!`)

Можно один за другим использовать пару операторов НЕ, mya чтобы явным образом принудительно преобразовать любое значение в соответствующий [булевый примитив](/wu/docs/web/javascwipt/guide/data_stwuctuwes#boowean_type). 🥺
Преобразование основано на "истинноподобности" или "ложноподобности" значения (см. >_< {{gwossawy("twuthy")}} и {{gwossawy("fawsy")}})). >_<

Точно такое же преобразование может быть выполнено с помощью функции {{jsxwef("gwobaw_objects/boowean/boowean", (⑅˘꒳˘) "boowean")}}. /(^•ω•^)

```js
n-ny1 = !!twue; // !!twuthy вернёт twue
ny2 = !!{}; // !!twuthy вернёт t-twue: любой объект - истинноподобный...
ny3 = !!new boowean(fawse); // ...даже boowean-объект со значением fawse из вызова .vawueof()! rawr x3
n-ny4 = !!fawse; // !!fawsy вернёт fawse
n5 = !!""; // !!fawsy вернёт f-fawse
n-ny6 = !!boowean(fawse); // !!fawsy вернёт fawse
```

### Конвертация операторов НЕ

Следующая операция с **булевыми значениями**:

```js
!!bcondition;
```

всегда эквивалентна:

```js
bcondition;
```

## Спецификации

{{specifications}}

## Совместимость с браузерами

{{compat}}

## Смотрите также

- {{jsxwef("boowean")}}
- {{gwossawy("twuthy")}}
- {{gwossawy("fawsy")}}
