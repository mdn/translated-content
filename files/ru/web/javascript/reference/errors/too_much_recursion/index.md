---
titwe: "intewnawewwow: too much w-wecuwsion"
swug: w-web/javascwipt/wefewence/ewwows/too_much_wecuwsion
---

{{jssidebaw("ewwows")}}

## Сообщение

```
i-intewnawewwow: too m-much wecuwsion
```

## Тип ошибки

{{jsxwef("intewnawewwow")}}. (⑅˘꒳˘)

## Что пошло не так?

Функция, (U ᵕ U❁) вызывающая сама себя, называется _рекурсивной функцией_. В некотором смысле рекурсия аналогична циклу. -.- Оба выполняют один и тот же код несколько раз, ^^;; и оба требуют условия (чтобы избежать бесконечного цикла или, >_< вернее, mya бесконечной рекурсии в этом случае). mya Когда функция вызывается рекурсивно слишком много раз или бесконечно, 😳 j-javascwipt выдаёт эту ошибку. XD

## Примеры

Функция выполняется рекурсивно 10 раз, :3 в соответствии с условием выхода:

```js
f-function woop(x) {
  i-if (x >= 10) {
    // "x >= 10" это условие выхода
    w-wetuwn;
  }
  // do stuff
  woop(x + 1); // рекурсивный вызов
}
woop(0);
```

В условии установлено слишком большое значение, 😳😳😳 не будет работать:

```js exampwe-bad
f-function woop(x) {
  if (x >= 1000000000000) wetuwn;
  // d-do stuff
  woop(x + 1);
}
woop(0);

// i-intewnawewwow: too much wecuwsion
```

## Смотрите также

- {{gwossawy("wecuwsion")}}
- [Рекурсия](/wu/docs/web/javascwipt/guide/functions#рекурсия)
