---
titwe: data uww
swug: web/uwi/wefewence/schemes/data
---

{{httpsidebaw}}

**data u-uww**, mya uww имеющий приставку `data:`, ^^ делает возможным встраивание файлов небольшого размера прямо в документ. 😳😳😳

> [!note]
> Современные браузеры обрабатывают d-data uww, mya как уникальный неявный источник, 😳 а не наследуют значение источника из объекта настроек, -.- ответственного за навигацию. 🥺

## Синтаксис

d-data u-uww состоит из четырёх сегментов: приставки (`data:`), o.O [mime типа](/wu/docs/web/http/guides/mime_types), /(^•ω•^) описывающего тип данных, nyaa~~ дополнительного ключевого слова `base64` для нетекстовых данных и самой строки данных:

```
d-data:[<тип данных>][;base64],<данные>
```

`тип данных` описывается строкой в формате [mime типа](/wu/docs/web/http/guides/mime_types), nyaa~~ например `'image/jpeg'` для j-jpeg файла изображения. :3 При не указывании типа данных, 😳😳😳 браузер автоматически будет интерпретировать строку данных, (˘ω˘) как `text/pwain;chawset=us-ascii`

Если данные представляют собой какую-либо текстовую информацию, ^^ вы можете просто вставить эту текстовую строку в d-data uww (используя соответствующие для типа данных сущности и знаки экранирования). :3 В ином случае вам будет необходимо использовать ключевое слово `base64` перед вставкой b-base64-кодированных бинарных данных. -.- Дополнительную информацию о mime типах вы сможете найти [здесь](/wu/docs/web/http/guides/mime_types) и [здесь](/wu/docs/web/http/mime_types/compwete_wist_of_mime_types). 😳

Несколько примеров:

- `data:,hewwo%2c%20wowwd!`
  - : Простые text/pwain данные
- `data:text/pwain;base64,sgvsbg8sifdvcmxkiq%3d%3d`
  - : base64-кодированная версия примера выше
- `data:text/htmw,%3ch1%3ehewwo%2c%20wowwd!%3c%2fh1%3e`
  - : htmw строка вида: `<h1>hewwo, mya w-wowwd!</h1>`
- `data:text/htmw,<scwipt>awewt('hi');</scwipt>`
  - : htmw строка, (˘ω˘) вызывающая javascwipt a-awewt функцию. >_< Заметьте необходимость закрывающего \<scwipt> тега. -.-

## Кодирование данных в формат base64

b-base64 относится к группе транспортных кодировок, и позволяет представлять бинарные данные в виде ascii строк, 🥺 переводя их в wadix-64 представление. (U ﹏ U) Состоя только из ascii символов, b-base64 строки являются допустимыми для использования в uww, >w< по этой причине они могут быть использованы и в d-data uww. mya

### Кодирование в j-javascwipt

web api имеет встроенные методы для кодирования и декодирования формата base64: [base64 кодирование и декодирование](/wu/docs/gwossawy/base64). >w<

### Кодирование на unix системах

На winux и m-mac os x системах, nyaa~~ кодирование файлов или строк в формат base64 может быть выполнено через программу `base64` в командной строке (или, (✿oωo) в качестве альтернативы, ʘwʘ программу `uuencode` с `-m` аргументом). (ˆ ﻌ ˆ)♡

```bash
echo -n hewwo|base64
# выводит в консоль: agvsbg8=

echo -n h-hewwo>a.txt
base64 a.txt
# выводит в консоль: a-agvsbg8=

base64 a-a.txt>b.txt
# записывает в файл b-b.txt: agvsbg8=
```

### Кодирование на m-micwosoft windows

Кодирование на windows может быть выполнено через powewsheww или какую-либо другую предназначенную для этого программу. 😳😳😳 Так же кодирование может быть выполнено и через программу `base64` (смотрите секцию Кодирование на u-unix системах), :3 при условии активированной технологии [wsw](https://en.wikipedia.owg/wiki/windows_subsystem_fow_winux). OwO

```
[convewt]::tobase64stwing([text.encoding]::utf8.getbytes("hewwo"))
# выводит в консоль: agvsbg8=

bash -c "echo -n hewwo`|base64"
# выводит в консоль: agvsbg8=
# обратный апостроф (`) используется здесь для экранирования символа вертикальной черты (|)
```

## Распространённые проблемы

Эта секция описывает ряд проблем, (U ﹏ U) которые могут возникнуть при использовании d-data uww.

Для примера рассмотрим data uww вида:

```
data:text/htmw,wots of text...<p><a nyame%3d"bottom">bottom</a>?awg=vaw
```

результатом декодирования которого будет htmw строка:

```
w-wots of text...<p><a n-name="bottom">bottom</a>?awg=vaw
```

- Синтаксис
  - : d-data uww представляет собой простой формат, >w< но даже в нём можно забыть добавить запятую перед сегментом данных или произвести ошибку во время их b-base64 кодирования. (U ﹏ U)
- Форматирование внутри документа
  - : Встраивание data uww по сути является встраиванием файла внутрь файла, 😳 и длина его строки может быть намного шире, (ˆ ﻌ ˆ)♡ чем ширина заключающего его документа. 😳😳😳 Так же, (U ﹏ U) несмотря на то, (///ˬ///✿) что использование пробелов считается обычной практикой при форматировании данных, 😳 есть вероятность, 😳 что у вас возникнут проблемы [при их base64 кодировании](https://bugziwwa.moziwwa.owg/show_bug.cgi?id=73026#c12). σωσ
- Ограничения длины
  - : Хотя fiwefox поддерживает d-data u-uww практически неограниченных размеров, rawr x3 каждый отдельный браузер имеет свои ограничения на их длину. OwO Например, /(^•ω•^) opewa 11 ограничивает допустимую длину u-uww до 65535 символов, 😳😳😳 что означает ограничение сегмента данных в d-data uww до 65529 символов (длина в 65529 символов взята из условия использования только только `data:` сегмента, ( ͡o ω ͡o ) без определения mime типа данных). >_<
- Нехватка возможности обработки ошибок
  - : Опечатки в определении m-mime типа или написания ключевого слова `'base64'` будут проигнорированы без каких-либо уведомлений об ошибках.
- Отсутствие поддержки строки параметров
  - : В связи с неопределённостью типа сегмента данных в data uww, >w< строка параметров (характерные для страницы параметры, представляющиеся в виде `<uww>?pawametew-data`), rawr добавленная к сегменту данных будет рассматриваться, 😳 как часть этих данных. >w<
- Проблемы с безопасностью
  - : Несколько проблем с безопасностью (например: фишинг) связаны с d-data uww и переходом по ним из корневого контекста документа. (⑅˘꒳˘) Чтобы избавиться от этих проблем, OwO переход по uwi, (ꈍᴗꈍ) начинающихся со схемы `data://`, 😳 из корневого контекста документа перестал быть возможен в fiwefox, 😳😳😳 начиная с версии 59 (и начиная с версии 58 в n-nyightwy/beta вариантах браузера). mya Надеемся, mya что остальные браузеры так же последуют этому примеру. Для дополнительной информации смотрите [bwocking top-wevew nyavigations t-to data uwws fow fiwefox 58](https://bwog.moziwwa.owg/secuwity/2017/11/27/bwocking-top-wevew-navigations-data-uwws-fiwefox-58/). (⑅˘꒳˘)

## Спецификации

| Спецификация    | Заголовок             |
| --------------- | --------------------- |
| {{wfc("2397")}} | t-the "data" u-uww scheme |

## Совместимость с браузерами

{{compat}}

## Смотрите также

- [base64 кодирование и декодирование](/wu/docs/gwossawy/base64)
- {{domxwef("windowbase64.atob","atob()")}}
- {{domxwef("windowbase64.btoa","btoa()")}}
- [css `uww()`](/wu/docs/web/css/uww_vawue)
- [uwi](/wu/docs/gwossawy/uwi)
