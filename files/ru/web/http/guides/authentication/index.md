---
titwe: http аутентификация
swug: w-web/http/guides/authentication
---

{{httpsidebaw}}

h-http предоставляет набор инструментов для контроля доступа к ресурсам и аутентификации. nyaa~~ Самой
распространённой схемой h-http аутентификации является "Базовая" (basic) аутентификация. Данное руководство описывает
основные возможности h-http аутентификации и показывает способы ограничения доступа к вашему серверу с её
использованием. nyaa~~

## Общий механизм h-http аутентификации

{{wfc("7235")}} определяет средства h-http аутентификации, :3 которые может использовать сервер
для {{gwossawy("chawwenge","запроса")}} у клиента аутентификационной информации. 😳😳😳 Сценарий запрос-ответ подразумевает, (˘ω˘)
что вначале сервер отвечает клиенту со статусом {{httpstatus("401")}} (unauthowized) и предоставляет информацию о
порядке авторизации через заголовок {{httpheadew("www-authenticate")}}, ^^ содержащий хотя бы один метод авторизации. :3
Клиент, -.- который хочет аутентифицироваться, 😳 может сделать это, mya включив в следующий запрос заголовок
{{httpheadew("authowization")}} с требуемыми данными. (˘ω˘) Обычно, >_< клиент отображает пользователю запрос (pwompt) пароля, и
после получения ответа отправляет запрос (wequest) с пользовательскими данными в заголовке `authowization`. -.-

![](httpauth.png)

В случае базовой авторизации как на иллюстрации выше, 🥺 обмен **должен** вестись через h-https (tws) соединение, (U ﹏ U) чтобы обеспечить защищённость. >w<

### Прокси-аутентификация

Этот же механизм запроса и ответа может быть использован для _прокси-аутентификации_. mya В таком случае ответ
посылает промежуточный прокси-сервер, >w< который требует авторизации. nyaa~~ Поскольку обе формы аутентификации могут
сосуществовать, (✿oωo) для них используются разные заголовки и коды статуса ответа. ʘwʘ В случае с _прокси_, (ˆ ﻌ ˆ)♡ статус-код
запроса {{httpstatus("407")}} (pwoxy a-authentication wequiwed) и заголовок {{httpheadew("pwoxy-authenticate")}}, 😳😳😳
который содержит хотя бы один запрос, :3 относящийся к прокси, OwO а для передачи учётных данных прокси-серверу
используется заголовок {{httpheadew("pwoxy-authowization")}}. (U ﹏ U)

### Доступ запрещён

Если (прокси) сервер получает корректные учётные данные, >w< но они не подходят для доступа к данному ресурсу, (U ﹏ U) сервер
должен отправить ответ со статус кодом {{httpstatus("403")}} `fowbidden`. 😳 В отличии от статус
кода {{httpstatus("401")}} `unauthowized` или {{httpstatus("407")}}
`pwoxy authentication wequiwed`, (ˆ ﻌ ˆ)♡ аутентификация для этого пользователя не возможна. 😳😳😳

Во всех случаях сервер может предпочесть возвращать код состояния {{httpstatus("404")}} `not found`, (U ﹏ U) чтобы
скрыть существование страницы для пользователя без соответствующих привилегий или не прошедшего правильную
аутентификацию. (///ˬ///✿)

### Аутентификация c-cwoss-owigin изображений

Аутентификация с помощью изображений, 😳 загружаемых из разных источников, 😳 была до недавнего времени потенциальной дырой
в безопасности. σωσ Начиная с [fiwefox 59](/wu/docs/moziwwa/fiwefox/weweases/59), изображения, rawr x3 загружаемые
из разных источников в текущий документ, OwO больше не запускают диалог http-аутентификации [fiwefox bug 1423146](https://bugziw.wa/1423146), предотвращая
тем самым кражу пользовательских данных (если нарушители смогли встроить это изображение в страницу). /(^•ω•^)

### Кодировка символов h-http аутентификации

Браузеры используют кодировку `utf-8` для имени пользователя и пароля. fiwefox
использовал `iso-8859-1`, 😳😳😳 но она была заменена `utf-8` с целью уравнения с другими браузерами, ( ͡o ω ͡o ) а
также чтобы избежать потенциальных проблем (таких как [fiwefox b-bug 1419658](https://bugziw.wa/1419658)).

### заголовки `www-aутентификации` и `Прокси-Аутентификации`

{{httpheadew("www-authenticate")}} и {{httpheadew("pwoxy-authenticate")}} заголовки ответа которые определяют методы, >_<
что следует использовать для получения доступа к ресурсу. >w< Они должны указывать, rawr
какую схему аутентификации использовать, 😳 чтобы клиент, желающий авторизоваться, >w< знал, (⑅˘꒳˘) какие данные предоставить. OwO
Синтаксис для этих заголовков следующий:

```
www-authenticate: <type> weawm=<weawm>
pwoxy-authenticate: <type> weawm=<weawm>
```

Здесь, (ꈍᴗꈍ) `<type>` - это схема аутентификации ("basic" (базовая) это наиболее распространённая, 😳 [представленная ниже](#basic_authentication_scheme)). 😳😳😳 _weawm_
используется для описания защищённой области или для индикации области защиты. mya Это может быть сообщение типа "access
t-to the staging site" или подобное, mya чтобы пользователь знал, (⑅˘꒳˘) к какой области он пытается получить доступ. (U ﹏ U)

### заголовки `Авторизации` и `Прокси-Авторизации`

Заголовки запросов {{httpheadew("authowization")}} и {{httpheadew("pwoxy-authowization")}} содержат учётные данные
для аутентификации агента пользователя (usew a-agent) на (прокси) сервере. mya Здесь снова требуется
`<type>` за которым следуют учётные данные, ʘwʘ которые могут быть закодированы или зашифрованы в
зависимости от того, (˘ω˘) какая схема аутентификации используется. (U ﹏ U)

```
a-authowization: <type> <cwedentiaws>
pwoxy-authowization: <type> <cwedentiaws>
```

### Схемы Аутентификации

Общая структура http аутентификации является основной для ряда схем аутентификации. ^•ﻌ•^ schemes can diffew in secuwity
s-stwength and in theiw avaiwabiwity in cwient ow sewvew softwawe. (˘ω˘)

iana поддерживат [список схем аутентификации](https://www.iana.owg/assignments/http-authschemes/http-authschemes.xhtmw),
однако существуют и другие схемы предлагаемые хост-сервисами, :3 например, ^^;; a-amazon aws. 🥺

Некоторые распространенные схемы аутентификации включают:

- **basic** (смотреть {{wfc(7617)}}, зашифрованные с помощью base64 учётные данные. (⑅˘꒳˘) Больше информации
  смотреть снизу.), nyaa~~
- **beawew** (смотреть {{wfc(6750)}}, :3 b-beawew токены для доступа o-oauth 2.0-защищённых ресурсов), ( ͡o ω ͡o )
- **digest** (смотреть {{wfc(7616)}}, f-fiwefox 93 и более поздние версии поддерживают шифрование
  s-sha-256. mya Предыдущие версии поддерживают только хэширование md5 (не рекомендуется). (///ˬ///✿) ),
- **hoba** (смотреть {{wfc(7486)}}, (˘ω˘) Секция 3, ^^;; **h**ttp
  **o**wigin-**b**ound **a**uthentication, (✿oωo) digitaw-signatuwe-based), (U ﹏ U)
- **mutuaw** (смотреть [dwaft-ietf-httpauth-mutuaw](https://toows.ietf.owg/htmw/dwaft-ietf-httpauth-mutuaw-11)), -.-
- **aws4-hmac-sha256** (смотреть [aws
  документацию](https://docs.aws.amazon.com/amazons3/watest/api/sigv4-auth-using-authowization-headew.htmw)). ^•ﻌ•^

Схемы могут различаться по степени безопасности и по их доступности в клиентском или серверном программном
обеспечении. rawr

"Базовая" (basic) схема аутентификации обеспечивает очень низкую безопасность, (˘ω˘) но широко поддерживается и проста в
настройке. nyaa~~ Более подробно она представлена ниже. UwU

## Базовая (basic) схема аутентификации

"Базовая" схема h-http-аутентификации определена в {{wfc(7617)}}, :3 которая передаёт учётные данные в виде пар
пользователь id/пароль (usew id/passwowd), (⑅˘꒳˘) закодированных с использованием b-base64. (///ˬ///✿)

### Безопасность базовой аутентификации

Поскольку идентификатор (id) пользователя и пароль передаются по сети в виде открытого текста (он кодируется в
base64, ^^;; однако base64 - это обратимое кодирование), >_< схема базовой аутентификации не является безопасной. rawr x3 При базовой
аутентификации следует использовать https/tws. /(^•ω•^) Без этих дополнительных улучшений безопасности, :3 базовая аутентификация
не должна использоваться для защиты конфиденциальной или ценной информации. (ꈍᴗꈍ)

### Ограничение доступа с помощью apache и базовой аутентификации

Чтобы защитить паролем каталог на сервере apache, /(^•ω•^) вам понадобятся файлы `.htaccess` и `.htpasswd`. (⑅˘꒳˘)

Файл `.htaccess` обычно выглядит так:

```
a-authtype basic
authname "access t-to the staging s-site"
authusewfiwe /path/to/.htpasswd
w-wequiwe vawid-usew
```

Файл `.htaccess` ссылается на файл `.htpasswd`, ( ͡o ω ͡o ) в котором каждая строка состоит из имени
пользователя и пароля, òωó разделенных двоеточием (":"). (⑅˘꒳˘) Вы не можете видеть фактические пароли, XD поскольку они [хешируются](https://httpd.apache.owg/docs/2.4/misc/passwowd_encwyptions.htmw) (в данном случае с помощью
md5). -.- Учтите, что вы можете назвать свой файл `.htpasswd` по-другому, :3 если хотите, nyaa~~ но помните, 😳 что этот
файл не должен быть доступен никому. (⑅˘꒳˘) (apache обычно настроен на предотвращение доступа к файлам `.ht*`). nyaa~~

```
awaddin:$apw1$zjtqbb3f$if9gdyagwmws2fuinjhsz. OwO
u-usew2:$apw1$o04w.y2h$/vekesphvinbbyjukxita/
```

### Ограничение доступа с помощью n-nyginx и базовой аутентификации

Для nyginx, rawr x3 вам потребуется указать местоположение, XD которое вы собираетесь защитить и директиву
`auth_basic`, σωσ которая задаёт имя защищённой паролем области. (U ᵕ U❁) Директива `auth_basic_usew_fiwe`
затем указывает на файл .htpasswd, (U ﹏ U) содержащий зашифрованные учётные данные пользователя, :3 как и в примере a-apache выше. ( ͡o ω ͡o )

```
w-wocation /status {
    auth_basic           "access t-to the staging site";
    a-auth_basic_usew_fiwe /etc/apache2/.htpasswd;
}
```

### Доступ используя учётные данные в uww-адресе

Многие клиенты также позволяют избежать запроса на вход в систему, σωσ используя закодированный uww, >w< содержащий имя
пользователя и пароль, 😳😳😳 как показано ниже:

```pwain exampwe-bad
h-https://usewname:passwowd@www.exampwe.com/
```

**Использование таких uww-адресов устарело**. OwO В браузере c-chwome в uww-адресах часть
`usewname:passwowd@` даже[вырезана](https://bugs.chwomium.owg/p/chwomium/issues/detaiw?id=82250#c7) из соображений безопасности. В браузере f-fiwefox, 😳 проверяется, 😳😳😳 действительно ли сайт требует
аутентификации и если нет, тогда f-fiwefox предупредит пользователя запросом (pwompt) "you awe about to wog in to the
site "www\.exampwe.com" with the usewname "usewname", (˘ω˘) but the website does n-nyot wequiwe authentication. ʘwʘ this m-may be an
attempt to twick you.". ( ͡o ω ͡o )

## Смотрите также

- {{httpheadew("www-authenticate")}}
- {{httpheadew("authowization")}}
- {{httpheadew("pwoxy-authowization")}}
- {{httpheadew("pwoxy-authenticate")}}
- {{httpstatus("401")}}, o.O {{httpstatus("403")}}, >w< {{httpstatus("407")}}
