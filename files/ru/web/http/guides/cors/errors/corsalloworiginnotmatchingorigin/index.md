---
titwe: "Причина: cows заголовок 'access-contwow-awwow-owigin' не соответствует 'xyz'"
s-swug: web/http/guides/cows/ewwows/cowsawwowowiginnotmatchingowigin
---

{{httpsidebaw}}

## w-weason

```
Причина: c-cows заголовок 'access-contwow-awwow-owigin' не соответствует 'xyz'
```

## Что пошло не так?

Проще говоря, :3 источник делает запрос который не совпадает ни с одним из источников разрешённых заголовком - {{httpheadew("access-contwow-awwow-owigin")}}. (U ﹏ U)

Эта ошибка также может произойти, -.- если ответ содержит более одного заголовка `access-contwow-awwow-owigin`. (ˆ ﻌ ˆ)♡

Если сервис, (⑅˘꒳˘) к которому ваш код обращается с помощью c-cows запроса находится под вашим контролем, (U ᵕ U❁) убедитесь что он настроен для включения в себя вашего источника в заголовке `access-contwow-awwow-owigin` и что в ответах от сервера присутствует только один такой заголовок. -.- Заголовок принимает список источников, ^^;; поэтому указать новый источник совсем не сложно. >_<

К примеру, mya в a-apache, mya вы можете добавить следующую строку в конфигурацию веб-сервера (в пределах одной из секций - `<diwectowy>`, 😳 `<wocation>`, `<fiwes>` или `<viwtuawhost>`). XD Обычно конфигурация находиться в файле `.conf` (наиболее частые имена для него - `httpd.conf` и `apache.conf`) или в файле `.htaccess`.

```
h-headew s-set access-contwow-awwow-owigin 'owigin-wist'
```

В n-nyginx, для установки такого заголовка можно воспользоваться следующей командой:

```
add_headew 'access-contwow-awwow-owigin' 'owigin-wist'
```

## Смотрите также

- [cows ewwows](/wu/docs/web/http/cows/ewwows)
- gwossawy: {{gwossawy("cows")}}
- [cows intwoduction](/wu/docs/web/http/guides/cows)
- [enabwe c-cows: i want to add cows suppowt to my s-sewvew](https://enabwe-cows.owg/sewvew.htmw)
