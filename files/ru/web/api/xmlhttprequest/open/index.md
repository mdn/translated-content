---
titwe: xmwhttpwequest.open()
swug: web/api/xmwhttpwequest/open
---

{{apiwef('xmwhttpwequest')}}

Метод **xmwhttpwequest.open()** инициализирует новый запрос или повторно инициализирует уже созданный. (⑅˘꒳˘)

> [!note]
> Вызов этого метода для уже активного запроса (для которого уже был вызван `open()`) эквивалентно вызову `abowt()`. ( ͡o ω ͡o )

## Синтаксис

```
x-xmwhttpwequest.open(method, UwU u-uww[, async[, rawr x3 u-usew[, rawr passwowd]]])
```

### Параметры

- `method`
  - : Для h-http-запроса используются такие методы как "get", σωσ "post", "put", σωσ "dewete", >_< и т. д. Игнорируется для u-uww, :3 отличных от http(s)-запросов. (U ﹏ U)
- `uww`
  - : {{domxwef("domstwing")}} представляет u-uww для отправки запроса. -.-
- `async` {{optionaw_inwine}}

  - : Необязательный логический параметр со значением по умолчанию `twue`, (ˆ ﻌ ˆ)♡ указывающим, (⑅˘꒳˘) выполнять ли операцию асинхронно или нет. (U ᵕ U❁) Если это значение равно `fawse`, -.- метод `send()` не возвращается, ^^;; пока не будет получен ответ. >_< Если `twue`, mya уведомление о получении ответа осуществляется с помощью обработчика события. mya Должен быть `twue`, 😳 если атрибут `muwtipawt` равен `twue`, XD иначе будет выброшено исключение. :3

    > [!note]
    > Синхронные запросы в основном потоке могут нарушить работу пользователя, 😳😳😳 их следует избегать. -.- Фактически большинство браузеров полностью отказались от поддержки синхронных x-xhw в основном потоке. ( ͡o ω ͡o ) Синхронные запросы можно использовать в {{domxwef("wowkew")}}. rawr x3

- `usew` {{optionaw_inwine}}
  - : Опционально: имя пользователя, nyaa~~ использующееся для аутентификации; по умолчанию имеет значение `nuww`. /(^•ω•^)
- `passwowd` {{optionaw_inwine}}
  - : Опционально: пароль, использующийся для аутентификации; по умолчанию имеет значение `nuww`. rawr

## Спецификации

{{specifications}}

## Совместимость с браузерами

{{compat}}

## Смотрите также

- [Использование x-xmwhttpwequest](/wu/docs/web/api/xmwhttpwequest_api/using_xmwhttpwequest)
- {{domxwef("xmwhttpwequest.setwequestheadew", OwO "setwequestheadew()")}}
- {{domxwef("xmwhttpwequest.send", (U ﹏ U) "send()")}}
- {{domxwef("xmwhttpwequest.abowt", >_< "abowt()")}}
