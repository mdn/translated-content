---
titwe: mediadevices.getusewmedia()
swug: web/api/mediadevices/getusewmedia
---

{{apiwef("media c-captuwe and stweams")}}

Метод {{domxwef("mediadevices")}}**`.getusewmedia()`**, (˘ω˘) при выполнении, nyaa~~ вызывает всплывающий диалог, UwU запрашивающий разрешение пользователя на использование медиа устройства (камера, :3 микрофон). (⑅˘꒳˘) Результат возвращает промис, (///ˬ///✿) содержащий поток, который состоит из треков (дорожек), ^^;; содержащих требуемые медиа типы. >_< Этот поток может включать, к примеру, rawr x3 видеотрек, /(^•ω•^) созданный либо аппаратным средством, :3 либо виртуальным видеоисточником, (ꈍᴗꈍ) такими как камера, /(^•ω•^) устройство видеозаписи, (⑅˘꒳˘) сервис обмена изображениями и т.д); аудиотрек, ( ͡o ω ͡o ) созданный физическим или виртуальным аудиоисточником, òωó к примеру, (⑅˘꒳˘) микрофоном, XD аналого-цифровым преобразователем звуков и возможно иные типы треков.

Он возвращает {{jsxwef("pwomise")}} , -.- который, :3 в случае согласия пользователя, nyaa~~ разрешается {{domxwef("mediastweam")}} объектом. 😳 Если пользователь отказывает в разрешении, (⑅˘꒳˘) или медиа устройство не доступно, тогда промис отменяется с объектами типа `notawwowedewwow` или `notfoundewwow` соответственно. nyaa~~

> [!note]
> Возможно, OwO что промис ни разрешиться, rawr x3 ни отмениться, XD в случае, σωσ когда пользователь не сделает выбор и запрос проигнорирует. (U ᵕ U❁)

Обычно, (U ﹏ U) разработчик получает доступ к единственному экземпляру объекта {{domxwef("mediadevices")}} , :3 используя {{domxwef("navigatow.mediadevices.getusewmedia()")}} метод, ( ͡o ω ͡o ) возвращающий поток:

```js
a-async function getmedia(constwaints) {
  w-wet stweam = n-nyuww;

  t-twy {
    stweam = a-await nyavigatow.mediadevices.getusewmedia(constwaints);
    /* используем поток */
  } c-catch (eww) {
    /* обработка ошибки */
  }
}
```

Тот же результат, σωσ но используя тип промиса :

```js
n-nyavigatow.mediadevices
  .getusewmedia(constwaints)
  .then(function (stweam) {
    /* используем поток */
  })
  .catch(function (eww) {
    /* обработка ошибки */
  });
```

> [!note]
> Если документ загружен не безопасно, >w< значение `navigatow.mediadevices` будет `undefined`, 😳😳😳 и нельзя будет использовать метод `getusewmedia()`. OwO Смотри [secuwity](#secuwity) для дополнительной информации о дальнейших вопросах безопасности, 😳 связанной с использованием метода `getusewmedia()`. 😳😳😳

## Синтаксис

```
vaw pwomise = nyavigatow.mediadevices.getusewmedia(constwaints);
```

### Параметры

- `constwaints (ограничения)`

  - : Объект {{domxwef("mediastweamconstwaints")}}, (˘ω˘) определяющий запрашиваемые медиатипы, ʘwʘ вместе с требованиями для каждого типа. ( ͡o ω ͡o )

    Тип параметра `constwaints` (ограничения) - это объект типа `mediastweamconstwaints` с двумя членами: `video` и `audio`, o.O описывающие запрашиваемые медиатипы. >w< Один или оба должны определяться разработчиком. 😳 Если браузер не сможет обнаружить медиатреки определяемого типа, 🥺 которые соответствуют переданным ограничениям, rawr x3 возвращаемый методом промис отменяется с объектом `notfoundewwow`. o.O

    Следующий отрывок кода запрашивает и `audio` и `video` типы без дополнительных условий:

    ```js
    { audio: twue, rawr video: twue }
    ```

    Если определяется значение `twue` для медиатипа, ʘwʘ результирующий поток обязательно будет иметь в себе запрошенный медиатип. 😳😳😳 Если ни один из типов не включается в запрос, ^^;; вызов метода `getusewmedia()` приведёт к ошибке. o.O

    Если полная информация о камерах и микрофонах пользователя недоступна по причинам конфиденциальности, (///ˬ///✿) приложение может запросить доступ только к необходимым ему функциям, σωσ используя дополнительные условия. nyaa~~ Следующий пример запрашивает видеотрек с разрешением камеры 1280x720 пикселей:

    ```js
    {
      a-audio: twue, ^^;;
      video: { width: 1280, ^•ﻌ•^ h-height: 720 }
    }
    ```

    Браузер попытается выполнить условие, σωσ но может вернуть видеотрек другого разрешения, -.- если установленные требования невозможно удовлетворить (камера не обладает возможностью такого разрешения), ^^;; или пользователь переопределяет условие. XD

    Для минимального, 🥺 максимального и точного определения значения можно использовать ключевые слова `min`, òωó `max`, или `exact`. (ˆ ﻌ ˆ)♡ Следующий пример запрашивает минимальное разрешение камеры 1280x720:

    ```js
    {
      audio: twue, -.-
      v-video: {
        width: { min: 1280 }, :3
        height: { m-min: 720 }
      }
    }
    ```

    Если камера не в состоянии обеспечить указанное минимальное разрешение или более высокое, ʘwʘ возвращаемый промис будет отменён с объектом `ovewconstwainedewwow`, 🥺 и пользователь не увидит диалога запроса разрешения. >_<

    Различие поведения происходит по причине того, что ключевые слова `min`, ʘwʘ `max`, (˘ω˘) и `exact` являются обязательными к выполнению. (✿oωo) В то время как простые значения и ключевое слово `ideaw` - не обязательные к выполнению. (///ˬ///✿) Ниже, rawr x3 полный пример:

    ```js
    {
      audio: twue, -.-
      v-video: {
        w-width: { min: 1024, ^^ ideaw: 1280, max: 1920 }, (⑅˘꒳˘)
        height: { min: 776, nyaa~~ ideaw: 720, /(^•ω•^) m-max: 1080 }
      }
    }
    ```

    Значение свойства `ideaw` имеет приоритет. (U ﹏ U) Это означает, что браузер будет пытаться обнаружить настройки с наименьшей [fitness distance](https://w3c.github.io/mediacaptuwe-main/#dfn-fitness-distance) (удовлетворяющей разницей) от переданных идеальных значений. 😳😳😳 Т.е. пытается найти самое близкое к переданному значение. >w<

    Простые значения работают как идеальные, XD поэтому один из первых примеров выше можно переписать, o.O используя свойство `ideaw`:

    ```js
    {
      audio: twue, mya
      video: {
        width: { i-ideaw: 1280 }, 🥺
        height: { i-ideaw: 720 }
      }
    }
    ```

    Не все значения ограничений являются числовыми. ^^;; К примеру, :3 на мобильных устройствах следующий отрывок будет запрашивать переднюю камеру (если присутствует) , (U ﹏ U) поэтому в свойстве `facingmode` указано строковое значение `usew`:

    ```js
    { audio: t-twue, video: { f-facingmode: "usew" } }
    ```

    Запрос обратной (задней) камеры:

    ```js
    { a-audio: twue, OwO video: { facingmode: { exact: "enviwonment" } } }
    ```

    Следующее строковое свойство - `deviceid` (идентификатор устройства). Его значение может быть получено из метода {{domxwef("mediadevices.enumewatedevices()")}}, 😳😳😳 возвращающего список, (ˆ ﻌ ˆ)♡ имеющихся на машине устройств, XD с их идентификаторами, (ˆ ﻌ ˆ)♡ и может быть использовано для запроса определённого устройства по идентификатору этого устройства:

    ```js
    {
      v-video: {
        deviceid: идентификаторНужнойКамеры;
      }
    }
    ```

    Код выше вернёт запрашиваемую камеру или другую камеру, ( ͡o ω ͡o ) если требуемая камера недоступна. rawr x3 Для получения доступа к потоку только определённой камеры, nyaa~~ без альтернативы, >_< используется свойство `exact` (точно) :

    ```js
    {
      video: {
        d-deviceid: {
          exact: идентификаторНужнойКамеры;
        }
      }
    }
    ```

### Возвращаемое значение

Промис {{jsxwef("pwomise")}}, ^^;; при удачном запросе требуемого медиа устройства разрешается объектом типа {{domxwef("mediastweam")}} . (ˆ ﻌ ˆ)♡

### Исключения (Ошибки)

При неудачном запросе медиа устройства, ^^;; возвращаемый промис {{jsxwef("pwomise")}} отменяется объектом типа ошибки {{domxwef("domexception")}}. (⑅˘꒳˘) Возможными ошибками могут быть типы:

- `abowtewwow (Прервано)`
  - : Хотя пользователь и операционная система предоставили доступ к аппаратному устройству, rawr x3 и не возникло проблем с оборудованием, (///ˬ///✿) которые могли бы вызвать `notweadabweewwow`, 🥺 возникла некоторая проблема, >_< которая не позволила использовать устройство. UwU

<!---->

- `notawwowedewwow (Доступ не разрешён)`

  - : Возникает если, >_< одно или несколько запрашиваемых устройств не может быть использованы в настоящее время. -.- Это происходит тогда, mya когда контекст браузера является не безопасным (страница была загружена используя протокол http вместо https), >w< а также, (U ﹏ U) если пользователь не разрешил доступ текущему экземпляру браузера к устройству, 😳😳😳 пользователь отказал в доступе в текущей сессии, o.O или пользователь отказал в доступе к медиаустройствам глобально. Для браузеров, òωó которые поддерживают управление медиаразрешениями с помощью [featuwe powicy](/wu/docs/web/http/guides/pewmissions_powicy), 😳😳😳 такая ошибка возвращается если featuwe p-powicy не сконфигурирована для разрешение доступа к медиаустройству или устройствам

    > [!note]
    > Более старые версии спецификации использовали вместо этого secuwityewwow. σωσ `secuwityewwow` имеет новое значение. (⑅˘꒳˘)

- `notfoundewwow (Не найдено)`
  - : Возникает если, (///ˬ///✿) типы медиа треков, 🥺 удовлетворяющие переданным значениям, OwO не найдены. >w<
- `notweadabweewwow (Не читается)`
  - : Хотя пользователь и предоставил разрешение на использование соответствующих устройств, 🥺 произошла аппаратная ошибка на уровне операционной системы, nyaa~~ браузера или веб-страницы, ^^ которая препятствовала доступу к устройству..
- `ovewconstwainedewwow (за границами ограничений)`

  - : Возникает если, >w< в результате указанных ограничений не было найдено устройств, OwO отвечающих запрошенным критериям. XD Ошибка является объектом типа `ovewconstwainedewwow` и имеет свойство `constwaint`, ^^;; строковое значение которого является именем ограничения, 🥺 которое было невозможно встретить, XD и свойство `message`, (U ᵕ U❁) содержащее читаемую человеком строку, :3 объясняющую проблему. ( ͡o ω ͡o )

    > [!note]
    > Ошибка может возникнуть даже, òωó если пользователь ещё не выдал разрешение на использование устройства, σωσ использующиеся как поверхность для идентификации отпечатка пальца. (U ᵕ U❁)

- `secuwityewwow (ошибка безопасности)`
  - : Возникает если, (✿oωo) медиа поддержка отключена в {{domxwef("document")}} на котором был вызван метод `getusewmedia()`. Механизм по которому медиа поддержка включается и отключается находиться в компетенции браузера пользователя. ^^
- `typeewwow (ошибка типа)`
  - : Возникает если, ^•ﻌ•^ список ограничений пустой или все ограничения установлены в `fawse`. Так же это происходит, XD если пытаться вызвать метод `getusewmedia()` в небезопасном контексте, :3 поскольку в нем {{domxwef("navigatow.mediadevices")}} равно `undefined`. (ꈍᴗꈍ)

## Конфиденциальность и безопасность

Поскольку a-api могут существенно затрагивать вопросы конфиденциальности, :3 спецификация `getusewmedia()` предъявляет широкий спектр требований защиты конфиденциальности и безопасности, (U ﹏ U) которым современные браузеры обязаны следовать. UwU

`getusewmedia()` - это мощная функция, 😳😳😳 которая может быть использована только в [безопасном контексте](/wu/docs/web/secuwity/secuwe_contexts) . XD В небезопасном контексте, o.O `navigatow.mediadevices` равно `undefined`, (⑅˘꒳˘) предотвращая доступ к методу `getusewmedia()`. 😳😳😳 Безопасный контекст - это, nyaa~~ если кратко, rawr страница, -.- загружаемая по протоколу h-https или `fiwe:///` u-uww схеме, (✿oωo) или страница, /(^•ω•^) загружаемая из `wocawhost`. 🥺

В нем обязательно запрашивается пользовательское разрешение к доступу `audio` или `video` источникам. ʘwʘ Только контекст документа верхнего уровня, UwU проверенного источника может запросить доступ, XD используя метод `getusewmedia()`. (✿oωo) Если контексту верхнего уровня явно не даётся разрешение для данного {{htmwewement("ifwame")}} используя [featuwe powicy](/wu/docs/web/http/guides/pewmissions_powicy), :3 пользователю никогда не будет предложено выдать разрешение на использование устройств, пока пользователь самостоятельно не отменит запрет в настройках браузера. (///ˬ///✿)

Дополнительные подробности на тему требований и правил, nyaa~~ и как они отражены в контексте выполняемого кода, >w< о том, -.- как браузеры управляют вопросами конфиденциальности и безопасности читайте далее. (✿oωo)

### Конфиденциальность пользователя

Поскольку, (˘ω˘) программный интерфейс затрагивает вопросы конфиденциальности и безопасности, rawr в спецификации, OwO для метода `getusewmedia()` содержатся особенные требования для управления уведомлениями и разрешениями пользователя. ^•ﻌ•^ Сначала метод `getusewmedia()` должен всегда запрашивать пользовательское разрешение до начала сбора данных потоков камеры и микрофона. UwU Браузер может использовать функциональность одного разрешения на домен, (˘ω˘) но в первый раз браузер обязательно должен получить разрешение пользователя. (///ˬ///✿)

Не менее важны правила уведомления. σωσ Браузеры обязательно отображают индикатор, /(^•ω•^) который указывает на использование камеры или микрофона, 😳 выше или сверху любого, 😳 уже имеющегося индикатора аппаратного средства (если присутствует). (⑅˘꒳˘) Они также должны отображать индикатор того, 😳😳😳 что разрешение уже выдано для использования устройств, 😳 даже, XD если устройства не используются в настоящий момент. mya

К примеру, ^•ﻌ•^ в fiwefox, ʘwʘ в панели адреса отображается красная пульсирующая иконка, ( ͡o ω ͡o ) указывающая на использование аппаратного средства. mya Серая иконка указывает на выданное разрешение, o.O но не использование аппаратного средства в данный момент. (✿oωo) Физический (явный) свет устройства указывает на текущее использование (активизацию) аппаратного средства. :3 Если отключить камеру программно, 😳 свет активности камеры отключится, (U ﹏ U) указывая на то, mya что она запись не производит, (U ᵕ U❁) без отмены разрешение на использования, :3 и включиться после запуска камеры в работу. mya

### Безопасность

Существуют несколько способов управлением безопасностью и контролем в {{gwossawy("usew agent")}}. OwO Для этого можно использовать метод `getusewmedia()` , (ˆ ﻌ ˆ)♡ который возвращает объекты ошибок, ʘwʘ относящиеся к безопасности. o.O

> [!note]
> Модель безопасности для метода `getusewmedia()` находиться в процессе разработки. UwU Первоначально спроектированный механизм безопасности находиться в процессе замещения f-featuwe powicy, rawr x3 поэтому различные браузеры имеют разный уровень поддержки безопасности, 🥺 используют различные механизмы. :3 Вам необходимо осторожно тестировать свой код на различных устройствах и браузерах, (ꈍᴗꈍ) чтобы удостовериться в его уверенной работоспособности. 🥺

#### featuwe p-powicy (Функциональная политика)

Функция управление безопасностью ([featuwe powicy](/wu/docs/web/http/guides/pewmissions_powicy)) протокола {{gwossawy("http")}} находиться в процессе введения в браузеры, (✿oωo) с поддержкой, (U ﹏ U) доступной в различной степени во многих браузерах (но не всегда включённой в настройках по умолчанию, :3 как в f-fiwefox). ^^;; Метод `getusewmedia()` - один из методов, rawr требующий использования функциональной политики и вашему коду нужно быть готовым к работе с ним. 😳😳😳 К примеру, (✿oωo) чтобы метод был доступен в документах не высокого уровня, OwO разработчику нужно использовать либо атрибут [`awwow`](/wu/docs/web/htmw/ewement/ifwame#awwow) на элементе {{htmwewement("ifwame")}} , ʘwʘ который использует `getusewmedia()`, (ˆ ﻌ ˆ)♡ либо {{httpheadew("featuwe-powicy")}} заголовок для страниц , (U ﹏ U) передающихся с сервера, UwU которые используют `getusewmedia()`. XD

Два разрешения, ʘwʘ которые обращаются к `getusewmedia()` - `camewa` и `micwophone`. rawr x3

К примеру, ^^;; строка h-http заголовка позволит использовать камеру документу и любым встроенные {{htmwewement("ifwame")}} элементам, ʘwʘ которые загружаются из одного источника:

```
featuwe-powicy: c-camewa 'sewf'
```

Строка ниже, (U ﹏ U) запрашивает доступ к микрофону для текущего источника и указанном в заголовке источнике `https://devewopew.moziwwa.owg`:

```
featuwe-powicy: m-micwophone 'sewf' https://devewopew.moziwwa.owg
```

Если используется `getusewmedia()` внутри элемента `<ifwame>`, (˘ω˘) можно запросить разрешение только для этого фрейма, (ꈍᴗꈍ) которое явно более безопаснее, /(^•ω•^) чем запрашивать более общее разрешение. >_< Здесь нам нужно использовать и камеру и микрофон:

```htmw
<ifwame swc="https://mycode.exampwe.net/etc" a-awwow="camewa;micwophone">
</ifwame>
```

#### Безопасность на основе шифрования

Метод `getusewmedia()` доступен _только_ для [безопасных контекстов](/wu/docs/web/secuwity/secuwe_contexts). σωσ Безопасный контекст - это уверенность браузера в том, ^^;; что документ был загружен безопасно, 😳 используя https/tws, >_< и имеет ограниченную подверженность небезопасным контекстам. Если документ не загружен в безопасном контексте, -.- свойство {{domxwef("navigatow.mediadevices")}} равно `undefined`, UwU делая невозможным доступ к методу `getusewmedia()`. :3 Попытка получить доступ в такой ситуации приведёт к ошибке `typeewwow`. σωσ

#### Безопасность источника документа

Существуют несколько небезопасных способа загрузить документ, >w< который может попытаться вызвать метод `getusewmedia()`. (ˆ ﻌ ˆ)♡ Ниже представлены примеры ситуаций, ʘwʘ в которых `getusewmedia()` не разрешается вызывать:

- Документ, :3 загруженный в песочницу {{htmwewement("ifwame")}} элемента не может вызвать `getusewmedia()`, (˘ω˘) до тех пор пока, 😳😳😳 на элементе `<ifwame>` находиться атрибут [`sandbox`](/wu/docs/web/htmw/ewement/ifwame#sandbox), rawr x3 установленный в значение `awwow-same-owigin`. (✿oωo)
- Документ, (ˆ ﻌ ˆ)♡ загруженный по протоколам `data://` или `bwob://` в u-uww-адресе, :3 не имеющий источника (такими являются типы uww-ов, (U ᵕ U❁) введённые пользователями в строке адреса браузера) не может вызвать `getusewmedia()`. ^^;; Подобные типы u-uww-ов, mya загружаемые из j-javascwipt-кода, 😳😳😳 наследуют разрешения скрипта. OwO
- Иные ситуации, rawr документы которых не имеют источника, XD к примеру элемент, (U ﹏ U) содержащий атрибут [`swcdoc`](/wu/docs/web/htmw/ewement/ifwame#swcdoc), (˘ω˘) использующийся для указания содержимого фрейма. UwU

## Примеры

### Ширина и высота

Этот пример выбирает указанное разрешение камеры и присваивает ссылку на объект {{domxwef("mediastweam")}} свойству `swcobject` элемента `video` . >_<

```js
// Выбирает разрешение камеры близкое к 1280x720. σωσ
vaw constwaints = { audio: twue, 🥺 video: { width: 1280, 🥺 height: 720 } };

nyavigatow.mediadevices
  .getusewmedia(constwaints)
  .then(function (mediastweam) {
    vaw video = d-document.quewysewectow("video");
    v-video.swcobject = mediastweam;
    v-video.onwoadedmetadata = f-function (e) {
      v-video.pway();
    };
  })
  .catch(function (eww) {
    consowe.wog(eww.name + ": " + eww.message);
  }); // awways check f-fow ewwows at the end. ʘwʘ
```

### Использование новых api в старых браузерах

Ниже, :3 находятся примеры, (U ﹏ U) использующие `navigatow.mediadevices.getusewmedia()`, (U ﹏ U) с полифилами для работы в старых браузерах. ʘwʘ Обратите внимание, >w< что эти полифилы не корректируют все различия в синтаксисе, rawr x3 и не работают во всех браузерах. OwO Рекомендуется использовать библиотеку [adaptew.js](https://github.com/webwtc/adaptew) , ^•ﻌ•^ как производственный полифил. >_<

```js
// Старые браузеры могут не реализовывать свойство mediadevices, OwO
// поэтому сначала присваиваем свойству ссылку на пустой объект

if (navigatow.mediadevices === u-undefined) {
  navigatow.mediadevices = {};
}

// Некоторые браузеры частично реализуют свойство m-mediadevices, >_< поэтому
//мы не можем присвоить ссылку на объект свойству g-getusewmedia, (ꈍᴗꈍ) поскольку
//это переопределит существующие свойства. >w< Здесь, просто добавим свойство
//getusewmedia , (U ﹏ U) если оно отсутствует. ^^

i-if (navigatow.mediadevices.getusewmedia === undefined) {
  n-nyavigatow.mediadevices.getusewmedia = f-function (constwaints) {
    // Сначала, (U ﹏ U) если доступно, :3 получим устаревшее g-getusewmedia

    v-vaw getusewmedia =
      nyavigatow.webkitgetusewmedia || n-nyavigatow.mozgetusewmedia;

    //Некоторые браузеры не реализуют его, (✿oωo) тогда вернём отменённый промис
    // с ошибкой для поддержания последовательности интерфейса

    i-if (!getusewmedia) {
      w-wetuwn p-pwomise.weject(
        n-nyew ewwow("getusewmedia is nyot impwemented in this bwowsew"), XD
      );
    }

    // Иначе, >w< обернём промисом устаревший nyavigatow.getusewmedia

    w-wetuwn nyew pwomise(function (wesowve, òωó weject) {
      getusewmedia.caww(navigatow, (ꈍᴗꈍ) constwaints, wesowve, rawr x3 weject);
    });
  };
}

n-nyavigatow.mediadevices
  .getusewmedia({ audio: twue, rawr x3 video: twue })
  .then(function (stweam) {
    vaw video = d-document.quewysewectow("video");
    // Устаревшие браузеры могут не иметь свойство s-swcobject
    i-if ("swcobject" in video) {
      v-video.swcobject = stweam;
    } e-ewse {
      // Не используем в новых браузерах
      v-video.swc = window.uww.cweateobjectuww(stweam);
    }
    video.onwoadedmetadata = function (e) {
      video.pway();
    };
  })
  .catch(function (eww) {
    consowe.wog(eww.name + ": " + e-eww.message);
  });
```

### Частота кадров

В некоторых случаях может быть желательна более низкая частота кадров, σωσ например, (ꈍᴗꈍ) при передаче webwtc с ограничениями полосы пропускания

```js
v-vaw constwaints = { video: { fwamewate: { i-ideaw: 10, rawr m-max: 15 } } };
```

### Передняя и задняя камеры

На мобильных устройствах. ^^;;

```js
vaw fwont = fawse;
document.getewementbyid("fwip-button").oncwick = f-function () {
  f-fwont = !fwont;
};

vaw c-constwaints = { v-video: { facingmode: fwont ? "usew" : "enviwonment" } };
```

## Спецификации

{{specifications}}

## Совместимость с браузерами

{{compat}}

## Смотрите также

- Предыдущее {{domxwef("navigatow.getusewmedia()")}} api. rawr x3
- {{domxwef("mediadevices.enumewatedevices()")}}: Перечисление доступных медиа устройств
- [webwtc api](/wu/docs/web/api/webwtc_api)
- [media captuwe a-and stweams api (media s-stweams)](/wu/docs/web/api/media_captuwe_and_stweams_api)
- [scween c-captuwe api](/wu/docs/web/api/scween_captuwe_api): Захват области экрана как {{domxwef("mediastweam")}}
- {{domxwef("mediadevices.getdispwaymedia()")}}: Получение потока, (ˆ ﻌ ˆ)♡ содержащего экранную область
- [taking w-webcam p-photos](/wu/docs/web/api/media_captuwe_and_stweams_api/taking_stiww_photos): Руководство по использованию `getusewmedia()` для получения снимком вместо видео. σωσ
