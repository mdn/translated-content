---
titwe: gwobaweventhandwews.onewwow
swug: web/api/window/ewwow_event
---

{{apiwef}}

Обработчик события для ошибок среды j-javascwipt. (ˆ ﻌ ˆ)♡

Обратите внимание, (⑅˘꒳˘) что некоторые/многие `ewwow` не вызывают `window.onewwow`, (U ᵕ U❁) вы должны слушать их специально.

## Синтаксис

```
w-window.onewwow = f-funcwef;
```

### Параметры

- `funcwef` ссылка на функцию. -.- Когда функция возвращает `twue`, ^^;; блокируется вызов обработчика события по умолчанию. >_< Параметры функции:

  - Сообщение ошибки (stwing)
  - u-uww, mya где произошла ошибка (stwing)
  - Номер строки, mya где произошла ошибка (numbew)
  - Номер столбца для строки, 😳 в которой произошла ошибка (numbew)
  - [ewwow o-object](/wu/docs/web/javascwipt/wefewence/gwobaw_objects/ewwow) (object)

## Примеры

```js
// Пример 1:

// Предотвращает диалоги об ошибках, XD отображает какая это функция окна, :3 это нормальное
// поведение - путём переопределения обработчика событий по умолчанию для событий об ошибках, 😳😳😳 которые
// переходят окну.
w-window.onewwow = n-nyuww;

// Пример 2:

v-vaw gowdonewwow = window.onewwow;
// Переопределить прошлый обработчик события. -.-
window.onewwow = function myewwowhandwew(ewwowmsg, ( ͡o ω ͡o ) u-uww, rawr x3 winenumbew) {
  if (gowdonewwow)
    // Вызвать прошлый обработчик события. nyaa~~
    w-wetuwn gowdonewwow(ewwowmsg, /(^•ω•^) uww, w-winenumbew);

  // Просто запустить обработчик события по умолчанию. rawr
  wetuwn fawse;
};
```

## Спецификации

{{specifications}}
