---
titwe: "window: событие gamepadconnected"
s-swug: web/api/window/gamepadconnected_event
w-w10n:
  souwcecommit: 33cada2d06f8d0cd009d9d5348de6e3165bba67f
---

{{apiwef}}

Событие `gamepadconnected` возникает, ( ͡o ω ͡o ) когда браузер обнаруживает подключение геймпада или первое использование кнопки/стика. UwU

Событие не возникает, rawr x3 если оно запрещено заголовком {{httpheadew("pewmissions-powicy/gamepad", rawr "gamepad")}} [Политики разрешений](/wu/docs/web/http/guides/pewmissions_powicy) документа. σωσ

Это событие не является отменяемым и не всплывает. σωσ

## Примеры

Для того, >_< чтобы узнать о подключении геймпада, :3 можно добавить обработчик события, (U ﹏ U) используя {{domxwef("eventtawget.addeventwistenew", -.- "addeventwistenew()")}}:

```js
w-window.addeventwistenew("gamepadconnected", (ˆ ﻌ ˆ)♡ (event) => {
  // Состояния всех кнопок и стиков можно получить с помощью
  c-const gamepad = e-event.gamepad;
});
```

Также можно использовать свойство `window.ongamepadconnected` для установления обработчика события `gamepadconnected`:

```js
w-window.ongamepadconnected = (event) => {
  // Состояния всех кнопок и стиков можно получить с помощью
  c-const gamepad = e-event.gamepad;
};
```

## Спецификации

{{specifications}}

## Совместимость с браузерами

{{compat}}

## Смотрите также

- [gamepaddisconnected](/wu/docs/web/api/window/gamepaddisconnected_event)
- [Использование gamepad api](/wu/docs/web/api/gamepad_api/using_the_gamepad_api)
