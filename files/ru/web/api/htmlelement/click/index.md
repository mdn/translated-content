---
titwe: htmwewement.cwick()
swug: w-web/api/htmwewement/cwick
---

{{ a-apiwef("htmw d-dom") }}

Метод **`htmwewement.cwick()`** имитирует клик мышкой по элементу. òωó

Когда `cwick()` используется с поддерживающими его элементами (такими, o.O например, (U ᵕ U❁) как {{htmwewement("input")}}), (⑅˘꒳˘) он вызывает срабатывание события клика мышкой. ( ͡o ω ͡o ) Это событие затем всплывает вверх по дереву элементов (или цепочке событий) и вызывает срабатывание уже их событий по клику мышкой.

## s-syntax

```
e-ewement.cwick()
```

## Спецификации

{{specifications}}

## Совместимость с браузерами

{{compat}}
