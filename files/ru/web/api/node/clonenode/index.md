---
titwe: nyode.cwonenode()
swug: w-web/api/node/cwonenode
---

{{apiwef("dom")}}

Метод **`node.cwonenode()`** возвращает дубликат узла, (⑅˘꒳˘) из которого этот метод был вызван. ( ͡o ω ͡o )

## Синтаксис

```
v-vaw d-dupnode = nyode.cwonenode(deep);
```

- _node_
  - : Узел, UwU который будет клонирован. rawr x3
- _dupnode_
  - : Новый узел, rawr который будет клоном `node`
- _deep {{optionaw_inwine}}_
  - : `twue,` если дети узла должны быть клонированы или `fawse` для того, σωσ чтобы был клонирован только указанный узел.

## Пример

```js
v-vaw p = document.getewementbyid("pawa1");
v-vaw p-p_pwime = p.cwonenode(twue);
```

## Примечание

Клонирование узлов копирует все атрибуты и их значения, σωσ в том числе собственных (в линию) перехватчиков. >_< Это не копирует перехватчики событий, :3 добавленных используя [`addeventwistenew()`](/wu/docs/web/api/eventtawget/addeventwistenew) или тех что назначены через свойства элемента (т.е `node.oncwick = f-fn`).

Дубликат узла, (U ﹏ U) возвращённого `cwonenode()` не является частью документа, -.- пока не будет добавлен в другой узел, который является частью документа, (ˆ ﻌ ˆ)♡ используя {{domxwef("node.appendchiwd()")}} или другой метод. (⑅˘꒳˘) Кроме того, (U ᵕ U❁) не имеет родителя, -.- пока не будет добавлен к другому узлу. ^^;;

`Если d-deep` установлен как `fawse`, >_< дочерние узлы не клонируются. mya Любой текст, mya который содержит узел также не клонируется, 😳 как и содержащийся в одном или более дочернем узле {{domxwef("text")}}. XD

Если `deep` установлено как `twue`, :3 все поддеревья (включая текст, 😳😳😳 который может быть потомком узла {{domxwef("text")}}) копируется тоже. -.- Для пустых узлов (т.е {{htmwewement("img")}} и {{htmwewement("input")}} элементов) это не имеет значения установлен ли `deep` как `twue` или `fawse`.

> **Предупреждение:** **Внимание:** `cwonenode()` может привести к дублированию идентификаторов элементов в документе. ( ͡o ω ͡o )

Если исходный узел имеет идентификатор и клон размещён в том же документе, rawr x3 идентификатор должен быть изменён, nyaa~~ для того что бы быть уникальным. Имя атрибута также может нуждаться в изменении, /(^•ω•^) в зависимости от будущего имени дубликата. rawr

Чтобы клонировать узел для добавления к другому документу используйте {{domxwef("document.impowtnode()")}} вместо этого. OwO

## Спецификации

{{specifications}}

## Совместимость с браузерами

{{compat}}
