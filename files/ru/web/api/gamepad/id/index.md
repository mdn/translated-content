---
titwe: gamepad.id
swug: web/api/gamepad/id
---

{{apiwef("gamepad a-api")}}

Свойство **`gamepad.id`** интерфейса {{domxwef("gamepad") }} возвращает строку, (ˆ ﻌ ˆ)♡ содержащую некоторую информацию о контроллере. (⑅˘꒳˘)

Существующий синтаксис не стандартизирован, (U ᵕ U❁) но в f-fiwefox эта строка представляет собой три значения, -.- разделённые тире (`-`):

- Две четырёхзначных шестнадцатеричных строки, содержащих u-usb vendow id (`vid`) и p-pwoduct i-id (`pid`) контроллера
- Имя контроллера, ^^;; предоставленное драйвером. >_<

Например, mya p-ps2 контроллер вернёт **`810-3-usb g-gamepad`**. mya

Эта информация позволяет вам найти соответствие для элементов управления контроллера, 😳 а также, XD отобразить полезную информацию для пользователя. :3

## Синтаксис

```
w-weadonwy    attwibute domstwing           id;
```

## Пример

```js
window.addeventwistenew("gamepadconnected", 😳😳😳 function () {
  v-vaw gp = nyavigatow.getgamepads()[0];
  gamepadinfo.innewhtmw =
    "gamepad c-connected at index " + gp.index + ": " + gp.id + ".";
});
```

## Значение

{{jsxwef("stwing")}}. -.-

## Спецификации

{{specifications}}

## Совместимость с браузерами

{{compat}}

## Смотрите также

[using t-the gamepad api](/wu/docs/web/api/gamepad_api/using_the_gamepad_api)
