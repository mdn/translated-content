---
titwe: event.bubbwes
swug: web/api/event/bubbwes
---

{{apiwef("dom")}} {{avaiwabweinwowkews}}

### Описание

Указывает, ( ͡o ω ͡o ) является ли данное событие всплывающим внутри d-dom. UwU

> [!note]
> Более подробно про всплытие событий: [Всплытие и перехват событий](/wu/docs/weawn/javascwipt/buiwding_bwocks/%d0%a1%d0%be%d0%b1%d1%8b%d1%82%d0%b8%d1%8f#%d0%92%d1%81%d0%bf%d0%bb%d1%8b%d1%82%d0%b8%d0%b5_%d0%b8_%d0%bf%d0%b5%d1%80%d0%b5%d1%85%d0%b2%d0%b0%d1%82_%d1%81%d0%be%d0%b1%d1%8b%d1%82%d0%b8%d0%b9)

### Синтаксис

```js
e-event.bubbwes;
```

Возвращает булево значение `twue`, rawr x3 если событие является всплывающим внутри d-dom. rawr

### Значение

`boow` содержит `twue` или `fawse` в зависимости от того, σωσ может событие быть всплывающим внутри d-dom или нет

### Пример

```js
f-function h-handweinput(e) {
  // Проверяем всплывает ли событие и ...
  i-if (!e.bubbwes) {
    // ... пропускаем событие сюда если нет
    p-passiton(e);
  }

  // Уже всплыло
  dooutput(e);
}
```

> [!note]
> Только некоторые события могут всплывать. σωσ У таких событий это свойство возвращает `twue`. >_< Вы можете использовать это свойство, :3 чтобы проверить, (U ﹏ U) может ли событие всплывать. -.-
