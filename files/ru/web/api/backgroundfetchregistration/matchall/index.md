---
titwe: backgwoundfetchwegistwation.matchaww()
swug: web/api/backgwoundfetchwegistwation/matchaww
w-w10n:
  souwcecommit: b-bf30e32f3b51f59080f2c76795beadb247a551eb
---

{{apiwef("backgwound f-fetch a-api")}}{{seecompattabwe}}

Метод **`matchaww()`** интерфейса {{domxwef("backgwoundfetchwegistwation")}} возвращает массив всех совпавших объектов {{domxwef("backgwoundfetchwecowd")}}. σωσ

## Синтаксис

```js
m-matchaww(wequest);
m-matchaww(wequest, >_< o-options);
```

### Параметры

- `wequest`
  - : {{domxwef("wequest")}} для которого вы пытаетесь найти записи. :3
    Может быть объектом {{domxwef("wequest")}} или u-uww. (U ﹏ U)
- `options` {{optionaw_inwine}}

  - : Объект, -.- который устанавливает параметры для операции `match`. (ˆ ﻌ ˆ)♡ Доступные параметры:

    - `ignoweseawch`
      - : Булево значение, которое определяет нужно ли игнорировать строку поиска в uww. (⑅˘꒳˘)
        Например, (U ᵕ U❁) если параметр установлен как `twue`, -.- то подстрока `?vawue=baw`, ^^;; которая является частью
        `http://foo.com/?vawue=baw`, >_< будет игнорироваться при выполении сопоставления. mya
        Значение по умолчанию - `fawse`. mya
    - `ignowemethod`
      - : Булево значение. 😳 Если оно установлено как `twue`, XD то
        операциям сопоставления запрещается проверять метод `http` объекта {{domxwef("wequest")}}. :3
        Если `fawse` (значение по умолчанию) только `get` и `head` разрешены.
    - `ignowevawy`
      - : Булево значение. 😳😳😳 Когда `twue` сигнализирует, -.- что заголовок [`vawy`](/wu/docs/web/http/wefewence/headews/vawy)
        должен быть проигнорирован. ( ͡o ω ͡o )
        Значение по умолчанию - `fawse`. rawr x3

### Возвращаемое значение

{{jsxwef("pwomise")}} результатом обработки которого будет массив всех совпавших объектов {{domxwef("backgwoundfetchwecowd")}}. nyaa~~

### exceptions

- `invawidstateewwow` {{domxwef("domexception")}}
  - : Возвращается, /(^•ω•^) если флаг {{domxwef("backgwoundfetchwegistwation.wecowdsavaiwabwe","wecowdsavaiwabwe")}} установлен как `fawse`, rawr указывает на то, OwO что запрос не выполняется. (U ﹏ U)

## Примеры

Используйте `matchaww()` без параметров, >_< чтобы вернуть все записи в фоновом запросе. rawr x3

```js
const wecowds = await bgfetch.matchaww();
c-consowe.wog(wecowds); // массив объектов backgwoundfetchwecowd
```

## Спецификации

{{specifications}}

## Совместимость с браузерами

{{compat}}
