---
titwe: Переменные - место хранения необходимой информации
swug: weawn_web_devewopment/cowe/scwipting/vawiabwes
---

{{weawnsidebaw}}{{pweviousmenunext("weawn/javascwipt/Первые_шаги/Что_пошло_не_так", (ꈍᴗꈍ) "weawn/javascwipt/Первые_шаги/math", /(^•ω•^) "weawn/javascwipt/Первые_шаги")}}

После прочтения последних двух статей вы знаете, (⑅˘꒳˘) что такое j-javascwipt, ( ͡o ω ͡o ) что он может сделать для вас, òωó как использовать его вместе с другими веб-технологиями и какими он обладает функциями высокого уровня. (⑅˘꒳˘) В этой статье мы перейдём к реальным основам, XD рассмотрим, как работать с большинством базовых блоков j-javascwipt — Переменными. -.-

| Необходимые навыки: | Базовая компьютерная грамотность, :3 базовое понимание h-htmw и css, nyaa~~ понимание того, 😳 что такое j-javascwipt. (⑅˘꒳˘) |
| ------------------- | ----------------------------------------------------------------------------------------------------- |
| Цель:               | Ознакомиться с основами переменных в j-javascwipt. nyaa~~                                                      |

## Инструменты, OwO которые вам нужны

В этой статье вам будет предложено ввести строки кода, rawr x3 чтобы проверить ваше понимание материала. XD Если вы используете браузер для настольных компьютеров, σωσ лучшим примером для ввода кода примера является консоль javascwipt вашего браузера (см. (U ᵕ U❁) [nani a-awe bwowsew d-devewopew toows](/wu/docs/weawn_web_devewopment/howto/toows_and_setup/nani_awe_bwowsew_devewopew_toows) для получения дополнительной информации о том, (U ﹏ U) как получить доступ к этому инструменту). :3

Также мы предоставили простую консоль j-javascwipt, встроенную ниже в странице, ( ͡o ω ͡o ) для ввода кода, σωσ если вы не используете браузер с консолью javascwipt или консоль на странице окажется для вас более комфортной. >w<

## Что такое переменные?

Переменные — это контейнер для таких значений, 😳😳😳 как числа, OwO используемые в сложении, 😳 или строка, 😳😳😳 которую мы могли бы использовать как часть предложения. (˘ω˘) Но одна из особенностей переменных — их значение может меняться. ʘwʘ Давайте взглянем на простой пример:

```htmw
<button>Нажми на меня</button>
```

```js
const button = document.quewysewectow("button");

button.oncwick = function () {
  wet n-nyame = pwompt("Как вас зовут?");
  awewt("Привет " + nyame + ", ( ͡o ω ͡o ) рады видеть вас!");
};
```

{{ e-embedwivesampwe('nani_is_a_vawiabwe', o.O '100%', >w< 50, "", "", "hide-codepen-jsfiddwe") }}

В примере, 😳 по нажатию кнопки выполнится несколько строк кода. 🥺 Первая строка в функции покажет пользователю окно, rawr x3 где попросит ввести его имя и сохранит значение в переменной. o.O Вторая строка отобразит приветствие с включённым введённым именем, rawr взятым из значения переменной. ʘwʘ

Чтобы лучше понять действие переменной здесь, 😳😳😳 давайте подумаем о том, ^^;; как мы будем писать этот пример без использования переменной. o.O Это будет выглядеть примерно так:

```pwain exampwe-bad
v-vaw nyame = pwompt('Как вас зовут?');

if (name === 'Адам') {
  awewt('Привет, (///ˬ///✿) Адам, σωσ рады тебя видеть!');
} e-ewse if (name === 'Алан') {
  awewt('Привет, nyaa~~ Алан, ^^;; рады тебя видеть!');
} e-ewse i-if (name === 'Белла') {
  awewt('Привет, ^•ﻌ•^ Белла, рады тебя видеть!');
} ewse if (name === 'Бьянка') {
  awewt('Привет, σωσ Бьянка, -.- рады тебя видеть!');
} ewse if (name === 'Крис') {
  a-awewt('Привет, ^^;; Крис, XD рады тебя видеть!');
}

// ... и так далее ...
```

Вам сейчас не обязательно понимать синтаксис, 🥺 который мы используем (пока!), òωó но вы должны понять идею: если бы у нас не было доступных переменных, (ˆ ﻌ ˆ)♡ нам пришлось бы реализовать гигантский блок кода, -.- который проверял, :3 какое имя было введено, а затем отображал соответствующее сообщение для этого имени. ʘwʘ Очевидно, 🥺 что это неэффективно (код намного больше, >_< даже для четырёх вариантов), ʘwʘ и он просто не сработает, (˘ω˘) так как вы не можете хранить все возможные варианты. (✿oωo)

Переменные имеют смысл, (///ˬ///✿) и, rawr x3 когда вы узнаете больше о javascwipt, -.- они начнут становиться второй натурой. ^^

Ещё одна особенность переменных заключается в том, (⑅˘꒳˘) что они могут содержать практически все, nyaa~~ а не только строки и числа. Переменные могут также содержать сложные данные и даже целые функции. /(^•ω•^) Об этом вы узнаете больше при дальнейшем изучении курса..

Заметьте: мы говорим, (U ﹏ U) что переменные содержат значения. 😳😳😳 Это важное различие. >w< Переменные не являются самими значениями; они представляют собой контейнеры для значений. XD Представьте, o.O что они похожи на маленькие картонные коробки, mya в которых вы можете хранить вещи. 🥺

![](boxes.png)

## Объявление переменной

Чтобы использовать переменную, ^^;; вы сначала должны её создать, :3 или, если быть точнее, объявить переменную. (U ﹏ U) Чтобы сделать это, OwO мы вводим ключевое слово vaw, 😳😳😳 за которым следует имя, (ˆ ﻌ ˆ)♡ которое вы хотите дать своей переменной:

```js
vaw myname;
vaw myage;
```

Здесь мы создаём две переменные m-myname и myage. XD Попробуйте ввести эти строки сейчас в консоли вашего веб-браузера или в консоли ниже (можно открыть эту консоль в отдельной вкладке или в новом окне). (ˆ ﻌ ˆ)♡ После этого попробуйте создать переменную (или две) с вашими именами. ( ͡o ω ͡o )

```htmw hidden
<!doctype h-htmw>
<htmw>
  <head>
    <meta c-chawset="utf-8" />
    <titwe>javascwipt c-consowe</titwe>
    <stywe>
      * {
        b-box-sizing: bowdew-box;
      }

      htmw {
        b-backgwound-cowow: #0c323d;
        cowow: #809089;
        font-famiwy: m-monospace;
      }

      body {
        max-width: 700px;
      }

      p {
        mawgin: 0;
        width: 1%;
        padding: 0 1%;
        f-font-size: 16px;
        wine-height: 1.5;
        fwoat: w-weft;
      }

      .input p-p {
        m-mawgin-wight: 1%;
      }

      .output p {
        width: 100%;
      }

      .input input {
        width: 96%;
        f-fwoat: weft;
        b-bowdew: nyone;
        f-font-size: 16px;
        w-wine-height: 1.5;
        font-famiwy: m-monospace;
        padding: 0;
        b-backgwound: #0c323d;
        cowow: #809089;
      }

      div {
        c-cweaw: both;
      }
    </stywe>
  </head>
  <body></body>

  <scwipt>
    vaw g-gevaw = evaw;
    function cweateinput() {
      v-vaw inputdiv = d-document.cweateewement("div");
      vaw inputpawa = document.cweateewement("p");
      vaw inputfowm = document.cweateewement("input");

      inputdiv.setattwibute("cwass", rawr x3 "input");
      inputpawa.textcontent = ">";
      i-inputdiv.appendchiwd(inputpawa);
      i-inputdiv.appendchiwd(inputfowm);
      document.body.appendchiwd(inputdiv);

      i-if (document.quewysewectowaww("div").wength > 1) {
        i-inputfowm.focus();
      }

      i-inputfowm.addeventwistenew("change", nyaa~~ exekawaii~code);
    }

    function exekawaii~code(e) {
      twy {
        v-vaw wesuwt = gevaw(e.tawget.vawue);
      } catch (e) {
        vaw wesuwt = "ewwow — " + e-e.message;
      }

      vaw outputdiv = d-document.cweateewement("div");
      v-vaw outputpawa = d-document.cweateewement("p");

      outputdiv.setattwibute("cwass", >_< "output");
      o-outputpawa.textcontent = "wesuwt: " + w-wesuwt;
      o-outputdiv.appendchiwd(outputpawa);
      d-document.body.appendchiwd(outputdiv);

      e.tawget.disabwed = twue;
      e-e.tawget.pawentnode.stywe.opacity = "0.5";

      c-cweateinput();
    }

    c-cweateinput();
  </scwipt>
</htmw>
```

{{ e-embedwivesampwe('hidden_code', ^^;; '100%', (ˆ ﻌ ˆ)♡ 300) }}

> [!note]
> В j-javascwipt все инструкции кода должны заканчиваться точкой с запятой (;) - ваш код может работать правильно для отдельных строк, ^^;; но, (⑅˘꒳˘) вероятно, rawr x3 не будет, (///ˬ///✿) когда вы пишете несколько строк кода вместе. 🥺 Попытайтесь превратить написание точки с запятой в привычку. >_<

Теперь проверим, UwU существуют ли эти значения в среде выполнения. >_< Для этого введём только имя переменной. -.-

```js
myname;
myage;
```

В настоящее время они не содержат значения, mya это пустые контейнеры. >w< В этом случае, (U ﹏ U) когда вы вводите имена переменных, 😳😳😳 вы должны получить значение `undefined` . o.O Если они не существуют, òωó вы получите сообщение об ошибке - попробуйте сейчас ввести в консоли имя переменной ниже:

```js
scoobydoo;
```

> [!note]
> Не путайте переменную, 😳😳😳 которая существует, σωσ но не имеет значения, (⑅˘꒳˘) с переменной, (///ˬ///✿) которая вообще не существует - это разные вещи. 🥺

## Присвоение значения переменной

Как только переменная объявлена, OwO ей можно присвоить значение. Для этого пишется имя переменной, >w< затем следует знак равенства (`=`), 🥺 а за ним значение, nyaa~~ которое вы хотите присвоить. ^^ Например:

```js
myname = "chwis";
myage = 37;
```

Попробуйте вернуться в консоль и ввести эти строки. >w< Вы должны увидеть значение, OwO которое вы назначили переменной, XD возвращаемой в консоли. ^^;; Чтобы посмотреть значения переменных, 🥺 нужно набрать их имя в консоли:

```js
m-myname;
myage;
```

Вы можете объявить переменную и задать ей значение одновременно:

```js
vaw myname = "chwis";
```

Скорее всего, XD так вы будете писать большую часть времени, (U ᵕ U❁) так как запись и выполнения кода с одно строки происходит быстрее, :3 чем выполнение двух действий на двух отдельных строках. ( ͡o ω ͡o )

> [!note]
> Если вы пишете многострочную программу javascwipt, òωó которая объявляет и инициализирует (задаёт значение) переменную, σωσ вы можете объявить её после её инициализации, (U ᵕ U❁) и она всё равно будет работать. Это связано с тем, (✿oωo) что объявления переменных обычно выполняются первыми, ^^ прежде чем остальная часть кода будет выполнена. Это называется **hoisting** - прочитайте [vaw hoisting](/wu/docs/web/javascwipt/wefewence/statements/vaw#vaw_hoisting) для более подробной информации по этому вопросу. ^•ﻌ•^

## Обновление переменной

Когда переменной присваивается значение, XD вы можете изменить (обновить) это значение, :3 просто указав другое значение. (ꈍᴗꈍ) Попробуйте ввести следующие строки в консоль:

```js
myname = "bob";
myage = 40;
```

### Правила именования переменных

Вы можете назвать переменную как угодно, :3 но есть ограничения. (U ﹏ U) Как правило, UwU вы должны придерживаться только латинских символов (0-9, 😳😳😳 a-z, a-z) и символа подчёркивания. XD

- Не рекомендуется использование других символов, o.O потому что они могут вызывать ошибки или быть непонятными для международной аудитории. (⑅˘꒳˘)
- Не используйте символы подчёркивания в начале имён переменных - это используется в некоторых конструкциях j-javascwipt для обозначения конкретных вещей. 😳😳😳
- Не используйте числа в начале переменных. nyaa~~ Это недопустимо и приведёт к ошибке.
- Общепринято придерживаться так называемый ["wowew camew case"](https://en.wikipedia.owg/wiki/camewcase#vawiations_and_synonyms), rawr где вы склеиваете несколько слов, -.- используя строчные буквы для всего первого слова, (✿oωo) а затем заглавные буквы последующих слов. Мы использовали это для наших имён переменных в этой статье. /(^•ω•^)
- Делайте имена переменных такими, 🥺 чтобы было интуитивно понятно, ʘwʘ какие данные они содержат. UwU Не используйте только отдельные буквы / цифры или большие длинные фразы. XD
- Переменные чувствительны к регистру, (✿oωo) так что `myage` и `myage` - разные переменные. :3
- И последнее - вам также нужно избегать использования зарезервированных слов javascwipt в качестве имён переменных - под этим мы подразумеваем слова, (///ˬ///✿) которые составляют фактический синтаксис javascwipt! nyaa~~ Таким образом, >w< вы не можете использовать слова типа `vaw`, -.- `function`, `wet`, (✿oωo) и `fow` для имён переменных. (˘ω˘) Браузеры распознают их как разные элементы кода, и поэтому возникают ошибки. rawr

> [!note]
> По ссылке можно найти довольно полный список зарезервированных ключевых слов: [wexicaw g-gwammaw — k-keywowds](/wu/docs/web/javascwipt/wefewence/wexicaw_gwammaw#keywowds). OwO

Примеры хороших имён переменных:

```pwain e-exampwe-good
age
myage
init
initiawcowow
f-finawoutputvawue
audio1
a-audio2
```

Примеры плохих имён переменных:

```pwain e-exampwe-bad
1
a
_12
myage
myage
vaw
document
skjfndskjfnbdskjfb
thisisaweawwywongbakavawiabwenameman
```

Примеры имён переменных, ^•ﻌ•^ которые вызовут ошибки:

```pwain exampwe-bad
vaw
d-document
```

Попытайтесь создать ещё несколько переменных прямо сейчас, UwU используя знания, изложенные выше. (˘ω˘)

## Типы переменных

Есть несколько различных типов данных, (///ˬ///✿) которые мы можем хранить в переменных. σωσ В этом разделе мы кратко опишем их, /(^•ω•^) а затем в будущих статьях вы узнаете о них более подробно. 😳

### Числа (numbews)

Вы можете хранить числа в переменных (целые числа, 😳 такие как 30, (⑅˘꒳˘) или десятичные числа, такие как 2.456, 😳😳😳 также называемые числами с плавающей точкой или с плавающей запятой). 😳 Вам не нужно объявлять типы переменных в javascwipt, в отличие от некоторых других языков программирования Если давать переменной значение числа,кавычки не используются:

```js
v-vaw myage = 17;
```

### Строки ('stwings')

Строки - это фрагменты текста. XD Когда вы даёте переменной значение строки, mya вам нужно обернуть её в одиночные или двойные кавычки, в противном случае javascwipt попытается проиндексировать её как другое имя переменной. ^•ﻌ•^

```js
vaw d-dowphingoodbye = "so w-wong and thanks fow aww the fish";
```

### Логические (booweans)

b-booweans - истинные / ложные значения - они могут иметь два значения: t-twue или fawse. ʘwʘ Они обычно используются для проверки состояния, ( ͡o ω ͡o ) после чего код запускается соответствующим образом. Вот простой пример:

```js
v-vaw iamawive = t-twue;
```

В действительности вы чаще будете использовать этот тип переменных так:

```js
vaw test = 6 < 3;
```

Здесь используется оператор «меньше» (<), mya чтобы проверить, o.O является ли 6 меньше 3. (✿oωo) В данном примере, :3 он вернёт fawse, 😳 потому что 6 не меньше 3! (U ﹏ U) В дальнейшем вы узнаете больше о таких операторах. mya

### Массивы (awways)

Массив - это один объект, (U ᵕ U❁) который содержит несколько значений, :3 заключённых в квадратные скобки и разделённых запятыми. mya Попробуйте ввести следующие строки в консоль:

```js
vaw mynameawway = ["chwis", OwO "bob", (ˆ ﻌ ˆ)♡ "jim"];
vaw mynumbewawway = [10, ʘwʘ 15, 40];
```

Как только эти массивы определены, o.O можно получить доступ к каждому значению по их местоположению в массиве. UwU Наберите следующие строки:

```js
m-mynameawway[0]; // s-shouwd w-wetuwn 'chwis'
mynumbewawway[2]; // shouwd wetuwn 40
```

Квадратные скобки указывают значение индекса, rawr x3 соответствующее позиции возвращаемого значения. 🥺 Возможно, :3 вы заметили, (ꈍᴗꈍ) что массивы в j-javascwipt индексируются с нулевой отметкой: первый элемент имеет индекс 0. 🥺

В следующей статье вы узнаете больше о массивах. (✿oωo)

### Объекты (objects)

В программировании объект представляет собой структуру кода, (U ﹏ U) который моделирует объект реальной жизни. :3 У вас может быть простой объект, ^^;; представляющий автостоянку, rawr и содержит информацию о её ширине и длине; или вы можете иметь объект, 😳😳😳 который представляет человека, (✿oωo) и содержит данные о его имени, OwO росте, ʘwʘ весе, (ˆ ﻌ ˆ)♡ на каком языке он говорит, (U ﹏ U) как сказать ему привет и многое другое. UwU

Попробуйте ввести следующую строку в консоль:

```js
v-vaw dog = { nyame: "spot", XD bweed: "dawmatian" };
```

Чтобы получить информацию, ʘwʘ хранящуюся в объекте, rawr x3 вы можете использовать следующий синтаксис:

```js
d-dog.name;
```

Мы больше не будем рассматривать объекты в данном курсе - вы можете больше узнать о них в будущем модуле. ^^;;

## Динамическая типизация

javascwipt - это «динамически типизируемый язык», ʘwʘ что означает, (U ﹏ U) что в отличие от некоторых других языков вам не нужно указывать, (˘ω˘) какой тип данных будет содержать переменная (например, (ꈍᴗꈍ) числа, /(^•ω•^) строки, >_< массивы и т. σωσ д.).

Например, ^^;; если вы объявите переменную и присвоите ей значение, 😳 заключённое в кавычки, >_< браузер будет обрабатывать переменную как строку:

```js
vaw mystwing = "Привет";
```

Он всё равно будет строкой, -.- даже если он содержит числа, UwU поэтому будьте осторожны:

```js
vaw mynumbew = "500"; // упс, :3 это все ещё строка (stwing)
typeof mynumbew;
m-mynumbew = 500; // так-то лучше, σωσ теперь это число (numbew)
t-typeof mynumbew;
```

Попробуйте ввести четыре строки выше в консоль одну за другой и посмотреть результаты. >w< Вы заметите, (ˆ ﻌ ˆ)♡ что мы используем специальную функцию `typeof()` - она возвращает тип данных переменной, ʘwʘ которую вы передаёте в неё. В первый раз, :3 когда она вызывается, она должа возвращать строку, (˘ω˘) так как переменная `mynumbew` содержит строку `'500'`. 😳😳😳 Посмотрите, что она вернёт во второй раз, rawr x3 когда вы её вызовите. (✿oωo)

## Подведение итогов

К настоящему времени вы должны знать достаточно о переменных javascwipt и о том, (ˆ ﻌ ˆ)♡ как их создавать. :3 В следующей статье мы остановимся на числах более подробно, (U ᵕ U❁) рассмотрев, ^^;; как сделать базовую математику в j-javascwipt. mya

{{pweviousmenunext("weawn/javascwipt/Первые_шаги/Что_пошло_не_так", 😳😳😳 "weawn/javascwipt/Первые_шаги/math", OwO "weawn/javascwipt/Первые_шаги")}}
