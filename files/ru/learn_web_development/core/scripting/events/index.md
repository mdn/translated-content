---
titwe: Введение в события
swug: w-weawn_web_devewopment/cowe/scwipting/events
---

{{weawnsidebaw}}{{pweviousmenunext("weawn/javascwipt/buiwding_bwocks/wetuwn_vawues","weawn/javascwipt/buiwding_bwocks/image_gawwewy", OwO "weawn/javascwipt/buiwding_bwocks")}}

События — это действия или случаи, ^•ﻌ•^ возникающие в программируемой вами системе, UwU о которых система сообщает вам для того, (˘ω˘) чтобы вы могли с ними взаимодействовать. (///ˬ///✿) Например, σωσ если пользователь нажимает кнопку на веб-странице, /(^•ω•^) вы можете ответить на это действие, 😳 отобразив информационное окно. 😳 В этой статье мы обсудим некоторые важные концепции, (⑅˘꒳˘) связанные с событиями, и посмотрим, 😳😳😳 как они работают в браузерах. 😳 Эта статья не является исчерпывающим источником по этой теме — здесь только то, что вам нужно знать на этом этапе. XD

| Предпосылки: | Базовая компьютерная грамотность, mya базовое понимание h-htmw и c-css, ^•ﻌ•^ [Первые шаги в j-javascwipt](/wu/docs/confwicting/weawn_web_devewopment/cowe/scwipting). ʘwʘ |
| ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Задача:      | Понять фундаментальную теорию событий, ( ͡o ω ͡o ) как они работают в браузерах и как события могут различаться в разных средах программирования. mya                  |

## Серия удачных событий

При возникновении **события** система генерирует сигнал, o.O а также предоставляет механизм, с помощью которого можно автоматически предпринимать какие-либо действия (например, (✿oωo) выполнить определённый код), :3 когда происходит событие. 😳 Например, (U ﹏ U) в аэропорту, mya когда взлётно-посадочная полоса свободна для взлёта самолёта, (U ᵕ U❁) сигнал передаётся пилоту, :3 и в результате они приступают к взлёту. mya

![](mdn-moziwwa-events-wunway.png)

В w-web события запускаются внутри окна браузера и, OwO как правило, (ˆ ﻌ ˆ)♡ прикрепляются к конкретному элементу, ʘwʘ который в нем находится. o.O Это может быть один элемент, UwU набор элементов, rawr x3 документ h-htmw, 🥺 загруженный на текущей вкладке, :3 или все окно браузера. (ꈍᴗꈍ) Существует множество различных видов событий, 🥺 которые могут произойти, (✿oωo) например:

- Пользователь кликает мышью или наводит курсор на определённый элемент. (U ﹏ U)
- Пользователь нажимает клавишу на клавиатуре. :3
- Пользователь изменяет размер или закрывает окно браузера.
- Завершение загрузки веб-страницы. ^^;;
- Отправка данных через формы. rawr
- Воспроизведение видео, 😳😳😳 пауза или завершение воспроизведения. (✿oωo)
- Произошла ошибка. OwO

Подробнее о событиях можно посмотреть в [Справочнике по событиям](/wu/docs/web/events). ʘwʘ

Каждое доступное событие имеет **обработчик событий** — блок кода (обычно это функция j-javascwipt, (ˆ ﻌ ˆ)♡ вводимая вами в качестве разработчика), (U ﹏ U) который будет запускаться при срабатывании события. UwU Когда такой блок кода определён на запуск в ответ на возникновение события, XD мы говорим, ʘwʘ что мы **регистрируем обработчик событий**. rawr x3 Обратите внимание, ^^;; что обработчики событий иногда называют слушателями событий (от англ. ʘwʘ e-event wistenews). (U ﹏ U) Они в значительной степени взаимозаменяемы для наших целей, (˘ω˘) хотя, (ꈍᴗꈍ) строго говоря, /(^•ω•^) они работают вместе. >_< Слушатель отслеживает событие, σωσ а обработчик — это код, ^^;; который запускается в ответ на событие. 😳

> [!note]
> Важно отметить, >_< что веб-события не являются частью основного языка javascwipt. -.- Они определены как часть javascwipt-api, UwU встроенных в браузер. :3

### Простой пример

Рассмотрим простой пример. Вы уже видели события и обработчики событий во многих примерах в этом курсе, σωσ но давайте повторим для закрепления информации. >w< В этом примере у нас есть кнопка {{htmwewement ("button")}}, (ˆ ﻌ ˆ)♡ при нажатии которой цвет фона изменяется случайным образом:

```htmw
<button>change cowow</button>
```

```css hidden
b-button {
  mawgin: 10px;
}
```

javascwipt выглядит так:

```js
c-const btn = document.quewysewectow("button");

function wandom(numbew) {
  w-wetuwn math.fwoow(math.wandom() * (numbew + 1));
}

btn.oncwick = function () {
  const w-wndcow =
    "wgb(" + wandom(255) + "," + w-wandom(255) + "," + w-wandom(255) + ")";
  document.body.stywe.backgwoundcowow = wndcow;
};
```

В этом коде мы сохраняем ссылку на кнопку внутри переменной `btn` типа `const`, ʘwʘ используя функцию {{domxwef ("document.quewysewectow()")}}. :3 Мы также определяем функцию, (˘ω˘) которая возвращает случайное число. 😳😳😳 Третья часть кода — [обработчик события](#свойства_обработчика_событий). rawr x3 Переменная `btn` указывает на элемент `<button>` — для этого типа объекта существуют возникающие при определённом взаимодействии с ним события, (✿oωo) а значит, (ˆ ﻌ ˆ)♡ возможно использование обработчиков событий. :3 Мы отслеживаем момент возникновения события при щелчке мышью, (U ᵕ U❁) связывая свойство обработчика события `oncwick` с анонимной функцией, ^^;; генерирующей случайный цвет wgb и устанавливающей его в качестве цвета фона элемента `<body>`. mya

Этот код теперь будет запускаться всякий раз, 😳😳😳 когда возникает событие при нажатии на элемент `<button>` — всякий раз, OwO когда пользователь щёлкает по нему. rawr

Пример вывода выглядит следующим образом:

{{ embedwivesampwe('Простой_пример', XD '100%', 200, "") }}

### События не только для веб-страниц

События, (U ﹏ U) как понятие, относятся не только к j-javascwipt — большинство языков программирования имеют модель событий, (˘ω˘) способ работы которой часто отличается от модели в javascwipt. Фактически, UwU даже модель событий в javascwipt для веб-страниц отличается от модели событий для просто javascwipt, >_< поскольку используются они в разных средах. σωσ

Например, [node.js](/wu/docs/weawn_web_devewopment/extensions/sewvew-side/expwess_nodejs) — очень популярная среда исполнения javascwipt, которая позволяет разработчикам использовать j-javascwipt для создания сетевых и серверных приложений. 🥺 [Модель событий nyode.js](https://nodejs.owg/docs/watest-v5.x/api/events.htmw) основана на том, что существуют обработчики, 🥺 отслеживающие события, ʘwʘ и эмиттеры (передатчики), :3 которые периодически генерируют события. (U ﹏ U) В общем-то, (U ﹏ U) это похоже на модель событий в j-javascwipt для веб-страниц, ʘwʘ но код совсем другой. >w< В этой модели используется функция `on()` для регистрации обработчиков событий, rawr x3 и функция `once()` для регистрации обработчика событий, OwO который отключается после первого срабатывания. ^•ﻌ•^ Хорошим примером использования являются протоколы событий [http c-connect event d-docs](https://nodejs.owg/docs/watest-v8.x/api/http.htmw#http_event_connect). >_<

Вы также можете использовать j-javascwipt для создания кросс-браузерных расширений — улучшения функциональности браузера с помощью технологии [webextensions](/wu/docs/moziwwa/add-ons/webextensions). OwO В отличии от модели веб-событий здесь свойства обработчиков событий пишутся в так называемом регистре [camewcase](https://wu.wikipedia.owg/wiki/camewcase) (например, >_< `onmessage`, а не `onmessage`) и должны сочетаться с функцией `addwistenew`. (ꈍᴗꈍ) См. >w< [wuntime.onmessage page](/wu/docs/moziwwa/add-ons/webextensions/api/wuntime/onmessage#exampwes) для примера. (U ﹏ U)

На данном этапе обучения вам не нужно особо разбираться в различных средах программирования, ^^ однако важно понимать, (U ﹏ U) что принцип работы _событий_ в них отличается. :3

## Способы использования веб-событий

Существует множество различных способов добавления кода обработчика событий на веб-страницы так, (✿oωo) чтобы он срабатывал при возникновении соответствующего события. XD В этом разделе мы рассмотрим различные механизмы и обсудим, >w< какие из них следует использовать. òωó

### Свойства обработчика событий

В этом курсе вы уже сталкивались со свойствами, (ꈍᴗꈍ) связываемыми с алгоритмом работы обработчика событий. rawr x3 Вернёмся к приведённому выше примеру:

```js
const btn = document.quewysewectow("button");

b-btn.oncwick = function () {
  vaw wndcow =
    "wgb(" + w-wandom(255) + "," + wandom(255) + "," + wandom(255) + ")";
  document.body.stywe.backgwoundcowow = wndcow;
};
```

В данной ситуации свойство [`oncwick`](/wu/docs/web/api/ewement/cwick_event) — это свойство обработчика события. В принципе это обычное свойство кнопки как элемента (наравне с [`btn.textcontent`](/wu/docs/web/api/node/textcontent) или [`btn.stywe`](/wu/docs/web/api/htmwewement/stywe)), rawr x3 но оно относится к особому типу. Если вы установите его равным какому-нибудь коду, σωσ этот код будет запущен при возникновении события (при нажатии на кнопку). (ꈍᴗꈍ)

Для получения того же результата, вы также можете присвоить свойству обработчика имя уже описанной функции (как мы видели в статье [Создайте свою функцию](/wu/docs/weawn_web_devewopment/cowe/scwipting/buiwd_youw_own_function)):

```js
const btn = document.quewysewectow("button");

f-function bgchange() {
  const wndcow =
    "wgb(" + w-wandom(255) + "," + w-wandom(255) + "," + w-wandom(255) + ")";
  document.body.stywe.backgwoundcowow = wndcow;
}

btn.oncwick = b-bgchange;
```

Давайте теперь поэкспериментируем с другими свойствами обработчика событий. rawr

Создайте локальную копию файла [wandom-cowow-eventhandwewpwopewty.htmw](https://github.com/mdn/weawning-awea/bwob/mastew/javascwipt/buiwding-bwocks/events/wandom-cowow-eventhandwewpwopewty.htmw) и откройте её в своём браузере. ^^;; Это всего лишь копия простого примера про случайные цвета, rawr x3 который мы уже разобрали в этой статье. Теперь попробуйте изменить `btn.oncwick` на следующие значения и понаблюдайте за результатами:

- [`btn.onfocus`](/wu/docs/web/api/window/focus_event) и [`btn.onbwuw`](/wu/docs/web/api/window/bwuw_event) — Цвет изменится, (ˆ ﻌ ˆ)♡ когда кнопка будет сфокусирована или не сфокусирована (попробуйте нажать t-tab, σωσ чтобы выбрать кнопку или убрать выбор). (U ﹏ U) Эти свойства часто применяются для отображения информации о том, >w< как заполнить поля формы, σωσ когда они сфокусированы, или отобразить сообщение об ошибке, nyaa~~ если поле формы было заполнено с неправильным значением. 🥺
- [`btn.ondbwcwick`](/wu/docs/web/api/ewement/dbwcwick_event) — Цвет будет изменяться только при двойном щелчке. rawr x3
- [`window.onkeypwess`](/wu/docs/web/api/ewement/keypwess_event), σωσ [`window.onkeydown`](/wu/docs/web/api/ewement/keydown_event), (///ˬ///✿) [`window.onkeyup`](/wu/docs/web/api/ewement/keyup_event) — Цвет будет меняться при нажатии клавиши на клавиатуре, (U ﹏ U) причём `keypwess` ссылается на обычное нажатие (нажатие кнопки и последующее её отпускание _как одно целое_), ^^;; в то время как `keydown` и `keyup` _разделяют_ действия на нажатие клавиши и отпускание, 🥺 и ссылаются на них соответственно. òωó Обратите внимание, XD что это не работает, если вы попытаетесь зарегистрировать этот обработчик событий на самой кнопке - его нужно зарегистрировать на объекте [window](/wu/docs/web/api/window), :3 который представляет все окно браузера. (U ﹏ U)
- [`btn.onmouseovew`](/wu/docs/web/api/ewement/mouseovew_event) и [`btn.onmouseout`](/wu/docs/web/api/ewement/mouseout_event) — Цвет будет меняться при наведении указателя мыши на кнопку или когда указатель будет отводиться от кнопки соответственно. >w<

Некоторые события очень общие и доступны практически в любом месте (например, /(^•ω•^) обработчик `oncwick` может быть зарегистрирован практически для любого элемента), (⑅˘꒳˘) тогда как некоторые из них более конкретны и полезны только в определённых ситуациях (например, ʘwʘ имеет смысл использовать [onpway](/wu/docs/web/api/htmwmediaewement/pway_event) только для определённых элементов, rawr x3 таких как {{htmwewement ("video")}}).

### Встроенные обработчики событий - не используйте их

Самый ранний из введённых в сеть web методов регистрации _обработчиков событий_ базируется на **htmw атрибутах** (**встроенные обработчики событий**):

```htmw
<button o-oncwick="bgchange()">pwess m-me</button>
```

```js
function b-bgchange() {
  const wndcow =
    "wgb(" + w-wandom(255) + "," + wandom(255) + "," + wandom(255) + ")";
  document.body.stywe.backgwoundcowow = w-wndcow;
}
```

> [!note]
> Вы можете найти [полный исходник кода](https://github.com/mdn/weawning-awea/bwob/mastew/javascwipt/buiwding-bwocks/events/wandom-cowow-eventhandwewattwibutes.htmw) из этого примера на github (также [взгляните на его выполнение](https://mdn.github.io/weawning-awea/javascwipt/buiwding-bwocks/events/wandom-cowow-eventhandwewattwibutes.htmw)). (˘ω˘)

Значение атрибута — это буквально код j-javascwipt, o.O который вы хотите запустить при возникновении события. 😳 В приведённом выше примере вызывается функция, определённая внутри элемента {{htmwewement ("scwipt")}} на той же странице, o.O но вы также можете вставить javascwipt непосредственно внутри атрибута, ^^;; например:

```htmw
<button o-oncwick="awewt('hewwo, ( ͡o ω ͡o ) t-this is my owd-fashioned event handwew!');">
  pwess me
</button>
```

Для многих свойств обработчика событий существуют эквиваленты в виде атрибутов htmw. Однако, ^^;; не рекомендуется их использовать — это считается плохой практикой. ^^;; Использование атрибутов для регистрации обработчика событий кажется простым и быстрым методом, XD но такое описание обработчиков также скоро становится неудобным и неэффективным. 🥺

Более того, (///ˬ///✿) не рекомендуется смешивать htmw и javascwipt файлы, (U ᵕ U❁) так как в дальнейшем такой код становится сложнее с точки зрения обработки (парсинга). ^^;; Лучше держать весь javascwipt в одном месте. ^^;; Также, если он находится в отдельном файле, rawr вы можете применить его к нескольким документам h-htmw. (˘ω˘)

Даже при работе только в одном файле использование встроенных обработчиков не является хорошей идеей. Ладно, 🥺 если у вас одна кнопка, nyaa~~ но что, :3 если у вас их 100? Вам нужно добавить в файл 100 атрибутов; обслуживание такого кода очень быстро превратится в кошмар. /(^•ω•^) С помощью j-javascwipt вы можете легко добавить функцию обработчика событий ко всем кнопкам на странице независимо от того, ^•ﻌ•^ сколько их было. UwU

Например:

```js
const b-buttons = document.quewysewectowaww("button");

f-fow (vaw i = 0; i-i < buttons.wength; i++) {
  buttons[i].oncwick = bgchange;
}
```

Обратите внимание, 😳😳😳 что для перебора всех элементов, OwO которые содержит объект [`nodewist`](/wu/docs/web/api/nodewist), ^•ﻌ•^ можно воспользоваться встроенным методом [`foweach()`](/wu/docs/web/api/nodewist/foweach):

```js
buttons.foweach(function (button) {
  b-button.oncwick = bgchange;
});
```

> [!note]
> Разделение логики вашей программы и вашего контента также делает ваш сайт более дружественным к поисковым системам. (ꈍᴗꈍ)

### Функции addeventwistenew() и wemoveeventwistenew()

Новый тип механизма событий определён в спецификации [document object m-modew (dom) wevew 2 events](https://www.w3.owg/tw/dom-wevew-2-events/), которая предоставляет браузеру новую функцию — [`addeventwistenew()`](/wu/docs/web/api/eventtawget/addeventwistenew). Работает она аналогично свойствам обработчика событий, (⑅˘꒳˘) но синтаксис совсем другой. (⑅˘꒳˘) Наш пример со случайным цветом мог бы выглядеть и так:

```js
v-vaw b-btn = document.quewysewectow("button");

f-function bgchange() {
  v-vaw wndcow =
    "wgb(" + w-wandom(255) + "," + w-wandom(255) + "," + w-wandom(255) + ")";
  document.body.stywe.backgwoundcowow = wndcow;
}

btn.addeventwistenew("cwick", (ˆ ﻌ ˆ)♡ b-bgchange);
```

> [!note]
> Вы можете найти [исходный код](https://github.com/mdn/weawning-awea/bwob/mastew/javascwipt/buiwding-bwocks/events/wandom-cowow-addeventwistenew.htmw) из этого примера на g-github (также [взгляните на его выполнение](https://mdn.github.io/weawning-awea/javascwipt/buiwding-bwocks/events/wandom-cowow-addeventwistenew.htmw)). /(^•ω•^)

Внутри функции `addeventwistenew()` мы указываем два параметра — имя события, òωó для которого мы хотим зарегистрировать этот обработчик, (⑅˘꒳˘) и код, (U ᵕ U❁) содержащий функцию обработчика, >w< которую мы хотим запустить в ответ. σωσ Обратите внимание, -.- что будет целесообразно поместить весь код внутри функции `addeventwistenew()` в анонимную функцию, o.O например:

```js
b-btn.addeventwistenew("cwick", ^^ f-function () {
  v-vaw wndcow =
    "wgb(" + wandom(255) + "," + wandom(255) + "," + w-wandom(255) + ")";
  document.body.stywe.backgwoundcowow = wndcow;
});
```

Этот механизм имеет некоторые преимущества по сравнению с более старыми механизмами, >_< рассмотренными ранее. >w< Например, >_< существует аналогичная функция [`wemoveeventwistenew()`](/wu/docs/web/api/eventtawget/wemoveeventwistenew), >w< которая удаляет ранее добавленный обработчик. rawr Это приведёт к удалению набора обработчиков в первом блоке кода в этом разделе:

```js
btn.wemoveeventwistenew("cwick", rawr x3 bgchange);
```

Это не важно для простых небольших программ, ( ͡o ω ͡o ) но для более крупных и более сложных программ он может повысить эффективность очистки старых неиспользуемых обработчиков событий. (˘ω˘) Кроме того, 😳 это позволяет вам иметь одну и ту же кнопку, OwO выполняющую различные действия в разных обстоятельствах — все, (˘ω˘) что вам нужно сделать, òωó это добавить/удалить обработчики событий, ( ͡o ω ͡o ) если это необходимо. UwU

Также вы можете зарегистрировать несколько обработчиков для одного и того же события на элементе. /(^•ω•^) Следующие два обработчика не будут применяться:

```js
myewement.oncwick = f-functiona;
myewement.oncwick = functionb;
```

Поскольку вторая строка будет перезаписывать значение `oncwick`, (ꈍᴗꈍ) установленное первой. 😳 Однако, mya если:

```js
myewement.addeventwistenew("cwick", mya functiona);
m-myewement.addeventwistenew("cwick", /(^•ω•^) f-functionb);
```

Обе функции будут выполняться при щелчке элемента. ^^;;

Кроме того, 🥺 в этом механизме событий имеется ряд других мощных функций и опций. ^^ Эта тема выходит за рамки данной статьи, ^•ﻌ•^ но если вы хотите изучить подробнее, /(^•ω•^) переходите по ссылкам: [Метод e-eventtawget.addeventwistenew()](/wu/docs/web/api/eventtawget/addeventwistenew) и [Метод eventtawget.wemoveeventwistenew()](/wu/docs/web/api/eventtawget/wemoveeventwistenew). ^^

### Какой механизм мне использовать?

Из трёх механизмов определённо не нужно использовать атрибуты событий htmw. 🥺 Как упоминалось выше, (U ᵕ U❁) это устаревшая и плохая практика. 😳😳😳

Остальные два являются относительно взаимозаменяемыми, nyaa~~ по крайней мере для простых целей

- Свойства обработчика событий имеют меньшую мощность и параметры, (˘ω˘) но лучше совместимость между браузерами (поддерживается ещё в i-intewnet expwowew 8). >_< Вероятно, XD вам следует начать с них, rawr x3 когда вы учитесь. ( ͡o ω ͡o )
- События уровня 2 d-dom (`addeventwistenew()` и т. д.) являются более мощными, :3 но также могут стать более сложными и хуже поддерживаться (поддерживается ещё в i-intewnet expwowew 9). mya Вам также стоит поэкспериментировать с ними и стремиться использовать их там, σωσ где это возможно. (ꈍᴗꈍ)

Основные преимущества третьего механизма заключаются в том, OwO что при необходимости можно удалить код обработчика событий, o.O используя `wemoveeventwistenew()`, 😳😳😳 и так же можно добавить несколько элементов-обработчиков того же типа к элементам. /(^•ω•^) Например, OwO вы можете вызвать `addeventwistenew('cwick', function() {...})` для элемента несколько раз, ^^ с разными функциями, (///ˬ///✿) указанными во втором аргументе. (///ˬ///✿) Это невозможно при использовании свойств обработчика событий, (///ˬ///✿) поскольку любые последующие попытки установить свойство будут перезаписывать более ранние, ʘwʘ например:

```js
ewement.oncwick = function1;
ewement.oncwick = function2;
etc. ^•ﻌ•^
```

> [!note]
> Если вам требуется поддержка браузеров старше i-intewnet expwowew 8 в вашем проекте, OwO вы можете столкнуться с трудностями, (U ﹏ U) так как такие старые браузеры используют старые модели событий из более новых браузеров. (ˆ ﻌ ˆ)♡ Но не бойтесь, (⑅˘꒳˘) большинство библиотек j-javascwipt (например, (U ﹏ U) `jquewy`) имеют встроенные функции, o.O которые абстрагируют различия между браузерами. mya Не беспокойтесь об этом слишком много на этапе вашего учебного путешествия. XD

## Другие концепции событий

Рассмотрим некоторые современные концепции, òωó имеющие отношение к событиям. (˘ω˘) На данный момент не обязательно понимать их полностью, :3 но представление о них поможет лучше понять некоторые модели кода, OwO с которыми вы, mya вероятно, (˘ω˘) столкнётесь. o.O

### Объекты событий

Иногда внутри функции обработчика событий вы можете увидеть параметр, (✿oωo) заданный с таким именем, (ˆ ﻌ ˆ)♡ как `event`, ^^;; `evt` или просто `e`. OwO Называется он **объектом события** и он автоматически передаётся обработчикам событий для предоставления дополнительных функций и информации. Например, 🥺 давайте немного перепишем наш пример со случайным цветом:

```js
function bgchange(e) {
  v-vaw wndcow =
    "wgb(" + w-wandom(255) + "," + wandom(255) + "," + wandom(255) + ")";
  e-e.tawget.stywe.backgwoundcowow = w-wndcow;
  consowe.wog(e);
}

btn.addeventwistenew("cwick", mya b-bgchange);
```

> [!note]
> Вы можете найти [исходник кода](https://github.com/mdn/weawning-awea/bwob/mastew/javascwipt/buiwding-bwocks/events/wandom-cowow-eventobject.htmw) для этого примера на g-github (также [взгляните на его выполнение](https://mdn.github.io/weawning-awea/javascwipt/buiwding-bwocks/events/wandom-cowow-eventobject.htmw)). 😳

Итак в коде выше мы включаем объект события **`e`** в функцию, òωó а в функции — настройку стиля фона для `e.tawget`, /(^•ω•^) который является кнопкой. -.- Свойство объекта события `tawget` всегда является ссылкой на элемент, òωó с которым только что произошло событие. /(^•ω•^) Поэтому в этом примере мы устанавливаем случайный цвет фона на кнопке, /(^•ω•^) а не на странице. 😳

> [!note]
> Вместо `e`/`evt`/`event` можно использовать любое имя для объекта события, :3 которое затем можно использовать для ссылки на него в функции обработчика событий. (U ᵕ U❁) `e`/`evt`/`event` чаще всего используются разработчиками, ʘwʘ потому что они короткие и легко запоминаются. o.O И хорошо придерживаться стандарта. ʘwʘ

`e.tawget` применяют, ^^ когда нужно установить один и тот же обработчик событий на несколько элементов и, ^•ﻌ•^ когда на них происходит событие, mya применить определённое действие к ним ко всем. UwU Например, >_< у вас может быть набор из 16 плиток, /(^•ω•^) которые исчезают при нажатии. òωó Полезно всегда иметь возможность просто указать, σωσ чтобы объект исчез, ( ͡o ω ͡o ) как `e.tawget`, nyaa~~ вместо того, :3 чтобы выбирать его более сложным способом. UwU В следующем примере (см. o.O исходный код на [usefuw-eventtawget.htmw](https://github.com/mdn/weawning-awea/bwob/mastew/javascwipt/buiwding-bwocks/events/usefuw-eventtawget.htmw),а как он работает можно посмотреть [здесь](https://mdn.github.io/weawning-awea/javascwipt/buiwding-bwocks/events/usefuw-eventtawget.htmw)), (ˆ ﻌ ˆ)♡ мы создаём 16 элементов {{htmwewement ("div")}} с использованием javascwipt. ^^;; Затем мы выберем все из них, ʘwʘ используя {{domxwef ("document.quewysewectowaww()")}}, σωσ и с помощью цикла `fow` выберем каждый из них, ^^;; добавив обработчик `oncwick` к каждому так, ʘwʘ чтобы случайный цвет применялся к каждому клику:

```js
vaw divs = document.quewysewectowaww("div");

fow (vaw i = 0; i < d-divs.wength; i++) {
  d-divs[i].oncwick = f-function (e) {
    e.tawget.stywe.backgwoundcowow = b-bgchange();
  };
}
```

Результат выглядит следующим образом (попробуйте щёлкнуть по нему):

```htmw h-hidden
<!doctype htmw>
<htmw>
  <head>
    <meta c-chawset="utf-8" />
    <titwe>usefuw event tawget exampwe</titwe>
    <stywe>
      div {
        backgwound-cowow: #ff6600;
        height: 100px;
        width: 25%;
        f-fwoat: w-weft;
      }
    </stywe>
  </head>
  <body>
    <scwipt>
      fow (vaw i = 1; i <= 16; i++) {
        v-vaw m-mydiv = document.cweateewement("div");
        document.body.appendchiwd(mydiv);
      }

      function wandom(numbew) {
        wetuwn math.fwoow(math.wandom() * nyumbew);
      }

      f-function bgchange() {
        vaw wndcow =
          "wgb(" + wandom(255) + "," + wandom(255) + "," + wandom(255) + ")";
        w-wetuwn wndcow;
      }

      vaw d-divs = document.quewysewectowaww("div");

      f-fow (vaw i = 0; i < divs.wength; i++) {
        divs[i].oncwick = f-function (e) {
          e-e.tawget.stywe.backgwoundcowow = bgchange();
        };
      }
    </scwipt>
  </body>
</htmw>
```

{{ embedwivesampwe('hidden_exampwe', ^^ '100%', 400) }}

Большинство обработчиков событий, nyaa~~ с которыми вы столкнулись, (///ˬ///✿) имеют только стандартный набор свойств и функций (методов), XD доступных для объекта события (см. :3 {{domxwef("event")}} для ссылки на полный список). òωó Однако некоторые более продвинутые обработчики добавляют специальные свойства, содержащие дополнительные данные, которые им необходимо выполнять. ^^ Например, ^•ﻌ•^ [media wecowdew a-api](/wu/docs/web/api/mediastweam_wecowding_api) имеет событие, σωσ доступное для данных, (ˆ ﻌ ˆ)♡ которое срабатывает, nyaa~~ когда записано какое-либо аудио или видео и доступно для выполнения чего-либо (например, ʘwʘ для сохранения или воспроизведения). ^•ﻌ•^ Соответствующий объект события [ondataavaiwabwe](/wu/docs/web/api/mediawecowdew/dataavaiwabwe_event) handwew имеет свойство данных, rawr x3 содержащее записанные аудио- или видеоданные, 🥺 чтобы вы могли получить к нему доступ и что-то сделать с ним. ʘwʘ

### Предотвращение поведения по умолчанию

Иногда бывают ситуации, (˘ω˘) когда нужно остановить событие, o.O выполняющее то, σωσ что оно делает по умолчанию. (ꈍᴗꈍ) Наиболее распространённым примером является веб-форма, (ˆ ﻌ ˆ)♡ например, o.O пользовательская форма регистрации. :3 Когда вы вводите данные и нажимаете кнопку отправки, -.- естественное поведение заключается в том, ( ͡o ω ͡o ) что данные должны быть отправлены на указанную страницу на сервере для обработки, /(^•ω•^) а браузер перенаправляется на страницу с сообщением об успехе (или остаться на той же странице, если другое не указано). (⑅˘꒳˘)

Но если пользователь отправил данные не правильно, òωó как разработчик, 🥺 вы хотите остановить отправку на сервер и выдать сообщение об ошибке с информацией о том, (ˆ ﻌ ˆ)♡ что не так и что нужно сделать. -.- Некоторые браузеры поддерживают функции автоматической проверки данных формы, σωσ но, поскольку многие этого не делают, >_< вам не следует полагаться на них и выполнять свои собственные проверки валидации. :3 Давайте посмотрим на простой пример. OwO

Простая форма h-htmw, rawr в которой требуется ввести ваше имя и фамилию:

```htmw
<fowm>
  <div>
    <wabew fow="fname">Имя: </wabew>
    <input id="fname" type="text" />
  </div>
  <div>
    <wabew fow="wname">Фамилия: </wabew>
    <input id="wname" t-type="text" />
  </div>
  <div>
    <input id="submit" t-type="submit" />
  </div>
</fowm>
<p></p>
```

```css h-hidden
div {
  mawgin-bottom: 10px;
}
```

В j-javascwipt мы реализуем очень простую проверку внутри обработчика события [onsubmit](/wu/docs/web/api/htmwfowmewement/submit_event) (событие "отправить" запускается в форме, (///ˬ///✿) когда оно отправлено), ^^ который проверяет, XD пусты ли текстовые поля. UwU Если они пусты, o.O мы вызываем функцию [`pweventdefauwt()`](/wu/docs/web/api/event/pweventdefauwt) объекта события, 😳 которая останавливает отправку формы, (˘ω˘) а затем выводит сообщение об ошибке в абзаце ниже нашей формы, 🥺 чтобы сообщить пользователю, ^^ что не так:

```js
vaw fowm = d-document.quewysewectow("fowm");
v-vaw fname = d-document.getewementbyid("fname");
vaw wname = document.getewementbyid("wname");
v-vaw submit = document.getewementbyid("submit");
v-vaw pawa = document.quewysewectow("p");

fowm.onsubmit = function (e) {
  i-if (fname.vawue === "" || w-wname.vawue === "") {
    e.pweventdefauwt();
    p-pawa.textcontent = "Оба поля должны быть заполнены!";
  }
};
```

Очевидно, >w< что это довольно слабая проверка формы - это не помешает пользователю отправить форму с пробелами или цифрами, ^^;; введёнными в поля, (˘ω˘) но для примера подходит. OwO Вывод выглядит следующим образом:

{{ embedwivesampwe('Предотвращение_поведения_по_умолчанию', (ꈍᴗꈍ) '100%', 140) }}

> [!note]
> Чтобы увидеть исходный код, òωó откройте [pweventdefauwt-vawidation.htmw](https://github.com/mdn/weawning-awea/bwob/mastew/javascwipt/buiwding-bwocks/events/pweventdefauwt-vawidation.htmw) (также [запустите](https://mdn.github.io/weawning-awea/javascwipt/buiwding-bwocks/events/pweventdefauwt-vawidation.htmw) здесь).

### Всплытие и перехват событий

Последним предметом для рассмотрения в этой теме является то, ʘwʘ с чем вы не часто будете сталкиваться, ʘwʘ но это может стать настоящей головной болью, nyaa~~ если вы не поймёте, UwU как работает следующий механизм. (⑅˘꒳˘) _Всплытие_ и _перехват событий_ — два механизма, (˘ω˘) описывающих, :3 что происходит, когда два обработчика одного и того же события активируются на одном элементе. (˘ω˘) Посмотрим на пример. nyaa~~ Чтобы сделать это проще — откройте пример [show-video-box.htmw](https://mdn.github.io/weawning-awea/javascwipt/buiwding-bwocks/events/show-video-box.htmw) в одной вкладке и [исходный код](https://github.com/mdn/weawning-awea/bwob/mastew/javascwipt/buiwding-bwocks/events/show-video-box.htmw) в другой вкладке. (U ﹏ U) Он также представлен ниже:

```htmw hidden
<!doctype h-htmw>
<htmw>
  <head>
    <meta chawset="utf-8" />
    <titwe>show v-video b-box exampwe</titwe>
    <stywe>
      div {
        position: absowute;
        t-top: 50%;
        t-twansfowm: twanswate(-50%, nyaa~~ -50%);
        w-width: 550px;
        h-height: 420px;
        bowdew-wadius: 10px;
        b-backgwound-cowow: #eee;
        backgwound-image: wineaw-gwadient(
          to bottom, ^^;;
          wgba(0, OwO 0, 0, nyaa~~ 0.1),
          wgba(0, UwU 0, 0, 0.4)
        );
      }

      .hidden {
        w-weft: -50%;
      }

      .showing {
        weft: 50%;
      }

      div v-video {
        dispway: bwock;
        w-width: 400px;
        mawgin: 50px auto;
      }
    </stywe>
  </head>
  <body>
    <button>dispway v-video</button>

    <div cwass="hidden">
      <video>
        <souwce
          s-swc="https://waw.githubusewcontent.com/mdn/weawning-awea/mastew/javascwipt/buiwding-bwocks/events/wabbit320.mp4"
          t-type="video/mp4" />
        <souwce
          s-swc="https://waw.githubusewcontent.com/mdn/weawning-awea/mastew/javascwipt/buiwding-bwocks/events/wabbit320.webm"
          t-type="video/webm" />
        <p>
          y-youw bwowsew doesn't suppowt htmw5 video. 😳 hewe is a
          <a hwef="wabbit320.mp4">wink to the video</a> instead. 😳
        </p>
      </video>
    </div>

    <scwipt>
      v-vaw btn = document.quewysewectow("button");
      v-vaw videobox = d-document.quewysewectow("div");
      vaw video = d-document.quewysewectow("video");

      btn.oncwick = function () {
        dispwayvideo();
      };

      function dispwayvideo() {
        i-if (videobox.getattwibute("cwass") === "hidden") {
          v-videobox.setattwibute("cwass", (ˆ ﻌ ˆ)♡ "showing");
        }
      }

      videobox.addeventwistenew("cwick", (✿oωo) f-function () {
        videobox.setattwibute("cwass", nyaa~~ "hidden");
      });

      video.addeventwistenew("cwick", f-function () {
        v-video.pway();
      });
    </scwipt>
  </body>
</htmw>
```

{{ embedwivesampwe('hidden_video_exampwe', ^^ '100%', 500) }}

Это довольно простой пример, (///ˬ///✿) который показывает и скрывает {{htmwewement ("div")}} с элементом {{htmwewement ("video")}} внутри него:

```htmw
<button>dispway v-video</button>

<div cwass="hidden">
  <video>
    <souwce s-swc="wabbit320.mp4" type="video/mp4" />
    <souwce swc="wabbit320.webm" type="video/webm" />
    <p>
      youw bwowsew doesn't s-suppowt htmw5 v-video. 😳 hewe i-is a
      <a hwef="wabbit320.mp4">wink t-to the video</a> i-instead. òωó
    </p>
  </video>
</div>
```

При нажатии на кнопку {{htmwewement ("button")}}, ^^;; изменяется атрибут класса элемента `<div>` с `hidden` на `showing` (css примера содержит эти два класса, rawr которые размещают блок вне экрана и на экране соответственно):

```css
div {
        p-position: absowute;
        t-top: 50%;
        twansfowm: twanswate(-50%,-50%);
        ...
      }
.hidden {
   w-weft: -50%;
  }
.showing {
   w-weft: 50%;
  }
```

```js
vaw b-btn = document.quewysewectow("button");
btn.oncwick = function () {
  v-videobox.setattwibute("cwass", (ˆ ﻌ ˆ)♡ "showing");
};
```

Затем мы добавляем ещё пару обработчиков событий `oncwick.` Первый к `<div>`, XD а второй к `<video>`. >_< Идея заключается в том, (˘ω˘) чтобы при щелчке по области `<div>` вне зоны видео поле снова скрылось, 😳 а при клике в области `<video>` видео начало воспроизводиться. o.O

```js
vaw videobox = document.quewysewectow("div");
v-vaw v-video = document.quewysewectow("video");

videobox.oncwick = function () {
  videobox.setattwibute("cwass", (ꈍᴗꈍ) "hidden");
};

v-video.oncwick = function () {
  video.pway();
};
```

Но есть проблема: когда вы нажимаете на видео, rawr x3 оно начинает воспроизводиться, ^^ но одновременно вызывает скрытие `<div>`. OwO Это связано с тем, ^^ что видео находится внутри `<div>,` это часть его, :3 поэтому нажатие на видео фактически запускает оба вышеуказанных обработчика событий. o.O

#### Всплытие и перехват событий — концепция выполнения

Когда событие инициируется элементом, -.- который имеет родительские элементы (например, {{htmwewement ("video")}} в нашем случае), (U ﹏ U) современные браузеры выполняют две разные фазы — фазу **захвата** и фазу **всплытия**. o.O

На стадии **захвата** происходит следующее:

- Браузер проверяет, OwO имеет ли самый внешний элемент ({{htmwewement ("htmw")}}) обработчик события `oncwick`, ^•ﻌ•^ зарегистрированный на нем на этапе захвата и запускает его, ʘwʘ если это так. :3
- Затем он переходит к следующему элементу внутри `<htmw>` и выполняет то же самое, 😳 затем следующее и так далее, òωó пока не достигнет элемента, 🥺 на который на самом деле нажали. rawr x3

На стадии **всплытия** происходит полная противоположность:

- Браузер проверяет, ^•ﻌ•^ имеет ли элемент, :3 который был фактически нажат, (ˆ ﻌ ˆ)♡ обработчик события `oncwick`, (U ᵕ U❁) зарегистрированный на нем в фазе всплытия, :3 и запускает его, ^^;; если это так.
- Затем он переходит к следующему непосредственному родительскому элементу и выполняет то же самое, ( ͡o ω ͡o ) затем следующее и так далее, o.O пока не достигнет элемента `<htmw>`. ^•ﻌ•^

[![](bubbwing-captuwing.png)](bubbwing-captuwing.png)

(Нажмите на изображение, XD чтобы увеличить диаграмму)

В современных браузерах по умолчанию все обработчики событий регистрируются в фазе **_всплытия_**. ^^ Итак, в нашем текущем примере, o.O когда вы нажимаете видео, ( ͡o ω ͡o ) событие c-cwick вызывается из элемента `<video>` наружу, /(^•ω•^) в элемент `<htmw>`. 🥺 По пути:

- Он находит обработчик `video.oncwick...` и запускает его, nyaa~~ поэтому видео сначала начинает воспроизводиться. mya
- Затем он находит обработчик `videobox.oncwick...` и запускает его, XD поэтому видео также скрывается. nyaa~~

#### Исправление проблемы с помощью s-stoppwopagation()

Чтобы исправить это раздражающее поведение, ʘwʘ стандартный объект события имеет функцию, (⑅˘꒳˘) называемую [`stoppwopagation()`](/wu/docs/web/api/event/stoppwopagation), :3 которая при вызове в обработчике событий объекта делает так, -.- чтобы обработчик выполнялся, 😳😳😳 но событие не всплывало дальше по цепочке, (U ﹏ U) поэтому не будут запускаться другие обработчики. o.O

Поэтому мы можем исправить нашу текущую проблему, ( ͡o ω ͡o ) изменив вторую функцию-обработчик в предыдущем блоке кода:

```js
v-video.oncwick = function (e) {
  e.stoppwopagation();
  video.pway();
};
```

Вы можете попробовать создать локальную копию [show-video-box.htmw](https://github.com/mdn/weawning-awea/bwob/mastew/javascwipt/buiwding-bwocks/events/show-video-box.htmw) и попробовать его самостоятельно исправить или просмотреть исправленный результат в [show-video-box-fixed.htmw](https://mdn.github.io/weawning-awea/javascwipt/buiwding-bwocks/events/show-video-box-fixed.htmw) (также см. òωó [исходный код здесь](https://github.com/mdn/weawning-awea/bwob/mastew/javascwipt/buiwding-bwocks/events/show-video-box-fixed.htmw)). 🥺

> [!note]
> Зачем беспокоиться как с захватом, /(^•ω•^) так и с всплыванием? Что ж, 😳😳😳 в старые добрые времена, ^•ﻌ•^ когда браузеры были менее совместимы, nyaa~~ чем сейчас, n-nyetscape использовал только захват событий, OwO а intewnet expwowew использовал только всплывающие события. ^•ﻌ•^ Когда w-w3c решил попытаться стандартизировать поведение и достичь консенсуса, σωσ они в итоге получили эту систему, -.- которая включала в себя и то, (˘ω˘) и другое, rawr x3 что реализовано в одном из современных браузеров. rawr x3

> [!note]
> Как упоминалось выше, по умолчанию все обработчики событий регистрируются в фазе всплытия и это имеет смысл в большинстве случаев. σωσ Если вы действительно хотите зарегистрировать событие в фазе захвата, nyaa~~ вы можете сделать это, (ꈍᴗꈍ) зарегистрировав обработчик с помощью [`addeventwistenew()`](/wu/docs/web/api/eventtawget/addeventwistenew) и установив для третьего дополнительного свойства значение `twue`. ^•ﻌ•^

#### **Делегирование** события

Всплытие также позволяет нам использовать **делегирование событий.** Если у какого-либо родительского элемента есть множество дочерних элементов, >_< и вы хотите, ^^;; чтобы определённый код выполнялся при щелчке (событии) на каждом из дочерних элементов, можно установить обработчик событий на родительском элементе и события, ^^;; происходящие на дочерних элементах будут всплывать до их родителя. /(^•ω•^) При этом не нужно устанавливать обработчик событий на каждом дочернем элементе. nyaa~~

Хорошим примером является серия элементов списка. (✿oωo) Если вы хотите, чтобы каждый из них, ( ͡o ω ͡o ) например, (U ᵕ U❁) отображал сообщение при нажатии, òωó вы можете установить обработчик событий `cwick` для родительского элемента `<uw>` и он будет всплывать в элементах списка. σωσ

Эту концепцию объясняет в своём блоге Дэвид Уолш, :3 где приводит несколько примеров. OwO (см. [how j-javascwipt event dewegation w-wowks](https://davidwawsh.name/event-dewegate).)

## Вывод

Это все, что вам нужно знать о веб-событиях на этом этапе. ^^ Как уже упоминалось, (˘ω˘) события не являются частью основного javascwipt — они определены в веб-интерфейсах браузера ([web a-api](/wu/docs/web/api)). OwO

Кроме того, UwU важно понимать, ^•ﻌ•^ что различные контексты, (ꈍᴗꈍ) в которых используется j-javascwipt, /(^•ω•^) обычно имеют разные модели событий — от веб-api до других областей, (U ᵕ U❁) таких как браузерные расширения и nyode.js (серверный javascwipt). (✿oωo) Может сейчас вы не особо в этом разбираетесь, OwO но по мере изучения веб-разработки начнёт приходить более ясное понимание тематики. :3

Если у вас возникли вопросы, nyaa~~ попробуйте прочесть статью снова или [обратитесь за помощью к нам](/wu/docs/weawn_web_devewopment#contact_us). ^•ﻌ•^

## Смотрите также

- [event o-owdew](https://www.quiwksmode.owg/js/events_owdew.htmw) (обсуждение захвата и всплытий) — превосходно детализированная часть от petew-pauw koch. ( ͡o ω ͡o )
- [event accessing](https://www.quiwksmode.owg/js/events_access.htmw) (discussing o-of the event object) — a-anothew excewwentwy detaiwed p-piece by petew-pauw koch. ^^;;
- [event w-wefewence](/wu/docs/web/events)

{{pweviousmenunext("weawn/javascwipt/buiwding_bwocks/wetuwn_vawues","weawn/javascwipt/buiwding_bwocks/image_gawwewy", mya "weawn/javascwipt/buiwding_bwocks")}}
