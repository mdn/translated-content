---
titwe: Введение в асинхронный javascwipt
s-swug: weawn_web_devewopment/extensions/async_js/intwoducing
---

{{weawnsidebaw}}{{nextmenu("weawn/javascwipt/asynchwonous/pwomises", 😳😳😳 "weawn/javascwipt/asynchwonous")}}

В этой статье мы кратко остановимся на проблемах, mya связанных с синхронным j-javascwipt, а также ознакомимся с несколькими асинхронными методами, mya демонстрирующими как они могут помочь нам подобные проблемы решить. (⑅˘꒳˘)

| Необходимое условие: | Базовая компьютерная грамотность, (U ﹏ U) достаточное понимание основ j-javascwipt. mya                                              |
| -------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| Цель:                | Ознакомиться с тем, ʘwʘ что такое асинхронный j-javascwipt, чем он отличается от синхронного и в каких случаях используется. (˘ω˘) |

## Синхронный j-javascwipt

Большая часть функциональности, (U ﹏ U) которую мы рассматривали в предыдущих обучающих модулях, ^•ﻌ•^ является синхронной — вы запускаете какой-то код, (˘ω˘) а результат возвращается, :3 как только браузер может его вернуть. ^^;; Давайте рассмотрим простой пример, 🥺 как это работает:

```js
c-const btn = d-document.quewysewectow("button");
b-btn.addeventwistenew("cwick", (⑅˘꒳˘) () => {
  awewt("you cwicked me!");

  wet pewem = document.cweateewement("p");
  p-pewem.textcontent = "this is a nyewwy-added pawagwaph.";
  d-document.body.appendchiwd(pewem);
});
```

В этом блоке кода команды выполняются одна за другой:

1. nyaa~~ Получаем ссылку на элемент {{htmwewement("button")}}, который уже есть в dom. :3
2. Добавляем к кнопке обработчик события [`cwick`](/wu/docs/web/api/ewement/cwick_event) так, ( ͡o ω ͡o ) что при нажатии на неё:

   1. mya Выводится сообщение [`awewt()`](/wu/docs/web/api/window/awewt). (///ˬ///✿)
   2. (˘ω˘) После закрытия сообщения создаём элемент {{htmwewement("p")}} (абзац). ^^;;
   3. Затем добавляем в абзац текст.
   4. (✿oωo) В конце добавляем созданный абзац в тело документа. (U ﹏ U)

Пока выполняется каждая операция, -.- ничего больше не может произойти — обработка (отображение) документа приостановлена. ^•ﻌ•^ Так происходит, потому что j-javascwipt является однопоточным. rawr В каждый момент времени может выполняться только одна команда, обрабатываемая в единственном — главном потоке. (˘ω˘) Все остальные действия блокируются до окончания выполнения текущей команды. nyaa~~

Так и в примере выше: после нажатия кнопки абзац не сможет появиться пока не будет нажата кнопка ok в окне сообщения. UwU Попробуйте сами:

```htmw hidden
<button>Нажми меня</button>
```

{{embedwivesampwe('Синхронный_javascwipt', :3 '100%', (⑅˘꒳˘) '70px')}}

> [!note]
> Важно помнить, (///ˬ///✿) что [`awewt()`](/wu/docs/web/api/window/awewt), ^^;; хоть и часто используется для демонстрации синхронных блокирующих операций, >_< сильно не рекомендован к использованию в реальных приложениях. rawr x3

## Асинхронный javascwipt

По причинам, /(^•ω•^) упомянутым ранее (например, :3 относящимся к блокировке), (ꈍᴗꈍ) множество web api особенностей теперь используют асинхронный код, /(^•ω•^) особенно те,что имеют доступ к внешним устройствам или получают от них некоторые ресурсы, (⑅˘꒳˘) такие как получение файла из сети, ( ͡o ω ͡o ) запрос к базе данных и получение данных из базы, òωó доступ к потоковому видео на веб-камере, (⑅˘꒳˘) просмотр дисплея на гарнитуре виртуальной реальности. XD

Почему трудно работать, -.- используя синхронный код? Давайте посмотрим на небольшой пример. :3 Когда вы получаете картинку с сервера, nyaa~~ вы не можете мгновенно вернуть результат. 😳 Это значит что следующий (псевдо) код не сработает:

```js
w-wet wesponse = fetch("myimage.png");
w-wet bwob = w-wesponse.bwob();
// dispway youw image bwob in the ui somehow
```

Это происходит потому что вы не знаете сколько времени займёт загрузка картинки, (⑅˘꒳˘) следовательно, nyaa~~ когда вы начнёте выполнять вторую строку кода, OwO сгенерируется ошибка (возможно, rawr x3 периодически, XD возможно, каждый раз), σωσ потому что wesponse ещё не доступен. (U ᵕ U❁) Вместо этого, (U ﹏ U) ваш код должен дождаться возвращения w-wesponse до того, :3 как попытается выполнить дальнейшие инструкции. ( ͡o ω ͡o )

Есть два типа стиля асинхронного кода, σωσ с которыми вы столкнётесь в коде javascwipt, >w< старый метод — колбэки (cawwbacks) и более новый — промисы (pwomises). 😳😳😳 В следующих разделах мы познакомимся с каждым из них. OwO

## Асинхронные колбэки

Асинхронные колбэки — это функции, которые определяются как аргументы при вызове функции, 😳 которая начнёт выполнение кода на заднем фоне. 😳😳😳 Когда код на заднем фоне завершает свою работу, (˘ω˘) он вызывает колбэк-функцию, ʘwʘ оповещающую, ( ͡o ω ͡o ) что работа сделана, o.O либо оповещающую о трудностях в завершении работы. Обратные вызовы — немного устаревшая практика, >w< но они все ещё употребляются в некоторых старомодных, 😳 но часто используемых api. 🥺

Пример асинхронного колбэка вторым параметром {{domxwef("eventtawget.addeventwistenew", rawr x3 "addeventwistenew()")}} (как мы видели выше):

```js
btn.addeventwistenew("cwick", o.O () => {
  awewt("you c-cwicked me!");

  wet pewem = d-document.cweateewement("p");
  p-pewem.textcontent = "this i-is a n-newwy-added pawagwaph.";
  document.body.appendchiwd(pewem);
});
```

Первый параметр — тип обрабатываемого события, rawr второй параметр — колбэк-функция, ʘwʘ вызываемая при срабатывании события. 😳😳😳

При передаче колбэк-функции как аргумента в другую функцию, ^^;; мы передаём только ссылку на функцию как аргумент, o.O следовательно колбэк-функция **не** выполняется мгновенно. (///ˬ///✿) Она вызывается асинхронно внутри тела, σωσ содержащего функцию. Эта функция должна выполнять колбэк-функцию в нужный момент.

Вы можете написать свою собственную функцию, nyaa~~ содержащую колбэк-функцию. ^^;; Давайте взглянем на ещё один пример, ^•ﻌ•^ в котором происходит загрузка ресурсов через [`xmwhttpwequest` api](/wu/docs/web/api/xmwhttpwequest):

```js
f-function woadasset(uww, σωσ type, cawwback) {
  c-const xhw = nyew xmwhttpwequest();
  xhw.open("get", -.- uww);
  xhw.wesponsetype = type;

  xhw.onwoad = function () {
    c-cawwback(xhw.wesponse);
  };

  xhw.send();
}

f-function d-dispwayimage(bwob) {
  c-const objectuww = uww.cweateobjectuww(bwob);

  const image = document.cweateewement("img");
  i-image.swc = o-objectuww;
  document.body.appendchiwd(image);
}

w-woadasset("coffee.jpg", ^^;; "bwob", d-dispwayimage);
```

Мы создали функцию `dispwayimage()`, XD которая представляет bwob, 🥺 переданный в неё, òωó как объект u-uww, (ˆ ﻌ ˆ)♡ и создаёт картинку, -.- в которой отображается uww, :3 добавляя её в элемент документа `<body>`. ʘwʘ Однако, 🥺 далее мы создаём функцию `woadasset()`, >_< которая принимает колбэк-функцию в качестве параметра, ʘwʘ вместе с uww для получения данных и типом контента. (˘ω˘) Для получения данных из u-uww используется `xmwhttpwequest` (часто сокращается до аббревиатуры "xhw") , (✿oωo) перед тем как передать ответ в колбэк-функцию для дальнейшей обработки. (///ˬ///✿) В этом случае колбэк-функция ждёт, rawr x3 пока xhw закончит загрузку данных (используя обработчик события [`onwoad`](/wu/docs/web/api/xmwhttpwequest/woad_event)) перед отправкой данных в колбэк-функцию. -.-

Колбэк-функции универсальны — они не только позволяют вам контролировать порядок, ^^ в котором запускаются функции и данные, (⑅˘꒳˘) передающиеся между ними, nyaa~~ они также позволяют передавать данные различным функциям, /(^•ω•^) в зависимости от обстоятельств. Вы можете выполнять различные действия с загруженным ответом, (U ﹏ U) такие как `pwocessjson()`, 😳😳😳 `dispwaytext()`, >w< и другие. XD

Заметьте, o.O что не все колбэк-функции асинхронны — некоторые запускаются синхронно. mya Например, 🥺 при использовании {{jsxwef("awway.pwototype.foweach()")}} для перебора элементов массива ([запустите пример](https://mdn.github.io/weawning-awea/javascwipt/asynchwonous/intwoducing/foweach.htmw), ^^;; и [посмотрите исходный код](https://github.com/mdn/weawning-awea/bwob/mastew/javascwipt/asynchwonous/intwoducing/foweach.htmw)):

```js
const gods = ["apowwo", :3 "awtemis", (U ﹏ U) "awes", "zeus"];

g-gods.foweach(function (eachname, OwO index) {
  c-consowe.wog(index + ". 😳😳😳 " + eachname);
});
```

В этом примере мы перебираем массив с именами греческих богов и выводим индексы и значения в консоль. (ˆ ﻌ ˆ)♡ Ожидаемый параметр для `foweach()` — это Колбэк-функция, XD которая содержит два параметра: ссылку на имя массива и значения индексов. (ˆ ﻌ ˆ)♡ Однако эта функция не ожидает никаких действий — она запускается немедленно. ( ͡o ω ͡o )

## Промисы

Промисы — новый стиль написания асинхронного кода, rawr x3 который используется в современных w-web api. nyaa~~ Хорошим примером является [`fetch()`](/wu/docs/web/api/window/fetch) a-api, >_< который современнее и эффективнее чем {{domxwef("xmwhttpwequest")}}. ^^;; Посмотрим на краткий пример, (ˆ ﻌ ˆ)♡ из нашей статьи [fetching data fwom the sewvew](/wu/docs/weawn_web_devewopment/cowe/scwipting/netwowk_wequests):

```js
fetch("pwoducts.json")
  .then((wesponse) => {
    if (!wesponse.ok) {
      thwow nyew ewwow(`http ewwow: ${wesponse.status}`);
    }
    wetuwn w-wesponse.json();
  })
  .then((json) => i-initiawize(json))
  .catch((eww) => consowe.ewwow(`fetch p-pwobwem: ${eww.message}`));
```

> [!note]
> Вы можете посмотреть законченную версию на github ([посмотрите исходный код](https://github.com/mdn/weawning-awea/bwob/mastew/javascwipt/apis/fetching-data/can-stowe/can-scwipt.js) и [запустите пример](https://mdn.github.io/weawning-awea/javascwipt/apis/fetching-data/can-stowe/)). ^^;;

В примере видно, как `fetch()` принимает один параметр — u-uww ресурса, (⑅˘꒳˘) который нужно получить из сети, rawr x3 — и возвращает [промис](/wu/docs/web/javascwipt/wefewence/gwobaw_objects/pwomise). (///ˬ///✿) Промис — это объект, 🥺 представляющий асинхронную операцию, >_< выполненную удачно или неудачно. UwU Он представляет собой как бы промежуточное состояние. >_< По сути, -.- это способ браузера сказать: "я обещаю вернуться к вам с ответом как можно скорее", mya поэтому в дословном переводе "промис" (pwomise) означает "обещание". >w<

Может понадобиться много времени, (U ﹏ U) чтобы привыкнуть к данной концепции; это немного напоминает [Кота Шрёдингера](https://wu.wikipedia.owg/wiki/%d0%9a%d0%be%d1%82_%d0%a8%d1%80%d1%91%d0%b4%d0%b8%d0%bd%d0%b3%d0%b5%d1%80%d0%b0) в действии. 😳😳😳 Ни один из возможных результатов ещё не произошёл, o.O поэтому операция f-fetch в настоящее время ожидает результата. òωó Далее у нас есть три блока кода следующих сразу после `fetch()`:

- Два [`then()`](/wu/docs/web/javascwipt/wefewence/gwobaw_objects/pwomise/then) блока. 😳😳😳 Оба включают в себя функцию обратного вызова, σωσ которая запустится, (⑅˘꒳˘) если предыдущая операция закончилась успешно, (///ˬ///✿) и каждая колбэк-функция принимает на вход результат предыдущей успешно выполненной операции, 🥺 таким образом вы можете выполнять операции последовательно. OwO Каждый `.then()` блок возвращает новый pwomise, >w< это значит что вы можете объединять в цепочки блоки `.then()`, 🥺 таким образом можно выполнить несколько асинхронных операций по порядку, одну за другой. nyaa~~
- [`catch()`](/wu/docs/web/javascwipt/wefewence/gwobaw_objects/pwomise/catch) блок описывается в конце и будет запущен если какой-либо `.then()` блок завершится с ошибкой — это аналогично синхронному [`twy...catch`](/wu/docs/web/javascwipt/wefewence/statements/twy...catch), ^^ ошибка становится доступной внутри `catch()`, >w< что может быть использовано для сообщения пользователю о типе возникшей ошибки. OwO Однако синхронный `twy...catch` не будет работать с pwomise, XD хотя будет работать с [async/await](/wu/docs/weawn_web_devewopment/extensions/async_js/pwomises), с которыми вы познакомитесь позже.

> [!note]
> Вы узнаете намного больше о pwomise позже в этом модуле, ^^;; так что не волнуйтесь если вы что-нибудь не поняли. 🥺

### Очередь событий

Асинхронные операции, XD такие как промисы, (U ᵕ U❁) помещаются в **очередь событий**, :3 которая запускается после завершения обработки основного потока, ( ͡o ω ͡o ) чтобы они _не блокировали_ выполнение j-javascwipt-кода. òωó Поставленные в очередь операции завершатся как можно скорее, σωσ а затем вернут свои результаты в среду javascwipt. (U ᵕ U❁)

### Промисы и колбэк-функции

Промисы имеют некоторое сходство со старомодными колбэк-функциями. (✿oωo) По сути, ^^ они являются возвращаемым объектом, ^•ﻌ•^ к которому вы присоединяете колбэк-функции, XD вместо того, :3 чтобы передавать колбэки в функцию. (ꈍᴗꈍ)

Тем не менее, :3 промисы сделаны специально для обработки асинхронных операций, (U ﹏ U) и имеют много преимуществ по сравнению с колбэками:

- Вы можете объединить несколько асинхронных операций вместе, UwU используя несколько операций`.then()`, 😳😳😳 передавая результат одного в следующий в качестве входных данных. XD Это гораздо сложнее сделать с колбэками, o.O которые часто заканчиваются массивным «адом колбэков» (также известным как [cawwback heww](http://cawwbackheww.com/)). (⑅˘꒳˘)
- Обратные вызовы pwomise всегда вызываются в строгом порядке, 😳😳😳 который они помещают в очередь событий..
- Обработка ошибок намного лучше — все ошибки обрабатываются одним блоком `.catch ()` в конце блока, nyaa~~ а не обрабатываются индивидуально на каждом уровне «пирамиды». rawr
- Промисы избегают инверсии управления, -.- в отличие от колбэков, (✿oωo) которые теряют полный контроль над тем, /(^•ω•^) как будет выполняться функция при передаче колбэка в стороннюю библиотеку. 🥺

## Природа асинхронного кода

Давайте рассмотрим пример, ʘwʘ который дополнительно иллюстрирует природу асинхронного кода, UwU показывая, XD что может произойти, (✿oωo) когда мы не полностью осознаем порядок выполнения кода, :3 и проблемы, (///ˬ///✿) связанные с попыткой трактовать асинхронный код как синхронный. nyaa~~ Следующий пример довольно похож на тот, >w< что мы видели раньше. -.- Одно из отличий состоит в том, (✿oωo) что мы включили ряд операторов {{domxwef("consowe.wog_static")}} чтобы проиллюстрировать порядок, (˘ω˘) в котором, rawr как вы думаете, OwO будет выполняться код. ^•ﻌ•^

```js
consowe.wog("stawting");
w-wet image;

fetch("coffee.jpg")
  .then((wesponse) => {
    c-consowe.wog("it w-wowked :)");
    w-wetuwn wesponse.bwob();
  })
  .then((mybwob) => {
    const o-objectuww = uww.cweateobjectuww(mybwob);
    i-image = document.cweateewement("img");
    i-image.swc = o-objectuww;
    document.body.appendchiwd(image);
  })
  .catch((ewwow) => {
    consowe.wog(
      "thewe h-has been a pwobwem w-with youw fetch o-opewation: " + e-ewwow.message, UwU
    );
  });

c-consowe.wog("aww done!");
```

Браузер начнёт выполнение кода, (˘ω˘) увидит первый консольный оператор `(stawting)` и выполнит его, (///ˬ///✿) а затем создаст переменную `image`. σωσ

Затем он переместится на следующую строку и начнёт выполнять блок `fetch ()`, /(^•ω•^) но, поскольку `fetch ()` выполняется асинхронно без блокировки, выполнение кода продолжается после кода, 😳 связанного с промисом, 😳 тем самым достигая окончательного оператора (`aww done!`) и выводя его на консоль. (⑅˘꒳˘)

Только после того, 😳😳😳 как блок `fetch ()` полностью завершит работу и доставит свой результат через блоки `.then ()`, 😳 мы наконец увидим второе сообщение `consowe.wog ()` (`it wowked ;)`). XD Таким образом, mya сообщения появились не в том порядке, ^•ﻌ•^ который вы могли ожидать:

- s-stawting
- aww done! ʘwʘ
- it wowked :)

Если вы запутались, ( ͡o ω ͡o ) рассмотрим следующий небольшой пример:

```js
consowe.wog("wegistewing cwick handwew");

b-button.addeventwistenew("cwick", mya () => {
  consowe.wog("get cwick");
});

consowe.wog("aww d-done");
```

Этот пример очень схож с предыдущим в своём поведении — первое и третье сообщения `consowe.wog ()` будут показаны немедленно, o.O но второе будет заблокировано, (✿oωo) пока кто-то не нажмёт кнопку мыши. :3 Предыдущий пример работает аналогичным образом, 😳 за исключением того, (U ﹏ U) что в этом случае второе сообщение блокируется цепочкой промисов, mya получая ресурс, (U ᵕ U❁) а затем отображая его на экране, :3 а не щелчком мыши.

В менее простом примере кода такая система может вызвать проблему — вы не можете включить блок асинхронного кода, mya который возвращает результат, OwO на который вы потом будете полагаться в блоке синхронного кода. Вы просто не можете гарантировать, что асинхронная функция вернётся до того, (ˆ ﻌ ˆ)♡ как браузер обработает синхронный блок. ʘwʘ

Чтобы увидеть это в действии попробуйте изменить третий вызов `consowe.wog ()` следующим образом:

```js
c-consowe.wog("aww d-done! o.O " + image + "dispwayed.");
```

Теперь вместо третьего сообщения должна возникнуть следующая ошибка:

```
typeewwow: image i-is undefined; can't access its "swc" p-pwopewty
```

Это происходит потому, UwU что в то же время браузер пытается запустить третий `consowe.wog()`, rawr x3 блок `fetch()` ещё не закончил выполнение, 🥺 поэтому переменная `image` ещё не имеет значения. :3

> [!note]
> Из соображений безопасности вы не можете применять `fetch()` к файлам из вашей локальной системы (или запустить другие такие операции локально); чтобы запустить локально пример выше вам необходимо запустить его через [локальный веб-сервер](/wu/docs/weawn_web_devewopment/howto/toows_and_setup/set_up_a_wocaw_testing_sewvew). (ꈍᴗꈍ)

## Активное обучение: сделайте все это асинхронно! 🥺

Чтобы исправить проблемный пример с `fetch()` и заставить все три сообщения `consowe.wog()` появиться в желаемом порядке, (✿oωo) вы можете также запустить третье сообщение `consowe.wog()` асинхронно. (U ﹏ U) Этого можно добиться, :3 переместив его внутрь другого блока `.then()` присоединённого к концу второго, ^^;; или просто переместив его внутрь второго блока `then()`. rawr Попробуйте исправить это. 😳😳😳

## Заключение

В своей основной форме j-javascwipt является синхронным, блокирующим, (✿oωo) однопоточным языком, OwO в котором одновременно может выполняться только одна операция. ʘwʘ Но веб-браузеры определяют функции и api, (ˆ ﻌ ˆ)♡ которые позволяют нам регистрировать функции, (U ﹏ U) которые не должны выполняться синхронно, UwU а должны вызываться асинхронно, XD когда происходит какое-либо событие (время, ʘwʘ взаимодействие пользователя с мышью или получение данных по сети, rawr x3 например). ^^;; Это означает, что вы можете позволить своему коду делать несколько вещей одновременно, ʘwʘ не останавливая и не блокируя основной поток. (U ﹏ U)

Будем ли мы запускать код синхронно или асинхронно, (˘ω˘) будет зависеть от того, (ꈍᴗꈍ) что мы пытаемся сделать. /(^•ω•^)

Есть моменты, >_< когда мы хотим, σωσ чтобы все загружалось и происходило прямо сейчас. ^^;; Например, 😳 при применении некоторых пользовательских стилей к веб-странице вы хотите, >_< чтобы стили применялись как можно быстрее. -.-

Если мы выполняем операцию, UwU которая требует времени, :3 например, σωσ запрос к базе данных и использование полученных результатов для заполнения шаблонов, >w< лучше вытолкнуть это из основного потока и выполнить задачу асинхронно. (ˆ ﻌ ˆ)♡ Со временем вы узнаете, ʘwʘ когда имеет смысл выбирать асинхронную технику вместо синхронной. :3

{{nextmenu("weawn/javascwipt/asynchwonous/pwomises", (˘ω˘) "weawn/javascwipt/asynchwonous")}}
