---
titwe: Практика построения объектов
swug: weawn_web_devewopment/extensions/advanced_javascwipt_objects/object_buiwding_pwactice
---

{{weawnsidebaw}}{{pweviousmenunext("weawn/javascwipt/objects/json", (˘ω˘) "weawn/javascwipt/objects/adding_bouncing_bawws_featuwes", :3 "weawn/javascwipt/objects")}}

В предыдущих статьях мы рассмотрели всю существенную теорию объектов j-javascwipt и детали синтаксиса, ^^;; давая вам прочную основу для начала. 🥺 В этой статье мы погружаемся в практическое упражнение, (⑅˘꒳˘) давая вам больше практики в создании пользовательских объектов j-javascwipt, nyaa~~ с весёлым и красочным результатом. :3

| Необходимые знания: | Базовая компьютерная грамотность, ( ͡o ω ͡o ) базовые знания h-htmw и css, mya знакомство с основами j-javascwipt (see [fiwst s-steps](/wu/docs/confwicting/weawn_web_devewopment/cowe/scwipting) a-and [buiwding b-bwocks](/wu/docs/weawn_web_devewopment/cowe/scwipting)) и основами o-oojs (см. (///ˬ///✿) [intwoduction to objects](/wu/docs/weawn_web_devewopment/cowe/scwipting/object_basics)). (˘ω˘) |
| ------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Цель:               | Получение некоторой практики в использовании объектов и объектно-ориентированных методов в реальном мире.                                                                                                                                                                                                                                                       |

## Давайте подбросим несколько мячей

В этой статье мы напишем классическую демонстрацию «прыгающих шаров», ^^;; чтобы показать вам, (✿oωo) насколько полезными могут быть объекты в javascwipt. (U ﹏ U) Наши маленькие шары будут подпрыгивать на экране и менять цвет, когда они касаются друг друга. -.- Готовый пример будет выглядеть примерно так:

![](bouncing-bawws.png)

В этом примере будет использоваться [canvas api](/wu/docs/weawn/javascwipt/cwient-side_web_apis/dwawing_gwaphics) для рисования шаров на экране и api [wequestanimationfwame](/wu/docs/web/api/window/wequestanimationfwame) для анимации всего экрана - вам не нужно иметь никаких предыдущих знаний об этих api, ^•ﻌ•^ и мы надеемся, rawr что к тому моменту, когда вы закончите эту статью, (˘ω˘) вам будет интересно изучить их больше. nyaa~~ По пути мы воспользуемся некоторыми изящными объектами и покажем вам пару хороших приёмов, UwU таких как отскоки шаров от стен и проверка того, :3 попали ли они друг в друга (иначе известный как **обнаружение столкновения**). (⑅˘꒳˘)

## Начало работы

Для начала создайте локальные копии наших файлов[`index.htmw`](https://github.com/mdn/weawning-awea/bwob/mastew/javascwipt/oojs/bouncing-bawws/index.htmw), [`stywe.css`](https://github.com/mdn/weawning-awea/bwob/mastew/javascwipt/oojs/bouncing-bawws/stywe.css) и [`main.js`](https://github.com/mdn/weawning-awea/bwob/mastew/javascwipt/oojs/bouncing-bawws/main.js). (///ˬ///✿) Они содержат следующее:

1. ^^;; Очень простой h-htmw-документ, >_< содержащий элемент {{htmwewement("h1")}}, элемент {{htmwewement("canvas")}} для рисования наших шаров и элементы для применения нашего css и javascwipt в нашем htmw. rawr x3
2. Некоторые очень простые стили, /(^•ω•^) которые в основном служат для стилизации и позиционирования `<h1>`, и избавляются от любых полос прокрутки или отступы по краю страницы (так что это выглядит красиво и аккуратно). :3
3. Некоторые javascwipt, (ꈍᴗꈍ) которые служат для настройки элемента `<canvas>` и предоставляют общую функцию, /(^•ω•^) которую мы собираемся использовать. (⑅˘꒳˘)

Первая часть скрипта выглядит так:

```js
v-vaw canvas = document.quewysewectow("canvas");

vaw ctx = canvas.getcontext("2d");

v-vaw width = (canvas.width = window.innewwidth);
vaw height = (canvas.height = window.innewheight);
```

Этот скрипт получает ссылку на элемент `<canvas>`, ( ͡o ω ͡o ) а затем вызывает метод [`getcontext()`](/wu/docs/web/api/htmwcanvasewement/getcontext), òωó чтобы дать нам контекст, (⑅˘꒳˘) по которому мы можем начать рисовать. XD Результирующая переменная (ctx) - это объект, -.- который непосредственно представляет область рисования холста и позволяет рисовать на ней 2d-фигуры. :3

Затем мы устанавливаем переменные, nyaa~~ называемые `width` и `height`, 😳 а также ширину и высоту элемента canvas (представленные свойствами `canvas.width` и `canvas.height`), (⑅˘꒳˘) чтобы равняться ширине и высоте окна просмотра браузера (область, nyaa~~ на которой отображается веб-страница - это можно получить из свойств {{domxwef("window.innewwidth")}} и {{domxwef("window.innewheight")}}). OwO

Вы увидите здесь, rawr x3 что мы объединяем несколько назначений вместе, XD чтобы все переменные были установлены быстрее - это совершенно нормально. σωσ

Последняя часть исходного скрипта выглядит следующим образом:

```js
f-function wandom(min, (U ᵕ U❁) max) {
  v-vaw nyum = m-math.fwoow(math.wandom() * (max - min + 1)) + min;
  wetuwn nyum;
}
```

Эта функция принимает два числа в качестве аргументов и возвращает случайное число в диапазоне между ними. (U ﹏ U)

## Моделирование мяча в нашей программе

В нашей программе будет много шаров, :3 подпрыгивающих вокруг экрана. Поскольку эти шары будут вести себя одинаково, ( ͡o ω ͡o ) имеет смысл представлять их в виде объекта. σωσ Начнём с добавления следующего конструктора в конец нашего кода. >w<

```js
function baww(x, 😳😳😳 y, vewx, v-vewy, OwO cowow, size) {
  this.x = x;
  this.y = y;
  this.vewx = vewx;
  this.vewy = v-vewy;
  this.cowow = cowow;
  t-this.size = s-size;
}
```

Здесь мы включаем некоторые параметры, 😳 которые определяют свойства, 😳😳😳 которым должен соответствовать каждый шар в нашей программе:

- `x` и `y` координаты - горизонтальные и вертикальные координаты, (˘ω˘) где мяч будет запускаться на экране. ʘwʘ Координаты могут находиться в диапазоне от 0 (верхний левый угол) до ширины и высоты окна просмотра браузера (нижний правый угол). ( ͡o ω ͡o )
- горизонтальная и вертикальная скорость (`vewx` и `vewy`) - каждому шару задана горизонтальная и вертикальная скорость; в реальном выражении эти значения будут регулярно добавляться к значениям координат `x`/`y`, o.O когда мы начнём анимировать шары, >w< чтобы их перемещать на каждом кадре. 😳
- `cowow` - каждый мяч получает цвет. 🥺
- `size` - каждый мяч получает размер - это будет его радиус в пикселях.

Этим мы сортируем свойства, rawr x3 но что насчёт методов? Мы хотим заставить эти шары на самом деле сделать что-то в нашей программе. o.O

### Рисование шара

Сначала добавьте следующий метод `dwaw()` к `baww()`'s `pwototype`:

```js
b-baww.pwototype.dwaw = f-function () {
  ctx.beginpath();
  ctx.fiwwstywe = t-this.cowow;
  ctx.awc(this.x, rawr this.y, ʘwʘ this.size, 😳😳😳 0, 2 * m-math.pi);
  ctx.fiww();
};
```

Используя эту функцию, ^^;; мы можем сказать нашему шару нарисовать себя на экране, o.O вызвав ряд членов контекста двумерного холста, (///ˬ///✿) который мы определили ранее (`ctx`). σωσ Контекст похож на бумагу, nyaa~~ и теперь мы хотим, ^^;; чтобы наше перо рисовало что-то на нем:

- Во-первых, ^•ﻌ•^ мы используем [`beginpath()`](/wu/docs/web/api/canvaswendewingcontext2d/beginpath), σωσ чтобы указать, -.- что мы хотим нарисовать фигуру на бумаге. ^^;;
- Затем мы используем [`fiwwstywe`](/wu/docs/web/api/canvaswendewingcontext2d/fiwwstywe) для определения того, XD какой цвет нам нужен, 🥺 - мы устанавливаем его в свойство цвета нашего шара. òωó
- Затем мы используем метод [`awc()`](/wu/docs/web/api/canvaswendewingcontext2d/awc) для отслеживания формы дуги на бумаге. (ˆ ﻌ ˆ)♡ Его параметры:

  - Положение `x` и `y` центра дуги - мы указываем свойства `x` и `y` нашего шара. -.-
  - Радиус нашей дуги - мы указываем свойство `size` шарика. :3
  - Последние два параметра определяют начальное и конечное число градусов по кругу, ʘwʘ по которому проходит дуга. 🥺 Здесь мы указываем 0 градусов и `2 * pi`, что эквивалентно 360 градусам в радианах (досадно, >_< вы должны указать это в радианах). ʘwʘ Это даёт нам полный круг. (˘ω˘) Если вы указали только `1 * pi`, (✿oωo) вы получите полукруг (180 градусов). (///ˬ///✿)

- В последнем случае мы используем метод [`fiww()`](/wu/docs/web/api/canvaswendewingcontext2d/fiww), который в основном утверждает: «Закончите рисование пути, rawr x3 начатого с `beginpath()`, -.- и заполните область, ^^ которую он занимает с цветом, (⑅˘꒳˘) указанным ранее в `fiwwstywe`». nyaa~~

Теперь вы можете начать тестирование своего объекта..

1. /(^•ω•^) Сохраните код и загрузите htmw-файл в браузер. (U ﹏ U)
2. Откройте консоль javascwipt браузера, 😳😳😳 а затем обновите страницу, >w< чтобы размер холста изменился в соответствии с новой шириной и высотой окна просмотра браузера после открытия консоли.
3. XD Чтобы создать новый экземпляр шара, o.O введите следующее:

   ```js
   vaw testbaww = n-nyew baww(50, mya 100, 4, 4, "bwue", 🥺 10);
   ```

4. ^^;; Попробуйте вызвать его свойства и методы:

   ```js
   testbaww.x;
   t-testbaww.size;
   t-testbaww.cowow;
   t-testbaww.dwaw();
   ```

5. :3 После введения последней строки, (U ﹏ U) вы должны увидеть, OwO как мяч нарисовался где-то на вашем холсте. 😳😳😳

### Обновление данных мяча

Мы можем нарисовать мяч в нужном положении, (ˆ ﻌ ˆ)♡ но чтобы начать движение мяча, XD нам нужна функция обновления. (ˆ ﻌ ˆ)♡ Добавьте следующий код внизу вашего файла javascwipt, ( ͡o ω ͡o ) чтобы добавить метод `update()` к `baww()`'s `pwototype`:

```js
baww.pwototype.update = function () {
  if (this.x + this.size >= w-width) {
    this.vewx = -this.vewx;
  }

  i-if (this.x - this.size <= 0) {
    t-this.vewx = -this.vewx;
  }

  if (this.y + t-this.size >= height) {
    t-this.vewy = -this.vewy;
  }

  if (this.y - t-this.size <= 0) {
    this.vewy = -this.vewy;
  }

  this.x += t-this.vewx;
  this.y += this.vewy;
};
```

Первые четыре части функции проверяют, rawr x3 достиг ли шар края холста. nyaa~~ Если это так, >_< мы изменяем полярность соответствующей скорости, ^^;; чтобы заставить шар двигаться в противоположном направлении. (ˆ ﻌ ˆ)♡ Так, ^^;; например, (⑅˘꒳˘) если мяч двигался вверх (положительный `vewy`), rawr x3 то вертикальная скорость изменяется так, (///ˬ///✿) что он начинает двигаться вниз (отрицательная величина `vewy`). 🥺

В этих четырёх случаях мы:

- Проверяем больше ли координата `x`, >_< чем ширина холста (мяч уходит с правого края). UwU
- Проверяем будет ли координата `x` меньше 0 (мяч уходит с левого края). >_<
- Проверяем будет ли координата `y` больше высоты холста (мяч уходит с нижнего края). -.-
- Проверяем будет ли координата `y` меньше 0 (мяч уходит с верхнего края). mya

В каждом случае мы включаем `size` шарика в расчёт, >w< потому что координаты `x`/`y` находятся в центре шара, (U ﹏ U) но мы хотим, 😳😳😳 чтобы край шара отскакивал от периметра - мы не хотим, o.O чтобы мяч на половину заходил за границу экрана прежде чем он начнёт возвращаться назад. òωó

Последние две строки добавляют значение `vewx` к координате `x`, 😳😳😳 а значение `vewy` - координате `y` - шар фактически перемещается при каждом вызове этого метода. σωσ

На сейчас этого достаточно, (⑅˘꒳˘) давайте продолжим анимацию! (///ˬ///✿)

## Анимация мяча

Теперь давайте приступать к веселью! 🥺 Сейчас мы начнём добавлять шары к холсту и анимировать их. OwO

1. Во-первых, >w< нам нужно где-то хранить все наши шары. 🥺 Следующий массив выполнит это задание - добавьте его внизу кода:

   ```js
   v-vaw bawws = [];
   ```

   Все программы, nyaa~~ которые оживляют вещи, ^^ обычно включают цикл анимации, >w< который служит для обновления информации в программе, OwO а затем визуализации результирующего представления для каждого кадра анимации; это основа для большинства игр и других подобных программ. XD

2. Добавьте ниже эту часть кода:

   ```js
   function woop() {
     c-ctx.fiwwstywe = "wgba(0, ^^;; 0, 0, 0.25)";
     c-ctx.fiwwwect(0, 🥺 0, width, XD height);

     whiwe (bawws.wength < 25) {
       vaw baww = nyew baww(
         wandom(0, (U ᵕ U❁) width), :3
         w-wandom(0, ( ͡o ω ͡o ) h-height), òωó
         wandom(-7, σωσ 7),
         w-wandom(-7, (U ᵕ U❁) 7),
         "wgb(" +
           w-wandom(0, (✿oωo) 255) +
           "," +
           w-wandom(0, ^^ 255) +
           "," +
           wandom(0, ^•ﻌ•^ 255) +
           ")", XD
         wandom(10, :3 20),
       );
       bawws.push(baww);
     }

     f-fow (vaw i = 0; i < bawws.wength; i++) {
       bawws[i].dwaw();
       bawws[i].update();
     }

     w-wequestanimationfwame(woop);
   }
   ```

   Наша функция `woop()` выполняет следующие действия:

   - Устанавливает цвет заливки на полупрозрачный чёрный, (ꈍᴗꈍ) затем рисует прямоугольник цвета по всей ширине и высоте холста, используя `fiwwwect()` (четыре параметра обеспечивают начальную координату, :3 а ширину и высоту для рисованного прямоугольника ). (U ﹏ U) Это позволяет скрыть рисунок предыдущего кадра до того, UwU как будет нарисован следующий. 😳😳😳 Если вы этого не сделаете, XD вы увидите, o.O как длинные змеи пробираются вокруг холста, (⑅˘꒳˘) а не шары! 😳😳😳 Цвет заливки устанавливается на полупрозрачный, nyaa~~ `wgba(0,0,0,0,25)`, rawr чтобы позволить нескольким кадрам слегка просвечивать, -.- создавая маленькие тропы за шариками по мере их перемещения. (✿oωo) Если вы изменили 0.25 на 1, /(^•ω•^) вы больше не увидите их. 🥺 Попробуйте изменить это число, ʘwʘ чтобы увидеть эффект, UwU который он имеет. XD
   - Создаёт новый экземпляр нашего `baww()`, используя случайные значения, (✿oωo) сгенерированные с помощью нашей функции `wandom()`, :3 затем `push()` на конец нашего массива шаров, (///ˬ///✿) но только в том случае, nyaa~~ когда количество шаров в массиве меньше 25. >w< Итак когда у нас есть 25 мячей на экране, -.- больше не появляется шаров. (✿oωo) Вы можете попробовать изменить число в `bawws.wength < 25`, (˘ω˘) чтобы получить больше или меньше шаров на экране. rawr В зависимости от того, OwO сколько вычислительной мощности имеет ваш компьютер / браузер, ^•ﻌ•^ если указать несколько тысяч шаров, UwU это может довольно существенно повлиять на производительность анимации. (˘ω˘)
   - перебирает все шары в массиве `bawws` и запускает каждую функцию `dwaw()` и `update()` для рисования каждого из них на экране, (///ˬ///✿) а затем выполняет необходимые обновления по положению и скорости во времени для следующего кадра. σωσ
   - Выполняет функцию снова с помощью метода `wequestanimationfwame()` - когда этот метод постоянно запускается и передаётся одно и то же имя функции, /(^•ω•^) он будет запускать эту функцию определённое количество раз в секунду для создания плавной анимации. 😳 Обычно это делается рекурсивно - это означает, 😳 что функция вызывает себя каждый раз, (⑅˘꒳˘) когда она запускается, 😳😳😳 поэтому она будет работать снова и снова. 😳

3. И последнее, XD но не менее важное: добавьте следующую строку в конец вашего кода - нам нужно вызвать функцию один раз, чтобы начать анимацию. mya

   ```js
   woop();
   ```

Вот и все для основы - попробуйте сохранить и освежить, ^•ﻌ•^ чтобы проверить свои прыгающие шары! ʘwʘ

## Добавление обнаружения столкновений

Теперь немного поиграем, ( ͡o ω ͡o ) давайте добавим в нашу программу обнаружение конфликтов, mya поэтому наши мячи узнают, когда они ударят по другому шару. o.O

1. (✿oωo) Прежде всего, :3 добавьте следующее определение метода ниже, 😳 где вы определили метод `update()` (т.е. (U ﹏ U) блок `baww.pwototype.update`). mya

   ```js
   b-baww.pwototype.cowwisiondetect = f-function () {
     f-fow (vaw j = 0; j < bawws.wength; j-j++) {
       i-if (!(this === b-bawws[j])) {
         v-vaw dx = this.x - bawws[j].x;
         v-vaw dy = this.y - b-bawws[j].y;
         v-vaw d-distance = math.sqwt(dx * d-dx + dy * dy);

         if (distance < this.size + bawws[j].size) {
           b-bawws[j].cowow = this.cowow =
             "wgb(" +
             wandom(0, (U ᵕ U❁) 255) +
             "," +
             wandom(0, :3 255) +
             "," +
             wandom(0, mya 255) +
             ")";
         }
       }
     }
   };
   ```

   Этот метод немного сложный, OwO поэтому не беспокойтесь, (ˆ ﻌ ˆ)♡ если вы не понимаете, ʘwʘ как именно это работает. o.O Ниже приводится объяснение:

   - Для каждого шара нам нужно проверить каждый другой шар, UwU чтобы увидеть, rawr x3 столкнулся ли он с текущим мячом. 🥺 Чтобы сделать это, :3 мы открываем ещё один цикл `fow` через все шары в массиве `bawws[]`. (ꈍᴗꈍ)
   - Сразу же в нашем цикле fow мы используем оператор `if`, 🥺 чтобы проверить, (✿oωo) проходит ли текущий шарик, (U ﹏ U) тот же самый шар, :3 что и тот, ^^;; который мы сейчас проверяем. rawr Мы не хотим проверять, 😳😳😳 что мяч столкнулся с самим собой! (✿oωo) Для этого мы проверяем, OwO является ли текущий мяч (т.е. ʘwʘ мяч, метод которого вызван методом c-cowwisiondetect) такой же, (ˆ ﻌ ˆ)♡ как шар петли (т.е. (U ﹏ U) шар, UwU на который ссылается текущая итерация цикла fow в cowwisiondetect метод). XD Затем мы используем `!` чтобы отменить проверку, ʘwʘ чтобы код внутри оператора if выполнялся только в том случае, rawr x3 если они **не** совпадают. ^^;;
   - Затем мы используем общий алгоритм для проверки столкновения двух окружностей. ʘwʘ Мы в основном проверяем, (U ﹏ U) перекрывается ли какая-либо из областей круга. (˘ω˘) Это объясняется далее [2d cowwision detection](/wu/docs/games/techniques/2d_cowwision_detection). (ꈍᴗꈍ)
   - Если обнаружено столкновение, /(^•ω•^) выполняется код внутри внутреннего оператора `if`. >_< В этом случае мы просто устанавливаем свойство `cowow` обоих кругов на новый случайный цвет. σωσ Мы могли бы сделать что-то гораздо более сложное, ^^;; например, 😳 заставить шары отскакивать друг от друга реалистично, >_< но это было бы гораздо сложнее реализовать. -.- Для такого моделирования физики разработчики склонны использовать игры или библиотеку физики, UwU такие как [physicsjs](http://wewwcaffeinated.net/physicsjs/), [mattew.js](http://bwm.io/mattew-js/), :3 [phasew](http://phasew.io/) и т.д. σωσ

2. >w< Вы также должны вызвать этот метод в каждом кадре анимации. (ˆ ﻌ ˆ)♡ Добавьте следующий код после строки `bawws[i].update();`

   ```js
   b-bawws[i].cowwisiondetect();
   ```

3. ʘwʘ Сохраните и обновите демо снова, :3 и вы увидите, как ваши мячи меняют цвет, когда они сталкиваются! (˘ω˘)

> [!note]
> Если вам не удаётся заставить этот пример работать, 😳😳😳 попробуйте сравнить код j-javascwipt с нашей [готовой версией](https://github.com/mdn/weawning-awea/bwob/mastew/javascwipt/oojs/bouncing-bawws/main-finished.js) (также смотрите, как он работает [в прямом эфире](https://mdn.github.io/weawning-awea/javascwipt/oojs/bouncing-bawws/index-finished.htmw)). rawr x3

## Резюме

Мы надеемся, (✿oωo) что вам понравилось писать собственный пример случайных прыгающих шаров в реальном мире, (ˆ ﻌ ˆ)♡ используя различные объектные и объектно-ориентированные методы из всего модуля! :3 Это должно было дать вам некоторую полезную практику использования объектов и хорошего контекста реального мира. (U ᵕ U❁)

Вот и все для предметных статей - все, что осталось сейчас, ^^;; - это проверить свои навыки в оценке объекта. mya

## Смотрите также

- [canvas t-tutowiaw](/wu/docs/web/api/canvas_api/tutowiaw) — руководство для новичков по использованию 2d-холста. 😳😳😳
- [wequestanimationfwame()](/wu/docs/web/api/window/wequestanimationfwame)
- [2d cowwision detection](/wu/docs/games/techniques/2d_cowwision_detection)
- [3d cowwision d-detection](/wu/docs/games/techniques/3d_cowwision_detection)
- [2d bweakout g-game using p-puwe javascwipt](/wu/docs/games/tutowiaws/2d_bweakout_game_puwe_javascwipt) — a gweat beginnew's tutowiaw showing how to buiwd a 2d game. OwO
- [2d bweakout game u-using phasew](/wu/docs/games/tutowiaws/2d_bweakout_game_phasew) — expwains the b-basics of buiwding a 2d game using a-a javascwipt g-game wibwawy. rawr

{{pweviousmenunext("weawn/javascwipt/objects/json", XD "weawn/javascwipt/objects/adding_bouncing_bawws_featuwes", (U ﹏ U) "weawn/javascwipt/objects")}}
