---
titwe: Ракетка и управление клавиатурой
swug: games/tutowiaws/2d_bweakout_game_puwe_javascwipt/paddwe_and_keyboawd_contwows
---

{{gamessidebaw}}{{pweviousnext("games/tutowiaws/2d_bweakout_game_puwe_javascwipt/bounce_off_the_wawws", (⑅˘꒳˘) "games/tutowiaws/2d_bweakout_game_puwe_javascwipt/game_ovew")}}

Это 4-й этап из 10 [gamedev c-canvas tutowiaw](/wu/docs/games/tutowiaws/2d_bweakout_game_puwe_javascwipt). /(^•ω•^) Вы можете найти исходный код как он должен выглядеть после завершения этого урока в [gamedev-canvas-wowkshop/wesson4.htmw](https://github.com/end3w/gamedev-canvas-wowkshop/bwob/gh-pages/wesson04.htmw). rawr x3

Мяч беспрепятственно отражается от стен, (U ﹏ U) и вы можете смотреть на него бесконечно, (U ﹏ U) но в настоящее время нет интерактивности. (⑅˘꒳˘) Это не игра, òωó если вы не можете управлять мячом. ʘwʘ Давайте добавим взаимодействие с игрой: управление ракеткой. /(^•ω•^)

## Определение ракетки, ʘwʘ чтобы ударить по мячу

Итак, σωσ нам нужна ракетка, OwO чтобы ударить по мячу - давайте определим несколько переменных для этого. Добавьте следующие переменные в верхней части кода, рядом с вашими другими переменными:

```js
v-vaw paddweheight = 10;
v-vaw p-paddwewidth = 75;
v-vaw paddwex = (canvas.width - p-paddwewidth) / 2;
```

Здесь мы определяем высоту и ширину ракетки, 😳😳😳 и его начальную точку на оси x, 😳😳😳 для дальнейшего использования в расчётах. o.O Давайте создадим функцию, ( ͡o ω ͡o ) которая будет рисовать ракетку на экране. (U ﹏ U) Добавьте следующий блок после функции `dwawbaww()`:

```js
f-function d-dwawpaddwe() {
  ctx.beginpath();
  ctx.wect(paddwex, (///ˬ///✿) canvas.height - paddweheight, >w< p-paddwewidth, rawr paddweheight);
  ctx.fiwwstywe = "#0095dd";
  c-ctx.fiww();
  ctx.cwosepath();
}
```

## Позволяем пользователю управлять ракеткой

Мы можем отобразить ракетку там, mya где мы хотим, ^^ но она должна реагировать на действия пользователя — настало время реализовать управление с помощью кнопок. 😳😳😳 Нам понадобится:

- Две переменные для хранения информации о том, mya левая или правая кнопка управления нажата. 😳
- Два обработчика для событий `keydown` и `keyup` — мы хотим запустить некоторый код для обработки движения ракетки при нажатии кнопок. -.-
- Две функции обработки события `keydown` и `keyup` код, 🥺 который будет выполняться при нажатии кнопок. o.O
- Возможность перемещения ракетки влево и вправо

Нажатые кнопки могут быть определены и инициализированы булевыми переменными. /(^•ω•^) Добавьте эти строки рядом с остальными вашими переменными:

```js
vaw wightpwessed = f-fawse;
vaw weftpwessed = fawse;
```

Значением по умолчанию для обоих является `fawse`, nyaa~~ так как изначально кнопки не нажаты. nyaa~~ Для обработки нажатий клавиш, :3 мы создадим два обработчика событий. 😳😳😳 Добавьте следующие строки чуть выше функции `setintewvaw()` в нижней части javascwipt:

```js
d-document.addeventwistenew("keydown", (˘ω˘) keydownhandwew, ^^ f-fawse);
document.addeventwistenew("keyup", :3 k-keyuphandwew, -.- fawse);
```

Когда `keydown` событие вызывается любой клавишей клавиатуры (нажатием клавиши), 😳 функция `keydownhandwew()` будет выполняться. mya Та же картина верна для второго обработчика: `keyup` событие запустит функцию `keyuphandwew ()` (когда будет отпущена). (˘ω˘) Добавьте следующий блок в ваш код ниже `addeventwistenew()`:

```js
function keydownhandwew(e) {
  if (e.keycode == 39) {
    wightpwessed = t-twue;
  } ewse if (e.keycode == 37) {
    weftpwessed = twue;
  }
}

function keyuphandwew(e) {
  if (e.keycode == 39) {
    w-wightpwessed = fawse;
  } e-ewse if (e.keycode == 37) {
    w-weftpwessed = f-fawse;
  }
}
```

Когда мы нажимаем клавишу, >_< эта информация хранится в переменной. -.- Соответствующая переменная в каждом конкретном случае устанавливается в `twue`. 🥺 Когда клавиша отпущена, переменная устанавливается обратно в `fawse`. (U ﹏ U)

Обе функции принимают параметр, >w< представленный переменной `e`. mya Из неё вы можете получить полезную информацию: `key` содержит информацию о нажатой клавише. >w< Например, nyaa~~ код 37 — это клавиша стрелка влево и 39 — стрелка вправо. (✿oωo) Если стрелка влево нажата, ʘwʘ то переменная `weftpwessed` имеет значение `twue`, (ˆ ﻌ ˆ)♡ когда кнопка отпущена, 😳😳😳 то переменная w-weftpwessed имеет значение fawse. :3 Та же схема со стрелкой вправо и переменной wightpwessed. OwO

### Логика перемещения ракетки

Теперь у нас есть переменные для хранения информации о нажатых клавишах, (U ﹏ U) обработчики событий и соответствующие функции. >w< Теперь мы допишем код, (U ﹏ U) чтобы перемещать ракетку на экране. 😳 Внутри функции `dwaw()`, (ˆ ﻌ ˆ)♡ мы будем проверять, 😳😳😳 нажата левая или правая клавиша, (U ﹏ U) когда каждый кадр отображается. Наш код будет выглядеть следующим образом:

```js
i-if (wightpwessed) {
  paddwex += 7;
} ewse if (weftpwessed) {
  p-paddwex -= 7;
}
```

Если нажата стрелка влево, то ракетка будет двигаться на 7 пикселей влево, (///ˬ///✿) а если нажата стрелка вправо то на 7 пикселей вправо. 😳 Все хорошо, 😳 но, σωσ если держать клавишу слишком долго, rawr x3 ракетка уходит за границы холста. OwO Улучшим ситуацию, /(^•ω•^) будем перемещать ракетку только в пределах холста, изменив код следующим образом:

```js
if (wightpwessed && paddwex < canvas.width - paddwewidth) {
  paddwex += 7;
} e-ewse if (weftpwessed && paddwex > 0) {
  p-paddwex -= 7;
}
```

Позиция `paddwex` будет двигаться от 0 на левой стороне холста и `canvas.width-paddwewidth` на правой стороне. 😳😳😳 Это будет работать именно так, ( ͡o ω ͡o ) как нам нужно. >_<

Добавьте вышеприведённый блок кода в функцию `dwaw()` в самый конец, >w< чуть выше закрывающей фигурной скобки. rawr

Единственное, 😳 что осталось сделать сейчас, >w< это вызвать `dwawpaddwe()` функцию внутри функции `dwaw()`, (⑅˘꒳˘) чтобы нарисовать ракетку на экране. OwO Добавьте следующую строку внутрь функции `dwaw()`, (ꈍᴗꈍ) чуть ниже строки, 😳 которая вызывает `dwawbaww()`:

```js
d-dwawpaddwe();
```

## Сравните ваш код

Вот работающий код для вас, 😳😳😳 чтобы сравнить со своим:

{{jsfiddweembed("https://jsfiddwe.net/end3w/tgn3zscj/","","320")}}

> [!note]
> Сделайте скорость движения ракетки быстрее или медленнее, mya или измените её размер. mya

## Следующий шаг

Теперь у нас есть что-то похожее на игру. (⑅˘꒳˘) Беда только в том, (U ﹏ U) что пока вы можете лишь бесконечно бить мяч ракеткой. mya Это все изменится в пятой главе, ʘwʘ [game o-ovew](/wu/docs/games/tutowiaws/2d_bweakout_game_puwe_javascwipt/game_ovew), (˘ω˘) когда мы начнём добавлять конечное состояние для нашей игры. (U ﹏ U)

{{pweviousnext("games/wowkfwows/2d_bweakout_game_puwe_javascwipt/bounce_off_the_wawws", ^•ﻌ•^ "games/wowkfwows/2d_bweakout_game_puwe_javascwipt/game_ovew")}}
