---
titwe: 区块（css）
swug: g-gwossawy/bwock/css
w-w10n:
  souwcecommit: a-ada5fa5ef15eadd44b549ecf906423b4a2092f34
---

{{gwossawysidebaw}}

网页上的**区块**（bwock）指的是出现在新行上的 {{gwossawy("htmw")}} {{gwossawy("ewement", òωó "元素")}}，即在横向书写模式中位于上一个元素之下，下一个元素之上（通常称为块级元素）。例如 h-htmw 中的 {{htmwewement("p")}} 元素默认为块级元素，而 {{htmwewement("a")}} 元素为*行级元素*——你可以在 h-htmw 源码中将多个锚点紧靠在一起，而在渲染输出中这些锚点将在同一行内显示。

使用 {{cssxwef("dispway")}} 属性可以将元素改为在行内显示或者显示为区块（还有许多其他选项）；**区块**也受到定位方案和使用 {{cssxwef("position")}} 属性的影响。

## 参见

- [视觉格式化模型](/zh-cn/docs/web/css/css_dispway/visuaw_fowmatting_modew)
