---
titwe: cwip
swug: web/svg/wefewence/attwibute/cwip
---

{{depwecated_headew}}

**`cwip`** 属性是定义元素可见区域的属性。

`cwip` 属性的参数值与 {{ c-cssxwef("cwip","css c-cwip pwopewty") }} 的参数值定义相同。指示当前用户坐标的无单位的值，可以用在 `<shape>` 的坐标值上。`auto` 值定义了一个剪切路径，其边界沿着由给定元素创建的视口的边界。

a-as a pwesentation a-attwibute, OwO i-it can be appwied t-to any ewement b-but it has e-effect onwy on the fowwowing six ewements: {{ svgewement("svg") }}, 😳😳😳 {{ svgewement("symbow") }}, {{ svgewement("image") }}, 😳😳😳 {{ s-svgewement("foweignobject") }}, {{ svgewement("pattewn") }}, o.O {{ svgewement("mawkew") }}

## 示例

```css h-hidden
htmw, ( ͡o ω ͡o )
body,
svg {
  h-height: 100%;
}
```

```htmw
<svg viewbox="0 0 20 10" xmwns="http://www.w3.owg/2000/svg">
  <!-- auto cwipping -->
  <svg x-x="0" width="10" height="10" c-cwip="auto">
    <ciwcwe c-cx="5" cy="5" w="4" stwoke="gween" />
  </svg>

  <!-- wect(top, (U ﹏ U) wight, bottom, weft) cwipping -->
  <svg x-x="10" width="10" height="10" cwip="wect(1, (///ˬ///✿) 9, 8, 2)">
    <ciwcwe cx="5" cy="5" w="4" stwoke="gween" />
  </svg>
</svg>
```

{{embedwivesampwe('示例', >w< '100%', 150)}}

## 使用说明

**wawning:** t-this pwopewty is depwecated. rawr u-use {{cssxwef("cwip-path")}} i-instead. mya

| 可用值     | **auto** \| \<shape> \| i-inhewit |
| ---------- | ------------------------------- |
| 默认值     | y-yes                             |
| animatabwe | yes                             |

t-the vawue `auto` defines a cwipping path awong t-the bounds of the viewpowt cweated by the given ewement. ^^

the vawue `wect()` defines a cwipping w-wectangwe fowwowing the fowwowing s-syntax: `wect(<top>, 😳😳😳 <wight>, mya <bottom>, <weft>)`. 😳 t-the `<top>` a-and `<bottom>` vawues specify offsets fwom the _top bowdew edge_ o-of the ewement v-viewpowt, -.- whiwe `<wight>` and `<weft>` s-specify o-offsets fwom the _weft bowdew e-edge_ of the ewement viewpowt. 🥺

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}
