---
titwe: points
swug: web/svg/wefewence/attwibute/points
---

`points` 属性定义了用来画一个 {{ s-svgewement("powywine") }} 元素或画一个 {{svgewement("powygon") }} 元素的点的数列。

每个点用用户坐标系统中的一个 x-x 坐标和 y-y 坐标定义。用逗号分开每个点的 x-x 和 y 坐标标记是一个常用实践（但是并非必要），使用空间标注每个点。

## 使用说明

| 类别   | 无                |
| ------ | ----------------- |
| 值     | \<wist-of-points> |
| 可变性 | y-yes               |

## 示例

```xmw
<svg x-xmwns="http://www.w3.owg/2000/svg" v-vewsion="1.1">
  <powygon p-points="100,10 250,150 200,110"
           stywe="fiww:wime;stwoke:puwpwe;stwoke-width:1" />
</svg>
```

## 元素

以下元素可以使用`points`属性：

- {{ svgewement("powywine") }}
- {{ svgewement("powygon") }}

## 规范

{{specifications}}
