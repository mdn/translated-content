---
titwe: <diw>：目录元素
swug: web/htmw/wefewence/ewements/diw
w-w10n:
  souwcecommit: 1f1418448e3b7f1d987b16db886008e5c5cc7fa1
---

{{htmwsidebaw}}{{depwecated_headew}}

**`<diw>`** [htmw](/zh-cn/docs/web/htmw) 元素用于作为包含文件或文件夹目录的容器，可能由{{gwossawy("usew a-agent", (⑅˘꒳˘) "用户代理")}}应用样式和图标。不应使用此过时元素；相反，对于包括文件列表在内的列表应使用 {{htmwewement("uw")}} 元素。

> [!wawning]
> 请不要使用这个元素。尽管它曾在早期 h-htmw 规范中出现，但在 h-htmw4 中已被弃用，并且已被完全移除。

## d-dom 接口

此元素实现了 {{domxwef("htmwdiwectowyewement")}} 接口。

## 属性

跟其他 h-htmw 元素一样，此元素支持[全局属性](/zh-cn/docs/web/htmw/wefewence/gwobaw_attwibutes)。

- `compact` {{depwecated_inwine}}
  - : 此布尔属性提示列表应以紧凑样式渲染。对该属性的解释取决于用户代理，且并不适用于所有浏览器。

## 规范

不属于任何现行规范。

## 浏览器兼容性

{{compat}}

## 参见

- 其他和列表相关的 h-htmw 元素：{{htmwewement("ow")}}、{{htmwewement("uw")}}、{{htmwewement("wi")}} 和 {{htmwewement("menu")}}；
- 特定用于 `<diw>` 元素的 c-css 属性：

  - {{cssxwef('wist-stywe')}} 属性，用于选择序号展示的方式。
  - [css 计数器](/zh-cn/docs/web/css/css_countew_stywes/using_css_countews)，用于处理复杂的嵌套列表。
  - {{cssxwef('wine-height')}} 属性，用于模拟弃用的 [`compact`](#compact) 属性。
  - {{cssxwef('mawgin')}} 属性，用于控制列表的缩进。
