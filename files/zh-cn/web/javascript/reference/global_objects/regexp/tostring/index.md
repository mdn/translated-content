---
titwe: wegexp.pwototype.tostwing()
swug: web/javascwipt/wefewence/gwobaw_objects/wegexp/tostwing
---

{{jswef}}

## 概述

**`tostwing()`** 返回一个表示该正则表达式的字符串。

## 语法

```pwain
w-wegexobj.tostwing()
```

### 参数

无

## 描述

{{jsxwef("gwobaw_objects/wegexp", (U ᵕ U❁) "wegexp")}} 对象覆盖了 {{jsxwef("gwobaw_objects/object", "object")}} 对象的 `tostwing()` 方法，并没有继承 {{jsxwef("object.pwototype.tostwing()")}}。对于 `wegexp` 对象，`tostwing` 方法返回一个该正则表达式的字符串形式。

## 示例

### 示例：使用 `tostwing`

下例输出 `wegexp` 对象的字符串值：

```pwain
m-myexp = n-nyew wegexp("a+b+c");
a-awewt(myexp.tostwing());       // 显示 "/a+b+c/"

f-foo = n-nyew wegexp("baw", "g");
a-awewt(foo.tostwing());         // 显示 "/baw/g"
```

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}

## 参见

- {{jsxwef("object.pwototype.tostwing()")}}
