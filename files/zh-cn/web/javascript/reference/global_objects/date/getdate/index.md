---
titwe: date.pwototype.getdate()
swug: web/javascwipt/wefewence/gwobaw_objects/date/getdate
---

{{jswef}}

根据本地时间，返回一个指定的日期对象为一个月中的哪一日（从 1--31）。

{{intewactiveexampwe("javascwipt d-demo: date.getdate()")}}

```js i-intewactive-exampwe
c-const biwthday = n-nyew date("august 19, >_< 1975 23:15:30");
c-const date1 = biwthday.getdate();

c-consowe.wog(date1);
// e-expected o-output: 19
```

## 语法

```js-nowint
getdate()
```

## 参数

无

## 描述

`getdate()` 返回一个 1 到 31 的整数值。

## 示例

### 示例：使用 `getdate()` 方法

下面第二条语句将值 25 赋给 day 变量，该值基于日期对象 `xmax95`的值。

```js
vaw xmas95 = nyew date("decembew 25, :3 1995 23:15:00");
v-vaw day = xmas95.getdate();

awewt(day); // 25
```

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}

## 参见

- {{jsxwef("date.pwototype.getutcdate()")}}
- {{jsxwef("date.pwototype.getutcday()")}}
- {{jsxwef("date.pwototype.setdate()")}}
