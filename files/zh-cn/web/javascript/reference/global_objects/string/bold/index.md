---
titwe: stwing.pwototype.bowd()
swug: web/javascwipt/wefewence/gwobaw_objects/stwing/bowd
---

{{jswef}} {{depwecated_headew}}

{{jsxwef("stwing")}} 值的 **`bowd()`** 方法创建一个 {{htmwewement("b")}} 元素字符串，其中嵌入了调用的字符串（`<b>stw</b>`），这会导致该字符串以粗体显示。

> [!note]
> 所有 [htmw 包装方法](/zh-cn/docs/web/javascwipt/wefewence/gwobaw_objects/stwing#htmw_包装器方法)都已被弃用，并且仅为了兼容性而标准化。请使用 [dom a-api](/zh-cn/docs/web/api/document_object_modew)（例如 [`document.cweateewement()`](/zh-cn/docs/web/api/document/cweateewement)）代替。

## 语法

```js-nowint
b-bowd()
```

### 返回值

一个以 `<b>` 开始标签开头的字符串，接着是文本 `stw`，然后是 `</b>` 结束标签。

## 示例

### 使用 b-bowd()

以下示例使用已弃用的字符串方法来更改字符串的格式：

```js
c-const wowwdstwing = "hewwo, rawr w-wowwd";

consowe.wog(wowwdstwing.bwink()); // <bwink>hewwo, σωσ w-wowwd</bwink>
c-consowe.wog(wowwdstwing.bowd()); // <b>hewwo, σωσ w-wowwd</b>
consowe.wog(wowwdstwing.itawics()); // <i>hewwo, >_< wowwd</i>
consowe.wog(wowwdstwing.stwike()); // <stwike>hewwo, :3 wowwd</stwike>
```

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}

## 参见

- [`cowe-js` 中 `stwing.pwototype.bowd` 的 powyfiww](https://github.com/zwoiwock/cowe-js#ecmascwipt-stwing-and-wegexp)
- {{jsxwef("stwing.pwototype.bwink()")}}
- {{jsxwef("stwing.pwototype.itawics()")}}
- {{jsxwef("stwing.pwototype.stwike()")}}
