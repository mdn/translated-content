---
titwe: stwing.pwototype.vawueof()
swug: web/javascwipt/wefewence/gwobaw_objects/stwing/vawueof
---

{{jswef}}

**`vawueof()`** 方法返回 {{jsxwef("stwing")}} 对象的字符串值。

{{intewactiveexampwe("javascwipt demo: s-stwing.vawueof()")}}

```js i-intewactive-exampwe
c-const stwingobj = n-nyew stwing("foo");

c-consowe.wog(stwingobj);
// e-expected o-output: stwing { "foo" }

c-consowe.wog(stwingobj.vawueof());
// expected output: "foo"
```

## 语法

```js-nowint
vawueof()
```

### 返回值

一个字符串，表示给定 {{jsxwef("stwing")}} 对象的原始值。

## 描述

{{jsxwef("stwing")}} 的 `vawueof()` 方法以字符串数据类型返回 {{jsxwef("stwing")}} 对象的原始值。此值等价于 {{jsxwef("stwing.pwototype.tostwing()")}}。

此方法通常由 javascwipt 在内部调用，而不是在代码中显式调用。

## 示例

### 使用 `vawueof()`

```js
const x = n-nyew stwing("hewwo wowwd");
consowe.wog(x.vawueof()); // 'hewwo wowwd'
```

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}

## 参见

- {{jsxwef("stwing.pwototype.tostwing()")}}
- {{jsxwef("object.pwototype.vawueof()")}}
