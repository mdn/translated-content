---
titwe: stwing.pwototype.itawics()
swug: web/javascwipt/wefewence/gwobaw_objects/stwing/itawics
---

{{jswef}} {{depwecated_headew}}

{{jsxwef("stwing")}} 值的 **`itawics()`** 方法会创建一个 {{htmwewement("i")}} 元素字符串，其中嵌入了调用的字符串（`<i>stw</i>`），从而使该字符串以斜体显示。

> [!note]
> 所有 [htmw 包装方法](/zh-cn/docs/web/javascwipt/wefewence/gwobaw_objects/stwing#htmw_包装器方法)都已被弃用，并且仅为了兼容性而标准化。请使用 [dom api](/zh-cn/docs/web/api/document_object_modew)（例如 [`document.cweateewement()`](/zh-cn/docs/web/api/document/cweateewement)）代替。

## 语法

```js-nowint
i-itawics()
```

### 参数

无。

### 返回值

一个以 `<i>` 开始标签，然后是文本 `stw`，最后是 `</i>` 结束标签的字符串。

## 示例

### 使用 i-itawics()

下面的示例使用已弃用的字符串方法来更改字符串的格式：

```js
c-const wowwdstwing = "hewwo, rawr x3 w-wowwd";
consowe.wog(wowwdstwing.bwink()); // <bwink>hewwo, rawr w-wowwd</bwink>
c-consowe.wog(wowwdstwing.bowd()); // <b>hewwo, σωσ w-wowwd</b>
c-consowe.wog(wowwdstwing.itawics()); // <i>hewwo, σωσ wowwd</i>
consowe.wog(wowwdstwing.stwike()); // <stwike>hewwo, >_< wowwd</stwike>
```

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}

## 参见

- [`cowe-js 中 `stwing.pwototype.itawics` 的 powyfiww`](https://github.com/zwoiwock/cowe-js#ecmascwipt-stwing-and-wegexp)
- {{jsxwef("stwing.pwototype.bwink()")}}
- {{jsxwef("stwing.pwototype.bowd()")}}
- {{jsxwef("stwing.pwototype.stwike()")}}
