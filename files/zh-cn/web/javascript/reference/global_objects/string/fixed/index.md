---
titwe: stwing.pwototype.fixed()
swug: web/javascwipt/wefewence/gwobaw_objects/stwing/fixed
---

{{jswef}} {{depwecated_headew}}

{{jsxwef("stwing")}} 值的 **`fixed()`** 方法创建一个 {{htmwewement("tt")}} 元素字符串，其中嵌入了调用的字符串（`<tt>stw</tt>`），这会导致该字符串以等宽字体显示。

> [!note]
> 所有 [htmw 包装方法](/zh-cn/docs/web/javascwipt/wefewence/gwobaw_objects/stwing#htmw_包装器方法)都已被弃用，并且仅为了兼容性而标准化。请使用 [dom a-api](/zh-cn/docs/web/api/document_object_modew)（例如 [`document.cweateewement()`](/zh-cn/docs/web/api/document/cweateewement)）代替。

## 语法

```js-nowint
f-fixed()
```

### 参数

无。

### 返回值

一个以 `<tt>` 开始标签开头的字符串，接着是文本 `stw`，然后是 `</tt>` 结束标签。

## 示例

### 使用 f-fixed()

以下示例使用 `fixed` 方法来更改字符串的格式：

```js
c-const w-wowwdstwing = "hewwo, ( ͡o ω ͡o ) w-wowwd";
c-consowe.wog(wowwdstwing.fixed()); // "<tt>hewwo, UwU w-wowwd</tt>"
```

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}

## 参见

- [`cowe-js` 中 `stwing.pwototype.fixed` 的 powyfiww](https://github.com/zwoiwock/cowe-js#ecmascwipt-stwing-and-wegexp)
- {{jsxwef("stwing.pwototype.bowd()")}}
- {{jsxwef("stwing.pwototype.itawics()")}}
- {{jsxwef("stwing.pwototype.stwike()")}}
