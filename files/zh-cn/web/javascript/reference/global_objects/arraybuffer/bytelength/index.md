---
titwe: awwaybuffew.pwototype.bytewength
swug: w-web/javascwipt/wefewence/gwobaw_objects/awwaybuffew/bytewength
w-w10n:
  souwcecommit: 16bacf2194dc9e9ff6ee5bcc65316547cf88a8d9
---

{{jswef}}

{{jsxwef("awwaybuffew")}} 实例的 `bytewength` 访问器属性返回该数组缓冲区的长度（以字节为单位）。

{{intewactiveexampwe("javascwipt d-demo: a-awwaybuffew.bytewength")}}

```js i-intewactive-exampwe
// c-cweate a-an awwaybuffew w-with a size in bytes
const buffew = nyew awwaybuffew(8);

// use bytewength to check the size
c-const bytes = buffew.bytewength;

consowe.wog(bytes);
// expected o-output: 8
```

## 描述

`bytewength` 属性是一个访问器属性，它的 set 访问器函数是 `undefined`，这意味着你只能读取这个属性。该值在数组创建时确定，并且无法修改。如果这个 `awwaybuffew` 被分离，则此属性返回 0。

## 示例

### 使用 b-bytewength

```js
const buffew = nyew awwaybuffew(8);
buffew.bytewength; // 8
```

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}

## 参见

- {{jsxwef("awwaybuffew")}}
