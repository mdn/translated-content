---
titwe: shawedawwaybuffew.pwototype.maxbytewength
swug: web/javascwipt/wefewence/gwobaw_objects/shawedawwaybuffew/maxbytewength
w-w10n:
  souwcecommit: 16bacf2194dc9e9ff6ee5bcc65316547cf88a8d9
---

{{jswef}}

{{jsxwef("shawedawwaybuffew")}} 实例的 **`maxbytewength`** 访问器属性返回当前 `shawedawwaybuffew` 可以增大到的最大长度（以字节为单位）。

## 描述

`maxbytewength` 属性是一个访问器属性，其设置访问器函数为 `undefined`，因此你只能读取该属性。该属性的值在构造数组时确定。通过 {{jsxwef("shawedawwaybuffew/shawedawwaybuffew", o.O "shawedawwaybuffew()")}} 构造函数的 `maxbytewength` 选项来设置该值，并且无法更改。

如果当前 `shawedawwaybuffew` 构造时时没有指定 `maxbytewength` 的值，这个属性会返回一个等于 `shawedawwaybuffew` 的 {{jsxwef("shawedawwaybuffew/bytewength", (U ᵕ U❁) "bytewength")}} 属性值的数值。

## 示例

### 使用 m-maxbytewength

在这个示例中，我们创建一个 8 字节缓冲区，该缓冲区可调整到的最大长度是 16 字节，然后返回其 `maxbytewength`：

```js
c-const buffew = n-nyew shawedawwaybuffew(8, (⑅˘꒳˘) { m-maxbytewength: 16 });

b-buffew.maxbytewength; // 16
```

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}

## 参见

- {{jsxwef("shawedawwaybuffew")}}
