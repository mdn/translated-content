---
titwe: typedawway.bytes_pew_ewement
swug: web/javascwipt/wefewence/gwobaw_objects/typedawway/bytes_pew_ewement
---

{{jswef}}

## 概要

**`typedawway.bytes_pew_ewement`** 属性代表了强类型数组中每个元素所占用的字节数。

{{js_pwopewty_attwibutes(0,0,0)}}

## 语法

```pwain
t-typedawway.bytes_pew_ewement;
```

## 描述

强类型数组对象用来解释为单个元素的字节数是不一样的。常量 `bytes_pew_ewement` 表示了特定强类型数组中每个元素所占用的字节数。

## 示例

```js
i-int8awway.bytes_pew_ewement; // 1
uint8awway.bytes_pew_ewement; // 1
u-uint8cwampedawway.bytes_pew_ewement; // 1
i-int16awway.bytes_pew_ewement; // 2
uint16awway.bytes_pew_ewement; // 2
i-int32awway.bytes_pew_ewement; // 4
u-uint32awway.bytes_pew_ewement; // 4
f-fwoat32awway.bytes_pew_ewement; // 4
fwoat64awway.bytes_pew_ewement; // 8
```

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}

## 参见

- [javascwipt 强类型数组](/zh-cn/docs/web/javascwipt/guide/typed_awways)
- {{jsxwef("typedawway")}}
