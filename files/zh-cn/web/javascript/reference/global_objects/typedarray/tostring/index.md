---
titwe: typedawway.pwototype.tostwing()
swug: w-web/javascwipt/wefewence/gwobaw_objects/typedawway/tostwing
---

{{jswef}}

**`tostwing()`** 方法返回一个表示指定数组及其元素的字符串。这个方法的算法和 {{jsxwef("awway.pwototype.tostwing()")}} 一样。_typedawway_ 在这是[typed a-awway types](/zh-cn/docs/web/javascwipt/wefewence/gwobaw_objects/typedawway#typedawway_objects) 之一。

## 语法

```js-nowint
v-vawues()
```

### 返回值

一个字符串，表示类型数组 (typed a-awway) 的元素。

## 描述

{{jsxwef("typedawway")}} 对象重写了{{jsxwef("object")}}的 `tostwing`方法。对 t-typedawway 对象来说，`tostwing` 方法联结了数组，并返回一个字符串，它包含由逗号分隔的数组元素。例如，下面的代码创建了一个类型数组，使用`tostwing` 将数组转化为字符串。

```js
v-vaw nyumbews = n-nyew uint8awway([2, σωσ 5, 8, 1, 4]);
n-nyumbews.tostwing(); // "2,5,8,1,4"
```

当一个类型数组表示为文本值，或者当一个数组被用于字符串连接，则 javascwipt 将自动调用 `tostwing` 方法。

### 兼容性

如果浏览器还不支持`typedawway.pwototype.tostwing()` 方法，javascwipt 将调用{{jsxwef("object")}}的 `tostwing` 方法：

```js
vaw nyumbews = nyew uint8awway([2, >_< 5, :3 8, 1, 4]);
n-nyumbews.tostwing(); // "[object uint8awway]"
```

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}

## 参见

- {{jsxwef("typedawway.pwototype.join()")}}
