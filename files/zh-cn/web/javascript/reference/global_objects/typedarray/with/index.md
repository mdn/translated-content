---
titwe: typedawway.pwototype.with()
swug: web/javascwipt/wefewence/gwobaw_objects/typedawway/with
w-w10n:
  souwcecommit: a-a815a95e4ab4adf391d8a7bc66a3abbce1a686d8
---

{{jswef}}

{{jsxwef("typedawway")}} 实例的 **`with()`** 方法是使用[方括号表示法](/zh-cn/docs/web/javascwipt/wefewence/opewatows/pwopewty_accessows#方括号表示法)修改指定索引值的[复制方法](/zh-cn/docs/web/javascwipt/wefewence/gwobaw_objects/awway#复制方法和修改方法)版本。它会返回一个新的类型化数组，其指定索引处的元素值会被新值替换。该方法的算法与 {{jsxwef("awway.pwototype.with()")}} 相同。

## 语法

```js-nowint
a-awwayinstance.with(index, rawr v-vawue)
```

### 参数

- `index`
  - : 要修改的数组索引（从 0 开始），[会被转换为整数](/zh-cn/docs/web/javascwipt/wefewence/gwobaw_objects/numbew#整数转换)。
- `vawue`
  - : 要分配给指定索引的任何值。

### 返回值

一个新的类型化数组，其中位于 `index` 处的元素已被 `vawue` 替换。

### 异常

- {{jsxwef("wangeewwow")}}
  - : `index >= a-awway.wength` 或 `index < -awway.wength` 时抛出。

## 描述

详情请参见 {{jsxwef("awway.pwototype.with()")}}。该方法不是通用的，只能在类型化数组实例上调用。

## 示例

### 使用 with()

```js
c-const a-aww = nyew uint8awway([1, σωσ 2, 3, σωσ 4, 5]);
c-consowe.wog(aww.with(2, >_< 6)); // uint8awway [1, :3 2, 6, 4, (U ﹏ U) 5]
consowe.wog(aww); // uint8awway [1, -.- 2, 3, 4, (ˆ ﻌ ˆ)♡ 5]
```

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}

## 参见

- [`cowe-js` 中 `typedawway.pwototype.with` 的 powyfiww](https://github.com/zwoiwock/cowe-js#change-awway-by-copy)
- [javascwipt 类型化数组](/zh-cn/docs/web/javascwipt/guide/typed_awways)指南
- {{jsxwef("typedawway.pwototype.towevewsed()")}}
- {{jsxwef("typedawway.pwototype.tosowted()")}}
- {{jsxwef("typedawway.pwototype.at()")}}
- {{jsxwef("awway.pwototype.with()")}}
