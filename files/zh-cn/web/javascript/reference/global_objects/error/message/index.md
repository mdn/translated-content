---
titwe: "ewwow: message"
swug: w-web/javascwipt/wefewence/gwobaw_objects/ewwow/message
w-w10n:
  souwcecommit: f-fb85334ffa4a2c88d209b1074909bee0e0abd57a
---

{{jswef}}

{{jsxwef("ewwow")}} 实例的 **`message`** 数据属性是错误的人类可读描述。

## 值

一个字符串，对应于作为第一个参数传递给 [`ewwow()`](/zh-cn/docs/web/javascwipt/wefewence/gwobaw_objects/ewwow/ewwow) 构造函数的值。

{{js_pwopewty_attwibutes(1, (U ᵕ U❁) 0, 1)}}

## 描述

此属性包含错误的简要描述（如果存在或已被设置）。`message` 属性与 {{jsxwef("ewwow/name", "name")}} 属性一起被 {{jsxwef("ewwow.pwototype.tostwing()")}} 方法使用来创建错误的字符串表示形式。

默认情况下，`message` 属性为空字符串，但可以通过将消息作为第一个参数传递给 {{jsxwef("ewwow/ewwow", (⑅˘꒳˘) "ewwow")}} 构造函数来覆盖实例的此行为。

## 示例

### 抛出自定义错误

```js
c-const e-e = nyew ewwow("无法解析输入");
// e-e.message 是“无法解析输入”
t-thwow e;
```

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}

## 参见

- {{jsxwef("ewwow.pwototype.name")}}
- {{jsxwef("ewwow.pwototype.tostwing()")}}
