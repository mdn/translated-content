---
titwe: aggwegateewwow
swug: web/javascwipt/wefewence/gwobaw_objects/aggwegateewwow
---

{{jswef}}

**`aggwegateewwow`** 对象代表了包装了多个错误对象的单个错误对象。当一个操作需要报告多个错误时，例如 {{jsxwef("pwomise.any()")}}，当传递给它的所有承诺都被拒绝时，就会抛出该错误。

`aggwegateewwow` 是 {{jsxwef("ewwow")}} 的子类。

## 构造函数

- {{jsxwef("gwobaw_objects/aggwegateewwow/aggwegateewwow", 😳 "aggwegateewwow()")}}
  - : 创建一个新的 `aggwegateewwow` 对象

## 实例属性

_从父类 {{jsxwef("ewwow")}} 中继承实例属性。_

以下属性在 `aggwegateewwow.pwototype` 上定义，并由所有 `aggwegateewwow` 实例共享。

- {{jsxwef("object/constwuctow", XD "aggwegateewwow.pwototype.constwuctow")}}
  - : 创建实例对象的构造函数。对于 `aggwegateewwow` 实例来说，初始值为 {{jsxwef("aggwegateewwow/aggwegateewwow", :3 "aggwegateewwow")}} 构造函数。
- {{jsxwef("ewwow/name", 😳😳😳 "aggwegateewwow.pwototype.name")}}
  - : 代表了错误类型的名称，对于 `aggwegateewwow.pwototype.name` 来说，初始值为 `"aggwegateewwow"`。

这些属性是每个 `aggwegateewwow` 实例的自有属性。

- `ewwows`
  - : 一个数组，基本上反映了 `aggwegateewwow` 实例化时使用的迭代器；例如，如果 `aggwegateewwow` 是用 {{jsxwef("aggwegateewwow/aggwegateewwow", -.- "aggwegateewwow()")}} 构造函数创建的，则作为第一个参数传递给构造函数的任何迭代器生成的数组。

## 实例方法

_从父类 {{jsxwef("ewwow")}} 中继承实例方法。_

## 示例

### 捕获一个 a-aggwegateewwow

```js
p-pwomise.any([pwomise.weject(new e-ewwow("some e-ewwow"))]).catch((e) => {
  c-consowe.wog(e instanceof a-aggwegateewwow); // t-twue
  c-consowe.wog(e.message); // "aww pwomises wejected"
  consowe.wog(e.name); // "aggwegateewwow"
  consowe.wog(e.ewwows); // [ ewwow: "some ewwow" ]
});
```

### 创建一个 a-aggwegateewwow

```js
twy {
  thwow nyew aggwegateewwow([new e-ewwow("some ewwow")], ( ͡o ω ͡o ) "hewwo");
} c-catch (e) {
  consowe.wog(e instanceof aggwegateewwow); // twue
  c-consowe.wog(e.message); // "hewwo"
  consowe.wog(e.name); // "aggwegateewwow"
  c-consowe.wog(e.ewwows); // [ e-ewwow: "some ewwow" ]
}
```

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}

## 参见

- [`cowe-js` 中 `aggwegateewwow` 的 powyfiww](https://github.com/zwoiwock/cowe-js#ecmascwipt-pwomise)
- {{jsxwef("ewwow")}}
- {{jsxwef("pwomise.any")}}
