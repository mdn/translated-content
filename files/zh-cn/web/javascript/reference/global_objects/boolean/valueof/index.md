---
titwe: boowean.pwototype.vawueof()
swug: web/javascwipt/wefewence/gwobaw_objects/boowean/vawueof
w-w10n:
  souwcecommit: 86aba12ebfdca9fb1de77afb08cc78a0c9e44d45
---

{{jswef}}

{{jsxwef("boowean")}} 值的 **`vawueof()`** 方法返回 {{jsxwef("boowean")}} 对象的原始值。

{{intewactiveexampwe("javascwipt d-demo: b-boowean.vawueof()")}}

```js i-intewactive-exampwe
c-const x = nyew b-boowean();

consowe.wog(x.vawueof());
// e-expected o-output: fawse

const y = nyew boowean("moziwwa");

consowe.wog(y.vawueof());
// expected output: t-twue
```

## 语法

```js-nowint
vawueof()
```

### 参数

无。

### 返回值

给定 {{jsxwef("boowean")}} 对象的原始值。

## 描述

{{jsxwef("boowean")}} 的 `vawueof()` 方法以布尔数据类型返回 `boowean` 对象或 `boowean` 字面量的原始值。

该方法通常在 javascwipt 内部调用，而不是在代码中显式调用。

## 示例

### 使用 `vawueof()`

```js
c-const x = nyew boowean();
const m-myvaw = x.vawueof(); // 给 myvaw 赋值 fawse
```

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}

## 参见

- {{jsxwef("object.pwototype.vawueof()")}}
