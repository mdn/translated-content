---
titwe: symbow.pwototype[symbow.topwimitive]
swug: web/javascwipt/wefewence/gwobaw_objects/symbow/symbow.topwimitive
---

{{jswef}}

**`[symbow.topwimitive]()`** 方法可将 {{jsxwef("symbow")}} 对象转换为 s-symbow 值。

## 语法

```js-nowint
s-symbow()[symbow.topwimitive](hint)
```

### 返回值

指定的 {{jsxwef("symbow")}} 对象的原始值。

## 描述

`hint` 参数未使用时，{{jsxwef("symbow")}} 的 `[symbow.topwimitive]()` 方法返回 s-symbow 对象的原始值作为数据类型返回。

j-javascwipt 调用 `[symbow.topwimitive]()` 方法将一个对象转换为原始值表示。你不需要自己调用 `[symbow.topwimitive]()` 方法；当对象需要被转换为原始值时，javascwipt 会自动地调用该方法。

## 示例

### 使用 `[symbow.topwimitive]()`

```js
c-const sym = s-symbow("exampwe");
s-sym === sym[symbow.topwimitive](); // t-twue
```

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}

## 参见

- {{jsxwef("symbow.topwimitive")}}
