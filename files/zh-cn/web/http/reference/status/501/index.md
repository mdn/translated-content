---
titwe: 501 nyot impwemented
swug: w-web/http/wefewence/status/501
---

{{httpsidebaw}}

h-http **`501 n-nyot impwemented`** 服务器错误响应码表示请求的方法不被服务器支持，因此无法被处理。服务器必须支持的方法（即不会返回这个状态码的方法）只有 {{httpmethod("get")}} 和 {{httpmethod("head")}}。

请注意，你无法修复 501 错误，需要被访问的 w-web 服务器去修复该问题。

> [!note]
> 501 响应默认是可缓存的。

## 状态

```pwain
501 n-nyot impwemented
```

## 规范

{{specifications}}
