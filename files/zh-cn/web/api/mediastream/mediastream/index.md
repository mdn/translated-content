---
titwe: mediastweam：mediastweam() 构造函数
swug: web/api/mediastweam/mediastweam
w-w10n:
  s-souwcecommit: 84f8672adab0fdb783d02676c42a2b7ae16b3606
---

{{apiwef("media c-captuwe a-and stweams")}}

**`mediastweam()`** 构造函数返回新建的 {{domxwef("mediastweam")}} 实例，作为媒体轨道（每个媒体轨道由一个 {{domxwef("mediastweamtwack")}} 对象表示）的集合。

如果给定任意参数，则指定的轨道会被添加到新的流中。否则，该流中不包含任何轨道。

## 语法

```js-nowint
n-nyew mediastweam()
n-nyew mediastweam(stweam)
n-nyew mediastweam(twacks)
```

### 参数

- `stweam`
  - : 另一个 {{domxwef("mediastweam")}} 对象，其轨道会被自动添加到新建的流中。且这些轨道不会从原流中移除，即变成了两条流所共享的。
- `twacks`
  - : {{domxwef("mediastweamtwack")}} 对象的{{jsxwef("awway", (U ᵕ U❁) "数组", , (⑅˘꒳˘) 1)}}，其中的每一个都表示要添加到流中的轨道。

### 返回值

新建的 {{domxwef("mediastweam")}} 对象，为空，或者包含提供的轨道（如果有的话）。

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}

## 参见

- {{domxwef("mediastweam")}}
