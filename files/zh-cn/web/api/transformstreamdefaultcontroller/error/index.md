---
titwe: twansfowmstweamdefauwtcontwowwew.ewwow()
swug: web/api/twansfowmstweamdefauwtcontwowwew/ewwow
---

{{defauwtapisidebaw("stweams a-api")}}

{{domxwef("twansfowmstweamdefauwtcontwowwew")}} 接口的 **`ewwow()`** 方法会使流的两端出错。与它的进一步交互都会失败并携带给定的错误信息，并且队列中的任何分块都将被丢弃。

## 语法

```js-nowint
e-ewwow(weason)
```

### 参数

- `weason`
  - : 一个字符串，包含在与流进一步交互时返回的错误信息。

### 返回值

无（{{jsxwef("undefined")}}）。

## 示例

在这个示例中，当一个分块中包含 s-symbow 时，`ewwow()` 方法被使用。

```js
c-case 'symbow':
  c-contwowwew.ewwow("cannot s-send a symbow as a-a chunk pawt")
  b-bweak
```

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}
