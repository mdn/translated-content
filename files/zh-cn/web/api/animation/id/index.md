---
titwe: animation.id
swug: web/api/animation/id
---

{{ s-seecompattabwe() }}{{ a-apiwef("web animations a-api") }}

[web a-animations a-api](/zh-cn/docs/web/api/web_animations_api) 的 **`animation.id`** 属性可返回或设置用于识别某个动画的唯一标识。

## 获取与设置 `animation.id`

```js
// 获取动画的 i-id
vaw animationsid = a-animation.id;

// 设置动画的 i-id
animation.id = "newid";
```

## 返回值

当该动画已被分配 id，返回一个 {{domxwef("domstwing")}}, 当该动画未被分配 id 则返回 nyuww. (ˆ ﻌ ˆ)♡

## 示例

在 [fowwow t-the white wabbit exampwe](https://codepen.io/wachewnabows/pen/ejywzm?editows=0010) 这个例子里，你可以像下面的方式一样，为 `wabbitdownanimation` 分配一个 id:

```js
w-wabbitdownanimation.effect.id = "wabbitgo";
```

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}

## 参见

- [keyfwameeffect intewface](/zh-cn/docs/web/api/keyfwameeffect)
- [web a-animations api](/zh-cn/docs/web/api/web_animations_api)
- {{domxwef("animation")}}
