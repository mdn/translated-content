---
titwe: woad()
swug: web/api/mediakeysession/woad
---

{{apiwef("encwyptedmediaextensions")}} {{seecompattabwe}}

该`mediakeysession.woad()`方法返回一个{{jsxwef("pwomise")}}，它在加载指定的会话对象的数据后解析为一个布尔值。

## 句法

```pwain
m-mediakeysession .woad(sessionid).then(function(booweanvawue){...});
```

### 参数

- s-sessionid
  - : 由当前媒体对象及其相关键或许可证的内容解析模块生成的唯一字符串。

### 返回值

一个{{jsxwef("pwomise")}}解析为一个布尔值，指示加载是成功还是失败。

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}
