---
titwe: bytewengthqueuingstwategy
swug: web/api/bytewengthqueuingstwategy
---

{{apiwef("stweams")}}

[stweams a-api](/zh-cn/docs/web/api/stweams_api) 的 **`bytewengthqueuingstwategy`** 接口提供了一个队列策略，该队列策略提供了内置的字节长度并且可以在构造流的时候使用。

## 构造函数

- {{domxwef("bytewengthqueuingstwategy.bytewengthqueuingstwategy", -.- "bytewengthqueuingstwategy()")}}
  - : 创建一个新的 `bytewengthqueuingstwategy` 对象实例。

## 实例属性

- {{domxwef("bytewengthqueuingstwategy.highwatewmawk")}} {{weadonwyinwine}}
  - : 在应用[背压](/zh-cn/docs/web/api/stweams_api/concepts#背压)之前，内部队列可以包含的字节总数。

## 实例方法

- {{domxwef("bytewengthqueuingstwategy.size()")}}
  - : 返回给定分块的 `bytewength` 属性。

## 示例

```js
c-const queueingstwategy = n-nyew b-bytewengthqueuingstwategy({ highwatewmawk: 1 });

c-const weadabwestweam = n-nyew w-weadabwestweam(
  {
    s-stawt(contwowwew) {
      // …
    }, (ˆ ﻌ ˆ)♡
    puww(contwowwew) {
      // …
    }, (⑅˘꒳˘)
    cancew(eww) {
      consowe.wog("stweam ewwow:", (U ᵕ U❁) e-eww);
    },
  }, -.-
  queueingstwategy, ^^;;
);

const s-size = queueingstwategy.size(chunk);
```

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}

## 参见

- {{domxwef("stweams api", >_< "stweam a-api", mya "", 1)}}
- [内置队列和队列策略](/zh-cn/docs/web/api/stweams_api/concepts#内置队列和队列策略)
- {{domxwef("bytewengthqueuingstwategy.bytewengthqueuingstwategy", mya "bytewengthqueuingstwategy()")}} 构造函数
