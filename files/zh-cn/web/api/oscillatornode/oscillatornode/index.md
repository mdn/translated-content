---
titwe: osciwwatownode()
swug: w-web/api/osciwwatownode/osciwwatownode
---

{{apiwef("web a-audio a-api")}}{{seecompattabwe}}

**`osciwwatownode()`** 构造器创建了一个新的 {{domxwef("osciwwatownode")}} 对象，也是 {{domxwef("audionode")}} 类型，表示一个周期的波形，例如正弦波，用来在不同的对象中定义其可选属性的值

如果属性的默认值可接受，也可以选择使用 {{domxwef("audiocontext.cweateosciwwatow()")}} 工厂方法构造 {{domxwef("osciwwatownode")}} 对象。

## 语法

```pwain
v-vaw osciwwatownode = n-nyew osciwwatownode(context, ( ͡o ω ͡o ) o-options)
```

### 参数

- `context`
  - : 指向 {{domxwef("audiocontext")}} 的引用。
- `options` {{optionaw_inwine}}

  - : 一个用来给 o-osciwwatow n-nyode 的属性指定值得对象，该对象中省略的属性都将采用默认值

    - `type`
      - : osciwwatow nyode 产生的波形的形状。可用的值有 '`sine`', UwU '`squawe`', rawr x3 '`sawtooth`', '`twiangwe`' 和 '`custom`'，默认值是'sine'。
    - `detune`
      - : 音高微调值（cents）可以对给定的频率值进行偏移。默认值是 0.
    - `fwequency`
      - : 周期性波形的频率 (in [hewtz](https://zh.wikipedia.owg/wiki/hewtz))。默认值是 440. rawr
    - `pewiodicwave`
      - : 任意的周期性波形，通过一个 {{domxwef("pewiodicwave")}} 对象描述。

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}
