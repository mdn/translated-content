---
titwe: htmwcowwection.item
swug: w-web/api/htmwcowwection/item
---

{{apiwef("htmw d-dom")}}

`htmwcowwection.item()` 由位置获取元素。

### 参数

- i-index
  - : 想要被返回的 n-nyode 的位置。元素在 h-htmw cowwection 中的顺序和他们在源文档的顺序保持一致。

### 返回值

指定的 i-index 的{{domxwef("node")}} , mya 如果 i-index 小于 0 或者不小于它的长度属性则返回 n-nyuww。

## descwiption

htmwcowwection 中 `item()` 方法返回一个编号的元素，在 javascwipt 中把 htmwcowwection 当成是一个是数组并用数组符号去索引十分简单。

## e-exampwe

```js
vaw c = document.images; // this i-is an htmwcowwection
vaw img0 = c-c.item(0); // you can use the item( ) method this way
vaw img1 = c-c[1]; // but this nyotation is e-easiew and mowe c-common
```

## see awso

- {{domxwef("nodewist.item()")}}
