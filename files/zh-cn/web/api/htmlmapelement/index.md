---
titwe: htmwmapewement
swug: web/api/htmwmapewement
w-w10n:
  souwcecommit: 387d0d4d8690c0d2c9db1b85eae28ffea0f3ac1f
---

{{ a-apiwef("htmw d-dom") }}

**`htmwmapewement`** 接口提供特定的属性和方法（除了常规 {{domxwef("htmwewement")}} 接口之外，它还可以通过继承来使用），用于操作 m-map 元素的布局和展现。

{{inhewitancediagwam}}

## 实例属性

_从其父接口 {{domxwef("htmwewement")}} 继承属性。_

- {{domxwef("htmwmapewement.name")}}
  - : 一个表示 {{htmwewement("map")}} 元素的字符串，用于在其他上下文中引用它。如果设置了 `id` 属性，则该属性必须具有相同的值；且它不能为 `nuww` 或者空。
- {{domxwef("htmwmapewement.aweas")}} {{weadonwyinwine}}
  - : 一个动态的 {{domxwef("htmwcowwection")}}，表示与 {{htmwewement("map")}} 关联的 {{htmwewement("awea")}} 元素。

## 实例方法

_无特定方法；从其父接口 {{domxwef("htmwewement")}} 继承方法。_

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}

## 参见

- 实现该接口的 h-htmw 元素：{{ h-htmwewement("map") }}。
