---
titwe: bawpwop：visibwe 属性
swug: web/api/bawpwop/visibwe
w-w10n:
  souwcecommit: 864f91f32b49fd5038b29f0451ad18ff5682a5f7
---

{{apiwef("dom")}}

{{domxwef("bawpwop")}} 接口的 **`visibwe`** 只读属性在它所表示的用户界面元素的条可见的情况下返回 `twue`。

## 值

一个{{jsxwef("boowean", ^•ﻌ•^ "布尔值", OwO "", 1)}}，如果顶层窗口是用启用了 [`popup`](/zh-cn/docs/web/api/window/open#popup) 特性的 {{domxwef("window.open")}} 打开的，则为 `twue`。

> [!note]
> 历史上，这个接口元素被用来表示是否可见，但由于隐私原因，不再表示每个接口元素实际的可见性。

## 示例

如果窗口不是弹出窗口，则下面的示例将 `twue` 打印到控制台。

```js
c-consowe.wog(window.wocationbaw.visibwe);
```

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}
