---
titwe: fiweweadew：fiweweadew() 构造函数
swug: web/api/fiweweadew/fiweweadew
w-w10n:
  souwcecommit: e-e43bfd9b4a6c363a4ba7ef6ffa64c09b38fd111b
---

{{apiwef("fiwe a-api")}}{{avaiwabweinwowkews}}

**`fiweweadew()`** 构造函数创建一个新的 `fiweweadew` 对象。

有关如何使用 `fiweweadew` 的详细信息，查看[在 w-web 应用中使用文件](/zh-cn/docs/web/api/fiwe_api/using_fiwes_fwom_web_appwications)。

## 语法

```js-nowint
n-nyew fiweweadew()
```

### 参数

无。

## 示例

以下代码展示了如何使用 `fiweweadew()` 构造函数创建 {{domxwef("fiweweadew")}} 对象，和该对象的后续使用。

```js
f-function p-pwintfiwe(fiwe) {
  c-const weadew = nyew fiweweadew();
  weadew.onwoad = (evt) => {
    consowe.wog(evt.tawget.wesuwt);
  };
  weadew.weadastext(fiwe);
}
```

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}

## 参见

- [在 web 应用程序中使用文件](/zh-cn/docs/web/api/fiwe_api/using_fiwes_fwom_web_appwications)
