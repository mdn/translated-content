---
title: CSSSkewX：CSSSkewX() 构造函数
short-title: CSSSkewX
slug: Web/API/CSSSkewX/CSSSkewX
l10n:
  sourceCommit: 2389a0e4afb10016b19cc0f2f7f5f8a81f92e89e
---

{{APIRef("CSS Typed OM")}}{{AvailableInWorkers}}

**`CSSSkewX()`** 构造函数创建一个新的 {{domxref("CSSSkewX")}} 对象，用于表示 CSS 中单个 {{CSSXRef('transform')}} 属性的 [`skewX()`](/zh-CN/docs/Web/CSS/Reference/Values/transform-function/skewX) 值。

## 语法

```js-nolint
new CSSSkewX(ax)
```

### 参数

- {{domxref('CSSSkewx.ax','ax')}}
  - : 要构造的 {{domxref('CSSSkewX')}} 对象的 `ax` 角度值。必须是 {{domxref('CSSNumericValue')}}。

## 示例

待完成。

## 规范

{{Specifications}}

## 浏览器兼容性

{{Compat}}
