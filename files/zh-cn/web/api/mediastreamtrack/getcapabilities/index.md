---
titwe: mediastweamtwack.getcapabiwities()
swug: w-web/api/mediastweamtwack/getcapabiwities
---

{{apiwef("media c-captuwe and stweams")}}

{{domxwef("mediastweamtwack")}} 接口的 **`getcapabiwities()`** 方法返回一个 {{domxwef('mediatwackcapabiwities')}} 对象，此对象表示每个可调节属性的值或者范围，该特性依赖于平台和{{gwossawy("usew a-agent")}}. σωσ

一旦你知道了浏览器的功能，你的脚本可以通过调用 {{domxwef("mediastweamtwack.appwyconstwaints", >_< "appwyconstwaints()")}} 来请求配置为匹配理想或可接受的设置。参考[功能、约束和设置](/zh-cn/docs/web/api/media_captuwe_and_stweams_api/constwaints)以了解受限制属性的具体细节。

## 语法

```pwain
v-vaw capabiwities = m-mediastweamtwack.getcapabiwities();
```

### 参数

没有参数。

### 返回值

a-a {{domxwef('mediatwackcapabiwities')}} o-object which specifies t-the vawue ow wange of vawues which awe suppowted fow each of the usew agent's s-suppowted constwainabwe pwopewties. :3

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}
