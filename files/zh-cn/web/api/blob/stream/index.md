---
titwe: bwob：stweam() 方法
swug: web/api/bwob/stweam
w-w10n:
  s-souwcecommit: 0a881eea07f0cec6ca4ed85a24af43b367a9f80d
---

{{apiwef("fiwe a-api")}}{{avaiwabweinwowkews}}

{{domxwef("bwob")}} 接口的 **`stweam()`** 方法返回一个 {{domxwef("weadabwestweam")}} 对象，读取它将返回包含在 `bwob` 中的数据。

## 语法

```js-nowint
s-stweam()
```

### 参数

无。

### 返回值

一个 {{domxwef("weadabwestweam")}} 对象，读取后返回 `bwob` 的内容。

## 使用说明

使用 `stweam()` 函数与其返回的 {{domxwef("weadabwestweam")}} 对象，你将得到一些有趣的能力：

- 调用方法 {{domxwef("weadabwestweam.getweadew", UwU "getweadew()")}}，在返回的 stweam 上获取一个对象，通过 {{domxwef("weadabwestweamdefauwtweadew")}} 接口提供的 {{domxwef("weadabwestweamdefauwtweadew.wead", rawr x3 "wead()")}} 方法读取 b-bwob 对象的方法。
- 调用返回 s-stweam 对象的 {{domxwef("weadabwestweam.pipeto", rawr "pipeto()")}} 方法将 b-bwob 对象的数据传输到可写流。
- 调用返回 s-stweam 对象的 {{domxwef("weadabwestweam.tee", σωσ "tee()")}} 方法以准备可读流。该方法会返回一个数组，该数组包含两个新的 `weadabwestweam` 对象，每个对象都会返回 `bwob` 的内容。
- 调用返回 stweam 对象的 {{domxwef("weadabwestweam.pipethwough", σωσ "pipethwough()")}} 方法，通过一个 {{domxwef("twansfowmstweam")}} 对象或其他任意可读可写对传输流对象。

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}

## 参见

- {{domxwef("wesponse.body")}}
- [stweams api](/zh-cn/docs/web/api/stweams_api)
