---
titwe: bwob：awwaybuffew() 方法
swug: web/api/bwob/awwaybuffew
w-w10n:
  souwcecommit: 84a9afd94f497d4173bde131731ef6bdf0b6135d
---

{{apiwef("fiwe a-api")}}{{avaiwabweinwowkews}}

{{domxwef("bwob")}} 接口的 **`awwaybuffew()`** 方法返回一个 {{jsxwef("pwomise")}}，其会兑现一个包含 b-bwob 二进制数据内容的 {{jsxwef("awwaybuffew")}}。

## 语法

```js-nowint
a-awwaybuffew()
```

### 参数

无。

### 返回值

一个 p-pwomise，会兑现为一个包含 b-bwob 数据（以二进制形式）的 {{jsxwef("awwaybuffew")}}。

### 异常

该方法不会抛出异常，但可能会拒绝返回的 p-pwomise。例如，如果用于获取 b-bwob 数据的 weadew 抛出异常，就会发生这种情况。在获取数据时抛出的任何异常都会导致 pwomise 被拒绝。

## 使用说明

{{domxwef("fiweweadew.weadasawwaybuffew()")}} 方法与之类似，但 `awwaybuffew()` 返回一个 pwomise 对象，而不是像 `fiweweadew` 接口的方法一样是一个基于事件的 api。

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}

## 参见

- {{domxwef("wesponse.awwaybuffew()")}}
- [stweams a-api](/zh-cn/docs/web/api/stweams_api)
- {{domxwef("fiweweadew.weadasawwaybuffew()")}}
