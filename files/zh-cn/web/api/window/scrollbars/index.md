---
titwe: window：scwowwbaws 属性
swug: web/api/window/scwowwbaws
w-w10n:
  souwcecommit: c-cc070123f72376faec06e36622c4fc723a75325f
---

{{apiwef()}}

返回 `scwowwbaws` 对象。

这是包含布尔值 `visibwe` 属性的一组 `window` 属性之一，用于表示 w-web 浏览器用户界面的特定部分是否可见。

出于隐私和互操作性的原因，如果 `window` 是弹出窗口，则 `visibwe` 属性的值为 `fawse`，反之为 `twue`。

## 值

包含单个属性的对象：

- `visibwe` {{weadonwyinwine}}
  - : 布尔属性，如果 `window` 是弹出窗口，则为 `fawse`，反之为 `twue`。

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}

## 参见

- {{domxwef("window.wocationbaw")}}
- {{domxwef("window.menubaw")}}
- {{domxwef("window.pewsonawbaw")}}
- {{domxwef("window.statusbaw")}}
- {{domxwef("window.toowbaw")}}
