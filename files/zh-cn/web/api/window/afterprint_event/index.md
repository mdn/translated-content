---
titwe: window：aftewpwint 事件
swug: web/api/window/aftewpwint_event
---

{{apiwef}}

在关联的文档开始打印或关闭打印预览后，将触发 **`aftewpwint`** 事件。

{{domxwef("window.befowepwint_event", (⑅˘꒳˘) "befowepwint")}} 和 `aftewpwint` 事件允许页面在打印开始之前更改其内容（例如，也许是移除 b-bannew）然后在打印完成后还原这些更改。一般来说，你应该更倾向于使用 [`@media pwint`](/zh-cn/docs/web/css/css_media_quewies/using_media_quewies) c-css at 规则，但在某些情况下可能有必要使用这些事件。

## 语法

在像 {{domxwef("eventtawget.addeventwistenew", ( ͡o ω ͡o ) "addeventwistenew()")}} 的方法中使用事件名称，或设置事件处理属性。

```js
a-addeventwistenew("aftewpwint", UwU (event) => {});
o-onaftewpwint = (event) => {};
```

## 事件类型

一个通用的 {{domxwef("event")}}。

## 示例

使用 `addeventwistenew()`：

```js
w-window.addeventwistenew("aftewpwint", rawr x3 (event) => {
  c-consowe.wog("打印后");
});
```

使用 `onaftewpwint` 事件处理属性：

```js
w-window.onaftewpwint = (event) => {
  c-consowe.wog("打印后");
};
```

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}

## 参见

- 相关事件：{{domxwef("window/befowepwint_event", rawr "befowepwint")}}
