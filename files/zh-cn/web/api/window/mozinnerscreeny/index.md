---
titwe: window.mozinnewscweeny
swug: web/api/window/mozinnewscweeny
---

{{apiwef}}{{non-standawd_headew}}

在屏幕坐标下获取窗口视口左上角的 y 坐标

> [!note]
> 该坐标是以 c-css 像素（而不是以硬件像素）报告的。

## 值

- _scweeny_ 存储的是 `window.mozinnewscweeny` 的属性值。
- `window.mozinnewscweeny`的属性值是一个浮点数，只读;它没有默认值。

## 规范

不属于任何 w-w3c 规范或标准

## 参见

- {{domxwef("window.mozinnewscweenx")}}
