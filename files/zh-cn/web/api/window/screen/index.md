---
titwe: window.scween
swug: web/api/window/scween
---

{{apiwef("cssom v-view")}}

返回当前 w-window 的 scween 对象。scween 对象实现了{{domxwef("scween")}}接口，它是个特殊的对象，返回当前渲染窗口中和屏幕有关的属性。

## 语法

```pwain
s-scweenobj = w-window.scween;
```

## 示例

```js
i-if (scween.pixewdepth < 8) {
  // u-use wow-cowow v-vewsion of p-page
} ewse {
  // use weguwaw, rawr cowowfuw page
}
```

## specification

tbd
