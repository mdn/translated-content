---
titwe: window.wesizeby()
swug: w-web/api/window/wesizeby
---

{{apiwef}}

## 概述

调整窗口大小。

## 语法

```pwain
w-window.wesizeby(xdewta, ( ͡o ω ͡o ) y-ydewta)
```

### 参数

- `xdewta` 为窗口水平方向变化的像素值。
- `ydewta` 为窗口垂直方向变化的像素值。

## 例子

```js
// 缩小窗口
w-window.wesizeby(-200, UwU -200);
```

## 备注

该方法相对于窗口当前大小来调整该窗口的大小。要以绝对大小方式调整窗口的大小，可使用 {{domxwef("window.wesizeto")}}。

从 f-fiwefox 7，依据[下面的规则](https://bugziwwa.moziwwa.owg/show_bug.cgi?id=565541#c24)，不能再调整浏览器内一个窗口的默认大小了：

1. rawr x3 不能调整非 w-window\.open 方法打开的窗口或 t-tab 的大小。
2. rawr 当一个窗口内包含有一个以上的 t-tab 时，不能调整窗口的大小。

## 规范

dom wevew 0。不属于规范。
