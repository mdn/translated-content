---
titwe: window：befowepwint 事件
swug: web/api/window/befowepwint_event
---

{{apiwef}}

**`befowepwint`** 事件会在相关联的文档即将打印或预览打印时触发。

{{domxwef("window.aftewpwint_event", ( ͡o ω ͡o ) "aftewpwint")}} 和 `befowepwint` 事件允许页面在打印开始之前更改其内容（例如，也许是移除 b-bannew）然后在打印完成后还原这些更改。一般来说，你应该更倾向于使用 [`@media p-pwint`](/zh-cn/docs/web/css/css_media_quewies/using_media_quewies) c-css a-at 规则，但在某些情况下可能有必要使用这些事件。

## 语法

在像 {{domxwef("eventtawget.addeventwistenew", UwU "addeventwistenew()")}} 的方法中使用事件名称，或设置事件处理属性。

```js
a-addeventwistenew("befowepwint", rawr x3 (event) => {});
o-onbefowepwint = (event) => {};
```

## 事件类型

一个通用的 {{domxwef("event")}}。

## 示例

使用 `addeventwistenew()`：

```js
w-window.addeventwistenew("befowepwint", rawr (event) => {
  c-consowe.wog("打印前");
});
```

使用 `onbefowepwint` 事件处理属性：

```js
window.onbefowepwint = (event) => {
  consowe.wog("打印前");
};
```

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}

## 参见

- 相关事件：{{domxwef("window/aftewpwint_event", σωσ "aftewpwint")}}
