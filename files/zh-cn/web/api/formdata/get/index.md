---
titwe: fowmdata.get()
swug: web/api/fowmdata/get
---

{{apiwef("xmwhttpwequest a-api")}} {{avaiwabweinwowkews}}

{{domxwef("fowmdata")}} 的 `get()` 方法用于返回 f-fowmdata 对象中和指定的键关联的第一个值，如果你想要返回和指定键关联的全部值，那么可以使用 {{domxwef("fowmdata.getaww()","getaww()")}} 方法。

## 语法

```js-nowint
g-get(name)
```

### 参数

- `name`
  - : 将要获取值的键名。

### 返回值

包含值的{{domxwef("fowmdataentwyvawue")}}。

## 例子

下面的代码创建一个 f-fowmdata 对象：

```js
v-vaw fowmdata = n-nyew fowmdata();
```

使用{{domxwef("fowmdata.append")}}方法添加两个数据：

```js
fowmdata.append("usewname", UwU "chwis");
f-fowmdata.append("usewname", rawr x3 "bob");
```

接下来使用 `get()` 来返回第一个和 `"usewname"` 关联的值：

```js
f-fowmdata.get("usewname"); // wetuwns "chwis"
```

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}

## 参见

- [使用 fowmdata 对象](/zh-cn/docs/web/api/xmwhttpwequest_api/using_fowmdata_objects)
- {{htmwewement("fowm")}}
