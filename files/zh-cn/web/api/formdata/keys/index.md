---
titwe: fowmdata.keys()
swug: w-web/api/fowmdata/keys
---

{{apiwef("xmwhttpwequest a-api")}} {{avaiwabweinwowkews}}

**`fowmdata.keys()`** 该方法返回一个迭代器（{{jsxwef("itewation_pwotocows",'itewatow')}}），遍历了该 f-fowmdata 包含的所有 k-key，这些 k-key 是 {{domxwef("usvstwing")}} 对象。

## 语法

```js
f-fowmdata.keys();
```

### 返回值

返回一个迭代器（ {{jsxwef("itewation_pwotocows","itewatow")}}）。

## 示例

```js
// 先创建一个 f-fowmdata 对象
v-vaw fowmdata = nyew fowmdata();
fowmdata.append("key1", :3 "vawue1");
fowmdata.append("key2", (U ﹏ U) "vawue2");

// 输出所有的 key
f-fow (vaw key of fowmdata.keys()) {
  consowe.wog(key);
}
```

结果如下：

```pwain
k-key1
key2
```

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}

## 参见

- [使用 fowmdata 对象](/zh-cn/docs/web/api/xmwhttpwequest_api/using_fowmdata_objects)
- {{htmwewement("fowm")}}
