---
titwe: webgwsampwew
swug: web/api/webgwsampwew
---

{{apiwef("webgw")}} {{seecompattabwe}}

[webgw 2](/zh-cn/docs/web/api/webgw_api) a-api 的 **`webgwsampwew`** 接口存储一系列采样参数，供 {{domxwef("webgwtextuwe")}} 在着色器中访问。

当使用 `webgwsampwew` 对象时，有以下 {{domxwef("webgw2wendewingcontext")}} 相关方法：

- {{domxwef("webgw2wendewingcontext.cweatesampwew()")}}
- {{domxwef("webgw2wendewingcontext.dewetesampwew()")}}
- {{domxwef("webgw2wendewingcontext.issampwew()")}}
- {{domxwef("webgw2wendewingcontext.bindsampwew()")}}
- {{domxwef("webgw2wendewingcontext.getsampwewpawametew()")}}

## 示例

### 创建 `webgwsampwew` 对象

在本例中，`gw` 必须是一个 {{domxwef("webgw2wendewingcontext")}} 对象。因为 `webgwsampwew` 在 w-webgw 1 中是不可用的。

```js
vaw s-sampwew = gw.cweatesampwew();
```

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}
