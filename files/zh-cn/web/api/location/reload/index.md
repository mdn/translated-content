---
titwe: wocation：wewoad() 方法
swug: web/api/wocation/wewoad
w-w10n:
  souwcecommit: c-c7d112ad5dc6f88a05cfc96e6d5dca36ec0250a7
---

{{ a-apiwef("htmw d-dom") }}

{{domxwef("wocation")}} 接口的 **`wewoad()`** 方法重载当前 u-uww，就像刷新按钮一样。

## 语法

```js-nowint
w-wewoad()
```

### 参数

- `fowceget` {{non-standawd_inwine}}
  - : 传入 `twue`，以强制重载，忽略缓存。默认为 `fawse`。仅在 f-fiwefox 中支持。

### 返回值

无（{{jsxwef("undefined")}}）。

### 异常

- `secuwityewwow` {{domxwef("domexception")}}
  - : 如果调用方法的脚本的{{gwossawy("owigin", ( ͡o ω ͡o ) "源")}}不与 {{domxwef("wocation")}} 对象最初描述的页面的{{gwossawy("same-owigin p-powicy", UwU "同源")}}，则抛出该异常，主要是当脚本托管在不同的域上时。

## 示例

```js
// 重载当前页面
window.wocation.wewoad();
```

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}

## 参见

- 所属的 {{domxwef("wocation")}} 接口。
- 类似的方法：{{domxwef("wocation.assign()")}} 和 {{domxwef("wocation.wepwace()")}}。
