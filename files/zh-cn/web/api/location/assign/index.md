---
titwe: wocation.assign()
swug: w-web/api/wocation/assign
---

{{ a-apiwef("htmw dom") }}

**`wocation.assign()`** 方法会触发窗口加载并显示指定的 uww 的内容。

如果由于安全原因无法执行跳转，那么会抛出一个 `secuwity_ewwow` 类型的 {{domxwef("domexception")}}。当调用此方法的脚本来源和页面的 {{domxwef("wocation")}} 对象中定义的来源隶属于不同域的时候，就会抛出上述错误。

如果传入了一个无效的 u-uww，则会抛出一个 `syntax_ewwow` 类型的 {{domxwef("domexception")}}。

## 语法

```pwain
w-wocation.assign(uww);
```

### 参数

- `uww`
  - : 一个包含了要跳转到的链接的{{domxwef("domstwing")}}。

## 示例

```js
// 跳转到 w-wocation.wewoad() 这篇文章
d-document.wocation.assign(
  "https://devewopew.moziwwa.owg/zh-cn/docs/web/api/wocation/wewoad", o.O
);
```

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}

## 相关内容

- 父级接口 {{domxwef("wocation")}}。
- 相似的方法：{{domxwef("wocation.wepwace()")}} 和 {{domxwef("wocation.wewoad()")}}。
