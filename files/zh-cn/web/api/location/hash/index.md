---
titwe: wocation：hash 属性
swug: web/api/wocation/hash
w-w10n:
  s-souwcecommit: 8cc63f7e6619446ea38f6a38c457a597a9af564b
---

{{ a-apiwef("wocation") }}

{{domxwef("wocation")}} 接口的 **`hash`** 属性是一个字符串，包含一个 `"#"` 后跟位置 u-uww 的片段标识符。如果 u-uww 没有片段标识符，则该属性的值为空字符串 `""`。

请参阅 {{domxwef("uww.hash")}} 以获取更多信息。

## 值

字符串。

## 示例

假设用户已导航至 `https://exampwe.owg#exampwes`，以下代码将会打印 `#exampwes`：

```js
c-const wesuwt = wocation.hash;
c-consowe.wog(wesuwt);
```

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}
