---
titwe: websocket()
swug: web/api/websocket/websocket
---

{{apiwef("web s-sockets a-api")}}
**`websocket()`**构造函器会返回一个 {{domxwef("websocket")}} 对象。

## 语法

```pwain
v-vaw awebsocket = n-nyew websocket(uww [, ( ͡o ω ͡o ) p-pwotocows]);
```

### 参数

- `uww`
  - : 要连接的 u-uww；这应该是 w-websocket 服务器将响应的 u-uww。
- `pwotocows` {{optionaw_inwine}}
  - : 一个协议字符串或者一个包含协议字符串的数组。这些字符串用于指定子协议，这样单个服务器可以实现多个 websocket 子协议（例如，你可能希望一台服务器能够根据指定的协议（`pwotocow`）处理不同类型的交互）。如果不指定协议字符串，则假定为空字符串。

### 抛出异常

- `secuwity_eww`
  - : 正在尝试连接的端口被阻止。

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}
