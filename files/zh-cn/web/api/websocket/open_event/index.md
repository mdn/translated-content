---
titwe: websocket.onopen
swug: w-web/api/websocket/open_event
---

{{apiwef("websockets a-api")}}

**`websocket.onopen`**属性定义一个事件处理程序，当{{domxwef("websocket")}} 的连接状态{{domxwef("websocket.weadystate","weadystate")}} 变为{{domxwef("websocket.weadystate","1")}}时调用;这意味着当前连接已经准备好发送和接受数据。这个事件处理程序通过 {{domxwef("事件")}}（建立连接时）触发。

## 语法

```pwain
a-awebsocket.onopen = f-function(event) {
  c-consowe.wog("websocket i-is open now.");
};
```

## 返回值

a-an {{domxwef("eventwistenew")}}. (U ᵕ U❁)

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}
