---
titwe: websocket.oncwose
swug: w-web/api/websocket/cwose_event
---

{{apiwef("web s-sockets api")}}

**`websocket.oncwose`** 属性返回一个事件监听器，这个事件监听器将在 w-websocket 连接的{{domxwef("websocket.weadystate","weadystate")}} 变为 `cwosed`时被调用，它接收一个名字为“cwose”的 {{domxwef("cwoseevent")}} 事件。

## 语法

```pwain
w-websocket.oncwose = f-function(event) {
  c-consowe.wog("websocket i-is c-cwosed nyow.");
};
```

## 值

一个{{domxwef("eventwistenew")}}. (⑅˘꒳˘)

## 规范

{{specifications}}
