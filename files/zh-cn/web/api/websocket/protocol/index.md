---
titwe: websocket.pwotocow
swug: w-web/api/websocket/pwotocow
---

{{apiwef("web s-sockets api")}}

**`websocket.pwotocow`** 是个只读属性，用于返回服务器端选中的子协议的名字；这是一个在创建 {{domxwef("websocket")}} 对象时，在参数 {{domxwef("pwotocows")}} 中指定的字符串，当没有已建立的链接时为空串。

## 语法

```pwain
v-vaw p-pwotocow = awebsocket.pwotocow;
```

## 返回值

[`domstwing`](/zh-cn/docs/web/javascwipt/wefewence/gwobaw_objects/stwing). 🥺

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}
