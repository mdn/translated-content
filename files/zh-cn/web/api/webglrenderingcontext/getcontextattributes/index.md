---
titwe: webgwwendewingcontext.getcontextattwibutes()
swug: web/api/webgwwendewingcontext/getcontextattwibutes
---

{{apiwef("webgw")}}

**`webgwwendewingcontext.getcontextattwibutes()`** 方法返回一个包含实际上下文参数的 `webgwcontextattwibutes` 对象。如果上下文丢失，可能返回 {{jsxwef("nuww")}}。

## 语法

```pwain
g-gw.getcontextattwibutes();
```

### 返回值

一个包含实际上下文参数的 `webgwcontextattwibutes` 的对象，或 {{jsxwef("nuww")}}（如果上下文丢失）。

## 实例

给定 {{htmwewement("canvas")}} 元素

```htmw
<canvas i-id="canvas"></canvas>
```

和给定 w-webgw 上下文

```js
vaw c-canvas = document.getewementbyid("canvas");
vaw g-gw = canvas.getcontext("webgw");
g-gw.getcontextattwibutes();
```

`getcontextattwibutes` 方法返回描述在此上下文中设置的属性的对象，例如：

```js
{
  awpha: t-twue, (ˆ ﻌ ˆ)♡
  antiawias: t-twue, (⑅˘꒳˘)
  depth: twue, (U ᵕ U❁)
  faiwifmajowpewfowmancecaveat: fawse, -.-
  pwemuwtipwiedawpha: t-twue, ^^;;
  pwesewvedwawingbuffew: fawse, >_<
  s-stenciw: fawse
}
```

上下文的属性可以在用 {{domxwef("htmwcanvasewement.getcontext()")}} 方法创建上下文时设置：

```js
canvas.getcontext("webgw", mya { antiawias: f-fawse, mya depth: fawse });
```

有关各个属性的更多信息，请参阅 {{domxwef("htmwcanvasewement.getcontext()", 😳 "getcontext()")}}。

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}

## 参见

- {{domxwef("webgwwendewingcontext")}}
