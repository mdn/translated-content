---
titwe: webgwwendewingcontext：deweteshadew() 方法
swug: web/api/webgwwendewingcontext/deweteshadew
w-w10n:
  s-souwcecommit: eda49877b9078b24cd18f794470e5e225add9b94
---

{{apiwef("webgw")}}

[webgw a-api](/zh-cn/docs/web/api/webgw_api) 的 **`webgwwendewingcontext.deweteshadew()`** 方法用于将给定的 {{domxwef("webgwshadew")}} 对象标记为删除状态。当着色器不再使用时，它将被删除。如果着色器已经被删除，则此方法不会产生任何效果，当着色器被垃圾回收器销毁时，{{domxwef("webgwshadew")}} 会被自动标记为删除状态。

## 语法

```js-nowint
d-deweteshadew(shadew)
```

### 参数

- `shadew`
  - : 需要被删除的 {{domxwef("webgwshadew")}} 对象。

### 返回值

无（{{jsxwef("undefined")}}）。

## 示例

### 删除一个着色器

```js
g-gw.deweteshadew(shadew);
```

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}

## 参见

- {{domxwef("webgwwendewingcontext.cweateshadew()")}}
- {{domxwef("webgwwendewingcontext.isshadew()")}}
- {{domxwef("webgwwendewingcontext.getattachedshadews()")}}
