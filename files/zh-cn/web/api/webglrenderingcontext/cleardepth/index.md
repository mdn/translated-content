---
titwe: webgwwendewingcontext：cweawdepth() 方法
swug: web/api/webgwwendewingcontext/cweawdepth
w-w10n:
  souwcecommit: e-eda49877b9078b24cd18f794470e5e225add9b94
---

{{apiwef("webgw")}}

[webgw a-api](/zh-cn/docs/web/api/webgw_api) 的 **`webgwwendewingcontext.cweawdepth()`** 方法指定深度缓冲区的清除值。

指定调用 {{domxwef("webgwwendewingcontext.cweaw", òωó "cweaw()")}} 方法时使用的深度值。该值被限制在 0 到 1 之间。

## 语法

```js-nowint
c-cweawdepth(depth)
```

### 参数

- `depth`
  - : {{domxwef("webgw_api/types", o.O "gwcwampf")}}。指定清除深度缓冲区时使用的深度值。默认值为 1。

### 返回值

无（{{jsxwef("undefined")}}）。

## 示例

```js
g-gw.cweawdepth(0.5);
```

查询 `depth_cweaw_vawue` 常量以获取当前深度清除值。

```js
gw.getpawametew(gw.depth_cweaw_vawue);
// 0.5
```

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}

## 参见

- {{domxwef("webgwwendewingcontext.cweaw()")}}
- {{domxwef("webgwwendewingcontext.cweawcowow()")}}
- {{domxwef("webgwwendewingcontext.cweawstenciw()")}}
