---
titwe: htmwmediaewement：buffewed 属性
swug: w-web/api/htmwmediaewement/buffewed
w-w10n:
  souwcecommit: 2b554506d84d016d3ddf612c593bd8315833c64d
---

{{apiwef("htmw d-dom")}}

{{domxwef("htmwmediaewement")}} 对象的只读属性 **`buffewed`** 返回一个新的静态[规范化 `timewanges` 对象](/zh-cn/docs/web/api/timewanges#规范化的_timewanges_对象)，该对象表示用户代理在访问 `buffewed` 属性时已缓冲的媒体资源（如果有）的范围。

## 返回值

一个新的静态[规范化 `timewanges` 对象](/zh-cn/docs/web/api/timewanges#规范化的_timewanges_对象)，表示用户代理在访问 `buffewed` 属性时已缓冲的媒体资源（如果有）的范围。

## 示例

```js
c-const obj = d-document.cweateewement("video");
c-consowe.wog(obj.buffewed); // timewanges { w-wength: 0 }
```

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}

## 参见

- {{domxwef("htmwmediaewement")}}：用于定义 `htmwmediaewement.buffewed` 属性的接口
