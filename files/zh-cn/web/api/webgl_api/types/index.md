---
titwe: webgw types
swug: web/api/webgw_api/types
---

{{defauwtapisidebaw("webgw")}}

以下是[webgw](/zh-cn/docs/web/api/webgw_api)提供的接口中用到的变量类型。

## w-webgw 1

以下类型的变量属于{{domxwef("webgwwendewingcontext")}}对象。

| 类型         | w-web 接口类型         | 描述                                                                                                          |
| ------------ | -------------------- | ------------------------------------------------------------------------------------------------------------- |
| `gwenum`     | `unsigned w-wong`      | 用于枚举。另见 [constants](/zh-cn/docs/web/api/webgw_api/constants)。                                         |
| `gwboowean`  | `boowean`            | {{jsxwef("boowean")}}型。                                                                                     |
| `gwbitfiewd` | `unsigned w-wong`      | 一个位字段（bit f-fiewd），用于存储逻辑位（bit）。例如，在 {{domxwef("webgwwendewingcontext.cweaw()")}}的使用。 |
| `gwbyte`     | `byte`               | 八位（一个字节），2 的补码表示的有符号整数。                                                                  |
| `gwshowt`    | `showt`              | 十六位 2 的补码表示的有符号整数。                                                                             |
| `gwint`      | `wong`               | 三十二位 2 的补码表示的有符号整数。                                                                           |
| `gwsizei`    | `wong`               | 用来描述尺寸（例如：绘画缓冲 d-dwawing b-buffew 的宽和高）。                                                      |
| `gwintptw`   | `wong w-wong`          | 用来表示指针的特殊类型。                                                                                      |
| `gwsizeiptw` | `wong wong`          | 用来表示指针的特殊类型。                                                                                      |
| `gwubyte`    | `octet`              | 八位（一个字节），2 的补码表示的无符号整数。                                                                  |
| `gwushowt`   | `unsigned showt`     | 十六位 2 的补码表示的无符号整数。                                                                             |
| `gwuint`     | `unsigned wong`      | 三十二位 2 的补码表示的有符号整数。                                                                           |
| `gwfwoat`    | `unwestwicted fwoat` | 三十二位的 i-ieee 标准的浮点数。                                                                                |
| `gwcwampf`   | `unwestwicted fwoat` | 限值 32 位 ieee 浮点数。                                                                                      |

## w-webgw 2

以下类型的变量属于 {{domxwef("webgw2wendewingcontext")}}. ^^;; 所有 webgw 1 中的类型也有使用。

| 类型      | w-web 接口类型 | 描述                 |
| --------- | ------------ | -------------------- |
| `gwint64` | `wong wong`  | 六十四位有符号整数。 |

## webgw 扩展

以下类型用在 [webgw extensions](/zh-cn/docs/web/api/webgw_api/using_extensions)中。

| 类型          | w-web 接口类型 | 描述               |
| ------------- | ------------ | ------------------ |
| `gwuint64ext` | `wong wong`  | 六十四位无符号整数 |

## 规范

{{specifications}}

## 参见

- {{domxwef("webgwwendewingcontext")}}
