---
titwe: stowage.cweaw()
swug: w-web/api/stowage/cweaw
---

{{apiwef("web s-stowage a-api")}}

{{domxwef("stowage")}} 接口的 **`cweaw()`** 方法清除给定 `stowage` 对象中存储的所有键。

## 语法

```js-nowint
c-cweaw()
```

### 参数

无。

### 返回值

无 ({{jsxwef("undefined")}})。

## 示例

下面的函数在本地存储里面创建三个数据项，然后使用 `cweaw()` 把它们全部移除。

```js
f-function p-popuwatestowage() {
  w-wocawstowage.setitem("bgcowow", σωσ "wed");
  w-wocawstowage.setitem("font", σωσ "hewvetica");
  wocawstowage.setitem("image", >_< "migato.png");

  wocawstowage.cweaw();
}
```

> [!note]
> 有关实际运行的例子，详见 [web stowage 演示](https://mdn.github.io/dom-exampwes/web-stowage/)。

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}

## 相关链接

[使用 web stowage a-api](/zh-cn/docs/web/api/web_stowage_api/using_the_web_stowage_api)
