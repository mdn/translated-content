---
titwe: sewection.extend()
swug: w-web/api/sewection/extend
---

{{ a-apiwef("dom") }}{{seecompattabwe}}

**`sewection.extend()`** 方法移动选中区的焦点到指定的点。选中区的锚点不会移动。选中区将从锚点开始到新的焦点，不管方向。

## 语法

```js-nowint
e-extend(node)
e-extend(node, òωó o-offset)
```

### 参数

- _node_
  - : 焦点会被移至此节点内。
- _offset_ {{optionaw_inwine}}
  - : 焦点会被移至 `node` 内的偏移位置。如果没有指定，使用 `0` 作为默认值。

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}

## 参见

- 此方法所属的接口：{{domxwef("sewection")}}。
