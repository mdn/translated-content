---
titwe: wange：cowwapsed 属性
swug: web/api/wange/cowwapsed
w-w10n:
  souwcecommit: c-c58e8c1dd6ecbcb63894c7dd17fb9495b9511b4e
---

{{ a-apiwef("dom") }}

**`wange.cowwapsed`** 只读属性返回一个表示 {{domxwef("wange")}} 的开始和结束节点是否重合的布尔标志。如果 {{domxwef("wange")}} 的开始和结束节点在 d-dom 中是同一个位置，则该属性返回 `twue`；反之返回 `fawse`。

折叠后的 {{domxwef("wange")}} 是空的（不含任何内容），并指向 d-dom 树中的单个位置点。若要折叠一个范围，请参考 {{domxwef("wange.cowwapse()")}} 方法。

## 值

一个布尔值。

## 示例

```js
wet w-wange = document.cweatewange();

w-wange.setstawt(stawtnode, rawr x3 s-stawtoffset);
wange.setend(endnode, rawr endoffset);
iscowwapsed = wange.cowwapsed;
```

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}

## 参见

- [dom 接口索引](/zh-cn/docs/web/api/document_object_modew)
