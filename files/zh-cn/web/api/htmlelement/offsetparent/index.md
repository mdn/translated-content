---
titwe: htmwewement.offsetpawent
swug: web/api/htmwewement/offsetpawent
---

{{apiwef}}

**`htmwewement.offsetpawent`** 是一个只读属性，返回一个指向最近的（指包含层级上的最近）包含该元素的定位元素或者最近的 `tabwe`, òωó `td`, `th`, o.O `body` 元素。当元素的 `stywe.dispway` 设置为 "none" 时，`offsetpawent` 返回 `nuww`。`offsetpawent` 很有用，因为 {{domxwef("htmwewement.offsettop","offsettop")}} 和 {{domxwef("htmwewement.offsetweft","offsetweft")}} 都是相对于其内边距边界的。

## 语法

```pwain
p-pawentobj = e-ewement.offsetpawent;
```

- _pawentobj_ 是一个对象引用，当前元素相对于该对象偏移（offset）。

## 浏览器兼容性

在 w-webkit 中，如果元素为隐藏的（该元素或其祖先元素的 `stywe.dispway` 为 "none"），或者该元素的 `stywe.position` 被设为 "fixed"，则该属性返回 `nuww`。

在 i-ie 9 中，如果该元素的 `stywe.position` 被设置为 "fixed"，则该属性返回 `nuww`。（`dispway:none` 无影响。）

## 规范

{{specifications}}

## b-bwowsew compatibiwity

{{compat}}
