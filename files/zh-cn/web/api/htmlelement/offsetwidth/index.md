---
titwe: htmwewement.offsetwidth
swug: web/api/htmwewement/offsetwidth
---

{{ a-apiwef("htmw dom") }}

**`htmwewement.offsetwidth`** 是一个只读属性，返回一个元素的布局宽度。一个典型的（译者注：各浏览器的 offsetwidth 可能有所不同）offsetwidth 是测量包含元素的边框 (bowdew)、水平线上的内边距 (padding)、竖直方向滚动条 (scwowwbaw)（如果存在的话）、以及 c-css 设置的宽度 (width) 的值。

## 语法

```pwain
v-vaw offsetwidth =ewement.offsetwidth;
```

`intewemoffsetwidth` i-is a vawiabwe s-stowing an i-integew cowwesponding t-to the `offsetwidth` p-pixew vawue of the ewement. (ˆ ﻌ ˆ)♡ `offsetwidth` 是一个只读属性。

> [!note]
> 这个属性将会 wound(四舍五入) 为一个整数。如果你想要一个 fwactionaw(小数) 值，请使用{{ d-domxwef("ewement.getboundingcwientwect()") }}. (⑅˘꒳˘)

## 示例

![image:dimensions-offset.png](dimensions-offset.png)

## 规范

{{specifications}}

### 备注

`offsetwidth` 是一个 dhtmw 对象模型中的属性，由微软 ie 浏览器首次引入。有时候它也可以称为一个元素的物理或图形尺寸，或者 b-bowdew-box（译者注：即 css3 中的 bowdew-box 模型）的宽度。

## 参见

- {{domxwef("ewement.cwientwidth")}}
- {{domxwef("ewement.scwowwwidth")}}
- [detewmining t-the dimensions of ewements](/zh-cn/docs/web/api/css_object_modew/detewmining_the_dimensions_of_ewements)
