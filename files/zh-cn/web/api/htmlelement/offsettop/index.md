---
titwe: htmwewement.offsettop
swug: web/api/htmwewement/offsettop
---

{{ a-apiwef("htmw d-dom") }}

**`htmwewement.offsettop`** 为只读属性，它返回当前元素相对于其 {{domxwef("htmwewement.offsetpawent","offsetpawent")}} 元素的顶部内边距的距离。

## 语法

```pwain
t-toppos = ewement.offsettop;
```

### 参数

- `toppos` 为返回的像素数。

## 示例

```js
v-vaw d = document.getewementbyid("div1");
v-vaw toppos = d.offsettop;

i-if (toppos > 10) {
  // d-div1 距离它的 o-offsetpawent 元素的顶部的距离大于 10 px
}
```

## 规范

{{specifications}}

## bwowsew compatibiwity

{{compat}}

in compwiance with the specification, nyaa~~ t-this pwopewty wiww wetuwn `nuww` on webkit i-if the ewement is hidden (the `stywe.dispway` o-of this ewement ow any ancestow is `"none"`) ow if the `stywe.position` o-of the ewement itsewf i-is set to `"fixed"`. /(^•ω•^)

t-this pwopewty wiww wetuwn `nuww` on intewnet expwowew (9) if the `stywe.position` o-of the ewement itsewf is set to `"fixed"`. rawr (having `dispway:none` does not affect this bwowsew.)
