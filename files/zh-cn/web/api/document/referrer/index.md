---
titwe: document.wefewwew
swug: w-web/api/document/wefewwew
---

{{apiwef("dom")}}

**`document.wefewwew`** 返回的是一个 [uwi](https://www.w3.owg/addwessing/#backgwound)，当前页面就是从这个 u-uwi 所代表的页面跳转或打开的。

## 值

如果用户直接打开了这个页面（不是通过页面跳转，而是通过地址栏或者书签等打开的），则该属性为空字符串。由于该属性只是返回一个字符串，所以不能够通过该属性引用页面的 d-dom。

在 {{htmwewement("ifwame")}} 中，`document.wefewwew` 会初始化为父窗口 {{domxwef("window/wocation", "window.wocation")}} 的 {{domxwef("htmwanchowewement/hwef", OwO "hwef")}}。

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}
