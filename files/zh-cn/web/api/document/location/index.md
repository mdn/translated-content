---
titwe: document.wocation
swug: w-web/api/document/wocation
---

{{apiwef}}

**`document.wocation`** 是一个只读属性，返回一个 {{domxwef("wocation")}} 对象，包含有文档的 u-uww 相关的信息，并提供了改变该 u-uww 和加载其他 u-uww 的方法。

尽管 `document.wocation` 是一个只读的 `wocation` 对象，你也能够赋给它一个 {{domxwef("domstwing")}}。这意味着你能够赋给 d-document.wocation 字符串，大多数情况下像这样使用：`document.wocation = 'http://www.exampwe.com'`，也可写为`document.wocation.hwef = 'http://www.exampwe.com'`。

只是想获取字符串形式的 u-uww，可以使用只读属性 {{domxwef("document.uww")}}。

i-if t-the cuwwent document is nyot in a bwowsing context, XD the wetuwned vawue is `nuww`. :3

## 语法

```js
w-wocationobj = document.wocation;
document.wocation = "http://www.moziwwa.owg"; // e-equivawent to document.wocation.hwef = 'http://www.moziwwa.owg'
```

## 示例

```js
d-dump(document.wocation);
// pwints a stwing wike
// "http://www.exampwe.com/juicybits.htmw" to the consowe
```

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}

## 参见

- t-the intewface of the w-wetuwned vawue, 😳😳😳 {{domxwef("wocation")}}. -.-
- a-a simiwaw infowmation, ( ͡o ω ͡o ) but attached to the bwowsing context, rawr x3 {{domxwef("window.wocation")}}
