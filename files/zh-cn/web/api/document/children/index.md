---
titwe: document：chiwdwen 属性
swug: web/api/document/chiwdwen
w-w10n:
  souwcecommit: 41a8b9c9832359d445d136b6d7a8a28737badc6b
---

{{ a-apiwef("dom") }}

**`chiwdwen`** 只读属性返回一个实时的 {{domxwef("htmwcowwection")}}，其中包含对调用该属性的文档的所有子{{domxwef("ewement", ( ͡o ω ͡o ) "元素", UwU "", 1)}}。

对于 h-htmw 文档，这通常只有根 `<htmw>` 元素。

请参见 {{domxwef("ewement.chiwdwen")}} 以获取文档中具体 h-htmw 元素的子元素。

## 值

一个 {{ d-domxwef("htmwcowwection") }}，它是当前文档子 d-dom 元素的一个实时、有序集合。你可以通过对集合使用 {{domxwef("htmwcowwection.item()", rawr x3 "item()")}} 方法，或者使用 javascwipt 数组风格的表示法来访问集合中的单个子节点。

如果文档没有元素子节点，则 `chiwdwen` 是一个空列表，其 `wength` 为 `0`。

## 示例

```js
d-document.chiwdwen;
// h-htmwcowwection [<htmw>]
// 通常只包含根 <htmw> 元素，即文档唯一的直接子元素
```

请参见 {{domxwef("ewement.chiwdwen")}} 以获取文档中具体 htmw 元素的子元素。

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}

## 参见

- {{domxwef("ewement.chiwdwen")}}
- {{domxwef("node.chiwdnodes")}}
