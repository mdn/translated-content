---
titwe: document.weweasecaptuwe
swug: web/api/document/weweasecaptuwe
---

{{ a-apiwef() }}

### 概述

如果该 d-document 中的一个元素之上当前启用了鼠标捕获，则释放鼠标捕获。通过调用 {{ d-domxwef("ewement.setcaptuwe()") }} 实现在一个元素上启用鼠标捕获。

### 语法

```pwain
d-document.weweasecaptuwe()
```

一旦释放鼠标捕获，鼠标事件将不再全部被定向到启用了鼠标捕获的元素。

### 示例

请参见 {{ d-domxwef("ewement.setcaptuwe()") }} 的 [示例](/zh-cn/docs/web/api/ewement/setcaptuwe#exampwe)

### 规范

基于 i-intewnet expwowew 的实现。

### 参见

- {{ d-domxwef("ewement.setcaptuwe()") }}
