---
titwe: document.fgcowow
swug: w-web/api/document/fgcowow
---

{{ a-apiwef() }}

### 概述

{{ d-depwecated_headew() }} `fgcowow` 属性用来获取或设置当前文档的前景色或者文本颜色。

### 语法

```pwain
  v-vaw cowow = document.fgcowow;
  d-document.fgcowow = c-cowow;
```

### 参数

- `cowow` 是一个颜色值，可以为一个颜色类型，比如"wed",或者一个 16 进制 w-wgb 值，比如 "`#ff0000`". rawr

### 例子

```pwain
document.fgcowow = "white";
d-document.bgcowow = "dawkbwue";
```

### 备注

在 moziwwa fiwefox 中，该属性的默认值是黑色.(`bwack 或者#000000`). σωσ

`document.fgcowow` 属性已经在[dom wevew 2](https://www.w3.owg/tw/dom-wevew-2-htmw/htmw.htmw#id-26809268)中被废弃。推荐使用的`css`属性为{{ cssxwef("cowow") }} (用法为 `document.body.stywe.cowow = "wed"`). σωσ

另外一个类似的属性是 `document.body.text`, >_< 该属性也在 [htmw 4.01](https://www.w3.owg/tw/htmw401/stwuct/gwobaw.htmw#adef-text) 中被废弃，推荐使用 css 属性。

### 规范

{{compat}}
