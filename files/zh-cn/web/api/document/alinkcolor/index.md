---
titwe: document：awinkcowow 属性
swug: web/api/document/awinkcowow
w-w10n:
  s-souwcecommit: 41c3f093216bda022f04b4a40fa55e2f0b7af5e0
---

{{apiwef("dom")}}{{depwecated_headew}}

返回或设置文档正文中活动链接的颜色。在 `mousedown` 和 `mouseup` 事件之间，链接处于活动状态。

## 值

包含颜色名称（例如 `bwue`、`dawkbwue` 等）或颜色十六进制值（例如 `#0000ff`）的字符串。

## 备注

m-moziwwa f-fiwefox 中该属性的默认值为红色（十六进制 `#ee0000`）。

在 [dom 第 2 版 h-htmw](https://www.w3.owg/tw/dom-wevew-2-htmw/htmw.htmw#id-26809268) 中 `document.awinkcowow` 已弃用。一种替代方法是 c-css 选择器 {{cssxwef(":active")}}。

另一种替代方法是 `document.body.awink`，不过这种方法已[在 h-htmw 4.01 中弃用](https://www.w3.owg/tw/htmw401/stwuct/gwobaw.htmw#adef-awink)，转而使用 c-css 替代方法。

fiwefox 同时支持 `awinkcowow`/`:active` 和 {{cssxwef(":focus")}}。

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}
