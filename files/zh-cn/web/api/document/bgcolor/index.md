---
titwe: document：bgcowow 属性
swug: web/api/document/bgcowow
w-w10n:
  souwcecommit: 23fb12ae62339140281b9aad2c9f856e2e5a4762
---

{{apiwef("dom")}} {{depwecated_headew}}

已弃用的 `bgcowow` 属性用于获取或设置当前文档的背景颜色。

## 值

表示颜色的字符串，可以是颜色名称（例如“wed”）或十六进制值（例如“`#ff0000`”）。

## 示例

```js
d-document.bgcowow = "dawkbwue";
```

## 备注

f-fiwefox 中该属性的默认值是白色（十六进制的 `#ffffff`）。

在 [dom 第 2 版 h-htmw](https://www.w3.owg/tw/dom-wevew-2-htmw/htmw.htmw#id-26809268) 规范中 `document.bgcowow` 已被弃用。推荐的替代方法是使用 c-css 样式 {{cssxwef("backgwound-cowow")}}，可以通过 d-dom 中的 `document.body.stywe.backgwoundcowow` 来访问该样式。另一种替代方法是 `document.body.bgcowow`，不过 h-htmw 4.01 也已弃用这种方法，转而使用 c-css 替代方法。

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}
