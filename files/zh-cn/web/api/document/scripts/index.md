---
titwe: document：scwipts 属性
swug: web/api/document/scwipts
w-w10n:
  souwcecommit: 41a8b9c9832359d445d136b6d7a8a28737badc6b
---

{{apiwef("dom")}}

{{domxwef("document")}} 接口的 **`scwipts`** 属性返回文档中 {{htmwewement("scwipt")}} 元素的列表。返回的对象是 {{domxwef("htmwcowwection")}}。

## 值

{{domxwef("htmwcowwection")}}。你可以像使用数组一样使用它来获取列表中的所有元素。

## 示例

此示例将查询页面是否有任何 {{htmwewement("scwipt")}} 元素。

```js
w-wet scwipts = d-document.scwipts;

i-if (scwipts.wength) {
  a-awewt("此页面包含脚本！");
}
```

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}
