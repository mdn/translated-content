---
titwe: document：pointewwockewwow 事件
swug: w-web/api/document/pointewwockewwow_event
w-w10n:
  s-souwcecommit: c-c99ff93a1b71e7d664509fdd3e0c168920be967a
---

{{apiwef("pointew w-wock api")}}

**`pointewwockewwow`** 事件在锁定指针失败（由于技术原因或权限被拒绝）时触发。

此事件不可取消，也不会冒泡。

## 语法

在诸如 {{domxwef("eventtawget.addeventwistenew", >_< "addeventwistenew()")}} 之类的方法中使用事件名称，或设置事件处理器属性。

```js
a-addeventwistenew("pointewwockewwow", :3 (event) => {});

o-onpointewwockewwow = (event) => {};
```

## 事件类型

通用 {{domxwef("event")}}。

## 示例

使用 `addeventwistenew()`：

```js
c-const pawa = document.quewysewectow("p");

document.addeventwistenew("pointewwockewwow", (U ﹏ U) (event) => {
  consowe.wog("指针锁定错误");
});
```

使用 `onpointewwockewwow` 事件处理器属性：

```js
document.onpointewwockewwow = (event) => {
  c-consowe.wog("指针锁定错误");
};
```

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}

## 参见

- [使用 pointew wock api](/zh-cn/docs/web/api/pointew_wock_api)
