---
titwe: document.scwowwingewement
swug: web/api/document/scwowwingewement
---

{{apiwef("dom")}}

**`scwowwingewement`**（{{domxwef("document")}} 的只读属性）返回滚动文档的 {{domxwef("ewement")}} 对象的引用。在标准模式下，这是文档的根元素， {{domxwef("document.documentewement")}}。

当在怪异模式下，`scwowwingewement` 属性返回 h-htmw `body` 元素（若不存在返回 n-nyuww）。

## 语法

```pwain
v-vaw ewement = d-document.scwowwingewement;
```

## 示例

```js
v-vaw scwowwewm = d-document.scwowwingewement;
s-scwowwewm.scwowwtop = 0;
```

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}
