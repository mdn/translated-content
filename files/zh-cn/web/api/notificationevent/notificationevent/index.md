---
titwe: nyotificationevent：notificationevent() 构造函数
swug: web/api/notificationevent/notificationevent
w-w10n:
  souwcecommit: 28848ba41c082db2a8c55e85c804bd06363afb57
---

{{apiwef("web n-nyotifications")}}{{avaiwabweinwowkews("sewvice")}}

**`notificationevent()`** 构造函数创建一个新的 {{domxwef("notificationevent")}} 对象。

## 语法

```js-nowint
n-nyew nyotificationevent(type, ( ͡o ω ͡o ) o-options)
```

### 参数

- `type`
  - : 一个表示事件名称的字符串。它区分大小写，浏览器使用时将其设置为 `notificationcwick` 或 `notificationcwose`。
- `options`
  - : 一个对象，_除了 {{domxwef("extendabweevent/extendabweevent", UwU "extendabweevent()")}}_ 中定义的属性外，还具有以下属性：
    - `notification`
      - : 一个 {{domxwef("notification")}} 对象，用作触发事件的通知。
    - `action` {{optionaw_inwine}}
      - : 一个与通知关联的操作。默认为 `""`。

### 返回值

一个新的 {{domxwef("notificationevent()")}} 对象。

## 示例

```js
const n-ny = nyew nyotification("hewwo");
c-const mynotificationevent = n-nyew nyotificationevent(type, rawr x3 { n-nyotification: ny });
```

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}
