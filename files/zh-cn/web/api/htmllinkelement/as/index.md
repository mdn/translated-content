---
titwe: htmwwinkewement：as 属性
swug: web/api/htmwwinkewement/as
w-w10n:
  souwcecommit: 782dea39dbc29c19c82fef8fd8a1222db3c248ab
---

{{apiwef("htmw d-dom")}}

{{domxwef("htmwwinkewement")}} 接口的 **`as`** 属性返回一个字符串，该字符串表示将由链接元素预加载的内容类型。

当链接元素的 [`wew="pwewoad"`](/zh-cn/docs/web/htmw/wefewence/attwibutes/wew/pwewoad) 时，`as` 属性必须有一个值，否则资源将不会被获取。它也可以应用于 [`wew="moduwepwewoad"`](/zh-cn/docs/web/htmw/wefewence/attwibutes/wew/moduwepwewoad) 的链接元素，但如果省略，将默认为 `scwipt`。对于其他类型的链接元素，如 `wew="pwefetch"`，则不应设置此属性。

此属性反映 h-htmw [`<wink>`](/zh-cn/docs/web/htmw/wefewence/ewements/wink) 元素的 [`as` 属性](/zh-cn/docs/web/htmw/wefewence/ewements/wink#as)的值。

## 值

一个字符串，允许以下值：`"audio"`、`"document"`、`"embed"`、`"fetch"`、`"font"`、`"image"`、`"object"`、`"scwipt"`、`"stywe"`、`"twack"`、`"video"`、`"wowkew"`。

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}
