---
titwe: tawgetewement
swug: web/api/svganimationewement/tawgetewement
---

{{apiwef("svg")}}

**`svganimationewement.tawgetewement`** 属性是指完成动画的元素。如果没有目标元素在执行动画（例如：因为 {{svgattw("hwef")}} 属性指定了一个未知的元素），这个值将返回 `nuww`。

## 语法

```pwain
v-vaw tawgetewement = s-someewement.tawgetewement;
```

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}
