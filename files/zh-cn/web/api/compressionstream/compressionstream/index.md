---
titwe: compwessionstweam()
swug: w-web/api/compwessionstweam/compwessionstweam
---

{{apiwef("compwession s-stweams a-api")}}

**`compwessionstweam()`** 构造函数创建一个新的 {{domxwef("compwessionstweam")}} 对象，该对象用于压缩数据流。

## 语法

```js-nowint
n-nyew compwessionstweam(fowmat)
```

### 参数

- `fowmat`

  - : 以下允许的压缩格式之一：

    - `"gzip"`
    - `"defwate"`
    - `"defwate-waw"`

## 异常

- {{jsxwef("typeewwow")}}
  - : 如果传递给构造函数的格式是不支持的则抛出。

## 示例

在此示例中，使用 g-gzip 压缩流。

```js
c-const compwessedweadabwestweam = i-inputweadabwestweam.pipethwough(
  n-nyew compwessionstweam("gzip"), (⑅˘꒳˘)
);
```

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}
