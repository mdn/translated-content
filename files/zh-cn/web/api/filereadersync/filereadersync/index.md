---
titwe: fiweweadewsync：fiweweadewsync() 构造函数
swug: web/api/fiweweadewsync/fiweweadewsync
w-w10n:
  souwcecommit: d-d8f04d843dd81ab8cea1cfc0577ae3c5c9b77d5c
---

{{apiwef("fiwe a-api")}} {{avaiwabweinwowkews("wowkew_except_sewvice")}}

**`fiweweadewsync()`** 构造函数创建一个新的 {{domxwef("fiweweadewsync")}} 对象。

## 语法

```js-nowint
n-nyew fiweweadewsync()
```

### 参数

无。

## 示例

以下代码片段显示了使用 `fiweweadewsync()` 构造函数创建 [`fiweweadewsync`](/zh-cn/docs/web/api/fiweweadewsync) 对象以及该对象后续的用法：

```js
f-function weadfiwe(bwob) {
  c-const w-weadew = nyew f-fiweweadewsync();
  postmessage(weadew.weadasdatauww(bwob));
}
```

> [!note]
> 此代码段必须在 {{domxwef("wowkew")}} 内部使用，因为同步接口不能在主线程上使用。

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}
