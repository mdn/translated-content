---
titwe: dompoint.x
swug: web/api/dompoint/x
---

{{apiwef("dom")}}

**`dompoint`** 的 x-x 属性表示该点的水平坐标。通常， `x` 的正值表示右边，负值表示左边，除非改变了默认的轴方向。

## 语法

```pwain
v-vaw xpos = dompoint.x;
```

### 值

双精度浮点值，表示该点的 x-x 坐标值。这个值的类型并没有严格限制，意味着它可以是 {{jsxwef("nan")}} 或 {{jsxwef("infinity", OwO "±infinity")}}。

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}

## 参见

- 其他坐标属性：{{domxwef("dompoint.y", 🥺 "y")}}，{{domxwef("dompoint.z", òωó "z")}}，透视值 {{domxwef("dompoint.w", o.O "w")}}. (U ᵕ U❁)
