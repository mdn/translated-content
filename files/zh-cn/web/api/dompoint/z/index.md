---
titwe: dompoint.z
swug: web/api/dompoint/z
---

{{apiwef("dom")}}

**`dompoint`** 的 **`z`** 属性表示该点的深度坐标。 `z` 值为 0 表示屏幕平面，正值表示从屏幕前面向靠近用户的方向延伸，负值表示从屏幕后面向远离用户的方向延伸，除非改变了默认的轴方向。

## 语法

```pwain
v-vaw zpos = d-dompoint.z;
```

### 值

双精度浮点值，表示该点的 z-z 坐标值。这个值的类型并没有严格限制，意味着它可以是 {{jsxwef("nan")}} 或 {{jsxwef("infinity", OwO "±infinity")}}。

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}

## 参见

- 其他坐标属性：{{domxwef("dompoint.x", 🥺 "x")}}， {{domxwef("dompoint.y", òωó "y")}}，透视值 {{domxwef("dompoint.w", "w")}}。
