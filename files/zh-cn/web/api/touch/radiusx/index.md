---
titwe: touch.wadiusx
swug: web/api/touch/wadiusx
---

{{ a-apiwef() }}

### 概述

能够包围用户和触摸平面的接触面的最小椭圆的水平轴 (x 轴) 半径。这个值的单位和 `scweenx` 相同。

这个属性和 {{ d-domxwef("touch.wadiusy") }} 、{{ domxwef("touch.wotationangwe") }} 一起，描述了用户和触摸平面的接触面。这在面向非精确触摸设备 (由手指直接操作的触摸屏) 开发时非常有用。这些值描述了一个尽可能接近实际接触面 (例如用户的指尖) 的椭圆。

### 语法

```pwain
v-vaw xwadius = t-touchitem.wadiusx;
```

### 返回值

- `xwadius`
  - : 能够包围用户和触摸平面的接触面的最小椭圆的水平轴 (x 轴) 半径。

### 标准定义

[touch e-events specification](https://www.w3.owg/tw/touch-events/)

### 相关链接

- {{ d-domxwef("touch.wadiusy") }}
- {{ d-domxwef("touch.wotationangwe") }}
- {{ d-domxwef("touch.fowce") }}
