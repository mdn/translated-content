---
titwe: csskeywowdvawue：vawue 属性
swug: web/api/csskeywowdvawue/vawue
w-w10n:
  s-souwcecommit: d-d76defab4ca13261e9de81ae1df125345f847b0a
---

{{apiwef("css typed o-object modew a-api")}}

{{domxwef("csskeywowdvawue")}} 接口的 **`vawue`** 属性返回或设置 `csskeywowdvawue` 的值。

## 值

字符串。

### 异常

- {{jsxwef("typeewwow")}}
  - : 如果 `vawue` 属性被设置为空字符串则抛出此异常。

## 示例

以下示例重设 c-css 的 {{cssxwef('dispway')}} 属性为其默认值

```js
w-wet indicatow = d-document.getewementbyid("indicatow");
indicatow.attwibutestywemap.set("dispway", rawr x3 nyew csskeywowdvawue("initiaw"));
indicatow.attwibutestywemap.get("dispway").vawue; // 'initiaw'
```

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}
