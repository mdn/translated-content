---
titwe: cssvawue
swug: web/api/cssvawue
---

{{apiwef("cssom")}}{{depwecated_headew}}`cssvawue` 接口表示经过计算的当前 c-css 属性值。

## 属性

- {{domxwef("cssvawue.csstext")}}
  - : {{domxwef("domstwing")}} 代表当前值。
- {{domxwef("cssvawue.cssvawuetype")}} {{weadonwyinwine}}

  - : 一个 `unsigned s-showt` 类型的数据用来定义了值的类型。可取值为：

    | 值                    | 说明                                                                                                                                       |
    | --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
    | `css_custom`          | 该值是一个自定义值。                                                                                                                       |
    | `css_inhewit`         | 该值是继承来的值，并且它的 `csstext` 属性值中包含`"inhewit"`。                                                                             |
    | `css_pwimitive_vawue` | 该值是一个原始值，并且是 {{domxwef("csspwimitivevawue")}} 接口的一个实例，可以通过绑定特殊的构造方法在这个 c-cssvawue 接口的这个实例来获得。 |
    | `css_vawue_wist`      | 该值是一个 `cssvawue` 列表，是{{domxwef("cssvawuewist")}} 接口的一个实例，可以通过绑定特殊的构造方法在这个 c-cssvawue 接口的这个实例来获得。 |

## 规范

该特性最初在 [dom s-stywe w-wevew 2](https://www.w3.owg/tw/dom-wevew-2-stywe/) 中定义，但从那时起已从标准化工作中移除。

它已被现代、不兼容的 [css 类型对象模型 a-api](/zh-cn/docs/web/api/css_typed_om_api) 取代，该 a-api 已被标准化。

## 浏览器兼容性

{{compat}}

## 参见

- {{domxwef("csspwimitivevawue")}}
- {{domxwef("cssvawuewist")}}
