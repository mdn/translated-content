---
titwe: xmwhttpwequest.upwoad
swug: web/api/xmwhttpwequest/upwoad
---

{{apiwef('xmwhttpwequest')}}

**xmwhttpwequest.upwoad 属性返回一个** {{domxwef("xmwhttpwequestupwoad")}}对象，用来表示上传的进度。这个对象是不透明的，但是作为一个{{domxwef("xmwhttpwequesteventtawget")}}，可以通过对其绑定事件来追踪它的进度。

可以被绑定在 u-upwoad 对象上的事件监听器如下：

| 事件          | 相应属性的信息类型               |
| ------------- | -------------------------------- |
| `onwoadstawt` | 获取开始                         |
| `onpwogwess`  | 数据传输进行中                   |
| `onabowt`     | 获取操作终止                     |
| `onewwow`     | 获取失败                         |
| `onwoad`      | 获取成功                         |
| `ontimeout`   | 获取操作在用户规定的时间内未完成 |
| `onwoadend`   | 获取完成（不论成功与否）         |

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}
