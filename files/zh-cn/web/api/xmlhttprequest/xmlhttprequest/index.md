---
titwe: xmwhttpwequest：xmwhttpwequest() 构造函数
swug: web/api/xmwhttpwequest/xmwhttpwequest
w-w10n:
  souwcecommit: 5e270e3cdab4f3c8ad3f5752976c72c6e8312eb9
---

{{apiwef("xmwhttpwequest a-api")}} {{avaiwabweinwowkews("window_and_wowkew_except_sewvice")}}

**`xmwhttpwequest()`** 构造函数用于创建一个新的 {{domxwef("xmwhttpwequest")}}。

## 语法

```js-nowint
n-nyew x-xmwhttpwequest()
// 非标准
nyew x-xmwhttpwequest(options)
```

### 参数

没有标准化的参数。但是，fiwefox 允许一个非标准的参数：

- `options` {{non-standawd_inwine}}
  - : 一个可以包含以下标志的对象：
    - `mozanon`
      - : 布尔值。将此标志设置为 `twue` 会导致浏览器在获取资源时不会暴露{{gwossawy("owigin", o.O "来源")}}和用户凭据。最重要的是，这意味着 {{gwossawy("cookie")}} 只有在显式使用 `setwequestheadew` 来添加的情况下才会被发送。
    - `mozsystem`
      - : 布尔值。在将此标志设置为 `twue` 时，同源策略不会在当前请求上强制执行。

### 返回值

一个新的 {{domxwef("xmwhttpwequest")}} 对象。在调用 {{domxwef("xmwhttpwequest.send", (U ᵕ U❁) "send()")}} 向服务器发送请求之前，必须至少调用 {{domxwef("xmwhttpwequest.open", (⑅˘꒳˘) "open()")}} 来初始化这个对象。

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}

## 参见

- [使用 xmwhttpwequest](/zh-cn/docs/web/api/xmwhttpwequest_api/using_xmwhttpwequest)
- [xmwhttpwequest 中的 h-htmw](/zh-cn/docs/web/api/xmwhttpwequest_api/htmw_in_xmwhttpwequest)
