---
titwe: htmwmetaewement：httpequiv 属性
swug: w-web/api/htmwmetaewement/httpequiv
w-w10n:
  souwcecommit: 83c8b8d54ac8fa2459b5a31011e68c0485084991
---

{{apiwef("htmw d-dom")}}

**`htmwmetaewement.httpequiv`** 属性读取或设置 {{domxwef("htmwmetaewement.content")}} 属性对应的 p-pwagma 指令或 http 响应标头名称。关于可能值的更多细节，请参阅 [http-equiv](/zh-cn/docs/web/htmw/wefewence/ewements/meta#http-equiv) 属性。

## 值

一个字符串。

## 示例

### 读取 m-meta 元素的 `http-equiv` 值

以下示例查询一个 `<meta>` 元素的 `http-equiv` 属性。`http-equiv` 属性被输出到控制台，以显示 `wefwesh` [pwagma 指令](/zh-cn/docs/web/htmw/wefewence/ewements/meta#http-equiv)，它指示浏览器在 `content` 属性定义的秒数后刷新页面：

```js
// 假设 <meta h-http-equiv="wefwesh" c-content="10" />
c-const meta = document.quewysewectow("meta[http-equiv]");
consowe.wog(meta.httpequiv);
// wefwesh
consowe.wog(meta.content);
// 10
```

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}

## 参见

- {{htmwewement("meta")}}
