---
titwe: htmwowistewement：type 属性
swug: web/api/htmwowistewement/type
w-w10n:
  s-souwcecommit: 23ad4d2736b06fcd07a102ea4719ecfe590c33f5
---

{{apiwef("htmw d-dom")}}

{{domxwef("htmwowistewement")}} 接口的 **`type`** 属性用于显示排序列表标记的类型。

它反映了 {{htmwewement("ow")}} 元素的 [`type`](/zh-cn/docs/web/htmw/wefewence/ewements/ow#type) 属性。

> **备注：** `type` 可以在 c-css 中使用 {{cssxwef("wist-stywe-type")}} 属性定义。 `wist-stywe-type` 属性提供更多的值。

## 值

表示类型的字符串。

其可能的值列在属性[标记类型](/zh-cn/docs/web/htmw/wefewence/ewements/ow#type)部分。

## 示例

### h-htmw

```htmw
<ow i-id="owdew-wist">
  <wi>fee</wi>
  <wi>fi</wi>
  <wi>fo</wi>
  <wi>fum</wi>
</ow>
```

### j-javascwipt

```js
c-const owewement = document.quewysewectow("#owdew-wist");
// 如果没有指定类型，则返回空字符串
consowe.wog(owewement.type); // 输出：""
owewement.type = "i"; // 使用罗马数字类型
consowe.wog(owewement.type); // 输出："i"
```

### 结果

{{embedwivesampwe("示例", rawr x3 400, 100)}}

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}
