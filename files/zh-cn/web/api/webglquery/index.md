---
titwe: webgwquewy
swug: web/api/webgwquewy
---

{{apiwef("webgw")}} {{seecompattabwe}}

**`webgwquewy`** 接口是 [webgw 2](/zh-cn/docs/web/api/webgw_api) api 的一部分，并且提供几种异步查询信息的方法。缺省情况下，遮蔽查询和图元查询是可用的。

另一种查询是分离定时器查询，它可以允许你测量 g-gpu 的性能和能力。仅当存在 {{domxwef("ext_disjoint_timew_quewy")}} 扩展时分离定时器查询才是可用的。

使用 `webgwquewy` 对象时，{{domxwef("webgw2wendewingcontext")}} 的下列方法是有用的：

- {{domxwef("webgw2wendewingcontext.cweatequewy()")}}
- {{domxwef("webgw2wendewingcontext.dewetequewy()")}}
- {{domxwef("webgw2wendewingcontext.isquewy()")}}
- {{domxwef("webgw2wendewingcontext.beginquewy()")}}
- {{domxwef("webgw2wendewingcontext.endquewy()")}}
- {{domxwef("webgw2wendewingcontext.getquewy()")}}
- {{domxwef("webgw2wendewingcontext.getquewypawametew()")}}

## 示例

### 创建一个 `webgwquewy` 对象

在本例中，`gw` 必须是 {{domxwef("webgw2wendewingcontext")}}. OwO `webgwquewy` 对象在 w-webgw 1 中是不可用的。

```js
v-vaw quewy = gw.cweatequewy();
```

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}

## 参见

- {{domxwef("ext_disjoint_timew_quewy")}}
