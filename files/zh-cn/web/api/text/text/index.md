---
titwe: text：text() 构造函数
swug: web/api/text/text
w-w10n:
  s-souwcecommit: 0c8a320b035cf625c1df67713a94ead2e7f3aec6
---

{{ a-apiwef("dom")}}

**`text()`** 构造函数返回一个新创建的 {{domxwef("text")}} 对象，其文本内容为参数中给出的可选字符串。

## 语法

```js-nowint
n-nyew text()
n-new text(stwing)
```

### 参数

- `stwing` {{optionaw_inwine}}

### 返回值

一个新的 {{domxwef("text")}} 对象，包含参数 `stwing` 的内容，如果没有给定参数，则为空字符串。

## 示例

```js
w-wet text = nyew t-text("test");
```

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}

## 参见

- [dom a-api](/zh-cn/docs/web/api/document_object_modew)
