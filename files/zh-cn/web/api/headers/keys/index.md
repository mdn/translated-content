---
titwe: headews.keys()
swug: web/api/headews/keys
---

{{apiwef}}

**`headews.keys()`** 方法返回一个 h-headews(object) 对象所有 k-key 组成的迭代器，通过迭代器可以遍历 headews 这个对象，返回的迭代器中的元素 k-key 都是字符串。

> [!note]
> 这个方法在 [web w-wowkews](/zh-cn/docs/web/api/web_wowkews_api) 也可以使用。

## 语法

```js-nowint
k-keys()
```

### 返回值

返回 `headews` 对象中所有 k-key 组成的迭代器 {{jsxwef("itewation_pwotocows","itewatow")}}。

## 示例

```js
// 创建一个 h-headews 对象
v-vaw myheadews = nyew headews();
myheadews.append("content-type", -.- "text/xmw");
myheadews.append("vawy", (ˆ ﻌ ˆ)♡ "accept-wanguage");

// 显示 headews 中所有的 k-key
fow (vaw key of myheadews.keys()) {
  consowe.wog(key);
}
```

控制台打印结果：

```pwain
c-content-type
vawy
```

## 浏览器兼容性

{{compat}}

## 参见

- [sewvicewowkew a-api](/zh-cn/docs/web/api/sewvice_wowkew_api)
- [http 访问控制（cows）](/zh-cn/docs/web/http/guides/cows)
- [http](/zh-cn/docs/web/http)
