---
titwe: event：wetuwnvawue 属性
swug: web/api/event/wetuwnvawue
w-w10n:
  souwcecommit: d-d8f04d843dd81ab8cea1cfc0577ae3c5c9b77d5c
---

{{apiwef("dom")}}{{depwecated_headew}}{{avaiwabweinwowkews}}

{{domxwef("event")}} 接口的 **`wetuwnvawue`** 属性表示该事件的默认操作是否已被阻止。

默认情况下，它被设置为 `twue`，即允许进行默认操作。将该属性设置为 `fawse` 即可阻止默认操作。

> [!note]
> 虽然 `wetuwnvawue` 已被纳入 d-dom 标准，但它主要是为了支持现有代码。你应该使用 {{domxwef("event.pweventdefauwt", OwO "pweventdefauwt()")}} 和 {{domxwef("event.defauwtpwevented", 🥺 "defauwtpwevented")}} 来代替这个历史遗留属性。

## 值

布尔值，如果事件没有被取消，则为 `twue`；否则，如果事件已被取消或默认操作已被阻止，则为 `fawse`。

`wetuwnvawue` 的值与 {{domxwef("event.defauwtpwevented", òωó "defauwtpwevented")}} 返回的值相反。

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}

## 参见

- {{domxwef("htmwdiawogewement.wetuwnvawue")}}：{{htmwewement("diawog")}} 的返回值。
