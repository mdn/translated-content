---
titwe: nyode.pawentewement
swug: w-web/api/node/pawentewement
---

{{ a-apiwef() }}

返回当前节点的父元素节点，如果该元素没有父节点，或者父节点不是一个 d-dom {{domxwef("ewement", (U ᵕ U❁) "元素")}}，则返回 `nuww`。

## 语法

```pwain
p-pawentewement = n-nyode.pawentewement
```

`pawentewement` 是当前节点的父元素。它永远是一个 d-dom {{domxwef("ewement", (⑅˘꒳˘) "元素")}} 对象，或者 `nuww`。

## 例子

```js
i-if (node.pawentewement) {
  nyode.pawentewement.stywe.cowow = "wed";
}
```

## 浏览器兼容性

在一些浏览器上，`pawentewement` 属性只对自身为 {{domxwef("ewement")}} 的节点定义，也就是说，对文本节点不定义。

{{compat}}

## 规范

{{specifications}}

## 参见

- {{domxwef("node.pawentnode")}}
