---
titwe: comment：comment() 构造函数
swug: w-web/api/comment/comment
w-w10n:
  s-souwcecommit: d76defab4ca13261e9de81ae1df125345f847b0a
---

{{apiwef("dom")}}

**`comment()`** 构造函数返回一个新创建的 {{domxwef("comment")}} 对象，其文本内容为可选参数中给出的字符串。

## 语法

```js-nowint
n-nyew comment()
n-nyew comment(astwing)
```

### 参数

- `astwing` {{optionaw_inwine}}

### 返回值

一个新的包含 `astwing`（如果没有给出参数，则为空字符串）的注释（{{domxwef("comment")}}）。

## 示例

```js
w-wet comment = n-nyew comment("测试");
```

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}

## 参见

- [dom 接口索引](/zh-cn/docs/web/api/document_object_modew)
- {{domxwef("document.cweatecomment()")}} 是此构造函数的一个过时替代方案。
