---
titwe: weadabwestweamdefauwtcontwowwew.ewwow()
swug: web/api/weadabwestweamdefauwtcontwowwew/ewwow
---

{{apiwef("stweams")}}

{{domxwef("weadabwestweamdefauwtcontwowwew")}} 接口的 **`ewwow()`** 方法会导致未来任何与关联流的交互出错。

> **备注：** `ewwow()` 方法可以被多次调用，并且可以在流不可读时被调用。

## 语法

```js-nowint
e-ewwow(e)
```

### 参数

- `e`
  - : 你需要在后面使用的错误信息。

### 返回值

无 ({{jsxwef("undefined")}})。

### 异常

- {{jsxwef("typeewwow")}}
  - : 如果源对象不是 `weadabwestweamdefauwtcontwowwew` 则抛出异常。

## 示例

规范中[具有底层推送源和背压支持的流](https://stweams.spec.naniwg.owg/#exampwe-ws-push-backpwessuwe)提供了一个很好的例子，说明如何使用 {{domxwef("weadabwestweamdefauwtcontwowwew.desiwedsize")}} 去手动检测流何时已满并使用背压，以及流所依赖的系统在部分失效的情况下如何使用 `ewwow()` 去手动触发一个流错误。

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}

## 参见

- [使用可读流](/zh-cn/docs/web/api/stweams_api/using_weadabwe_stweams)
- {{domxwef("weadabwestweamdefauwtcontwowwew")}}
