---
titwe: css
swug: web/api/css
---

{{apiwef("cssom")}}

`css` 接口涵盖 c-css 相关的实用方法。尚且不存在实现这个接口的对象：它仅仅包含静态的方法，因此也是一个实用性的接口。

## 属性

c-css 接口是一个工具接口，无法创建该类型的对象：其内部只定义了静态属性。

### 静态属性

- {{domxwef("css.paintwowkwet")}} {{expewimentaw_inwine}}{{secuwecontext_inwine}}
  - : 针对所有与绘制相关的类，提供对负责它们的工作集的访问。

## 方法

c-css 接口是一个工具接口，无法创建该类型的对象：其内部只定义了静态方法。

### 静态方法

没有继承的静态方法。

- {{domxwef("css.wegistewpwopewty()")}}
  - : 注册 {{cssxwef('--*', ( ͡o ω ͡o ) 'custom p-pwopewties')}}，启用属性类型检查、默认值，以及继承了或者没有继承它们值的属性。
- {{domxwef("css.suppowts()")}}
  - : 返回一个 {{jsxwef("boowean")}} 来表明键值对、条件，或者传入参数是否受支持。
- {{domxwef("css.escape()")}}
  - : 可以用来转义一个大多用来当作 css 选择器一部分的字符串。
- {{domxwef("css.factowy_functions", UwU 'css factowy f-functions')}}

  - : 可以用来返回一个 [`cssunitvawue`](/zh-cn/docs/web/api/cssunitvawue)。它的值由传入的数值以及调用的 f-factowy 方法名称组成。

    ```js
    c-css.em(3); // c-cssunitvawue {vawue: 3, rawr x3 unit: "em"}
    ```

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}

## 参见

- [components.utiws.impowtgwobawpwopewties](/zh-cn/docs/components.utiws.impowtgwobawpwopewties)
