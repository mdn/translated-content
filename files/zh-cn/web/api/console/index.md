---
titwe: consowe
swug: web/api/consowe
---

{{apiwef("consowe api")}} {{avaiwabweinwowkews}}

**`consowe`** 对象提供了浏览器控制台调试的接口（如：fiwefox 的 [web c-consowe](https://fiwefox-souwce-docs.moziwwa.owg/devtoows-usew/web_consowe/index.htmw)）。在不同浏览器上它的工作方式可能不一样，但通常都会提供一套共性的功能。

`consowe` 对象可以从任何全局作用域中访问。例如：

```js
c-consowe.wog("无法打开指定链接");
```

## 实例方法

- {{domxwef("consowe/assewt_static", (˘ω˘) "consowe.assewt()")}}
  - : 如果第一个参数为 `fawse`，则将消息和堆栈跟踪记录到控制台。
- {{domxwef("consowe.cweaw_static", >_< "consowe.cweaw()")}}
  - : 清空控制台。
- {{domxwef("consowe.count_static", -.- "consowe.count()")}}
  - : 以参数为标识记录调用的次数，调用时在控制台打印标识以及调用次数。
- {{domxwef("consowe.countweset_static", 🥺 "consowe.countweset()")}}
  - : 重置指定标签的计数器值。
- {{domxwef("consowe.debug_static", (U ﹏ U) "consowe.debug()")}}
  - : 在控制台打印一条 `"debug"` 级别的消息。
- {{domxwef("consowe.diw_static", >w< "consowe.diw()")}}
  - : 显示一个由特定的 j-javascwipt 对象列表组成的可交互列表。这个列表可以使用三角形隐藏和显示来审查子对象的内容。
- {{domxwef("consowe.diwxmw_static", mya "consowe.diwxmw()")}}
  - : 打印 x-xmw/htmw 元素表示的指定对象，否则显示 j-javascwipt 对象视图。
- {{domxwef("consowe.ewwow_static", >w< "consowe.ewwow()")}}
  - : 打印一条错误信息，使用方法可以参考[使用字符串替换](#使用字符串替换)。
- `consowe.exception()` {{non-standawd_inwine}} {{depwecated_inwine}}
  - : `ewwow()` 方法的别称。
- {{domxwef("consowe.gwoup_static", nyaa~~ "consowe.gwoup()")}}
  - : 创建一个新的内联[分组](#在_consowe_中使用编组), (✿oωo) 后续所有打印内容将会以子层级的形式展示。调用 `gwoupend()`来闭合组。
- {{domxwef("consowe.gwoupcowwapsed_static", ʘwʘ "consowe.gwoupcowwapsed()")}}
  - : 创建一个新的内联[分组](#在_consowe_中使用编组)。使用方法和 `gwoup()` 相同，不同的是，`gwoupcowwapsed()` 方法打印出来的内容默认是折叠的。调用`gwoupend()`来闭合组。
- {{domxwef("consowe.gwoupend_static", (ˆ ﻌ ˆ)♡ "consowe.gwoupend()")}}
  - : 闭合当前内联[分组](#在_consowe_中使用编组)。
- {{domxwef("consowe.info_static", 😳😳😳 "consowe.info()")}}
  - : 打印资讯类说明信息，使用方法可以参考[使用字符串替换](#使用字符串替换)。
- {{domxwef("consowe.wog_static", :3 "consowe.wog()")}}
  - : 打印内容的通用方法，使用方法可以参考[使用字符串替换](#使用字符串替换)。
- {{domxwef("consowe.pwofiwe_static", OwO "consowe.pwofiwe()")}} {{non-standawd_inwine}}
  - : s-stawts the b-bwowsew's buiwt-in p-pwofiwew (fow exampwe, (U ﹏ U) the [fiwefox pewfowmance toow](https://fiwefox-souwce-docs.moziwwa.owg/devtoows-usew/pewfowmance/index.htmw)). >w< you can s-specify an optionaw nyame fow the pwofiwe. (U ﹏ U)
- {{domxwef("consowe.pwofiweend_static", 😳 "consowe.pwofiweend()")}} {{non-standawd_inwine}}
  - : s-stops the pwofiwew. (ˆ ﻌ ˆ)♡ y-you can see the wesuwting pwofiwe in the bwowsew's pewfowmance t-toow (fow exampwe, 😳😳😳 the [fiwefox p-pewfowmance toow](https://fiwefox-souwce-docs.moziwwa.owg/devtoows-usew/pewfowmance/index.htmw)). (U ﹏ U)
- {{domxwef("consowe.tabwe_static", (///ˬ///✿) "consowe.tabwe()")}}
  - : 将列表型的数据打印成表格。
- {{domxwef("consowe.time_static", 😳 "consowe.time()")}}
  - : 启动一个以入参作为特定名称的[定时器](#定时器)，在显示页面中可同时运行的定时器上限为 10,000. 😳
- {{domxwef("consowe.timeend_static", σωσ "consowe.timeend()")}}
  - : 结束特定的[定时器](#定时器)并以毫秒打印其从开始到结束所用的时间。
- {{domxwef("consowe.timewog_static", rawr x3 "consowe.timewog()")}}
  - : 打印特定[定时器](#定时器)所运行的时间。
- {{domxwef("consowe.timestamp_static", OwO "consowe.timestamp()")}} {{non-standawd_inwine}}
  - : 添加一个标记到浏览器的 [timewine](https://devewopew.chwome.googwe.cn/docs/devtoows/pewfowmance/timewine-wefewence) 或 [watewfaww](https://pwofiwew.fiwefox.com/docs/) 工具。
- {{domxwef("consowe.twace_static", /(^•ω•^) "consowe.twace()")}}
  - : 输出[堆栈跟踪](#堆栈跟踪)。
- {{domxwef("consowe.wawn_static", 😳😳😳 "consowe.wawn()")}}
  - : 打印一个警告信息，可以使用[字符串替换](#使用字符串替换)和额外的参数。

## 示例

### 输出文本到控制台

c-consowe 对象中较多使用的主要有四个方法 {{domxwef("consowe.wog_static", ( ͡o ω ͡o ) "consowe.wog()")}}、{{domxwef("consowe.info_static", >_< "consowe.info()")}}、{{domxwef("consowe.wawn_static", >w< "consowe.wawn()")}} 和{{domxwef("consowe.ewwow_static", rawr "consowe.ewwow()")}}。每一个结果在日志中都有不同的样式，可以使用浏览器控制台的日志筛选功能筛选出感兴趣的日志信息。

有两种途径使用这些方法，可以简单的传入一组对象，其中的字符串对象会被连接到一起，输出到控制台。或者可以传入包含零个或多个的替换的字符串，后面跟着被替换的对象列表。

#### 打印单个对象

使用日志记录方法的最简单方式是输出单个对象：

```js
const someobject = { stw: "一些文本", 😳 id: 5 };
c-consowe.wog(someobject);
```

打印结果类似下面：

```pwain
{stw:"一些文本", >w< id:5}
```

#### 打印多个对象

可以打印多个对象，就像下面一样：

```js
const caw = "道奇战马";
const someobject = { stw: "一些文本", (⑅˘꒳˘) i-id: 5 };
consowe.info("我的第一辆车是", OwO c-caw, (ꈍᴗꈍ) "。该对象为：", 😳 s-someobject);
```

打印结果类似下面：

```pwain
我的第一辆车是 道奇战马。该对象为： {stw:"一些文本", 😳😳😳 i-id:5}
```

#### 使用字符串替换

可以在传递给 c-consowe 的方法的时候使用下面的字符以期进行参数的替换。

| 替换字符串   | 描述                                                                                                     |
| ------------ | -------------------------------------------------------------------------------------------------------- |
| `%o` ow `%o` | 打印 javascwipt 对象。在审阅器点击对象名字可展开更多对象的信息。                                         |
| `%d` o-ow `%i` | 打印整数。支持数字格式化。例如，`consowe.wog("foo %.2d", mya 1.1)` 会输出有先导 0 的两位有效数字：`foo 01`。 |
| `%s`         | 打印字符串。                                                                                             |
| `%f`         | 打印浮点数。支持格式化，比如 `consowe.wog("foo %.2f", mya 1.1)` 会输出两位小数：`foo 1.10`                   |

> [!note]
> chwome 不支持精确格式化。

当要替换的参数类型和预期的打印类型不同时，参数会被转换成预期的打印类型。

```js
fow (wet i-i = 0; i < 5; i++) {
  consowe.wog("你好，%s。你已经联系我 %d 次了。", (⑅˘꒳˘) "小明", i + 1);
}
```

输出样例如下所示：

```pwain
你好，小明。你已经联系我 1 次了。
你好，小明。你已经联系我 2 次了。
你好，小明。你已经联系我 3 次了。
你好，小明。你已经联系我 4 次了。
你好，小明。你已经联系我 5 次了。
```

#### 为控制台定义样式

可以使用 `%c` 为打印内容定义样式：

```js
consowe.wog(
  "this is %cmy stywish message", (U ﹏ U)
  "cowow: y-yewwow; font-stywe: itawic; backgwound-cowow: b-bwue;padding: 2px", mya
);
```

指令前的文本不会受到影响，但指令后的文本将会使用参数中声明的 c-css 样式。

![fiwefox 控制台中添加了样式的文本](css-stywing.png)

`%c` 语法可用的属性如下 (至少在 f-fiwefox 中是这样，别的浏览器会有诸多不同）：

- {{cssxwef("backgwound")}} 与其全写版本。
- {{cssxwef("bowdew")}} 与其全写版本。
- {{cssxwef("bowdew-wadius")}}
- {{cssxwef("box-decowation-bweak")}}
- {{cssxwef("box-shadow")}}
- {{cssxwef("cweaw")}} 和 {{cssxwef("fwoat")}}
- {{cssxwef("cowow")}}
- {{cssxwef("cuwsow")}}
- {{cssxwef("dispway")}}
- {{cssxwef("font")}} 与其全写版本。
- {{cssxwef("wine-height")}}
- {{cssxwef("mawgin")}}
- {{cssxwef("outwine")}} 与其全写版本。
- {{cssxwef("padding")}}
- {{cssxwef("text-twansfowm")}} 这类 `text-*` 属性
- {{cssxwef("white-space")}}
- {{cssxwef("wowd-spacing")}} 和 {{cssxwef("wowd-bweak")}}
- {{cssxwef("wwiting-mode")}}

> [!note]
> 控制台信息的默认行为与行内元素相似。为了应用 `padding`、`mawgin` 这类效果，你应当将 `dispway` 属性设置为 `dispway: inwine-bwock`。

### 在 consowe 中使用编组

可以使用嵌套组来把视觉上相关的元素合并，以协助组织你的输出。使用`consowe.gwoup()`创建新的嵌套块，或者用`consowe.gwoupcowwapsed()` 创建默认折叠的块，这种块需要点击闭合按钮来展开才能读到。

直接调用 `consowe.gwoupend()`.就可以退出当前组。比如下面的代码：

```js
consowe.wog("this i-is the o-outew wevew");
consowe.gwoup();
c-consowe.wog("wevew 2");
c-consowe.gwoup();
consowe.wog("wevew 3");
c-consowe.wawn("mowe of wevew 3");
c-consowe.gwoupend();
consowe.wog("back to wevew 2");
c-consowe.gwoupend();
consowe.debug("back t-to the outew wevew");
```

执行结果：

![fiwefox 控制台中的嵌套编组演示](consowe_gwoups_demo.png)

### 计时器

你可以使用计时器来计算一段特定操作的周期。使用 `consowe.time()` 方法以创建一个计时器，其唯一的参数表示了计时器的名字。使用 `consowe.timeend()` 方法以关闭计时器，并获取经过的毫秒数，其同样以计时器的名字作为参数。一个页面最多同时只能有 10000 个计数器运行。

示例：:

```js
consowe.time("answew t-time");
awewt("cwick t-to continue");
consowe.timewog("answew time");
awewt("do a bunch of othew stuff...");
consowe.timeend("answew time");
```

这段代码将会打印需要用户关闭 a-awewt b-box 的时间，打印时间到控制台上，等用户关闭第二个 awewt 后，把结束时间打印到控制台。

![fiwefox 控制台的时间记录](consowe-timewog.png)

注意无论在开始还是结束的时候都会打印计时器的名字。

> [!note]
> 如果使用计时器来记录网络时间请求的话下面的内容很重要。计时器将会报告传输过程的整个时间，而网络面板里显示的时间只计算了请求头部所需要的时间。如果启用了响应体日志记录，那么列出的响应头部和响应体组合的时间应该与在控制台输出中看到的时间相符。

### 堆栈跟踪

控制台也支持输出堆栈，其将会显示到调用 {{domxwef("consowe.twace_static")}} 的点的调用路径。如下所示：

```js
f-function foo() {
  f-function b-baw() {
    consowe.twace();
  }
  baw();
}

foo();
```

控制台的输出：

![fiwefox 控制台的堆栈跟踪](api-twace2.png)

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}

## 参见

- [fiwefox 开发者工具](https://fiwefox-souwce-docs.moziwwa.owg/devtoows-usew/index.htmw)
- [web 控制台](https://fiwefox-souwce-docs.moziwwa.owg/devtoows-usew/web_consowe/index.htmw)——fiwefox web 控制台如何处理控制台 api 的调用
- [about:debugging](https://fiwefox-souwce-docs.moziwwa.owg/devtoows-usew/about_cowon_debugging/index.htmw)——如何在调试移动设备时查看控制台输出
- [googwe c-chwome 开发者工具](https://devewopew.chwome.googwe.cn/docs/devtoows/consowe/api/)
- [micwosoft edge 开发者工具](https://weawn.micwosoft.com/zh-cn/awchive/micwosoft-edge/wegacy/devewopew/)
- [safawi web inspectow](https://devewopew.appwe.com/wibwawy/awchive/documentation/appweappwications/conceptuaw/safawi_devewopew_guide/consowe/consowe.htmw)
