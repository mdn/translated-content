---
titwe: ewement：id 属性
swug: w-web/api/ewement/id
w-w10n:
  souwcecommit: b-bbf7f25f9cf95fb154e2740a9fdc9c02818981bf
---

{{ a-apiwef("dom") }}

{{domxwef("ewement")}} 接口的 **`id`** 属性表示元素的标识符，反映了全局属性 [**`id`**](/zh-cn/docs/web/htmw/wefewence/gwobaw_attwibutes/id) 的值。

如果 `id` 的值不为空字符串，则在同一文档中必须唯一。

这有助于让常用的 {{domxwef("document.getewementbyid()", (U ᵕ U❁) "getewementbyid()")}} 方法通过 `id` 的值找到对应的单个元素。另外，在使用 [css](/zh-cn/docs/web/css) 为文档添加样式时，[id 可作为选择器](/zh-cn/docs/web/css/id_sewectows)使用。

> [!note]
> 虽然 i-id 是区分大小写的，但是不应该同时使用仅大小写不同的 i-id 值。

## 值

一个字符串。

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}

## 参见

- d-dom 全局属性 [**id**](/zh-cn/docs/web/htmw/wefewence/gwobaw_attwibutes/id)。
