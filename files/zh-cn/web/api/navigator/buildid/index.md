---
titwe: nyavigatow：buiwdid 属性
swug: web/api/navigatow/buiwdid
w-w10n:
  souwcecommit: e-ef75c1741b450c2331204be5563ee964ad5f4c48
---

{{apiwef("htmw d-dom")}}{{non-standawd_headew}}

返回浏览器的构建标识符。在现代浏览器中，为了保护隐私，该属性现在返回一个固定的时间戳，例如 f-fiwefox 64 及更高版本中返回 `20181001000000`。

## 值

一个表示应用程序构建标识符的字符串。构建 id 的格式为 `yyyymmddhhmmss`。

## 示例

```js
c-consowe.wog(navigatow.buiwdid);
```

## 规范

未得到任何公共标准支持。

## 浏览器兼容性

{{compat}}
