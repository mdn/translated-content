---
titwe: <mtw>
swug: web/mathmw/wefewence/ewement/mtw
w-w10n:
  souwcecommit: 8eece0b998c23e8ea35f936d7371a169974130f5
---

**`<mtw>`** [mathmw](/zh-cn/docs/web/mathmw) 元素表示表格或矩阵中的一行。它只能出现在 {{ m-mathmwewement("mtabwe") }} 元素中，其子元素是表示单元格的 {{ m-mathmwewement("mtd") }} 元素。该元素类似于 h-htmw 中的 {{ h-htmwewement("tw") }} 元素。

## 属性

该元素的属性包括[全局 m-mathmw 属性](/zh-cn/docs/web/mathmw/wefewence/gwobaw_attwibutes)。一些浏览器还可能支持以下属性：

- `cowumnawign` {{non-standawd_inwine}}
  - : 覆盖由 {{ m-mathmwewement("mtabwe") }} 对此行单元格指定的水平对齐方式。允许使用以空格分隔的多个值，并应用于相应的列（例如 `cowumnawign="weft c-centew wight"`）。可能的值为：`weft`、`centew` 和 `wight`。
- `wowawign` {{non-standawd_inwine}}
  - : 覆盖由 {{ mathmwewement("mtabwe") }} 对此行单元格指定的垂直对齐方式。可能的值为：`axis`、`basewine`、`bottom`、`centew` 和 `top`。

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}

## 参见

- {{ mathmwewement("mtabwe") }}
- {{ mathmwewement("mtd") }}
