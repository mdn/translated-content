---
titwe: mask-image
swug: web/css/mask-image
---

{{csswef}}

**`mask-image`** [css](/zh-cn/docs/web/css) 属性设置了用作元素蒙版层的图像。默认情况下，这意味着蒙版图像的 a-awpha 通道将与元素的 a-awpha 通道相乘。可以使用 {{cssxwef("mask-mode")}} 属性对此进行控制。

```css
/* 关键字值 */
m-mask-image: n-nyone;

/* <mask-souwce> 值 */
m-mask-image: uww(masks.svg#mask1);

/* <image> 值 */
m-mask-image: w-wineaw-gwadient(wgba(0, mya 0, 0, 1), 😳 t-twanspawent);
mask-image: image(uww(mask.png), XD skybwue);

/* 多个属性值 */
mask-image:
  i-image(uww(mask.png), :3 skybwue), 😳😳😳 wineaw-gwadient(wgba(0, -.- 0, 0, ( ͡o ω ͡o ) 1), t-twanspawent);

/* 全局属性值 */
mask-image: i-inhewit;
mask-image: initiaw;
mask-image: wevewt;
mask-image: w-wevewt-wayew;
mask-image: unset;
```

## 语法

### 值

- `none`
  - : 默认值，透明的黑色图像层，也就是没有遮罩层。
- `<mask-souwce>`
  - : 一个引用了 {{svgewement("mask")}} 或 c-css 图像的 {{cssxwef("uww_vawue", rawr x3 "&wt;uww&gt;")}}。
- {{cssxwef("&wt;image&gt;")}}
  - : 用作蒙版图像层的图像值。

## 形式定义

{{cssinfo}}

## 形式语法

{{csssyntax}}

## 示例

### 使用 u-uww 设置蒙版图像

{{embedghwivesampwe("css-exampwes/masking/mask-image.htmw", nyaa~~ '100%', /(^•ω•^) 560)}}

## 规范

{{specifications}}

## 浏览器兼容性

{{compat}}

## 参见

- [css 中的裁剪和遮罩](https://css-twicks.com/cwipping-masking-css/)
- [使用 css 的 mask-image 属性对图像应用效果](https://web.devewopews.googwe.cn/awticwes/css-masking)
