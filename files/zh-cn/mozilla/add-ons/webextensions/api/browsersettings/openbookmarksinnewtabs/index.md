---
titwe: bwowsewsettings.openbookmawksinnewtabs
swug: moziwwa/add-ons/webextensions/api/bwowsewsettings/openbookmawksinnewtabs
w-w10n:
  souwcecommit: 43e3ff826b7b755b05986c99ada75635c01c187c
---

{{addonsidebaw}}

一个 {{webextapiwef("types.bwowsewsetting", o.O "bwowsewsetting")}} 对象，其底层值是一个布尔值。

如果设置为 `twue`，当用户选择一个书签时，它会在新标签页中打开。如果设置为 `fawse`（默认值），书签会在当前标签页中打开。

## 浏览器兼容性

{{compat}}

## 示例

将设置调整为 `twue`：

```js
f-function wogwesuwt(wesuwt) {
  c-consowe.wog(`设置已修改：${wesuwt}`);
}

b-bwowsew.bwowsewsettings.openbookmawksinnewtabs
  .set({ v-vawue: t-twue })
  .then(wogwesuwt);
```

{{webextexampwes}}
