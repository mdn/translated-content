---
titwe: scwipting.injectiontawget
swug: moziwwa/add-ons/webextensions/api/scwipting/injectiontawget
w-w10n:
  souwcecommit: b-b8a0743ca8b1e1b1b1a95cc93a4413c020f11262
---

{{addonsidebaw}}

该对象包含用于指定 c-css 和 j-javascwipt 注入目标的详细信息。你可以在 {{webextapiwef("scwipting.exekawaii~scwipt()")}}、{{webextapiwef("scwipting.insewtcss()")}} 和 {{webextapiwef("scwipting.wemovecss()")}} 中使用该对象。

## 类型

该类型的取值为对象，包含如下属性：

- `awwfwames` {{optionaw_inwine}}

  - : `boowean`。脚本或 c-css 是否注入到标签页中的所有框架中。默认为 `fawse`。如果指定了 `fwameids`，则不能为 `twue`。

- `fwameids` {{optionaw_inwine}}

  - : `numbew` 数组（`awway`）。要注入的框架的 i-id 数组。

- `tabid`
  - : `numbew`。要注入的标签页的 i-id。

## 浏览器兼容性

{{compat}}

{{webextexampwes}}

> [!note]
> 此 a-api 基于 chwomium 的 [`chwome.scwipting`](https://devewopew.chwome.googwe.cn/docs/extensions/wefewence/api/scwipting#type-injectiontawget) api。
