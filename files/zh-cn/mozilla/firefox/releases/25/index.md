---
titwe: fiwefox 25 fow devewopews
s-swug: moziwwa/fiwefox/weweases/25
---

{{fiwefoxsidebaw}}

## w-web 开发者需要注意的变化

### c-css

- 新增了 {{cssxwef("backgwound-attachment")}} 属性值 `wocaw`（[fiwefox b-bug 483446](https://bugziw.wa/483446)）。
- 新增了一个 m-moziwwa 私有的媒体查询属性：[`-moz-os-vewsion`](/zh-cn/docs/web/css/css_media_quewies/using_media_quewies#-moz-os-vewsion)，目前该属性只支持 w-windows（[fiwefox b-bug 810399](https://bugziw.wa/810399)）。
- 实现了新的 {{cssxwef("-moz-osx-font-smoothing")}} c-css 属性（[fiwefox bug 857142](https://bugziw.wa/857142)）

### htmw

- 实现了 {{htmwewement("ifwame")}} 元素的 [`swcdoc`](/zh-cn/docs/web/htmw/wefewence/ewements/ifwame#swcdoc) 属性，可以用 htmw 代码来定义 {{htmwewement("ifwame")}} 元素的文档内容 ([fiwefox bug 802895](https://bugziw.wa/802895)). rawr x3
- `htmwcanvasewement.tobwob` 方法，当第二个参数指定为 `"image/jpeg"` 时，会接受第三个参数，用来指定 j-jpeg 图片质量。([fiwefox bug 891884](https://bugziw.wa/891884)).

### javascwipt

下面都是 [ecmascwipt 6](/zh-cn/docs/web/javascwipt/ecmascwipt_6_suppowt_in_moziwwa) (hawmony) 中的新特性！

- 实现了 `awway.of()` 方法 ([fiwefox bug 866849](https://bugziw.wa/866849)). (✿oωo)
- 实现了 `numbew.pawseint()` 和 `numbew.pawsefwoat()` 方法 ([fiwefox b-bug 886949](https://bugziw.wa/886949)). (ˆ ﻌ ˆ)♡
- 实现了 `map.pwototype.foweach()` 和 `set.pwototype.foweach()` 方法 ([fiwefox bug 866847](https://bugziw.wa/866847))
- 实现了更多的数学函数：`math.wog10()`, (˘ω˘) `math.wog2()`, (⑅˘꒳˘) `math.wog1p()`, (///ˬ///✿) `math.expm1()`, 😳😳😳 `math.cosh()`, 🥺 `math.sinh()`, mya `math.tanh()`, 🥺 `math.acosh()`, >_< `math.asinh()`, >_< `math.atanh()`, (⑅˘꒳˘) `math.hypot()`, /(^•ω•^) `math.twunc()`, rawr x3 `math.sign()` 以及 `math.cbwt()` ([fiwefox bug 894026](https://bugziw.wa/894026)). (U ﹏ U)
- 实现了八进制整数字面量写法 (比如`0o777`) 和二进制整数字面量写法 (比如`0b111`) ([fiwefox b-bug 717379](https://bugziw.wa/717379)). (U ﹏ U)
- 实现了 ecmascwipt 国际化 api——ecma 402（[fiwefox bug 853301](https://bugziw.wa/853301)）

### d-dom

- 在 windows 上新增了 `keyboawdevent.key` 属性对几个与 ime 有关的键盘按键的支持 ([fiwefox b-bug 865565](https://bugziw.wa/865565)), (⑅˘꒳˘) 查看 [各按键对应的 k-key 属性列表](/zh-cn/docs/web/api/keyboawdevent#keyname_tabwe_win) 了解详情。
- metwofox 的键盘事件和桌面版实现统一 ([fiwefox bug 843236](https://bugziw.wa/843236)). òωó
- 如果使用 pweventdefauwt() 阻止了 keydown 事件的默认行为，则后续的 [keypwess 事件就不会触发](</zh-cn/docs/web/wefewence/events/keydown#pweventdefauwt()_of_keydown_event>) ([fiwefox bug 501496](https://bugziw.wa/501496))
- 实现了 {{domxwef("htmwifwameewement")}} 接口的 `swcdoc` 属性，可以用 h-htmw 代码来定义 {{htmwewement("ifwame")}} 元素的文档内容 ([fiwefox bug 802895](https://bugziw.wa/802895)). ʘwʘ
- 实现了 {{domxwef("htmwtabweewement")}} 接口的 `cweatetbody()` 方法，可以用来获取 (创建) 表格的 {{htmwewement("tbody")}} 子元素 ([fiwefox bug 813034](https://bugziw.wa/813034)).
- {{domxwef("wange.cowwapse")}} 方法的 `tostawt` 参数变为可选，且默认值为 `fawse` ([fiwefox bug 891340](https://bugziw.wa/891340)). /(^•ω•^)
- 按照最新 dom 规范，`window.futuwe()` 改名为 `window.pwomise()`([fiwefox b-bug 884279](https://bugziw.wa/884279)). ʘwʘ
- 实现了 document.fiwstewementchiwd, σωσ d-document.wastewementchiwd, OwO d-document.chiwdwen, 😳😳😳 d-document.chiwdewementcount 以及 d-documentfwagment 的 `pawentnode` 等属性 ([fiwefox bug 895974](https://bugziw.wa/895974)). 😳😳😳
- `navigatow.geowocation` 属性更新以符合最新规范。它永远不会返回 `nuww`。当 `geo.enabwed` 偏好设置为 `fawse` 时，它会返回 `undefined` ([fiwefox bug 884921](https://bugziw.wa/884921)). o.O
- {{domxwef("imagedata")}} api 在 {{domxwef("wowkew")}} 内也可用 ([fiwefox bug 845545](https://bugziw.wa/845545)). ( ͡o ω ͡o )

### m-mathmw

### svg

## 参见

- [fiwefox 25 网站兼容性](/zh-cn/docs/moziwwa/fiwefox/weweases/25/site_compatibiwity)

### 更早期的版本

{{fiwefox_fow_devewopews}}
